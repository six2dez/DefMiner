---
phase: 07-sourcemap-reconstruction
plan: 17
type: execute
wave: 1
depends_on: []
gap_closure: true
nyquist_compliant: true
autonomous: true
requirements: [UI-05, MAP-04]
files_modified:
  - packages/frontend/src/sourcemap/tree.ts
  - packages/frontend/src/sourcemap/tree.spec.ts

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Two directory segments that differ only past the display truncation cap produce TWO nodes, not one — the display tree merges on a byte-identical verbatim segment and nothing weaker, which is the rule the module's own header states."
    - "A source file is never shown as a sibling of a file it is not a sibling of."
    - "The rendered label is still the sanitised, truncated one: nothing this plan does puts an untruncated or unsanitised segment into the DOM, into a `title` or into a `data-*` attribute."
    - "Every case in the 23-entry hostile label corpus still produces the answer it produces today, byte for byte."
    - "The `duplicate` marker and the climb accounting are unchanged."
  prohibitions:
    - "No Unicode normalisation and no case folding at the merge, ever. The module's step-5 comment refuses both by name and the reason is the same reason this finding exists: a weaker equality merges two genuinely distinct source files."
    - "No `node:path` and no path library. That import is banned on this path and the import list is asserted as an exact set."
    - "No change to `forCellText`, to `SOURCE_LINE_MAX_GRAPHEMES` or to any engine sanitiser. The bug is in what is COMPARED, not in what is displayed."
    - "No change to `packages/backend`. This plan is frontend-only, which is why it runs in wave 1 beside the store work."
    - "No change to `.planning/REQUIREMENTS.md` or to `07-UI-SPEC.md`."
  artifacts:
    - "packages/frontend/src/sourcemap/tree.ts — the directory merge keyed on the verbatim segment, with the display label carried separately"
  key_links:
    - "tree.ts `resolveClimbs` → the raw segment array → the directory merge key — the merge reads the value the store holds, not the value the screen shows"
    - "tree.ts `displaySegment` → `forCellText` → SourceTree.vue — the rendered label is unchanged and still sanitised"
---

<objective>
Make the display tree merge directories on a byte-identical VERBATIM segment, which is the rule the
module's own header spends a paragraph stating and which the code does not currently follow.

This closes **MD-02**. `tree.ts:493-511` finds an existing sibling directory by comparing the
post-`forCellText` label — sanitised AND truncated at 256 graphemes — so two directory segments that
differ only past character 256 become one node. The review executed it: a map declaring
`"A"x300 + "bank/secret.js"` and `"A"x300 + "evil/x.js"` produces ONE root directory with two children,
and the tree tells the operator that `secret.js` and `x.js` are siblings in one directory. They are
not; they are in two directories whose names differ.

Purpose. The module's header already refuses weaker-than-byte-identical merges, in its own words:
"directory nodes merge on a BYTE-IDENTICAL label and nothing weaker… Collapsing either pair merges TWO
GENUINELY DISTINCT SOURCE FILES into one node and loses the fact that the target shipped both." The
truncation is a display concern that leaked into an identity comparison. The `label truncated` note
does render, but it says the LABEL was cut — not that two directories were merged — and the `duplicate`
marker does not fire, because the two leaves have different labels.

Mitigating and worth stating: `sources_verbatim` is untouched at rest and the viewer header shows the
verbatim entry for whichever leaf is opened, so the evidence is recoverable. This is a display-integrity
defect, not data loss.

Output: a merge key that is the raw segment, a display label that is unchanged, and a case over the
executed reproduction.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sourcemap-reconstruction/07-CONTEXT.md
@.planning/phases/07-sourcemap-reconstruction/07-PATTERNS.md
@.planning/phases/07-sourcemap-reconstruction/07-REVIEW.md
@.planning/phases/07-sourcemap-reconstruction/07-UI-SPEC.md
@.planning/phases/07-sourcemap-reconstruction/07-07-SUMMARY.md
</context>

<gap_trace>
| Gap id | Source | What this plan closes |
|---|---|---|
| MD-02 | `07-REVIEW.md` MEDIUM, with an executed reproduction | The directory merge keys on the verbatim segment; two 300-character siblings stay two nodes. |
</gap_trace>

<artifacts_this_plan_produces>
**Changed internal type**
- `packages/frontend/src/sourcemap/tree.ts` — the `Building` node type gains a merge key carrying the
  raw segment, kept separate from the display `label`. It is internal to the module and is not part of
  the frozen `SourceTreeNode` the component renders, so no rendered shape changes.

**Changed comparison**
- The interior-directory `siblings.find` compares the raw segment. The ROOT merge already keys on the
  raw `rootLabel` and is not changed; confirm that by reading it and record the confirmation.

**No new file, no new export, no new dependency, no change to any rendered attribute.** Both paths in
`files_modified` are git-tracked.
</artifacts_this_plan_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: TRACER — the executed reproduction, from two raw segments to two rendered nodes</name>
  <files>packages/frontend/src/sourcemap/tree.ts, packages/frontend/src/sourcemap/tree.spec.ts</files>
  <read_first>
    - `packages/frontend/src/sourcemap/tree.ts:1-40` — the module header, including the paragraph refusing weaker-than-byte-identical merges and the sentence about collapsing two genuinely distinct source files. That paragraph is the specification this task implements.
    - `packages/frontend/src/sourcemap/tree.ts:300-345` — `Resolved`, `resolveClimbs` and `splitSegments`. `resolved.segments` already holds the RAW segments; the display array is derived from it. That is why the fix costs one field and no extra walk.
    - `packages/frontend/src/sourcemap/tree.ts:355-380` — `displaySegment` and its "WHY TRUNCATION IS DETECTED BY A PROBE" comment, including the stated one-value over-report at exactly the cap and the argument that it errs in the safe direction. Nothing here changes.
    - `packages/frontend/src/sourcemap/tree.ts:455-520` — `buildSourceTree`: the `Building` type, `rootByLabel` (already keyed on the raw `rootLabel`), the `display` array, and the interior-directory `siblings.find` at the heart of MD-02.
    - `packages/frontend/src/sourcemap/tree.ts:380-430` — step 5, the duplicate-sibling disambiguation, and the "NO UNICODE NORMALISATION AND NO CASE FOLDING" comment. The new merge key must honour it: raw byte identity, nothing weaker and nothing stronger.
    - `packages/frontend/src/sourcemap/tree.ts:420-445` — `freezeNode` and the `labels.get(child.label)` duplicate computation, which reads the DISPLAY label. Decide and record whether that stays on the display label (it is a display marker) or moves with the merge key.
    - `packages/frontend/src/sourcemap/tree.spec.ts` — the existing structure and the import-list assertion pinning this module's imports as an exact set.
    - `packages/engine/src/sourcemap/map-fixture.ts` — the 23-entry hostile label corpus and its exhaustiveness gate.
    - `.planning/phases/07-sourcemap-reconstruction/07-REVIEW.md:310-353` — MD-02 in full, including the executed output and the `mergeKey` fix sketch.
  </read_first>
  <behavior>
    - A map declaring `"A"x300 + "/bank/secret.js"` and `"A"x300 + "/evil/x.js"` — two directory names differing only past the truncation cap — produces TWO directory nodes, each with one child.
    - Two identical directory segments still produce ONE node with two children, unchanged.
    - Each of the two nodes still renders a truncated, sanitised label and still carries the `truncated` note.
    - Segments differing only in case, or only in Unicode normalisation form, still produce TWO nodes — the merge is byte identity and this change must not accidentally strengthen it into a normalising comparison.
  </behavior>
  <action>
Key the merge on the value the store holds, and carry the display label beside it.

Give the `Building` type a merge key alongside `label`, holding the RAW segment as `resolveClimbs`
produced it. Change the interior-directory `siblings.find` to compare that key. `resolved.segments` is
already in scope beside `display`, so this costs one field and no extra pass — pair the two arrays by
index rather than re-deriving either.

Keep `label` exactly as it is: `displaySegment`'s sanitised, truncated output, with its `truncated`
flag. Nothing that reaches the DOM changes, which is the property that makes this a safe fix rather
than a rendering change.

Update the module header. It currently states the byte-identical rule as though the code implements it;
after this edit it does. Add one sentence naming MD-02 and the specific leak — that the comparison read
a value the display had already truncated — because that is the class of bug a reader should be
watching for the next time a display helper's output is convenient to compare.

**Confirm the ROOT merge, do not assume it.** `rootByLabel` keys on the raw `rootLabel` and applies
`forCellText` only when building the node. Read it, confirm it, and record the confirmation with its
line number. If it turns out to key on a display value, it has the same bug and this task fixes both.

**Decide the `duplicate` marker's basis explicitly.** `freezeNode` counts labels using the DISPLAY
label. Two siblings whose display labels collide because of truncation would be marked duplicate while
being distinct, which is the same leak in a different place — or it is the correct behaviour for a
marker whose job is to warn the operator that two rendered rows look identical. Choose, state the choice
in the code, and record it in the SUMMARY. A marker that warns "these two look the same on screen" is
defensible; one that claims "these two ARE the same" is not.

**Cases.** The executed reproduction from the review, asserting two roots. The identical-siblings case
asserting one. The case-only and normalisation-only pairs asserting two each, so the merge is pinned as
byte identity from both sides. The reproduction must be RED against the shipped code — record the
observed root count and the observed children.
  </action>
  <acceptance_criteria>
    - `tree.spec.ts` carries a case over two 300-character sibling directory names differing only past the cap, asserting two directory nodes each with one child.
    - That case is RED against the shipped code; the observed pre-fix structure — one root, two children — is recorded in the SUMMARY.
    - A case asserts two identical directory segments still merge to one node with two children.
    - Two cases pin the merge as byte identity: a case-only pair and a Unicode-normalisation pair, each asserting two nodes.
    - The rendered label of each node is still the `forCellText` output and still carries the `truncated` note; asserted on the reproduction case.
    - The root-merge confirmation is recorded in the SUMMARY with its line number.
    - The `duplicate`-marker basis decision is stated in the code and in the SUMMARY.
    - `tree.spec.ts`'s import-list assertion is green and the import set is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run packages/frontend/src/sourcemap/tree.spec.ts --reporter=dot</automated>
    <fails_when>non-zero exit; the reproduction case reporting 1 root where 2 are expected means the merge still reads the display label</fails_when>
    <automated>pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm knip</automated>
    <fails_when>non-zero exit from any of the three; a knip unused-export report means the `Building` field was added to the exported node type by mistake</fails_when>
  </verify>
  <done>Two directories whose names differ are two nodes, and the rendered labels are byte-unchanged.</done>
  <reversibility rating="reversible">One internal field and one comparison. No rendered shape and no stored value changes.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The hostile corpus, unchanged — prove the fix moved nothing it should not</name>
  <files>packages/frontend/src/sourcemap/tree.spec.ts</files>
  <read_first>
    - `packages/engine/src/sourcemap/map-fixture.ts` — all 23 label cases by id, including the legal control `src/app/index.js` whose stated purpose is that a corpus in which every case fires proves only that the check fires.
    - `packages/frontend/src/sourcemap/tree.spec.ts` — the existing corpus-driven cases and their exhaustiveness gate over the id set.
    - `packages/frontend/src/safety/hostile.spec.ts` — the assertions that no `title` and no `data-*` attribute carries the untruncated label, and the byte-identical round trip of the stored string. Read them to know exactly what must remain true; do not modify that file, which is outside this plan.
    - `packages/frontend/src/sourcemap/tree.ts` — the climb accounting (`climbs`, `clampedClimbs`) and where it is recorded on the leaf, so a change in node identity cannot silently move it.
  </read_first>
  <behavior>
    - Every one of the 23 corpus labels produces the same tree structure it produces today: same node count, same kinds, same labels, same climb counts, same notes.
    - The exhaustiveness gate over the corpus id set still passes, so no case was dropped.
    - No node carries a raw segment into any rendered field.
  </behavior>
  <action>
Pin the blast radius against the corpus that already exists, rather than against cases invented for
this fix.

Run the whole 23-case corpus through `buildSourceTree` before and after, and assert the resulting trees
are equal. Snapshot the pre-fix structure into the test as explicit expectations rather than as an
opaque snapshot file — this repository's fixture discipline prefers assertions a reader can evaluate,
and an opaque snapshot would silently absorb exactly the kind of change this task exists to detect.

Assert the exhaustiveness gate over the corpus id set still holds, so a case cannot be dropped to make
the comparison pass.

Add the assertion that closes the loop on the new field: no rendered field of any node — `label` and
anything else the frozen node type exposes — equals the raw segment for any corpus case where the raw
and display segments differ. That is the guard against the merge key leaking into the render path,
which would undo the sanitisation `hostile.spec.ts` asserts from the other side.

Record in the SUMMARY the node counts for the corpus before and after, so the equality is a measured
claim rather than an asserted one.
  </action>
  <acceptance_criteria>
    - A corpus-driven case asserts the tree for all 23 labels is structurally identical to the pre-fix tree, with explicit expectations rather than an opaque snapshot.
    - The exhaustiveness gate over the corpus id set is present and green.
    - A case asserts no exposed node field equals the raw segment where raw and display differ.
    - The corpus node counts before and after are recorded in the SUMMARY and are equal.
    - `packages/frontend/src/safety/hostile.spec.ts` is byte-unchanged; `git diff --stat` on it prints nothing.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run packages/frontend/src/sourcemap/tree.spec.ts --reporter=dot</automated>
    <fails_when>non-zero exit; an exhaustiveness failure naming a missing corpus id means a case was dropped rather than fixed</fails_when>
    <automated>git diff --stat packages/frontend/src/safety/hostile.spec.ts</automated>
    <fails_when>the command prints any line — the hostile-render assertions are outside this plan and must not be adjusted to accommodate it</fails_when>
  </verify>
  <done>The 23 hostile labels build the same tree they built before, and the raw segment never reaches a rendered field.</done>
  <reversibility rating="reversible">Test additions.</reversibility>
</task>

<task type="auto">
  <name>Task 3: The surfaces above the tree, proven unmoved</name>
  <files>packages/frontend/src/sourcemap/tree.spec.ts</files>
  <read_first>
    - `packages/frontend/src/components/SourceTree.vue` — the virtualised render at 32px and the `ProducibilityMark`, so the node-count change in the reproduction case is understood against the scroller's sizing. Read it; do not modify it.
    - `packages/frontend/src/components/SourceBrowser.spec.ts` — the case asserting the REAL tree and the REAL viewer are mounted, never a stub. It is the assertion that makes this fix visible end to end. Read it; do not modify it.
    - `packages/engine/src/contract.ts` — `SOURCE_TREE_LOAD_MAX`, the 2,000-row bound the tree fills to, and the note that the read answers with the TOTAL beside the returned count. A merge that produces MORE nodes for the same rows interacts with that bound; work out whether it can and record the answer.
    - `.planning/phases/07-sourcemap-reconstruction/07-UI-SPEC.md` — the `source-tree` / `overflow` row, recorded as the one unresolved UI row because 2,000 is a reuse of `IN_MEMORY_WINDOW_ROWS` rather than a measurement. Do not resolve it here; record whether this fix touches it.
  </read_first>
  <action>
Work out and record what more nodes means for the surfaces above.

**The load bound.** `SOURCE_TREE_LOAD_MAX` bounds the ROWS loaded, not the nodes built. Confirm that by
reading, and state whether un-merging directories can increase the NODE count for a fixed row count —
it can, by at most one directory node per row — and whether the virtualised scroller has any assumption
about node count relative to row count. Record the finding either way. If the bound is on rows and the
scroller is node-count-agnostic, say so and say where you read it; that is a two-line answer that stops
the next reader re-deriving it.

**The unresolved UI row.** `07-UI-SPEC.md`'s `source-tree` / `overflow` row is the phase's one
self-declared unresolved row and it is about the NUMBER 2,000, not about node identity. Record whether
this fix changes anything about it. It should not; saying so is what stops it being folded into this
plan by a later reader.

**Do not modify any component or spec outside `tree.spec.ts`.** If a component-level assertion turns out
to be needed, record it as a follow-up with its file and its reason rather than reaching outside this
plan's `files_modified`.
  </action>
  <acceptance_criteria>
    - The SUMMARY records whether node count can exceed row count after this fix, with the bound it can reach and the line where `SOURCE_TREE_LOAD_MAX`'s subject was read.
    - The SUMMARY records that the `source-tree` / `overflow` UI row is unaffected, or names precisely how it is affected.
    - No file inside `packages/frontend/src/sourcemap/` other than the two in `files_modified` is modified: `git diff --name-only -- packages/frontend/src/sourcemap` lists only `tree.ts` and `tree.spec.ts`. **The pathspec is load-bearing.** `workflow.use_worktrees` is false, so every plan in this phase runs against the one working tree; a bare `git diff --name-only` would report 07-11's edits to `SourceBrowser.vue`, `client.ts` and three frontend specs as this plan's overreach. Scope the question to the directory this plan owns and let the wave overlap guard do the rest.
    - `pnpm vitest run packages/frontend/src` exits 0 with a collected-test count no lower than at the start of the plan; both numbers recorded.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run packages/frontend/src --reporter=dot</automated>
    <fails_when>non-zero exit; a `SourceBrowser.spec.ts` or `SourceTree` failure means the real mounted tree read a shape that moved</fails_when>
    <automated>git diff --name-only -- packages/frontend/src/sourcemap</automated>
    <fails_when>the output lists any path other than `packages/frontend/src/sourcemap/tree.ts` and `packages/frontend/src/sourcemap/tree.spec.ts` — this plan is scoped to two files inside one directory, and the pathspec is what keeps a shared working tree from making a sibling plan's uncommitted edits look like this plan's</fails_when>
    <automated>pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm knip</automated>
    <fails_when>non-zero exit from any of the three</fails_when>
  </verify>
  <done>The node-count consequence is measured and written down, and nothing outside the normaliser moved.</done>
  <reversibility rating="reversible">Recorded findings and one test run.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored `sources_verbatim` → the display tree | The segment strings are target-controlled and are the input to both the merge comparison and the rendered label. |
| display tree → the operator's belief about the target | A merged node is a factual claim about the target's directory layout. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-66 | Spoofing | the interior-directory merge in `buildSourceTree` | medium | mitigate | The merge keys on the raw segment, so a target cannot make two distinct directories appear as one by choosing names that agree for the first 256 graphemes. Asserted by the review's executed reproduction, plus case-only and normalisation-only pairs pinning the comparison as byte identity from both sides. |
| T-07-73 | Information Disclosure | the raw segment carried on the internal `Building` node | low | mitigate | The merge key is internal and is not a field of the frozen `SourceTreeNode` the component renders. Task 2 asserts no exposed node field equals the raw segment where raw and display differ, which is the guard against the unsanitised value reaching a `title`, a `data-*` or the DOM — the property `hostile.spec.ts` already asserts from the render side and which this plan must not weaken from the build side. |
| T-07-74 | Denial of Service | node count rising for a fixed row count | low | accept | Un-merging directories can add at most one directory node per loaded row, against a row bound of `SOURCE_TREE_LOAD_MAX`. Accepted rather than mitigated because the scroller is virtualised and the increase is bounded by the same constant that bounds the rows; task 3 measures and records the relationship rather than assuming it. |

No package-manager install task exists in this plan, so no supply-chain threat row applies.
</threat_model>

<verification>
- `pnpm vitest run` — whole suite green.
- `pnpm typecheck && pnpm lint && pnpm knip` — exit 0.
- `git diff --name-only -- packages/frontend/src/sourcemap` — only `tree.ts` and `tree.spec.ts`. Always with the pathspec: 07-11 shares this wave and this working tree.
</verification>

<success_criteria>
- The review's executed reproduction produces two directory nodes, proven by a case that was RED.
- The merge is byte identity, pinned from both sides by a case-only pair and a normalisation pair.
- All 23 hostile labels build the same tree they built before, with node counts recorded.
- No raw segment reaches a rendered field.
</success_criteria>

<output>
Create `.planning/phases/07-sourcemap-reconstruction/07-17-SUMMARY.md` when done.
Record: the pre-fix reproduction output, the root-merge confirmation with its line number, the
`duplicate`-marker basis decision, the corpus node counts before and after, the node-count-versus-row-count
finding, and MD-02 marked closed with its commit.
</output>
