---
phase: 07-sourcemap-reconstruction
plan: 23
type: execute
wave: 1
depends_on: []
gap_closure: true
nyquist_compliant: true
autonomous: true
requirements: [MAP-06]
files_modified:
  - packages/backend/src/ingest/consumer.ts

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`consumer.ts`'s two depth-refusal justifications say what `consumer.spec.ts`'s mixed-map case says — that the size refusal is structurally unreachable through the ingest path — and neither of them names it as a reason `admitDerived` can refuse a source before it could recurse."
    - "The `empty` half carries both justifications alone, and each one states positively WHY the other half is absent, so the next reader does not restore it as a helpful completion."
    - "The depth guard's BEHAVIOUR is byte-unchanged: `admitDerivedDepth(input.depth + 1)`, the `admittedForRecursion` counter, the loop's increment site and the `sm.derivedRejected[nextDepth.reason]++` emission are all untouched."
    - "`consumer.spec.ts` is unmodified — it is the statement the comments are held to, not a file this plan edits."
  prohibitions:
    - "No change to the depth guard's behaviour in `consumer.ts`. The guard is correct and verified 26/26; this plan changes comment text and nothing else. A behavioural edit here would reopen G-07-4, which round 2 closed."
    - "No edit to `packages/backend/src/ingest/consumer.spec.ts`. Its mixed-map case is the SPECIFICATION this plan aligns the production comments to; editing it would move the target instead of hitting it."
    - "No edit to `packages/engine/src/sourcemap/parse.ts` or `packages/engine/src/sourcemap/derive.ts`. They are read as the corroborating evidence for the unreachability argument and are cited, never touched."
    - "No change to `MAP_MAX_BYTES`. Open by operator decision (W-6) and prohibited in every plan of this phase."
    - "No change to `DERIVED_SOURCE_MAX_BYTES`, `DERIVED_MAX_DEPTH` or any member of `DERIVED_REJECT_REASONS`. The vocabulary stays exactly as it ships; only prose about it changes."
    - "No docblock caveat added to `telemetry.ts`'s `derivedRejected`. The operator answered IN-04 explicitly — no note needed — and adding one is planning a decision that was already made the other way."
    - "No new schema migration. `SCHEMA_VERSION` is 9 and stays 9; this round is comment-and-test only."
    - "No new foreign key and no `ON DELETE CASCADE`, anywhere, for any reason."
    - "No lowering of `RETENTION_SWEEP_MAX_PASSES`."
    - "No change to `redactUrlForExport`, the export vocabulary, or `observations.url`'s shipped output."
    - "No outbound fetch (D-01) and no filesystem access from plugin code (D-17)."
    - "A comment must never state that a code path is reachable when the code makes it unreachable. This is the defect class the whole round exists to close."
  artifacts:
    - "packages/backend/src/ingest/consumer.ts — the reconstruction stage's opening depth-decision comment (the `COUNTED AND LOGGED FROM WHAT WAS ADMITTED` paragraph), with the size refusal removed as a reachable reason and its unreachability stated"
    - "packages/backend/src/ingest/consumer.ts — the `DECIDED AT THE TOP, REPORTED AT THE BOTTOM` comment above the refusal emission, with the same correction"
  key_links:
    - "`consumer.ts`'s sole production decode site → `parseSourceMap`'s byte refusal in `parse.ts` → `DERIVED_SOURCE_MAX_BYTES` in `derive.ts` — the three-step chain that makes the size refusal unreachable, and the chain both corrected comments now assert rather than contradict"
    - "`consumer.spec.ts`'s mixed-map case → the two production comments — the agreement that did not exist and is what this plan creates"
---

<objective>
Delete the unreachable half of the depth-refusal justification from both production comments in
`consumer.ts`, and say in each one WHY it is absent.

This closes **G-07-5 / WR-01 (round 3)**. Plan 07-19 hoisted the depth-refusal argument into two
production comments that name BOTH `empty` and the size refusal as reasons `admitDerived` can refuse
a recovered source before it recurses. The size refusal is structurally unreachable through the
ingest path, and the SAME plan committed the correct statement in `consumer.spec.ts`. One plan, one
commit range, two contradictory statements about the same guard.

Purpose. This is the TRACER for round 3. All four gaps in this round are the same shape as round 2's
— a sentence in the repository disagrees with the code beside it — but three of the four change no
behaviour at all, so a green suite proves nothing about them. This plan proves the round-3 repair
pattern end to end on the smallest of the four: the production prose corrected, and a REGION-SCOPED
gate that reads the comment itself and flips from red to green on exactly the edit. Plans 07-24 and
07-25 expand that pattern to the other three sites.

Output: two corrected comments, zero behavioural change, and a gate shape the two expansion plans
reuse by name.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sourcemap-reconstruction/07-CONTEXT.md
@.planning/phases/07-sourcemap-reconstruction/07-UAT.md
@.planning/phases/07-sourcemap-reconstruction/07-VERIFICATION.md
@.planning/phases/07-sourcemap-reconstruction/07-19-SUMMARY.md
</context>

<gap_trace>
| Gap id | Source | What this plan closes |
|---|---|---|
| G-07-5 / test 1 | `07-UAT.md` gap `G-07-5` (minor); `07-VERIFICATION.md` finding `WR-01` | Both production comments stop naming the size refusal as a reachable `admitDerived` reason and state its unreachability instead; `consumer.spec.ts` is left as the statement they agree with. |
</gap_trace>

<artifacts_this_plan_produces>
## Artifacts this phase produces

**No new symbol of any kind.** No new file, no new export, no new constant, no new test name, no new
helper, no new dependency. This plan changes comment text in one git-tracked file
(`packages/backend/src/ingest/consumer.ts`) and nothing else.

**Named gate shape, reused by 07-24 and 07-25 — cite it by this name in their SUMMARYs:**
- **the region-scoped comment gate** — an `awk` range anchored on two unique literals that bracket
  the comment, piped to `grep`, with THREE parts that must all be present: a **non-vacuity
  companion** (`grep -c` over a literal that must be present, printing a positive number, which
  proves the range resolved), a **negative half** (`! ... | grep -q` for the retired claim), and a
  **positive half** (`grep -c` for the corrected claim). The non-vacuity companion is not optional:
  `! grep` over a collapsed `awk` range exits 0 and passes vacuously.
- **The comment-stripping prefilter is deliberately INVERTED here and that is not an oversight.**
  Round 2's lesson was that a forbidden-CODE-construct grep must strip comments first, because prose
  discussing the construct matched six times in `retention.ts`. In this plan the comment IS the
  subject, so the region is bracketed to comment text and a comment-stripping prefilter would strip
  the entire subject and leave a gate that can never fail. Record this inversion in the SUMMARY.
</artifacts_this_plan_produces>

<planner_assumptions>
## Spec-less edge-probe fallback — 16 surfaced, 16 recorded, 0 dropped

Phase 07 has no plain `-SPEC.md`, so `## Edge Coverage` is absent and the deterministic edge probe
ran over the eight phase requirement IDs. It returned **16 applicable edges, 0 resolved, 16
unresolved** (`byVerification: {explicit: 0, backstop: 0}`) — identical to round 2. Per §A/§C of the
fallback reference an `unresolved` row is never auto-resolved with a backstop and never silently
dropped, and — because none resolved — **none becomes a `must_haves.truths` entry**. All sixteen are
recorded here as explicit flagged planner assumptions. **This block is the round-scoped record for
plans 07-23 through 07-25; the two expansion plans restate only the rows for the requirement IDs they
carry.**

**No-silent-drop equality: 16 surfaced == 16 flagged here == 0 authored as truths.**

| # | Req | Category | Probe question | Flagged planner assumption |
|---|-----|----------|----------------|----------------------------|
| 1 | MAP-01 | adjacency | When two things are exactly equal or just touch, do they merge, collide, or separate? | Assumed settled by the shipped announcement path: `findAnnouncement` takes the LAST announcement in the bounded tail, so two announcements do not merge — the later wins. Unverified against a probe of its own; unchanged by round 3. |
| 2 | MAP-01 | empty | What is the result for empty, single-element, or null input? | Assumed settled: a body with no announcement produces no reconstruction stage and no counter movement. Not re-probed. |
| 3 | MAP-01 | ordering | When elements compare equal, is output order specified and stable? | Assumed settled: announcement order is body order and `lastIndexOf` makes the tie deterministic. Not re-probed. |
| 4 | MAP-01 | concurrency | If interrupted or run in parallel, what is guaranteed? | Assumed settled by `stillCurrent()` re-checks at every write site; a project change abandons the stage. **This plan reads that paragraph as the tail of its second comment region and must leave it byte-unchanged.** |
| 5 | MAP-02 | boundary | What happens exactly at each min/max/threshold — and one step either side? | Assumed settled: `MAP_MAX_BYTES` has an inclusive boundary fixture at exactly the cap. **W-6 records that the constant under-serves recovery ~2x and it is open by operator decision** — this row inherits that open state and is NOT closed by this round. |
| 6 | MAP-02 | precision | Where can precision loss, overflow, or rounding/tie-breaking occur? | Assumed settled: the base64 4/3 expansion bound is computed with `ceil`, and the row-unit gate converts sources at two rows per source before comparing. Not re-probed. |
| 7 | MAP-03 | unclassified | unclassified — review manually | Reviewed manually: MAP-03 is the codec-scope requirement, enforced by `codec-prohibition.spec.ts` as a static gate. No behavioural edge applies; recorded rather than dismissed because the probe could not classify it. |
| 8 | MAP-04 | empty | What is the result for empty, single-element, or null input? | Assumed settled: the corpus carries `empty` and `dot-only` as named degenerate cases exercised in both directions. Not re-probed. |
| 9 | MAP-04 | encoding | Whose definition of length/equality applies — bytes, code points, graphemes, or normalised form? | Assumed settled: `truncateToCodePoints` cuts on a code-POINT boundary and the display caps are stated in graphemes. Untouched by round 3. |
| 10 | MAP-04 | concurrency | If interrupted or run in parallel, what is guaranteed? | Assumed settled: no filesystem exists to race under D-17. Not re-probed. |
| 11 | MAP-05 | boundary | What happens exactly at each min/max/threshold — and one step either side? | Assumed settled: `sizeBoundaryCases` builds a document of exactly `MAP_MAX_BYTES` and one byte over. Not re-probed. |
| 12 | MAP-05 | precision | Where can precision loss, overflow, or rounding/tie-breaking occur? | Assumed settled: the byte arithmetic in the fixture builder is exact by construction and guarded by a `RangeError`. Not re-probed. |
| 13 | MAP-06 | unclassified | unclassified — review manually | Reviewed manually, and it is **THIS PLAN'S SUBJECT**: the depth and size bounds of the derived path. The behavioural bounds are verified 26/26 and are NOT reopened; what changes is the prose describing which of them a hostile map can actually reach. Plan 07-25 carries the other MAP-06 row. |
| 14 | MAP-07 | empty | What is the result for empty, single-element, or null input? | Assumed settled: `NOTHING_TO_EXPORT_LABEL` is the shipped empty state and the UI-SPEC `manifest-export / empty` row is covered. Carried by plan 07-24, not this one. |
| 15 | MAP-07 | encoding | Whose definition of length/equality applies? | Assumed settled: the manifest field ceiling is 4,113 — a 4,096 code-point stored cap plus one 17-character marker. Carried by plan 07-24, which must not raise it. |
| 16 | UI-05 | unclassified | unclassified — review manually | Reviewed manually: the viewer's state matrix is covered by `07-UI-SPEC.md`'s 35-row considerations table (32 covered, 2 backstop, 1 unresolved). The one unresolved row is `source-tree / overflow`, whose NUMBER (2,000) is a defensible reuse rather than a measurement — carried forward unchanged and untouched by this round. |

## UI Considerations touched by this gap

**None.** G-07-5 is a comment in the backend ingest path. No surface in `07-UI-SPEC.md`'s
considerations table renders, reads or depends on either comment, so nothing is lifted here. Plan
07-24 carries the one row this round touches.
</planner_assumptions>

<assumption_delta_decision>
The detector returned `detected: true` on three `pluralization` cues — `second`, `another`, `also` —
every one of them harvested from round-2 PLAN prose ("the second half of a justification", "another
site", "the verifier also recorded"), not from a real second case.

- **Primary noun:** unchanged — a recovered source, keyed by the sha256 of its content (D-05).
- **Decision:** `no-change`. This round introduces no second platform, tenant, auth method or source
  of truth; it deletes a claim about a refusal reason. Nothing gains or loses a monopoly.
- No invariant test is suggested, because no generalisation occurred to encode.
</assumption_delta_decision>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — the two comments corrected, held by a gate that reads the comment</name>
  <reversibility rating="reversible">Comment text in one file, no behavioural change, no exported symbol. Reverting is a single-commit revert with no data, schema or contract implication.</reversibility>
  <files>packages/backend/src/ingest/consumer.ts</files>
  <read_first>
    - `packages/backend/src/ingest/consumer.ts:1246-1275` — the whole `COUNTED AND LOGGED FROM WHAT WAS ADMITTED` paragraph, ending at the `const nextDepth = admitDerivedDepth(input.depth + 1);` line. Read the whole paragraph including the MD-03 preamble above it; the argument runs across it and a spot edit on one line will leave it incoherent. The false half is the second reason listed on the line immediately after ``refuses as``.
    - `packages/backend/src/ingest/consumer.ts:1441-1460` — the `--- D-13's DEPTH REFUSAL, REPORTED FROM WHAT ACTUALLY HAPPENED ---` block through to `if (!nextDepth.ok && admittedForRecursion > 0) {`. The false half here is phrased differently from the first site — it is the ``or whose every source is over`` clause and the three words that follow the constant name — so a find-and-replace tuned to the first site will silently miss this one. The `A STAGE ABANDONED ON PROJECT CHANGE` paragraph at the tail of this region is CORRECT and must survive byte-unchanged.
    - `packages/backend/src/ingest/consumer.spec.ts:2680-2696` — the `WHY THE INADMISSIBLE HALF IS EMPTY AND NOT OVER-SIZE` comment above the mixed-map case. **This is the SPECIFICATION for this task.** It is the correct statement the two production comments must agree with, it was committed by the same plan that introduced the defect, and it is NOT a file this plan edits.
    - `packages/backend/src/ingest/consumer.ts:1118-1150` — the sole production decode site (`decodeInlineMap(announcement.url, MAP_MAX_BYTES)`) and the `parseSourceMap(inline.json, ...)` call that is the only producer of `parsed.recovered`. Confirm from the code that `MAP_MAX_BYTES` is what is passed; the plan states it so you can check agreement, not so you can skip the check.
    - `packages/engine/src/sourcemap/parse.ts:220-235` — the `Buffer.byteLength(json, 'utf8')` refusal. Read the comparison operator and the bound it is compared against.
    - `packages/engine/src/sourcemap/derive.ts:135-150` and `:225-240` — the `DERIVED_SOURCE_MAX_BYTES = MAP_MAX_BYTES` assignment and the per-source refusal. Read both; the equality of the two bounds is the load-bearing fact.
    - `.planning/phases/07-sourcemap-reconstruction/07-UAT.md` — gap `G-07-5`, whose `root_cause` traces the full chain and whose `missing` list is the authoritative scope fence.
    - `.planning/phases/07-sourcemap-reconstruction/07-19-SUMMARY.md` — what plan 07-19 believed it was recording, so the SUMMARY can name what changed in the belief rather than only in the text.
  </read_first>
  <behavior>
    - The first comment region names exactly ONE reason `admitDerived` can refuse a recovered source before it could recurse, and that reason is `empty`.
    - The second comment region describes exactly ONE hostile-map shape that recovers sources and admits none of them, and that shape is the all-empty-`sourcesContent` map.
    - Both regions state POSITIVELY that the size refusal is structurally unreachable through the ingest path, and both point at the mixed-map case in `consumer.spec.ts` as where the argument is made — so the omission reads as a decision rather than as an oversight a later reader should complete.
    - The region-scoped gate is RED before the edit and GREEN after it, in both directions, for both regions.
    - The compiled behaviour is identical: no statement, expression, condition or counter changes.
  </behavior>
  <action>
Observe the gate RED first, then edit. Do NOT restore files with `cp` at any point in this plan —
`cp` is aliased to `cp -i` in this environment and silently no-ops an overwrite; write the failing
observation down before the change instead of trying to recreate it after.

**Step 1 — record the RED state.** Run all four region probes and paste the output into the SUMMARY
verbatim. Every one of them is currently in the state the fix must flip:

- `awk '/COUNTED AND LOGGED FROM WHAT WAS ADMITTED/,/const nextDepth = admitDerivedDepth/' packages/backend/src/ingest/consumer.ts | grep -c 'RECOVERY IS NOT'` prints `1` — the non-vacuity companion for region 1.
- `awk '/COUNTED AND LOGGED FROM WHAT WAS ADMITTED/,/const nextDepth = admitDerivedDepth/' packages/backend/src/ingest/consumer.ts | grep -c 'too_large'` prints `1` today and must print `0` after the edit.
- `awk '/DECIDED AT THE TOP, REPORTED AT THE BOTTOM/,/if \(!nextDepth.ok/' packages/backend/src/ingest/consumer.ts | grep -c 'stillCurrent'` prints `1` — the non-vacuity companion for region 2.
- `awk '/DECIDED AT THE TOP, REPORTED AT THE BOTTOM/,/if \(!nextDepth.ok/' packages/backend/src/ingest/consumer.ts | grep -c 'both reachable'` prints `1` today and must print `0` after the edit.

<!-- planner-discipline-allow: too_large -->
<!-- planner-discipline-allow: both reachable -->

**Step 2 — rewrite region 1.** The `RECOVERY IS NOT ADMISSION` sentence currently lists two refusal
reasons where only one is reachable. Replace the run of comment lines from `ADMISSION: a map can
recover sources that` through `declined nothing at any depth.` with exactly this text, keeping the
existing `    // ` prefix and the surrounding lines untouched:

```
    // ADMISSION: a map can recover sources that `admitDerived` refuses as
    // `empty` before they could recurse, and a stage that admitted none of them
    // declined nothing at any depth. The size refusal is NOT the other half of
    // that sentence and never was: it is structurally unreachable through the
    // ingest path, argued at the mixed-map case in `consumer.spec.ts`, so
    // `empty` carries this justification alone.
```

The `Guarding the emission on \`parsed.recovered.length\`…` sentence that follows, and the whole
MD-03 preamble above, stay byte-unchanged.

**Step 3 — rewrite region 2.** Replace the run of comment lines from `fires on ADMISSION rather than
on recovery.` through `therefore declines nothing.` with exactly this text:

```
    // fires on ADMISSION rather than on recovery. A map whose every
    // `sourcesContent` entry is the empty string recovers sources and admits
    // none of them, attempts no recursion at any depth, and therefore declines
    // nothing. An empty entry is the ONLY refusal a hostile map can mix in here.
    // The size refusal is structurally unreachable through the ingest path: the
    // whole document has already been refused above the same ceiling the
    // per-source gate applies, and a decoded entry is a byte-subset of the JSON
    // that carried it. That is argued at the mixed-map case in
    // `consumer.spec.ts`, and it is the statement these comments agree with.
```

The `The number beside the reason is the ADMITTED count…` sentence that follows and the
`A STAGE ABANDONED ON PROJECT CHANGE` paragraph after it stay byte-unchanged.

**Step 4 — confirm nothing executable moved.** `git diff --unified=0 -- packages/backend/src/ingest/consumer.ts`
must show `+`/`-` lines that all begin with `    // ` after the diff marker. A changed line that is
not a comment line means a statement moved and the edit is wrong.

**The unreachability argument, stated here so the executor can verify it rather than trust it.**
`consumer.ts:1124` is the sole production decode site and passes `MAP_MAX_BYTES`. `parse.ts:227`
refuses on `Buffer.byteLength(json, 'utf8')` exceeding that bound, so the decoded map document is at
most `MAP_MAX_BYTES` bytes. `derive.ts:142` sets `DERIVED_SOURCE_MAX_BYTES` equal to
`MAP_MAX_BYTES`, and `:233` refuses strictly above it. A decoded `sourcesContent` entry is a strict
byte-subset of the JSON string that carried it — JSON string encoding never shrinks a character
below its UTF-8 width and every escape costs strictly more — so the per-source size gate cannot fire
for anything that reached it through ingest. Check all four line references against the code before
writing the sentences; if any of them has moved, record the real line number in the SUMMARY and
proceed, because the ARGUMENT is what matters and it does not depend on the line numbers.
  </action>
  <acceptance_criteria>
    - **Source:** `awk '/COUNTED AND LOGGED FROM WHAT WAS ADMITTED/,/const nextDepth = admitDerivedDepth/' packages/backend/src/ingest/consumer.ts | grep -c 'too_large'` prints `0`.
    - **Source:** the same region contains the phrase `structurally unreachable` at least once, and still contains `RECOVERY IS NOT` exactly once.
    - **Source:** `awk '/DECIDED AT THE TOP, REPORTED AT THE BOTTOM/,/if \(!nextDepth.ok/' packages/backend/src/ingest/consumer.ts | grep -c 'both reachable'` prints `0`.
    - **Source:** the same region contains `structurally unreachable` at least once, and still contains `stillCurrent` exactly once — the abandoned-stage paragraph survived.
    - **Source:** every changed line in `git diff --unified=0 -- packages/backend/src/ingest/consumer.ts` is a `    // ` comment line. No `+`/`-` line contains `admitDerivedDepth`, `admittedForRecursion`, `derivedRejected` or a brace.
    - **Behavior:** `packages/backend/src/ingest/consumer.spec.ts` reports the same 80 tests it reports at HEAD, all passing — the guard is untouched.
    - **Test command:** `git diff --name-only -- packages/backend/src/ingest` lists `packages/backend/src/ingest/consumer.ts` and nothing else.
  </acceptance_criteria>
  <verify>
    <automated>awk '/COUNTED AND LOGGED FROM WHAT WAS ADMITTED/,/const nextDepth = admitDerivedDepth/' packages/backend/src/ingest/consumer.ts | grep -c 'RECOVERY IS NOT'</automated>
    <fails_when>the printed count is 0, or the command exits non-zero — the NON-VACUITY companion for the region-1 gate below. A zero means the awk range collapsed because one of the two anchors moved, which would let the negated gate below pass against an empty stream</fails_when>
    <automated>! awk '/COUNTED AND LOGGED FROM WHAT WAS ADMITTED/,/const nextDepth = admitDerivedDepth/' packages/backend/src/ingest/consumer.ts | grep -q 'too_large'</automated>
    <fails_when>non-zero exit, which here means the retired reason is still named inside region 1. Region-scoped on purpose: the same token is legitimate production code at `consumer.ts:1005` and legitimate prose at `:276`, so a file-wide gate would be unsatisfiable. The comment-stripping prefilter is deliberately absent — the comment IS the subject, and stripping it would leave a gate that can never fail</fails_when>
    <automated>awk '/COUNTED AND LOGGED FROM WHAT WAS ADMITTED/,/const nextDepth = admitDerivedDepth/' packages/backend/src/ingest/consumer.ts | grep -c 'structurally unreachable'</automated>
    <fails_when>the printed count is 0 — the positive half. A zero means the false claim was deleted without the correct claim replacing it, leaving an omission that reads as an oversight a later reader would helpfully restore</fails_when>
    <automated>awk '/DECIDED AT THE TOP, REPORTED AT THE BOTTOM/,/if \(!nextDepth.ok/' packages/backend/src/ingest/consumer.ts | grep -c 'stillCurrent'</automated>
    <fails_when>the printed count is 0, or the command exits non-zero — the NON-VACUITY companion for the region-2 gate below, and simultaneously the check that the abandoned-stage paragraph at the tail of the region survived the edit</fails_when>
    <automated>! awk '/DECIDED AT THE TOP, REPORTED AT THE BOTTOM/,/if \(!nextDepth.ok/' packages/backend/src/ingest/consumer.ts | grep -q 'both reachable'</automated>
    <fails_when>non-zero exit, which here means region 2 still asserts that two hostile shapes reach the admission gate when only one does. This is the second site's phrasing of the same false claim and it is worded differently from region 1's, so a gate written only for region 1 would report success over an uncorrected file</fails_when>
    <automated>awk '/DECIDED AT THE TOP, REPORTED AT THE BOTTOM/,/if \(!nextDepth.ok/' packages/backend/src/ingest/consumer.ts | grep -c 'structurally unreachable'</automated>
    <fails_when>the printed count is 0 — the positive half for region 2</fails_when>
    <automated>pnpm vitest run packages/backend/src/ingest/consumer.spec.ts --reporter=dot</automated>
    <fails_when>non-zero exit, or the dot summary reports fewer than 80 tests — 80 is the count measured at HEAD before this plan, and a lower count means a case stopped being collected rather than a comment being corrected</fails_when>
    <automated>git diff --name-only -- packages/backend/src/ingest</automated>
    <fails_when>the output lists any path other than `packages/backend/src/ingest/consumer.ts`. A listed `consumer.spec.ts` means the specification was edited to meet the comments instead of the comments being edited to meet it, which is the one failure mode this gap's scope fence names explicitly</fails_when>
  </verify>
  <done>Both comment regions name `empty` as the only reachable refusal, both state the size refusal is structurally unreachable and where that is argued, the six region probes read in the corrected direction, `consumer.spec.ts` is unmodified and its 80 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Prove nothing executable moved, then run the repository gate</name>
  <reversibility rating="reversible">Verification only — this task writes no source.</reversibility>
  <files>packages/backend/src/ingest/consumer.ts</files>
  <read_first>
    - `packages/backend/src/ingest/consumer.ts` — the full diff of task 1, re-read as a whole rather than as a diff, to confirm both paragraphs still read as continuous arguments and not as sentences with a hole where a clause was removed.
    - `.planning/phases/07-sourcemap-reconstruction/07-22-SUMMARY.md` — the last SUMMARY of round 2, for the exact shape of the full-gate numbers to compare against.
    - `packages/backend/src/telemetry.ts` — the `derivedRejected` docblock. **Read it and do not edit it.** The operator answered IN-04 explicitly: no caveat is wanted while `DERIVED_MAX_DEPTH` is 1. Confirm in the SUMMARY that it is unchanged, so the next round does not re-litigate a settled question.
  </read_first>
  <behavior>
    - The full repository suite reports no fewer files and no fewer tests than the baseline measured after round 2 sealed.
    - `typecheck`, `lint`, `knip` and `build` all exit 0 — a comment-only change cannot move any of them, and a move means something executable was touched.
    - The working tree contains exactly one modified path.
  </behavior>
  <action>
Run the repository gate and compare against the baseline the orchestrator measured at HEAD after
round 2 sealed: **90 files / 4,321 tests, exit 0, ~13.9 s**, with `typecheck`, `lint`, `knip` and
`build` all exit 0.

`workflow.use_worktrees` is false, so this round's three plans run SEQUENTIALLY on the main working
tree. This plan is the head of the serialised 07-23 → 07-24 → 07-25 chain, so nothing from the other
two is in the tree yet — every pathspec below is the belt to that braces rather than a necessity,
and 07-24 and 07-25 inherit the same discipline with their own floors.

This plan adds no test, so the count floor is the baseline exactly: **4,321**, not more. State that
explicitly in the SUMMARY so 07-24 retargets at whatever this plan RECORDS rather than at a number
copied forward, exactly as round 2 did.

Record in the SUMMARY: the six region-probe outputs before and after, the full before/after text of
both rewritten passages, the four verified line references for the unreachability chain (with the
real line numbers if any have moved), the confirmation that `consumer.spec.ts` and `telemetry.ts`
are unmodified, the full-gate numbers against the 4,321 baseline, the named gate shape for 07-24 and
07-25 to cite, and G-07-5 marked closed with its commit.
  </action>
  <acceptance_criteria>
    - **Test command:** `pnpm vitest run --reporter=dot` exits 0 and reports at least 90 files and at least 4,321 tests.
    - **Test command:** `pnpm typecheck && pnpm lint && pnpm knip && pnpm build` exits 0.
    - **Source:** `git diff --name-only` lists exactly one path, `packages/backend/src/ingest/consumer.ts`.
    - **Source:** `git diff --stat -- packages/backend/src/ingest/consumer.ts` shows a line count change consistent with two rewritten comment passages and no net change in the number of executable statements.
    - **CLI output:** the SUMMARY quotes the vitest summary line verbatim, so 07-24's floor is a recorded measurement rather than a copied figure.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run --reporter=dot</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 90 files or fewer than 4321 tests — a lower count means a spec stopped being collected. This plan adds no test, so a HIGHER count is also worth recording: it would mean a case was added that this plan did not authorise</fails_when>
    <automated>pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm knip &amp;&amp; pnpm build</automated>
    <fails_when>non-zero exit from any of the four. A comment-only change cannot move any of these, so ANY failure here means something executable was edited and task 1's diff discipline was breached</fails_when>
    <automated>git diff --name-only</automated>
    <fails_when>the output lists any path other than `packages/backend/src/ingest/consumer.ts`, or lists nothing at all. Nothing at all means the edit was never made or was already committed without the gates being run against the working tree</fails_when>
    <automated>git diff --name-only -- packages/backend/src/telemetry.ts packages/backend/src/sourcemap packages/engine/src/sourcemap</automated>
    <fails_when>any output at all — `telemetry.ts` carries the IN-04 question the operator explicitly closed, and `derive.ts` / `parse.ts` are read as evidence for the unreachability argument. All three are cited by this plan and edited by none of it</fails_when>
  </verify>
  <done>The repository gate is green at or above the 4,321 baseline, exactly one file is modified, the cited-but-unedited files are provably untouched, and the SUMMARY records the recorded test count as 07-24's floor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository prose → the next engineer's belief about which guards can fire | A production comment naming a refusal reason is what a future reader uses to decide whether that path needs a test, a counter or a health surface. A reason that cannot fire is work invented for nobody. |
| a hostile map's `sourcesContent` → the derived-artifact admission gate | Target-controlled content crosses into the reconstruction stage here. The comments this plan corrects describe exactly which of the gate's refusals that content can actually provoke. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-101 | Repudiation | the depth-refusal justification as a record of what the guard defends against | medium | mitigate | A comment that names an unreachable refusal makes the guard's real coverage unauditable: a reviewer cannot tell which claims were traced and which were assumed, and the one claim that WAS traced loses credibility by association. Task 1 removes the untraced half and states the traced argument positively, with the spec that proves it named by file. |
| T-07-102 | Tampering | a later edit restoring the deleted half as a helpful completion | medium | mitigate | An omission with no stated reason invites restoration. Both rewritten passages say the size refusal is structurally unreachable and point at the mixed-map case, so a future reader who disagrees must first read the argument. The region-scoped gate is the mechanical half of the same defence. |
| T-07-103 | Denial of Service | a behavioural edit to the depth guard while "fixing a comment" | high | mitigate | The guard bounds recursion over target-controlled content; weakening it is the one change in this file with a real availability consequence. The prohibition block forbids it, task 1's acceptance criteria require every changed diff line to be a comment line and explicitly name the four identifiers that must not appear in the diff, and task 2 re-runs the 80-test `consumer.spec.ts` and the full suite. |
| T-07-104 | Information Disclosure | the corrected comments naming target-controlled data | low | accept | The new text names shapes (`sourcesContent` entries that are the empty string) and DefMiner's own constants, never a value from a scanned target. No fixture, no captured body and no operator datum enters the source. |

No package-manager install task exists in this plan, so no supply-chain threat row applies. Blocking
threshold is `high`; T-07-103 reaches it and is mitigated by a diff-shape gate, an identifier
blocklist and two test runs rather than by assertion.
</threat_model>

<verification>
- The six region probes of task 1, each recorded before and after, each flipping in the intended direction.
- `pnpm vitest run packages/backend/src/ingest/consumer.spec.ts --reporter=dot` — 80 tests, exit 0, spec file unmodified.
- `pnpm vitest run --reporter=dot` — no fewer than 90 files and 4,321 tests, exit 0.
- `pnpm typecheck && pnpm lint && pnpm knip && pnpm build` — all exit 0.
- `git diff --name-only` — exactly `packages/backend/src/ingest/consumer.ts`. Always with a pathspec on the scoped checks: this phase runs without worktrees and the three plans of this round share one working tree.
- Read-only confirmation that `packages/backend/src/telemetry.ts`, `packages/backend/src/sourcemap/derive.ts` and `packages/engine/src/sourcemap/parse.ts` are unmodified.
</verification>

<success_criteria>
- Neither production comment names the size refusal as a reason `admitDerived` can refuse a recovered source before recursion.
- Both comments state positively that it is structurally unreachable through the ingest path and name the mixed-map case in `consumer.spec.ts` as where that is argued.
- `consumer.spec.ts` is byte-unchanged and its 80 tests pass.
- No executable line changed; the repository gate is green at or above the 4,321 baseline.
- The region-scoped comment gate shape — non-vacuity companion, negative half, positive half, prefilter deliberately inverted — is written down for plans 07-24 and 07-25 to cite.
</success_criteria>

<output>
Create `.planning/phases/07-sourcemap-reconstruction/07-23-SUMMARY.md` when done.
Record: the six region-probe outputs before and after, the before/after text of both rewritten
passages, the four verified line references of the unreachability chain, the confirmation that
`consumer.spec.ts` and `telemetry.ts` are unmodified, the verbatim vitest summary line as 07-24's
floor, the named gate shape for the two expansion plans, and G-07-5 marked closed with its commit.
</output>
