---
phase: 07-sourcemap-reconstruction
plan: 24
type: execute
wave: 2
depends_on: [07-23]
gap_closure: true
nyquist_compliant: true
autonomous: true
requirements: [MAP-07, UI-05]
files_modified:
  - packages/backend/src/store/export.ts

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The never-claim-a-false-redaction principle in `redactSourceLabelForExport`'s docblock is SCOPED to the label branch that honours it, rather than stated of the export as a whole."
    - "`redactUrlForExport`'s shared query marker over a fragment-only URL is recorded in that docblock as a KNOWN and ACCEPTED exception, with the reason stated: it discloses LESS than the truth, so the cost is operator trust and reviewability, not disclosure."
    - "The `sources_verbatim` column comment describes what `redactSourceLabelForExport` now does — the same MARKER applied per AXIS, a protocol-shaped label delegating to `redactUrlForExport` and every other label cut at its first `?` — and no longer calls it one redactor applied more narrowly."
    - "The `sources_verbatim` column comment describes the two cuts by their SHIPPED semantics — a protocol-shaped label DELEGATES to `redactUrlForExport` and is cut on `?` or `#`, every other label is cut by hand at its first `?` and keeps its `#` tail — and makes NO claim about how either axis's scope changed over time; the full baseline-anchored history lives in `redactSourceLabelForExport`'s docblock at `export.ts:255-275`. [CORRECTED 2026-09-03, gap-closure round 4, `07-UAT.md` G-07-9 — this entry previously asserted that the two axes changed scope in mutually opposite ways, which authored the false sentence that shipped into `export.ts`. See the ERRATA block immediately below this frontmatter.]"
    - "No target-controlled string reaches any label, dialog or download name on the manifest-export path; the `sources` label appears only as a manifest COLUMN governed by the export redaction policy, never as a control label. (`07-UI-SPEC.md`, `long-text / manifest-export`, resolved explicit — unchanged by this plan and re-asserted by it.)"
    - "`redactUrlForExport`, `EXPORT_QUERY_REDACTION`, `isProtocolShapedLabel`, `redactSourceLabelForExport`'s body and `observations.url`'s shipped output are all byte-unchanged, and `export.spec.ts` is unmodified."
  prohibitions:
    - "No change to `redactUrlForExport`. The operator explicitly declined the vocabulary repair as a decision needing its own round; this plan records the exception, it does not remove it."
    - "No change to the export vocabulary. `EXPORT_QUERY_REDACTION` keeps its single spelling and gains no sibling; the count of the literal marker text in `export.ts` must not increase."
    - "No change to `observations.url`'s shipped output. That column does not reach `redactSourceLabelForExport` and must stay byte-identical to what it has always been."
    - "No edit to `packages/backend/src/store/export.spec.ts`. Its fragment-only URL case is the EVIDENCE for the exception this plan records, not a pin to be relaxed."
    - "No edit to the quoted-by-reference withholding text at `export.ts:291-295`. The operator ACCEPTED it as discharging 07-22's criterion; it must not be rewritten, re-attributed or re-worded, and it is adjacent to the lines this plan does change."
    - "No re-run of the deferred sweep for other hardcoded restatements of the query marker's length. The round-3 reviewer independently reported that sweep now has nothing left in it."
    - "No new schema migration. `SCHEMA_VERSION` is 9 and stays 9; this round is comment-and-test only."
    - "No new foreign key and no `ON DELETE CASCADE`, anywhere, for any reason."
    - "No lowering of `RETENTION_SWEEP_MAX_PASSES`."
    - "No change to `MAP_MAX_BYTES`. Open by operator decision (W-6) and prohibited in every plan of this phase."
    - "No change to the depth guard's behaviour in `consumer.ts`."
    - "No raising of the manifest field ceiling of 4,113 — a 4,096 code-point stored cap plus one 17-character marker."
    - "No outbound fetch (D-01) and no filesystem access from plugin code (D-17)."
    - "A comment must never state a principle the module does not keep, and must never summarise a function as doing something it stopped doing."
  artifacts:
    - "packages/backend/src/store/export.ts — `redactSourceLabelForExport`'s docblock: the principle scoped to the label branch, plus a new paragraph recording the delegated branch's shared marker as a known and accepted exception with its stated reason"
    - "packages/backend/src/store/export.ts — the `sources_verbatim` entry's column comment in `EXPORT_COLUMNS`, restated per axis"
  key_links:
    - "`redactSourceLabelForExport`'s two branches → the docblock principle that only one of them honours → the exception paragraph that names the other — the link whose absence let the file criticise on one branch what it retains on the other"
    - "`redactSourceLabelForExport`'s inline comment (`the SAME marker, a narrower cut`) → the `sources_verbatim` column comment — the two summaries of one function that must agree, and did not"
    - "`export.spec.ts`'s fragment-only URL case → the exception paragraph — the pinned behaviour that the recorded exception now names by file rather than leaving a reader to discover"
---

> ## ERRATA — 2026-09-03, gap-closure round 4, `07-UAT.md` gap `G-07-9`
>
> **Read this before any other line of this file.**
>
> This plan authored a false claim about the history of the `sources_verbatim` column's
> redaction, and that claim shipped into `packages/backend/src/store/export.ts`, where it
> stood for a full round contradicting the same file's own docblock 190 lines away.
>
> The column's real history, reconstructed from git:
>
> | Commit | `sources_verbatim` binding at that commit | Which labels each axis was cut on |
> |---|---|---|
> | `d5cd5e0` (07-06) | `{ name: "sources_verbatim", redact: redactUrlForExport },` | `redactUrlForExport` cuts at the first `?` or `#` with no shape test in front of it, so BOTH axes were cut on EVERY label. |
> | `a901b9e` (07-16) | `{ name: "sources_verbatim", redact: redactSourceLabelForExport },`, whose body was `return isProtocolShapedLabel(label) ? redactUrlForExport(label) : label;` | One shape test gates both axes at once: BOTH were cut on PROTOCOL-SHAPED LABELS ONLY, and a non-protocol label was returned untouched. |
> | `0e44102` (07-22) | `{ name: "sources_verbatim", redact: redactSourceLabelForExport },`, whose body delegates a protocol-shaped label to `redactUrlForExport` and otherwise cuts at `label.indexOf("?")` | QUERY axis cut on EVERY label again (protocol-shaped by delegation, every other by hand). FRAGMENT axis still cut on PROTOCOL-SHAPED LABELS ONLY — the by-hand branch never looks at `#`. |
>
> **The conclusion the table supports: there is no baseline under which the two axes changed
> scope in mutually opposite ways.** From 07-06 to 07-16 both axes moved the same way, together,
> under one shape test. From 07-16 to 07-22 only the query axis moved. From 07-06 to 07-22 the
> query axis ends where it started and only the fragment axis moved. Each half of the original
> claim borrowed a different baseline, which is why no single reading of the history makes both
> halves true at once.
>
> **What round 4 did.** Plan `07-26-PLAN.md` DELETED the sentence from `export.ts` rather than
> restating it a fourth time. The three previous repairs — G-07-3 under 07-16, G-07-6 under
> 07-22, WR-01/G-07-9 under 07-24 — were each a restatement, and each restatement became the
> next round's defect. Nothing was added in its place: `redactSourceLabelForExport`'s docblock at
> `export.ts:255-275` already states each axis against a NAMED baseline, and the column comment's
> closing clause already sends the reader there. `must_haves.truths[4]` above is corrected to
> match.
>
> **Every other occurrence of the claim in this file is left byte-unchanged ON PURPOSE.** Twelve
> further restatements live in the objective, `read_first`, `behavior`, action, acceptance
> criteria, verify and success-criteria blocks below. This file is the record of what was PLANNED
> and EXECUTED, `07-24-SUMMARY.md` cites it, and retroactively falsifying it would be a worse
> defect than the one being repaired. **This block governs them all: no reader should treat any of
> them as current.**
>
> **Verify this table yourself** — do not take it on trust, which is the mistake that produced the
> claim it replaces: `git show <commit>:packages/backend/src/store/export.ts` for each of
> `d5cd5e0`, `a901b9e` and `0e44102`.


<objective>
Scope the never-claim-a-false-redaction principle to the branch that honours it, record the other
branch's marker as a known and accepted exception, and restate the `sources_verbatim` column comment
per axis.

This closes **G-07-6** and **G-07-7**. Both are single-passage prose corrections in one file, and
they are planned together because they are the same file, the same reader and the same audit: the
column comment is the first thing a redaction-policy review reads, and the function docblock is the
second.

- **G-07-7.** `export.ts:297-299` states unconditionally that a marker claiming a withheld query
  makes a redaction unreliable, while `redactUrlForExport` at `:199-202` appends that marker on `?`
  OR `#` — so a fragment-only URL exports with a query marker over a value that has no query axis,
  pinned as EXPECTED in `export.spec.ts`. The file criticises on one branch precisely what it
  retains and pins on the other. The operator chose the comment-scoping repair.
- **G-07-6.** `export.ts:424-427` says the label column applies "the SAME redactor… a narrowed
  application". That was true under 07-16; 07-22 falsified it by adding a hand-rolled `?`-only cut
  with different semantics AND by widening the query axis to every label. The function's own inline
  comment at `:311-314` already says it correctly.

Purpose. This is the first expansion of the tracer's pattern. It reuses 07-23's **region-scoped
comment gate** verbatim — non-vacuity companion, negative half, positive half, prefilter deliberately
inverted — against two regions of a different file, and adds one gate of its own: a file-wide count
that proves the export vocabulary did not grow while the prose about it was rewritten.

Output: a docblock whose principle is true of the branch it names, an exception a reviewer can find
without deriving it, and a column comment that matches the function beneath it.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sourcemap-reconstruction/07-CONTEXT.md
@.planning/phases/07-sourcemap-reconstruction/07-UAT.md
@.planning/phases/07-sourcemap-reconstruction/07-VERIFICATION.md
@.planning/phases/07-sourcemap-reconstruction/07-22-SUMMARY.md
@.planning/phases/07-sourcemap-reconstruction/07-23-SUMMARY.md
</context>

<gap_trace>
| Gap id | Source | What this plan closes |
|---|---|---|
| G-07-7 / test 3 | `07-UAT.md` gap `G-07-7` (minor); `07-VERIFICATION.md` finding `WR-03` | The principle at `:297-299` is scoped to the label branch, and the delegated branch's shared marker over a fragment-only URL is recorded as a known, accepted exception with its reason. |
| G-07-6 / test 2 | `07-UAT.md` gap `G-07-6` (minor); `07-VERIFICATION.md` finding `WR-02` | The `sources_verbatim` column comment is restated per axis and stops describing pre-07-22 behaviour. |
</gap_trace>

<artifacts_this_plan_produces>
## Artifacts this phase produces

**No new symbol of any kind.** No new file, no new export, no new constant, no new test name, no new
helper, no new dependency, no new vocabulary word. This plan changes comment text in one git-tracked
file (`packages/backend/src/store/export.ts`) and nothing else.

**Named gate shapes:**
- **the region-scoped comment gate** — inherited verbatim from `07-23-SUMMARY.md`. Cite it by that
  name. Applied here to two regions: the `redactSourceLabelForExport` docblock (bracketed by `The
  redacted form of one manifest` and the `export function redactSourceLabelForExport` declaration,
  which is 07-22's own anchor pair and is reused rather than re-derived) and the `sources_verbatim`
  column comment (bracketed by `WHAT DOES NOT SURVIVE` and `name: "sources_verbatim"`).
- **the vocabulary non-growth gate** — new here, and file-wide on purpose. `grep -c` over the literal
  marker text in `export.ts` prints **2** at HEAD: the declaration at `:178` and one illustrative use
  inside the docblock at `:265`. It must still print 2 after this plan. This is what makes "no new
  vocabulary" mechanically checkable while a paragraph ABOUT the vocabulary is being rewritten, and
  it is the reason the new exception paragraph refers to the marker through `{@link
  EXPORT_QUERY_REDACTION}` rather than spelling it.
- **the adjacent-text preservation gate** — new here. The quoted-by-reference withholding text at
  `:291-295` is OUT of scope and sits three lines above the passage this plan rewrites, so two
  positive `grep -c` checks pin it rather than trusting the edit's aim.
</artifacts_this_plan_produces>

<planner_assumptions>
## Spec-less edge-probe fallback — this plan's slice

The round-scoped record is `07-23-PLAN.md`'s `<planner_assumptions>` block: **16 applicable edges, 0
resolved, 16 unresolved**, all sixteen flagged there as explicit planner assumptions, **none**
authored as a `must_haves.truths` entry. **No-silent-drop equality holds at the round level: 16
surfaced == 16 flagged == 0 authored as truths.** Restated here are only the rows for the requirement
IDs this plan carries.

| # | Req | Category | Probe question | Flagged planner assumption |
|---|-----|----------|----------------|----------------------------|
| 14 | MAP-07 | empty | What is the result for empty, single-element, or null input? | Assumed settled: `NOTHING_TO_EXPORT_LABEL` is the shipped empty state and the UI-SPEC `manifest-export / empty` row is covered. **This plan changes no export behaviour**, so the row is carried forward unchanged and un-reprobed. |
| 15 | MAP-07 | encoding | Whose definition of length/equality applies — bytes, code points, grapheme clusters, or normalized form? | Assumed settled: the manifest field ceiling is 4,113 — a 4,096 code-point stored cap plus one 17-character marker. **This plan must not raise it, and cannot: it writes no code.** The vocabulary non-growth gate is the mechanical check that the marker half of that arithmetic did not move. |
| 16 | UI-05 | unclassified | unclassified — review manually | Reviewed manually: the viewer's state matrix is covered by `07-UI-SPEC.md`'s 35-row considerations table (32 covered, 2 backstop, 1 unresolved). The one unresolved row is `source-tree / overflow`, whose NUMBER (2,000) is a defensible reuse rather than a measurement — carried forward unchanged and untouched by this plan. |

## UI Considerations touched by this gap

One row, and only one, is genuinely touched — because these two gaps rewrite the prose describing the
redaction policy that governs the manifest column named in its resolution.

- **`long-text / manifest-export` — ✅ covered (explicit)** → lifted into `must_haves.truths` as a
  string, per the resolved-explicit rule. The property it asserts is unchanged by this plan and is
  re-asserted by it: no target-controlled string reaches a label, dialog or download name; the
  `sources` label appears only as a manifest column.

**Observation, recorded rather than fixed — this is NOT in this plan's scope and must not be edited
here.** That row's resolution text says the `sources` label is a column "where `redactUrlForExport`
governs it". Since 07-16 and 07-22 the governing function for that column is
`redactSourceLabelForExport`; `redactUrlForExport` governs only the protocol-shaped branch of it.
That is the same defect class as G-07-6, one artifact removed — a planning document rather than
source. `07-UI-SPEC.md` is outside this plan's `files_modified` and outside every gap's `artifacts`
list, so it is recorded in the SUMMARY as a deferred item with its suggested owner (whoever next
edits `07-UI-SPEC.md`), not corrected here.
</planner_assumptions>

<assumption_delta_decision>
The detector returned `detected: true` on `pluralization` cues harvested from round-2 PLAN prose, not
from a real second case.

- **Primary noun:** unchanged — one exported manifest column with one redaction policy, expressed as
  one marker applied on one or two axes depending on the label's shape.
- **Decision:** `no-change`. This plan adds no second redactor, no second marker and no second policy;
  it writes down that the ONE marker already reaches two branches with different cuts, which is a
  description of the shipped design rather than a change to it.
- No invariant test is suggested, because no generalisation occurred to encode. The invariant that
  would encode it — one marker, spelt once — already exists as the vocabulary non-growth gate.
</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: Scope the principle to its branch, and record the exception the other branch is</name>
  <reversibility rating="reversible">Comment text in one docblock, no behavioural change, no exported symbol. Reverting is a single-commit revert with no data, schema or contract implication.</reversibility>
  <files>packages/backend/src/store/export.ts</files>
  <read_first>
    - `packages/backend/src/store/export.ts:178` — the `EXPORT_QUERY_REDACTION` declaration. Read the literal so the vocabulary non-growth gate's count of 2 is a number you have confirmed rather than one this plan asserted.
    - `packages/backend/src/store/export.ts:185-203` — `redactUrlForExport`'s own docblock and body, including the `url.search(/[?#]/)` cut and the APPENDED-rather-than-dropped argument. **This is the branch that does not honour the principle, and it is the branch this plan must NOT change.** Read the regex character class carefully: the exception paragraph you write turns on it appending the query marker for a `#` hit.
    - `packages/backend/src/store/export.ts:255-310` — the whole `redactSourceLabelForExport` docblock. Read it end to end before editing three lines of it; the argument runs across the two-axes paragraph, the fragment-axis paragraph, the query-axis paragraph and the RFC 3986 population claim, and the passage you rewrite is the conclusion of all four.
    - `packages/backend/src/store/export.ts:291-295` — the operator's quoted-by-reference withholding text, ending with the closing quotation mark on line 295. **OUT OF SCOPE and adjacent.** The operator accepted it as discharging 07-22's task-1 criterion. It sits two lines above the passage this task rewrites, so read it precisely enough to know where your edit starts.
    - `packages/backend/src/store/export.ts:310-318` — the function itself and its inline comment (`the SAME marker, a narrower cut`). This is the correct summary; task 2's column comment must agree with it and this task's docblock must not contradict it.
    - `packages/backend/src/store/export.spec.ts:875-895` — the fragment-only URL case pinning `webpack:///./src/app.js#L5` as EXPECTED with the query marker. **Read it, cite its line numbers in the SUMMARY, do not edit it.** It is the evidence for the exception being recorded, and the gap's scope fence names it as evidence rather than as a file to change.
    - `.planning/phases/07-sourcemap-reconstruction/07-UAT.md` — gap `G-07-7`, whose `missing` list is the authoritative scope fence and whose third bullet is the prohibition this task is most likely to be overreached against.
    - `.planning/phases/07-sourcemap-reconstruction/07-23-SUMMARY.md` — the region-scoped comment gate as 07-23 recorded it, and the vitest test count it recorded, which is this plan's floor.
  </read_first>
  <behavior>
    - The principle sentence names the branch it governs before it states itself, so a reader cannot take it as a claim about the export as a whole.
    - A separate, clearly-headed paragraph records the delegated branch's behaviour as a KNOWN and ACCEPTED exception, states the specific input shape that triggers it, names the spec file that pins it, and gives the reason it is accepted: the marker discloses LESS than the truth, so the cost is operator trust and reviewability rather than disclosure.
    - That paragraph also records why it was not repaired — the repair needs a second vocabulary word and a change to `observations.url`'s shipped output, which the operator declined as needing its own round.
    - The marker is referred to through its `{@link}` and is not spelt again, so the count of the literal marker text in the file stays at 2.
    - The quoted-by-reference withholding text two lines above is byte-unchanged.
    - `redactSourceLabelForExport`'s body, `redactUrlForExport` and `isProtocolShapedLabel` are byte-unchanged.
  </behavior>
  <action>
Observe the gate RED first, then edit. Do NOT use `cp` to snapshot or restore anything in this plan —
`cp` is aliased to `cp -i` here and silently no-ops an overwrite; record the failing observation
before the change instead of trying to recreate it after.

**Step 1 — record the RED state.** Run these and paste the output into the SUMMARY verbatim:

- `awk '/The redacted form of one manifest/,/^export function redactSourceLabelForExport/' packages/backend/src/store/export.ts | grep -c 'NO NEW VOCABULARY'` prints `1` — the non-vacuity companion.
- `awk '/The redacted form of one manifest/,/^export function redactSourceLabelForExport/' packages/backend/src/store/export.ts | grep -cF 'A redaction that reports withholding'` prints `1` today and must print `0` after the edit.
- `grep -c 'query-redacted' packages/backend/src/store/export.ts` prints `2` today and must still print `2` after the edit.

<!-- planner-discipline-allow: A redaction that reports withholding -->

**Step 2 — replace the three principle lines.** Delete exactly the three comment lines that currently
begin the unqualified statement (they run from the capitalised sentence opener through `has no reason
to trust the next.`) and write in their place:

```
 * THAT PRINCIPLE IS STATED OF THIS BRANCH, NOT OF THE EXPORT AS A WHOLE. On the
 * label branch this function decides, a redaction that reports withholding
 * something that was never there is not a stronger redaction. It is an
 * unreliable one, and an operator who finds one marker they can prove is false
 * has no reason to trust the next. The scoping is deliberate: the delegated
 * branch does NOT honour it, and that exception is recorded immediately below
 * rather than papered over by a sentence that covers it silently.
```

The blank comment line above (` *`) and the operator's quoted withholding text above THAT are
untouched. Start your edit at the first line that is not part of the quotation.

**Step 3 — add the exception paragraph** immediately after the passage from step 2, separated by a
` *` line, and immediately BEFORE the existing `NO NEW VOCABULARY.` paragraph:

```
 * THE KNOWN AND ACCEPTED EXCEPTION, ON THE DELEGATED BRANCH.
 * {@link redactUrlForExport} cuts at the first `?` OR `#` and appends
 * {@link EXPORT_QUERY_REDACTION} for either hit, so a fragment-only URL such as
 * `webpack:///./src/app.js#L5` exports with a query marker over a value that has
 * no query axis. `export.spec.ts` pins exactly that as EXPECTED, and the column
 * comment on `sources_verbatim` names the same output as the wrong thing LO-04's
 * fix removed on the path branch. It is PRE-EXISTING `redactUrlForExport`
 * behaviour and it is `observations.url`'s shipped behaviour on every row it has
 * ever written.
 *
 * IT IS ACCEPTED RATHER THAN DEFENDED, and the cost is named so nobody has to
 * re-derive it: the marker discloses LESS than the truth, so what it costs is
 * OPERATOR TRUST AND REVIEWABILITY, not disclosure. Nothing leaks. Making the
 * marker true on both branches needs a second vocabulary word and a change to
 * `observations.url`'s shipped output — the operator declined that here as a
 * decision needing its own round rather than a drive-by (07-UAT.md G-07-7,
 * 2026-09-03).
```

**Do not spell the marker text anywhere in the new prose.** Refer to it through the `{@link}` or as
"a query marker". The count of the literal marker text in this file is 2 at HEAD — the declaration
and one illustrative use that predates this plan — and it must still be 2 afterwards. That count IS
the "spelt in exactly one place" property the existing `NO NEW VOCABULARY` paragraph claims, made
checkable.

**Step 4 — confirm the adjacent out-of-scope text survived.** `grep -cF 'A non-protocol label is cut
at the first' packages/backend/src/store/export.ts` must print `1`, and `grep -cF 'in the words the'`
must print `1`. Those two lines are the operator's accepted quotation; they are three lines from
where you edited and they must be byte-identical.
  </action>
  <acceptance_criteria>
    - **Source:** `awk '/The redacted form of one manifest/,/^export function redactSourceLabelForExport/' packages/backend/src/store/export.ts | grep -cF 'A redaction that reports withholding'` prints `0` — the sentence no longer opens unqualified.
    - **Source:** the same region contains `NOT OF THE EXPORT AS A WHOLE` at least once and `KNOWN AND ACCEPTED EXCEPTION` at least once.
    - **Source:** the same region contains `OPERATOR TRUST AND REVIEWABILITY` at least once — the stated reason, not merely the label "accepted".
    - **Source:** the same region still contains `NO NEW VOCABULARY` exactly once.
    - **Source:** `grep -c 'query-redacted' packages/backend/src/store/export.ts` prints `2`, unchanged from HEAD.
    - **Source:** `grep -cF 'A non-protocol label is cut at the first' packages/backend/src/store/export.ts` prints `1` and `grep -cF 'in the words the' packages/backend/src/store/export.ts` prints `1` — the operator's accepted quotation is intact.
    - **Source:** `git diff --unified=0 -- packages/backend/src/store/export.ts` shows no `+`/`-` line containing `url.search`, `isProtocolShapedLabel`, `label.indexOf` or `export function`.
    - **Behavior:** `packages/backend/src/store/export.spec.ts` reports the same 138 tests it reports at HEAD, all passing, and the file is unmodified.
  </acceptance_criteria>
  <verify>
    <automated>awk '/The redacted form of one manifest/,/^export function redactSourceLabelForExport/' packages/backend/src/store/export.ts | grep -c 'NO NEW VOCABULARY'</automated>
    <fails_when>the printed count is 0, or the command exits non-zero — the NON-VACUITY companion for the negated gate below. A zero means the awk range collapsed because the opening summary line or the function declaration moved, which would let the negated gate pass against an empty stream. This is 07-22's own anchor pair, reused rather than re-derived</fails_when>
    <automated>! awk '/The redacted form of one manifest/,/^export function redactSourceLabelForExport/' packages/backend/src/store/export.ts | grep -qF 'A redaction that reports withholding'</automated>
    <fails_when>non-zero exit, which here means the principle still opens as an unqualified claim about the export as a whole. Case-sensitive on purpose: the corrected passage carries the same words mid-sentence in lower case after naming the branch, and it is the sentence-initial capital that marks the unqualified form. Region-scoped and comment-stripping deliberately INVERTED, per 07-23's gate shape — the comment IS the subject</fails_when>
    <automated>awk '/The redacted form of one manifest/,/^export function redactSourceLabelForExport/' packages/backend/src/store/export.ts | grep -c 'KNOWN AND ACCEPTED EXCEPTION'</automated>
    <fails_when>the printed count is 0 — the positive half. A zero means the principle was narrowed without the exception being recorded, which leaves the reader with a scoped claim and no statement of what falls outside it: strictly worse than the unqualified sentence, because it now signals that an exception exists and hides which</fails_when>
    <automated>awk '/The redacted form of one manifest/,/^export function redactSourceLabelForExport/' packages/backend/src/store/export.ts | grep -c 'OPERATOR TRUST AND REVIEWABILITY'</automated>
    <fails_when>the printed count is 0 — the exception was recorded without its REASON. The gap's `missing` list requires the reason verbatim in substance: it discloses less than the truth, so the cost is trust and reviewability rather than disclosure. An exception labelled "accepted" with no stated cost is an admission, not a record</fails_when>
    <automated>grep -c 'query-redacted' packages/backend/src/store/export.ts</automated>
    <fails_when>the printed count is anything other than 2 — the vocabulary non-growth gate. 2 is the measured count at HEAD (the declaration at `:178` and one illustrative use at `:265`). A 3 means the new prose spelt the marker instead of linking it, growing the very thing the paragraph beside it claims is spelt in exactly one place; a 1 means the illustrative use or the declaration was deleted</fails_when>
    <automated>grep -cF 'A non-protocol label is cut at the first' packages/backend/src/store/export.ts</automated>
    <fails_when>the printed count is anything other than 1 — the adjacent-text preservation gate. This is the operator's quoted-by-reference withholding text, accepted as discharging 07-22's criterion and explicitly out of this round's scope. It sits two lines above the passage this task rewrites, so a rewrite that reached one line too far up would silently consume it</fails_when>
    <automated>pnpm vitest run packages/backend/src/store/export.spec.ts --reporter=dot</automated>
    <fails_when>non-zero exit, or the dot summary reports fewer than 138 tests — 138 is the count measured at HEAD. A failure in the fragment-only URL case means `redactUrlForExport` was changed instead of documented, which is the one repair the operator explicitly declined</fails_when>
    <automated>git diff --name-only -- packages/backend/src/store</automated>
    <fails_when>the output lists any path other than `packages/backend/src/store/export.ts`. A listed `export.spec.ts` means the pinned evidence was edited rather than cited</fails_when>
  </verify>
  <done>The principle names the label branch before stating itself, the delegated branch's marker is recorded as a known and accepted exception with its cost stated, the marker count is still 2, the operator's quoted text is intact, and `export.spec.ts`'s 138 tests pass unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Restate the column comment per axis, then run the repository gate</name>
  <reversibility rating="reversible">Comment text in one column declaration, no behavioural change. Reverting is a single-commit revert.</reversibility>
  <files>packages/backend/src/store/export.ts</files>
  <read_first>
    - `packages/backend/src/store/export.ts:398-432` — the whole `sources_verbatim` column comment from the D-06 evidence argument through the `WHAT DOES NOT SURVIVE` paragraph and the `Hence {@link redactSourceLabelForExport}` conclusion, ending at the `{ name: "sources_verbatim", redact: redactSourceLabelForExport },` entry. Read the whole comment; only its last sentence is wrong, and it is wrong as the conclusion of a correct argument.
    - `packages/backend/src/store/export.ts:310-318` — `redactSourceLabelForExport`'s signature, the delegating branch, the inline comment and the hand-rolled `?` cut. **This is the specification for the column comment.** The inline comment already says it correctly: the same marker, a narrower CUT. Note that a narrower cut and a wider AXIS are both true at once and the corrected column comment must say both without collapsing them.
    - `packages/backend/src/store/export.ts` — task 1's committed docblock, re-read as text. The column comment must not contradict what task 1 wrote three hundred lines above it.
    - `.planning/phases/07-sourcemap-reconstruction/07-UAT.md` — gap `G-07-6`, whose second `missing` bullet is the one most easily missed: the query axis was WIDENED, so "a narrowed application" is backwards on that axis and deleting the phrase without stating the direction leaves the reader no better off.
    - `.planning/phases/07-sourcemap-reconstruction/07-23-SUMMARY.md` — the vitest test count 07-23 recorded, which is this plan's full-suite floor. Do not use a number copied forward from this plan; use the one 07-23 measured.
  </read_first>
  <behavior>
    - The column comment states that ONE marker is applied per AXIS, and names both branches explicitly: a protocol-shaped label delegates and is cut on `?` or `#`; every other label is cut by hand at its first `?` and keeps its `#` tail.
    - It states the two axis directions separately and correctly: the FRAGMENT axis narrowed to protocol-shaped labels only, the QUERY axis WIDENED to every label.
    - It no longer describes the arrangement as one redactor applied more narrowly, because there are two cuts with different semantics.
    - It still refuses the per-column-exemption reading, which is the part of the original conclusion that remains true.
    - Nothing executable changes anywhere in the file.
  </behavior>
  <action>
**Step 1 — record the RED state:**

- `awk '/WHAT DOES NOT SURVIVE/,/name: "sources_verbatim"/' packages/backend/src/store/export.ts | grep -c 'WHAT DOES NOT SURVIVE'` prints `1` — the non-vacuity companion.
- `awk '/WHAT DOES NOT SURVIVE/,/name: "sources_verbatim"/' packages/backend/src/store/export.ts | grep -cF 'the SAME redactor'` prints `1` today and must print `0` after the edit.
- The same region piped to `grep -c 'WIDENED'` prints `0` today and must print at least `1` after the edit.

<!-- planner-discipline-allow: the SAME redactor -->

**Step 2 — replace the conclusion.** Delete the four comment lines beginning `// Hence {@link
redactSourceLabelForExport}:` through `// function.` and write in their place:

```
    // Hence {@link redactSourceLabelForExport}: the SAME MARKER, applied PER
    // AXIS. A protocol-shaped label DELEGATES to `redactUrlForExport` and is cut
    // on `?` or `#`; every other label is cut by hand at its first `?` and keeps
    // its `#` tail. Two cuts with different semantics, not one redactor reused —
    // and the two axes moved in OPPOSITE directions: the FRAGMENT axis NARROWED,
    // to protocol-shaped labels only, while the QUERY axis WIDENED, to every
    // label. Still not a per-column exemption, and the argument in full — with
    // the delegated branch's known exception — is at that function.
```

Keep the existing `    // ` prefix and the four-space indentation; the `WHAT DOES NOT SURVIVE`
paragraph above and the `{ name: "sources_verbatim", … }` entry below stay byte-unchanged.

**Step 3 — confirm nothing executable moved.** Every `+`/`-` line in `git diff --unified=0 --
packages/backend/src/store/export.ts` across BOTH tasks of this plan must be a comment line — either
`    // ` inside `EXPORT_COLUMNS` or ` * ` inside the docblock. A changed line that is not a comment
line means a statement moved.

**Step 4 — run the repository gate.** `workflow.use_worktrees` is false, so this round's three plans
run SEQUENTIALLY on the main working tree. 07-23 is committed by the time this plan runs, so the
full-suite floor is the count **07-23's SUMMARY recorded**, not a number this plan invented and not
the pre-round 4,321 beneath it. Read it from `07-23-SUMMARY.md` and state in this plan's SUMMARY both
the floor you used and where you read it, so 07-25 can retarget at whatever THIS plan records.

Record in the SUMMARY: the six region-probe outputs before and after across both tasks, the full
before/after text of all three rewritten passages, the marker count before and after, the two
adjacent-text preservation counts, the cited `export.spec.ts:875-895` line numbers, the confirmation
that `export.spec.ts` is unmodified, the deferred `07-UI-SPEC.md` observation from
`<planner_assumptions>` with its suggested owner, the full-gate numbers against 07-23's recorded
floor, and G-07-6 and G-07-7 marked closed with their commits.
  </action>
  <acceptance_criteria>
    - **Source:** `awk '/WHAT DOES NOT SURVIVE/,/name: "sources_verbatim"/' packages/backend/src/store/export.ts | grep -cF 'the SAME redactor'` prints `0`.
    - **Source:** the same region contains `WIDENED` at least once and `NARROWED` at least once — both directions stated, not one deleted.
    - **Source:** the same region contains `redactUrlForExport` at least once, so the delegating branch is named rather than implied.
    - **Source:** the same region still contains `WHAT DOES NOT SURVIVE` exactly once — the correct argument above the conclusion survived.
    - **Source:** every `+`/`-` line in `git diff --unified=0 -- packages/backend/src/store/export.ts` starts with a comment prefix after the diff marker. No such line contains `url.search`, `label.indexOf`, `export const`, `export function` or `redact:`.
    - **Test command:** `pnpm vitest run --reporter=dot` exits 0 with at least 90 files and no fewer tests than `07-23-SUMMARY.md` recorded.
    - **Test command:** `pnpm typecheck && pnpm lint && pnpm knip && pnpm build` exits 0.
    - **CLI output:** the SUMMARY names the numeric floor it used and the file it read it from.
  </acceptance_criteria>
  <verify>
    <automated>awk '/WHAT DOES NOT SURVIVE/,/name: "sources_verbatim"/' packages/backend/src/store/export.ts | grep -c 'WHAT DOES NOT SURVIVE'</automated>
    <fails_when>the printed count is 0, or the command exits non-zero — the NON-VACUITY companion for the negated gate below. A zero means one of the two anchors moved and the awk range collapsed, which would let the negated gate pass over an empty stream</fails_when>
    <automated>! awk '/WHAT DOES NOT SURVIVE/,/name: "sources_verbatim"/' packages/backend/src/store/export.ts | grep -qF 'the SAME redactor'</automated>
    <fails_when>non-zero exit, which here means the column comment still summarises the label column as one redactor reused. Region-scoped, though the phrase happens to be unique in this file at HEAD: scoping keeps the gate correct if a later plan writes the phrase somewhere it is true</fails_when>
    <automated>awk '/WHAT DOES NOT SURVIVE/,/name: "sources_verbatim"/' packages/backend/src/store/export.ts | grep -c 'WIDENED'</automated>
    <fails_when>the printed count is 0 — the positive half, and the bullet most easily missed. Deleting the false summary without stating that the query axis was widened leaves the reader with no description at all, and the gap's second `missing` bullet asks for the direction, not just the deletion</fails_when>
    <automated>awk '/WHAT DOES NOT SURVIVE/,/name: "sources_verbatim"/' packages/backend/src/store/export.ts | grep -c 'NARROWED'</automated>
    <fails_when>the printed count is 0 — the other direction. Both axes moved and they moved opposite ways; a comment that states only the widening is as incomplete as the one that stated only the narrowing</fails_when>
    <automated>grep -c 'query-redacted' packages/backend/src/store/export.ts</automated>
    <fails_when>the printed count is anything other than 2 — the vocabulary non-growth gate, re-run after the second edit because the column comment is another place a marker spelling could be introduced</fails_when>
    <automated>pnpm vitest run packages/backend/src/store/export.spec.ts --reporter=dot</automated>
    <fails_when>non-zero exit, or the dot summary reports fewer than 138 tests</fails_when>
    <automated>pnpm vitest run --reporter=dot</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 90 files, or a test count lower than the one `07-23-SUMMARY.md` recorded — 07-23 precedes this plan in the serialised chain and is committed by now, so its recorded count is the floor and the pre-round 4,321 is only the floor beneath it</fails_when>
    <automated>pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm knip &amp;&amp; pnpm build</automated>
    <fails_when>non-zero exit from any of the four. A comment-only change cannot move any of these, so ANY failure means something executable was edited; a knip report naming `export.ts` means a new export was introduced where only prose was authorised</fails_when>
    <automated>git diff --name-only</automated>
    <fails_when>the output lists any path other than `packages/backend/src/store/export.ts`, or lists nothing at all. `workflow.use_worktrees` is false, so all three plans of this round share one working tree; nothing at all means the edits were never made or were committed without the gates being run against the tree</fails_when>
    <automated>git diff --name-only -- packages/backend/src/store/export.spec.ts .planning/phases/07-sourcemap-reconstruction/07-UI-SPEC.md</automated>
    <fails_when>any output at all — `export.spec.ts` is the pinned evidence for the exception and `07-UI-SPEC.md` carries the deferred observation this plan records rather than fixes. Both are cited by this plan and edited by none of it</fails_when>
  </verify>
  <done>The column comment states one marker applied per axis, names both branches and both axis directions, no longer calls it one redactor reused, and the repository gate is green at or above 07-23's recorded floor with exactly one file modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → the exported manifest, in redacted mode | The operator chooses redacted mode BECAUSE the artifact is going to be shared. What that mode promises to withhold is the whole basis of the choice, and the docblock is where the promise is written down. |
| a target-controlled `sources` label → the exported column | The label is written by the scanned bundle. The redaction policy these comments describe is what stands between it and an artifact leaving the operator's machine. |
| repository prose → a redaction-policy audit | The column comment is the first thing a reviewer auditing this policy reads and the docblock is the second. A summary that describes retired behaviour sends the audit to the wrong conclusion faster than no summary would. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-105 | Repudiation | the never-claim-a-false-redaction principle stated of the whole export | high | mitigate | An operator who finds one marker they can prove false has no reason to trust the next — the file says so itself, and then supplies exactly such a marker on the delegated branch. Left unqualified, the principle makes every OTHER marker in the manifest less credible, which is a security-relevant loss on the artifact the operator shares. Task 1 scopes the principle to the branch that keeps it and records the other as a named, reasoned exception, so a reviewer who finds the fragment-only case finds it already documented instead of already denied. |
| T-07-106 | Tampering | the vocabulary growing while the prose about it is rewritten | high | mitigate | A second marker spelling would break the single-spelling property the manifest field ceiling of 4,113 is computed from, and would do it inside a comment edit nobody would think to check. The vocabulary non-growth gate pins the literal count at the measured 2 and is re-run after BOTH edits. |
| T-07-107 | Spoofing | the `sources_verbatim` column comment describing retired behaviour to an auditor | medium | mitigate | The comment attached to the column declaration is the entry point of a redaction-policy audit; describing a delegating-only arrangement when a second hand-rolled cut exists leads an auditor to verify one function and conclude about two. Task 2 restates it per axis, names both branches and both axis directions, and is gated on all four. |
| T-07-108 | Information Disclosure | an overreach that removes the query marker from the delegated branch | high | mitigate | Making the marker true on both branches means changing `redactUrlForExport`, which governs `observations.url` — a shipped column whose output would change for every row it has ever written. The operator declined that repair explicitly. Prohibitions forbid it, `export.spec.ts`'s fragment-only case is re-run unmodified, and the diff-shape criteria name `url.search` as a token that must not appear in the diff. |
| T-07-109 | Tampering | the operator's accepted quoted-by-reference text being consumed by an adjacent rewrite | medium | mitigate | It sits two lines above the passage task 1 rewrites and the operator accepted it as discharging 07-22's criterion. The adjacent-text preservation gate pins both of its lines by literal count rather than trusting the edit's aim. |
| T-07-110 | Information Disclosure | the new prose naming target-controlled data | low | accept | The exception paragraph names one synthetic label shape (`webpack:///./src/app.js#L5`) that already appears in `export.spec.ts` as a fixture. No captured body, no scanned target and no operator datum enters the source. |

No package-manager install task exists in this plan, so no supply-chain threat row applies. Blocking
threshold is `high`; T-07-105, T-07-106 and T-07-108 reach it and each is mitigated by a runnable
gate — a region-scoped positive assertion, a file-wide literal count pinned at a measured value, and
an unmodified 138-test spec run plus a diff-token blocklist — rather than by assertion.
</threat_model>

<verification>
- The region probes of both tasks, each recorded before and after, each flipping in the intended direction.
- `grep -c 'query-redacted' packages/backend/src/store/export.ts` — 2 before, 2 after, checked twice.
- The two adjacent-text preservation counts — 1 and 1, unchanged.
- `pnpm vitest run packages/backend/src/store/export.spec.ts --reporter=dot` — 138 tests, exit 0, spec file unmodified.
- `pnpm vitest run --reporter=dot` — no fewer than 90 files and no fewer tests than `07-23-SUMMARY.md` recorded, exit 0.
- `pnpm typecheck && pnpm lint && pnpm knip && pnpm build` — all exit 0.
- `git diff --name-only` — exactly `packages/backend/src/store/export.ts`. Always with a pathspec on the scoped checks: this phase runs without worktrees and the three plans of this round share one working tree.
- Read-only confirmation that `packages/backend/src/store/export.spec.ts` and `.planning/phases/07-sourcemap-reconstruction/07-UI-SPEC.md` are unmodified.
</verification>

<success_criteria>
- The principle names the label branch before stating itself, and the delegated branch is recorded as a known, accepted exception with its cost stated as operator trust and reviewability rather than disclosure.
- The `sources_verbatim` column comment states one marker applied per axis, names both branches, and states both axis directions — fragment narrowed, query widened.
- `redactUrlForExport`, the export vocabulary and `observations.url`'s output are provably unchanged; the marker literal count is still 2.
- The operator's quoted-by-reference withholding text is byte-unchanged.
- The repository gate is green at or above 07-23's recorded floor, with exactly one file modified.
- The `07-UI-SPEC.md` `long-text / manifest-export` wording drift is recorded as a deferred item with a suggested owner, not fixed here.
</success_criteria>

<output>
Create `.planning/phases/07-sourcemap-reconstruction/07-24-SUMMARY.md` when done.
Record: the region-probe outputs before and after for both tasks, the before/after text of all three
rewritten passages, the marker count and the two adjacent-text counts before and after, the cited
`export.spec.ts` line numbers, the confirmation that `export.spec.ts` and `07-UI-SPEC.md` are
unmodified, the deferred `07-UI-SPEC.md` observation with its suggested owner, the numeric full-suite
floor used and the file it was read from, the verbatim vitest summary line as 07-25's floor, and
G-07-6 and G-07-7 marked closed with their commits.
</output>
