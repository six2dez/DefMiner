---
phase: 07-sourcemap-reconstruction
plan: 15
type: execute
wave: 4
# 07-16 is a FILE-OWNERSHIP dependency, not a semantic one: this plan rewrites the
# `derivedRejected` docblock in `packages/backend/src/telemetry.ts` and 07-16 rewrites
# `snapshotCounters` in the same file. `workflow.use_worktrees` is false, so wave-parallel
# plans share one working tree and the only isolation available is ordering.
depends_on: ["07-12", "07-14", "07-16"]
gap_closure: true
nyquist_compliant: true
autonomous: true
requirements: [MAP-06, MAP-05]
files_modified:
  - packages/backend/src/ingest/consumer.ts
  - packages/backend/src/ingest/consumer.spec.ts
  - packages/backend/src/store/sources.ts
  - packages/backend/src/store/sources.spec.ts
  - packages/backend/src/telemetry.ts

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-13's depth refusal is counted and logged ONCE per reconstruction stage, not once per recovered source: one artifact carrying a 781-source map emits one refusal line, not 781."
    - "`counters.sourcemap.derivedRejected.depth_exceeded` is no longer equal by construction to `sourcesRecovered`, so the number carries information about the run rather than restating another counter."
    - "`telemetry.ts`'s `derivedRejected` docblock describes the meaning that ships: `depth_exceeded` counts reconstruction STAGES that declined to recurse, not recovered sources. No comment in the tree still describes the per-source meaning."
    - "MAP-06's aggregate bound is enforced by a production caller, in the same row unit `parseSourceMap` now uses, and the refusal is NAMED rather than silent."
    - "Re-ingesting an artifact that has already been fully recorded is ACCEPTED, never refused `too_many_sources`: the aggregate comparison is on the PROJECTED POST-WRITE row total, and after `v: 9`'s four-column upsert a re-ingest adds no rows."
    - "No docblock in `store/sources.ts` describes a caller that does not exist."
    - "`sources_verbatim` is truncated on a code-POINT boundary, so a stored label can never end in an unpaired surrogate."
  prohibitions:
    - "No new member of `MAP_PARSE_REASONS` or `DERIVED_REJECT_REASONS`. Both are closed vocabularies with every-reason-has-a-case gates; the aggregate refusal reuses the shipped reason."
    - "No change to `DERIVED_MAX_DEPTH` or to `SOURCE_ROWS_PER_MAP_MAX`. Plan 07-14 settled the second one's unit and this plan consumes that decision rather than revisiting it."
    - "No change to `packages/backend/src/store/migrations.ts` and no change to `SOURCES_LABEL_MAX`'s value. LO-03 is about the unit the cap is enforced in, not about the number."
    - "No change to `.planning/REQUIREMENTS.md`."
    - "No frontend file is modified. `derivedRejected` reaches no health surface — `SOURCEMAP_COUNTERS` in `packages/frontend/src/components/health-contract.ts` holds six rows and this is not one of them — so the counter's meaning is described in exactly one place and that place is `telemetry.ts`."
    - "No change to `snapshotCounters` or to any member of the `Counters` shape. 07-16 owns `telemetry.ts`'s behaviour; this plan owns one docblock in it."
    - "No work on W-4, W-6 or UAT gap 3."
  artifacts:
    - "packages/backend/src/ingest/consumer.ts — the depth gate hoisted to the call site, counted and logged once per stage"
    - "packages/backend/src/telemetry.ts — the `derivedRejected` docblock restated in the per-stage unit"
    - "packages/backend/src/store/sources.ts — `countSourcesForMap` with a production caller and a docblock that names it truthfully"
    - "packages/backend/src/store/sources.ts — the code-point truncation of `sources_verbatim`"
  key_links:
    - "consumer.ts reconstruction stage → sources.ts `countSourcesForMap` → the named refusal — MAP-06's aggregate half finally has an enforcer"
    - "consumer.ts per-source loop → `admitDerivedDepth` at the call site → telemetry.ts `derivedRejected` — one decision, one increment, one log line"
---

<objective>
Close three findings that all have the same shape: a claim stated in a docblock and a weaker thing
delivered in the code.

- **MD-03** — D-13's depth gate is re-taken inside `reconstruct`'s preamble for EVERY recovered source,
  so one artifact carrying monaco's 781-source map emits 781 identical `sdk.console.log` lines on the
  proxy thread and drives `derivedRejected.depth_exceeded` to exactly `sourcesRecovered`. A counter
  equal by construction to another counter carries no information, and a health surface reading
  "781 sources rejected: depth_exceeded" describes a bound working as designed as if it were a refusal.
- **MD-04** — `countSourcesForMap`'s docblock states MAP-06's aggregate enforcement as fact ("plan
  07-05's ingest path compares the count and refuses with a reason"). It has no production caller. The
  aggregate half of MAP-06, across repeated ingests, is enforced nowhere.
- **LO-03** — `sources_verbatim` is cut at code unit 4,096, so a label whose 4,095th code unit is a
  high surrogate stores an unpaired surrogate: an invalid UTF-8 sequence in a SQLite `TEXT` column that
  round-trips through the driver and the RPC boundary unpredictably.

Purpose: MAP-06 is a ticked requirement, and its ledger row currently rests on a bound that does not
exist. That is the exact failure mode MD-04 names — a docblock a verifier will cite.

Output: one refusal per stage, an enforced aggregate, and a stored label that is always valid UTF-8.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sourcemap-reconstruction/07-CONTEXT.md
@.planning/phases/07-sourcemap-reconstruction/07-PATTERNS.md
@.planning/phases/07-sourcemap-reconstruction/07-REVIEW.md
@.planning/phases/07-sourcemap-reconstruction/07-05-SUMMARY.md
@.planning/phases/07-sourcemap-reconstruction/07-12-SUMMARY.md
@.planning/phases/07-sourcemap-reconstruction/07-14-SUMMARY.md
</context>

<gap_trace>
| Gap id | Source | What this plan closes |
|---|---|---|
| MD-03 | `07-REVIEW.md` MEDIUM | The depth decision moves to the call site: one count, one log line, per stage. |
| MD-04 | `07-REVIEW.md` MEDIUM | MAP-06's aggregate bound gets a production caller and a named refusal. |
| LO-03 | `07-REVIEW.md` LOW | The label cap is enforced in code points. |
</gap_trace>

<artifacts_this_plan_produces>
**Changed control flow**
- `packages/backend/src/ingest/consumer.ts` — `admitDerivedDepth` is evaluated once, ABOVE the
  per-source loop, on `input.depth + 1`. The counter increment and the log line move with it. The gate
  inside `reconstruct` STAYS, because it is the real bound; it simply stops being the path every source
  takes.

**Changed comment, in a file another plan owns**
- `packages/backend/src/telemetry.ts` — the `derivedRejected` docblock only. Declared in
  `files_modified` because an action clause that edits a file the frontmatter does not name is
  invisible to the wave overlap guard. 07-16 rewrites `snapshotCounters` in this same file, which is
  why this plan depends on it and runs in a later wave rather than beside it.

**New production call**
- `packages/backend/src/ingest/consumer.ts` calls `countSourcesForMap` before the per-source loop and
  refuses past `SOURCE_ROWS_PER_MAP_MAX`, in the row unit plan 07-14 established, on the PROJECTED
  POST-WRITE row total rather than on `existing + declared`.

**Changed signature**
- `countSourcesForMap(db, projectId, artifactSha256, mapSha256)` — the aggregate bound is about what
  ONE map-bearing artifact writes, and after migration `v: 9` the same map in two bundles legitimately
  has two sets of sightings.

**Changed truncation**
- `packages/backend/src/store/sources.ts` — `sources_verbatim` is cut on a code-point boundary.
  `SOURCES_LABEL_MAX` keeps its value of 4,096; only the unit it is applied in changes, and the
  docblock states the unit.

**No new vocabulary member. No new counter. No new file.** Every path in `files_modified` is
git-tracked.
</artifacts_this_plan_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: TRACER — MD-03, one map-bearing artifact, one depth refusal</name>
  <files>packages/backend/src/ingest/consumer.ts, packages/backend/src/ingest/consumer.spec.ts, packages/backend/src/telemetry.ts</files>
  <read_first>
    - `packages/backend/src/ingest/consumer.ts:1039-1082` — `reconstruct`'s signature and its depth-gate preamble: the "COUNTER-LOG-RETURN, in the shape the authoritative size gate uses" comment, the reason the refusal is NOT propagated as a `reason`, and the T-07-31 argument for why the bound is one level with no re-entry.
    - `packages/backend/src/ingest/consumer.ts:1265-1300` — the recursion call site: `derivedVisit`, the "THE DEADLINE IS THE ARTIFACT'S, NOT A FRESH ONE" comment, the `reconstruct(..., depth: input.depth + 1, ...)` call, and the `rowsInserted` / `sliceMs` accumulation that follows it.
    - `packages/backend/src/ingest/consumer.ts:1160-1180` — the OTHER `derivedRejected` increment (`admitted.reason`), so the two are not confused and the counter's members stay distinguishable.
    - `packages/backend/src/sourcemap/derive.ts` — `admitDerivedDepth`, `DERIVED_MAX_DEPTH`, `DERIVED_REJECT_REASONS` and the sibling-vocabulary essay. The gate's semantics must not change; only where it is asked.
    - `packages/backend/src/sourcemap/derive.spec.ts` — the every-reason-has-a-case gate over `DERIVED_REJECT_REASONS`. `depth_exceeded` must stay reachable and observed.
    - `packages/backend/src/telemetry.ts:299-313` — the `derivedRejected` docblock ("Refusals on the DERIVED-ARTIFACT path, by reason", the sub-map-not-a-widening argument, the derivation from the frozen array) and the `Record<DerivedRejectReason, number>` member it sits above. This is the ONE place the counter's meaning is written down, and this task edits the docblock only.
    - `packages/backend/src/telemetry.ts` as 07-16 leaves it — that plan rewrites `snapshotCounters` in this same file and lands first by dependency. Read the file at its post-07-16 state before editing, so the docblock edit does not land on a stale copy.
    - `packages/backend/src/sourcemap/derive.ts:60-80` — the "SEPARATE PRESENTATION MAPS" clause naming `counters.sourcemap.derivedRejected`. It argues about which SUB-MAP the reasons live in, not about what `depth_exceeded` counts, so it is read to confirm it needs NO edit — record that you confirmed it.
    - `packages/backend/src/ingest/consumer.spec.ts` — the three existing D-13 cases asserting `derivedRejected.depth_exceeded` at 0, 1 and 2. Their expectations change and each change must be argued in the case's own message.
    - `.planning/phases/07-sourcemap-reconstruction/07-REVIEW.md:353-386` — MD-03 in full, including its fix sketch and the instruction to KEEP the gate inside `reconstruct` as well.
  </read_first>
  <behavior>
    - One artifact carrying an inline map with N recovered sources produces exactly ONE `depth_exceeded` increment and exactly ONE refusal log line, for any N greater than 1.
    - `derivedRejected.depth_exceeded` is no longer equal to `sourcesRecovered` on that run.
    - The bound itself is unchanged: no recovered source is analysed at depth 2, and `reconstruct` still refuses if called at or past `DERIVED_MAX_DEPTH` by any other route.
    - The walk of each recovered source at depth 1 still happens, with the artifact's own deadline, exactly as today.
  </behavior>
  <action>
Ask the depth question where the decision is made, once, and stop asking it once per source.

**Hoist the gate to the call site.** Immediately above the per-source loop, evaluate
`admitDerivedDepth` on the depth the loop WOULD recurse at — `input.depth + 1` — and hold the answer.
When it refuses, increment `derivedRejected` and emit the log line ONCE, there, with a message naming
the stage and the source count it applies to rather than a single source. When it admits, the loop
recurses as it does today.

**Keep the gate inside `reconstruct`.** It is the real bound and it must stay reachable by any future
caller; MD-04's whole lesson is that a bound whose enforcement lives only at one call site is a bound
somebody removes by adding a second call site. What changes is that the ordinary path no longer runs
through it. Leave its counter increment and its log in place for that case and say, in the comment,
that the hot path is now gated above and this arm is the backstop.

**Rewrite the counter's meaning in the one file that states it, and prove there is no second one.**
Before this change `depth_exceeded` was the number of recovered sources on map-bearing artifacts;
after it, it is the number of reconstruction stages that declined to recurse. Edit the
`derivedRejected` docblock in `packages/backend/src/telemetry.ts` so it says the second thing,
naming the unit explicitly and naming this plan as the change that moved it.

That file is the ONLY edit this clause authorises, and the scope was measured rather than assumed:

- `grep -rn 'derivedRejected' packages --include=*.ts --include=*.vue | grep -v spec` returns four
  lines — the `telemetry.ts` declaration, the `telemetry.ts` initialiser, `derive.ts`'s
  separate-presentation-maps clause, and the two `consumer.ts` increment sites. The `derive.ts` clause
  is about which SUB-MAP the reasons live in and is unaffected; confirm by reading and record it.
- `grep -rn 'derivedRejected\|depth_exceeded' packages/frontend/src` returns NOTHING. The health
  surface's `SOURCEMAP_COUNTERS` in `packages/frontend/src/components/health-contract.ts` carries six
  rows — `announcedInline`, `announcedExternal`, `mapRefusedTooLarge`, `mapMalformed`,
  `sourcesRecovered`, `sightingsRecorded` — and this counter is not one of them. **There is no
  health-surface copy to rewrite.** Do not open a `.vue` file for this clause.

Run both greps before editing. If either returns more than the above, STOP and record it in the
SUMMARY rather than reaching into a file this plan does not declare — `telemetry.ts` is shared with
07-16 and the frontend is shared with 07-11 and 07-17.

**The three existing D-13 cases.** Their asserted values change from per-source to per-stage. Update
each with a message that states the OLD expectation and why it changed, in the register
`consumer.spec.ts` already uses for `retentionSweeps` ("THIS EXPECTATION CHANGED FROM 2 TO 3, AND THE
CHANGE IS THE POINT"). A silently-edited expectation is indistinguishable from a weakened one.

**Add the case MD-03's failure scenario names**: an artifact whose map recovers many sources, asserting
exactly one increment and — this is the half that makes it a regression test rather than an arithmetic
check — asserting the refusal log line was emitted once. Capture the log through whatever seam
`consumer.spec.ts` already uses for log assertions rather than adding a new one.
  </action>
  <acceptance_criteria>
    - A `consumer.spec.ts` case ingests one artifact whose map recovers at least 10 sources and asserts `derivedRejected.depth_exceeded` equals 1.
    - The same case asserts the refusal log line appears exactly once.
    - A case asserts `derivedRejected.depth_exceeded` is NOT equal to `sourcesRecovered` on that run — the inequality is the property MD-03 is about.
    - The three pre-existing D-13 cases carry a message naming their old expected value and the reason it changed.
    - `derive.spec.ts`'s every-reason-has-a-case gate over `DERIVED_REJECT_REASONS` is green and `depth_exceeded` is still observed.
    - The RED text observed for the one-increment case against the shipped code is recorded in the SUMMARY — it should report the source count where 1 was expected.
    - No recovered source is analysed at depth 2; the existing bound case is unchanged and green.
    - `telemetry.ts`'s `derivedRejected` docblock states the per-STAGE meaning for `depth_exceeded` and names this plan. The `Counters` shape is byte-unchanged: `git diff -U0 packages/backend/src/telemetry.ts` shows changed lines only inside a comment block, and every added line begins with `*` or `/*` after leading whitespace.
    - No frontend file is touched by this task: `git diff --name-only -- packages/frontend` prints nothing.
    - The two scope greps and their outputs are recorded in the SUMMARY, including the confirmation that `derive.ts`'s clause needed no edit.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run packages/backend/src/ingest/consumer.spec.ts --reporter=dot</automated>
    <fails_when>non-zero exit; a `depth_exceeded` assertion reporting the source count instead of 1 means the gate is still inside the callee's preamble on the hot path</fails_when>
    <automated>pnpm vitest run packages/backend/src/telemetry.spec.ts packages/backend/src/sourcemap/derive.spec.ts --reporter=dot</automated>
    <fails_when>non-zero exit — a counter-shape failure in `telemetry.spec.ts` means the docblock edit reached the `Counters` member beneath it; an every-reason-has-a-case failure in `derive.spec.ts` means `depth_exceeded` stopped being observed</fails_when>
    <automated>git diff --name-only -- packages/frontend</automated>
    <fails_when>the command prints any line — this task's telemetry clause is scoped to `telemetry.ts` and the health surface names this counter nowhere</fails_when>
  </verify>
  <done>A map-bearing artifact emits one refusal, and the counter beside it says something the other counters do not.</done>
  <reversibility rating="reversible">A hoisted predicate. The bound itself is unmoved and still enforced in two places.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MD-04 — give MAP-06's aggregate bound the caller its docblock claims</name>
  <files>packages/backend/src/store/sources.ts, packages/backend/src/store/sources.spec.ts, packages/backend/src/ingest/consumer.ts, packages/backend/src/ingest/consumer.spec.ts</files>
  <read_first>
    - `packages/backend/src/store/sources.ts:425-450` — `countSourcesForMap`, `COUNT_SOURCES_FOR_MAP_SQL`, and the docblock beginning "THE BOUND IS ENFORCED BY THE CALLER, AND THE REFUSAL IS NAMED", which is the claim this task makes true.
    - `packages/backend/src/ingest/consumer.ts:1080-1200` — the reconstruction stage from the whole-payload stretch through the per-source loop, so the count lands before the loop and inside the same epoch discipline as the writes that follow.
    - `packages/backend/src/ingest/consumer.ts` — `mapRefusalCode` and the `map:`-namespaced reason path that records `analyses.scan_state = 'partial'` under D-11. The aggregate refusal must travel that path, not invent a second one.
    - `packages/engine/src/sourcemap/parse.ts` — `MAP_PARSE_REASONS` and the `too_many_sources` member as plan 07-14 leaves it, and the row unit that plan established.
    - `packages/engine/src/thresholds.ts:417-441` — `SOURCE_ROWS_PER_MAP_MAX` after plan 07-14, so the comparison is written in the settled unit.
    - `packages/backend/src/store/migrations.ts` step `v: 9` — the four-column key, which is why the count must be scoped by artifact AND why a re-ingest writes zero new rows.
    - `packages/backend/src/store/sources.ts:112-135` — `RECORD_SIGHTING_SQL` as plan 07-12 leaves it: the four-column `ON CONFLICT` target and the `DO UPDATE SET` arm. Read the UPDATE arm specifically; it is the proof that a repeated ingest updates in place rather than inserting, which is the whole basis for `max` over `+`.
    - `packages/backend/src/ingest/consumer.ts:885-895` — the `isAnalysed(db, projectId, sha256, detectorSetHash)` short-circuit. Re-analysis is reachable whenever the detector-set hash changes, so "the same artifact reaches the reconstruction stage twice" is the ordinary path and not a corner case.
    - `knip.json` — the entry globs, including `src/**/*.spec.ts`, which is why `knip` did not catch a spec-only consumer. Confirm the wiring does not create a new knip exemption.
    - `.planning/phases/07-sourcemap-reconstruction/07-REVIEW.md:387-405` — MD-04 in full, including its explicit either/or and the "worse than no docblock" closing.
  </read_first>
  <precondition>Migration `v: 9` has landed: `SCHEMA_VERSION` evaluates to 9 and `RECORD_SIGHTING_SQL`'s `ON CONFLICT` target names all four key columns. The whole `max`-not-`+` argument rests on the four-column upsert writing zero rows on a repeat; under the three-column key it is false. If 07-12's checkpoint answered C or D, HALT and return to the orchestrator rather than scoping the count by an artifact column that is not in the key.</precondition>
  <behavior>
    - The quantity compared is the PROJECTED POST-WRITE sighting count for this `(artifact, map)` — `max(existing, recovered)` — and never `existing + recovered`.
    - A map whose projected post-write row cost would exceed `SOURCE_ROWS_PER_MAP_MAX` is REFUSED before the per-source loop, with the shipped `too_many_sources` reason recorded through the existing `map:`-namespaced path.
    - The refused artifact finishes `partial` with a named reason, not `done` and not silently.
    - A map at exactly the bound is accepted.
    - **Re-analysing an artifact whose sightings are already fully recorded is ACCEPTED.** Re-analysis is reachable whenever `detectorSetHash` changes — `consumer.ts`'s `isAnalysed` short-circuit is keyed on it, and Phase 3 landing is exactly that event. The projected total for an unchanged artifact equals what is already stored, so it is accepted on exactly the terms it was accepted on the first time.
    - The count is scoped by artifact: the SAME map in a second bundle starts from zero, because after `v: 9` that second bundle's sightings are its own.
    - Exactly one `COUNT(*)` runs per map-bearing artifact, not one per source.
  </behavior>
  <action>
Wire the counter, in the unit plan 07-14 settled, and rewrite the paragraph so it describes what ships.

**The decision, and why it went this way rather than the other.** MD-04 offers two exits: wire the
function, or delete it and rewrite the paragraph to state where the bound actually lives. Wire it.
MAP-06 is a TICKED requirement whose aggregate half is currently enforced nowhere, so deleting the
function would leave the ledger row resting on a bound that does not exist — the same defect one layer
down. The cost is one bounded, indexed `COUNT(*)` per map-bearing artifact, which is the same order as
the no-op retention sweep the A8 measurement put at roughly 0.28 ms per artifact. Record this reasoning
in the SUMMARY, because a later reader will find the alternative in the review file and should not have
to guess which way it went.

**`countSourcesForMap` takes the artifact digest.** After migration `v: 9` a sighting belongs to a
bundle, and the bound MAP-06 states is about what one map-bearing artifact writes. Scope the statement
and the signature accordingly, `project_id` still first in the `WHERE`.

**The call site, and the arithmetic, stated precisely because the obvious form is wrong.** In
`consumer.ts`, before the per-source loop and after the map parses, read the existing sighting count
for this `(artifact, map)` once. Then compare the PROJECTED POST-WRITE state against
`SOURCE_ROWS_PER_MAP_MAX`, in the row unit plan 07-14 settled:

- `existing` = `countSourcesForMap(db, projectId, artifactSha256, mapSha256)`.
- `recovered` = the number of sightings this parse will write.
- `projectedSightings` = `max(existing, recovered)`.
- `projectedRows` = `2 * projectedSightings`, the same two-rows-per-recovered-source relationship
  07-14's gate uses — one `sources` row per new content hash and one `source_sightings` row per
  `(map, index)`. It is a WORST case in the `sources` half, because a source whose content hash is
  already stored writes no `sources` row; say so in the comment rather than letting a reader read it
  as exact.
- Refuse iff `projectedRows > SOURCE_ROWS_PER_MAP_MAX`.

**Why `max` and not `+`, written into the comment at the call site.** After migration `v: 9`
`recordSighting` upserts on `(project_id, artifact_sha256, map_sha256, source_index)`, so re-ingesting
the same artifact writes ZERO new rows — it updates in place. `existing + recovered` counts the same
rows twice. And `map_sha256` is content-addressed over the decoded map JSON, so the stored index set
for one `(artifact, map)` is drawn from the same `sources` array this parse is reading: the stored set
is a subset of the set about to be written, which makes the new-row count exactly
`max(0, recovered - existing)` and the projected total exactly `max(existing, recovered)`.

**The failure the `+` form produces, so an executor recognises it in review.** `SOURCE_ROWS_PER_MAP_MAX`
is 2,048. Under `existing + recovered`, any map already holding more than 1,024 sightings — well inside
what 07-14's row-unit gate admits — computes over 2,048 on the next pass and is refused
`too_many_sources`, writing `scan_state = 'partial'` on an artifact that was previously accepted whole.
Re-analysis is reachable at every detector-set change, so this is the ordinary path, not a corner. The
monaco 781-source case is squarely in the band. Under `max` the same artifact projects the number it
already holds and is accepted.

Refuse with the shipped `too_many_sources` reason, through the existing `mapRefusalCode` /
`map:`-namespaced path so the artifact records `partial` with a reason the operator can read. Do not
add a vocabulary member: both vocabularies are closed with every-reason-has-a-case gates.

Re-check the project epoch immediately before the count, in the idiom the five shipped `stillCurrent()`
sites use. A count taken under one project and acted on under another is exactly the class of thing
those five sites exist to prevent.

**Rewrite the docblock, and state what the bound actually bounds after `v: 9` rather than what it
bounded before.** It currently asserts a caller in plan 07-05's ingest path. Name the caller that
exists, name the unit the comparison is in, name the reason it refuses with, and add the honest
paragraph:

> Under the four-column key a `(project, artifact, map)` can hold at most as many sightings as the map
> declares distinct source indices, and `parseSourceMap`'s row gate already bounds that at parse time.
> So this check is not the only bound and it is not a bound the parse gate lacks — it is the SECOND
> enforcement point, taken in the store's unit against what is actually on disk. It catches divergence
> between what the parse gate projected and what the write path produced, including rows written by an
> earlier build under a different gate, and it is what makes MAP-06's aggregate half a thing a verifier
> can execute rather than a sentence a docblock asserts.

Say plainly that this is a defence-in-depth check, because a docblock claiming to be the sole enforcer
of a bound something else already enforces is the same defect MD-04 names, one layer over.

Keep the paragraph's closing argument — that a map which silently wrote 2,048 of its 2,049 sources is
indistinguishable from a map that had 2,048 — because that argument is why the refusal is named.

**Note what does NOT change.** A legacy database holding a map recorded under the pre-07-14
source-unit gate is refused at `parseSourceMap`, by 07-14's decision, before this check ever runs. This
plan adds no refusal that plan did not already make; record that in the SUMMARY so a later reader does
not attribute it here.

**Cases.** `sources.spec.ts`: the artifact-scoped count, including the two-bundle case proving the same
map in a second bundle starts from zero. `consumer.spec.ts`: the over-bound refusal reaching `partial`
with the named reason, the at-bound acceptance, and an assertion that exactly one count statement runs
per map-bearing artifact rather than one per source.

**The re-analysis case, and it is the one that pins B2.** `consumer.spec.ts`: ingest one artifact
carrying a map with well over 1,024 recovered sources, let it complete `done`, then re-analyse the SAME
artifact bytes under a DIFFERENT `detectorSetHash` so `isAnalysed` does not short-circuit. Assert the
second pass finishes `done` and NOT `partial`, that no `too_many_sources` reason is recorded, and that
the sighting row count for that `(artifact, map)` is unchanged across the two passes — the last clause
is what proves the acceptance came from the upsert writing nothing rather than from the fixture being
small. Write the case's message so it names the arithmetic it guards: that `existing + recovered` would
refuse here and `max(existing, recovered)` does not.
  </action>
  <acceptance_criteria>
    - `grep -rn 'countSourcesForMap' packages/backend/src --include=*.ts | grep -v spec` returns at least 2 lines — the declaration and a production call site. Before this task it returns 1.
    - `consumer.spec.ts` carries an over-bound case asserting `scan_state` is `partial` and the recorded error carries the `map:` namespace prefix and the shipped reason.
    - `consumer.spec.ts` carries an at-bound case asserting acceptance.
    - `consumer.spec.ts` carries the re-analysis case: the same artifact re-analysed under a changed `detectorSetHash` finishes `done`, records no `too_many_sources`, and leaves the `(artifact, map)` sighting count identical to the first pass.
    - The comparison is on the projected post-write total, taken with a maximum rather than a sum: `grep -v -E '^\s*(//|\*|/\*)' packages/backend/src/ingest/consumer.ts | grep -c 'Math.max('` returns 2. It returns 1 at HEAD — measure it before the edit and record both numbers, because the criterion is the DELTA and a bare count would drift with any unrelated use.
    - `sources.spec.ts` carries a case proving the count is artifact-scoped: the same map in a second bundle counts zero.
    - A case asserts the count runs once per map-bearing artifact; instrument through the fixture database rather than by adding a counter.
    - `MAP_PARSE_REASONS` and `DERIVED_REJECT_REASONS` are byte-unchanged, and both every-reason gates are green.
    - `pnpm knip` exits 0 and no new knip ignore entry was added; confirm by `git diff --stat knip.json` printing nothing.
    - `countSourcesForMap`'s docblock names its production caller and the unit of its comparison.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run packages/backend/src/store/sources.spec.ts packages/backend/src/ingest/consumer.spec.ts --reporter=dot</automated>
    <fails_when>non-zero exit; an every-reason-has-a-case failure means a vocabulary member was added or made unreachable; a re-analysis case reporting `partial` where `done` was expected means the comparison is still additive and is refusing a write that adds no rows</fails_when>
    <automated>pnpm vitest run packages/backend/src/store/sql-discipline.spec.ts --reporter=dot</automated>
    <fails_when>non-zero exit — the widened count statement must keep `project_id` first in its predicate</fails_when>
    <automated>git diff --stat knip.json</automated>
    <fails_when>the command prints any line — wiring a real caller must not need a dead-code exemption</fails_when>
  </verify>
  <done>MAP-06's aggregate bound has an enforcer, and the docblock that claimed one is describing it.</done>
  <reversibility rating="reversible">One added read and one added refusal branch on an existing reason.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: LO-03 — cut the stored label on a code-point boundary, and say which unit</name>
  <files>packages/backend/src/store/sources.ts, packages/backend/src/store/sources.spec.ts</files>
  <read_first>
    - `packages/backend/src/store/sources.ts:60-70` — `SOURCES_LABEL_MAX` and its justification, which argues from `observations.url`'s `URL_MAX` precedent and reasons about 4,096 relative to the 1,024-grapheme display caps.
    - `packages/backend/src/store/sources.ts:355-380` — `recordSighting`'s "TRUNCATED, NEVER TRANSFORMED" comment and the `slice` it describes as the only thing done to this value on the whole write path. That claim must survive: a code-point cut is still a truncation and still not a transformation, and the comment should say so explicitly.
    - `packages/engine/src/sourcemap/map-fixture.ts` — the hostile label corpus and its fixture-discipline header requiring control characters to be written as escapes. The new fixture involves a surrogate pair and must honour it.
    - `packages/backend/src/store/schema.spec.ts` — the `sources_verbatim` `COLUMN_ALLOWLIST` justification, which argues the column's safety rests on R1/R2 at render. Confirm nothing there needs an edit, and record that you confirmed it.
    - `.planning/phases/07-sourcemap-reconstruction/07-REVIEW.md:448-466` — LO-03 in full, including its note that 4,096 code units is up to ~16 KB of stored bytes for astral characters, so the at-rest bound is not the bound the argument computes.
  </read_first>
  <behavior>
    - A label whose 4,096th code unit is a high surrogate is stored WITHOUT an unpaired surrogate: the stored value round-trips through the driver and equals what was written.
    - A label of 4,096 or fewer code points is stored whole.
    - A label of ASCII characters longer than the cap is cut at exactly the cap, unchanged from today.
    - Nothing else is done to the value: no trim, no normalise, no decode, no sanitiser.
  </behavior>
  <action>
Enforce the cap in code POINTS so the stored prefix is never half a surrogate pair, and make the
docblock state the unit it is enforced in.

Replace the code-unit `slice` with a code-point-bounded cut. Keep it a pure prefix — the value is
evidence under D-06 and the write path's one rule is that it is truncated and never transformed.

Update `SOURCES_LABEL_MAX`'s justification with the two things LO-03 says it is missing: the UNIT the
cap is enforced in, and the honest byte range that unit implies. 4,096 code points of astral characters
is a larger at-rest footprint than the paragraph's current argument computes, and stating the range is
what makes the bound reviewable. Do not change the number — the operator approved a column whose cap is
4,096 and this task changes the unit, not the budget.

Update the "TRUNCATED, NEVER TRANSFORMED" comment so a reader can see that a code-point cut is still a
truncation. That comment is load-bearing: it is the reason D-06's write-time-sanitisation refusal is
checkable.

**The fixture.** Add a `sources.spec.ts` case whose label's 4,096th code unit is a high surrogate, with
the surrogate written as an escape sequence and never as a literal, and assert the stored value
contains no unpaired surrogate. Assert it by re-reading the row through the store rather than by
inspecting the string that was passed in — the defect LO-03 names is about what survives the driver and
the RPC boundary, not about what the function returns.

Add the two boundary cases: a label of exactly the cap in code points stored whole, and a label of ASCII
longer than the cap cut at the cap.

Confirm and record that the `COLUMN_ALLOWLIST` justification for `sources_verbatim` needs no edit.
  </action>
  <acceptance_criteria>
    - `sources.spec.ts` carries a case whose label's 4,096th code unit is a high surrogate and asserts the re-read stored value contains no unpaired surrogate.
    - That case is RED against the shipped `slice`; record the observed stored value's final code unit.
    - Two boundary cases pass: exactly-at-cap stored whole, and ASCII-over-cap cut at the cap.
    - `SOURCES_LABEL_MAX` still evaluates to 4,096; `grep -v -E '^\s*(//|\*|/\*)' packages/backend/src/store/sources.ts | grep -c 'SOURCES_LABEL_MAX = 4096'` returns 1.
    - The docblock names the unit and the implied byte range.
    - No literal surrogate, C0 or C1 byte is added to any file: `grep` does not report `packages/backend/src/store/sources.spec.ts` as binary.
    - `schema.spec.ts` is byte-unchanged; `git diff --stat packages/backend/src/store/schema.spec.ts` prints nothing.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run packages/backend/src/store/sources.spec.ts --reporter=dot</automated>
    <fails_when>non-zero exit, or the surrogate case absent from the printed test names</fails_when>
    <automated>pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm knip</automated>
    <fails_when>non-zero exit from any of the three</fails_when>
    <automated>pnpm vitest run packages/backend/src/outbound-prohibition.spec.ts --reporter=dot</automated>
    <fails_when>non-zero exit — a failure means `.planning/REQUIREMENTS.md` was touched, which this plan prohibits</fails_when>
  </verify>
  <done>A stored label is always valid UTF-8, and the cap says which unit it counts.</done>
  <reversibility rating="reversible">A truncation unit. Rows already stored with a lone surrogate are not rewritten — note that in the SUMMARY.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recovered source content → the reconstruction stage | A recovered source is target-controlled bytes that re-enter the pipeline; D-13's depth bound is what stops that being unbounded work. |
| target-declared `sources` label → SQLite `TEXT` | `sources_verbatim` is stored verbatim under D-06 and crosses the driver and the RPC boundary. |
| map's declared source count → row volume | The aggregate bound decides how many rows one map can accumulate across ingests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-61 | Denial of Service | D-13's depth gate reached per recovered source | medium | mitigate | The gate is evaluated once above the per-source loop, so one map-bearing artifact emits one refusal log line instead of one per source. The bound itself is unmoved and is still enforced inside `reconstruct` as a backstop for any other caller. Asserted by a case over a many-source map. |
| T-07-62 | Denial of Service | MAP-06's unenforced aggregate bound | medium | mitigate | `countSourcesForMap` gains a production caller that refuses past `SOURCE_ROWS_PER_MAP_MAX` rows for this `(artifact, map)`, through the shipped `map:`-namespaced reason path so the artifact records `partial` with a reason rather than stopping quietly. One bounded indexed count per map-bearing artifact, re-checked against the project epoch. Defence in depth: after `v: 9` the per-`(artifact, map)` sighting count is already bounded at parse time, and this is the second enforcement point taken against what is on disk. |
| T-07-75 | Denial of Service | the aggregate comparison refusing a write that adds no rows | medium | mitigate | The comparison is on `max(existing, recovered)`, the projected post-write total, never on `existing + recovered`. Under the additive form a re-analysis — reachable at every `detectorSetHash` change, which Phase 3 landing is — would double-count rows the `v: 9` upsert overwrites in place, refuse any map past 1,024 sightings, and write `scan_state = 'partial'` on an artifact previously accepted whole. That is a self-inflicted availability loss on ordinary traffic, target-triggerable by serving a large map. Asserted by a re-analysis case that must finish `done` with an unchanged sighting count. |
| T-07-63 | Tampering | `sources_verbatim` truncated by UTF-16 code unit | low | mitigate | The cut moves to a code-point boundary, so a target cannot arrange a label whose stored prefix ends in an unpaired surrogate — an invalid UTF-8 sequence in a `TEXT` column whose behaviour through the driver and the RPC boundary is not defined. Asserted by re-reading the row rather than by inspecting the input. |

No package-manager install task exists in this plan, so no supply-chain threat row applies.
</threat_model>

<verification>
- `pnpm vitest run` — whole suite green.
- `pnpm typecheck && pnpm lint && pnpm knip` — exit 0.
- `git diff --stat knip.json packages/backend/src/store/migrations.ts packages/backend/src/store/schema.spec.ts .planning/REQUIREMENTS.md packages/frontend` — prints nothing.
- `grep -rn 'countSourcesForMap' packages/backend/src --include=*.ts | grep -v spec` — at least two lines.
- `git diff -U0 packages/backend/src/telemetry.ts` — every added and removed line is inside a comment block. This plan's claim on that file is one docblock; 07-16 owns its behaviour.
</verification>

<success_criteria>
- One depth refusal per stage, and a counter that is not a restatement of another counter.
- The counter's docblock, in the one file that carries it, states the per-stage meaning that ships.
- MAP-06's aggregate bound is enforced by a production caller, in rows, with a named refusal, on the projected post-write total.
- Re-analysing an unchanged artifact is accepted, proven by a committed case.
- No docblock in `store/sources.ts` describes a caller that does not exist, or claims to be the sole enforcer of a bound something else already holds.
- A stored label can never end in an unpaired surrogate, proven by a re-read.
</success_criteria>

<output>
Create `.planning/phases/07-sourcemap-reconstruction/07-15-SUMMARY.md` when done.
Record: the wire-versus-delete decision for MD-04 with its reasoning, the three RED observations, the
old and new expected values for the three edited D-13 cases, the note that existing rows with a lone
surrogate are not rewritten, the two `derivedRejected` scope greps and the confirmation that no
frontend file names the counter, the `Math.max(` count before and after, the re-analysis case and the
arithmetic it pins, and MD-03 / MD-04 / LO-03 marked closed with their commits.
</output>
