---
phase: 07-sourcemap-reconstruction
plan: 06
type: execute
wave: 5
depends_on: ["07-04", "07-05"]
files_modified:
  - packages/backend/src/index.ts
  - packages/backend/src/index.spec.ts
  - packages/backend/src/api/spec.ts
  - packages/engine/src/contract.ts
  - packages/engine/src/contract.spec.ts
  - packages/backend/src/store/export.ts
  - packages/backend/src/store/export.spec.ts
  - packages/frontend/src/api/client.ts
  - packages/frontend/src/api/client.spec.ts
autonomous: true
requirements: [MAP-07, UI-05]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-07: content is DERIVED ON DEMAND. `deriveSource` reloads the originating request through `sdk.requests.get`, re-verifies, re-parses the map and returns one source's content. Nothing is held at rest between calls and nothing is cached to disk."
    - "D-24: every derivation RE-VERIFIES the reloaded body's sha256 against the recorded `artifact_sha256` and FAILS CLOSED on a mismatch. This is an INTEGRITY control, not a cache key: the operator can never be shown source attributed to a bundle it did not come from, and a re-deploy is detected for free. Transparently re-deriving from the new body was rejected — the row would say one hash and the viewer show another, which is the quiet mismatch every gate in this codebase exists to prevent."
    - "D-23: unreachability is detected LAZILY at open and the outcome STICKS. The write is a single bound `project_id`-scoped UPDATE issued from the RPC handler, with `pid` captured BEFORE any `await` so the scoping IS the epoch check — if the project changed during the await the predicate matches zero rows and the write is a no-op, which is why `retryAnalysis` needs no `stillCurrent()` while the consumer does."
    - "O-06: the SQL gate models statement TEXT and has no concept of which RPC issues a statement, so 'a write on a read path' is not a category it can express. `RETRY_ANALYSIS_SQL` is the shipped, green precedent for the exact shape and the new statement passes the gate unchanged."
    - "The two reload-undefined branches stay DISTINCT. `sdk.requests.get` returning undefined and returning a record with no response are different facts about the target's history — the consumer already counts them separately with the verbatim reason that conflating them hides WHICH one is happening — and the tombstone must be able to say which."
    - "MAP-07 splits at the export path's own seam, and this plan states which half each mechanism meets: **'browsable in the plugin UI'** is met by `listRecoveredSources` + `deriveSource` with content re-derived on demand; **'exportable with a manifest'** is met by MANIFEST ROWS riding `export.ts` unchanged; single-file content is a browser download from what the viewer already holds. There is no bulk content export and D-20 records why."
    - "MAP-07/encoding: the manifest's `sources` label is URL-shaped by construction (`webpack://...`) and is registered as a redactable column so `redactUrlForExport` applies to it in redacted mode. No per-column exemption is invented — the evidence D-06 preserves is retrievable through the raw option, which is what the raw option is for."
    - "MAP-07/ordering: manifest rows are emitted in the map's own `sources` declaration order within an artifact, with the same deterministic tie-break `serialiseRows` already relies on, so two exports of an unchanged database are byte-identical."
    - "MAP-07/empty: an artifact with zero recovered sources exports a header-only file only if the operator forces it; the shipped `NOTHING_TO_EXPORT_LABEL` path disables the control and states its own reason instead."
    - "O-01: the `mappings` string crosses the RPC in ONE un-chunked response and needs no new transport. `mappings` is a JSON string member of a map bounded at `MAP_MAX_BYTES`, which plan 07-01 asserted is at or below `floor(PASSIVE_MAX_BYTES x 3/4) = 6,291,456`, itself strictly below the project's `MAX_RPC_PAYLOAD_BYTES` budget of 8,388,608 — with roughly 2.1 MiB of slack before the announcement prefix and the surrounding JS are subtracted."
    - "The `mappings` payload is LAZY: it does not ride the first `deriveSource` response, because the viewer must render, scroll and be fully usable before one byte of it has crossed the RPC. It is fetched by a second call when the operator first asks for a position."
    - "A map over `MAP_MAX_BYTES` is REFUSED, never truncated — a truncated VLQ stream decodes to WRONG POSITIONS, not to an error, which is the quiet-wrongness class every gate in this codebase exists to prevent."
    - "`CONTRACT_VERSION` is bumped from 5. It exists precisely to prevent a stale frontend talking to a new backend, and the frontend client already handles the mismatch."
    - "Every new handler follows the shipped registration idiom: capture `pid` before any await, fail closed to a NAMED SENTINEL, and LOG the redacted error rather than returning it — the description is already redacted and it still does not cross the boundary."
    - "The `could not ask` outcome is a sentinel, never an inferred tombstone. A derivation RPC that failed, timed out or was not answered returns a distinct result from a genuine missing-request finding, because a durable-looking claim made from an absence of evidence is the exact defect the shipped compat rule names: a call that did not answer is not evidence of a refusal."
  artifacts:
    - packages/backend/src/index.ts
    - packages/backend/src/api/spec.ts
    - packages/backend/src/store/export.ts
    - packages/frontend/src/api/client.ts
  key_links:
    - "`deriveSource` re-verifies against `source_sightings.artifact_sha256`, which plan 07-04 stored for exactly this purpose."
    - "`markProducibility` is plan 07-04's idempotent statement; this plan is its only caller, and its stickiness is a property of that statement rather than of this handler's discipline."
    - "The manifest's third `EXPORT_COLUMNS` entry rides the shipped chunking at `EXPORT_RPC_CHUNK_ROWS`, the floor statement, the raw-confirm ceremony and the `export_raw`/`export_redacted` audit kinds — nothing new is needed from the export path."
    - "Plan 07-08's viewer consumes `deriveSource`'s four outcomes and the lazy `mappings` call; plan 07-09 wires the scoped manifest CTA to this export table."
  prohibitions:
    - "The frontend never infers producibility. `markProducibility` is called only on the backend and only when the reload genuinely returns missing-or-no-response."
    - "A hash mismatch never transparently re-derives from the new body. Fail closed and return a distinct reason."
    - "No bulk content export is added. `serialiseRows` describes rows, not file contents, and a bulk export would re-derive every file during the export as a long serial job needing its own progress surface."
    - "No new chunked transport is built for the `mappings` string. It fits in one call by construction."
    - "A map over the bound is not truncated to fit a response."
    - "No handler returns a caught exception's text across the RPC boundary. Reason codes and sentinels only; the redacted description is logged."
    - "`EXPORT_RPC_CHUNK_ROWS`, `serialiseRows`, `redactUrlForExport` and `exportFilename` are not modified. The manifest rides them unchanged."
    - "The `export_raw` / `export_redacted` audit kinds are not renamed and no new audit kind is added — `audit` is written to only when something is destroyed, and its `kind` is a closed CHECK."
---

<objective>
The two RPCs that make recovered source readable without ever storing it, and the manifest half of
MAP-07.

D-07 is the load-bearing decision of the phase: nothing is held at rest, so every open reloads the
bundle, re-verifies its hash and re-reads the map. D-24 makes that reload an integrity control rather
than a convenience — the operator can never be shown source attributed to a bundle it did not come
from. D-23 makes the unreachable case detected lazily and recorded once.

D-20 splits MAP-07 at the export path's own seam. The manifest is rows and rides `export.ts`
unchanged; content is one file at a time from the viewer. This plan states which half each mechanism
meets, because CONTEXT.md requires the phase to say so.

Purpose: the drill-down gets something to call, and the manifest gets a table.
Output: `listRecoveredSources`, `deriveSource`, `readSourceMappings`, a third `EXPORT_COLUMNS` entry,
and a contract-version bump.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sourcemap-reconstruction/07-CONTEXT.md
@.planning/phases/07-sourcemap-reconstruction/07-RESEARCH.md
@.planning/phases/07-sourcemap-reconstruction/07-PATTERNS.md
@.planning/phases/07-sourcemap-reconstruction/07-UI-SPEC.md
@.planning/phases/07-sourcemap-reconstruction/07-04-SUMMARY.md
@.planning/phases/07-sourcemap-reconstruction/07-05-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TRACER — `deriveSource`, reload to re-verify to content, with all four outcomes</name>
  <files>packages/backend/src/index.ts, packages/backend/src/index.spec.ts, packages/backend/src/api/spec.ts, packages/engine/src/contract.ts, packages/engine/src/contract.spec.ts</files>
  <read_first>
    - `packages/backend/src/index.ts:846-874` — the `retryAnalysis` registration: `pid` captured before any await, the `NO_RETRY` sentinel, and the verbatim comment that the error is LOGGED HERE, NOT RETURNED because the description is already redacted and it still does not cross the boundary
    - `packages/backend/src/index.ts` `init()` — the ordering contract and step 6b, where the new handlers register: success path only, after the consumer, before the ready latch
    - `packages/backend/src/ingest/consumer.ts:524-542` — the reload with its TWO undefined branches and a counter each, and the verbatim reason conflating them hides which one is happening
    - `packages/backend/src/ingest/consumer.ts:558` — `byteLenMismatch` and the note that a non-zero value means the body-length invariant no longer holds
    - `packages/backend/src/store/sources.ts` — `markProducibility` and its idempotent trailing predicate
    - `packages/backend/src/store/reads.ts` — `listRecoveredSourcesPage`, `countRecoveredSourcesByArtifact`
    - `packages/engine/src/digest.ts:9,23-25` — `sha256Hex` and the native `createHash` path
    - `packages/engine/src/sourcemap/parse.ts` — `decodeInlineMap`, `parseSourceMap`, `MAP_PARSE_REASONS`
    - `packages/backend/src/api/spec.ts:148` — `CONTRACT_VERSION`
    - `packages/engine/src/contract.ts` — `SOURCE_PRODUCIBILITY_STATES` (plan 07-04) and the `RpcReason` register described at :1354-1357
    - `.planning/phases/07-sourcemap-reconstruction/07-UI-SPEC.md` § "The Four Body States of the Viewer" — Content / Gone / Changed / Could not ask, and the rule that outranks the table
  </read_first>
  <action>
Register `deriveSource` and make its four outcomes distinct at the contract level, so the frontend
cannot collapse them even by accident.

Declare the result shape in `packages/engine/src/contract.ts` as a closed discriminated union, in the
`RpcReason` register — REASONS, NEVER MESSAGES, because the frontend maps each member to its own copy
and nothing a driver said is interpolated into a sentence the operator reads. Four arms:

  - `content` — carrying `content: string`, `byteLen`, `lineCount`, and the source's `sha256`.
  - `gone` — carrying which of the two reload branches produced it (`no_request` versus `no_response`)
    and the `recoveredAt` date the tombstone sentence interpolates. Both branches are distinct facts
    about the target's history and the consumer already keeps them apart.
  - `changed` — D-24's fail-closed mismatch. Carries no content, by construction.
  - `unavailable` — the sentinel for "could not ask": the database is not open, the project is null,
    the reload threw, or the re-parse failed. **This arm never writes a producibility row.**

Register the handler in `init()` step 6b, on the success path only, after the consumer and before the
ready latch, in `retryAnalysis`'s exact idiom:

1. `const pid = currentProjectId();` BEFORE any await. `if (!db || pid === null) return UNAVAILABLE;`
2. `await sdk.requests.get(req.requestId)`. Undefined → `markProducibility(..., "gone", ...)` and
   return the `gone` arm tagged `no_request`. A record with no response → the same write and the
   `gone` arm tagged `no_response`. Keep the two branches separate and increment a counter each, in
   the shipped two-branch discipline.
3. Extract the bytes and compute `sha256Hex`. If it does not equal the recorded `artifact_sha256`,
   `markProducibility(..., "changed", ...)` and return the `changed` arm. **FAIL CLOSED — do not
   re-derive from the new body.** Reuse `byteLenMismatch`'s REPORTING shape so a re-deploy, the
   expected benign cause, is visible as a number rather than only as a refusal the operator cannot
   explain.
4. Re-announce, re-decode, re-parse, take `sourcesContent[req.sourceIndex]`, and return the `content`
   arm. Any failure here returns `unavailable`, never `gone` — the source has not been proven
   unproducible, only unread this time.

`pid` lands in every `WHERE`. The scoping IS the epoch check for a single statement: if the project
changed during the await the predicate matches zero rows and the write is a no-op, so no
`stillCurrent()` re-check is needed here. Add that sentence as a doc comment so the asymmetry with
`consumer.ts` is explained where a reader will ask about it.

Bump `CONTRACT_VERSION` from 5 to 6.

In `index.spec.ts`, drive all four arms against a fake SDK: a present request with a matching digest;
a `get` returning undefined; a `get` returning a record with no response; a present request whose body
hashes differently. For each, assert the returned arm AND the resulting `producibility` value in the
database — including that the `unavailable` arm wrote NOTHING.
  </action>
  <acceptance_criteria>
    - `deriveSource` is registered in `init()` step 6b and `index.spec.ts` asserts its registration order relative to the consumer and the ready latch.
    - The four arms are a closed union in `contract.ts` with a compile-time exhaustiveness helper; adding a fifth arm without handling it is a typecheck error, demonstrated by a scratch edit whose `pnpm typecheck` failure message is recorded.
    - The `no_request` and `no_response` cases return DIFFERENT tags and both set `producibility` to the gone member; asserted as two separate tests.
    - The mismatch case returns the `changed` arm, sets `producibility` to the changed member, and the response object has no content field at all — asserted structurally, not by checking for an empty string.
    - The `unavailable` case leaves `producibility` at the initial member; asserted by reading the row back after the call.
    - `CONTRACT_VERSION` is 6 and `packages/frontend/src/api/client.spec.ts` passes against it.
    - `pnpm vitest run packages/backend/src/store/sql-discipline.spec.ts` stays green — the write-on-a-read-path statement passes the gate unchanged.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run packages/backend/src/index.spec.ts packages/engine/src/contract.spec.ts packages/backend/src/store/sql-discipline.spec.ts --reporter=dot</automated>
    <fails_when>non-zero exit, "0 passed" in the vitest summary line, or fewer than 3 test files reported as collected</fails_when>
    <automated>pnpm typecheck &amp;&amp; pnpm lint</automated>
    <fails_when>non-zero exit, or any line matching "error TS" in the typecheck output</fails_when>
  </verify>
  <done>One source is produced on demand from a reloaded, hash-verified bundle, and the three ways that can fail are three different answers.</done>
  <reversibility rating="costly">D-07's no-content-at-rest design is what this handler exists to serve. Reversing it means adding a content column, which fires `schema.spec.ts` by design and re-opens Phase 6's D-24.</reversibility>
</task>

<task type="auto">
  <name>Task 2: The list read and the lazy `mappings` call</name>
  <files>packages/backend/src/index.ts, packages/backend/src/index.spec.ts, packages/backend/src/api/spec.ts, packages/frontend/src/api/client.ts, packages/frontend/src/api/client.spec.ts</files>
  <read_first>
    - `packages/backend/src/index.ts` — the paged-read handler registrations already shipped, for the cursor and page-shape idiom
    - `packages/backend/src/store/reads.ts` — `listRecoveredSourcesPage`, `countRecoveredSourcesByArtifact`, `KEYSET_PAGE_ROWS`
    - `packages/frontend/src/api/client.ts` and `client.spec.ts` — the typed RPC surface and the contract-version mismatch handling
    - `packages/backend/src/store/export.ts:104-130` — `EXPORT_RPC_CHUNK_ROWS` and the verbatim sentence that the 8 MiB figure is a budget this project sets, not a ceiling it measured from Caido
    - `packages/engine/src/thresholds.ts` — `MAP_MAX_BYTES` and the O-01 inequality plan 07-01 asserted
    - `.planning/phases/07-sourcemap-reconstruction/07-UI-SPEC.md` § "The `mappings` string over the RPC" and § "The source list read, and why the tree is loaded eagerly"
  </read_first>
  <action>
Register the two remaining reads and give the frontend a typed surface for all three handlers.

**`listRecoveredSources(artifactSha256, cursor)`** — wraps `listRecoveredSourcesPage`. Keyset, never
`OFFSET`, page size the existing `KEYSET_PAGE_ROWS` unchanged and re-read. Rows carry metadata only:
label, index, map digest, source digest, byte length, line count, producibility, recovered-at. NO
content. That is what makes the UI-SPEC's eager-to-the-bound load policy affordable — the tree is a
hierarchy over the WHOLE set and presenting a subset as the whole set is the defect the client-side
sorting ban already exists to prevent.

The handler enforces the load bound (U7-1's 2,000 rows) and returns BOTH the returned count and the
TOTAL, so the frontend can say *Showing the first {bound} of {total}* in words rather than truncating
silently.

**`countRecoveredSources()`** — wraps `countRecoveredSourcesByArtifact`, returning the digest-keyed
map the `Sources` column consumes as an optional lookup prop. The handler must preserve the
zero-versus-unknown distinction end to end: an artifact with a parsed map that yielded nothing appears
in the map with value 0; an artifact never analysed does not appear at all. Collapsing those two is the
one thing the column exists to prevent.

**`readSourceMappings(mapSha256, requestId, artifactSha256)`** — a SEPARATE handler from
`deriveSource`, and separate on purpose. The `mappings` string is the larger half of the payload and is
unused until the operator asks for a position, so it must not ride the first response: the viewer has
to render, scroll and be fully usable before one byte of it has crossed the RPC. The handler performs
the same reload and the same D-24 re-verify, then returns the raw `mappings` string in ONE un-chunked
response.

Write O-01's bound into the handler's doc comment as an argument a reader can check, not as a claim:
`mappings` is a JSON string member of a map bounded at `MAP_MAX_BYTES`; plan 07-01 asserted
`MAP_MAX_BYTES <= floor(PASSIVE_MAX_BYTES x 3/4) = 6,291,456`, which is strictly below the project's
8,388,608-byte RPC budget, leaving roughly 2.1 MiB of slack before the announcement prefix and the
surrounding JS are even subtracted. Record the residual honestly in the same comment: the 8 MiB figure
is a project BUDGET and not a measured Caido ceiling — `export.ts` says so verbatim — and
`exportInventory` already ships 7.00 MiB responses under the same assumption, so Phase 7 INHERITS that
residual rather than creating it. A map over the bound is refused, never truncated.

**`packages/frontend/src/api/client.ts`** — add the three typed methods to the shipped RPC surface,
mirroring `api/spec.ts` exactly. No behaviour, just the contract. `client.spec.ts` asserts the method
set matches the backend spec's registered names and that `CONTRACT_VERSION` agrees on both sides.
  </action>
  <acceptance_criteria>
    - Three handlers are registered and `index.spec.ts` asserts each by name in the registered set.
    - The paging test seeds more than `KEYSET_PAGE_ROWS` rows and asserts the union of pages equals the seeded set with no duplicate and no gap, and that the returned total is the seeded count rather than the page count.
    - Requesting more than the load bound returns exactly the bound and a total greater than it, asserted as two fields rather than inferred.
    - `countRecoveredSources` returns an entry with value 0 for an analysed artifact that yielded nothing, and NO entry for an unanalysed artifact — two separate assertions.
    - `readSourceMappings` is a distinct registered handler; `index.spec.ts` asserts `deriveSource`'s success response has no `mappings` field at all.
    - `readSourceMappings` performs the D-24 re-verify: a mismatched body returns the changed arm and no `mappings`.
    - `packages/frontend/src/api/client.spec.ts` asserts the frontend method set equals the backend registered set and that both sides read the same `CONTRACT_VERSION`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run packages/backend/src/index.spec.ts packages/frontend/src/api/client.spec.ts packages/backend/src/store/reads.spec.ts --reporter=dot</automated>
    <fails_when>non-zero exit, "0 passed" in the vitest summary line, or fewer than 3 test files reported as collected</fails_when>
    <automated>pnpm exec caido-dev build packages &amp;&amp; pnpm typecheck &amp;&amp; pnpm check:bundle</automated>
    <fails_when>non-zero exit, any line matching "error TS", or the bundle check reporting a specifier outside the allowlist</fails_when>
  </verify>
  <done>The tree can be loaded eagerly to a stated bound, the count column can tell zero from unknown, and the position table does not block first paint.</done>
</task>

<task type="auto">
  <name>Task 3: The manifest — a third export table, riding the shipped ceremony unchanged</name>
  <files>packages/backend/src/store/export.ts, packages/backend/src/store/export.spec.ts, packages/engine/src/contract.ts, packages/engine/src/contract.spec.ts</files>
  <read_first>
    - `packages/backend/src/store/export.ts:36-38,104-130,232-254,433,588` — the header's argument that a second field escaper would make the exported bytes and the tested rule two different things, `EXPORT_RPC_CHUNK_ROWS` with its derivation, `EXPORT_COLUMNS`, `serialiseRows`, and the table-membership guard
    - `packages/engine/src/contract.ts:1125,1146` — `EXPORT_FORMATS` and `EXPORT_REDACTION_MODES`, and the export-table union
    - `packages/backend/src/store/audit.ts:73-114` — `AUDIT_KINDS` including `export_raw` and `export_redacted`, and the closed CHECK
    - `packages/backend/src/store/export.spec.ts` — the existing per-table column assertions and the byte-level serialisation tests
    - `tests/export-payload-budget.spec.ts:78,86` — `MAX_RPC_PAYLOAD_BYTES` and its verbatim A BUDGET, NOT A DISCOVERED LIMIT justification, and the 20,000-versus-25,000-row derivation
    - `.planning/phases/07-sourcemap-reconstruction/07-UI-SPEC.md` § "The manifest export (D-20, MAP-07)"
  </read_first>
  <action>
Add the manifest as a third export table and change nothing else about the export path.

Append the new member to the export-table union in `packages/engine/src/contract.ts` and add its
`EXPORT_COLUMNS` entry in `export.ts`. Columns, in this order, mirroring the row shape
`listRecoveredSourcesPage` already returns: the artifact digest, the map digest, the source index, the
`sources` label, the source content digest, the byte length, the line count, the producibility, and
the recovered-at timestamp.

**One column is target-controlled and gets `{ redact: redactUrlForExport }`:** the `sources` label. It
is URL-shaped by construction — `webpack://...`, `file://...`, `http://...` are all real shapes SPIKE-12
measured — so the shipped URL redactor is the right function and NO PER-COLUMN EXEMPTION IS INVENTED.
State the reasoning in the entry's comment: the evidence D-06 preserves is retrievable through the raw
option, which is what the raw option is for.

Everything else rides unchanged and must be asserted unchanged: chunking at `EXPORT_RPC_CHUNK_ROWS`,
the floor statement, `serialiseRows`'s field escaping, `exportFilename`, the raw-confirm ceremony, and
the `export_raw` / `export_redacted` audit kinds. Write no second field escaper — `export.ts`'s own
header states the rule and it is the reason this table needs almost no code.

**State which half of MAP-07 each mechanism meets**, in `export.ts`'s doc comment for the new entry
and again in the SUMMARY, because CONTEXT.md requires the phase to say so rather than leave a verifier
to work it out:

| Half of MAP-07 | Mechanism |
|----------------|-----------|
| "browsable in the plugin UI" | `listRecoveredSources` + `deriveSource`, content re-derived on demand per D-07 |
| "exportable with a manifest" | manifest ROWS through `export.ts` unchanged, delivered by the shipped chunked RPC download |
| single-file content | a browser download from the content already in the viewer, one file at a time |

And record what was REJECTED and why: a bulk content export would need a second export shape
`serialiseRows` does not describe, would re-derive every file during the export, and would be a long
serial job needing its own progress surface — Phase 6's lesson about what a long serial job costs, paid
a second time.

In `export.spec.ts`: assert the new table's column list exactly; assert the label column is registered
as redactable and that redacted mode actually redacts a `webpack://`-shaped fixture while raw mode
returns it byte-identical; assert row ORDER is the map's `sources` index ascending and that two
exports of an unchanged database are byte-identical; assert the row count crosses
`EXPORT_RPC_CHUNK_ROWS` correctly by seeding one row more than the chunk size; and assert an artifact
with zero recovered sources produces the empty-result path rather than a header-only file.
  </action>
  <acceptance_criteria>
    - `EXPORT_COLUMNS` has exactly three table entries and the new one lists exactly nine columns in the stated order; the spec asserts the list with `toEqual`.
    - The `sources` label column carries `redact: redactUrlForExport`; a `webpack://app/src/secret.ts` fixture is redacted in redacted mode and byte-identical in raw mode, asserted as two tests.
    - Two consecutive exports of an unchanged database produce byte-identical output, asserted by comparing the serialised strings.
    - Seeding `EXPORT_RPC_CHUNK_ROWS + 1` rows produces two chunks whose concatenation equals the full row set, with no duplicate and no gap.
    - `git diff --exit-code -- packages/frontend/src/components/export-download.ts` reports no change — the frontend download path needs nothing new.
    - `grep -c 'export_raw' packages/backend/src/store/audit.ts` is unchanged from its pre-plan value, and `pnpm vitest run packages/backend/src/store/audit.spec.ts` passes — no audit kind was added or renamed.
    - `pnpm test` is green at the end of the plan.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run packages/backend/src/store/export.spec.ts packages/backend/src/store/audit.spec.ts packages/engine/src/contract.spec.ts tests/export-payload-budget.spec.ts --reporter=dot</automated>
    <fails_when>non-zero exit, "0 passed" in the vitest summary line, or fewer than 4 test files reported as collected</fails_when>
    <automated>pnpm test &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm knip</automated>
    <fails_when>non-zero exit, any line containing "FAIL" in the vitest dot-reporter output, or any line matching "error TS"</fails_when>
  </verify>
  <done>The manifest is a third table on the shipped export path, the label is redactable by the shipped redactor, and which half of MAP-07 each mechanism meets is written down.</done>
  <reversibility rating="reversible">One union member and one column list; removing them removes an export target and nothing else.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caido request store → `deriveSource` | A reloaded body may not be the bundle the row describes |
| backend → frontend RPC | Recovered content, the `sources` label and the raw `mappings` string cross to the browser |
| database rows → an exported file | The manifest leaves the tool |
| RPC handler → SQLite, across an `await` | The project-epoch window |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Spoofing | source attributed to the wrong bundle | high | mitigate | D-24: sha256 of the reloaded body compared to the recorded `artifact_sha256`, FAIL CLOSED, content withheld and a distinct `changed` arm returned. Never re-derived transparently from the new body. The single most valuable security property this phase adds |
| T-07-13 | Repudiation | a failed call rendered as a durable claim | medium | mitigate | The `unavailable` arm is a sentinel that writes NOTHING; `markProducibility` is called only on a genuine missing-request or no-response reload. Asserted by reading the row back after an `unavailable` result |
| T-07-09 | Information disclosure | cross-project reads or writes | high | mitigate | `pid` captured before any `await` and placed in every `WHERE`; for a single statement the scoping IS the epoch check — a project change during the await matches zero rows |
| T-07-11 | Tampering | the raw `mappings` string | medium | mitigate | Returned as data, never rendered; bounded by construction at `MAP_MAX_BYTES` under the project's RPC budget; refused rather than truncated, because a truncated VLQ stream decodes to wrong positions rather than to an error |
| T-07-10 | Information disclosure | driver text crossing the RPC | medium | mitigate | Named sentinels and closed reason unions only; the redacted description is LOGGED, not returned, in the shipped `retryAnalysis` idiom |
| T-07-17 | Information disclosure | a `sources` label leaving in a raw manifest | low | accept | The raw path is an explicit operator ceremony with a `danger` confirmation and no remember-this-choice; redacted is the pre-selected default and `redactUrlForExport` governs the label. The evidence D-06 preserves is exactly what the raw option exists to release |
| T-07-34 | Denial of service | an oversized RPC response | medium | accept | Inherited, not created: the 8 MiB figure is a project budget and not a measured Caido ceiling, and `exportInventory` already ships 7.00 MiB responses under the same assumption. Recorded in the handler's doc comment rather than hedged with machinery for a defect nobody has observed |
| T-07-35 | Denial of service | derivation cost on the proxy thread | medium | mitigate | One native hash per open, measured at 13.2 ms on 8.3 MB against 720 ms for the JS loop; the re-parse is bounded by `MAP_MAX_BYTES`; the `mappings` read is a separate lazy call so the common path never pays for it |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs nothing and adds no dependency. `pnpm knip` and `pnpm check:bundle` in the verify gates fail on an undeclared import or an out-of-allowlist specifier |
</threat_model>

## Artifacts this phase produces

Symbols created by THIS plan:

- **RPC handlers (new, registered in `init()` step 6b):** `deriveSource`, `listRecoveredSources`,
  `countRecoveredSources`, `readSourceMappings`
- **Exports (new, `packages/engine/src/contract.ts`):** the `DeriveSourceResult` closed union with
  arms `content` / `gone` / `changed` / `unavailable`, the `gone` sub-tags `no_request` and
  `no_response`, the `RecoveredSourceRow` type, and one new export-table union member
- **Exports (new, `packages/backend/src/api/spec.ts`):** the four handler signatures;
  `CONTRACT_VERSION` bumped 5 → 6
- **Exports (new, `packages/frontend/src/api/client.ts`):** the four typed client methods
- **Config entry (new, `packages/backend/src/store/export.ts`):** a third `EXPORT_COLUMNS` entry with
  nine columns, one of them carrying `redact: redactUrlForExport`
- **Sentinels (new, `packages/backend/src/index.ts`):** the `unavailable` result constant, in
  `NO_RETRY`'s shape
- **Consumed, not created:** `markProducibility`, `listRecoveredSourcesPage`,
  `countRecoveredSourcesByArtifact` (plan 07-04); `decodeInlineMap`, `parseSourceMap` (plan 07-02);
  `MAP_MAX_BYTES` (plan 07-01); `sha256Hex`, `EXPORT_RPC_CHUNK_ROWS`, `serialiseRows`,
  `redactUrlForExport`, `exportFilename`, `export_raw`, `export_redacted` (all shipped)

## Which half of MAP-07 each mechanism meets

Stated here because CONTEXT.md D-20 requires the phase to say so:

| Half | Mechanism | Where |
|------|-----------|-------|
| "browsable in the plugin UI" | `listRecoveredSources` + `deriveSource`, content re-derived on demand per D-07 | this plan, rendered by plans 07-07 / 07-08 |
| "exportable with a manifest" | manifest ROWS riding `export.ts` unchanged | this plan, CTA wired by plan 07-09 |
| single-file content | a browser download from what the viewer already holds | plan 07-08 |

**Rejected, with the reason:** a bulk content export. It would need a second export shape
`serialiseRows` does not describe, would re-derive every file during the export, and would be a long
serial job needing its own progress surface — Phase 6's lesson about what a long serial job costs,
paid a second time. Recorded as a deferred idea in `07-CONTEXT.md`.

## Planner assumptions carried forward

- **A3 (RESEARCH):** Caido's real RPC ceiling is at or above the project's 8 MiB budget. Unmeasured
  and unmeasurable in-repo — `export.ts` states verbatim that nothing in this repository can push
  bytes through Caido's RPC. PRE-EXISTING: `exportInventory` already ships 7.00 MiB responses under
  the same assumption, so Phase 7 inherits the residual rather than creating it.
- **A7 (RESEARCH):** `MAP_MAX_BYTES` lands below 6,291,456, making the RPC bound doubly slack. Plan
  07-01 settles this; if it landed AT the ceiling, the `mappings` response is 6.29 MB against an 8 MiB
  budget — still inside, with less margin than stated, and the SUMMARY must say which.

<verification>
- `pnpm test`, `pnpm typecheck`, `pnpm lint`, `pnpm knip`, `pnpm check:bundle` all green at plan end.
- All four `deriveSource` arms asserted with their database side-effects, including the arm that must
  have none.
- `export-download.ts` and `audit.ts` asserted unchanged.
- The contract-version bump agreed on both sides of the boundary.
</verification>

<success_criteria>
- Content is produced on demand, hash-verified, and withheld when the bundle changed.
- A failed call and a genuine absence are different answers at the contract level, not just in copy.
- The manifest is nine columns on the shipped export path with no new export machinery.
- The `mappings` string crosses in one call and does not block first paint.
</success_criteria>

<output>
Create `.planning/phases/07-sourcemap-reconstruction/07-06-SUMMARY.md` when done, recording: the four
result arms as shipped, the exhaustiveness typecheck failure message, the byte-identical export
observation, the redacted-versus-raw label observation, and whether A7 held with the actual
`MAP_MAX_BYTES` figure and the resulting RPC margin.
</output>
