---
phase: 07-sourcemap-reconstruction
plan: 20
type: execute
wave: 2
depends_on: ["07-18"]
gap_closure: true
nyquist_compliant: true
autonomous: false
requirements: [MAP-06]
files_modified:
  - packages/backend/src/store/retention.ts
  - packages/backend/src/store/retention.spec.ts

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Either every eviction removes its sightings in the same statement sequence as its observations and analyses, or `retention.ts:42-45` and `:389-390` say plainly that sightings are reaped as orphans by design and the window closes on the next pass — whichever the operator chooses at the checkpoint, and exactly one of the two."
    - "A SINGLE `sweepRetention` call — not a `sweepToConvergence` loop — pins whichever property is chosen, in a case that was RED against the shipped code."
    - "The RED is produced by the scenario the gap describes: a pass whose budget is consumed by childless artifacts, so step 3d's `budget() > 0` guard skips the orphan collection entirely."
    - "The boundary of the claim is stated as well as the claim: `source_sightings` is a child of `artifacts`; `sources` is NOT, and dies with its LAST sighting by anti-join across passes, by design."
    - "No foreign key and no `ON DELETE CASCADE` is introduced, and `SCHEMA_VERSION` is still 9."
    - "The properties `retention.spec.ts` already pins are unchanged: a source sighted from two bundles outlives the eviction of one, a budget-exhausted sightings pass deletes no `sources` row, and the `NOT IN` NULL trap stays closed on both sides."
  prohibitions:
    - "NO FOREIGN KEY AND NO `ON DELETE CASCADE`, under any repair. The operator approved exactly one schema change this round — 07-12's PK widening at migration `v: 9` — and explicitly excluded both of these. `PRAGMA foreign_keys` is per-connection and the pool holds up to five connections; decision P4-D3 is why the cascade is explicit in the first place."
    - "No new schema migration and no new index. `SCHEMA_VERSION` stays 9 and `EXPECTED_TABLES` stays at eight members, byte-unchanged."
    - "No multi-statement `exec`. Every delete stays one fully-bound single-row statement, because a multi-statement `exec` that fails strands an open write transaction on a pooled connection nothing in the plugin API can reach. That is measured, not feared."
    - "`deleteDigest` must NOT delete from `sources`. `sources` is content-addressed and shared: a delete driven off the evicted bundle's sightings would take a row out from under a bundle still sighting it, which is the exact failure `UNSIGHTED_SOURCES_SQL`'s anti-join exists to prevent and which `retention.spec.ts` already fails on."
    - "No replacement of `UNSIGHTED_SOURCES_SQL`'s `NOT IN` with a correlated `NOT EXISTS`. That form was measured at 1,600x worse and projects past a minute per call at the shipped retention ceiling."
    - "No removal of `AND source_sha256 IS NOT NULL` from `UNSIGHTED_SOURCES_SQL`. One null sighting anywhere in the project would silently make the statement return zero rows for ever."
    - "No change to `RETENTION_SWEEP_MAX_ROWS`, `RETENTION_SWEEP_MAX_PASSES` or `RETENTION_SWEEP_EVERY_N`. Plan 07-18 corrected their documentation this wave; this plan must not move the values that documentation now derives from."
    - "No change to `MAP_MAX_BYTES`. Open by operator decision (W-6) and prohibited in every plan of this phase."
    - "No outbound fetch (D-01) and no filesystem access from plugin code (D-17)."
    - "No generalisation of `trimChildTable` to a variadic key. The decision not to widen it is recorded in the file with its reasoning; a repair that widens it for one more caller makes two tables that asked for nothing pay for it."
    - "A comment must never state an invariant the code does not enforce. Whichever repair is chosen, the module header and the code must agree when this plan finishes — that is the acceptance test, not the repair itself."
  artifacts:
    - "packages/backend/src/store/retention.ts — under repair A, a `SIGHTING_KEYS_FOR_DIGEST_SQL` enumeration and a third child block in `deleteDigest` before `DELETE_ARTIFACT_SQL`; under repair B, a scoped module header naming the two children the cascade actually covers"
    - "packages/backend/src/store/retention.spec.ts — a SINGLE-PASS case driving `sweepRetention` once against a budget-exhausting artifact backlog, plus the `sources`-is-not-a-child boundary assertion"
  key_links:
    - "`DELETE_ARTIFACT_SQL` → the rows that named that artifact → `source_sightings.artifact_sha256` — the child relationship `deleteDigest` does not currently walk"
    - "step 3d's `budget() > 0` guard → `sightingsCapped` → the anti-join deferred to the next pass — the arm that makes orphaning the ORDINARY path for an evicted map-bearing bundle rather than a crash-recovery corner"
    - "`sweepRetention` (one pass) versus `sweepToConvergence` (the loop) — the distinction that decides whether the property under test is the single-pass one or the across-passes one"
    - "`retention.ts:42-45` and `:389-394` → the reader's belief about what an eviction leaves behind — the two paragraphs that must be true when this plan finishes"
---

<objective>
Make the eviction cascade and the module header agree about `source_sightings`, and pin whichever
agreement the operator chooses with a SINGLE-PASS test.

This closes **G-07-2 / WR-02**, which the verifier also recorded as this phase's one
`coincidental_reliance_items` entry. Plan 07-13 added `source_sightings` as a third child table but did
not extend `deleteDigest` (`retention.ts:1112-1193`), which still enumerates only
`OBSERVATION_KEYS_FOR_DIGEST_SQL` and `ANALYSIS_KEYS_FOR_DIGEST_SQL` before `DELETE_ARTIFACT_SQL`. Step
3d's orphan collection is guarded by `budget() > 0` at `:927` with an `else { sightingsCapped = true; }`
arm, so a pass whose budget is consumed by childless artifacts returns having orphaned every evicted
bundle's sightings.

There is no data loss: `workRemains` re-detects the state through `ORPHAN_SIGHTINGS_SQL` and the sweep
converges ACROSS passes, and `readSightingOrigin` LEFT JOINs `artifacts` so the read path errs toward
keeping rows alive. What is wrong is that the module header states three times — at `:42-45` and again
at `:389-394` — that the cascade cannot create that state, and it is now the ORDINARY path for every
evicted map-bearing bundle rather than the crash-recovery corner the paragraph describes.

The decision turns on whether the operator's UAT cascade choice meant "in the same statement sequence"
or "in the same pass", so it is asked rather than assumed. **The recommendation is repair A** — put
`source_sightings` into `deleteDigest`'s cascade — because it makes the paragraph true rather than
making it smaller, and because `retention.spec.ts:1320-1330` already records the operator's eviction
choice as CASCADE by anti-join and never a foreign key.

Purpose. `07-18` proved the repair pattern for this round on prose and `07-19` on a counter. This is the
same class at its most consequential site: an invariant stated three times in a module header, relied on
by a reader deciding whether an orphan is possible, and untested in the direction that matters because
the spec drives the convergence loop rather than one pass.

Output: one repair, chosen at a checkpoint, and a single-pass test that was RED.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sourcemap-reconstruction/07-CONTEXT.md
@.planning/phases/07-sourcemap-reconstruction/07-VERIFICATION.md
@.planning/phases/07-sourcemap-reconstruction/07-UAT.md
@.planning/phases/07-sourcemap-reconstruction/07-13-SUMMARY.md
@.planning/phases/07-sourcemap-reconstruction/07-18-SUMMARY.md
</context>

<gap_trace>
| Gap id | Source | What this plan closes |
|---|---|---|
| G-07-2 / WR-02 | `07-UAT.md` gap `G-07-2` (major); `07-VERIFICATION.md` finding `WR-02` AND its `coincidental_reliance_items` entry, which carries the verifier's own `harden:` wording | One of the two repairs, chosen at a checkpoint, plus the single-pass test that pins whichever property is chosen. |
| IN-03 | `07-VERIFICATION.md` finding `IN-03`; `07-REVIEW.md:254-262` | Folded into task 2 because it is a one-word comment fix inside the file that task already edits: `ORPHAN_SIGHTINGS_SQL`'s comment says it selects "the four key columns" and it selects three. |
</gap_trace>

<artifacts_this_plan_produces>
## Artifacts this phase produces

**New SQL constant (repair A only)**
- `SIGHTING_KEYS_FOR_DIGEST_SQL` in `packages/backend/src/store/retention.ts` — module-private, in
  `OBSERVATION_KEYS_FOR_DIGEST_SQL`'s exact shape: `project_id` and `artifact_sha256` bound,
  the two remaining key columns selected, an explicit deterministic `ORDER BY`, and a `LIMIT ?`.

**New spec cases (both repairs)**
- `packages/backend/src/store/retention.spec.ts` gains
  **"ONE PASS, not a convergence loop: a budget-exhausting backlog leaves NO sighting naming a deleted artifact"** — the single-pass pin, and
  **"`sources` is NOT a child of `artifacts` — the single-pass claim stops at the sighting"** — the boundary assertion that stops the first case being read as a promise about `sources`.
- Under repair A only, a third case asserting `SIGHTING_KEYS_FOR_DIGEST_SQL`'s scoping, ordering and
  `LIMIT`, through the existing `statementText` helper.

**New spec-local constant**
- A backlog size derived from `RETENTION_SWEEP_MAX_ROWS` rather than written out, so the case keeps
  exhausting the budget if the constant ever moves.

**Changed prose**
- `packages/backend/src/store/retention.ts` module header at `:42-45` and the orphan paragraph at
  `:389-394` — made true under whichever repair is chosen.
- `ORPHAN_SIGHTINGS_SQL`'s comment: the column-arity sentence corrected (IN-03).

**Nothing else.** No new table, no new index, no new migration, no foreign key, no new export, no new
dependency. Both paths in `files_modified` are git-tracked.
</artifacts_this_plan_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: CHECKPOINT — same statement sequence, or same pass?</name>
  <read_first>
    - `packages/backend/src/store/retention.ts:30-46` — the module header's EVERY DELETE IS ONE STATEMENT block, ending with the sentence that is the subject of this decision: children go first, so a pass that runs out of budget halfway leaves a parent with fewer children and never a child with no parent.
    - `packages/backend/src/store/retention.ts:386-394` — the orphan paragraph: "The cascade below cannot create one — children go first — but a crash mid-pass in some future version, or a row written before this module existed, can."
    - `packages/backend/src/store/retention.ts:1100-1193` — `deleteDigest` in full, including its own DEPENDENCY ORDER docblock, which repeats the same claim a third time.
    - `packages/backend/src/store/retention.ts:905-960` — step 3d, the `budget() > 0` guard, the `else { sightingsCapped = true; }` arm, and the paragraph explaining why the anti-join is deferred when the sightings work was capped.
    - `packages/backend/src/store/retention.spec.ts:1316-1340` — the comment recording the operator's UAT eviction choice: "The eviction order is the operator's, decided at UAT: CASCADE — a source dies with its LAST sighting, by anti-join rather than by a foreign key." This is the strongest available evidence of what the operator meant, and it is why A is recommended.
    - `.planning/phases/07-sourcemap-reconstruction/07-VERIFICATION.md` — the `coincidental_reliance_items` entry, which carries the verifier's own `harden:` wording, and the `human_verification` entry for WR-02.
  </read_first>
  <decision>
`deleteDigest` cascades two children of three. The third — `source_sightings` — is collected in step 3d
behind a `budget() > 0` guard, so a pass whose budget is consumed by the artifact loop returns having
orphaned every evicted bundle's sightings. The sweep converges across passes, so nothing is lost; what
is false is the module header, which states three times that the cascade cannot create that state.

**The question is what the cascade decision meant.** At UAT the operator chose CASCADE for the eviction
order — a `sources` row dies with its LAST sighting, by anti-join and never a foreign key. That settled
the ORDER. It did not settle whether "cascade" meant *in the same statement sequence as the artifact's
other children* or *somewhere in the same pass*. Both readings are defensible and only one of them is
what the code does today.

**Magnitude, stated so this is not overread.** No data loss in either direction: `workRemains`
re-detects orphans through `ORPHAN_SIGHTINGS_SQL` and the next pass collects them; `readSightingOrigin`
LEFT JOINs `artifacts`, and the anti-join errs toward keeping `sources` rows alive. The window is one
sweep pass. This is a correctness-of-documentation decision with a real code consequence, not an
outage.

**What is NOT on the table, under either option.** No foreign key. No `ON DELETE CASCADE`. No new
migration, no new index, no change to `SCHEMA_VERSION`. The operator approved exactly one schema change
this round and it already shipped.
  </decision>
  <options>
    - **A — RECOMMENDED. Put `source_sightings` into `deleteDigest`'s cascade.** A third enumeration block between the analyses block and `DELETE_ARTIFACT_SQL`, using a new `SIGHTING_KEYS_FOR_DIGEST_SQL` in the existing statement's shape and the shipped `DELETE_SIGHTING_SQL` bound on all four key columns. The completeness check gains a sightings arm, so a capped enumeration leaves the artifact standing exactly as a capped observations enumeration does. The module header's claim becomes TRUE, step 3d's orphan collection goes back to being the crash-recovery path it says it is, and the single-pass property becomes real rather than incidental. **Costs:** one statement, one loop, and sighting deletions now consume pass budget ahead of the artifact row — so a bundle with many sightings may take more passes to fully evict, which is the same behaviour observations and analyses already have. **Buys:** the invariant a reader is invited to rely on.
    - **B — Scope the module header's claim to the two children it actually covers.** No code change. `:42-45`, `:389-394` and `deleteDigest`'s own docblock are rewritten to say plainly that `observations` and `analyses` cascade in the same statement sequence, that `source_sightings` is reaped as an orphan by step 3d and may therefore survive one pass by design, and that the window closes on the next pass through `workRemains`. **Costs:** the repository keeps a state a reader must remember is transient, and `readSightingOrigin`'s LEFT JOIN becomes load-bearing rather than defensive. **Buys:** zero behaviour change, and a header that describes the shipped sweep exactly.
    - **C — Do nothing.** Ship as-is with the header contradicting the cascade, recorded and disclosed. Not recommended: this is the item the verifier singled out as the more interesting of the two warnings precisely because it is not a stale comment but a live gap between what the code does and what the module says it does.

**Under B, task 2's code half is ABANDONED and its prose half is the whole task.** Do not write
`SIGHTING_KEYS_FOR_DIGEST_SQL` under B. Task 3's single-pass case still runs under B, with its polarity
INVERTED: it asserts that one pass CAN leave an orphan under budget exhaustion and that the header says
so, which is the property B chooses. Task 3's boundary assertion about `sources` is unchanged under both.

**Under C, tasks 2 and 3 are both abandoned**, the answer and its date are recorded in the SUMMARY, and
the plan returns `## CHECKPOINT REACHED` to the orchestrator with nothing committed.
  </options>
  <resume-signal>The operator replies with a single option letter — A, B or C — optionally followed by a constraint. Any other reply is treated as unresolved and the plan halts rather than choosing a default: this is the one item the verifier escalated as depending on what the operator's earlier cascade choice meant, and inferring that meaning here would be the second time it was inferred. Record the letter and the date in the SUMMARY before any edit to `retention.ts`.</resume-signal>
  <verify>
    <human-check>The operator states A, B or C, and confirms that no foreign key, no `ON DELETE CASCADE` and no migration is implied by their choice.</human-check>
  </verify>
  <acceptance_criteria>
    - An option letter and a date are recorded in `07-20-SUMMARY.md` before any edit to `packages/backend/src/store/retention.ts`.
    - If the answer is B, the SUMMARY states explicitly that `SIGHTING_KEYS_FOR_DIGEST_SQL` was NOT written and that task 3's case polarity is inverted; `grep -c 'SIGHTING_KEYS_FOR_DIGEST_SQL' packages/backend/src/store/retention.ts` reports 0.
    - If the answer is C, `git diff --stat` over this plan's `files_modified` prints nothing, the SUMMARY records what was abandoned, and the plan returns `## CHECKPOINT REACHED` rather than substituting work of its own.
    - The SUMMARY records the operator's answer to the foreign-key confirmation verbatim.
  </acceptance_criteria>
  <done>An option is recorded in the SUMMARY with its date, and tasks 2 and 3 proceed only under it.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the chosen repair, and correct the column-arity sentence</name>
  <reversibility rating="costly">The choice decides which of two contradictory statements in the repository becomes the truth. Undoing it after the fact means rewriting the module header a second time and inverting the single-pass case's polarity — many call sites of one claim, not one line. It is NOT one-way: no schema, no persisted format and no published contract moves, and either direction reverts by a single-commit revert.</reversibility>
  <files>packages/backend/src/store/retention.ts</files>
  <read_first>
    - `packages/backend/src/store/retention.ts:1112-1193` — `deleteDigest` in full: the `left()` helper, the `obsLimit` / `anaLimit` computation against `CANDIDATE_SCAN_LIMIT`, the two enumeration loops with their `if (left() <= 0)` capped returns, the reached-the-LIMIT completeness check, and the `DELETE_ARTIFACT_SQL` call with its `artifactRemoved` bookkeeping. The new block must match this shape statement for statement.
    - `packages/backend/src/store/retention.ts:468-512` — `SIGHTINGS_OVER_AGE_SQL`, `SIGHTINGS_OLDEST_SQL`, `ORPHAN_SIGHTINGS_SQL` and `COUNT_SIGHTINGS_SQL`, for the exact column list, the tie-break ordering convention and the `NOT EXISTS`-with-`project_id`-on-both-sides idiom.
    - `packages/backend/src/store/retention.ts:508-512` — `DELETE_SIGHTING_SQL`, which already binds all four key columns and is REUSED by the new block; no second delete statement is written.
    - `packages/backend/src/store/retention.ts:494-498` — `ORPHAN_SIGHTINGS_SQL`'s comment, whose column-arity sentence is IN-03 and is corrected in this task.
    - `packages/backend/src/store/retention.ts:520-585` — the `UNSIGHTED_SOURCES_SQL` essay: the anti-join, the 1,600x measurement, and the `AND source_sha256 IS NOT NULL` paragraph. **Read it before writing anything, because it is the argument for why `deleteDigest` must not touch `sources`.**
    - `packages/backend/src/store/retention.ts:1195-1260` — `ChildTableSpec` and `trimChildTable`, and the recorded decision NOT to generalise it. The new block is written inline in `deleteDigest`, not through this helper.
    - `packages/backend/src/store/sql-discipline.spec.ts:715-770` — the audited-file list, which already includes `retention.ts`. A new statement is audited automatically; it must be `project_id`-scoped, single-statement and fully bound, with no interpolation and no array bind.
    - `07-20-SUMMARY.md` as task 1 leaves it — the recorded option letter. **Read it first and branch on it.**
  </read_first>
  <action>
Branch on the recorded option letter. Do not begin until it is in the SUMMARY.

**UNDER A — the cascade.** Declare `SIGHTING_KEYS_FOR_DIGEST_SQL` beside the other sightings
statements, selecting `map_sha256` and `source_index` from `source_sightings` where `project_id` and
`artifact_sha256` are both bound, ordered `map_sha256` then `source_index` ascending so a capped
enumeration resumes deterministically, with a `LIMIT ?`. Two columns and not four: `project_id` and
`artifact_sha256` are the bound scope, and the delete binds all four from the two selected plus the two
bound — say that in the comment, because it is the same distinction IN-03 records one statement above.

Add a third enumeration block in `deleteDigest`, positioned AFTER the analyses loop and BEFORE the
reached-the-LIMIT completeness check. Compute its limit as `Math.min(left(), CANDIDATE_SCAN_LIMIT)` and
guard the enumeration on a non-positive limit exactly as the analyses block does. Add the returned row
count to `examined`. Loop with the same `if (left() <= 0) return { capped: true, artifactRemoved: false }`
arm, deleting through the shipped `DELETE_SIGHTING_SQL` with all four key columns bound — the project,
the digest this cascade is for, and the two selected columns coerced the way the neighbouring loops
coerce theirs.

Extend the completeness check to include the sightings arm, so reaching the sightings LIMIT leaves the
artifact standing exactly as reaching the observations LIMIT does. This is the half most likely to be
forgotten, and forgetting it would delete a parent while its enumeration was capped — the precise
failure the check exists to prevent.

**`deleteDigest` must NOT delete from `sources`, and the code must say why.** `sources` is
content-addressed and shared across bundles; a delete driven off the evicted bundle's sightings would
take a row out from under a bundle still sighting it. That is the failure `UNSIGHTED_SOURCES_SQL`'s
anti-join exists to prevent, it is already pinned by the two-bundles case, and the eviction order the
operator chose is exactly the anti-join. Write one sentence in `deleteDigest`'s docblock saying that
`source_sightings` is a child of `artifacts` and cascades here, while `sources` is not a child of
anything and dies with its LAST sighting in step 3d, across passes by design.

Update the module header at `:42-45`, the orphan paragraph at `:389-394` and `deleteDigest`'s own
DEPENDENCY ORDER docblock so all three name THREE children. Add one sentence to the orphan paragraph
recording that step 3d's collection is a crash-recovery path again rather than the ordinary one, and
naming WR-02 as the reason the distinction is now written down.

**UNDER B — the scoping.** Write no SQL and change no control flow. Rewrite the same three paragraphs to
say plainly: `observations` and `analyses` cascade in the same statement sequence; `source_sightings` is
reaped by step 3d's orphan collection and may therefore outlive its artifact for one pass BY DESIGN; the
window closes on the next pass because `workRemains` re-detects the state through `ORPHAN_SIGHTINGS_SQL`;
and `readSightingOrigin`'s LEFT JOIN is what makes the transient state safe to read through. Name the
`budget() > 0` guard and its `sightingsCapped` arm by name, because the paragraph is only useful if a
reader can find the line that makes the claim true.

**UNDER BOTH — IN-03.** `ORPHAN_SIGHTINGS_SQL`'s comment says it selects "the four key columns"; it
selects three, because `project_id` is bound rather than selected. Correct the sentence to say the three
non-scope key columns are selected, that `project_id` is the bound scope, and that every delete below
binds all four. One sentence, in the file this task already edits.

**UNDER BOTH — the prohibitions are checkable and will be checked.** No `FOREIGN KEY`, no
`ON DELETE CASCADE`, no `PRAGMA foreign_keys`, no new migration step, no `exec` with more than one
statement, no interpolated SQL. If a repair seems to need any of them, stop and record why rather than
introducing one.
  </action>
  <acceptance_criteria>
    - The SUMMARY's recorded option letter is quoted at the top of this task's notes, and the branch taken matches it.
    - Under A: `SIGHTING_KEYS_FOR_DIGEST_SQL` exists, binds `project_id` and `artifact_sha256`, selects exactly `map_sha256` and `source_index`, carries an explicit two-column `ORDER BY` and a `LIMIT ?`.
    - Under A: `deleteDigest` contains three enumeration blocks; the sightings block sits after the analyses block and before the completeness check; the completeness check names all three limits.
    - Under A: `DELETE_SIGHTING_SQL` is reused; no second sighting-delete statement is declared.
    - Under B: `grep -c 'SIGHTING_KEYS_FOR_DIGEST_SQL' packages/backend/src/store/retention.ts` reports 0 and `git diff` over `retention.ts` shows changed comment lines only — no changed statement and no changed control flow.
    - Under both: `retention.ts` contains no `FOREIGN KEY`, no `ON DELETE CASCADE` and no `PRAGMA foreign_keys`; the check is run and its output recorded.
    - Under both: `deleteDigest`'s docblock states that `source_sightings` is a child of `artifacts` and that `sources` is not, with the anti-join named as what owns `sources`.
    - Under both: the module header at `:42-45`, the orphan paragraph and `deleteDigest`'s docblock agree with each other and with the code; the three passages are quoted in the SUMMARY.
    - Under both: `ORPHAN_SIGHTINGS_SQL`'s comment states three selected columns and one bound scope.
    - `pnpm vitest run packages/backend/src/store/sql-discipline.spec.ts` exits 0 — the new statement, if any, complies with the shipped SQL discipline.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run packages/backend/src/store/retention.spec.ts packages/backend/src/store/sql-discipline.spec.ts --reporter=dot</automated>
    <fails_when>non-zero exit; a `sql-discipline` violation naming `retention.ts` means the new statement is unscoped, interpolated or array-bound, and a `retention.spec.ts` failure at the two-bundles case means the cascade reached `sources`</fails_when>
    <automated>grep -nE 'FOREIGN KEY|ON DELETE CASCADE|PRAGMA foreign_keys' packages/backend/src/store/retention.ts</automated>
    <fails_when>any matching line is printed — the operator excluded all three explicitly and this is the check that proves the repair honoured that</fails_when>
    <automated>git diff --name-only -- packages/backend/src/store/migrations.ts packages/backend/src/store/schema.spec.ts</automated>
    <fails_when>any output at all — a repair that reaches the migration ladder or the table inventory is a schema change, which is a hard stop this round</fails_when>
  </verify>
  <done>The cascade and the header state the same thing, whichever the operator chose, and no schema moved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The SINGLE-PASS case, RED first, plus the boundary the claim stops at</name>
  <reversibility rating="reversible">Spec cases and verification runs.</reversibility>
  <files>packages/backend/src/store/retention.spec.ts</files>
  <read_first>
    - `packages/backend/src/store/retention.spec.ts:73-310` — the seeding and counting helpers in full: `seedArtifacts`, `seedSource`, `seedSighting`, `sightingsNaming`, `sourceIds`, `danglingSourceRows`, `directCount`, `statementText` and `sweepToConvergence`. The new cases use these and must not add a parallel set.
    - `packages/backend/src/store/retention.spec.ts:293-310` — `sweepToConvergence`'s body, which shows the direct `sweepRetention(fx.db, projectId, bounds, nowMs)` call. **The new case calls `sweepRetention` ONCE and never the loop.** That distinction is the entire reason this gap exists: `:1337` drives the loop, so the single-pass property was never the one under test.
    - `packages/backend/src/store/retention.spec.ts:1316-1420` — the three cascade cases, for the register their failure messages are written in and for the `MAP_A` / `SRC_ONLY_A` fixture-digest convention.
    - `packages/backend/src/store/retention.spec.ts:1560-1600` — the budget-exhausted sightings-pass case, which is the closest existing analog to the backlog this new case builds.
    - `packages/backend/src/store/retention.ts:905-960` — step 3d, so the case's backlog is sized to actually reach the `else { sightingsCapped = true; }` arm rather than merely to look large.
    - `packages/engine/src/thresholds.ts` — `RETENTION_SWEEP_MAX_ROWS`, imported so the backlog size is DERIVED. Plan 07-18 corrected this constant's documentation in the same wave; do not write its value out by hand.
    - `07-20-SUMMARY.md` as tasks 1 and 2 leave it — the option letter, which sets this case's polarity.
  </read_first>
  <behavior>
    - Seed one map-bearing artifact as the OLDEST, carrying a sighting and a `sources` row only it sights, then seed enough additional childless artifacts that one pass's deletions exceed `RETENTION_SWEEP_MAX_ROWS` before step 3d is reached. Call `sweepRetention` exactly once.
    - Under repair A: no `source_sightings` row names the evicted artifact after that single pass, because the sighting died inside the artifact's own cascade.
    - Under repair B: a `source_sightings` row DOES still name the evicted artifact after that single pass, and the module header says so — the case asserts the documented transient state rather than its absence.
    - Under both: the `sources` row the evicted bundle alone sighted SURVIVES the single pass, because `sources` is not a child of `artifacts` and step 3d's anti-join did not run. This is the boundary assertion and it holds in both directions.
    - Under both: a second `sweepToConvergence` run from the same state converges to zero orphans and zero unsighted sources, proving the across-passes property the existing cases already own is not weakened.
    - The existing cascade cases at `:1337`, `:1373` and the budget-exhaustion case at `:1573` are unchanged and still green.
  </behavior>
  <action>
Write the case first, observe RED, then read the result against the option letter.

**Build the backlog from the constant, not from a number.** Import `RETENTION_SWEEP_MAX_ROWS` and derive
the artifact count as that value plus a small margin. A hand-written 520 rots the moment the constant
moves; a derived one keeps exhausting the budget for ever. Seed the map-bearing artifact FIRST so
oldest-first ordering reaches it before the budget is spent — that ordering is the mechanism, and a case
that seeded it last would pass for the wrong reason.

**Use `sweepRetention` directly, exactly once.** Not `sweepToConvergence`, not a loop, not a helper that
wraps a loop. Write a comment in the case saying why, naming `:1337` as the case that drives the loop and
therefore cannot see this property. That sentence is the durable half of this fix: the next person adding
a cascade case will otherwise reach for the convergence helper again.

**Assert the single-pass property in the direction the option letter chose.** Under A, assert
`sightingsNaming(P1, evicted)` is 0 after the one pass. Under B, assert it is greater than 0 and add an
assertion that the module header's text contains the sentence saying so — reuse the `RETENTION_SOURCE`
text constant the file already holds, in the `AUDIT_OVER_AGE_SQL` absence case's idiom. Under B the
documented transient state IS the property, so the documentation is part of what the test pins.

**Assert the boundary in both directions.** The `sources` row the evicted bundle alone sighted must
still exist after the single pass. State in the assertion message that this is not a bug: `sources` is
content-addressed, is not a child of `artifacts`, and dies with its LAST sighting through the anti-join
in step 3d, which did not run because the budget was spent. Without this assertion the first case reads
as a promise about `sources` that no repair in this plan makes.

**Then run the convergence half.** From the same post-pass state, call the existing `sweepToConvergence`
helper and assert `danglingSourceRows(P1)` reports zero orphan sightings and zero unsighted sources.
That is the across-passes property the existing cases own; this proves the single-pass fix did not cost
it.

**Under A only, add the statement case.** Use the existing `statementText` helper to assert
`SIGHTING_KEYS_FOR_DIGEST_SQL` contains both bound predicates, an explicit `ORDER BY` naming both
selected columns ascending, and a `LIMIT ?`. That is the same discipline the `SCANS_*` statement cases
apply, and it is what stops a later edit dropping the tie-break and making a capped enumeration
resumable only by luck.

**Record the RED.** Run the case against the state task 2 started from and record the observed value and
message. Under A the pre-fix run must report a non-zero `sightingsNaming`; under B the pre-fix run must
report a missing header sentence. If the case is GREEN before task 2's edit, the backlog did not exhaust
the budget — fix the fixture, do not weaken the assertion.

**Run the round's baseline gates and record the numbers** against the verifier's recorded baseline of 90
files / 4,302 tests / exit 0 and four green static gates.

**Do not modify any file outside `files_modified`.**
  </action>
  <acceptance_criteria>
    - The single-pass case calls `sweepRetention` exactly once; `sweepToConvergence` appears in the case only in the separate convergence-half assertion.
    - The backlog size is computed from an imported `RETENTION_SWEEP_MAX_ROWS`; no artifact count literal appears in the case.
    - The case was RED before task 2's edit; the observed value and message are recorded verbatim in the SUMMARY, together with confirmation that the budget was genuinely exhausted.
    - The boundary assertion states that the evicted bundle's sole `sources` row survives the single pass, with the reason in the assertion message.
    - The convergence-half assertion reports zero orphan sightings and zero unsighted sources.
    - Under A: a statement case asserts `SIGHTING_KEYS_FOR_DIGEST_SQL`'s two bound predicates, its two-column ascending `ORDER BY` and its `LIMIT ?`.
    - Under B: the case asserts the module header text contains the sentence documenting the transient state, read from the file's existing source-text constant.
    - The three existing cascade cases and the budget-exhaustion case are unmodified and green; their titles are quoted in the SUMMARY.
    - `pnpm vitest run` reports no fewer than 90 files and 4,302 tests and exits 0; both recorded.
    - `pnpm typecheck`, `pnpm lint`, `pnpm knip` and `pnpm build` each exit 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run packages/backend/src/store/retention.spec.ts --reporter=dot</automated>
    <fails_when>non-zero exit, or the dot summary reports a lower test count than before this task; a failure at the two-bundles case means the repair reached `sources`, and a failure at the boundary assertion means step 3d ran when the fixture intended it not to</fails_when>
    <automated>pnpm vitest run packages/backend/src/store/retention.spec.ts packages/backend/src/ingest/consumer.spec.ts packages/backend/src/store/sql-discipline.spec.ts --reporter=dot</automated>
    <fails_when>non-zero exit; a `consumer.spec.ts` failure here means a retention change reached the ingest path, which this plan does not touch</fails_when>
    <automated>pnpm vitest run --reporter=dot</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 90 files or fewer than 4302 tests</fails_when>
    <automated>pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm knip &amp;&amp; pnpm build</automated>
    <fails_when>non-zero exit from any of the four</fails_when>
    <automated>git diff --name-only -- packages/backend/src/store</automated>
    <fails_when>the output lists any path other than `packages/backend/src/store/retention.ts` and `packages/backend/src/store/retention.spec.ts` — the pathspec is what keeps 07-19's and 07-21's uncommitted edits in the shared working tree out of this plan's scope check</fails_when>
  </verify>
  <done>One `sweepRetention` call pins the chosen property, the boundary is stated, and convergence is unweakened.</done>
</task>

</tasks>

<planner_assumptions>
## Spec-less edge-probe fallback — this plan's slice

The round-scoped 16-row record lives in `07-18-PLAN.md` under `<planner_assumptions>`; **16 surfaced,
16 flagged, 0 dropped, 0 authored as `must_haves.truths`**. This plan carries **MAP-06**, whose single
probe row is:

| # | Req | Category | Probe question | Flagged planner assumption |
|---|-----|----------|----------------|----------------------------|
| 13 | MAP-06 | unclassified | unclassified — review manually | Reviewed manually. MAP-06's aggregate half is what puts rows into `source_sightings` and `sources`, and retention is what bounds them. The assumption carried forward, flagged rather than resolved: that ONE sweep pass is the right granularity at which to state a cascade invariant. No requirement says so; the header asserts it and this plan is what makes the assertion checkable in one direction or honest in the other. |
</planner_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| target-chosen row volume → the retention sweep's per-pass budget | The target decides how many artifacts and how many map-bearing bundles exist, and therefore whether a pass's budget is spent before step 3d is reached. |
| module header → the next engineer's belief about what an eviction leaves behind | The paragraph is what a reader uses to decide whether an orphan is reachable and whether a read path needs to defend against one. |
| one shared SQLite file → per-project row scoping | Every candidate statement binds `project_id` on both sides of every anti-join, so a sweep in one project cannot reach another project's rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-84 | Repudiation | `retention.ts`'s thrice-stated no-orphan invariant | medium | mitigate | The header tells a reader the cascade cannot create an orphan; the cascade creates one on the ordinary path for every evicted map-bearing bundle when the budget is spent. Task 2 makes the two agree in the direction the operator chooses, and task 3 pins it with a SINGLE-PASS case rather than the convergence loop that could never see it. |
| T-07-85 | Denial of Service | a target driving pass budget exhaustion to defer the anti-join indefinitely | medium | mitigate | A target that keeps enough childless artifacts evictable can spend every pass's budget before step 3d, deferring both the orphan collection and the unsighted-sources anti-join. Repair A removes the sighting half of that exposure by cascading inside `deleteDigest`; the `sources` half remains bounded by `workRemains` and `RETENTION_SWEEP_MAX_PASSES`, and task 3's convergence-half assertion is what proves it still drains. |
| T-07-86 | Tampering | a repair introducing a foreign key or `ON DELETE CASCADE` | high | mitigate | Explicitly excluded by the operator. `PRAGMA foreign_keys` is per-connection and the pool holds up to five connections, so a foreign key would be enforced on some connections and not others — a per-connection correctness property is not a correctness property. Task 2 carries a grep over the three forbidden constructs as a runnable verify, and a schema-file diff check as a second. |
| T-07-87 | Tampering | `deleteDigest` reaching `sources` | high | mitigate | A delete driven off the evicted bundle's sightings would remove a content-addressed row another bundle still sights, silently destroying evidence for a bundle that was never evicted. Prohibited in the plan, argued in `deleteDigest`'s docblock, and already pinned by the two-bundles case at `retention.spec.ts:1373`, which task 3 re-runs and quotes. |
| T-07-88 | Information Disclosure | cross-project reach in the new statement | medium | mitigate | Under A the new statement binds `project_id` and `artifact_sha256`, matching `ORPHAN_SIGHTINGS_SQL`'s both-sides scoping (T-07-56). `sql-discipline.spec.ts` audits `retention.ts` and would report an unscoped statement; it is a runnable verify on task 2. |
| T-07-89 | Denial of Service | a multi-statement `exec` stranding a write transaction | high | mitigate | Measured, not feared: a failed multi-statement `exec` leaves an open write transaction on a pooled connection nothing in the plugin API can reach, and the database is locked for writes until the plugin restarts. The new block is a sequence of independent single-row deletes through the shipped `remove` function, exactly like the two blocks beside it; the prohibition block forbids anything else. |

No package-manager install task exists in this plan, so no supply-chain threat row applies. Blocking
threshold is `high`; T-07-86, T-07-87 and T-07-89 reach it and each carries a runnable check rather than
an assurance — that is what discharges them.
</threat_model>

<verification>
- `pnpm vitest run packages/backend/src/store/retention.spec.ts packages/backend/src/ingest/consumer.spec.ts packages/backend/src/store/sql-discipline.spec.ts --reporter=dot` — exit 0.
- `pnpm vitest run --reporter=dot` — no fewer than 90 files and 4,302 tests, exit 0.
- `pnpm typecheck && pnpm lint && pnpm knip && pnpm build` — all exit 0.
- `grep -nE 'FOREIGN KEY|ON DELETE CASCADE|PRAGMA foreign_keys' packages/backend/src/store/retention.ts` — no output.
- `git diff --name-only -- packages/backend/src/store/migrations.ts packages/backend/src/store/schema.spec.ts` — empty.
- `git diff --name-only -- packages/backend/src/store` — only `retention.ts` and `retention.spec.ts`.
</verification>

<success_criteria>
- One repair is chosen at a blocking checkpoint and recorded with its date before any edit.
- The module header, the orphan paragraph and `deleteDigest`'s docblock all state the same thing, and the code does that thing.
- A single `sweepRetention` call pins the chosen property, in a case that was RED.
- The `sources` boundary is asserted in both directions, so the claim is not read as wider than it is.
- No foreign key, no `ON DELETE CASCADE`, no migration, and `SCHEMA_VERSION` still 9.
</success_criteria>

<output>
Create `.planning/phases/07-sourcemap-reconstruction/07-20-SUMMARY.md` when done.
Record: the option letter and date, the operator's foreign-key confirmation verbatim, the before/after
text of the three header passages, the new statement's text under A or the explicit note that it was not
written under B, the observed RED value and message of the single-pass case with confirmation the budget
was exhausted, the boundary assertion's result, the convergence-half result, the IN-03 sentence
before and after, the four forbidden-construct check outputs, the full-gate numbers against the
verifier's baseline, and G-07-2 / WR-02 plus IN-03 marked closed with their commits.
</output>
