---
phase: 07-sourcemap-reconstruction
plan: 26
type: execute
wave: 1
depends_on: []
gap_closure: true
gap_ids: [G-07-9, G-07-12]
nyquist_compliant: true
autonomous: true
requirements: [MAP-07, UI-05]
files_modified:
  - packages/backend/src/store/export.ts
  - .planning/phases/07-sourcemap-reconstruction/07-24-PLAN.md
  - .planning/ROADMAP.md

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The `sources_verbatim` column comment in `EXPORT_COLUMNS` describes the two cuts by their SHIPPED semantics — a protocol-shaped label DELEGATES to `redactUrlForExport`, every other label is cut by hand at its first `?` and keeps its `#` tail — and makes NO claim about how either axis's scope changed over time. (D-06: the raw `sources` entry is stored verbatim and this column is the one target-controlled column governed by the export redaction policy.)"
    - "The clause asserting a symmetric change of scope across the two axes is GONE from that column comment, and the region carries no direction word at all. The repair is subtraction: the region's byte count strictly decreases and no sentence is added to replace the deleted one."
    - "`redactSourceLabelForExport`'s docblock states the shared query marker as `observations.url`'s shipped behaviour on every REDACTED row it has ever written, agreeing with `serialiseRows`, which applies `column.redact` only when the mode is redacted."
    - "The planning record that AUTHORED the deleted claim carries a dated errata block reconstructing the column's real history from `d5cd5e0`, `a901b9e` and `0e44102`, and `07-24-PLAN.md`'s `must_haves.truths[4]` no longer asserts a change of scope in either direction."
    - "The ROADMAP's round-3 `07-24-PLAN.md` entry no longer repeats the claim, and the entry itself survives — its requirement tags and its G-07-6/G-07-7 subject are intact, so the correction is not a deletion of the record."
    - "No target-controlled string reaches any label, dialog or download name on the manifest-export path; the `sources` label appears only as a manifest COLUMN governed by the export redaction policy, never as a control label. (`07-UI-SPEC.md`, `long-text / manifest-export`, resolved explicit — unchanged by this plan and re-asserted by it.)"
    - "`redactSourceLabelForExport`'s BODY, `redactUrlForExport`, `isProtocolShapedLabel`, `serialiseRows`, `EXPORT_QUERY_REDACTION`, both redacting column bindings and `observations.url`'s shipped output are all byte-identical to HEAD, proven by named md5 digests rather than asserted, and `export.spec.ts` is unmodified."
  prohibitions:
    - "ADD NO SENTENCE to the `sources_verbatim` column comment. The repair for G-07-9 is DELETION. That one sentence has carried a false claim in three consecutive rounds — G-07-3 under 07-16, G-07-6 under 07-22, WR-01/G-07-9 under 07-24 — and each repair was a restatement that became the next round's defect. The region's final clause already points the reader at `redactSourceLabelForExport`, whose docblock states each axis against a named baseline at `export.ts:255-275`. A fourth telling here is the move that has failed three times."
    - "No word-presence probe may stand as the sole acceptance evidence for any prose must-have in this plan (VF-01, `07-UAT.md` test 5). The evidence that shipped the current falsehood was two `grep -c` counts on direction adverbs, which are structurally incapable of falsifying a directional claim. Every prose must-have here is gated on a region-scoped ABSENCE assertion, on a byte-count decrease, or on a `git show` comparison — each falsifiable by the shipped text."
    - "No edit to `.planning/phases/07-sourcemap-reconstruction/07-24-SUMMARY.md`. It is the record of what was EXECUTED, and `07-UAT.md` and `07-VERIFICATION.md` both cite its lines 97 and 100 as the evidence for VF-01. Rewriting it would destroy the evidentiary chain that justifies this round's gate change. Its diffstat must stay EMPTY."
    - "No rewrite of the twelve other restatements of the claim inside `07-24-PLAN.md`'s body. That file records what was planned and executed; falsifying it retroactively is a worse defect than the one being repaired. The errata block governs them and says so explicitly."
    - "No change to `redactUrlForExport`. The operator explicitly declined the vocabulary repair as a decision needing its own round (`07-UAT.md` G-07-7); this plan records the exception's mode boundary, it does not remove the exception."
    - "No change to the export vocabulary. `EXPORT_QUERY_REDACTION` keeps its single spelling and gains no sibling; the count of the literal marker text in `export.ts` is 2 at HEAD and must stay 2."
    - "No change to `observations.url`'s shipped output, to the `{ name: \"url\", redact: redactUrlForExport }` binding, or to the `{ name: \"sources_verbatim\", redact: redactSourceLabelForExport }` binding."
    - "No change to `serialiseRows`. It is the EVIDENCE for G-07-12's qualifier — its conditional at `export.ts:606-609` is why the unqualified sentence is false — not a function to adjust so the sentence becomes true."
    - "No edit to `packages/backend/src/store/export.spec.ts`. Its fragment-only URL case is the pinned evidence for the accepted exception, not a pin to relax."
    - "No edit to the quoted-by-reference withholding text at `export.ts:291-295`. The operator ACCEPTED it as discharging 07-22's criterion and it is adjacent to lines this plan does change."
    - "No new schema migration. `SCHEMA_VERSION` is 9 and stays 9; this round is comment-only in `packages/`."
    - "No new foreign key and no `ON DELETE CASCADE`, anywhere, for any reason."
    - "No change to `MAP_MAX_BYTES` (W-6), no wiring of `vue-tsc` into a gate (W-4), no frame-budget backstop in `tests/frontend-load.spec.ts` (round-1 UAT gap 3), no docblock caveat on `derivedRejected.depth_exceeded` (IN-04). All open by operator decision and named here rather than silently omitted."
    - "No edit to `packages/engine/src/thresholds.ts` or `packages/engine/src/thresholds.spec.ts`. Those are plan 07-27's subjects and `workflow.use_worktrees` is false — the two plans share one working tree and are serialised for that reason."
    - "No lowering of `RETENTION_SWEEP_MAX_PASSES`, and no change to `parse.ts`, `derive.ts`, `telemetry.ts`, `retention.ts`, `migrations.ts` or `consumer.spec.ts`."
    - "No outbound fetch (D-01) and no filesystem access from plugin code (D-17)."
    - "A comment must never make a historical claim the repository's own git history contradicts, and must never state of every row a behaviour that only one export mode produces."
  artifacts:
    - "packages/backend/src/store/export.ts — the `sources_verbatim` entry's column comment in `EXPORT_COLUMNS`, three lines shorter, carrying no axis-direction clause"
    - "packages/backend/src/store/export.ts — `redactSourceLabelForExport`'s docblock, KNOWN AND ACCEPTED EXCEPTION paragraph, with the mode qualifier the file already uses at `:172` and `:191`"
    - ".planning/phases/07-sourcemap-reconstruction/07-24-PLAN.md — a dated errata block immediately below the frontmatter reconstructing the column's history from three named commits, plus a corrected `must_haves.truths[4]`"
    - ".planning/ROADMAP.md — the round-3 `07-24-PLAN.md` entry, corrected in place with a dated marker, requirement tags and subject intact"
  key_links:
    - "the `sources_verbatim` column comment → `redactSourceLabelForExport`'s docblock at `export.ts:255-275` → the git history of the column binding — the three-hop chain whose middle link is CORRECT and whose first link contradicted it 190 lines away in the same file"
    - "`07-24-PLAN.md`'s `must_haves.truths[4]` → the round-3 ROADMAP entry → `export.ts:449-452` — the authoring chain. The defect was planned, not mis-executed, so cutting only the source link leaves the plan able to re-author it in round 5"
    - "`serialiseRows`'s `mode === 'redacted' && column.redact !== null` conditional → the sentence at `export.ts:311-313` — the link between what the code does per mode and what the comment claims of every row"
---

<objective>
Delete the axis-direction claim from the `sources_verbatim` column comment, add the mode qualifier the
rest of `export.ts` already uses, and correct the two planning documents that authored the deleted
claim.

This closes **G-07-9** and **G-07-12**, both in `packages/backend/src/store/export.ts`, planned
together for the same reason 07-24 paired G-07-6 with G-07-7: one file, one reader, one audit. The
column comment is the first thing a redaction-policy review reads and the function docblock is the
second, and serialising two comment edits in one file across two plans would buy nothing but a second
whole-suite run.

- **G-07-9.** `export.ts:449-452` asserts that the two redaction axes moved in opposite directions.
  The verifier reconstructed the column's history from git — `d5cd5e0` (07-06), `a901b9e` (07-16),
  `0e44102` (07-22) — and there is **no baseline under which that is true**; each half of the sentence
  borrows a different one. `export.ts:261` reads "THE FRAGMENT AXIS, UNMOVED", a direct contradiction
  190 lines away in the same file. This sentence has now shipped a false claim in **three consecutive
  rounds**, and each repair was a restatement that became the next round's defect. The operator chose
  the verifier's preferred repair: **DELETE it.** A sentence that makes no historical claim cannot
  make a false one.
- **G-07-12.** `export.ts:311-313` states the shared query marker as `observations.url`'s behaviour
  "on every row it has ever written". `serialiseRows` at `:606-609` applies `column.redact` only when
  `mode === 'redacted'`, so in raw mode that column writes the URL whole and no marker appears. Two
  words, zero behavioural impact, and the file already qualifies exactly this at `:172` and `:191`.

**And the origin is the plan, not the execution.** `07-24-PLAN.md`'s `must_haves.truths[4]` states
the false claim verbatim and the round-3 ROADMAP entry repeats it. A round-5 plan derived from those
documents re-ships the sentence unless they are corrected too, which is why the planning-record repair
is task 2 rather than a footnote.

Purpose. Round 4 is explicitly instructed not to run the way rounds 1–3 ran. The change that gives it
different odds is **VF-01**: the acceptance evidence for a prose must-have must be able to falsify the
CLAIM, not detect a WORD. The evidence that shipped the current falsehood was `grep -c 'NARROWED'`
→ 1 and `grep -c 'WIDENED'` → 1. That RED→GREEN transition proved that two words were typed and
nothing else. Every prose gate in this plan is therefore a region-scoped ABSENCE assertion, a strictly
decreasing byte count, or a `git show` comparison — each of them falsifiable by the shipped text.

Output: a column comment that makes no historical claim, a docblock sentence that names the mode it
describes, and a planning record from which the falsehood cannot be inherited.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sourcemap-reconstruction/07-CONTEXT.md
@.planning/phases/07-sourcemap-reconstruction/07-UAT.md
@.planning/phases/07-sourcemap-reconstruction/07-VERIFICATION.md
@.planning/phases/07-sourcemap-reconstruction/07-24-SUMMARY.md
</context>

<gap_trace>
| Gap id | Source | What this plan closes |
|---|---|---|
| G-07-9 / test 1 | `07-UAT.md` gap `G-07-9` (minor); `07-VERIFICATION.md` finding `WR-01` | The axis-direction clause is DELETED from the `sources_verbatim` column comment, and `07-24-PLAN.md`'s `must_haves.truths[4]` plus the ROADMAP's round-3 07-24 entry — the two documents that authored it — are corrected so the falsehood cannot be inherited. |
| G-07-12 / test 4 | `07-UAT.md` gap `G-07-12` (minor); `07-VERIFICATION.md` finding `IN-02` | `export.ts:311-313` gains the REDACTED qualifier the file already uses at `:172` and `:191`, so it stops claiming of every row a behaviour only the redacted mode produces. |
| VF-01 / test 5 | `07-UAT.md` `## Round-4 Planning Constraints`, first bullet | Binding on every prose task here: no `grep -c '<word>'` probe stands as sole acceptance evidence. Discharged by absence gates, a byte-count decrease and a recorded three-commit `git show` comparison. |
</gap_trace>

<artifacts_this_plan_produces>
## Artifacts this phase produces

**No new symbol of any kind.** No new file, no new export, no new constant, no new test, no new
helper, no new dependency, no new vocabulary word, and no new sentence in `export.ts`.

**Changed comment text in one git-tracked source file** (`packages/backend/src/store/export.ts`):
- The `sources_verbatim` entry's column comment in `EXPORT_COLUMNS` loses its axis-direction clause.
  The region between the `WHAT DOES NOT SURVIVE` anchor and the `{ name: "sources_verbatim"` binding
  goes from **15 lines / 1,104 bytes** to **13 lines** and strictly fewer bytes.
- `redactSourceLabelForExport`'s docblock gains one word — `REDACTED` — inside the KNOWN AND ACCEPTED
  EXCEPTION paragraph.

**New planning-record artifact:**
- A dated **errata block** in `.planning/phases/07-sourcemap-reconstruction/07-24-PLAN.md`,
  positioned immediately after the closing `---` of the frontmatter and before `<objective>`,
  carrying a three-row table reconstructed from `d5cd5e0`, `a901b9e` and `0e44102`.

**Corrected planning-record text:**
- `07-24-PLAN.md`'s `must_haves.truths[4]`, rewritten so it makes no claim about a change of scope.
- `.planning/ROADMAP.md`'s round-3 `07-24-PLAN.md` entry, corrected in place with a dated marker.

**Explicitly NOT produced, and each one is a prohibition above:** no replacement sentence in the
column comment, no edit to `07-24-SUMMARY.md`, no rewrite of `07-24-PLAN.md`'s body restatements, no
change to any redactor, binding, vocabulary word or spec file.

**Every path this plan writes is git-tracked**, verified with `git ls-files`:
`packages/backend/src/store/export.ts`,
`.planning/phases/07-sourcemap-reconstruction/07-24-PLAN.md`, `.planning/ROADMAP.md`.
</artifacts_this_plan_produces>

<source_coverage_audit>
## Multi-source coverage audit

| Source | Item | Status | Where |
|---|---|---|---|
| GOAL | ROADMAP Phase 7 — reconstructed source browsable and exportable with a manifest | COVERED (regression scope only) | No behaviour moves; the full-suite gate in task 3 carries it |
| REQ | **MAP-07** — reconstructed source browsable in the plugin UI and exportable with a manifest | COVERED | Tasks 1 and 3: the manifest's one target-controlled column and its redaction policy comment |
| REQ | **UI-05** — reconstructed-source viewer | COVERED | Task 1 and `must_haves.truths[6]`: the `sources` label appears only as a manifest column, never as a control label |
| REQ | MAP-01 … MAP-05 | NOT IN THIS PLAN — met, verified and untouched by both gaps; carried as regression scope through task 3's full-gate run | Same disposition round 3 recorded |
| REQ | **MAP-06** | NOT IN THIS PLAN — carried by plan 07-27, which owns G-07-10 and G-07-11 | `07-27-PLAN.md` |
| RESEARCH | Export redaction policy: per-axis redaction with a single shared marker | COVERED (unchanged, re-asserted) | Prohibitions: vocabulary count pinned at 2, both redactors md5-pinned |
| CONTEXT | **D-06** — the raw `sources` entry is stored VERBATIM and normalised only for display | COVERED | `must_haves.truths[1]`; the raw option remains the only route to unredacted bytes and nothing here changes it |
| CONTEXT | **D-01** — no outbound fetch | COVERED (prohibition) | Prohibitions block |
| CONTEXT | **D-17** — no filesystem access from plugin code | COVERED (prohibition) | Prohibitions block; this plan's disk reads are `git`/`awk` at verification time, not shipped code |
| CONTEXT | D-09, D-10, D-13 | NOT IN THIS PLAN — thresholds and retention are plan 07-27's subject (D-09) or open by operator decision (D-10 / W-6) | `07-27-PLAN.md`; prohibitions block |

**No item is MISSING.** The four round-4 gaps split cleanly across two plans by file; W-4, W-6,
round-1 UAT gap 3, IN-04, SC5's second half and MAP-01's external half are open by operator decision
and carried as prohibitions rather than dropped.
</source_coverage_audit>

<planner_assumptions>
## Spec-less edge-probe fallback — this plan's slice

The round-scoped record remains `07-23-PLAN.md`'s `<planner_assumptions>` block: **16 applicable
edges, 0 resolved, 16 unresolved**, all sixteen flagged there as explicit planner assumptions, none
authored as a `must_haves.truths` entry. No-silent-drop equality holds at the round level. Restated
here is only the row for the requirement IDs this plan carries.

| # | Req | Category | Probe question | Flagged planner assumption |
|---|-----|----------|----------------|----------------------------|
| 14 | MAP-07, UI-05 | unclassified | unclassified — review manually | Reviewed manually. The export redaction policy's BEHAVIOUR is verified and is not reopened: no redactor, binding or vocabulary word moves, and four md5 digests measured at HEAD are stated in this plan as the pins. What changes is prose ABOUT that behaviour. |

**One assumption is added by this plan and is flagged rather than resolved.** The errata block's
three-row history table is the verifier's reconstruction, restated. This plan measured the three
commits itself — all three resolve, and `git show <commit>:packages/backend/src/store/export.ts`
carries the `sources_verbatim` binding at line 301, 385 and 428 respectively — but the *semantic*
reading of each binding (which axis each one cuts, on which population of labels) is a judgement about
code, not a measurement. Task 3 requires the executor to re-derive that reading from the three
`git show` outputs directly and to record either agreement or disagreement. **If the executor's
reading disagrees with the table, stop and report** — do not silently write a second false history.

## UI Considerations touched by these gaps

`07-UI-SPEC.md`'s `long-text / manifest-export` consideration is resolved explicit and is the reason
`must_haves.truths[6]` exists: the `sources` label is a manifest COLUMN, never a control label, so no
target-controlled string reaches a label, dialog or download name. Nothing in this plan renders, and
the consideration is re-asserted rather than revisited.
</planner_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Delete the axis-direction claim, and name the mode in the exception paragraph</name>
  <reversibility rating="reversible">Two comment edits in one file. No behaviour, no schema, no contract, no data. A single-commit revert restores HEAD exactly.</reversibility>
  <files>packages/backend/src/store/export.ts</files>
  <read_first>
    - `packages/backend/src/store/export.ts:427-454` — the whole `sources_verbatim` column comment, from the `WHAT DOES NOT SURVIVE` anchor to the binding itself. **Read all fifteen lines before deleting three of them.** Note that the region's closing clause already points the reader at `redactSourceLabelForExport`; that pointer is why nothing needs to be added in place of the deletion.
    - `packages/backend/src/store/export.ts:255-275` — `redactSourceLabelForExport`'s docblock, specifically the `THE FRAGMENT AXIS, UNMOVED` paragraph and the `THE QUERY AXIS, RESTORED` paragraph. **This is the source of truth and it is CORRECT.** Each paragraph states one axis against a NAMED baseline (07-16, and 07-16's premise about a non-URL label). It must survive byte-unchanged. `:261`'s "UNMOVED" is what the column comment currently contradicts 190 lines away.
    - `packages/backend/src/store/export.ts:305-313` — the `THE KNOWN AND ACCEPTED EXCEPTION, ON THE DELEGATED BRANCH` paragraph, which is task 1's second subject. Its final clause states a behaviour of `observations.url` without naming which export mode produces it.
    - `packages/backend/src/store/export.ts:606-609` — `serialiseRows`'s conditional, `mode === 'redacted' && column.redact !== null ? column.redact(text) : text`. **This is the evidence for the second edit and it must not move.** In raw mode `observations.url` writes the URL whole and no marker appears, which is the large class of rows the unqualified sentence describes wrongly.
    - `packages/backend/src/store/export.ts:172` and `:191` — the two places the file already qualifies this correctly ("What a covered field reads as in the redacted mode"). Match their register; do not invent a third phrasing.
    - `packages/backend/src/store/export.ts:331-340` — `redactSourceLabelForExport`'s BODY. Read it so you can see the boundary: the docblock above it changes in this task, the body does not, and its md5 is pinned in the acceptance criteria.
    - `.planning/phases/07-sourcemap-reconstruction/07-UAT.md` — gap `G-07-9`, whose FIRST and FOURTH `missing` bullets are the fence this task is most likely to be overreached against: the repair is deletion, and nothing behavioural may move. And gap `G-07-12`, whose single substantive bullet is two words.
  </read_first>
  <behavior>
    - The region between the `WHAT DOES NOT SURVIVE` anchor and the `{ name: "sources_verbatim"` binding contains no direction word of any case, and no clause asserting a symmetric change of scope across the two axes.
    - That same region still contains `DELEGATES`, still contains the phrase describing the by-hand cut at the first `?`, and still contains its closing pointer to the function — so the deletion removed the false claim and not the description of the shipped behaviour.
    - The region is exactly 13 lines and strictly fewer than 1,104 bytes. Nothing was added.
    - `redactSourceLabelForExport`'s docblock states the shared marker as `observations.url`'s shipped behaviour on every REDACTED row it has ever written; the unqualified form of that clause appears nowhere in the docblock.
    - `redactSourceLabelForExport`'s body, `redactUrlForExport`, `isProtocolShapedLabel` and `serialiseRows` all hash to the digests measured at HEAD.
    - The literal export marker text still appears exactly twice in the file.
  </behavior>
  <action>
This task is SUBTRACTION plus one word. Do not use `cp` anywhere in this plan; `cp` is aliased to
`cp -i` here and silently no-ops an overwrite. If you need to undo, use `git checkout --`.

**Step 1 — record the RED state.** Run these four and paste the output into the SUMMARY verbatim,
before and after:

- `awk '/WHAT DOES NOT SURVIVE/,/name: "sources_verbatim"/' packages/backend/src/store/export.ts | grep -c 'WHAT DOES NOT SURVIVE'` prints `1` today and must STILL print `1` — the non-vacuity companion for every negated gate below.
- `awk '/WHAT DOES NOT SURVIVE/,/name: "sources_verbatim"/' packages/backend/src/store/export.ts | wc -l` prints `15` today and must print `13`.
- `awk '/WHAT DOES NOT SURVIVE/,/name: "sources_verbatim"/' packages/backend/src/store/export.ts | wc -c` prints `1104` today and must print strictly fewer.
- `awk '/The redacted form of one manifest/,/^export function redactSourceLabelForExport/' packages/backend/src/store/export.ts | sed 's|^[[:space:]]*\*[[:space:]]*||' | tr '\n' ' ' | grep -c 'on every REDACTED row it has ever written'` prints `0` today and must print `1`.

**Step 2 — the deletion (G-07-9).** In the column comment, the sentence ending `... not one redactor
reused` currently closes with an em dash that runs on into a clause asserting that the two axes
changed scope in mutually opposite ways. That clause spans two full lines and the first word of a
third.

1. Replace the trailing em dash after `not one redactor reused` with a full stop.
2. Delete everything from there through the word `label.` that opens the next line — the entire
   scope-change clause, and nothing else.
3. Re-wrap only what remains of the following sentence, which begins `Still not a per-column
   exemption` and ends `is at that function.` Its wording is byte-identical; only the line breaks
   move, to keep the file's comment width.

**Add nothing.** Not a replacement sentence, not a corrected version, not a pointer, not a note. The
region's closing clause already sends the reader to `redactSourceLabelForExport`, where `:255-275`
states each axis against a named baseline. This is the single most important instruction in the task:
three previous rounds each repaired this sentence by restating it, and each restatement was the next
round's defect. The byte-count gate exists precisely to catch an addition dressed as a repair.

**Step 3 — the qualifier (G-07-12).** In `redactSourceLabelForExport`'s docblock, inside the paragraph
headed `THE KNOWN AND ACCEPTED EXCEPTION, ON THE DELEGATED BRANCH`, the final clause states the
shared marker as `observations.url`'s shipped behaviour across the whole history of that column
without naming the export mode. Insert the single word `REDACTED` between `on every` and the words
`row it has ever written`, then re-wrap the paragraph so the line width matches its neighbours.
Change nothing else in that paragraph.

The justification is `serialiseRows` at `:606-609`: `column.redact` runs only when
`mode === 'redacted'`, so in raw mode that column writes the URL whole and the marker never appears.
Quote that conditional in the SUMMARY as the evidence. Do NOT adjust `serialiseRows` to make the old
sentence true — its md5 is pinned below.

**Step 4 — prove nothing behavioural moved.** Run the four md5 probes in the acceptance criteria and
paste each digest into the SUMMARY next to its expected value. These four digests were measured at
HEAD by the planner; if any of them differs, a construct this plan is forbidden to touch has moved
and you must stop and report rather than continue.
  </action>
  <acceptance_criteria>
    - **Source:** `awk '/WHAT DOES NOT SURVIVE/,/name: "sources_verbatim"/' packages/backend/src/store/export.ts | grep -c 'WHAT DOES NOT SURVIVE'` prints `1` — the region anchors still resolve, so the absence gates below are not passing over an empty stream.
    - **Source:** the same region piped to `grep -ciE 'narrow|widen'` prints `0`. This is the VF-01 gate: an ABSENCE assertion over a bounded region is falsifiable by the shipped text, where a presence count is not.
    - **Source:** the same region piped to `grep -c 'OPPOSITE directions'` prints `0` — the deleted clause's identifying fragment is gone, not merely reworded.
    - **Source:** the same region piped to `wc -l` prints `13`, and piped to `wc -c` prints strictly fewer than `1104`. The repair is subtraction; a byte count that did not fall means a sentence was added.
    - **Source:** the same region piped to `grep -c 'DELEGATES'` prints `1` and piped to `grep -c 'cut by hand at its first'` prints `1` — the shipped behaviour is still described, so the deletion took the false claim and not the explanation.
    - **Source:** `awk '/The redacted form of one manifest/,/^export function redactSourceLabelForExport/' packages/backend/src/store/export.ts | sed 's|^[[:space:]]*\*[[:space:]]*||' | tr '\n' ' ' | grep -c 'on every REDACTED row it has ever written'` prints `1`, and the same pipeline grepping for the same phrase without the mode word prints `0`.
    - **Source:** `grep -c 'query-redacted' packages/backend/src/store/export.ts` prints `2` — the vocabulary did not grow while the prose about it was edited.
    - **Behavior:** four md5 digests, each equal to the value measured at HEAD — `redactSourceLabelForExport` body `35a5e79faaada339fcc14f0889de4bed`, `redactUrlForExport` `bab965d76e6877dbf9ba0ba6e31e58fc`, `isProtocolShapedLabel` `0130e429cf81c63c7d60c5f5c63789fd`, `serialiseRows` `b17979f72c0cb2a42161af1f96ecb42e`.
    - **Test command:** `pnpm exec vitest run packages/backend/src/store/export.spec.ts --reporter=dot` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>awk '/WHAT DOES NOT SURVIVE/,/name: "sources_verbatim"/' packages/backend/src/store/export.ts | grep -c 'WHAT DOES NOT SURVIVE'</automated>
    <fails_when>the printed count is anything other than 1 — the NON-VACUITY companion for the three negated gates below. A 0 means one of the two awk anchors moved and the range collapsed, which would let every absence gate pass over an empty stream; a 2 or more means the opening anchor was duplicated and the range no longer bounds the column comment</fails_when>
    <automated>! awk '/WHAT DOES NOT SURVIVE/,/name: "sources_verbatim"/' packages/backend/src/store/export.ts | grep -qiE 'narrow|widen'</automated>
    <fails_when>non-zero exit, which here means a direction word of some case still stands inside the `sources_verbatim` column comment. Region-scoped deliberately: `export.ts:253` and `:335` carry the same stem in correct, required prose elsewhere in the file, so a file-wide ban would be unsatisfiable without deleting text this plan is forbidden to touch</fails_when>
    <automated>! awk '/WHAT DOES NOT SURVIVE/,/name: "sources_verbatim"/' packages/backend/src/store/export.ts | grep -q 'OPPOSITE directions'</automated>
    <fails_when>non-zero exit — the deleted clause's identifying fragment survives, which means the sentence was reworded rather than removed. Rewording is the move that failed in three consecutive rounds and this gate exists to refuse it</fails_when>
    <automated>awk '/WHAT DOES NOT SURVIVE/,/name: "sources_verbatim"/' packages/backend/src/store/export.ts | wc -l</automated>
    <fails_when>the printed count is anything other than 13. 15 means nothing was deleted; 14 means only part of the clause went; anything above 15 means a replacement sentence was added, which is the prohibition at the top of this plan's `must_haves.prohibitions`</fails_when>
    <automated>test "$(awk '/WHAT DOES NOT SURVIVE/,/name: \"sources_verbatim\"/' packages/backend/src/store/export.ts | wc -c)" -lt 1104</automated>
    <fails_when>non-zero exit — the region's byte count did not strictly fall below the 1,104 measured at HEAD. This is the addition detector: a repair that adds words while removing words can hold the line count at 13 and still smuggle in a fourth telling of the history</fails_when>
    <automated>awk '/WHAT DOES NOT SURVIVE/,/name: "sources_verbatim"/' packages/backend/src/store/export.ts | grep -c 'cut by hand at its first'</automated>
    <fails_when>the printed count is 0 — the positive half. The deletion was supposed to remove a claim about how the axes CHANGED, not the description of what the two cuts DO. A zero here means the comment was gutted and a reader now has no description of the by-hand branch at all</fails_when>
    <automated>awk '/The redacted form of one manifest/,/^export function redactSourceLabelForExport/' packages/backend/src/store/export.ts | sed 's|^[[:space:]]*\*[[:space:]]*||' | tr '\n' ' ' | grep -c 'on every REDACTED row it has ever written'</automated>
    <fails_when>the printed count is anything other than 1. The `sed` strips the leading comment markers and `tr` collapses the docblock to one line, so this matches the sentence regardless of where the re-wrap put the line breaks — a raw line-oriented grep would go green or red on the wrap rather than on the words</fails_when>
    <automated>! awk '/The redacted form of one manifest/,/^export function redactSourceLabelForExport/' packages/backend/src/store/export.ts | sed 's|^[[:space:]]*\*[[:space:]]*||' | tr '\n' ' ' | grep -q 'shipped behaviour on every row it has ever written'</automated>
    <fails_when>non-zero exit — the unqualified form of the clause is still in the docblock, so the mode qualifier was added somewhere else and the false statement survives beside it</fails_when>
    <automated>grep -c 'query-redacted' packages/backend/src/store/export.ts</automated>
    <fails_when>the printed count is anything other than 2 — the export vocabulary grew or shrank while the prose about it was being edited. 2 is the value measured at HEAD and it is what makes "no new vocabulary word" checkable rather than merely promised</fails_when>
    <automated>awk '/^export function redactSourceLabelForExport/,/^}/' packages/backend/src/store/export.ts | md5 -q</automated>
    <fails_when>the printed digest is anything other than `35a5e79faaada339fcc14f0889de4bed`. This task edits the DOCBLOCK above this function; a changed digest means the edit reached into the body, which is a behavioural change in a round that is allowed none. On a non-darwin host substitute `md5sum | cut -d' ' -f1`</fails_when>
    <automated>awk '/^export function redactUrlForExport/,/^}/' packages/backend/src/store/export.ts | md5 -q</automated>
    <fails_when>the printed digest is anything other than `bab965d76e6877dbf9ba0ba6e31e58fc`. The operator explicitly declined the vocabulary repair to this function as a decision needing its own round; a changed digest means it was taken as a drive-by</fails_when>
    <automated>awk '/^export function isProtocolShapedLabel/,/^}/' packages/backend/src/store/export.ts | md5 -q</automated>
    <fails_when>the printed digest is anything other than `0130e429cf81c63c7d60c5f5c63789fd` — the shape test that decides which branch a label takes moved, which would move the redaction policy this plan only describes</fails_when>
    <automated>awk '/^export function serialiseRows/,/^}/' packages/backend/src/store/export.ts | md5 -q</automated>
    <fails_when>the printed digest is anything other than `b17979f72c0cb2a42161af1f96ecb42e`. `serialiseRows` is the EVIDENCE for the qualifier — its `mode === 'redacted'` conditional is why the unqualified sentence is false. Changing it to make the old sentence true is the inverse of the fix and is forbidden outright</fails_when>
    <automated>git diff --name-only -- packages/</automated>
    <fails_when>the output lists any path other than `packages/backend/src/store/export.ts`. A listed `export.spec.ts` means the pinned fragment-only URL case was relaxed; a listed `packages/engine` path means this plan reached into plan 07-27's file, and `workflow.use_worktrees` is false so the two share one working tree</fails_when>
    <automated>pnpm exec vitest run packages/backend/src/store/export.spec.ts --reporter=dot</automated>
    <fails_when>non-zero exit. Nothing behavioural changed, so any failure here is a real regression rather than a stale expectation — read it as a signal that an edit landed outside a comment</fails_when>
  </verify>
  <done>The `sources_verbatim` column comment is 13 lines, strictly smaller in bytes, carries no direction word and no scope-change clause, and still describes both cuts; the exception paragraph names the redacted mode; four md5 digests match HEAD; `export.spec.ts` is unmodified and green.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the two planning documents that authored the deleted claim</name>
  <reversibility rating="reversible">Two planning documents, both git-tracked, no source and no behaviour. A single-commit revert restores both.</reversibility>
  <files>.planning/phases/07-sourcemap-reconstruction/07-24-PLAN.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/phases/07-sourcemap-reconstruction/07-24-PLAN.md:20-47` — the whole `must_haves` block. The fifth entry under `truths:` (index 4, the one beginning "That column comment states the axis directions correctly") is the sentence that authored the defect. Read the four entries around it so the replacement matches their register.
    - `.planning/phases/07-sourcemap-reconstruction/07-24-PLAN.md` in full, at least by skim — twelve further restatements of the claim live in its body (objective, `read_first`, `behavior`, action, acceptance criteria, verify, success criteria). **They are NOT to be rewritten.** Know where they are so you can confirm you left them alone and so the errata block can say truthfully that it governs them.
    - `.planning/phases/07-sourcemap-reconstruction/07-24-SUMMARY.md:92-102` — entry `D4` and its two `grep -c` verification refs at lines 97 and 100. **This file is READ-ONLY in this plan.** `07-UAT.md` test 5 and `07-VERIFICATION.md` finding VF-01 both cite those exact line numbers as the evidence that a word-presence probe cannot falsify a semantic claim. Editing it would destroy the evidence that justifies this round's gate change.
    - `.planning/ROADMAP.md` — the single line beginning `- [x] 07-24-PLAN.md`, inside the `**Gap-closure round 3 — Wave 2**` section. It repeats the claim in its own words and ends with the `(MAP-07, UI-05)` requirement tags. The correction is in place; the entry must survive.
    - `.planning/phases/07-sourcemap-reconstruction/07-UAT.md` — gap `G-07-9`, `missing` bullet 2, which is exactly this task, and its `artifacts` list, which names these two files and no others.
    - `packages/backend/src/store/export.ts:427-454` as task 1 left it. The corrected `truths[4]` must be TRUE of the file after task 1, so read the committed result rather than trusting this plan's description of it.
  </read_first>
  <behavior>
    - `07-24-PLAN.md` carries a dated errata block positioned after the frontmatter's closing `---` and before `<objective>`, containing a three-row table naming `d5cd5e0`, `a901b9e` and `0e44102` with each commit's `sources_verbatim` binding and the population of labels each axis cut.
    - That block states in so many words that the file's other occurrences of the claim are left byte-unchanged on purpose, that this file is the record of what was planned and executed, and that `07-24-SUMMARY.md` cites it.
    - `07-24-PLAN.md`'s frontmatter `must_haves` block contains no direction word of any case.
    - The corrected `truths[4]` is a true statement about `export.ts` as task 1 left it, and it makes no claim about a change of scope over time.
    - The ROADMAP's `07-24-PLAN.md` entry still exists, still names its G-07-6/G-07-7 subject, still ends with `(MAP-07, UI-05)`, carries a dated correction marker, and contains no direction word.
    - `07-24-SUMMARY.md` is byte-unchanged.
  </behavior>
  <action>
The defect was AUTHORED here, not mis-executed. `07-24-PLAN.md`'s `must_haves.truths[4]` states the
false claim verbatim and the ROADMAP entry repeats it, so a round-5 plan derived from either document
re-ships the sentence task 1 just deleted. That is why this is a task and not a footnote.

**Step 1 — record the RED state.** Run these and paste before/after into the SUMMARY:

- `sed -n '/^must_haves:/,/^---$/p' .planning/phases/07-sourcemap-reconstruction/07-24-PLAN.md | grep -c '^  truths:'` prints `1` today and must STILL print `1` — the non-vacuity companion; if the frontmatter range collapses, the absence gate beneath it means nothing.
- `sed -n '/^must_haves:/,/^---$/p' .planning/phases/07-sourcemap-reconstruction/07-24-PLAN.md | grep -ciE 'narrowed|widened'` prints `1` today and must print `0`.
- `grep -c '^- \[x\] 07-24-PLAN.md' .planning/ROADMAP.md` prints `1` today and must STILL print `1` — the entry survives the correction.
- `grep '^- \[x\] 07-24-PLAN.md' .planning/ROADMAP.md | grep -ciE 'narrowed|widened'` prints `1` today and must print `0`.

**Step 2 — write the errata block into `07-24-PLAN.md`.** Place it immediately after the closing `---`
of the frontmatter and immediately before the `<objective>` tag, as a Markdown blockquote so it reads
as an overlay on the document rather than as part of it. It must carry, in this order:

1. A heading naming the date `2026-09-03`, gap-closure round 4, and `07-UAT.md` gap `G-07-9`, and
   instructing the reader to read it before any other line of the file.
2. One sentence stating plainly that this plan authored a false claim about the `sources_verbatim`
   column's history and that the claim shipped into `export.ts`.
3. A three-column table — commit, the `sources_verbatim` binding at that commit, and which population
   of labels each of the two axes cut — with one row for each of `d5cd5e0` (07-06), `a901b9e` (07-16)
   and `0e44102` (07-22). Take the binding text from the `git show` outputs task 3 records, not from
   this plan's prose.
4. The conclusion the table supports: no baseline exists under which the two axes changed scope in
   mutually opposite ways, because each half of the original claim borrowed a different baseline.
5. What round 4 did: plan 07-26 DELETED the sentence from `export.ts` rather than restating it a
   fourth time, because `export.ts:255-275` already states each axis against a named baseline; and
   `must_haves.truths[4]` below is corrected.
6. An explicit statement that every other occurrence of the claim in this file is left byte-unchanged
   ON PURPOSE — this file records what was planned and executed, `07-24-SUMMARY.md` cites it, and
   retroactively falsifying it would be a worse defect than the one being repaired. The block governs
   them; no reader should treat any of them as current.
7. A closing line telling the reader to verify the table themselves with
   `git show <commit>:packages/backend/src/store/export.ts`.

Write no direction word anywhere in the block. Describe the movement as a change of scope and name
the population of labels each axis cut at each commit — that is the concrete, checkable form, and it
is what makes the table falsifiable against `git show` rather than a re-narration.

**Step 3 — correct `must_haves.truths[4]`.** Replace the entry with one that is TRUE of `export.ts` as
task 1 left it and that makes no claim about a change of scope over time. Its substance must be: the
column comment describes the two cuts by their shipped semantics — a protocol-shaped label delegating
to `redactUrlForExport`, every other label cut by hand at its first `?` and keeping its `#` tail — and
makes no historical claim, the full baseline-anchored history living in `redactSourceLabelForExport`'s
docblock. Append to the same entry a bracketed, dated correction marker naming round 4 and `G-07-9`
and pointing at the errata block. Leave the other five `truths` entries and every `prohibitions`,
`artifacts` and `key_links` entry byte-unchanged.

**Step 4 — correct the ROADMAP entry.** In the single line beginning `- [x] 07-24-PLAN.md`, replace
the clause that states the two axis directions with a phrase describing the restatement by its shipped
semantics, then append a dated correction marker in the same line: name 2026-09-03, round 4, `G-07-9`,
say that the entry previously asserted a change of scope in mutually opposite directions, that the
assertion was false under every baseline in the file's git history, and point at `07-26-PLAN.md` and
at the errata block in `07-24-PLAN.md`. **Edit the line in place.** Do not delete it, do not renumber
the wave, and do not touch the `(MAP-07, UI-05)` tags at its end — a deleted entry would satisfy an
absence gate while destroying the record, which is why the gates below check that the entry survives
and still carries its tags.

Use scoped edits throughout. `.planning/ROADMAP.md` is a very large file and a whole-file rewrite
would destroy every phase entry outside the diff window.

**Step 5 — prove the SUMMARY was not touched.** `git diff --stat -- .planning/phases/07-sourcemap-reconstruction/07-24-SUMMARY.md` must be EMPTY. Record the empty output in this plan's SUMMARY next to a one-line statement of why: `07-UAT.md` test 5 and `07-VERIFICATION.md` VF-01 both cite its lines 97 and 100 as the evidence for the gate change this whole round rests on.
  </action>
  <acceptance_criteria>
    - **Source:** `sed -n '/^must_haves:/,/^---$/p' .planning/phases/07-sourcemap-reconstruction/07-24-PLAN.md | grep -c '^  truths:'` prints `1` — the frontmatter range still resolves, so the absence gate below is not scanning an empty stream.
    - **Source:** the same range piped to `grep -ciE 'narrowed|widened'` prints `0`.
    - **Source:** `grep -c 'ERRATA' .planning/phases/07-sourcemap-reconstruction/07-24-PLAN.md` prints at least `1`, and the errata block's line number is greater than the frontmatter's closing `---` and less than the line carrying `<objective>`.
    - **Source:** `07-24-PLAN.md` contains each of `d5cd5e0`, `a901b9e` and `0e44102` at least once — the three commits the table is reconstructed from, so a reader can re-derive it rather than trust it.
    - **Source:** `grep -c '^- \[x\] 07-24-PLAN.md' .planning/ROADMAP.md` prints `1`, that line piped to `grep -ciE 'narrowed|widened'` prints `0`, and the same line still contains `(MAP-07, UI-05)` and `G-07-6`.
    - **Source:** that same ROADMAP line contains a dated 2026-09-03 correction marker naming `G-07-9`.
    - **CLI output:** `git diff --stat -- .planning/phases/07-sourcemap-reconstruction/07-24-SUMMARY.md` produces no output.
    - **CLI output:** `git diff --name-only -- .planning/` lists only `.planning/ROADMAP.md` and `.planning/phases/07-sourcemap-reconstruction/07-24-PLAN.md`, plus this round's own new PLAN files.
  </acceptance_criteria>
  <verify>
    <automated>sed -n '/^must_haves:/,/^---$/p' .planning/phases/07-sourcemap-reconstruction/07-24-PLAN.md | grep -c '^  truths:'</automated>
    <fails_when>the printed count is anything other than 1 — the NON-VACUITY companion for the negated gate below. A 0 means the frontmatter's `must_haves:` block or its closing `---` moved and the sed range collapsed, which would let the absence gate pass over nothing</fails_when>
    <automated>! sed -n '/^must_haves:/,/^---$/p' .planning/phases/07-sourcemap-reconstruction/07-24-PLAN.md | grep -qiE 'narrowed|widened'</automated>
    <fails_when>non-zero exit — `truths[4]` still asserts a change of scope, so the document that AUTHORED the defect can still author it again. Region-scoped to the frontmatter on purpose: twelve further occurrences live in the body and are deliberately preserved as the record of what was planned and executed</fails_when>
    <automated>grep -c 'ERRATA' .planning/phases/07-sourcemap-reconstruction/07-24-PLAN.md</automated>
    <fails_when>the printed count is 0 — the twelve preserved body occurrences are then ungoverned, and a future reader hits the false claim with nothing overlaying it. This is the half of the repair that makes preserving the record safe rather than negligent</fails_when>
    <automated>grep -c 'd5cd5e0' .planning/phases/07-sourcemap-reconstruction/07-24-PLAN.md</automated>
    <fails_when>the printed count is 0 — the errata block narrates a history without naming the commit a reader would check it against, which is the same unfalsifiable shape as the claim it replaces</fails_when>
    <automated>grep -c '^- \[x\] 07-24-PLAN.md' .planning/ROADMAP.md</automated>
    <fails_when>the printed count is anything other than 1. A 0 means the entry was DELETED rather than corrected — which would satisfy the absence gate below while destroying the round-3 record — and a 2 or more means the entry was duplicated instead of edited in place</fails_when>
    <automated>! grep '^- \[x\] 07-24-PLAN.md' .planning/ROADMAP.md | grep -qiE 'narrowed|widened'</automated>
    <fails_when>non-zero exit — the ROADMAP still repeats the claim in the third artifact that carries it. Line-scoped because the ROADMAP carries the same stem in correct prose on at least six other lines describing unrelated phases</fails_when>
    <automated>grep '^- \[x\] 07-24-PLAN.md' .planning/ROADMAP.md | grep -c 'MAP-07, UI-05'</automated>
    <fails_when>the printed count is 0 — the entry survived the absence gate by being gutted rather than corrected. The requirement tags are the cheapest proof that the surrounding record is intact</fails_when>
    <automated>git diff --stat -- .planning/phases/07-sourcemap-reconstruction/07-24-SUMMARY.md</automated>
    <fails_when>any output at all. That file is the record of what was EXECUTED and both `07-UAT.md` test 5 and `07-VERIFICATION.md` VF-01 cite its lines 97 and 100 as the evidence for VF-01. A non-empty diffstat means the evidence for this round's own gate change was edited away</fails_when>
    <automated>git diff --name-only -- packages/</automated>
    <fails_when>the output lists any path other than `packages/backend/src/store/export.ts`. This task edits planning documents only; a `packages/` path beyond task 1's file means a planning-record edit reached into source</fails_when>
  </verify>
  <done>`07-24-PLAN.md` carries a dated errata block naming the three commits and stating that the body's other occurrences are preserved on purpose, its `must_haves.truths[4]` makes no claim about a change of scope, the ROADMAP entry is corrected in place with its tags and subject intact, and `07-24-SUMMARY.md` is byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Falsify the history against git, then run the repository gate</name>
  <reversibility rating="reversible">Read-only verification plus the round's commit. Nothing is written to source; the only artifact is recorded output.</reversibility>
  <files>packages/backend/src/store/export.ts</files>
  <read_first>
    - `packages/backend/src/store/export.ts:427-454` and `:242-330` as tasks 1 and 2 left them, re-read whole. The column comment and the function docblock must read as one consistent account of the same function, which is the thing three rounds failed at.
    - `.planning/phases/07-sourcemap-reconstruction/07-UAT.md` — `## Round-4 Planning Constraints`, first bullet (VF-01). This task is that bullet's second half made runnable: "for any surviving historical claim, a review sign-off against `git show` of d5cd5e0 / a901b9e / 0e44102".
    - `.planning/phases/07-sourcemap-reconstruction/07-VERIFICATION.md` — the round-4 report body, especially "The multi-round trend". It is the argument for why a fourth restatement was refused, and the SUMMARY should state whether this plan's execution agrees with it.
    - `.planning/phases/07-sourcemap-reconstruction/07-25-SUMMARY.md` — the full-suite counts round 3 closed on. This round's floor is the round-4 verification baseline: 90 files / 4,322 tests, exit 0.
  </read_first>
  <behavior>
    - Three `git show` invocations resolve and their `sources_verbatim` binding lines are recorded verbatim in the SUMMARY.
    - The executor's own reading of which population of labels each axis cut at each commit is stated, and is either confirmed to agree with the errata table or reported as a disagreement that stops the plan.
    - The surviving history in `redactSourceLabelForExport`'s docblock at `:255-275` is checked against the same three outputs and confirmed to name its baselines correctly.
    - The full suite reports 90 files and 4,322 tests, exit 0 — no test added, none removed, because nothing behavioural changed.
    - `typecheck`, `lint`, `knip` and `build` all exit 0.
    - The working tree ends with exactly one modified path under `packages/`.
  </behavior>
  <action>
This task exists because of VF-01. Rounds 1–3 accepted prose repairs on word-presence probes and each
round shipped a false sentence. A `grep -c` on a word cannot falsify a claim about history; a
comparison against the history can.

**Step 1 — read the history out of git, not out of a document.** Run all three and paste the output
into the SUMMARY verbatim:

- `git show d5cd5e0:packages/backend/src/store/export.ts | grep -n 'name: "sources_verbatim"'`
- `git show a901b9e:packages/backend/src/store/export.ts | grep -n 'name: "sources_verbatim"'`
- `git show 0e44102:packages/backend/src/store/export.ts | grep -n 'name: "sources_verbatim"'`

Then, for each commit, read enough of the surrounding redactor to answer TWO questions in your own
words and write both answers into the SUMMARY: on which population of labels did the QUERY axis get
cut, and on which population did the FRAGMENT axis get cut. For `a901b9e` and `0e44102` you will need
the body of the label redactor at that commit as well as the binding.

**Step 2 — compare, and stop if you disagree.** Set your three-commit reading beside the errata table
task 2 wrote. If they agree, say so explicitly in the SUMMARY and name the sentence in the round-4
diagnosis that your reading confirms. **If they disagree in any particular, STOP and report** — do not
edit the errata block to match your reading and do not proceed to the commit. A second false history
written to fix a first one is exactly the failure mode this round exists to break, and a disagreement
here is a finding worth more than a closed gap.

**Step 3 — check the SURVIVING historical claims.** `export.ts:255-275` keeps two paragraphs that each
state one axis against a NAMED baseline. Confirm from the same three `git show` outputs that each of
those two paragraphs is true of the baseline it names, and record the confirmation. These are the only
historical claims about this column that survive round 4 anywhere in `packages/`, and VF-01 requires
them to be signed off against git rather than against a word count. If either is false, report it as a
new finding rather than editing it — it is outside G-07-9's `missing` list, which is the authoritative
scope fence.

**Step 4 — run the repository gate.** `workflow.use_worktrees` is false, so round 4's two plans run
SEQUENTIALLY on the main working tree and this plan is the first. The floor is the round-4
verification baseline recorded in `07-VERIFICATION.md`: **90 files / 4,322 tests, exit 0**. Nothing in
this plan adds or removes a test, so the count must be exactly 4,322 — not "at least". A higher count
means a test appeared from somewhere this plan does not own.

Run `pnpm vitest run --reporter=dot`, then `pnpm typecheck && pnpm lint && pnpm knip && pnpm build`.
State the numbers you observed and where you read the floor.

**Step 5 — record the round's position.** State in the SUMMARY: G-07-9 and G-07-12 closed with their
commit; G-07-10 and G-07-11 open and owned by plan 07-27; and W-4, W-6, round-1 UAT gap 3, IN-04,
SC5's second half and MAP-01's external half all still open by operator decision and untouched. Also
record, as a one-line process note, whether the acceptance evidence in this plan felt able to falsify
the claims it gated — that judgement is the round's own answer to VF-01 and the verifier will read it.
  </action>
  <acceptance_criteria>
    - **CLI output:** the SUMMARY contains the verbatim output of all three `git show … | grep -n 'name: "sources_verbatim"'` invocations, each resolving to a real line.
    - **CLI output:** the SUMMARY states, per commit and in the executor's own words, which population of labels each of the two axes was cut on, and states explicitly whether that reading agrees with the errata table task 2 wrote.
    - **Source:** `export.ts:255-275`'s two surviving axis paragraphs are each confirmed true of the baseline they name, against the three `git show` outputs, with the confirmation recorded.
    - **Test command:** `pnpm vitest run --reporter=dot` exits 0 reporting exactly 90 files and exactly 4,322 tests.
    - **Test command:** `pnpm typecheck && pnpm lint && pnpm knip && pnpm build` exits 0.
    - **CLI output:** `git status --porcelain -- packages/` lists exactly one path, `packages/backend/src/store/export.ts`.
    - **CLI output:** the SUMMARY names the numeric floor used and the file it was read from, and carries the one-line VF-01 process note.
  </acceptance_criteria>
  <verify>
    <automated>git show d5cd5e0:packages/backend/src/store/export.ts | grep -c 'name: "sources_verbatim"'</automated>
    <fails_when>the printed count is 0, or the command exits non-zero because the commit does not resolve. This is the first of the three baselines the errata table is reconstructed from; if it cannot be read, the table is documentary rather than measured and the SUMMARY must say so instead of claiming a git-checked history</fails_when>
    <automated>git show a901b9e:packages/backend/src/store/export.ts | grep -c 'name: "sources_verbatim"'</automated>
    <fails_when>the printed count is 0, or the command exits non-zero because the commit does not resolve — the 07-16 baseline, the one both surviving docblock paragraphs at `export.ts:255-275` state themselves against</fails_when>
    <automated>git show 0e44102:packages/backend/src/store/export.ts | grep -c 'name: "sources_verbatim"'</automated>
    <fails_when>the printed count is 0, or the command exits non-zero because the commit does not resolve — the 07-22 change, the one whose description the deleted sentence got wrong</fails_when>
    <automated>awk '/The redacted form of one manifest/,/^export function redactSourceLabelForExport/' packages/backend/src/store/export.ts | grep -c '07-16'</automated>
    <fails_when>the printed count is 0 — the surviving history in the docblock no longer names the baseline it states each axis against, which turns the two paragraphs VF-01 requires signed off against git back into unanchored claims of the kind this whole round removes</fails_when>
    <automated>pnpm vitest run --reporter=dot</automated>
    <fails_when>non-zero exit, or the summary line reports anything other than 90 files and 4,322 tests. Exactly, not "at least": this plan changes only comments and planning documents, so a count above 4,322 means a test appeared from a source this plan does not own and a count below it means one was lost</fails_when>
    <automated>pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm knip &amp;&amp; pnpm build</automated>
    <fails_when>non-zero exit from any of the four. All four exit 0 at HEAD and nothing executable changed, so any failure names an edit that left a comment. `vue-tsc` is deliberately NOT in this chain — it is wired into no running gate (W-4) and is open by operator decision</fails_when>
    <automated>git status --porcelain -- packages/</automated>
    <fails_when>the output lists any path other than ` M packages/backend/src/store/export.ts`. `workflow.use_worktrees` is false and plan 07-27 shares this working tree; a listed `packages/engine` path means 07-27's edits are already present uncommitted and this plan's full-suite run just measured a tree it does not own</fails_when>
    <automated>git diff --stat HEAD -- packages/backend/src/store/export.spec.ts packages/backend/src/store/retention.ts packages/backend/src/store/migrations.ts packages/engine/src/thresholds.ts</automated>
    <fails_when>any output at all — the diffstat-EMPTY prohibition on the four files this round is fenced out of. `export.spec.ts` holds the pinned fragment-only URL case that is the evidence for the accepted exception; the other three are behaviour this comment-only round is forbidden to reach</fails_when>
  </verify>
  <done>All three commits resolved and their bindings are recorded verbatim, the executor's own reading of the two axes agrees with the errata table and says so, the two surviving docblock paragraphs are signed off against git, the suite is green at exactly 90 files and 4,322 tests with all four gates exit 0, and exactly one file under `packages/` is modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the export redaction policy → the comment an auditor reads to understand it | An operator deciding whether a redacted manifest is safe to share reads the column comment and the redactor docblock, not the code. A false claim there is a false claim about what is withheld, even when no byte of output moves. |
| the redacted export mode → the raw export mode | `serialiseRows` applies `column.redact` only in the redacted mode. Prose that describes a redacted-mode behaviour as universal erases that boundary for the reader. |
| the planning record → the next round's plan | A GSD planner reads prior PLAN.md and ROADMAP entries as source material. A false claim in either is not inert documentation; it is an input that reproduces itself. |
| a comment-only round → the redaction code beside the comments | Both edits sit inside or immediately above functions that decide what is withheld. The only way this round can cause harm is by reaching one byte too far. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-117 | Repudiation | the `sources_verbatim` column comment as an audit artifact | high | mitigate | An operator auditing the redaction policy reads this comment first. It has carried a claim its own file contradicts 190 lines away for three consecutive rounds, and an auditor who catches one false statement has no reason to trust the next — the same "operator trust and reviewability" cost the file already names at `:314-320`. Task 1 DELETES the claim rather than restating it a fourth time, gated on a region-scoped absence assertion plus a strictly decreasing byte count, both falsifiable by the shipped text. |
| T-07-118 | Tampering | a comment-only round reaching into redaction code | high | mitigate | Four md5 digests measured at HEAD are stated as literals in task 1's acceptance criteria — `redactSourceLabelForExport`'s body, `redactUrlForExport`, `isProtocolShapedLabel` and `serialiseRows` — plus a vocabulary count pinned at 2, a `git diff --name-only -- packages/` restricted to one path, and a diffstat-EMPTY gate over `export.spec.ts`, `retention.ts`, `migrations.ts` and `thresholds.ts`. Three independent gates, none of which relies on the executor's own report. |
| T-07-119 | Information Disclosure | the mode boundary at `export.ts:311-313` | medium | mitigate | A reader who believes the shared query marker appears on every row this column has ever written may conclude that a RAW export is also marked, and so under-estimate what the raw option discloses. `serialiseRows` at `:606-609` makes the marker redacted-mode-only. Task 1 adds the qualifier the file already uses at `:172` and `:191`, gated on presence of the qualified form AND absence of the unqualified one over the collapsed docblock. |
| T-07-120 | Repudiation | the executed record in `07-24-SUMMARY.md` | high | mitigate | The cheapest way to make this round look clean is to edit the SUMMARY that recorded the defective gate. `07-UAT.md` test 5 and `07-VERIFICATION.md` VF-01 both cite its lines 97 and 100 as the evidence justifying this round's gate change; editing it would destroy that chain. Task 2 carries a diffstat-EMPTY gate on it and an explicit prohibition, and requires the reason to be restated in the SUMMARY. |
| T-07-121 | Spoofing | a corrected history that is itself unverified | high | mitigate | Replacing one narrated history with another narrated history is the failure mode of rounds 1–3. Task 3 requires all three commits to be read out of git, the executor's own per-commit reading of each axis to be written down, and an explicit STOP-and-report if that reading disagrees with the errata table. The two surviving historical paragraphs at `:255-275` are separately signed off against the same outputs. |
| T-07-122 | Tampering | `serialiseRows` adjusted to make the old sentence true | high | mitigate | The inverse-of-the-fix move: make the code match the comment instead of the comment match the code. It would turn a raw export into a marked one, changing shipped output in a round allowed no behavioural change. Pinned by md5 `b17979f72c0cb2a42161af1f96ecb42e`, by the one-path `git diff --name-only`, and by an explicit prohibition naming it. |
| T-07-123 | Denial of Service | the ROADMAP entry deleted rather than corrected | medium | mitigate | A deletion satisfies every absence gate while destroying the round-3 record. Task 2 gates on the entry SURVIVING: its anchor count must be exactly 1, and the surviving line must still carry `(MAP-07, UI-05)` and `G-07-6`. |

No package-manager install task exists in this plan — no `npm`, `pnpm add`, `pip` or `cargo`
invocation appears in any action — so no supply-chain threat row applies and no package-legitimacy
checkpoint is required. Blocking threshold is `high`; T-07-117, T-07-118, T-07-120, T-07-121 and
T-07-122 reach it, and each is mitigated by a runnable gate or a recorded git comparison rather than
by assertion.
</threat_model>

<verification>
- The four region probes of task 1, recorded before and after: the non-vacuity anchor holding at 1, the direction-stem count falling to 0, the region falling from 15 lines to 13 and from 1,104 bytes to strictly fewer, and the mode-qualified clause rising from 0 to 1 over the collapsed docblock.
- The two positive halves of task 1 — `DELEGATES` and the by-hand-cut phrase still present — proving the deletion removed the claim and not the explanation.
- Four md5 digests matching the values measured at HEAD, plus the export vocabulary count holding at 2.
- The four planning-record probes of task 2, recorded before and after, including the two non-vacuity companions (the frontmatter `truths:` anchor and the ROADMAP entry anchor) that must both stay at 1.
- `git diff --stat -- .planning/phases/07-sourcemap-reconstruction/07-24-SUMMARY.md` — empty.
- Task 3's three `git show` outputs recorded verbatim, with the executor's own per-commit reading of each axis and an explicit statement of agreement or a STOP.
- `pnpm vitest run --reporter=dot` — exactly 90 files and exactly 4,322 tests, exit 0, against the round-4 baseline in `07-VERIFICATION.md`.
- `pnpm typecheck && pnpm lint && pnpm knip && pnpm build` — all exit 0. `vue-tsc` is excluded on purpose (W-4, open by operator decision).
- `git status --porcelain -- packages/` — exactly one path. Always with a pathspec: this round runs without worktrees and plan 07-27 shares the working tree.
</verification>

<success_criteria>
- The `sources_verbatim` column comment carries no direction word and no scope-change clause, is exactly 13 lines and strictly smaller in bytes than at HEAD, and still describes both cuts and still points the reader at the function.
- Nothing was added to that comment. The repair was subtraction, proven by the byte count rather than promised in prose.
- `redactSourceLabelForExport`'s docblock names the redacted mode where it previously spoke of every row, and the unqualified form appears nowhere in it.
- `07-24-PLAN.md` carries a dated errata block reconstructed from three named commits, its `must_haves.truths[4]` makes no claim about a change of scope, and the twelve body restatements are preserved on purpose and governed by the block.
- The ROADMAP's round-3 07-24 entry is corrected in place, survives with its tags and subject, and carries a dated marker.
- `07-24-SUMMARY.md` is byte-unchanged and the reason is recorded.
- The executor read all three commits out of git, wrote down their own reading of each axis, and confirmed agreement — or stopped and reported.
- Four md5 digests match HEAD, the vocabulary count holds at 2, and the repository gate is green at exactly 90 files and 4,322 tests with one file modified under `packages/`.
</success_criteria>

<output>
Create `.planning/phases/07-sourcemap-reconstruction/07-26-SUMMARY.md` when done.

Record: the four region probes of task 1 before and after; the before/after text of the deleted clause
and of the qualified sentence; the four md5 digests beside their expected values; the vocabulary count;
the four planning-record probes before and after; the empty `07-24-SUMMARY.md` diffstat with the
one-line reason; the three `git show` outputs verbatim with the executor's own per-commit reading of
each axis and an explicit agreement statement; the confirmation that `export.ts:255-275`'s two
surviving paragraphs are true of the baselines they name; the numeric full-suite floor used and the
file it came from; and G-07-9 and G-07-12 marked closed with their commit.

State the round's position at the close of this plan: G-07-9 and G-07-12 closed; **G-07-10 and G-07-11
open and owned by plan 07-27**, which shares this working tree and must be run after this plan is
committed; and W-4, W-6, round-1 UAT gap 3, IN-04, SC5's second half and MAP-01's external half all
still open by operator decision and untouched.

Finally, record the one-line VF-01 process note: whether this plan's acceptance evidence felt able to
FALSIFY the claims it gated rather than merely detect words in them. That judgement is round 4's own
answer to the finding that started it, and the verifier will read it.
</output>
