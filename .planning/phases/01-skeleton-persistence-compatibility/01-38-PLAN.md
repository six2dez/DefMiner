---
phase: 01-skeleton-persistence-compatibility
plan: 38
type: execute
wave: 38
gap_closure: true
depends_on: [01-37]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/WINDOWS.md
  - .planning/ROADMAP.md
  # CONDITIONAL, TASK 3 ONLY. Written only if this round MOVED a probe row: Task 3's action says "If
  # something DID move, move the row in `01-PROBE.md` in the same commit". If the no-silent-drop
  # equality is unchanged at 38 == 27 + 11 the file is read and its equality confirmed IN WRITING and
  # the file is not edited at all — a `git diff` over it EMPTY is the expected outcome, not a failure.
  # `COVERAGE.md` is READ-ONLY in that same task and is deliberately absent from this list.
  - .planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md
# THIS PLAN CLOSES THE TWO DOCUMENT LEGS OF CRITERION (3) AND THEN RE-RUNS THE CROSS-SURFACE CHECK THAT
# OVERCLAIMED. READ THE THREE THINGS BELOW BEFORE WRITING ANYTHING.
#
# (A) THE VERIFIER'S CHECK (3)(d) REPORTED "zero unmarked STANDING statements on any of the four reader
#     surfaces" WHERE THE COUNT WAS TWO. That is the defect this plan closes and it is also the defect
#     this plan is most likely to repeat. The cure is not a better assertion; it is an ENUMERATION
#     EXECUTED OVER THE FILE'S OWN BYTES, pasted in full, one row per occurrence, classified by a rule
#     with exactly two values. A count arrived at by reading is the same artifact as a reach arrived at
#     by claiming.
#
# (B) I MEASURED MORE STANDING STATEMENTS THAN THE VERIFIER NAMED, AND THE REASON IS THAT THE REVERT
#     CREATED THEM AFTER THE VERIFIER MEASURED. Verification pass 8 was written at 2026-08-26T09:45;
#     commit `4105fd0` landed at 09:52. Everything wave 35 wrote in the present tense about an `[x]`
#     became false seven minutes after the report that cleared it. Measured by me during planning at
#     HEAD `4105fd0`:
#
#       .planning/REQUIREMENTS.md:46   CR-18 as named. Row-scoped counts: `SUPERSEDED` 0, `2026-08-25`
#                                      0, `2026-08-26` 0; newest internal marker `CORRECTION 2026-08-24`
#                                      (4 occurrences). The box itself is already `[ ]` — the direct
#                                      self-contradiction the verifier found is GONE and MUST NOT be
#                                      re-reverted. What remains is the stale superseded-bar sentence
#                                      plus the two standing statements of the box's state.
#       .planning/REQUIREMENTS.md:60   NOT NAMED BY THE VERIFIER. A standing bolded paragraph stating
#                                      the box's state and naming wave 28 as owning the flip. Stale
#                                      attribution: wave 35 owned it and the box is open again.
#       .planning/REQUIREMENTS.md:158  NOT NAMED BY THE VERIFIER — FALSIFIED BY `4105fd0`. States in
#                                      the present tense that the box is now `[x]`, read off wave 35's
#                                      three-row table. It is `[ ]`.
#       .planning/REQUIREMENTS.md:160  NOT NAMED BY THE VERIFIER — FALSIFIED BY `4105fd0`. A standing
#                                      paragraph explaining what "this `[x]`" means, presupposing an
#                                      `[x]` that no longer exists.
#       .planning/STATE.md:365         CR-19 as named. `wave 28 owns the CORE-11 checkbox flip` — stale.
#       .planning/STATE.md:366         CR-19 as named, and FALSE. Six shapes it says report nothing; the
#                                      verifier executed all six and all six report. It names
#                                      `silence-operator-around-global-receiver` as the blocking row,
#                                      which the suite POSITIVELY ASSERTS must not be in the registry.
#       .planning/STATE.md:367         CR-19 as named. Five rows from plan 01-32. I confirmed all five
#                                      still exist among the registry's 26 `kind: "measured-silence"`
#                                      rows — so the ROWS are real and it is the FRAMING that drifted:
#                                      under the re-scoped bar a disclosed named row is a RESIDUAL, not
#                                      a blocker, and the route the line prescribes states the OLD bar.
#       .planning/STATE.md:246         NOT NAMED BY THE VERIFIER, AND **NOT** A SECOND COPY OF `:366`'s
#                                      CLAIM — an earlier draft of this plan said it was and that was
#                                      WRONG, corrected here rather than left to be re-discovered.
#                                      `:246` is decision P9-D3 about SCOPE-BLIND ALIAS TRACKING. It
#                                      carries `report NOTHING` ZERO times and names
#                                      `silence-operator-around-global-receiver` exactly once, inside a
#                                      `POINTER AMENDMENT 2026-08-24` recording that the row was REMOVED
#                                      rather than reworded because re-measurement found no surviving
#                                      silence. It is nearer the CORRECTION than the claim. Dated,
#                                      plan-attributed HISTORY. Classify it over its own bytes and do
#                                      not inherit either characterisation.
#       .planning/STATE.md:290         NOT NAMED BY THE VERIFIER. `### Decisions`, P25-D4: "found six
#                                      spellings still silent — `(ok && globalThis).fetch(url)` and its
#                                      navigator/fetch/eval twins". The SAME false claim `:366` carries.
#                                      Dated and plan-attributed, so the two-valued rule makes it
#                                      HISTORY that stays byte-identical under a scoped marker.
#       .planning/STATE.md:296         NOT NAMED BY THE VERIFIER. `### Decisions`, P28-D1:
#                                      "`(ok && globalThis).fetch(url)` and four twins report NOTHING",
#                                      and "CORE-11 stays `[ ]` with a NAMED blocking row". This is one
#                                      of the TWO whole-file hits for the literal `report NOTHING`; the
#                                      other is `:366`, and NEITHER is on `:246`. Dated,
#                                      plan-attributed HISTORY.
#       .planning/STATE.md:369         NOT NAMED BY THE VERIFIER — FALSIFIED BY `4105fd0`. Wave 35's
#                                      pointer amendment states in the present tense that the box is now
#                                      `[x]`, moved with `CORE11_BOX_EXPECTED` in one commit.
#       .planning/STATE.md — SIX MORE, ALL IN `### Decisions`, ALL FOUND BY THE SAME MEASUREMENT AND
#                                      NONE NAMED BY THE VERIFIER: `:265` (box "stays unchecked", plan
#                                      01-19 owns the flip), `:266` ("CORE-11 marked `[x]`"), `:280`
#                                      (P23-D3, box stays `[ ]`, wave 28 owns the flip), `:291` (P25-D5,
#                                      "CORE-11's box stays `[ ]`"), `:295` (box stays `[ ]`, wave 28
#                                      owns the flip — and carrying NO date and NO plan attribution, so
#                                      the two-valued rule may well make this one STANDING), `:307`
#                                      (wave 33, "CORE-11's box is wave 35's"), `:312` ("The box moved
#                                      with `CORE11_BOX_EXPECTED` in ONE commit (f5652a1)" — falsified
#                                      by `4105fd0`), and `:368` (plan 01-34's pointer amendment,
#                                      "CORE-11's checkbox is untouched and belongs to wave 35").
#                                      Classify each yourself; several are borderline and the borderline
#                                      is where the two-valued rule earns its keep.
#
#     THE MEASURED TOTALS, WITH THE GREP THAT PRODUCED THEM SO THEY ARE FALSIFIABLE BY MEASUREMENT AND
#     NOT BY READING. `.planning/STATE.md`: FIFTEEN LINES — `:246 :265 :266 :280 :290 :291 :295 :296
#     :307 :312 :365 :366 :367 :368 :369` — from
#
#       grep -nE "CORE-11 stays unchecked|CORE-11 marked \[x\]|CORE-11's box stays|box stays \[|CORE-11's \[x\] means|CORE-11's \`\[x\]\` IS ALLOWED TO MEAN|The box moved with|owns the flip|owns the CORE-11 checkbox flip|checkbox is untouched and belongs to|CORE-11's box is wave|report NOTHING|still silent|CORE-11 blocked by|CORE-11's residual class is still open" .planning/STATE.md
#
#     `.planning/REQUIREMENTS.md`: NINE LINES — `:46 :52 :60 :72 :84 :86 :98 :158 :160` — from
#
#       grep -nE 'THE BOX IS DELIBERATELY|WHY THE BOX IS|THE BOX IS STILL|THE BOX IS READ OFF|WHAT THIS \`\[x\]\` MEANS|owns the flip|CORE-11.s box' .planning/REQUIREMENTS.md
#
#     Of the ledger's nine, `:46` carries THREE in-row passages, so eleven PASSAGES across nine LINES.
#     `:52`, `:72`, `:84`, `:86` and `:98` were not in this plan's first draft either: `:72` and `:84`
#     are two more standing "THE BOX IS STILL `[ ]` … Wave 32 owns the flip" paragraphs, `:86` is a
#     dated 2026-08-24 correction, `:98` is a standing "AND THE BOX IS STILL `[ ]`" paragraph, and
#     `:52` says the box "was flipped against it once and reverted TWICE" — which `4105fd0` makes
#     three, so it is stale on its own arithmetic.
#
#     TWENTY-FOUR LINES ACROSS THE TWO FILES, NOT NINE. That is the reconciliation target and it is a
#     FLOOR, not a total: the phrase sets above are two greps and a standing statement spelled outside
#     them is invisible to both, which is precisely the second class of unreached surface this plan is
#     required to disclose.
#
#     THESE LINE NUMBERS ARE HEAD-`4105fd0` VALUES AND PLANS 01-36 AND 01-37 EDIT
#     `.planning/REQUIREMENTS.md`. RE-DERIVE EVERY ONE BY ANCHOR. And do not treat my list as the
#     enumeration — it is a RECONCILIATION TARGET. Your enumeration is executed over the file's bytes
#     and any occurrence I missed is a finding, not an omission you inherit. A TARGET A CHECKER CAN
#     FALSIFY BEFORE EXECUTION IS THE SAME ARTIFACT THIS PLAN EXISTS TO REMOVE, and the first draft of
#     this plan shipped one; that is why the greps above are printed rather than the counts alone.
#
# (C) THE DISPOSITION RULE IS TWO-VALUED AND IT IS WAVE 35's, NOT A NEW ONE. An occurrence carrying a
#     DATE AND A PLAN ATTRIBUTION is HISTORY and stays byte-identical. An occurrence written as a
#     STANDING statement — undated, unattributed, written as a bound or as a present-tense fact rather
#     than as a record — is SUPERSEDED IN PLACE by a scoped marker naming what it supersedes, with the
#     superseded bytes left intact below it.
#
#     THE LEDGER ROW AT `:46` GETS THE THIRD ROUTE, AND ONLY THE ROW. The verifier prescribed it and
#     called it cheaper and in the spirit of wave 33's own disposition: apply the pointer-not-a-bound
#     rule to the row itself — remove the passages that state the box's state and the superseded-bar
#     sentence, and POINT at the narrative block and at `CORE11_BOX_EXPECTED`. "A row that carries no
#     statement of the box's state cannot carry a stale one." HISTORY IS NOT ERASED BY THIS: every
#     removed passage is preserved BYTE-IDENTICAL in a dated 2026-08-26 history block attributed to
#     plan 01-38, in the narrative section below, which is where the row's pointer points. Wave 35's
#     prohibition against rewriting history is satisfied by RELOCATION, never by deletion, and the diff
#     must show the bytes moved rather than lost.
#
# WHAT THIS PLAN MUST NOT DO.
#   - DO NOT FLIP CORE-11's CHECKBOX AND DO NOT RE-REVERT IT. It is `[ ]`, it is correct, and it is
#     already `[ ]` — the verifier's report predates the revert. Whether it may move is verification
#     pass 9's call after all four findings are closed and re-measured.
#   - DO NOT RE-OPEN THE 2026-08-25 RE-SCOPE. The verifier tested it for a quietly-lowered bar and
#     cleared it, on five separate grounds. The `[x]` is unearned because criterion (3) is unmet.
#   - DO NOT RESTORE `silence-operator-around-global-receiver`. The suite asserts its absence with a
#     failure message calling its return a REGRESSION in the descent. Correcting `:366` means deleting
#     a false blocker, never re-adding the row it names.
#   - DO NOT EDIT `packages/backend/src/outbound-prohibition.spec.ts`. The gate file is closed after
#     plan 01-37. `git diff` over it is EMPTY at every task boundary in this plan.
#   - DO NOT RESTATE A BOUND ON ANY POINTER SURFACE. `.planning/STATE.md` and `.planning/WINDOWS.md`
#     are reached by NO mechanical comparison; the pointer-not-a-bound rule is a prohibition with no
#     check there, and that is stated wherever this plan's result is stated.
#
# ENVIRONMENT FACTS, MEASURED DURING PLANNING:
#   - NO ACTIVE GIT HOOKS. Run the gate set by hand at every task boundary.
#   - COMMIT BEFORE MUTATING. Wave 29 destroyed its own Task 1 with `git checkout --`.
#   - `auditSource` is exported from `packages/backend/src/outbound-prohibition.spec.ts` at `:2798`
#     (HEAD-`4105fd0`); that is the entry point the six shapes are re-executed through.
#   - The registry carries 26 `kind: "measured-silence"` rows. The five `:367` names are all present
#     among them. `silence-operator-around-global-receiver` is NOT among them.
#   - `.planning/STATE.md`'s `### Blockers` section begins at `:361` and runs to end of file.
#   - `.planning/WINDOWS.md` is a dated append-only table driven by
#     `gsd-tools windows status|append|waive|fixed`; its front matter reports open 19, waived 0, fixed
#     21, total 40. Every row carries a `recorded_at`, so the two-valued rule classifies them all as
#     HISTORY by construction.
#   - `01-PROBE.md`'s no-silent-drop equality stands at 38 == 27 + 11.
#   - EVERY `<automated>` BLOCK IN THIS PLAN OPENS WITH `set -o pipefail` AND ASSERTS EVERY COUNT
#     NUMERICALLY, AND NEITHER IS DECORATION. `pnpm test 2>&1 | tail -8` exits with TAIL's status, so a
#     suite printing `Tests 2 failed` and `[ELIFECYCLE] Test failed` still exits 0 — measured on this
#     tree. A bare `grep -c` PRINTS a count without ASSERTING it, and `grep -c …; test $? -le 1` is true
#     whether the pattern matched or not, so it can never fail. A round whose whole discipline is
#     "measured, not claimed" may not ship a verify block that claims. Do not strip either, and run the
#     blocks as written rather than retyping them.

autonomous: true
requirements: [CORE-11]

estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 3
  confidence: low
# `estimate-calibration` reports `{"factor": 1, "applied": false, "sample_count": 0, "min_samples": 3}`,
# so `tokens` equals `raw_tokens` and the confidence is DERIVED from the sample count, never self-rated.
# Weigh the MEASUREMENT COUNT over the token figure: two whole-file enumerations, six probes re-executed
# through `auditSource`, one registry re-measurement against 26 rows, one four-surface cross-check, and
# a full gate set.

must_haves:
  truths:
    - "THE ENUMERATION IS EXECUTED OVER EACH FILE'S OWN BYTES AND PASTED IN FULL, ONE ROW PER OCCURRENCE, BEFORE ANY DISPOSITION IS WRITTEN. Check (3)(d) reported zero where the count was two, and the cure for a count arrived at by reading is a count arrived at by grep. Each row carries its file, its line, the phrasing it matched, its classification under the two-valued rule and the EVIDENCE for that classification — a date and a plan attribution, or their absence. The planning-time reconciliation target is TWENTY-FOUR LINES across two files — NINE in `.planning/REQUIREMENTS.md` (`:46 :52 :60 :72 :84 :86 :98 :158 :160`, of which `:46` carries three in-row passages, so eleven passages) and FIFTEEN in `.planning/STATE.md` (`:246 :265 :266 :280 :290 :291 :295 :296 :307 :312 :365 :366 :367 :368 :369`). The verifier named SEVEN of the twenty-four; commit `4105fd0` falsified several more AFTER the report was written; the rest this plan measured. The two greps that produced those counts are printed verbatim in this plan's header so the target is falsifiable BY MEASUREMENT rather than by reading — an earlier draft of this plan named nine, and a target a checker can falsify before execution is the same artifact this plan exists to remove. Any occurrence beyond the target is a FINDING, not an omission inherited from this plan; and the target is a FLOOR, because a standing statement spelled outside either phrase set is invisible to both."
    - "CORE-11's LEDGER ROW CARRIES NO STATEMENT OF THE BOX'S STATE AND NO BOUND, AND POINTS INSTEAD. The verifier's prescribed route, in its own words: a row that carries no statement of the box's state cannot carry a stale one. The two passages stating the box's state and the superseded-bar sentence leave the row; the row points at the narrative block and at `CORE11_BOX_EXPECTED` as the two places allowed to state that fact. The row's FIRST SENTENCE — the prohibition itself — is byte-identical, shown by diff."
    - "NO HISTORY IS ERASED: EVERY REMOVED PASSAGE IS PRESERVED BYTE-IDENTICAL IN A DATED HISTORY BLOCK. Wave 35 prohibited rewriting history to make the new bar look like it was always the bar, and that prohibition binds this wave. It is satisfied by RELOCATION, never by deletion: each passage removed from the row reappears character for character inside a 2026-08-26 history block attributed to plan 01-38 in the narrative section, which is where the row's pointer points. The diff shows bytes MOVED, and a file-wide grep for each removed literal still finds it while a row-scoped grep does not."
    - "THE STANDING STATEMENTS FALSIFIED BY THE REVERT ARE DISPOSED OF, AND THE PLAN SAYS WHY THEY EXIST. Commit `4105fd0` landed seven minutes after verification pass 8 was written, so everything wave 35 wrote in the present tense about an `[x]` became false immediately after the report that cleared it. That is not wave 35's failure and not the verifier's; it is what happens when a checkbox moves without the surfaces that describe it. Each such statement is superseded in place by a scoped marker naming the revert commit as what falsified it, with its bytes intact."
    - "EVERY SCOPED MARKER NAMES THE SENTENCE IT REACHES AND STATES THAT THE REMAINDER STANDS, AND IS A PURE INSERTION. This is wave 35's own trap, recorded in its own prohibitions: the paragraphs carrying superseded claims also carry claims that remain TRUE — the limits list, the STORE-03 / STORE-07 deferral, and in row 3's cell the measured evidence for criteria the verifier confirmed. A marker that falsifies a whole paragraph deletes a true record in the act of correcting a false one. Each marker's scoping is proved by diff: the surrounding bytes are unchanged."
    - "`.planning/STATE.md:366` IS CORRECTED AGAINST SIX PROBES RE-EXECUTED IN THIS SESSION, NOT AGAINST THE VERIFIER'S REPORT. All six shapes are run through `auditSource` and every result is pasted: `(ok && globalThis).fetch(url)`, `(globalThis ?? self).fetch(url)`, `(globalThis || self).fetch(url)`, `(b ? globalThis : self).fetch(url)`, `(ok && window).fetch(url)`, `(ok && navigator).sendBeacon(u,d)`. A correction that cites a report rather than executing its probes is the discharge-by-citation this phase has rejected across six rounds, arriving in the fix for a document that drifted for exactly that reason."
    - "THE ROW `:366` NAMES IS NOT RESTORED, AND THE CORRECTION SAYS SO IN ITS OWN WORDS. `silence-operator-around-global-receiver` is asserted ABSENT by the suite, whose failure message calls its return a REGRESSION in the descent and not a row to restore. The corrected line records that the blocker was false, that the six shapes report, and that the row it named is one the suite requires to stay gone — so no future reader takes the correction for an instruction to re-add it."
    - "`.planning/STATE.md:367`'s FIVE ROWS ARE RE-CHECKED AGAINST THE DERIVED SPAN'S CURRENT MEASURED SILENCES BY MEASUREMENT. The registry's `kind: \"measured-silence\"` rows are counted and listed, and each of the five names is checked for presence. Measured during planning: 26 rows, all five present. What drifted is the FRAMING, not the rows: under the 2026-08-25 bar a disclosed named row is a RESIDUAL, not a blocker, and the route `:367` prescribes — widen five shapes, re-run the discharge, move the box and the constant together — states the OLD bar. The line is re-stated as a pointer at the derived span, restating no bound of its own."
    - "THE WHOLE MEASURED SET IS SWEPT IN ONE PASS, BECAUSE ONE DRIFT MECHANISM PRODUCED ALL OF IT. The verifier asked for `:365`, `:366` and `:367` explicitly and named the mechanism. Planning-time measurement over the file's own bytes found TWELVE MORE lines carrying a statement of CORE-11's checkbox state or of a superseded bar — `:246 :265 :266 :280 :290 :291 :295 :296 :307 :312 :368 :369` — and they belong to the same sweep. Two of them, `:290` (P25-D4) and `:296` (P28-D1), carry the SAME false six-shapes claim `:366` carries, and `:296` is one of the two whole-file hits for the literal `report NOTHING`, the other being `:366`. `:246` is NOT one of them: it is P9-D3 about scope-blind alias tracking and its single mention of the removed row sits inside a 2026-08-24 pointer amendment recording that the row was REMOVED — an earlier draft of this plan said otherwise and the header records the correction. Each of the fifteen is classified by the two-valued rule and disposed of accordingly; the dated, plan-attributed ones are MARKED rather than rewritten. One line carries no date and no attribution at all — `:295` — and if the rule makes it STANDING it is superseded in place like any other standing statement."
    - "CRITERION (3)(d)'s CROSS-SURFACE CHECK IS RE-RUN AS A MEASUREMENT AND ITS RESULT IS WHATEVER IT IS. All four reader surfaces — `.planning/REQUIREMENTS.md`, `packages/backend/src/outbound-prohibition.spec.ts`, `.planning/STATE.md`, `.planning/WINDOWS.md` — are re-enumerated after the dispositions, and the check asserts that no STANDING statement of a superseded bar or of the box's state survives UNMARKED, and that the box's state is asserted in exactly two places which AGREE. If the re-run finds a surviving occurrence, THAT IS THE RESULT: it is written down and the surface is named, not absorbed into a passing verdict. The verdict is stated with the reach of the mechanism that produced it — a grep over four named files under one named phrase set — and never as an unqualified claim about every surface a reader touches."
    - "BOTH CLASSES OF UNREACHED SURFACE ARE NAMED WHEREVER THIS PLAN'S RESULT IS STATED, AS UNGUARDED LIMITS AND NOT AS SATISFIED CHECKS. CLASS ONE, UNGUARDED FILES: `.planning/STATE.md` and `.planning/WINDOWS.md` are reached by NO mechanical comparison — the byte comparisons reach the gate header and the ledger and no further — so the pointer-not-a-bound rule is a prohibition with no check for those two files, and this plan's own enumeration is a one-time grep rather than a standing gate. CLASS TWO, UNDECLARED SPELLINGS: a standing statement phrased outside this plan's phrase set is invisible to the enumeration that produced its verdict."
    - "`.planning/WINDOWS.md` IS AMENDED THROUGH THE TOOL AND NEVER BY HAND. The open entry for the gate file is superseded and a new pointer entry appended through `gsd-tools windows`, with every command and its raw output pasted and the resulting open-entry count stated. Every existing row carries a `recorded_at`, so the two-valued rule classifies them all as HISTORY and none is rewritten."
    - "THE PROBE LEDGER'S NO-SILENT-DROP EQUALITY IS PRESERVED AND EXPLICITLY RE-STATED. `01-PROBE.md` stands at 38 == 27 + 11. This round resolves no flagged assumption and surfaces no new one, so the equality is unchanged — and it is re-stated rather than left silent, because an equality that exists to be checkable is not served by being assumed. If any task in this round did move a row, it is moved in the ledger in the same commit and the new equality is pasted."
    - "THE FLOOR IS RE-MEASURED AT THE END OF THE ROUND, NOT ASSUMED FROM THE START OF IT. `pnpm test` exit 0 with file and test counts, `pnpm typecheck`, `pnpm lint`, `pnpm knip`, `pnpm build:backend && pnpm check:bundle` at exactly one specifier, and the real-tree run at 23 files and 0 violations. This round OPENED with a measured RED that the handoff described as green; it closes with a measured green rather than a claimed one."
    - "NO SENTENCE ADDED ANYWHERE BY THIS PLAN STATES OR IMPLIES THAT CRITERION (3) IS NOW DISCHARGED, THAT THE BOX MAY MOVE, OR THAT THE GATE IS COMPLETE. This plan closes two document legs and re-runs one check. Whether criterion (3) is discharged, and whether the box may move, are verification pass 9's determinations on the whole round's evidence. A plan that awards itself the verdict it exists to produce evidence for is this phase's signature defect in its purest form."
    - "EVERY PREDICTION-VS-MEASUREMENT DISCREPANCY IS RECORDED RATHER THAN ABSORBED, in the shape `01-27-SUMMARY.md` uses. This plan predicts twenty-four lines across two files — nine in the ledger carrying eleven passages, fifteen in `.planning/STATE.md` — six of six probes reporting, 26 measured-silence rows with all five `:367` names present, and `silence-operator-around-global-receiver` absent. If any prediction is wrong, THE DIFFERENCE IS THE FINDING and it is written down before the disposition that depends on it."

  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "CORE-11's row carrying no statement of the box's state and no bound, pointing at the narrative block and at `CORE11_BOX_EXPECTED`, with its first sentence byte-identical; a dated 2026-08-26 history block attributed to plan 01-38 holding every removed passage byte-identical; and scoped supersession markers over the standing statements at the narrative paragraphs that `4105fd0` falsified"
      contains: "CORE11_BOX_EXPECTED"
    - path: ".planning/STATE.md"
      provides: "A `### Blockers` section whose CORE-11 lines are true as measured: the false six-shapes blocker corrected against six probes re-executed in this session and recording that the row it named must stay gone; the stale flip-ownership line corrected; the five-row line re-framed as a pointer at the derived span's measured silences with no bound of its own; and every other measured occurrence in `### Decisions` and in the pointer amendments classified by the two-valued rule, the dated and plan-attributed ones MARKED rather than rewritten and any undated, unattributed one superseded in place"
      contains: "measured-silence"
    - path: ".planning/WINDOWS.md"
      provides: "The open entry for the gate file superseded and one new pointer entry appended through `gsd-tools windows`, restating no bound, with every command and its raw output pasted"
      contains: "outbound-prohibition.spec.ts"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 1's planner-written entries for waves 36, 37 and 38 reconciled against what actually executed — any description the measurement contradicted corrected with its before and after pasted, all three checkboxes marked, and the count paragraph confirmed at 38"
      contains: "01-38-PLAN.md"
  key_links:
    - from: "the pointer-not-a-bound rule wave 33 applied to the gate file's header"
      to: "CORE-11's ledger row"
      via: "the row's statements of the box's state and its superseded-bar sentence relocated byte-identical into a dated history block, leaving a pointer at the narrative block and at `CORE11_BOX_EXPECTED`"
      pattern: "CORE11_BOX_EXPECTED"
    - from: "six shapes a live blocker line says report nothing"
      to: "six executed `auditSource` results"
      via: "each probe run in this session and its result pasted, so the correction rests on execution rather than on the report that found the defect"
      pattern: "auditSource"
    - from: "`.planning/STATE.md:367`'s five named rows"
      to: "the registry's current `kind: \"measured-silence\"` rows"
      via: "the rows counted and listed and each name checked for presence, with the line re-framed from a blocker into a pointer at the derived span"
      pattern: "measured-silence"
    - from: "criterion (3)(d)'s cross-surface check, which reported zero where the count was two"
      to: "a pasted enumeration over four named files"
      via: "a grep-executed count with a two-valued classification, whose verdict is stated with the reach of the grep that produced it"
      pattern: "SUPERSEDED"
  prohibitions:
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "`git diff` over CORE-11's checkbox shown EMPTY at every task boundary; the state-agnostic count observed exactly 1"
      statement: "MUST NOT flip CORE-11's checkbox and MUST NOT re-revert it. It is already `[ ]` and that is correct — the verifier's report predates commit `4105fd0`, which had already reverted it. Whether the box may move is verification pass 9's call on the whole round's evidence, and a plan that moves it is the fourth unearned flip."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "Every removed passage reappears byte-identical in the dated 2026-08-26 history block; a file-wide grep for each removed literal still finds it while a row-scoped grep does not"
      statement: "MUST NOT erase history to make a row read cleanly. Wave 35 prohibited rewriting history so the new bar looks like it was always the bar, and that binds here. The pointer-not-a-bound route is satisfied by RELOCATING the bytes, never by deleting them, and the diff must show them moved."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "Each marker names the sentence it reaches and states the remainder stands; each is shown by diff to be a pure insertion with surrounding bytes unchanged"
      statement: "MUST NOT let a supersession marker reach past the sentence it supersedes. The paragraphs it sits in also carry the limits list, the STORE-03 / STORE-07 deferral and the measured evidence for criteria the verifier confirmed — all of which remain true. A marker that falsifies a whole paragraph deletes a true record in the act of correcting a false one."
    - requirement_id: CORE-11
      category: security
      status: resolved
      verification: gate
      resolution: "The corrected line records that the row is one the suite requires to stay gone, quoting the suite's own regression wording; `git diff` over `packages/backend/src/outbound-prohibition.spec.ts` shown EMPTY"
      statement: "MUST NOT restore `silence-operator-around-global-receiver`. The suite positively asserts its absence and its failure message calls the row's return a REGRESSION in the descent. Correcting a false blocker means deleting the falsehood, never re-adding the row it names."
    - requirement_id: CORE-11
      category: safety
      status: open
      verification: gate
      resolution: "DISCLOSED, NOT CLOSED — both classes of unreached surface named wherever this plan's result is stated, and the cross-surface verdict stated with the reach of the grep that produced it"
      statement: "MUST NOT state a cross-surface verdict whose reach exceeds the mechanism that produced it. The enumeration is a one-time grep over four named files under one named phrase set. It is not a standing gate, `.planning/STATE.md` and `.planning/WINDOWS.md` are reached by no mechanical comparison at all, and a standing statement phrased outside the phrase set is invisible to it. Check (3)(d) overclaimed in exactly this shape and this plan exists to correct it, not to repeat it one register quieter."
    - requirement_id: CORE-11
      category: safety
      status: open
      verification: gate
      resolution: "DISCLOSED — the round's evidence is presented; the verdict is not"
      statement: "MUST NOT state or imply that criterion (3) is now discharged, that the box may move, or that the gate is complete. This plan closes two document legs and re-runs one check. The determination belongs to verification pass 9 on the whole round's evidence, and a plan that awards itself the verdict it exists to produce evidence for is this phase's signature defect in its purest form."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "`.planning/STATE.md` and `.planning/WINDOWS.md` amendments read as pointers at the ledger and the derived span; the byte-comparison cases stay green and no bound is copied onto either file"
      statement: "MUST NOT restate a bound on a pointer surface. The bound of record is the machine-owned generated span, byte-compared to `deriveResidual(RESOLVER_REGISTRY)`. A copy of it on a file no comparison reaches is a claim nobody is checking, which is the artifact this whole mechanism replaces."
---

<objective>
Close criterion (3)'s two document legs — CR-18's self-describing ledger row and CR-19's false live
blocker — by enumerating every standing statement across the reader surfaces rather than by fixing the
lines somebody named, then re-run the cross-surface check that reported zero where the count was two.

Purpose: check (3)(d) claimed to have examined four reader surfaces and found zero unmarked standing
statements of the superseded position. Two of the four carried one, and one of those two was factually
false — `.planning/STATE.md:366` names six shapes it says report nothing, all six of which report, and
names as the blocking row a registry row the suite positively asserts must not exist. That is this
phase's signature defect, a stated reach exceeding an executed reach, arriving inside the discharge of
the criterion written to end it.

And it is now worse than the verifier could have known. Verification pass 8 was written at 09:45;
commit `4105fd0` landed at 09:52 and reverted the box. Everything wave 35 wrote in the present tense
about an `[x]` became false seven minutes after the report that cleared it. I measured three further
standing statements during planning that the verifier could not have seen, plus a fourth the sweep
should have caught. The enumeration in this plan is executed over the files' own bytes precisely so it
does not inherit anyone's list, including mine.

Output: a CORE-11 ledger row that carries no statement of the box's state and no bound and points
instead, with every removed passage preserved byte-identical in a dated history block; a `### Blockers`
section whose CORE-11 lines are true as measured, with six probes re-executed through `auditSource` in
this session; scoped supersession markers over the statements the revert falsified; a `WINDOWS.md`
pointer entry appended through the tool; a re-run four-surface enumeration whose verdict is stated with
the reach of the grep that produced it; the probe ledger's no-silent-drop equality re-stated; and a
floor measured green at the end of a round that opened measured red.

WHAT THIS PLAN DOES NOT DO. It does not flip CORE-11's checkbox and does not re-revert it — the box is
already `[ ]` and correct. It does not re-open the 2026-08-25 re-scope. It does not restore
`silence-operator-around-global-receiver`, whose return the suite treats as a regression. It does not
touch `packages/backend/src/outbound-prohibition.spec.ts`, which is closed after plan 01-37. And it
does not state that criterion (3) is discharged or that the box may move — this plan produces evidence
and verification pass 9 produces the verdict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md
@.planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md
@.planning/phases/01-skeleton-persistence-compatibility/01-37-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CR-18 — every standing statement in the ledger enumerated over its own bytes, the CORE-11 row reduced to a pointer with its removed passages preserved byte-identical, and the statements the revert falsified superseded in place</name>

  <precondition>
    Plans 01-36 and 01-37 have executed and their artefacts are on disk: `01-36-SUMMARY.md` and
    `01-37-SUMMARY.md` both exist. Assert before the first edit that `pnpm test` exits 0 and that BOTH
    byte comparisons are green — the block shipped in the gate header equals
    `deriveResidual(RESOLVER_REGISTRY)` byte for byte and the `.planning/REQUIREMENTS.md` twin does too
    — and HALT if either is red, because an edit to this file written on top of a diverged span records
    a state the code does not carry. Assert `git status --porcelain packages/ .planning/` is clean and
    cross-check any status snapshot against `git rev-parse HEAD`. This repo has NO active git hooks.
  </precondition>

  <read_first>
    - `.planning/REQUIREMENTS.md` — CORE-11's row IN FULL. It is one line of roughly 23,290 bytes and it
      currently begins `- [ ] **CORE-11**`. Read every dated correction it carries, from plan 01-12
      through plan 01-28, for the CORRECTION SHAPE — the dated, plan-attributed prefix. That shape is
      what makes a statement HISTORY rather than a standing bound, and this task's whole disposition
      rule turns on the distinction. Locate the row by pattern, not by the line number `46`, because
      plans 01-36 and 01-37 both edited this file.
    - `.planning/REQUIREMENTS.md` — the standing bolded paragraph that opens `THE BOX IS DELIBERATELY`
      and continues into a statement about which wave owns the flip. At HEAD-`4105fd0` this was `:60`.
      The verifier did not name it; I found it by measurement. Read it and classify it yourself.
    - `.planning/REQUIREMENTS.md` — the scoped supersession marker and the paragraph it reaches, at
      HEAD-`4105fd0` `:110` and `:112`. THIS IS THE MARKER SHAPE THIS TASK COPIES: it names WHICH
      sentence it supersedes, names what supersedes it, states that the rest of the paragraph stands,
      and leaves the superseded bytes intact below it. Read the whole of `:112` too — it carries the
      limits list AND the STORE-03 / STORE-07 deferral, both of which remain TRUE.
    - `.planning/REQUIREMENTS.md` — wave 35's three-row discharge table and the paragraphs after it, at
      HEAD-`4105fd0` `:150-161`. Row 3's evidence cell already carries plan 01-37's insertion. The two
      bolded paragraphs after the table state in the PRESENT TENSE that the box is now `[x]` and
      explain what that `[x]` means. Commit `4105fd0` falsified both, seven minutes after verification
      pass 8 was written. Read them and classify them yourself.
    - `.planning/REQUIREMENTS.md` — the 2026-08-25 operator decision and the 2026-08-26 correction plan
      01-36 added. The row's new pointer points at this narrative block, so read exactly what is there
      to point at.
    - `.planning/REQUIREMENTS.md` — the two sentinels bounding the machine-owned span. Nothing in this
      task may reproduce either marker line in prose: `extractDerivedBlock` bounds the span by
      searching this file's own bytes for them, and wave 28 turned the suite red doing exactly that.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md` `:162-175` and
      `:41-47` — the CR-18 section and the `missing` list entry prescribing the two routes, including
      the sentence that a row carrying no statement of the box's state cannot carry a stale one.
    - `01-35-PLAN.md` `:186-193` — wave 35's prohibitions against rewriting history and against a
      marker reaching past its sentence. Both bind this task and the relocation route is how the first
      is satisfied.
    - `01-36-SUMMARY.md` — what plan 01-36's correction says and where it sits, so this task's pointer
      names it correctly and this task's markers do not duplicate it.
  </read_first>

  <files>.planning/REQUIREMENTS.md</files>

  <reversibility rating="costly">The ledger is append-only in practice: a correction cannot be undone by deletion, only superseded by a further dated correction with its bytes left on the page. That permanence is the property that makes the record trustworthy and it is why this task relocates rather than deletes. The one genuinely costly judgement is the pointer-not-a-bound route applied to the row — a route the verifier prescribed and called cheaper and in the spirit of wave 33's own disposition, and one that a future wave could reverse only by writing the passages back with a new date and a new attribution.</reversibility>

  <action>
    ENUMERATE FIRST, OVER THE FILE'S OWN BYTES, AND PASTE THE ENUMERATION BEFORE WRITING ANY
    DISPOSITION. Grep `.planning/REQUIREMENTS.md` for every statement of CORE-11's checkbox state and
    every statement of a superseded acceptance bar. Search at minimum for the passages beginning
    `THE BOX IS DELIBERATELY`, for `WHY THE BOX IS`, for the future-tense sentence about what the box
    will eventually mean, for present-tense assertions that the box is now checked, and for the
    paragraph explaining what that checked state means. Do not stop at those: enumerate what the file
    contains, not what this plan lists. Paste a table with one row per occurrence carrying file, line,
    matched phrasing, classification and the evidence for the classification.

    RECONCILE AGAINST THE PLANNING-TIME TARGET AND RECORD ANY DISAGREEMENT AS A FINDING. Measured at
    HEAD-`4105fd0` over this file's bytes, by the ledger grep printed in this plan's header: NINE LINES
    — `:46 :52 :60 :72 :84 :86 :98 :158 :160` — carrying ELEVEN PASSAGES, because CORE-11's row at
    `:46` carries three of them. THREE ON THE ROW: the box deliberately still open with the blocking
    row named; the passage asking why the box is open; the sentence stating the superseded bar as the
    future acceptance condition. FOUR STANDING PARAGRAPHS ELSEWHERE: `:52` (the box "was flipped
    against it once and reverted TWICE" — `4105fd0` makes it three, so it is stale on its own
    arithmetic), `:60` (the box's state with wave 28 named as owning the flip), and `:72` and `:84`
    (two more "THE BOX IS STILL `[ ]` … Wave 32 owns the flip" paragraphs). ONE DATED CORRECTION:
    `:86`, 2026-08-24, plan 01-32. ONE MORE STANDING PARAGRAPH: `:98`, "AND THE BOX IS STILL `[ ]`,
    BECAUSE THE TABLE IS NOT THE WHOLE ARITHMETIC". TWO FALSIFIED BY `4105fd0`: `:158` stating in the
    present tense that the box is now checked, and `:160` explaining what that checked state means. AN
    EARLIER DRAFT OF THIS PLAN NAMED FOUR LINES AND SIX PASSAGES; the difference is `:52`, `:72`,
    `:84`, `:86` and `:98`, and it is recorded here rather than left to be re-discovered a tenth time.
    Re-run the grep rather than trusting the list. Row-scoped counts measured at the same time: `SUPERSEDED` 0,
    `2026-08-25` 0, `2026-08-26` 0, and four occurrences of the 2026-08-24 correction marker. Plans
    01-36 and 01-37 have since edited this file, so re-measure everything and treat a disagreement as a
    finding rather than adopting either set silently.

    CLASSIFY EVERY OCCURRENCE WITH WAVE 35's TWO-VALUED RULE AND NO THIRD VALUE. Dated AND
    plan-attributed is HISTORY and stays byte-identical. Undated, unattributed, written as a bound or
    as a present-tense fact rather than as a record, is STANDING and is superseded in place.

    APPLY THE POINTER-NOT-A-BOUND ROUTE TO THE ROW, AND ONLY TO THE ROW. This is the verifier's
    prescribed route and it prescribed it as the cheaper one, in the spirit of wave 33's own
    disposition. Remove from CORE-11's row the passage beginning `THE BOX IS DELIBERATELY`, the passage
    beginning `WHY THE BOX IS`, and the sentence stating the superseded bar as the future acceptance
    condition. In their place put ONE pointer clause naming the two places allowed to state the box's
    state — the narrative block below, and `CORE11_BOX_EXPECTED` in
    `packages/backend/src/outbound-prohibition.spec.ts` — and naming the 2026-08-26 correction plan
    01-36 wrote as where the blocking criterion is recorded. A row that carries no statement of the
    box's state cannot carry a stale one, and that is the whole reasoning.

    PRESERVE THE REMOVED BYTES BY RELOCATION, NEVER BY DELETION, AND PROVE IT BY DIFF. Every passage
    removed from the row reappears CHARACTER FOR CHARACTER inside a new history block in the narrative
    section, dated 2026-08-26 and attributed to plan 01-38 and wave 38, introduced by one sentence
    saying these are the row's own former statements of the box's state, preserved here because a
    ledger does not erase what it once asserted and a row does not carry a bound. Wave 35's prohibition
    against rewriting history binds this task and RELOCATION is how it is satisfied. Prove it: a
    file-wide grep for each removed literal still finds it; a grep restricted to CORE-11's row does not.

    DO NOT TOUCH THE ROW'S FIRST SENTENCE AND DO NOT TOUCH THE CHECKBOX. CORE-11's prohibition — no
    outbound network request in any enumerated spelling in shipped source — is byte-identical after
    this task, shown by diff. The checkbox is `[ ]` and stays `[ ]`; it is not flipped and it is not
    re-reverted, because it has already been reverted and the verifier's report predates that.

    SUPERSEDE THE STATEMENTS THE REVERT FALSIFIED, IN PLACE AND SCOPED. For the two paragraphs after
    wave 35's discharge table that state in the present tense that the box is checked and explain what
    that means, and for the standing bolded paragraph stating the box's state and naming wave 28 as
    owning the flip, insert a marker in the shape the existing one uses: name WHICH sentence it
    reaches, name commit `4105fd0` and verification pass 8's adjudication as what falsified it, and
    state that the remainder of the paragraph stands. Leave the superseded bytes intact below each
    marker. WATCH THE SCOPE: the paragraphs around these carry a limits list, the STORE-03 / STORE-07
    deferral, and the measured evidence for criteria (1) and (2) which the verifier confirmed — a
    marker that reaches past its sentence deletes a true record in the act of correcting a false one.
    Prove the scoping by diff: each marker is a pure insertion with the surrounding bytes unchanged.

    LEAVE STORE-03 AND STORE-07 ALONE AND SHOW IT. Both remain ledger collisions deferred with their
    owner by the STORE-01 → STORE-08 route. `git diff` over both entries EMPTY, pasted. No sentence
    added by this task claims this round reaches either.

    REPRODUCE NEITHER SENTINEL MARKER LINE IN ANY NEW PROSE, and refer to the machine-owned span
    descriptively exactly as the surviving corrections do.

    ADD NO SENTENCE THAT AWARDS A VERDICT. This task closes one leg of criterion (3). Whether criterion
    (3) is discharged, and whether the box may move, are verification pass 9's determinations on the
    whole round's evidence. Nothing written here may state or imply either.

    COMMIT BEFORE YOU MUTATE, THEN RUN ONE MUTATION. Paste `git log --oneline -1` first. Plant drift
    inside the machine-owned span in this file — one byte is enough — run, watch the ledger byte
    comparison go RED, paste the failing title and the FULL assertion message, restore, and cross-check
    with `git diff --exit-code`. That mutation proves the guard can still see the page this task wrote
    on.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && pnpm test 2>&1 | tail -8 && ROWS=$(grep -cE '^- \[[ x]\] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md || true) && echo "CORE-11 ledger rows: $ROWS" && [ "$ROWS" -eq 1 ] && STALE=$(grep '^- \[[ x]\] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md | grep -c 'THE BOX IS DELIBERATELY STILL `\[ \]`, AND THE BLOCKING ROW IS NAMED' || true) && echo "row-scoped box-state passages: $STALE" && [ "$STALE" -eq 0 ] && git diff --exit-code -- packages/backend/src/outbound-prohibition.spec.ts && echo GATE-FILE-UNTOUCHED</automated>
  </verify>

  <acceptance_criteria>
    - The precondition asserted and pasted BEFORE the first edit: `01-36-SUMMARY.md` and
      `01-37-SUMMARY.md` present, `pnpm test` exit 0, both byte comparisons green,
      `git status --porcelain packages/ .planning/` clean, `git rev-parse HEAD` pasted beside any
      status snapshot used.
    - The enumeration pasted as a table BEFORE any disposition was written, one row per occurrence,
      each carrying file, line, matched phrasing, classification and the evidence for it. Reconciled
      against the planning-time target of NINE LINES (`:46 :52 :60 :72 :84 :86 :98 :158 :160`)
      carrying ELEVEN PASSAGES, with the plan's own ledger grep re-run and its output pasted; any
      disagreement recorded as a finding rather than absorbed. The target is a FLOOR: a statement
      spelled outside that grep's phrase set is invisible to it, and that limit is stated beside the
      count rather than left implied.
    - Row-scoped counts re-measured and pasted for the supersession marker word, for `2026-08-25`, for
      `2026-08-26` and for the 2026-08-24 correction marker, reconciled against the planning-time
      values 0, 0, 0 and 4.
    - Executed, over CORE-11's row extracted by pattern rather than by line number,
      `grep -c 'THE BOX IS DELIBERATELY STILL `[ ]`, AND THE BLOCKING ROW IS NAMED'` — observed 0.
    - Executed, over the same row extracted by pattern,
      `grep -c 'WHY THE BOX IS `[ ]` HERE'` — observed 0.
    - Executed, over the same row extracted by pattern, a count of the future-tense sentence stating
      what the box will eventually mean — observed 0.
    - Executed, over the WHOLE FILE, a count of each of those three literals — each observed GREATER
      THAN OR EQUAL TO 1, proving the bytes were RELOCATED and not deleted. The dated 2026-08-26
      history block holding them is quoted in the SUMMARY with its introductory sentence.
    - The row's new pointer clause quoted verbatim in the SUMMARY, naming the narrative block, naming
      `CORE11_BOX_EXPECTED` and naming plan 01-36's 2026-08-26 correction as where the blocking
      criterion is recorded.
    - `git diff` over CORE-11's first sentence shown EMPTY, output pasted. `git diff` over the checkbox
      shown EMPTY; executed `grep -cE '^- \[[ x]\] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md`,
      observed exactly 1.
    - Each scoped supersession marker shown by `git diff` to be a pure INSERTION with the bytes above
      and below unchanged, and the SUMMARY quotes each marker's own clause naming which sentence it
      reaches, naming commit `4105fd0` as what falsified it, and stating that the remainder stands.
    - The limits list, the STORE-03 / STORE-07 deferral and the measured evidence for criteria (1) and
      (2) shown byte-unchanged by diff — no marker reached past its sentence.
    - `git diff` over the STORE-03 and STORE-07 entries shown EMPTY, output pasted.
    - `git diff --exit-code -- packages/backend/src/outbound-prohibition.spec.ts` shown clean — the
      gate file is closed after plan 01-37 and this task does not touch it.
    - Neither generated-residual sentinel marker line reproduced in any new prose, confirmed by running
      the suite and observing the extraction cases green.
    - No sentence added by this task states or implies that criterion (3) is discharged, that the box
      may move, or that the gate is complete. Every added sentence is re-read against that prohibition
      and the review recorded as a one-line verdict; any rewrite's before and after are both pasted.
    - MUTATION executed after the work was committed: `git log --oneline -1` pasted first, drift
      planted inside the machine-owned span, run, observed RED, the failing title and the FULL
      assertion message pasted, restored, `git diff --exit-code` shown clean.
    - Executed `pnpm test` — exit 0, file and test counts pasted, no fewer than 31 files and 1372
      tests.
    - Executed `pnpm typecheck`, `pnpm lint`, `pnpm knip` — all exit 0, output pasted.
    - Any prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses, or an
      explicit statement that there were none.
  </acceptance_criteria>

  <done>
    CORE-11's ledger row states the prohibition and points; it carries no statement of the box's state
    and no bound, so it cannot carry a stale one. Every passage removed from it survives byte-identical
    in a dated history block, proved by a file-wide grep that still finds what a row-scoped grep no
    longer does. The statements commit `4105fd0` falsified carry scoped markers naming it, with their
    bytes intact and the true records beside them untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: CR-19 — the live blockers corrected against six probes re-executed in this session and the registry's current measured silences, with every other measured occurrence classified and the dated ones marked rather than rewritten</name>

  <read_first>
    - `.planning/STATE.md`'s `### Blockers` section IN FULL, from its heading to end of file. At
      HEAD-`4105fd0` the heading was at `:361` and the file ended at `:369`. Read every line, not only
      the CORE-11 ones — the disposition rule is applied to the section, and a line nobody read is a
      line nobody classified.
    - `.planning/STATE.md`'s `### Decisions` section IN FULL, and in particular the ELEVEN lines
      planning-time measurement found there — at HEAD-`4105fd0` `:246 :265 :266 :280 :290 :291 :295
      :296 :307 :312`, plus `:368`'s pointer amendment. TWO of them carry the same false six-shapes
      claim `:366` carries: `:290` (P25-D4, "found six spellings still silent — `(ok &&
      globalThis).fetch(url)` and its navigator/fetch/eval twins") and `:296` (P28-D1, "`(ok &&
      globalThis).fetch(url)` and four twins report NOTHING"). `:246` is NOT one of them — it is P9-D3
      about scope-blind alias tracking, it carries `report NOTHING` zero times, and its single mention
      of `silence-operator-around-global-receiver` sits inside a 2026-08-24 pointer amendment recording
      that the row was REMOVED because re-measurement found no surviving silence. An earlier draft of
      this plan called `:246` a second copy of `:366`'s claim; it is not, and the correction is in this
      plan's header. `:295` carries NO date and NO plan attribution. Classify every one of them
      yourself under the two-valued rule rather than assuming which value it takes.
    - `.planning/STATE.md`'s last line at HEAD-`4105fd0`, the 2026-08-26 pointer amendment from plan
      01-35 and wave 35. It states in the present tense that CORE-11's box is now checked and that it
      moved with `CORE11_BOX_EXPECTED` in one commit. Commit `4105fd0` falsified the first half. Read
      the whole amendment before marking it: most of it — the re-scope, the three criteria, the scoped
      criterion-3 verdict, the narrowness sentence, the residuals — remains TRUE and must survive.
    - `packages/backend/src/outbound-prohibition.spec.ts` — `auditSource`'s signature and its export,
      at `:2798` at HEAD-`4105fd0`. This is the entry point the six shapes are re-executed through.
      READ IT ONLY; this task must not edit this file.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the case asserting that
      `silence-operator-around-global-receiver` is NOT in the registry, and its full failure message
      calling the row's return a REGRESSION in the descent and not a row to restore. At HEAD-`4105fd0`
      this was near `:9527`. The corrected blocker line quotes that reasoning so no future reader takes
      the correction for an instruction to re-add the row.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the `RESOLVER_REGISTRY` rows carrying
      `kind: "measured-silence"`. Measured during planning: 26 of them, and all five names from the
      five-row blocker line are present among them. Read enough to count them yourself and to check
      each of the five names.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md` `:176-191` — the
      CR-19 section with its six-shape table showing what the line claims against what the verifier
      measured, and `:44` — the `missing` entry asking for `:365` and `:367` to be swept in the same
      pass because the same drift mechanism produced all three.
    - `01-35-PLAN.md` `:120-125` — the STATE.md pointer-amendment artifact description, for the shape a
      pointer amendment takes and for the rule that it restates no bound of its own.
    - `01-36-SUMMARY.md` and `01-37-SUMMARY.md` — what those two waves measured and corrected, because
      this task's new pointer amendment records the round and must not restate anything they own.
  </read_first>

  <files>.planning/STATE.md</files>

  <reversibility rating="costly">`.planning/STATE.md` is an append-only record of position, decisions and blockers, and a correction to it is permanent in the same way the ledger's is. What makes this costly rather than merely irreversible is that a live blocker line is what a future planner reads first: the line corrected here has already misdirected at least one round's framing, and a correction that is itself wrong would misdirect the next. That is why every claim in the replacement is executed in this session rather than carried from a report.</reversibility>

  <action>
    RE-EXECUTE THE SIX PROBES BEFORE WRITING A WORD OF THE CORRECTION, AND PASTE EVERY RESULT. Run each
    of these through `auditSource` and paste the returned rule identifiers: `(ok && globalThis).fetch(url)`,
    `(globalThis ?? self).fetch(url)`, `(globalThis || self).fetch(url)`,
    `(b ? globalThis : self).fetch(url)`, `(ok && window).fetch(url)`, and
    `(ok && navigator).sendBeacon(u, d)`. The verifier measured all six reporting. A correction that
    cites that report rather than executing its probes is the discharge-by-citation this phase has
    rejected across six rounds, arriving in the fix for a document that drifted for exactly that
    reason. If any of the six does NOT report, that is a finding of the first order and it is written
    down before the line is touched, because the correction's content would then be different.

    RE-MEASURE THE REGISTRY BEFORE RE-FRAMING THE FIVE-ROW LINE. Count the rows carrying
    `kind: "measured-silence"` and list their ids. Check each of the five names the blocker line
    carries — the array element position, the object-literal property, the class field, the parameter
    default and the for-of binding — for presence. Measured during planning: 26 rows, all five present.
    Check also that `silence-operator-around-global-receiver` is ABSENT. Reconcile and record any
    disagreement as a finding.

    ENUMERATE EVERY CORE-11 STATEMENT IN THIS FILE AND PASTE THE TABLE BEFORE WRITING ANY DISPOSITION.
    Not only the three lines the verifier named: enumerate what the file contains. One row per
    occurrence with its section, its line, the claim it makes, its classification under the two-valued
    rule and the evidence for that classification. The planning-time reconciliation target is FIFTEEN
    LINES, measured over this file's bytes by the STATE grep printed in this plan's header: `:246 :265
    :266 :280 :290 :291 :295 :296 :307 :312 :365 :366 :367 :368 :369`. THREE are the live
    `### Blockers` lines the verifier named (`:365 :366 :367`); ELEVEN are in `### Decisions`; ONE is
    the last pointer amendment (`:369`). AN EARLIER DRAFT OF THIS PLAN NAMED FIVE AND CHARACTERISED
    `:246` AS A SECOND COPY OF `:366`'s CLAIM; both were wrong and both are corrected in the header —
    `:246` carries the literal `report NOTHING` ZERO times and records the row as REMOVED, while the
    two whole-file hits for that literal are `:296` and `:366`. Re-run the grep rather than trusting
    the list. Any occurrence beyond fifteen is a finding, and the fifteen are a FLOOR because a
    statement spelled outside that phrase set is invisible to the grep that produced them.

    CORRECT THE FALSE BLOCKER, AND CORRECT IT INTO A TRUE SENTENCE RATHER THAN A DELETED ONE. The line
    beginning `[Phase 01] CORE-11 blocked by` in `### Blockers` is false in two ways at once: the six
    shapes it names all report, and the row it names as the blocker is one the suite POSITIVELY
    ASSERTS must not exist. Replace it with what is true as measured in this session: that those six
    shapes report and the identifiers they return; that the row it named was removed on 2026-08-24 when
    the silence it measured stopped existing; and that the suite asserts its absence with a failure
    message calling its return a REGRESSION in the descent and not a row to restore. Quote that
    reasoning so no future reader takes the correction for an instruction to re-add the row.

    CORRECT THE STALE FLIP-OWNERSHIP LINE. The line stating that a named earlier wave owns the CORE-11
    checkbox flip is stale twice over: that wave did not own it, and the box has since been flipped and
    reverted. Replace it with the true position as measured: the box is `[ ]`, the 2026-08-25 operator
    decision states the three criteria, verification pass 8 found criterion (3) unmet on the legs this
    round addresses, and whether the box may move is verification pass 9's call. State no bound of your
    own and award no verdict.

    RE-FRAME THE FIVE-ROW LINE RATHER THAN DELETING IT, BECAUSE THE ROWS ARE REAL AND THE FRAMING IS
    NOT. All five are present among the registry's measured silences. What drifted is that under the
    2026-08-25 bar a disclosed named row is a RESIDUAL, not a blocker, and the route the line
    prescribes — widen five shapes, re-run the discharge, move the box and the constant together —
    states the OLD bar. Re-state it as a POINTER at the derived span's measured silences, giving the
    count you measured, naming the five, and restating no bound of its own. The bound of record is the
    machine-owned generated span byte-compared to `deriveResidual(RESOLVER_REGISTRY)`.

    MARK THE DATED COPIES RATHER THAN REWRITING THEM, AND THERE ARE MORE THAN TWO. Everything the
    enumeration classifies as HISTORY — dated AND plan-attributed — stays byte-identical under a scoped
    marker. That set includes, at minimum, `:290` (P25-D4, "found six spellings still silent") and
    `:296` (P28-D1, "and four twins report NOTHING"), which carry the SAME false claim `:366` carries
    and which this session's six probes falsify; `:312` and `:368`, whose statements about the box's
    state commit `4105fd0` falsified; and the last pointer amendment stating the box is now checked.
    Each gets a scoped marker naming WHICH clause it reaches, naming this session's execution or commit
    `4105fd0` as what falsified it, and stating that the remainder stands — and for the pointer
    amendment the remainder is most of it, including the re-scope, the three criteria, the scoped
    criterion-3 verdict, the narrowness sentence and the named residuals, all of which remain TRUE. Any
    occurrence the enumeration classifies as STANDING instead — `:295` carries no date and no plan
    attribution and is the likeliest — is superseded in place like any other standing statement, and
    `:52`'s ledger twin is the same shape one file over. `:246` is NOT in the false-claim set: it
    records the row as REMOVED. Each marker is a pure insertion, proved by diff.

    APPEND ONE NEW POINTER AMENDMENT FOR THIS ROUND, RESTATING NO BOUND. Dated 2026-08-26, attributed
    to plans 01-36 through 01-38 and waves 36 through 38. It records: that the tree was found RED at
    HEAD `4105fd0` and restored, with the two causes named; that CR-17's exemption anchoring landed
    with a failing-path fixture; that WR-48's exclusion was narrowed and what that measurement raised;
    that CR-18's row now points rather than states; that this line's own section was corrected against
    six probes re-executed in this session; and that CORE-11's box is `[ ]` and belongs to verification
    pass 9. It states the two classes of unreached surface and it states that IT IS A POINTER: the
    bound of record is the generated span, and this file is reached by no mechanical comparison.

    ADD NO SENTENCE THAT AWARDS A VERDICT. Whether criterion (3) is discharged and whether the box may
    move are verification pass 9's determinations on the whole round's evidence.

    DO NOT EDIT `packages/backend/src/outbound-prohibition.spec.ts`. `git diff` over it EMPTY at the
    end of this task, pasted. And do not restore `silence-operator-around-global-receiver` in any form.

    COMMIT BEFORE YOU MUTATE. Paste `git log --oneline -1`, then re-run the full suite and confirm the
    corrections to this file changed nothing in it — this file is reached by no byte comparison, which
    is precisely the limit this task must state rather than hide.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && pnpm test 2>&1 | tail -8 && NOTHING=$(awk '/^### Blockers/,0' .planning/STATE.md | grep -c 'all report NOTHING' || true) && echo "blockers-scoped six-shapes claim: $NOTHING" && [ "$NOTHING" -eq 0 ] && FLIP=$(awk '/^### Blockers/,0' .planning/STATE.md | grep -c 'wave 28 owns the CORE-11 checkbox flip' || true) && echo "blockers-scoped flip-ownership claim: $FLIP" && [ "$FLIP" -eq 0 ] && git diff --exit-code -- packages/backend/src/outbound-prohibition.spec.ts && echo GATE-FILE-UNTOUCHED</automated>
  </verify>

  <acceptance_criteria>
    - All six probes re-executed through `auditSource` IN THIS SESSION with each returned rule
      identifier pasted, in a table beside what the blocker line claimed. Reconciled against the
      verifier's measurement that all six report; any shape that did NOT report is written up as a
      first-order finding before the line was touched.
    - The registry re-measured: the count of `kind: "measured-silence"` rows pasted with their ids
      listed, each of the five names from the five-row blocker line checked for presence, and
      `silence-operator-around-global-receiver` checked for ABSENCE. Reconciled against the
      planning-time values 26, five present, one absent; any disagreement recorded as a finding.
    - The enumeration pasted as a table BEFORE any disposition, one row per occurrence with its
      section, line, claim, classification and evidence. Reconciled against the planning-time target
      of FIFTEEN LINES — three in `### Blockers`, eleven in `### Decisions`, one in the last pointer
      amendment — with the plan's own STATE grep re-run and its output pasted, and any disagreement
      recorded as a finding. The target is a FLOOR and that is stated beside the count.
    - Executed, over the `### Blockers` section extracted by heading rather than by line number,
      `grep -c 'all report NOTHING'` — observed 0.
    - Executed, over the same section, `grep -c 'wave 28 owns the CORE-11 checkbox flip'` — observed 0.
    - Executed, over the WHOLE FILE, a count of each of those two literals — the results pasted and
      each accounted for: any remaining occurrence sits in a DATED section and carries a scoped marker,
      and that is shown by diff.
    - The corrected blocker line quoted verbatim in the SUMMARY, containing the six executed results,
      the removal date of the row it formerly named, and the suite's own regression wording about that
      row not being one to restore.
    - The corrected flip-ownership line quoted verbatim, stating the box is `[ ]`, naming the
      2026-08-25 decision's three criteria by reference, and awarding no verdict.
    - The re-framed five-row line quoted verbatim, giving the measured count of measured-silence rows,
      naming the five, describing them as disclosed RESIDUALS under the 2026-08-25 bar rather than as
      blockers, and restating no bound.
    - Each scoped marker over a dated copy shown by `git diff` to be a pure INSERTION, and the SUMMARY
      quotes each marker's clause naming which claim it reaches and stating the remainder stands. For
      the last pointer amendment, the SUMMARY names explicitly which parts remain TRUE and are
      untouched — the re-scope, the three criteria, the scoped criterion-3 verdict, the narrowness
      sentence and the named residuals.
    - The new 2026-08-26 pointer amendment present, quoted verbatim, recording the round's four
      findings and their disposition, naming both classes of unreached surface, stating that CORE-11's
      box is `[ ]` and belongs to verification pass 9, and stating in its own words that it is a
      POINTER and restates no bound.
    - `silence-operator-around-global-receiver` NOT restored anywhere:
      `git diff --exit-code -- packages/backend/src/outbound-prohibition.spec.ts` shown clean, output
      pasted.
    - No sentence added by this task states or implies that criterion (3) is discharged, that the box
      may move, or that the gate is complete. Every added sentence re-read against that prohibition
      with the review recorded as a one-line verdict.
    - `git diff` over `.planning/REQUIREMENTS.md` shown EMPTY — Task 1 owns that file and this task
      does not return to it.
    - Executed `pnpm test` — exit 0, file and test counts pasted, no fewer than 31 files and 1372
      tests.
    - Executed `pnpm typecheck`, `pnpm lint`, `pnpm knip` — all exit 0, output pasted.
    - Any prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses, or an
      explicit statement that there were none.
  </acceptance_criteria>

  <done>
    The live blockers say what is true as measured in this session: six shapes that report, a row the
    suite requires to stay gone, five disclosed residuals framed as residuals, and a box whose state
    belongs to the ledger and to the constant rather than to this file. Every other measured occurrence
    — the eleven in `### Decisions` and the pointer amendments — is classified by the two-valued rule,
    the dated and plan-attributed ones carrying scoped markers with their bytes intact and their true
    parts standing, and one new pointer amendment records the round without restating a bound.
  </done>
</task>

<task type="auto">
  <name>Task 3: The closing pass — criterion (3)(d)'s cross-surface check re-run as a measurement over all four reader surfaces, the WINDOWS pointer appended through the tool, the probe ledger's equality re-stated, the roadmap updated, and the floor measured green</name>

  <read_first>
    - Task 1's and Task 2's enumerations and dispositions, in full. This task RE-RUNS the check across
      all four surfaces after those dispositions, so it needs to know exactly what was disposed of and
      how, in order to distinguish a marked occurrence from an unmarked one.
    - `.planning/WINDOWS.md`'s front matter and its ledger header, which carries the instructions for
      the `gsd-tools windows` subcommands — `status`, `append`, `waive`, `fixed`. Read the open entry
      or entries for `packages/backend/src/outbound-prohibition.spec.ts` and read the shape of the most
      recent appended rows. At planning time the front matter reported open 19, waived 0, fixed 21,
      total 40, and 37 rows mention the gate file.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md` sections B and C, and its
      tally line. The no-silent-drop equality stands at 38 == 27 + 11 with 27 explicit, 11
      unresolved-and-flagged and 0 dismissed. Read the resolution rules so you can tell whether
      anything this round did moves a row.
    - `.planning/phases/01-skeleton-persistence-compatibility/COVERAGE.md`'s header and its policy
      paragraph. This round integrates no external API surface, so the task is to CONFIRM the existing
      declaration still stands, not to add matrix rows.
    - `.planning/ROADMAP.md`'s Phase 1 entry — the `**Plans**` paragraph and the wave-by-wave plan
      list, whose waves 36, 37 and 38 entries the PLANNER already wrote with the count already at 38.
      Read all three against what actually executed; this task reconciles and marks them, it does not
      create them.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md` `:111-116` — the
      statement of what check (3)(d) claimed and what the bar's own construction requires. The re-run
      in this task is scoped to reproduce that check honestly, with its reach stated.
    - `01-35-PLAN.md` `:536-553` — how wave 35 stated the two classes of unreached surface in one
      sentence, as unguarded limits rather than as satisfied checks. This task uses the same
      formulation so the two rounds' verdicts are comparable.
  </read_first>

  <files>.planning/WINDOWS.md, .planning/ROADMAP.md, and — CONDITIONALLY, only if this round moved a probe row — .planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md. If the no-silent-drop equality is unchanged at 38 == 27 + 11, `01-PROBE.md` is READ and its equality confirmed in writing, and an EMPTY diff over it is the expected outcome rather than a failure. `COVERAGE.md` is read-only in this task and is deliberately not listed.</files>

  <reversibility rating="reversible">A `WINDOWS.md` entry appended through the tool, a roadmap plan list extended, and a set of measurements pasted into a summary. Nothing here changes a requirement, a gate or a checkbox, and every artifact is regenerable from the round's other outputs. The one care is that `WINDOWS.md` is tool-managed: hand-editing its table would desynchronise its front-matter counts, which is why every mutation goes through the subcommands.</reversibility>

  <action>
    RE-RUN CRITERION (3)(d)'s CROSS-SURFACE CHECK OVER ALL FOUR READER SURFACES AND PASTE THE RESULT
    WHATEVER IT IS. The four are `.planning/REQUIREMENTS.md`,
    `packages/backend/src/outbound-prohibition.spec.ts`, `.planning/STATE.md` and
    `.planning/WINDOWS.md`. Enumerate every statement of CORE-11's checkbox state and every statement
    of a superseded acceptance bar on each, and assert two things: that no STANDING occurrence survives
    UNMARKED, and that the box's state is asserted in exactly two places — the ledger row and
    `CORE11_BOX_EXPECTED` — which AGREE. THIS IS A MEASUREMENT. If it finds a surviving unmarked
    occurrence, that is the result: name the surface, name the occurrence and write it down. Do not
    absorb it into a passing verdict and do not fix it silently — check (3)(d) reported zero where the
    count was two, and a re-run that quietly repairs what it finds cannot be distinguished from one
    that quietly overlooks it.

    STATE THE VERDICT WITH THE REACH OF THE MECHANISM THAT PRODUCED IT, IN THE FORMULATION WAVE 35
    USED. This enumeration is a ONE-TIME GREP over four named files under one named phrase set. It is
    not a standing gate. Name both classes of unreached surface in one sentence, as unguarded limits
    rather than as satisfied checks. CLASS ONE, UNGUARDED FILES: `.planning/STATE.md` and
    `.planning/WINDOWS.md` are reached by NO mechanical comparison — the byte comparisons reach the
    gate header and the ledger and no further — so the pointer-not-a-bound rule remains a prohibition
    with no check for those two files. CLASS TWO, UNDECLARED PHRASINGS: a standing statement spelled
    outside this enumeration's phrase set is invisible to it, exactly as an undeclared spelling is
    invisible to the whole-file guard. A verdict that omits either class is a stated reach exceeding an
    executed reach, which is the defect this whole plan exists to correct.

    AWARD NO VERDICT ON CRITERION (3) ITSELF. Whether criterion (3) is discharged, and whether the box
    may move, are verification pass 9's determinations on the whole round's evidence. This task
    produces the measurement; it does not read a conclusion off it.

    AMEND `.planning/WINDOWS.md` THROUGH THE TOOL AND NEVER BY HAND. Run `gsd-tools windows status`
    first and paste it. Supersede the open entry for the gate file through the appropriate subcommand
    and append ONE new pointer entry recording this round — the four findings, their disposition, and
    the fact that CORE-11's box is `[ ]` and belongs to verification pass 9 — restating no bound of its
    own. Paste every command with its RAW output and state the resulting open-entry count. Hand-editing
    the table would desynchronise the front-matter counts, which is why the header says to use the
    subcommands.

    RE-STATE THE PROBE LEDGER'S NO-SILENT-DROP EQUALITY RATHER THAN LEAVING IT ASSUMED. `01-PROBE.md`
    stands at 38 == 27 + 11 with 0 dismissed. Check whether anything in plans 01-36, 01-37 or 01-38
    resolved a flagged assumption or surfaced a new one; this round fixes a guard's anchoring, an
    exclusion's description and two planning documents, and none of that is expected to move a row. If
    nothing moved, state the equality unchanged and say so explicitly — an equality that exists to be
    checkable is not served by being assumed. If something DID move, move the row in `01-PROBE.md` in
    the same commit, paste the new equality, and never drop a row silently.

    CONFIRM `COVERAGE.md` RATHER THAN EXTENDING IT. This round integrates no external API surface. Read
    the existing declaration, confirm in one sentence that it still stands, and add no matrix rows.
    Fabricating a row for a round that touched a spec file and four planning documents would be an
    invented measurement.

    RUN THE ASSUMPTION-DELTA DETECTOR OVER THIS ROUND'S SCOPE. A gap-closure round fixing a guard's
    anchoring is unlikely to fire a singular-to-plural, required-to-optional or derived-to-chosen
    transition. If it does not fire, say so in one line and raise no checkpoint. If it does, record
    what fired and why.

    RECONCILE `.planning/ROADMAP.md` AGAINST WHAT ACTUALLY EXECUTED. The planner already appended the
    wave 36, 37 and 38 entries and already updated the `**Plans**` count paragraph to 38, so this task
    does NOT create them. It CHECKS them: read each of the three entries against its plan's SUMMARY and
    correct any description the executed outcome contradicted — wave 37's entry in particular describes
    an obligation count that was unknown when it was written, and if the measurement disagreed with the
    entry's framing the entry is corrected to match the measurement. Then mark each of the three
    checkboxes `[x]`. Use a SCOPED edit that replaces only the region being changed; a whole-file
    rewrite of the roadmap destroys every phase entry outside the diff window.

    MEASURE THE FLOOR AT THE END AND PASTE EVERY RESULT. `pnpm test` with its file and test counts,
    `pnpm typecheck`, `pnpm lint`, `pnpm knip`, `pnpm build:backend && pnpm check:bundle`, and the
    real-tree run over both `SOURCE_ROOTS`. THIS ROUND OPENED WITH A MEASURED RED THAT THE HANDOFF
    DESCRIBED AS GREEN, and the SUMMARY says so in one sentence: it closes with a measured green rather
    than a claimed one.

    STATE THE MUST-NOT PLAINLY WITH ITS EVIDENCE. CORE-11's first sentence byte-identical, 23 files
    over both `SOURCE_ROOTS` at zero violations, the shipped bundle's entire import set one specifier.
    Every finding this round touched is a defect in a test-only gate's description of itself or in two
    planning documents. Nothing leaks, and this round re-established that by execution rather than
    carrying it forward.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && pnpm test 2>&1 | tail -8 && pnpm typecheck && pnpm lint && pnpm knip && pnpm build:backend && pnpm check:bundle 2>&1 | tail -5 && PLANREF=$(grep -c '01-38-PLAN.md' .planning/ROADMAP.md || true) && echo "ROADMAP references to this plan: $PLANREF" && [ "$PLANREF" -ge 1 ] && ROWS=$(grep -cE '^- \[[ x]\] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md || true) && echo "CORE-11 ledger rows: $ROWS" && [ "$ROWS" -eq 1 ]</automated>
  </verify>

  <acceptance_criteria>
    - The four-surface enumeration pasted in full, one row per occurrence across
      `.planning/REQUIREMENTS.md`, `packages/backend/src/outbound-prohibition.spec.ts`,
      `.planning/STATE.md` and `.planning/WINDOWS.md`, each row carrying surface, line, claim,
      classification and whether it is MARKED.
    - The two assertions stated with their measured results: the number of STANDING occurrences
      surviving UNMARKED, and the number of places asserting the box's state with whether they agree.
      If either number is non-zero in the wrong direction, the surface and occurrence are NAMED and
      written up rather than silently repaired, and the SUMMARY says explicitly that the re-run found
      it rather than that the round prevented it.
    - The verdict stated with the reach of the mechanism that produced it, and BOTH classes of
      unreached surface named in one sentence as unguarded limits: unguarded FILES
      (`.planning/STATE.md`, `.planning/WINDOWS.md`, reached by NO mechanical comparison) and
      undeclared PHRASINGS outside this enumeration's phrase set. That sentence is quoted verbatim in
      the SUMMARY.
    - No sentence in this task states or implies that criterion (3) is discharged, that the box may
      move, or that the gate is complete. The review is recorded as a one-line-per-surface verdict.
    - `gsd-tools windows status` run and pasted BEFORE any change; every subsequent `gsd-tools windows`
      command pasted with its RAW output; the open entry for the gate file superseded and exactly one
      new pointer entry appended; the resulting open-entry count stated. `git diff` shows
      `.planning/WINDOWS.md` changed only through tool-produced rows and front-matter counts.
    - The new WINDOWS entry quoted verbatim, recording the round's four findings and their disposition,
      stating CORE-11's box is `[ ]` and belongs to verification pass 9, and stating in its own words
      that it restates no bound.
    - `01-PROBE.md`'s equality re-stated explicitly: 38 == 27 + 11 with 0 dismissed, either confirmed
      unchanged with that confirmation written out, or updated in the same commit with the new equality
      pasted and the moved row named. No row is dropped silently.
    - `COVERAGE.md`'s existing declaration confirmed in one sentence as still standing, with NO matrix
      rows added and that stated explicitly.
    - The assumption-delta detector run over this round's scope with its result recorded in one line;
      if it did not fire, no checkpoint is raised and that is stated.
    - `.planning/ROADMAP.md`'s three planner-written entries for waves 36, 37 and 38 read against
      their SUMMARYs, any description the executed outcome contradicted corrected with the before and
      after pasted, and all three checkboxes marked `[x]`. Executed `grep -c '01-38-PLAN.md'
      .planning/ROADMAP.md`, observed at least 1, and the `**Plans**` count paragraph confirmed at 38.
      The edit is shown by diff to be SCOPED — every phase entry outside the changed region is
      byte-unchanged.
    - Executed `pnpm test` — exit 0, file and test counts pasted, no fewer than 31 files and 1372
      tests.
    - Executed `pnpm typecheck`, `pnpm lint`, `pnpm knip` — all exit 0, output pasted.
    - Executed `pnpm build:backend && pnpm check:bundle` — exactly one import specifier, `crypto`,
      output pasted.
    - Executed the real-tree run over both `SOURCE_ROOTS` — 23 files, 0 violations, output pasted.
    - `git diff` over CORE-11's first sentence shown EMPTY; executed
      `grep -cE '^- \[[ x]\] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md`, observed exactly 1.
    - `git diff --exit-code -- packages/backend/src/outbound-prohibition.spec.ts` shown clean.
    - The SUMMARY states in one sentence that this round opened with a measured RED the handoff
      described as green and closes with a measured green rather than a claimed one.
    - Any prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses, or an
      explicit statement that there were none.
  </acceptance_criteria>

  <done>
    The check that reported zero where the count was two has been re-run as a pasted enumeration over
    four named surfaces, with its verdict stated at the reach of the grep that produced it and both
    classes of unreached surface named beside it. The round is recorded on both append-only histories
    without a bound restated, the probe ledger's equality is re-stated rather than assumed, and the
    floor is measured green at the end of a round that was measured red at its start.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a count arrived at by READING to one arrived at by GREP | Check (3)(d) reported zero unmarked standing statements across four surfaces where the count was two. The cure is not a better assertion; it is an enumeration executed over each file's own bytes and pasted in full. |
| the occurrences a REPORT named to the occurrences a FILE contains | The verifier named four. I measured at least nine, because commit `4105fd0` landed seven minutes after the report and falsified everything wave 35 wrote in the present tense about a checked box. An enumeration that starts from anyone's list inherits its blind spot. |
| a row that STATES the box's state to one that POINTS at it | A row carrying no statement of the box's state cannot carry a stale one. That is the verifier's own reasoning and it is why the pointer route is cheaper than another round of markers. |
| history PRESERVED BY RELOCATION to history erased by deletion | Wave 35 prohibited rewriting history so the new bar looks like it was always the bar. Moving bytes into a dated history block satisfies that; deleting them does not, and the difference is visible only in a diff that shows them arriving somewhere else. |
| a marker SCOPED TO ITS SENTENCE to one that reaches its paragraph | The paragraphs carrying superseded claims also carry the limits list, the STORE-03 / STORE-07 deferral and the measured evidence for criteria the verifier confirmed. A marker that falsifies a paragraph deletes a true record in the act of correcting a false one. |
| a correction EXECUTED to a correction CITED | `.planning/STATE.md:366` drifted because nobody re-ran its six probes. Correcting it by quoting the verifier's table would be the same act one round later. The six run again in this session or the correction is not made. |
| deleting a FALSE BLOCKER to restoring the ROW it named | The suite positively asserts `silence-operator-around-global-receiver` absent and calls its return a regression. A correction that reads as an instruction to re-add it would turn a document fix into a code regression. |
| a REAL row to a STALE framing of it | The five rows `:367` names all exist. Under the 2026-08-25 bar a disclosed named row is a residual, not a blocker, and the route the line prescribes states the old bar. Deleting the line would lose true information; re-framing it keeps it. |
| a verdict stated at the reach of its MECHANISM to one stated at the reach of its AMBITION | This enumeration is a one-time grep over four named files under one named phrase set. Two of those files are reached by no standing comparison at all. A verdict that does not say so repeats check (3)(d) one register quieter. |
| a plan that produces EVIDENCE to one that awards itself a VERDICT | Whether criterion (3) is discharged and whether the box may move belong to verification pass 9 on the whole round's evidence. A plan that reads the conclusion off its own measurement is this phase's signature defect in its purest form. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-233 | Repudiation | a cross-surface check that reports zero without counting | critical | mitigate | The enumeration is executed by grep over each file's own bytes, pasted in full as a table with one row per occurrence, classification and evidence, BEFORE any disposition is written; the planning-time target is a reconciliation point and any disagreement is a finding. |
| T-01-234 | Tampering | history erased in the act of making a row read cleanly | high | mitigate | Every passage removed from CORE-11's row reappears byte-identical in a dated 2026-08-26 history block; proved by a file-wide grep that still finds each removed literal while a row-scoped grep does not. |
| T-01-235 | Tampering | a supersession marker reaching past its sentence | high | mitigate | Each marker names the sentence it reaches and states the remainder stands; each is shown by diff to be a pure insertion; the limits list, the STORE-03 / STORE-07 deferral and the measured evidence for criteria (1) and (2) are shown byte-unchanged. |
| T-01-236 | Repudiation | a live blocker corrected by citation rather than execution | high | mitigate | All six shapes re-run through `auditSource` in this session with every returned identifier pasted; the registry's measured-silence rows re-counted and each of the five names checked; a shape that does not report is a first-order finding written down before the line is touched. |
| T-01-237 | Tampering | `silence-operator-around-global-receiver` restored | critical | mitigate | Prohibited in frontmatter and in an acceptance criterion; the corrected line quotes the suite's own regression wording so the correction cannot read as an instruction to re-add it; `git diff --exit-code` over the gate file shown clean. |
| T-01-238 | Spoofing | a cross-surface verdict whose reach exceeds its grep | high | mitigate | The verdict is stated as a one-time grep over four named files under one named phrase set, with both classes of unreached surface named in one sentence as unguarded limits — the same formulation wave 35 used, so the two rounds' verdicts are comparable. |
| T-01-239 | Spoofing | a plan awarding itself the verdict it exists to produce evidence for | critical | mitigate | Prohibited in frontmatter, in the objective and in an acceptance criterion per task. No sentence may state that criterion (3) is discharged, that the box may move, or that the gate is complete; every added sentence is re-read against that prohibition with a one-line verdict recorded. |
| T-01-240 | Tampering | CORE-11's checkbox flipped or re-reverted | critical | mitigate | The box is already `[ ]` and correct. `git diff` over the checkbox asserted EMPTY at every task boundary and the state-agnostic count observed exactly 1, so an honest `[ ]` cannot fail its own gate. |
| T-01-241 | Tampering | a bound restated on a surface no comparison reaches | medium | mitigate | Both `.planning/STATE.md` and `.planning/WINDOWS.md` amendments are written as POINTERS at the ledger and the generated span, each saying so in its own words, and the limit is named wherever this plan's result is stated. |
| T-01-242 | Tampering | `WINDOWS.md` hand-edited out of sync with its front-matter counts | medium | mitigate | Every mutation goes through `gsd-tools windows` with the command and its raw output pasted and the resulting open-entry count stated; `status` is run and pasted before any change. |
| T-01-243 | Repudiation | a probe-ledger row dropped silently | medium | mitigate | `01-PROBE.md`'s equality is re-stated explicitly — confirmed unchanged in writing, or updated in the same commit with the new equality pasted and the moved row named. Never assumed. |
| T-01-244 | Tampering | a whole-file rewrite of the roadmap destroying phase entries | medium | mitigate | The roadmap edit is SCOPED to the Phase 1 plan list and count paragraph, and every phase entry outside the changed region is shown byte-unchanged by diff. |
| T-01-245 | Spoofing | a fabricated API-coverage matrix row | low | accept | This round integrates no external API surface. `COVERAGE.md`'s existing declaration is confirmed in one sentence and no row is added, with that stated explicitly rather than left as an absence. |
| T-01-187 | Tampering | mutating uncommitted task work | high | mitigate | Each task commits its real work before planting any mutation and cross-checks with `git diff --exit-code`. Carried forward unchanged. |
| T-01-188 | Tampering | a gate set nobody ran | medium | mitigate | No active git hooks in this repo, so the full set runs by hand at every task boundary with its output pasted. Carried forward unchanged. |
| T-01-212 | Information Disclosure | the two reader surfaces no mechanism reaches | medium | accept | `.planning/STATE.md` and `.planning/WINDOWS.md` are governed by the pointer-not-a-bound prohibition with no mechanical check; this plan's enumeration is a one-time grep and not a standing gate, and that is stated wherever the verdict is stated. Carried forward from T-01-144 unchanged. |
| T-01-51 | Information Disclosure | the accepted residual of the outbound walk | medium | mitigate | Unchanged by this plan — no registry row moves and the gate file is not edited — and byte-pinned into the ledger by the two comparisons, re-run green at every task boundary. |
| T-01-50 | Information Disclosure | `.spec.ts` files, which the walk skips | low | accept | Unchanged and carried forward, bounded by `pnpm check:bundle`, which specs never enter. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | NOT APPLICABLE: this plan installs no package. `pnpm install` is not run and no manifest appears in `files_modified`. RESEARCH.md's Package Legitimacy Audit is present and unchanged; no `[ASSUMED]` or `[SUS]` package is introduced, so no blocking human checkpoint is required. |
</threat_model>

<verification>
1. Precondition asserted and pasted: `01-36-SUMMARY.md` and `01-37-SUMMARY.md` present, `pnpm test`
   exit 0, both byte comparisons green, working tree clean, `git rev-parse HEAD` beside any snapshot.
2. The ledger enumeration pasted as a table before any disposition, reconciled against the
   planning-time target of nine LINES and eleven PASSAGES, with the plan's ledger grep re-run and its
   output pasted and any disagreement recorded as a finding.
3. Row-scoped counts re-measured for the supersession word, `2026-08-25`, `2026-08-26` and the
   2026-08-24 correction marker, reconciled against 0, 0, 0 and 4.
4. Row-scoped greps for the three removed literals observed 0, and file-wide greps for the same three
   observed at least 1 each — the bytes were relocated, not deleted.
5. The dated 2026-08-26 history block present with its introductory sentence quoted, holding every
   removed passage byte-identical.
6. The row's new pointer clause quoted verbatim, naming the narrative block, `CORE11_BOX_EXPECTED` and
   plan 01-36's correction.
7. CORE-11's first sentence and its checkbox byte-unchanged;
   `grep -cE '^- \[[ x]\] \*\*CORE-11\*\*'` observed exactly 1.
8. Each ledger supersession marker shown by diff to be a pure insertion, its scoping clause quoted, and
   the limits list, the STORE-03 / STORE-07 deferral and the criteria (1)/(2) evidence byte-unchanged.
9. STORE-03 and STORE-07 entries byte-identical, shown by an EMPTY diff.
10. All six shapes re-executed through `auditSource` with every returned identifier pasted beside what
    the blocker line claimed.
11. The registry's `kind: "measured-silence"` rows counted and listed, the five names checked present
    and `silence-operator-around-global-receiver` checked absent, reconciled against 26 / five / one.
12. The STATE.md enumeration pasted as a table, reconciled against the planning-time target of
    FIFTEEN lines across three regions, with the plan's STATE grep re-run and its output pasted.
13. Blockers-section greps for the six-shapes clause and the flip-ownership clause observed 0, with any
    remaining whole-file occurrence shown to sit in a dated section under a scoped marker.
14. The corrected blocker line, the corrected flip-ownership line and the re-framed five-row line each
    quoted verbatim, each restating no bound and awarding no verdict.
15. Every dated, plan-attributed occurrence marked by a pure-insertion marker whose scoping clause is
    quoted — including `:290` and `:296`, which carry the same false six-shapes claim as `:366` — with
    the still-true parts of the last pointer amendment named explicitly, and any undated, unattributed
    occurrence disposed of as STANDING.
16. The new 2026-08-26 pointer amendment present and quoted, recording the round's four findings, both
    classes of unreached surface, the box's state and verification pass 9's ownership of it.
17. The four-surface enumeration re-run and pasted, with the two assertions' measured results stated
    and any surviving unmarked occurrence NAMED rather than silently repaired.
18. The cross-surface verdict stated at the reach of the grep that produced it, with both classes of
    unreached surface named in one sentence, quoted verbatim.
19. No sentence anywhere in this plan states or implies that criterion (3) is discharged, that the box
    may move, or that the gate is complete; the review is recorded as a one-line-per-surface verdict.
20. `gsd-tools windows status` pasted before any change; every subsequent command pasted with raw
    output; one entry superseded, one pointer entry appended, the resulting open count stated.
21. `01-PROBE.md`'s equality re-stated explicitly — unchanged at 38 == 27 + 11 with 0 dismissed, or
    updated in the same commit with the new equality pasted and the moved row named.
22. `COVERAGE.md`'s existing declaration confirmed in one sentence with no matrix rows added.
23. The assumption-delta detector run with its result recorded in one line; no checkpoint raised if it
    did not fire.
24. `.planning/ROADMAP.md`'s three planner-written entries reconciled against their SUMMARYs, any
    contradicted description corrected with before and after pasted, all three checkboxes marked `[x]`,
    the count paragraph confirmed at 38, and the edit shown by diff to be SCOPED with every other phase
    entry byte-unchanged; `grep -c '01-38-PLAN.md' .planning/ROADMAP.md` at least 1.
25. `git diff --exit-code -- packages/backend/src/outbound-prohibition.spec.ts` clean at every task
    boundary.
26. `pnpm test` — exit 0, no fewer than 31 files and 1372 tests.
27. `pnpm typecheck`, `pnpm lint`, `pnpm knip` — all exit 0.
28. `pnpm build:backend && pnpm check:bundle` — exactly one import specifier, `crypto`.
29. Real-tree run over both `SOURCE_ROOTS` — 23 files, 0 violations.
30. One sentence recording that the round opened with a measured RED the handoff described as green and
    closes with a measured green.
31. Every prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses, or an
    explicit statement that there were none.
</verification>

<success_criteria>
- A reader opening `.planning/REQUIREMENTS.md` cold meets a CORE-11 row that states the prohibition and
  points — it carries no statement of the box's state and no bound, so it cannot carry a stale one.
- Every passage removed from that row is still in the file, byte-identical, in a dated history block,
  and a file-wide grep proves it while a row-scoped grep proves the row is clean.
- A future planner reading `.planning/STATE.md`'s live blockers is told what is true as measured in
  this session: six shapes that report, a row the suite requires to stay gone, and five disclosed
  residuals framed as residuals under the 2026-08-25 bar.
- The row `:366` named is not restored anywhere, and the correction says in its own words why it must
  not be.
- The statements commit `4105fd0` falsified — on both files — carry scoped markers naming that commit,
  with their bytes intact and the true records beside them untouched.
- Criterion (3)(d)'s cross-surface check has been re-run as a pasted enumeration rather than asserted,
  and its verdict is stated at the reach of the grep that produced it with both classes of unreached
  surface named beside it.
- Nothing in this plan awards criterion (3) a verdict or moves CORE-11's box; the round produces
  evidence and verification pass 9 produces the determination.
- Both append-only histories record the round through a pointer that restates no bound, and
  `WINDOWS.md` was amended only through its tool.
- The probe ledger's no-silent-drop equality is re-stated in writing rather than assumed, and no row
  moved silently.
- The floor is measured green at the end of a round that was measured red at its start: no fewer than
  31 files / 1372 tests, typecheck / lint / knip clean, bundle at exactly one specifier, real tree at
  23 files / 0 violations, and nothing leaks.
</success_criteria>

<artifacts_this_phase_produces>
| Artifact | Kind | Purpose |
|----------|------|---------|
| the ledger standing-statement enumeration | new evidence | Every statement of CORE-11's box state or of a superseded bar in `.planning/REQUIREMENTS.md`, found by grep over the file's own bytes and classified by a two-valued rule — the count check (3)(d) reported without taking |
| CORE-11's row reduced to a prohibition plus a pointer | corrected ledger | The verifier's prescribed route: a row that carries no statement of the box's state cannot carry a stale one, pointing at the narrative block and at `CORE11_BOX_EXPECTED` |
| the dated 2026-08-26 history block in `.planning/REQUIREMENTS.md` | corrected disclosure | Every passage removed from the row, preserved byte-identical, so the pointer route relocates history instead of erasing it |
| the scoped supersession markers over the revert-falsified paragraphs | corrected disclosure | The present-tense statements that the box is checked, marked in place with commit `4105fd0` named as what falsified them and their bytes left intact |
| the six re-executed `auditSource` probes | new evidence | The shapes a live blocker line said report nothing, run again in this session with every returned identifier pasted — the correction rests on execution, not on the report that found the defect |
| the registry measured-silence re-count | new evidence | The count and the ids, with the five names from the five-row blocker checked present and the removed row checked absent |
| the corrected `### Blockers` lines | corrected ledger | A false blocker replaced by what is measured, a stale flip-ownership line replaced by the true position, and five real rows re-framed from blockers into disclosed residuals under the 2026-08-25 bar |
| the scoped markers over the two dated STATE.md copies | corrected disclosure | The `### Decisions` copy and wave 35's pointer amendment marked rather than rewritten, with the still-true parts named explicitly |
| the 2026-08-26 pointer amendment for waves 36–38 | corrected ledger | The round recorded on an append-only history as a pointer, restating no bound, naming both classes of unreached surface and leaving the box to verification pass 9 |
| the re-run four-surface cross-surface enumeration | new evidence | Criterion (3)(d)'s check performed as a pasted measurement over four named files, with its verdict stated at the reach of the grep that produced it |
| the `WINDOWS.md` supersession and pointer entry | corrected ledger | The gate file's open entry superseded and one pointer entry appended through `gsd-tools windows`, with every command and raw output pasted |
| the re-stated `01-PROBE.md` equality | corrected disclosure | 38 == 27 + 11 confirmed in writing rather than assumed, so the no-silent-drop property stays checkable |
| the reconciled `ROADMAP.md` rows for waves 36–38 | corrected ledger | The planner's three entries checked against what actually executed, corrected where the measurement contradicted them and marked, by a scoped edit that leaves every other phase entry byte-unchanged |
| `01-38-SUMMARY.md` | new evidence | Both enumerations, the six probes, the registry re-count, every corrected line quoted verbatim, the four-surface re-run with its scoped verdict, the tool commands with raw output, the probe equality and the closing gate set |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-skeleton-persistence-compatibility/01-38-SUMMARY.md` when done.

It must contain, as pasted terminal output rather than as prose: the precondition assertions with
`git rev-parse HEAD`; the `.planning/REQUIREMENTS.md` enumeration table with every occurrence
classified and the planning-time target reconciled; the row-scoped counts for the supersession word,
`2026-08-25`, `2026-08-26` and the 2026-08-24 marker; the row-scoped greps for the three removed
literals at 0 beside the file-wide greps for the same three at one or more; the dated history block
with its introductory sentence; the row's new pointer clause; the EMPTY diffs over CORE-11's first
sentence, its checkbox, the STORE-03 and STORE-07 entries and the gate file; every ledger and STATE.md
supersession marker shown as a pure insertion with its scoping clause; the six `auditSource` results in
a table beside what the blocker line claimed; the registry's measured-silence count with its ids and
the five presence checks and the one absence check; the `.planning/STATE.md` enumeration table; the
Blockers-section greps at 0 with the whole-file occurrences accounted for; the three corrected lines
and the new pointer amendment quoted verbatim; the four-surface re-run enumeration with its two
measured results; every `gsd-tools windows` command with its RAW output and the resulting open count;
the scoped `ROADMAP.md` diff; and the final `pnpm test`, `typecheck`, `lint`, `knip`, `check:bundle`
and real-tree results.

It must quote VERBATIM the sentence stating the cross-surface verdict at the reach of the grep that
produced it, with BOTH classes of unreached surface named — unguarded FILES (`.planning/STATE.md` and
`.planning/WINDOWS.md`, reached by no mechanical comparison) and undeclared PHRASINGS outside the
enumeration's phrase set.

It must state in ONE sentence that no passage was erased — every byte removed from CORE-11's row is in
the dated history block, proved by a file-wide grep that still finds what a row-scoped grep no longer
does.

It must state in ONE sentence that the six probes were RE-EXECUTED in this session rather than cited
from verification pass 8, and in ONE further sentence that
`silence-operator-around-global-receiver` was not restored and that the suite requires it to stay gone.

It must state in ONE sentence that this plan awards no verdict on criterion (3) and does not move
CORE-11's box, and that both determinations belong to verification pass 9 on the whole round's
evidence.

It must state in ONE sentence that the round OPENED with a measured RED that the handoff described as
green — 2 failed of 1372, caused by commit `4105fd0` — and CLOSES with a measured green rather than a
claimed one, and in ONE further sentence that the must-NOT did not move: CORE-11's first sentence
byte-identical, 23 files over both `SOURCE_ROOTS` at zero violations, one import specifier in the
shipped bundle, and every finding this round touched a defect in a test-only gate's description of
itself or in two planning documents.

It must also record any PREDICTION-VS-MEASUREMENT discrepancy in the shape `01-27-SUMMARY.md` uses, and
if there were none, say so explicitly.
</output>
