---
phase: 01-skeleton-persistence-compatibility
plan: 42
type: execute
wave: 42
gap_closure: true
depends_on: [01-41]
files_modified:
  - packages/backend/src/outbound-prohibition.spec.ts
# ROUND 10 IS A DISCLOSURE CORRECTION, NOT AN ELEVENTH NARROWING. READ THIS BLOCK BEFORE THE TASKS.
#
# THE OPERATOR HAS TAKEN VERIFICATION PASS 10's EXIT 2. Pass 10's `## The mechanism judgement` offered
# two exits and the operator chose the second, in these words: STOP CLAIMING A SITE. Nothing in the
# 2026-08-25 re-scoped bar — DERIVED, DRIFT-DETECTABLE, SOLE BOUND — requires the anchor to identify a
# SITE. It requires the disclosure to be ACCURATE and CONTRADICTED NOWHERE. The site-identity claim is
# prose this file volunteered; it was never a bar requirement. So round 10 is four bounded edits, all
# green on arrival, that convert an overclaim into a named, measured, drift-detectable residual — the
# artifact kind waves 27-29 established for everything else in this gate.
#
# CR-22 IS THIS PLAN, AND IT IS BOTH PARTS OR IT IS NOT CLOSED.
#   PART 1 — THE PUBLISHED RESIDUAL IS WRONG IN KIND, NOT IN DEGREE. TWO SURFACES, TWO DIFFERENT
#   PARAGRAPHS, ONE WRONG-QUANTITY INFERENCE. Round 10's revision found that the previous draft of this
#   plan quoted BOTH surfaces as closing on the same words, and the tree contradicts it. RE-MEASURED
#   THIS SESSION: `grep -c 'interchangeability residual is that wide'` returns 1 for the whole file, at
#   `:9093` only. THAT MISQUOTE IS ITSELF A ROUND-10 FINDING and is recorded as one — a plan whose
#   subject is the accuracy of quoted claims must not misquote the file. The two surfaces, verbatim:
#     SURFACE A — LIMIT (5), `:9090-9093`:
#       `RE-MEASURED AT WAVE 39 AFTER BOTH CHANGES, the maximum distance from an / occurrence to its
#        anchor is 234 LINES — the occurrence on line 235 takes / this file's own title line — so `the
#        same construct` can span a couple / of hundred lines and the interchangeability residual is
#        that wide.`
#     SURFACE B — THE `constructAnchorFor` DOCBLOCK, `:9342-9345`, under the heading at `:9339-9341`
#     (`IT IS THE NEAREST PRECEDING ACCEPTED LINE, WHICH IS NOT THE SAME AS THE / FINEST ENCLOSING
#     CONSTRUCT, AND THE DIFFERENCE IS MEASURED RATHER THAN / ESTIMATED.`):
#       `region, and in this file those regions are large: WR-49 measured four / occurrences taking the
#        file's own title line as their anchor from up to 234 / lines above them, and wave 39
#        re-measured the maximum after correcting the / scan bounds and got 234 again, at the occurrence
#        on line 235.`
#   WHY SURFACE B IS IN SCOPE, STATED SEPARATELY BECAUSE IT IS A DIFFERENT SENTENCE: it offers 234 as
#   THE MEASURE OF THE PROXIMITY-VERSUS-CONTAINMENT GAP, under a heading that claims the difference is
#   `MEASURED RATHER THAN ESTIMATED`. That is the same wrong-quantity inference as surface A wearing
#   different words — the gap between proximity and containment is the width of the region a single
#   anchor covers, not the distance a shipped sentence happens to sit from it. THE 234 IS
#   CORRECT — pass 10 re-measured it at 234 — and the inference from it is not. Occurrence-to-anchor
#   distance is a fact about where 29 shipped sentences happen to sit. The interchangeability residual
#   is the width of the anchor's SHADOW: every line that resolves to the same anchor. Measured by pass
#   10 with the LIVE builder over all 8,846 surface lines, the widest shadow is the header row at `:417`
#   at 574 surface lines / 1,155 raw (419..1574), and it already holds FOUR shipped occurrences
#   (`:436`, `:468`, `:523`, `:600`); then 283, 202, 115, 98. The published figure is roughly FIVE TIMES
#   too small. Pass 10 relocated `:436` 514 lines and then 1,129 lines, byte-identical key, exemption
#   reason false in both new homes, `Tests 439 passed (439)` both times.
#   PART 2 — PIN THE SHADOW SO THE NEXT WIDENING IS LOUD. Group `SURFACE_LINES` by resolved anchor and
#   assert the maximum against a pinned value, in the shape this file already uses for every other
#   measured residual. This is the part that makes the corrected sentence DRIFT-DETECTABLE rather than
#   merely true on the day it was written, and it is why part 1 alone does not close the item.
#
# WHAT THIS PLAN IS FORBIDDEN TO DO, AND THE PROHIBITIONS BELOW MAKE EACH MECHANICAL.
#   - NO AST, NO PARSER, NO COMPILER-API, NO ENCLOSING-FRAME IDENTITY WORK. Pass 10's exit 1 was to
#     replace proximity with containment using the compiler API this file already imports for
#     `auditSource`. THAT EXIT WAS NOT TAKEN. The import stays where it is, `auditSource` is not read
#     for this purpose, and no new use of it appears in the diff. Asserted by a structural check over
#     the diff, not by a promise.
#   - NO FURTHER NARROWING OF `constructAnchorFor`. Not its five recognisers, not its backward-scan
#     bound, not its forward-walk bound, not the key format, not `constructTokenOf`, not
#     `CONSTRUCT_TOKEN_ELLIPSIS`, not `EXEMPTION_ANCHOR_SEP`, not `maskQuantifiers`, not
#     `normalizeGateLine`, not `nameableRemainder`, not `exemptionKeyFor`. Wave 39 took both scan bounds
#     to their limit; pass 10 states plainly that a fourth narrowing round is the option it would expect
#     to produce an eleventh instance of the signature defect. The BODY of every one of those is
#     asserted byte-unchanged by diff.
#   - DO NOT FOLD LINE NUMBERS INTO THE KEY. The census case's own message at `:10434` calls that `the
#     tempting one and the worst` and is right: it would make the case green having measured nothing.
#   - DO NOT RE-OPEN THE 2026-08-25 RE-SCOPE. Pass 8 tested it for a quietly-lowered bar and found none;
#     pass 9 called round 8 evidence FOR it; pass 10 did not re-test it and does not report it as a
#     defect. The `[x]` is unearned because criterion (3)'s mechanism was bypassable, not because the
#     bar is wrong.
#   - DO NOT FLIP OR REVERT CORE-11's CHECKBOX. It reads `[ ]`, `CORE11_BOX_EXPECTED` at `:10983`
#     agrees, round 9 did not flip it and plan 01-41 correctly left `requirements mark-complete` UNRUN.
#     That is the handoff pattern and it is preserved. Pass 11 makes the call, not this round. This plan
#     opens NO file under `.planning/`.
#   - DO NOT RETIRE THE SITE-IDENTITY CLAIM SILENTLY. It is retired DELIBERATELY and IN THE BYTES, dated,
#     with the reason stated — the bar never required it — and with an enumeration of every site claim
#     in the exemption-anchoring region, each given a one-line disposition. A deletion with no record is
#     the same defect this round exists to correct, pointing the other way.
#   - DO NOT SWEEP THE WORD `site`. This file uses `site` as a REGISTRY FIELD NAME (`ResolverRecord.site`
#     at `:4134`, `site:` values throughout `RESOLVER_REGISTRY`) and as ordinary prose about call sites.
#     Only the EXEMPTION-ANCHORING claims are in scope; every other occurrence is asserted byte-unchanged.
#
# WHAT NOTHING IN THIS ROUND TOUCHES, RE-ESTABLISHED INDEPENDENTLY BY PASS 10 AND RE-MEASURED BY TASK 1.
# The MUST-NOT holds and CORE-11's first sentence is byte-identical after this round. `pnpm test` 31
# files / 1379 tests exit 0; `tsc --build` exit 0; `check:bundle` ONE specifier, `crypto`; the gate suite
# 439; the real-tree walk 23 shipped non-spec modules across both `SOURCE_ROOTS` at ZERO violations; nine
# probes through `auditSource` all reporting as expected. EVERY ITEM IN THIS ROUND IS A DEFECT IN A
# TEST-ONLY GATE'S DESCRIPTION OF ITSELF. Nothing leaks and no severity here says otherwise.
#
# THE SIGNATURE DEFECT STANDS AT TEN AND CR-22 IS THE TENTH. It was written INTO THE PARAGRAPH CLOSING
# THE NINTH. Every item in this round is a sentence about what the gate covers, which makes it the
# highest-risk round yet for authoring an eleventh. Every sentence either task writes carries its REACH
# INSIDE THE CLAIMING CLAUSE, and every task records a one-line no-overclaim verdict per sentence.
#
# ENVIRONMENT FACTS, MEASURED THIS SESSION AT HEAD `483a8ee` — RECONCILIATION ANCHORS, NOT ADDRESSES.
# Round 9 shifted this file three times. Every number below is a reconciliation anchor with a re-derive
# instruction; DISAGREEMENT IS A FINDING and is recorded before anything is adjusted.
#   - THE FILE'S OWN LENGTH IS DISPUTED BY THREE SURFACES AND THIS IS THE FIRST FINDING TO RESOLVE.
#     `wc -l` and `awk 'END{print NR}'` both give 11071 this session at `483a8ee`. `01-VERIFICATION.md`
#     states 11,079 in two places; `01-REVIEW.md` states 11,072. The file ends with a newline (checked).
#     WR-55's prescribed replacement text contains the figure 11,072 — DO NOT COPY IT. Re-derive the
#     count, write the MEASURED number, and record which of the three surfaces it agrees with.
#   - LIMIT (5)'s 234 SENTENCE: `:9090-9093`, inside the docblock that closes at `:9099` above
#     `HEADER_QUANTIFIER_EXEMPTIONS` at `:9100`. `grep -n 234` returns exactly THREE lines this session:
#     `:9091`, `:9343`, `:9345`. If it returns anything else, that is a finding.
#   - THE `constructAnchorFor` DOCBLOCK's 234 SENTENCE: it OPENS mid-`:9342` (`… those regions are
#     large: WR-49 measured four`) and closes on `:9345`; `234` itself appears on `:9343` and `:9345`.
#     Its heading is `:9339-9341`. RE-MEASURED THIS SESSION AND IT IS NOT THE SAME TEXT AS SURFACE A:
#     `grep -c 'interchangeability residual is that wide'` = 1 (`:9093` only) and
#     `grep -c 'got 234 again'` = 1 (`:9345` only). The sentence sits inside the docblock that closes at
#     `:9372` above the declaration at `:9373`. The body runs `:9373-9420` with the backward scan opening
#     at `i = lineNumber - 1`, the forward walk bounded `k < lineNumber`, and the single
#     `return NO_PRECEDING_CONSTRUCT` at `:9419`.
#   - THE SITE-IDENTITY CLAIM PASS 10 NAMED: `:10282`, the clause `that is fine — the construct half is
#     what names it`, inside the failure message of the null-anchor case opening at `:10277`. Pass 10
#     cites it as `:10281-10283` and `01-REVIEW.md` as `:10272-10275`; both are reconciliation anchors
#     for one clause and the clause is located BY TEXT.
#   - THE OTHER FOUR SITE MENTIONS IN THE ANCHORING REGION, AS A FLOOR AND NOT A LIST: `:9416` (`an
#     anchor that travelled with the sentence and named no site`), `:10337` and `:10339` (`still names no
#     SITE` / `a self-anchoring key names no site`), `:10728` (`the key names no site`). ALL FOUR ARE
#     NEGATIVE STATEMENTS ABOUT A DEFECT SHAPE and are expected to survive; each still gets a written
#     disposition. Re-derive the enumeration; an unlisted hit is a finding.
#   - THE ANCHOR AT THE CENTRE OF CR-22, ALL CONFIRMED BY CONTENT THIS SESSION: `:417` is the header row
#     `//      SPELLING (operator, by POSITION)         RESOLVED BY          REPORTS`; `:418` is its rule
#     of dashes; `:436` is the wrapped cell `//                                                                       any depth`;
#     `:1` is the file's own path line; the imports begin at `:1567` and the compiler-API import is at
#     `:1570`. The machine-owned derived-residual span opens at `:952` and closes at `:1565`, and its
#     lines are EXCLUDED from `SURFACE_LINES` by exclusion one — which is why 1,155 raw lines carry only
#     574 surface lines.
#   - THE HELPERS, BY DECLARATION LINE: `HEADER_QUANTIFIER_EXEMPTIONS` `:9100`, `normalizeGateLine`
#     `:9172`, `maskQuantifiers` `:9202`, `NO_PRECEDING_CONSTRUCT` `:9241`, `nameableRemainder` `:9249`,
#     `EXEMPTION_ANCHOR_SEP` `:9265`, `CONSTRUCT_TOKEN_ELLIPSIS` `:9276`, `constructTokenOf` `:9286`,
#     `anchorTokenCensus` `:9300`, `constructAnchorFor` `:9373`, `exemptionKeyFor` `:9439`,
#     `preAnchoringExemptionKeyForFixtureOnly` `:9478`, `quantifierOccurrences` `:9491`,
#     `surfaceExemptionKeys` `:9517`. `lineOf` `:9895`. `EXCLUSIONS` `:9980-10016`. `SURFACE_LINES`
#     `:10026-10028`. `CORE11_BOX_EXPECTED` `:10983`.
#   - THE SUITE COUNTS TO RE-MEASURE RATHER THAN ASSUME: whole suite 31 files / 1379 tests exit 0;
#     `tsc --build` exit 0; `check:bundle` exactly ONE specifier, `crypto`; the gate file alone 439;
#     the real-tree walk 23 shipped non-spec modules / 0 violations; `SURFACE_LINES.length` 8846.
#   - THE ROUND BASE IS THE TREE, NOT THE COMMIT ID. `483a8ee` is the commit pass 10 measured, and the
#     spec file is byte-identical to it as of this revision — verified with
#     `git diff --stat 483a8ee..HEAD -- packages/backend/src/outbound-prohibition.spec.ts` returning
#     EMPTY. HEAD at execution time will NOT be `483a8ee`: the round-10 plan commits sit on top of it and
#     touch only `.planning/`. So the load-bearing precondition is the FILE's byte-identity to
#     `483a8ee`, asserted numerically, and `483a8ee` being an ancestor of HEAD — not an equality on
#     `git rev-parse HEAD`. A spec-file difference IS a finding; a HEAD difference explained entirely by
#     `.planning/` commits is not.
#   - NO ACTIVE GIT HOOKS. COMMIT BEFORE MUTATING — wave 29 destroyed its own Task 1 with
#     `git checkout --`, and every mutation in this plan is plant-observe-restore over committed work.
#   - EVERY `<automated>` BLOCK OPENS WITH `set -o pipefail` AND EVERY COUNT IS ASSERTED NUMERICALLY
#     AGAINST A PRECONDITION-CAPTURED VALUE READ FROM `"${TMPDIR:-/tmp}/defminer-round10-baseline"`,
#     never against a literal written into the plan. Round 8 shipped a revision because
#     `pnpm test 2>&1 | tail -8` exits with TAIL's status; round 9 shipped one because a `grep -c` gate
#     was green with the defect present; ROUND 10 SHIPPED THIS REVISION FOR THE SAME REASON A THIRD
#     TIME — three gates in the first draft were green with their own defect fully present. Both
#     regressions are one careless edit away. Do not strip `pipefail` and do not replace a captured
#     value with a literal.
#   - THE BASELINE FILE CARRIES TWO GATE COUNTS, NOT ONE, AND THE SPLIT IS LOAD-BEARING.
#     `BASE_GATE_ARRIVAL` and `BASE_TESTS_ARRIVAL` are written ONCE by Task 1's precondition from the
#     arrival measurement and are NEVER re-written by any task in either plan. `BASE_GATE_CURRENT` and
#     `BASE_TESTS_CURRENT` are re-written at every task boundary to the count as of that boundary. EVERY
#     failure message names WHICH of the two it compared against. The first draft of this plan carried a
#     single `BASE_GATE`, labelled it ARRIVAL in one failure message and mutated it after Task 1 — a
#     value labelled as one thing holding another, which is the species of defect this round exists to
#     correct, one level up.
#
# ROUND-10 REVISION — EVERY GATE THIS PLAN ALTERED WAS DEMONSTRATED RED AGAINST A PLANTED VIOLATION
# BEFORE IT SHIPPED, AND THE COUNTS ARE RECORDED HERE SO THE EXECUTOR CAN REPRODUCE THEM. Three gates in
# the first draft were green with their own defect fully present. All plants were made on the worktree
# at `483a8ee`, observed, and restored with `git checkout --` and `git diff --exit-code` clean.
#   (1) THE ANTI-NARROWING CHECK WAS A TOKEN GREP AND COULD NOT SEE THE MOST NATURAL NARROWING. In
#       `constructAnchorFor` the recogniser NAMES and their regex BODIES sit on different lines
#       (`:9377`/`:9378`, `:9379`/`:9380`, `:9382-9385`), so an edit to a body produces a diff line
#       carrying no gated token. MEASURED, three plants, each `git diff 483a8ee -- <spec>`:
#         plant A — `:9378` `[A-Za-z_$]` -> `[A-Za-z_]` (one character): OLD token grep 0, NEW body diff 4.
#         plant B — a `continue` guard inserted above `let head = -1;` at `:9390`: OLD 0, NEW 2.
#         plant C — `:9384` `n.length >= 4` -> `>= 6`: OLD 0, NEW 4.
#       REPLACEMENT: a BODY-SCOPED diff that asserts byte-identity by identifier, extracting each
#       helper's declaration-to-close range from `git show 483a8ee:<spec>` and from the worktree with the
#       same awk, then asserting the `diff` line count is 0 AND the extracted range is non-vacuous. On
#       the unmodified file it returns, and the executor must paste, exactly:
#         constructAnchorFor 47 lines / 0 diff; constructTokenOf 7 / 0; exemptionKeyFor 12 / 0;
#         maskQuantifiers 9 / 0; normalizeGateLine 5 / 0; nameableRemainder 11 / 0;
#         auditSource 1179 / 0; import block 4 lines / 0 diff.
#       The token grep is KEPT as a cheap second net; the body diff is what asserts.
#   (2) THE AST PROHIBITION WAS BROADER THAN ITS CHECK. `[^A-Za-z]ts\.[A-Za-z]` covers the named
#       temptation, but the prohibition also claimed `git diff` over `auditSource` and over the import
#       block was EMPTY and NO `<automated>` block contained either check. MEASURED: appending a
#       hand-rolled brace-depth `enclosingFrameOf` helper leaves the AST check at 0; adding
#       `import { sep } from "node:path";` leaves it at 0. REPLACEMENT: the `auditSource` body diff and
#       the import-SET diff are now executed (both 0 on the unmodified file, `auditSource` 1179 lines,
#       imports 4 lines), and the import plant turns the import check RED at 2 diff lines. A
#       named-identifier net for hand-rolled containment shapes is added as well: 0 on the clean tree, 4
#       against the planted `enclosingFrameOf`, and 0 against the reach PROSE Task 1 legitimately adds
#       (a comment naming CONTAINMENT), so it is non-vacuous without being false-positive.
#       ITS LIMIT, STATED RATHER THAN HIDDEN: that net matches SPELLINGS. A containment helper using
#       none of them passes it. The executable coverage of the hand-rolled case is that net plus the
#       per-sentence acceptance item requiring the executor to state, in writing, that no containment was
#       computed by ANY means, AST or hand-rolled. It is not a proof and this plan does not call it one.
#   (3) THE `T234` GUARD WAS HALF UNMATCHABLE. The fragment `maximum distance from an occurrence to its
#       anchor` is split across `:9090` and `:9091` by the line wrap, so it matches NO line:
#       `grep -c` returns 0 for the whole file. Only `interchangeability residual is that wide` did any
#       work, so a Task 1 edit to `:9091` alone went undetected. MEASURED: planting `is 234 LINES` ->
#       `is 234 lines` on `:9091` gives OLD guard 0, NEW guard 1; planting `got 234 again` ->
#       `got 233 again` on `:9345` gives OLD 0, NEW 1. REPLACEMENT: three SINGLE-LINE fragments, each
#       verified to match exactly one line of the unmodified file — `is 234 LINES` (`:9091`),
#       `interchangeability residual is that wide` (`:9093`), `got 234 again` (`:9345`).
#   (4) AND TASK 2's OWN COMPLETION GATE WAS GREEN ON A HALF-DONE FIX. It asserted only that
#       `interchangeability residual is that wide` was gone — which surface B does not contain — so
#       correcting limit (5) and leaving the `constructAnchorFor` docblock untouched passed. MEASURED
#       across four states with the replacement two-surface gate (`R1` = old-inference count, `T234N` =
#       total `234` mentions, `R2` = `234` lines with no `shadow` within +-3 lines):
#         unmodified            R1=1 T234N=3 R2=3  -> RED
#         both surfaces fixed   R1=0 T234N=3 R2=0  -> GREEN
#         all 234s deleted      R1=1 T234N=0 R2=0  -> RED   (the floor catches the dodge)
#         only limit (5) fixed  R1=0 T234N=3 R2=2  -> RED   (the state the old gate passed)

autonomous: true
requirements: [CORE-11]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low
# `estimate-calibration` reports `{"factor": 1, "applied": false, "sample_count": 0, "min_samples": 3}`,
# so `tokens` equals `raw_tokens` and the confidence is DERIVED from the sample count, never self-rated.
# Weigh the MEASUREMENT COUNT over the token figure: one live-builder census over every surface line, one
# new pinned case watched failing, one relocation watched STAYING GREEN as the honest statement of what
# the pin does not do, two disclosure paragraphs replaced, and one claim retired against an enumeration.

must_haves:
  truths:
    - "THE PUBLISHED INTERCHANGEABILITY RESIDUAL IS THE ANCHOR'S SHADOW, MEASURED, AND NOT THE OCCURRENCE-TO-ANCHOR DISTANCE — AT BOTH SURFACES, WHICH ARE TWO DIFFERENT PARAGRAPHS AND NOT ONE REPEATED SENTENCE. SURFACE A is limit (5) at `:9090-9093`, which closes on `so `the same construct` can span a couple of hundred lines and the interchangeability residual is that wide`. SURFACE B is the `constructAnchorFor` docblock at `:9342-9345`, which reads `WR-49 measured four occurrences taking the file's own title line as their anchor from up to 234 lines above them, and wave 39 re-measured the maximum after correcting the scan bounds and got 234 again, at the occurrence on line 235` — a DIFFERENT sentence making the SAME wrong-quantity inference, because it offers 234 as the measure of the proximity-versus-containment gap under a heading claiming that difference is `MEASURED RATHER THAN ESTIMATED`. Both state instead the measured width of the widest shadow over `SURFACE_LINES`, with the anchor that owns it named, the count of shipped occurrences inside it stated, and 234 kept and correctly re-described as the largest distance any shipped sentence happens to sit from its anchor, which bounds nothing. Asserted by a gate that is RED on the unmodified file, RED when only surface A is corrected, and RED if the 234s are deleted rather than re-described."
    - "THE SHADOW IS PINNED, SO THE NEXT WIDENING IS LOUD. A named case groups every line of `SURFACE_LINES` by the anchor `constructAnchorFor` resolves for it and asserts the MAXIMUM group size against a pinned value, non-vacuously, with a failure message that names the owning anchor at runtime and states that a shadow which grew is a residual which grew. Sampling, early exit and any form of partial evaluation are forbidden: the case reads every surface line."
    - "THE PIN WAS WATCHED FAILING, AND WHAT IT DOES NOT CATCH WAS WATCHED STAYING GREEN. Surface lines planted inside the widest shadow turn the new case RED with the arithmetic visible in its message. Verification pass 10's own relocation of the shipped occurrence at `:436` across that shadow is then re-run and observed GREEN, and that green is written into the bytes as the measured statement of what the pin does not do — it makes the residual's SIZE measured and drift-detectable; it does not prevent a relocation inside it."
    - "THE SITE-IDENTITY CLAIM IS RETIRED DELIBERATELY, DATED, AND WITH THE REASON. The clause at `:10282` that says the construct half NAMES the occurrence is replaced by a statement that the construct half names a REGION whose width is measured and pinned, that pass 10 falsified the naming claim by two executed relocations, and that the 2026-08-25 bar — DERIVED, DRIFT-DETECTABLE, SOLE BOUND — never required the anchor to identify a site. Every other `site` claim in the exemption-anchoring region is enumerated and given a written disposition; none is deleted without one."
    - "NOTHING WAS NARROWED AND NO CONTAINMENT WAS BUILT, ASSERTED BY BODY-SCOPED BYTE-IDENTITY AND NOT BY A TOKEN GREP. `constructAnchorFor`, `constructTokenOf`, `exemptionKeyFor`, `maskQuantifiers`, `normalizeGateLine` and `nameableRemainder` each have their declaration-to-close range extracted BY IDENTIFIER from `git show 483a8ee:<spec>` and from the worktree and the `diff` asserted EMPTY numerically, with each range asserted non-vacuous first — 47 / 7 / 12 / 9 / 5 / 11 lines respectively on the unmodified file. `auditSource`'s body (1179 lines) and the import SET (4 lines) are asserted byte-identical the same way. A token grep over the diff is kept as a cheap second net, and it is stated in the plan that it alone returns 0 against a one-character narrowing of `:9378`, a `continue` guard at `:9390` and a raised threshold at `:9384` — which is why it is not what asserts. Hand-rolled containment is covered by a named-identifier net (0 clean, 4 against a planted brace-depth helper, 0 against the reach prose) PLUS a written per-sentence statement, and the net's limit — it matches spellings, not shapes — is stated rather than hidden."
    - "THE BASELINE WAS RE-MEASURED, NOT ASSUMED, AND IT CARRIES ARRIVAL AND CURRENT AS SEPARATE VALUES. The whole suite, `tsc --build`, `check:bundle`, the gate suite's own count, `SURFACE_LINES.length` and the real-tree walk are all executed in this session and captured to the round's baseline file. `BASE_GATE_ARRIVAL` and `BASE_TESTS_ARRIVAL` are written once by Task 1's precondition and re-written by NO task in either plan; `BASE_GATE_CURRENT` and `BASE_TESTS_CURRENT` are re-written at every task boundary. Every later numeric assertion in this round is made against one of those captured values rather than against a literal, and every failure message NAMES WHICH ONE it compared against — a value labelled ARRIVAL that holds a post-edit count is the same species of defect this round exists to correct."

  artifacts:
    - path: "packages/backend/src/outbound-prohibition.spec.ts"
      provides: "A named case pinning the widest anchor shadow over `SURFACE_LINES` against a measured value, watched failing; the relocation it does NOT catch, re-run and recorded green in the bytes; limit (5) and the `constructAnchorFor` docblock restating the residual as the measured shadow instead of the 234-line distance; and the site-identity claim retired with its date, its reason and an enumeration of every claim in the region"
      contains: "shadow"

  key_links:
    - from: "a residual published as an occurrence-to-anchor distance"
      to: "a residual published as the measured width of the anchor's shadow"
      via: "a live-builder grouping of every line of `SURFACE_LINES` by resolved anchor, with the maximum, the owning anchor and the shipped occurrences inside it all measured before either sentence is written"
      pattern: "interchangeability residual"
    - from: "a residual stated once and true only on the day it was written"
      to: "a residual that turns the suite RED the next time it widens"
      via: "an exact equality against the measured maximum, non-vacuous, with the owning anchor named at runtime in the failure message"
      pattern: "shadow"
    - from: "a pin nobody has watched failing"
      to: "a pin watched failing, beside the relocation it is watched NOT catching"
      via: "surface lines planted inside the widest shadow for the RED, then pass 10's own 1,129-line relocation of `:436` re-run for the GREEN, with both pasted and both restored"
      pattern: "expected"
    - from: "prose claiming the construct half NAMES the occurrence"
      to: "prose claiming the construct half names a REGION whose width is measured and pinned"
      via: "the 2026-08-25 bar read for what it actually requires — accurate, uncontradicted disclosure — and pass 10's two executed relocations cited as the falsification"
      pattern: "what names it"

  prohibitions:
    - requirement_id: CORE-11
      category: security
      status: resolved
      verification: gate
      resolution: "THREE EXECUTED `<automated>` CHECKS, NOT ONE PLUS TWO CLAIMS. (a) A structural check over this plan's whole diff shows ZERO added or removed lines matching the compiler-API import form or a compiler-API member access. (b) The import SET, located by content with `grep '^import '`, is asserted byte-identical to `483a8ee` by numeric `diff` after a non-vacuity floor of 4 lines — RED at 2 diff lines against a planted `import { sep } from \"node:path\";`. (c) `auditSource`'s declaration-to-close body, extracted by identifier, is asserted byte-identical the same way after a non-vacuity floor of 100 lines (1179 on the unmodified file). A fourth, named-identifier net catches hand-rolled containment SPELLINGS — 0 on the clean tree, 4 against a planted brace-depth `enclosingFrameOf`, 0 against the reach prose that legitimately names CONTAINMENT in a comment. THAT NET IS NOT A PROOF: a containment helper spelled differently passes it, and the written per-sentence acceptance item is what covers that residue."
      statement: "MUST NOT build an AST, parser, compiler-API or enclosing-frame identity for the anchor. That was verification pass 10's exit 1 and the operator did not take it. The compiler API this file already imports for the outbound walk stays used for the outbound walk and for nothing else in this round."
    - requirement_id: CORE-11
      category: security
      status: resolved
      verification: gate
      resolution: "BODY-SCOPED BYTE-IDENTITY, EXECUTED. For each of `constructAnchorFor`, `constructTokenOf`, `exemptionKeyFor`, `maskQuantifiers`, `normalizeGateLine` and `nameableRemainder`, the declaration-to-close range is extracted BY IDENTIFIER from `git show 483a8ee:<spec>` and from the worktree with the same awk, the extracted range is asserted non-vacuous (>= 4 lines; measured 47 / 7 / 12 / 9 / 5 / 11), and the `diff` line count is asserted 0 — all inside `<automated>`. DEMONSTRATED RED against three planted narrowings the previous token-grep-only check returned 0 for: `:9378` `[A-Za-z_$]` -> `[A-Za-z_]` (4 diff lines), a `continue` guard above `:9390` (2), `:9384` `>= 4` -> `>= 6` (4). The token grep over the diff — which also covers `CONSTRUCT_TOKEN_ELLIPSIS`, `EXEMPTION_ANCHOR_SEP` and both scan-bound expressions — is kept as a cheap second net and is explicitly NOT what asserts."
      statement: "MUST NOT narrow the recognisers, the scan bounds or the key format again. Wave 39 took both bounds to their limit and drove self-anchoring and token ambiguity to zero of twenty-nine each, and the residual did not shrink — it moved to recogniser density over this file's prose, which no key format changes. A fourth narrowing round is the option pass 10 expects to reproduce the signature defect."
    - requirement_id: CORE-11
      category: security
      status: resolved
      verification: gate
      resolution: "`git diff` over `exemptionKeyFor` and over every exemption key asserted EMPTY; the new case shown by diff to group by the token `constructAnchorFor` returns and to write no line number into any key"
      statement: "MUST NOT fold a line number into the exemption key. The census case's own message calls that the tempting one and the worst, and it is right — it would make the case green having measured nothing. A pin over the shadow measures the residual; it does not encode it away."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "Surface lines planted inside the widest shadow and the new case observed RED with its FULL message pasted, restored, tree proved clean; pass 10's `:436` relocation then re-run and observed GREEN, pasted, restored"
      statement: "MUST NOT ship a pin that has never been watched failing, and MUST NOT ship one without also showing what it does not catch. WR-54 and CR-23 both exist because a correction shipped pinned by nothing. A pin whose reach is stated but never bounded by an executed counter-observation is the same unwatched assertion this file has spent eleven waves removing."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "The pinned value is set from a measurement executed AFTER every prose edit in this plan has landed; any movement between the arrival measurement and the final one is attributed line-by-line to added lines shown in the diff, and any unattributable movement is recorded as a finding with the pin left unmoved"
      statement: "MUST NOT move the pin to fit a number it exists to catch. The pin may be re-derived ONLY against growth attributed line-by-line to lines this round added, with the diff shown. Growth that cannot be attributed is a FINDING and the pin stays where it is."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "The enumeration of `site` claims in the exemption-anchoring region is pasted with its grep, each hit given a one-line disposition, and `git diff` asserted EMPTY over `ResolverRecord.site`, over every `site:` value in `RESOLVER_REGISTRY` and over every other occurrence of the word outside the region"
      statement: "MUST NOT sweep the word `site`. It is a registry FIELD NAME in this file and ordinary prose about call sites everywhere else. Only the exemption-anchoring claims are in scope, and the retirement is recorded per claim rather than executed as a search-and-replace."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "`git status --porcelain .planning/` shown EMPTY numerically at every task boundary; `requirements mark-complete` present in no command this plan runs; `CORE11_BOX_EXPECTED` asserted byte-unchanged and still reading the unchecked form"
      statement: "MUST NOT flip CORE-11's checkbox and MUST NOT revert it either. It reads unchecked, the pin agrees, round 9 did not move it and plan 01-41 left `requirements mark-complete` unrun. That is the correct handoff and it is preserved; whether the box may move is verification pass 11's determination."
    - requirement_id: CORE-11
      category: safety
      status: open
      verification: gate
      resolution: "DISCLOSED, NOT CLOSED — stated wherever this plan's result is stated, in the bytes and in the SUMMARY, that the pin bounds the MAXIMUM shadow only, that a non-maximal shadow may grow up to the maximum unreported, that the pin does not prevent a relocation inside a shadow, that it reads only `SURFACE_LINES` and nothing inside the three exclusions, and that the anchor is still a PROXIMITY and not a CONTAINMENT"
      statement: "MUST NOT state or imply that measuring and pinning the shadow makes the anchor identify a site, closes the relocation class, discharges criterion (3), or closes CORE-11. What this plan converts is an OVERCLAIM into a measured, drift-detectable residual. Pass 10 relocated a shipped occurrence 1,129 lines at 439 of 439 green and this plan does not stop that; it makes the width of the region that permits it a number the suite defends."
---

<objective>
Replace the two surfaces that publish CORE-11's interchangeability residual as a 234-line
occurrence-to-anchor distance with the measured width of the anchor's SHADOW, pin that width so the next
widening turns the suite red, and retire the site-identity claim deliberately — dated, reasoned, and
enumerated — because verification pass 10's exit 2 is the route the operator has taken.

Purpose: pass 10 re-measured 234 and confirms it. What it refutes is the inference. Distance from an
occurrence to its anchor is a fact about where 29 shipped sentences happen to sit; the interchangeability
residual is the width of the anchor's shadow — every line that resolves to the same anchor — and measured
with the live builder over all 8,846 surface lines the widest is `:417`'s at 574 surface lines / 1,155
raw, already holding four shipped occurrences. Pass 10 moved one of them 514 lines and then 1,129 lines
for a byte-identical key at 439 of 439 green, with the entry's stated reason false in both new homes.
The published number is roughly five times too small, and it is the tenth recorded instance of this
phase's signature defect — written, for the second consecutive round, inside the paragraph stating the
reach of the fix for the ninth.

Output: a named case that groups every line of `SURFACE_LINES` by resolved anchor and asserts the maximum
against a measured pinned value, non-vacuously, watched RED against planted surface lines and watched
GREEN against pass 10's own relocation so that what it does not catch is executed rather than argued;
limit (5) and the `constructAnchorFor` docblock restating the residual as that measured shadow with 234
correctly re-described; and the clause claiming the construct half NAMES the occurrence replaced by one
saying it names a REGION of measured width, with the retirement dated, its reason stated — the
2026-08-25 bar never required the anchor to identify a site — and every other site claim in the region
enumerated and disposed of in writing.

WHAT THIS PLAN DOES NOT DO. It builds no AST, no parser, no compiler-API identity and no enclosing-frame
containment: that was pass 10's exit 1 and the operator did not take it. It narrows nothing —
`constructAnchorFor`'s recognisers, both scan bounds and the key format are byte-unchanged, and no line
number goes into any key. It does not claim the relocation class is closed, does not claim criterion (3)
is discharged, does not flip or revert CORE-11's checkbox, does not run `requirements mark-complete`,
does not re-open the 2026-08-25 re-scope and opens no file under `.planning/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md
@.planning/phases/01-skeleton-persistence-compatibility/01-39-SUMMARY.md
@.planning/phases/01-skeleton-persistence-compatibility/01-41-SUMMARY.md
@packages/backend/src/outbound-prohibition.spec.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: CR-22 part 2 — the anchor's shadow measured with the live builder over every surface line, pinned as an exact equality, watched RED against planted lines and watched GREEN against pass 10's own relocation</name>

  <precondition>
    Plan 01-41 has executed and committed all of its tasks and `01-41-SUMMARY.md` exists on disk. Before
    the first edit, assert and PASTE: `git rev-parse HEAD`; `git merge-base --is-ancestor 483a8ee HEAD`
    exit 0; and — THE LOAD-BEARING ONE — `git diff --stat 483a8ee..HEAD -- <spec>` EMPTY, proving the
    spec file is byte-identical to the tree pass 10 measured. DO NOT assert `HEAD == 483a8ee`: the
    round-10 plan commits sit on top of it and touch only `.planning/`, so that equality is false by
    construction and asserting it would manufacture a finding. A SPEC-FILE difference IS a finding and is
    recorded before anything else. Then: `git status --porcelain packages/ scripts/ .planning/` clean;
    `pnpm test` exit 0 with its file and test counts; `pnpm exec tsc --build` exit 0; `pnpm check:bundle`'s
    specifier set; the gate file alone with its own count;
    `find packages/*/src -name '*.ts' ! -name '*.spec.ts' | wc -l`. THEN WRITE THE ROUND'S BASELINE FILE,
    which every verify block in plans 01-42 and 01-43 reads:
    `"${TMPDIR:-/tmp}/defminer-round10-baseline"` carrying, as shell assignments, `BASE_GATE_ARRIVAL`,
    `BASE_TESTS_ARRIVAL`, `BASE_FILES`, `BASE_MODULES`, `BASE_SURFACE` and — set to the SAME arrival
    values at this moment and re-written at every later task boundary — `BASE_GATE_CURRENT` and
    `BASE_TESTS_CURRENT`. THE TWO ARRIVAL VALUES ARE WRITTEN ONCE AND NEVER RE-WRITTEN BY ANY TASK IN
    EITHER PLAN; every failure message names which of the two it compared against. Each value comes from
    THIS SESSION's measurement and never from a number quoted in this plan. `BASE_SURFACE` is
    `SURFACE_LINES.length`, read out of the suite by the instrumented case this task builds. HALT if any
    check is red. NO ACTIVE GIT HOOKS: run the gate set by hand at the boundary.
  </precondition>

  <read_first>
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md` — the `gaps:` block's
      `artifacts:` entry beginning `CR-20 NOT CLOSED / CR-22`, the `missing:` entry beginning
      `CR-22 (which is CR-20's real residual, and it must be BOTH parts)`, and the body sections
      `### CR-20 is NOT CLOSED / CR-22 — CONFIRMED. Two relocations, both green.` and
      `## The mechanism judgement`. The `missing:` entry prescribes part 2 in the verifier's own words —
      group `SURFACE_LINES` by resolved anchor and assert the maximum against a pinned value — and the
      body section carries the measured distribution this task reconciles against.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-REVIEW.md`, `### CR-22` in full
      (~line 5621). It carries the four-row shadow table with raw widths, surface widths and the shipped
      occurrences inside each, and the `it(...)` skeleton the fix is expected to take. READ THE TABLE AS
      A RECONCILIATION ANCHOR, NOT AS A RESULT: pass 10's list of surface widths (574, 283, 202, 115, 98)
      and the review's table (283 at anchor 1, 62 at anchor 299, 574 at anchor 417, 55 at anchor 3022)
      do not enumerate the same anchors, and the review's raw-width column for anchor 1 is smaller than
      its own surface column, which cannot be. Re-derive the whole distribution; every disagreement is
      recorded as a finding.
    - `packages/backend/src/outbound-prohibition.spec.ts` — `constructAnchorFor` (declaration `:9373`,
      docblock `:9313-9372`, body through `:9420`), `constructTokenOf` `:9286`, `anchorTokenCensus`
      `:9300`, `quantifierOccurrences` `:9491`, `surfaceExemptionKeys` `:9517`, `EXCLUSIONS`
      `:9980-10016`, `SURFACE_LINES` `:10026-10028`, and the non-vacuity case at `:10034-10062` whose
      shape the new case's own non-vacuity assertions follow. RE-DERIVE EVERY ONE BY IDENTIFIER; the
      numbers are anchors measured at `483a8ee`.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the region CR-22 is about, by content: `:417`
      the header row, `:418` its rule, `:436` the wrapped cell whose entire normalized content is a
      declared phrasing, `:952` and `:1565` the machine-owned span's sentinels, `:1567` the imports.
      Confirm each by text. The 1,155 raw / 574 surface gap is exclusion one removing the machine-owned
      span, and the task must state that relationship rather than leave it to be inferred.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the existing pinned-residual cases this one
      is written in the shape of: the exclusion band-and-proof case at `:10034`, the width pin at
      `:10079`, and the census at `:10392`. Read how each states its own reach and how each asserts
      non-vacuity BEFORE its rule.
    - `01-39-SUMMARY.md`'s `### REAL-TREE RELOCATION` sections and `01-41-SUMMARY.md`'s mutation record,
      for the commit-plant-observe-restore shape both of this task's mutations must take.
  </read_first>

  <files>packages/backend/src/outbound-prohibition.spec.ts</files>

  <reversibility rating="reversible">One new case, one pinned integer and one reach paragraph added to an existing spec file. No shipped code, no behaviour, no exclusion boundary, no exemption key, no ledger byte. Both planted mutations are restored inside the task with `git diff --exit-code` shown clean.</reversibility>

  <behavior>
    - Grouping every line of `SURFACE_LINES` by the anchor `constructAnchorFor` resolves for it produces
      a map whose largest group is the interchangeability residual, and that maximum is asserted equal to
      a value measured in this session.
    - The case reads EVERY element of `SURFACE_LINES`. It does not sample, does not early-exit and does
      not stop once it has found a group larger than the pin.
    - With surface lines planted inside the widest shadow, the case FAILS, and its message shows the new
      maximum against the pinned one.
    - With the shadow map empty, with fewer than two distinct anchors, or with `SURFACE_LINES` collapsed,
      the case FAILS before its rule rather than passing having grouped nothing.
    - The failure message names the anchor token that owns the maximum, computed at runtime, so a
      DIFFERENT anchor arriving at the same width is visible in the output rather than silently accepted.
    - Verification pass 10's relocation of the occurrence at `:436` across the shadow leaves the whole
      suite GREEN, including this case — the residual's size is pinned, the relocation is not prevented.
  </behavior>

  <action>
    MEASURE FIRST, WITH THE SHIPPED BUILDER, AND PASTE THE WHOLE DISTRIBUTION. Insert a TEMPORARY case
    inside the gate's own `describe` — so it reads the shipped `constructAnchorFor`, the shipped
    `constructTokenOf` and the shipped `SURFACE_LINES` rather than a lifted copy, which is the method
    verification pass 10 used and the reason its numbers are trustworthy. Group every element of
    `SURFACE_LINES` by the anchor resolved for it and throw the result out through an assertion message:
    for each of the ten largest groups, the anchor token, the group size in SURFACE lines, the raw first
    and last line it spans, and the count of shipped occurrences inside it as `quantifierOccurrences`
    reports them. Also throw out `SURFACE_LINES.length` and the number of distinct anchors. Paste the
    raw output. Reconcile against pass 10's 574 / 283 / 202 / 115 / 98 and against the review's
    four-row table, and RECORD EVERY DISAGREEMENT AS A FINDING before writing a single byte of the
    permanent case. Remove the temporary case; prove it gone by diff. Write `BASE_SURFACE` into the
    round's baseline file from this measurement.

    GROUP BY THE TOKEN, NOT BY THE PRODUCER LINE, AND SAY WHY IN ONE SENTENCE. `constructAnchorFor`
    returns a TOKEN and it is the token that goes into the key, so two lines that resolve to the same
    token are interchangeable whether or not the same line produced it. Then MEASURE THE OTHER GROUPING
    TOO — group by the producer line instead — and state in ONE LINE whether the maximum differs. If it
    does, that difference is exactly the census case's business and it is recorded as a finding rather
    than absorbed; if it does not, say so with the number.

    AUTHOR THE PERMANENT CASE. Title it so a reader learns what it bounds without opening it: the widest
    ANCHOR SHADOW over the scanned surface, pinned, with the reason that an anchor's shadow IS the
    interchangeability residual. NON-VACUITY BEFORE THE RULE, in this file's established shape and with
    a failure message for each: `SURFACE_LINES.length` asserted above a floor; the shadow map asserted
    non-empty; the number of DISTINCT anchors asserted greater than one, because a single anchor over the
    whole surface would make the maximum trivially the surface size and the case would pass having
    measured a collapse; and the maximum asserted greater than zero. THEN THE RULE: the maximum group
    size asserted EQUAL — an exact equality, not an upper bound — to the value measured above.

    STATE WHY THE EQUALITY IS EXACT, IN ONE SENTENCE IN THE BYTES. An upper bound hides a shadow that
    SHRANK, and a shadow that shrank is information about the file's recogniser geometry that this gate
    should not discard silently. An exact equality makes both directions loud.

    THE FAILURE MESSAGE MUST CARRY ITS OWN ARITHMETIC AND ITS OWN INSTRUCTION. It names, computed at
    runtime: the measured maximum, the pinned maximum, the anchor token that owns the measured maximum,
    and the raw span it covers. It states that a shadow which grew is a residual which grew — every line
    inside it is interchangeable with every other for a byte-identical key — and that the correct
    responses are to re-site or rewrite whatever widened it, or to re-derive the pin ONLY against growth
    attributed line-by-line to lines the same commit added. It states explicitly that moving the pin to
    fit an unattributed number is decoration that reports green, which is the defect this whole file is
    organised against.

    STATE THE CASE'S REACH IN ITS OWN BYTES, AND STATE IT NARROWLY. It bounds the MAXIMUM shadow over
    `SURFACE_LINES` under the grouping named above, and nothing wider. IT DOES NOT bound a NON-MAXIMAL
    shadow, which may grow up to the maximum without this case reporting — say that in the bytes, with
    the second-largest measured width as the concrete illustration. IT DOES NOT reach any line inside the
    three exclusions. IT DOES NOT prevent a relocation inside a shadow. IT DOES NOT make the anchor a
    containment; the anchor is still the nearest preceding line five recognisers accept. And say why the
    whole distribution is NOT pinned as well: a pin that must be moved on every ordinary prose edit
    inside any of the top shadows is a pin nobody will trust, so the maximum is pinned and the
    distribution is recorded in the SUMMARY as evidence instead. That sentence is a disclosure, not an
    excuse, and it is written as one.

    NO SAMPLING, NO EARLY EXIT, AND MEASURE THE COST. The case reads every element of `SURFACE_LINES`.
    If the runtime is uncomfortable, MEMOIZE the resolution — the same measurement, cached — and never
    sample, never truncate and never break out of the loop when a group exceeds the pin. Paste the
    measured wall-clock time for the case and state it in one line.

    COMMIT. Paste `git log --oneline -1`. THEN RE-WRITE `BASE_GATE_CURRENT` AND `BASE_TESTS_CURRENT` IN
    THE BASELINE FILE to the counts as of this commit, and paste the file. LEAVE `BASE_GATE_ARRIVAL` AND
    `BASE_TESTS_ARRIVAL` EXACTLY AS TASK 1's PRECONDITION WROTE THEM — they are the round's arrival
    values and no task re-writes them. Paste all four side by side so it is visible which moved.

    MUTATION ONE — WATCH THE PIN FAIL. Plant a small number of SURFACE lines inside the widest shadow and
    OUTSIDE the machine-owned span, so the growth is real and attributable — the contiguous comment block
    between the rule at `:418` and the span's opening sentinel is the natural home, and lines inserted
    inside a contiguous line-comment block are not accepted by the fifth recogniser, so the shadow GROWS
    by exactly the number planted rather than splitting. PREDICT the new maximum before running. Run,
    observe RED, paste the failing title and the FULL assertion message, and confirm the arithmetic
    matches the prediction; a mismatch is a first-order finding and is written up rather than smoothed
    over. Restore; prove clean with `git diff --exit-code`. If it does NOT go red, STOP, diagnose, and
    write it up without adjusting the mutation to force a red.

    MUTATION TWO — WATCH WHAT THE PIN DOES NOT CATCH STAY GREEN. Re-run verification pass 10's own
    relocation: move the shipped occurrence at `:436` — the wrapped ASCII-table cell whose entire
    normalized content is a declared phrasing — out of the table and across the machine-owned span to the
    top level beside the imports, roughly 1,129 lines, leaving its exemption entry untouched. Run the
    gate suite. The expectation is GREEN, including the new pin. Paste the result and the resolved key,
    shown byte-identical. Restore; prove clean. IF IT GOES RED, that is a first-order finding and a
    genuinely better outcome than expected — write it up in full and do not describe the pin as catching
    relocations until the mechanism by which it did is understood and stated.

    WRITE MUTATION TWO'S GREEN INTO THE BYTES. Beside the new case, in one short paragraph, record that
    this exact relocation was re-run in this session and stayed green, and that the pin therefore makes
    the residual's SIZE measured and drift-detectable and does not prevent a relocation inside the
    shadow. Cite `01-VERIFICATION.md` for pass 10's two relocations at 439 of 439 green. THIS PARAGRAPH
    IS THE ROUND'S HIGHEST-RISK SENTENCE FOR AN ELEVENTH OVERCLAIM AND IT IS THE ONE THAT CONVERTS THE
    OVERCLAIM INTO A RESIDUAL — write it so a reader who reads only it cannot come away believing the
    relocation class is closed.

    TOUCH NOTHING OUTSIDE SCOPE. `constructAnchorFor`'s docblock and body, `constructTokenOf`,
    `exemptionKeyFor`, `preAnchoringExemptionKeyForFixtureOnly`, `maskQuantifiers`, `normalizeGateLine`,
    `nameableRemainder`, `anchorTokenCensus`, `quantifierOccurrences`, `surfaceExemptionKeys`,
    `CONSTRUCT_TOKEN_ELLIPSIS`, `EXEMPTION_ANCHOR_SEP`, `NO_PRECEDING_CONSTRUCT`, every key and value of
    `HEADER_QUANTIFIER_EXEMPTIONS`, all three `EXCLUSIONS`, every band, every `proof` token, the two 234
    sentences (Task 2's), `CORE11_BOX_EXPECTED` and the box case all stay byte-unchanged. Nothing under
    `.planning/` is opened. If new prose trips the whole-file declared-phrasing guard, REWRITE THE PROSE
    — do not add an exemption entry for it.

    RE-READ EVERY SENTENCE THIS TASK WRITES AGAINST THE NO-OVERCLAIM PROHIBITION AND RECORD A VERDICT PER
    SENTENCE, with the reach it claims and the reach it executes stated side by side.

    THE VERIFY BLOCK GATES TASK 2's TWO SENTENCES BY THEIR TEXT AND NOT BY THE BARE NUMBER, deliberately:
    a numeric gate on the digits alone would fire on any measured figure that happens to contain them,
    which is a gate that is green or red for the wrong reason. THE FRAGMENTS ARE SINGLE-LINE AND THAT IS
    NOT COSMETIC. The first draft of this plan gated on a fragment split across `:9090` and `:9091` by
    the line wrap, which therefore matched NO line of the file — half the guard could never fire, and an
    edit to `:9091` alone went undetected. The three fragments now used were each re-measured to match
    EXACTLY ONE line of the unmodified file, one per surface plus the shared inference clause, and each
    was watched firing against a planted one-word edit to the line it covers. BEFORE RELYING ON THE
    GUARD, RE-MEASURE ALL THREE COUNTS AND PASTE THEM; a fragment that returns 0 is a guard that cannot
    fire and is a finding, not a pass.

    THE ANTI-NARROWING AND ANTI-CONTAINMENT CHECKS ARE BODY-SCOPED DIFFS, NOT TOKEN GREPS, AND THE
    EXECUTOR PASTES THEIR NON-VACUITY LINE COUNTS. The verify block extracts each protected helper's
    declaration-to-close range by IDENTIFIER from `git show 483a8ee:<spec>` and from the worktree, asserts
    the extracted range is non-vacuous, and asserts the `diff` line count is 0. Paste the eight
    range/diff pairs. A range that comes back shorter than its recorded length means the extraction lost
    its terminator — that is a finding about the CHECK and it is written up before the task proceeds,
    because a check whose range collapsed passes vacuously.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && SP=packages/backend/src/outbound-prohibition.spec.ts; TD="${TMPDIR:-/tmp}"; B="$TD/defminer-round10-baseline"; [ -f "$B" ] || { echo "MISSING BASELINE FILE"; exit 1; }; . "$B"; GT=$(pnpm exec vitest run "$SP" 2>&1 | tee /dev/stderr | sed -n 's/.*Tests .*(\([0-9][0-9]*\))[[:space:]]*$/\1/p' | tail -1); [ -n "$GT" ] && [ "$GT" -gt "$BASE_GATE_ARRIVAL" ] || { echo "GATE COUNT $GT NOT ABOVE ARRIVAL $BASE_GATE_ARRIVAL"; exit 1; }; FT=$(pnpm test 2>&1 | tee /dev/stderr | sed -n 's/.*Tests .*(\([0-9][0-9]*\))[[:space:]]*$/\1/p' | tail -1); [ -n "$FT" ] && [ "$FT" -gt "$BASE_TESTS_ARRIVAL" ] || { echo "SUITE COUNT $FT NOT ABOVE ARRIVAL $BASE_TESTS_ARRIVAL"; exit 1; }; pnpm typecheck && pnpm lint && pnpm knip && pnpm build:backend && pnpm check:bundle && RB=$(git rev-parse 483a8ee) && D=$(git diff "$RB"..HEAD -- "$SP"); AST=$(printf '%s\n' "$D" | grep -cE '^[+-].*(from "typescript"|[^A-Za-z]ts\.[A-Za-z])' || true); [ "$AST" -eq 0 ] || { echo "COMPILER-API LINES IN DIFF: $AST"; exit 1; }; HAND=$(printf '%s\n' "$D" | grep '^+' | grep -vE '^\+[[:space:]]*(//|\*|/\*)' | grep -cE '(braceDepth|braceLevel|indentOf|indentLevel|enclosing[A-Z]|Enclosing|containmentOf|frameOf|depth\+\+|depth--)' || true); [ "$HAND" -eq 0 ] || { echo "HAND-ROLLED CONTAINMENT SPELLING IN DIFF: $HAND"; exit 1; }; NAR=$(printf '%s\n' "$D" | grep -cE '^[+-].*(DECLARATION =|FIXTURE_TITLE =|DOCBLOCK_OPEN =|isLineComment =|isRule =|CONSTRUCT_TOKEN_ELLIPSIS =|EXEMPTION_ANCHOR_SEP =|lineNumber - 1|k < lineNumber)' || true); [ "$NAR" -eq 0 ] || { echo "NARROWING TOKENS IN DIFF (SECOND NET): $NAR"; exit 1; }; XA='BEGIN{s=0} !s && $0 ~ "^const " n "[ =(]" {s=1; print; next} s && /^(\};|\/\*\*|const |export |type |interface |describe|it\()/ { if ($0 ~ /^\};/) print; exit } s {print}'; for n in constructAnchorFor constructTokenOf exemptionKeyFor maskQuantifiers normalizeGateLine nameableRemainder; do git show "$RB":"$SP" | awk -v n="$n" "$XA" > "$TD/r10.base.$n"; awk -v n="$n" "$XA" "$SP" > "$TD/r10.work.$n"; BL=$(grep -c . "$TD/r10.work.$n" || true); [ "$BL" -ge 4 ] || { echo "BODY RANGE FOR $n VACUOUS: $BL LINES — THE CHECK LOST ITS TERMINATOR"; exit 1; }; BD=$(diff "$TD/r10.base.$n" "$TD/r10.work.$n" | grep -c . || true); [ "$BD" -eq 0 ] || { echo "BODY OF $n CHANGED: $BD DIFF LINES"; exit 1; }; echo "BODY-BYTE-IDENTICAL $n lines=$BL"; done; AS='BEGIN{s=0} !s && /^export function auditSource\(/ {s=1; print; next} s && /^\}$/ {print; exit} s {print}'; git show "$RB":"$SP" | awk "$AS" > "$TD/r10.base.auditSource"; awk "$AS" "$SP" > "$TD/r10.work.auditSource"; AL=$(grep -c . "$TD/r10.work.auditSource" || true); [ "$AL" -ge 100 ] || { echo "auditSource RANGE VACUOUS: $AL LINES"; exit 1; }; AD=$(diff "$TD/r10.base.auditSource" "$TD/r10.work.auditSource" | grep -c . || true); [ "$AD" -eq 0 ] || { echo "auditSource CHANGED: $AD DIFF LINES"; exit 1; }; IMPN=$(grep -c '^import ' "$SP" || true); [ "$IMPN" -ge 4 ] || { echo "IMPORT BLOCK VACUOUS: $IMPN LINES"; exit 1; }; git show "$RB":"$SP" | grep '^import ' > "$TD/r10.base.imports"; grep '^import ' "$SP" > "$TD/r10.work.imports"; IMPD=$(diff "$TD/r10.base.imports" "$TD/r10.work.imports" | grep -c . || true); [ "$IMPD" -eq 0 ] || { echo "IMPORT SET CHANGED: $IMPD DIFF LINES"; exit 1; }; echo "STRUCTURAL-OK auditSource=$AL imports=$IMPN"; T234=$(printf '%s\n' "$D" | grep -cE '^[+-].*(interchangeability residual is that wide|is 234 LINES|got 234 again)' || true); [ "$T234" -eq 0 ] || { echo "TASK 2 SENTENCES TOUCHED: $T234"; exit 1; }; F1=$(grep -c 'is 234 LINES' "$SP" || true); F2=$(grep -c 'interchangeability residual is that wide' "$SP" || true); F3=$(grep -c 'got 234 again' "$SP" || true); { [ "$F1" -eq 1 ] && [ "$F2" -eq 1 ] && [ "$F3" -eq 1 ]; } || { echo "T234 GUARD FRAGMENTS NOT ALL AT EXACTLY ONE LINE: $F1/$F2/$F3 — A FRAGMENT AT 0 CANNOT FIRE"; exit 1; }; echo "T234-GUARD-LIVE $F1/$F2/$F3"; N=$(git status --porcelain .planning/ | grep -c . || true); [ "$N" -eq 0 ] || { echo "PLANNING DIRTY: $N"; exit 1; }; A=$(git diff --name-only "$RB"..HEAD -- packages/ scripts/ | grep -v '\.spec\.ts$' | grep -c . || true); [ "$A" -eq 0 ] || { echo "SHIPPED SOURCE TOUCHED: $A"; exit 1; }; M=$(find packages/*/src -name '*.ts' ! -name '*.spec.ts' | grep -c . || true); [ "$M" -eq "$BASE_MODULES" ] || { echo "MODULE COUNT $M != $BASE_MODULES"; exit 1; }; git diff --exit-code -- "$SP" && echo TREE-CLEAN</automated>
  </verify>

  <acceptance_criteria>
    - `git rev-parse HEAD` pasted; `git merge-base --is-ancestor 483a8ee HEAD` shown exit 0; and
      `git diff --stat 483a8ee..HEAD -- <spec>` shown EMPTY, proving the spec file byte-identical to the
      tree pass 10 measured. HEAD is NOT asserted equal to `483a8ee` — the round-10 plan commits sit on
      top of it — and the criterion states which of the two facts is load-bearing. Any spec-file
      difference is recorded as a finding with its cause named before any edit.
      `git status --porcelain packages/ scripts/ .planning/` pasted clean. `01-41-SUMMARY.md` confirmed
      present on disk.
    - The round's baseline file written and its contents pasted, with every value shown to come from a
      command executed in this session — `BASE_GATE_ARRIVAL`, `BASE_TESTS_ARRIVAL`, `BASE_GATE_CURRENT`,
      `BASE_TESTS_CURRENT`, `BASE_FILES`, `BASE_MODULES`, `BASE_SURFACE` — and none copied from a figure
      quoted in this plan. An explicit statement that the two `_ARRIVAL` values are written ONCE here and
      will not be re-written by any later task in either plan, and that the two `_CURRENT` values are the
      ones later boundaries update.
    - THE FILE'S LINE COUNT RE-DERIVED and pasted from `wc -l` and `awk 'END{print NR}'`, reconciled
      against `01-VERIFICATION.md`'s 11,079, `01-REVIEW.md`'s 11,072 and this plan's measured 11,071,
      with an explicit statement of which surfaces disagree and which number is being carried forward.
    - The TEMPORARY instrumented case's raw output pasted in full: the ten largest shadows with anchor
      token, surface size, raw span and shipped-occurrence count each, plus `SURFACE_LINES.length` and
      the number of distinct anchors — measured with the SHIPPED builder from inside the gate's own
      `describe`, shown by diff to be inside it and shown by diff to have been removed afterwards.
    - The distribution reconciled against pass 10's 574 / 283 / 202 / 115 / 98 and the review's four-row
      table, with EVERY disagreement recorded as a finding, including the review's raw-width column for
      the anchor at line 1 being smaller than its own surface column.
    - The 1,155-raw / 574-surface relationship explained in one line by exclusion one removing the
      machine-owned span, with the span's two sentinel lines located by text and pasted.
    - The grouping choice stated in one sentence, and the ALTERNATIVE grouping by producer line measured
      and its maximum pasted, with an explicit statement of whether it differs.
    - The permanent case present, shown by diff to: read every element of `SURFACE_LINES` with no
      sampling, no early exit and no break; assert non-vacuity BEFORE its rule on the surface size, the
      map, the distinct-anchor count and the maximum, each with its own message; and assert the maximum
      by exact equality against the measured value.
    - The one-sentence reason for the exact equality over an upper bound quoted verbatim.
    - The failure message quoted verbatim and shown to name, computed at runtime, the measured maximum,
      the pinned maximum, the owning anchor token and its raw span, plus the instruction that the pin may
      be re-derived only against attributed growth.
    - The case's own reach paragraph quoted verbatim and shown to state all five limits: the maximum only;
      a non-maximal shadow may grow up to it unreported, illustrated with the measured second-largest
      width; nothing inside the three exclusions; no prevention of relocation inside a shadow; and the
      anchor still a proximity rather than a containment — plus the stated reason the distribution is not
      pinned as well.
    - The case's measured wall-clock runtime pasted, with a statement of whether memoization was used and
      an explicit statement that no sampling or early exit was introduced.
    - `git log --oneline -1` pasted BEFORE mutation one.
    - MUTATION ONE: the planted lines shown by diff, the predicted new maximum stated BEFORE the run, the
      suite observed RED, the failing title and FULL assertion message pasted, the arithmetic confirmed
      against the prediction (a mismatch recorded as a first-order finding), restored, and
      `git diff --exit-code` shown clean.
    - MUTATION TWO: pass 10's `:436` relocation re-run, the destination shown, the suite observed GREEN
      with its count pasted, the resolved key shown byte-identical, restored, and `git diff --exit-code`
      shown clean. If it went RED instead, that is written up in full as a first-order finding and the
      pin is NOT described as catching relocations.
    - The paragraph recording mutation two's green quoted verbatim, with an explicit statement that a
      reader of that paragraph alone cannot conclude the relocation class is closed.
    - `git diff` shown EMPTY over `constructAnchorFor`'s docblock and body, `constructTokenOf`,
      `exemptionKeyFor`, `preAnchoringExemptionKeyForFixtureOnly`, `maskQuantifiers`, `normalizeGateLine`,
      `nameableRemainder`, `anchorTokenCensus`, `quantifierOccurrences`, `surfaceExemptionKeys`,
      `CONSTRUCT_TOKEN_ELLIPSIS`, `EXEMPTION_ANCHOR_SEP`, `NO_PRECEDING_CONSTRUCT`, every exemption key
      and value, all three `EXCLUSIONS`, every band, every `proof` token, `CORE11_BOX_EXPECTED` and the
      box case.
    - THE BODY-SCOPED BYTE-IDENTITY CHECKS PASTED WITH THEIR NON-VACUITY LINE COUNTS, all eight pairs:
      `constructAnchorFor`, `constructTokenOf`, `exemptionKeyFor`, `maskQuantifiers`, `normalizeGateLine`,
      `nameableRemainder`, `auditSource` and the import set. Each shown at 0 diff lines AND at a
      non-vacuous range — reconciled against this plan's recorded 47 / 7 / 12 / 9 / 5 / 11 / 1179 / 4. A
      range shorter than its recorded length is a finding ABOUT THE CHECK and is written up before the
      task is called done, because a collapsed range passes vacuously.
    - The compiler-API check, the hand-rolled-containment net and the narrowing TOKEN net all shown
      returning zero with their output pasted — AND a per-sentence written statement, for every sentence
      this task adds, that NO CONTAINMENT WAS COMPUTED BY ANY MEANS, AST OR HAND-ROLLED: no compiler API,
      no parser, no brace-depth walk, no indentation walk and no enclosing-frame identity by any other
      spelling. The plan records that the identifier net matches SPELLINGS and not shapes; this written
      statement is what covers the residue and the executor states that it is doing so.
    - THE THREE `T234` GUARD FRAGMENTS RE-MEASURED AND PASTED at `1/1/1` on the file as this task leaves
      it, with an explicit statement that a fragment returning 0 would be a guard that cannot fire. The
      previous draft's split fragment (`maximum distance from an occurrence to its anchor`, which matches
      no line of this file) is named as the defect this replaces.
    - `git status --porcelain .planning/` asserted EMPTY numerically; `requirements mark-complete` absent
      from every command run.
    - Executed `pnpm test` exit 0 with counts pasted and asserted ABOVE the captured baseline;
      `typecheck`, `lint`, `knip` exit 0; `build:backend && check:bundle` exactly one specifier, `crypto`;
      the real-tree walk asserted at the captured module count with zero violations; CORE-11's first
      sentence pasted and shown byte-identical.
    - A one-line no-overclaim verdict recorded for EVERY sentence added, each stating the reach claimed
      and the reach executed side by side.
    - Any prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses, or an
      explicit statement that there were none.
  </acceptance_criteria>

  <done>
    Every line of `SURFACE_LINES` is grouped by the anchor the shipped builder resolves for it, the widest
    group is asserted equal to a value measured in this session, and the case is non-vacuous, exhaustive
    and watched failing against planted lines — while verification pass 10's own 1,129-line relocation is
    re-run, observed green, and recorded in the bytes as the measured statement of what the pin does not
    do.
  </done>
</task>

<task type="auto">
  <name>Task 2: CR-22 part 1 — both surfaces restate the residual as the measured shadow, and the site-identity claim is retired deliberately, dated, and against an enumeration</name>

  <precondition>
    Task 1 has committed, its new case is present and green, and the round's baseline file exists at
    `"${TMPDIR:-/tmp}/defminer-round10-baseline"` with `BASE_GATE_CURRENT` and `BASE_TESTS_CURRENT`
    re-written by Task 1 to the counts INCLUDING Task 1's case, and with `BASE_GATE_ARRIVAL` and
    `BASE_TESTS_ARRIVAL` still holding the round's arrival values UNCHANGED. Assert and PASTE all four
    and state which is which; if `BASE_GATE_ARRIVAL` moved, that is a first-order finding and the task
    HALTS, because an arrival value that drifts makes every `-gt ARRIVAL` assertion in both plans
    meaningless. Also assert and PASTE `git rev-parse HEAD`, a clean
    `git status --porcelain packages/ scripts/ .planning/`, and the gate suite green at Task 1's recorded
    count. Re-read the pinned maximum from the file and paste it; this task adds prose and the pin is the
    mechanism that will report if that prose moved the residual. HALT if anything is red.
  </precondition>

  <read_first>
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md` — the `missing:` entry
      for CR-22, part (1) specifically, and the `## The mechanism judgement` section's exit 2 in full.
      Exit 2 is the operator's decision and its wording is the reason this task's retirement paragraph
      gives: nothing in DERIVED / DRIFT-DETECTABLE / SOLE BOUND requires the anchor to identify a site;
      SOLE BOUND requires the disclosure to be accurate and contradicted nowhere.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-REVIEW.md`, `### CR-22`, the **Fix**
      subsection's part 1. It supplies a candidate replacement paragraph. USE IT AS A SHAPE, NOT AS TEXT:
      its figures (574, 1,155, four occurrences, 8,846) must be replaced by Task 1's measured values, and
      a figure copied rather than measured is exactly the defect this round corrects.
    - `packages/backend/src/outbound-prohibition.spec.ts` — limit (5)'s closing paragraph at `:9084-9093`
      in the docblock ending `:9099`, with the sentence to replace at `:9090-9093`; and the
      `constructAnchorFor` docblock's paragraph at `:9339-9347` with the sentence to replace OPENING
      mid-`:9342` and closing on `:9345`. THE TWO SENTENCES DO NOT SHARE TEXT — re-measured this session,
      `grep -c 'interchangeability residual is that wide'` = 1 (`:9093`, surface A only) and
      `grep -c 'got 234 again'` = 1 (`:9345`, surface B only). Read the two paragraphs they sit in, in
      full, INCLUDING surface B's heading at `:9339-9341`, so each replacement carries its own
      surrounding argument rather than being spliced in from the other. `grep -n 234` returns exactly
      three lines at `483a8ee`: `:9091`, `:9343`, `:9345`.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the null-anchor case at `:10277-10285` and
      the clause at `:10282` reading `If the occurrence's own line genuinely normalizes to a bare
      declared phrasing, that is fine — the construct half is what names it`. This is the positive
      site-identity claim pass 10 falsified, and its own message is where the retirement goes.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the four NEGATIVE site statements that are
      expected to survive, as a floor for the enumeration and not as the enumeration: `:9416`, `:10337`,
      `:10339`, `:10728`. Read each so its disposition is written from the bytes.
    - `packages/backend/src/outbound-prohibition.spec.ts` — `ResolverRecord.site` at `:4134` and its
      docblock at `:4133`, plus the `site:` values throughout `RESOLVER_REGISTRY` from `:4161`. These are
      a REGISTRY FIELD NAME, they are out of scope, and reading them is how the executor knows the
      enumeration must be region-bounded rather than a word grep.
    - `01-41-SUMMARY.md`'s per-sentence no-overclaim verdict table, for the shape this task's verdict
      table takes.
  </read_first>

  <files>packages/backend/src/outbound-prohibition.spec.ts</files>

  <reversibility rating="costly">The two disclosure paragraphs are ordinary prose edits, but the retirement of the site-identity claim makes the operator's exit-2 decision durable in the file's own bytes. Reversing it means re-opening the mechanism question pass 10 adjudicated, so it is flagged rather than gated: the decision has already been taken and this task records it, it does not make it.</reversibility>

  <action>
    THE TWO SURFACES ARE TWO DIFFERENT PARAGRAPHS. QUOTE EACH BEFORE YOU TOUCH IT, AND DO NOT ASSUME
    THEY SHARE TEXT. The first draft of this plan asserted both closed on the same words; re-measurement
    refuted it — `grep -c 'interchangeability residual is that wide'` returns 1 for the whole file, at
    `:9093` only. RECORD THAT MISQUOTE AS A ROUND-10 FINDING in the SUMMARY, in its own row, with the
    grep pasted: a plan whose subject is the accuracy of quoted claims misquoted the file it was
    correcting, and that is exactly the phase's signature defect one level up in the toolchain. THE TWO
    SURFACES, EACH TO BE RE-READ AND PASTED FROM THE BYTES BEFORE EDITING:
      SURFACE A — LIMIT (5), `:9090-9093`. It closes: `RE-MEASURED AT WAVE 39 AFTER BOTH CHANGES, the
      maximum distance from an / occurrence to its anchor is 234 LINES — the occurrence on line 235
      takes / this file's own title line — so `the same construct` can span a couple / of hundred lines
      and the interchangeability residual is that wide.`
      SURFACE B — THE `constructAnchorFor` DOCBLOCK, `:9342-9345`. It reads: `region, and in this file
      those regions are large: WR-49 measured four / occurrences taking the file's own title line as
      their anchor from up to 234 / lines above them, and wave 39 re-measured the maximum after
      correcting the / scan bounds and got 234 again, at the occurrence on line 235.`

    WHY SURFACE B IS IN SCOPE, WRITTEN OUT SEPARATELY BECAUSE IT IS NOT SURFACE A REPEATED. Surface B
    sits under the heading at `:9339-9341`, which claims the difference between proximity and the finest
    enclosing construct is `MEASURED RATHER THAN ESTIMATED`, and then offers 234 as that measurement.
    That is the SAME wrong-quantity inference in different words: the proximity-versus-containment gap is
    the WIDTH OF THE REGION a single anchor covers, not the distance a shipped sentence happens to sit
    from its anchor. State this reason in the SUMMARY as surface B's own scope justification; do not
    carry surface A's reason across to it.

    REPLACE THE INFERENCE AT BOTH SURFACES, WITH TASK 1's MEASURED QUANTITY AND NOT WITH A COPIED ONE.
    At `:9090-9093` and at `:9342-9345`, delete the inference — that the maximum occurrence-to-anchor
    distance bounds the interchangeability residual, and that 234 measures the proximity-versus-
    containment gap — and state instead: that the residual is the
    ANCHOR'S SHADOW, every line resolving to the same anchor, and that it is NOT the occurrence-to-anchor
    distance and is much larger than it; the MEASURED widest shadow with the anchor that owns it named,
    its surface width, its raw span and the number of shipped occurrences already inside it; that an
    occurrence inside a shadow can be moved anywhere else inside it for a byte-identical key, with pass
    10's two relocations (514 lines and 1,129 lines, both at 439 of 439 green, the entry's stated reason
    false in both new homes) cited as the executed evidence; and that 234 is the largest distance any
    shipped sentence HAPPENS to sit from its anchor and bounds nothing. KEEP the 234 — it is correct and
    it was re-measured — and correct only what it is said to mean.

    EACH REPLACEMENT NAMES THE MECHANISM THAT NOW WATCHES IT. Both paragraphs point at Task 1's case by
    its title and state its reach and no wider, including that it pins the MAXIMUM only and that a
    non-maximal shadow may grow up to the maximum unreported. A disclosure that names a pin without
    naming the pin's limits is how limit (5) arrived at this sentence in the first place.

    THE TWO REPLACEMENTS MUST NOT DRIFT FROM EACH OTHER. They are two surfaces stating one fact, and
    WR-60 is in this same round because two surfaces stated one measurement at two values. Write the
    number ONCE with its measurement basis and have the second surface state the same figure with the
    same basis and a pointer to the first, then paste both and show them agreeing. Any difference is a
    finding.

    RETIRE THE SITE-IDENTITY CLAIM, DELIBERATELY AND IN THE BYTES. At `:10282`, the clause
    `that is fine — the construct half is what names it` is the positive claim pass 10 falsified by two
    executed relocations. Replace it with a statement that the construct half names a REGION — the
    anchor's shadow — whose width is measured and pinned by Task 1's case, and that it does NOT identify
    the occurrence's site. Beside it, in one marked and DATED bracket, record the retirement itself: that
    the site-identity claim is WITHDRAWN as of wave 42, that the reason is that the 2026-08-25 re-scoped
    bar never required it — DERIVED, DRIFT-DETECTABLE and SOLE BOUND ask for a disclosure that is
    accurate and contradicted nowhere, not for an anchor that identifies a site — and that the claim was
    prose this file volunteered. Cite `01-VERIFICATION.md`'s `## The mechanism judgement` by name. A
    silent deletion is forbidden: the bytes must say the claim was withdrawn and why.

    ENUMERATE BEFORE YOU DISPOSE. Before editing `:10282`, produce a BOUNDED census of every claim about
    an anchor identifying a site in the exemption-anchoring region — bounded by identifier, from
    `HEADER_QUANTIFIER_EXEMPTIONS` at `:9100` to the end of the anchoring cases — and paste it with its
    grep. Reconcile against the floor `:9416`, `:10282`, `:10337`, `:10339`, `:10728`; an unlisted hit is
    a finding. GIVE EVERY HIT A ONE-LINE DISPOSITION. The four negative statements — an anchor that
    named no site, a self-anchoring key that names no site — are expected to SURVIVE because they are
    true statements about a defect shape, and each survives with its reason written down rather than by
    not being noticed.

    THIS IS NOT A WORD SWEEP AND THE ENUMERATION IS BOUNDED FOR THAT REASON. `ResolverRecord.site` is a
    registry FIELD NAME, every `site:` value in `RESOLVER_REGISTRY` is data, and the word appears
    throughout this file as ordinary prose about call sites. All of it is out of scope and all of it is
    asserted byte-unchanged by diff.

    RE-RUN THE PIN AND ATTRIBUTE ANY MOVEMENT. This task adds prose to the file, and Task 1's case is
    exactly the mechanism that reports if that prose widened a shadow. Run the gate suite after the
    edits. If the pinned maximum moved, ATTRIBUTE IT LINE-BY-LINE to lines this task added, show the
    diff, and re-derive the pin with the attribution recorded in the SUMMARY. If any part of the movement
    cannot be attributed to this round's own added lines, the pin STAYS WHERE IT IS and the movement is
    written up as a first-order finding.

    TOUCH NOTHING OUTSIDE SCOPE. Task 1's case body and its pinned value stay byte-unchanged except for a
    re-derivation made under the attribution rule above. `constructAnchorFor`'s body, `constructTokenOf`,
    `exemptionKeyFor`, `maskQuantifiers`, `normalizeGateLine`, `nameableRemainder`, both scan bounds,
    `CONSTRUCT_TOKEN_ELLIPSIS`, `EXEMPTION_ANCHOR_SEP`, every exemption key and value, all three
    `EXCLUSIONS`, every band, every `proof` token, `CORE11_BOX_EXPECTED`, the box case, and the sentinel
    docblock and case (plan 01-43's) all stay byte-unchanged. Nothing under `.planning/` is opened.

    RE-READ EVERY SENTENCE THIS TASK WRITES AGAINST THE NO-OVERCLAIM PROHIBITION AND RECORD A VERDICT PER
    SENTENCE, with before and after pasted for every rewrite. Both replacements and the retirement
    bracket ARE overclaim corrections, written in the paragraphs whose previous versions were overclaims;
    they are the likeliest place in this plan to author the eleventh instance.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY. THEN RE-WRITE `BASE_GATE_CURRENT` AND
    `BASE_TESTS_CURRENT` in the baseline file to this boundary's counts and paste all four values,
    stating which two moved and which two are the untouched arrival values plan 01-43 will compare
    against.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && SP=packages/backend/src/outbound-prohibition.spec.ts; TD="${TMPDIR:-/tmp}"; B="$TD/defminer-round10-baseline"; [ -f "$B" ] || { echo "MISSING BASELINE FILE"; exit 1; }; . "$B"; GT=$(pnpm exec vitest run "$SP" 2>&1 | tee /dev/stderr | sed -n 's/.*Tests .*(\([0-9][0-9]*\))[[:space:]]*$/\1/p' | tail -1); [ -n "$GT" ] && [ "$GT" -ge "$BASE_GATE_CURRENT" ] || { echo "GATE COUNT $GT BELOW POST-TASK-1 CURRENT $BASE_GATE_CURRENT"; exit 1; }; [ "$GT" -gt "$BASE_GATE_ARRIVAL" ] || { echo "GATE COUNT $GT NOT ABOVE ARRIVAL $BASE_GATE_ARRIVAL"; exit 1; }; FT=$(pnpm test 2>&1 | tee /dev/stderr | sed -n 's/.*Tests .*(\([0-9][0-9]*\))[[:space:]]*$/\1/p' | tail -1); [ -n "$FT" ] && [ "$FT" -ge "$BASE_TESTS_CURRENT" ] || { echo "SUITE COUNT $FT BELOW POST-TASK-1 CURRENT $BASE_TESTS_CURRENT"; exit 1; }; [ "$FT" -gt "$BASE_TESTS_ARRIVAL" ] || { echo "SUITE COUNT $FT NOT ABOVE ARRIVAL $BASE_TESTS_ARRIVAL"; exit 1; }; pnpm typecheck && pnpm lint && pnpm knip && pnpm build:backend && pnpm check:bundle && RB=$(git rev-parse 483a8ee) && D=$(git diff "$RB"..HEAD -- "$SP"); AST=$(printf '%s\n' "$D" | grep -cE '^[+-].*(from "typescript"|[^A-Za-z]ts\.[A-Za-z])' || true); [ "$AST" -eq 0 ] || { echo "COMPILER-API LINES IN DIFF: $AST"; exit 1; }; HAND=$(printf '%s\n' "$D" | grep '^+' | grep -vE '^\+[[:space:]]*(//|\*|/\*)' | grep -cE '(braceDepth|braceLevel|indentOf|indentLevel|enclosing[A-Z]|Enclosing|containmentOf|frameOf|depth\+\+|depth--)' || true); [ "$HAND" -eq 0 ] || { echo "HAND-ROLLED CONTAINMENT SPELLING IN DIFF: $HAND"; exit 1; }; NAR=$(printf '%s\n' "$D" | grep -cE '^[+-].*(DECLARATION =|FIXTURE_TITLE =|DOCBLOCK_OPEN =|isLineComment =|isRule =|CONSTRUCT_TOKEN_ELLIPSIS =|EXEMPTION_ANCHOR_SEP =|lineNumber - 1|k < lineNumber)' || true); [ "$NAR" -eq 0 ] || { echo "NARROWING TOKENS IN DIFF (SECOND NET): $NAR"; exit 1; }; XA='BEGIN{s=0} !s && $0 ~ "^const " n "[ =(]" {s=1; print; next} s && /^(\};|\/\*\*|const |export |type |interface |describe|it\()/ { if ($0 ~ /^\};/) print; exit } s {print}'; for n in constructAnchorFor constructTokenOf exemptionKeyFor maskQuantifiers normalizeGateLine nameableRemainder; do git show "$RB":"$SP" | awk -v n="$n" "$XA" > "$TD/r10.base.$n"; awk -v n="$n" "$XA" "$SP" > "$TD/r10.work.$n"; BL=$(grep -c . "$TD/r10.work.$n" || true); [ "$BL" -ge 4 ] || { echo "BODY RANGE FOR $n VACUOUS: $BL LINES — THE CHECK LOST ITS TERMINATOR"; exit 1; }; BD=$(diff "$TD/r10.base.$n" "$TD/r10.work.$n" | grep -c . || true); [ "$BD" -eq 0 ] || { echo "BODY OF $n CHANGED: $BD DIFF LINES"; exit 1; }; echo "BODY-BYTE-IDENTICAL $n lines=$BL"; done; AS='BEGIN{s=0} !s && /^export function auditSource\(/ {s=1; print; next} s && /^\}$/ {print; exit} s {print}'; git show "$RB":"$SP" | awk "$AS" > "$TD/r10.base.auditSource"; awk "$AS" "$SP" > "$TD/r10.work.auditSource"; AL=$(grep -c . "$TD/r10.work.auditSource" || true); [ "$AL" -ge 100 ] || { echo "auditSource RANGE VACUOUS: $AL LINES"; exit 1; }; AD=$(diff "$TD/r10.base.auditSource" "$TD/r10.work.auditSource" | grep -c . || true); [ "$AD" -eq 0 ] || { echo "auditSource CHANGED: $AD DIFF LINES"; exit 1; }; IMPN=$(grep -c '^import ' "$SP" || true); [ "$IMPN" -ge 4 ] || { echo "IMPORT BLOCK VACUOUS: $IMPN LINES"; exit 1; }; git show "$RB":"$SP" | grep '^import ' > "$TD/r10.base.imports"; grep '^import ' "$SP" > "$TD/r10.work.imports"; IMPD=$(diff "$TD/r10.base.imports" "$TD/r10.work.imports" | grep -c . || true); [ "$IMPD" -eq 0 ] || { echo "IMPORT SET CHANGED: $IMPD DIFF LINES"; exit 1; }; echo "STRUCTURAL-OK auditSource=$AL imports=$IMPN"; SITE=$(printf '%s\n' "$D" | grep -cE '^[+-].*(site: "|readonly site:)' || true); [ "$SITE" -eq 0 ] || { echo "REGISTRY site FIELD TOUCHED: $SITE"; exit 1; }; R1=$(grep -c 'interchangeability residual is that wide' "$SP" || true); [ "$R1" -eq 0 ] || { echo "SURFACE A OLD INFERENCE STILL PRESENT: $R1"; exit 1; }; T234N=$(grep -c '234' "$SP" || true); [ "$T234N" -ge 2 ] || { echo "THE 234 WAS DELETED RATHER THAN RE-DESCRIBED: ONLY $T234N MENTIONS LEFT, EXPECTED AT LEAST 2"; exit 1; }; R2=$(awk '{L[NR]=$0} END{n=0; for(i=1;i<=NR;i++){ if (L[i] ~ /234/) { ok=0; for(j=i-3;j<=i+3;j++) if (j>=1 && j<=NR && tolower(L[j]) ~ /shadow/) ok=1; if(!ok) n++ } } print n}' "$SP"); [ "$R2" -eq 0 ] || { echo "234 STATED WITHOUT THE SHADOW CORRECTION BESIDE IT AT $R2 SITE(S) — SURFACE B WAS LEFT UNCORRECTED"; exit 1; }; echo "TWO-SURFACE-234-OK R1=$R1 T234N=$T234N R2=$R2"; N=$(git status --porcelain .planning/ | grep -c . || true); [ "$N" -eq 0 ] || { echo "PLANNING DIRTY: $N"; exit 1; }; A=$(git diff --name-only "$RB"..HEAD -- packages/ scripts/ | grep -v '\.spec\.ts$' | grep -c . || true); [ "$A" -eq 0 ] || { echo "SHIPPED SOURCE TOUCHED: $A"; exit 1; }; M=$(find packages/*/src -name '*.ts' ! -name '*.spec.ts' | grep -c . || true); [ "$M" -eq "$BASE_MODULES" ] || { echo "MODULE COUNT $M != $BASE_MODULES"; exit 1; }; git diff --exit-code -- "$SP" && echo TREE-CLEAN</automated>
  </verify>

  <acceptance_criteria>
    - The precondition asserted and PASTED, with `git rev-parse HEAD`, a clean status, the gate suite at
      Task 1's recorded count, and the pinned maximum re-read from the baseline file and quoted.
    - `grep -n 234` pasted BEFORE the edit and reconciled against the three expected lines; an unexpected
      hit recorded as a finding.
    - Both replacement paragraphs quoted VERBATIM and each shown to carry all five elements: the residual
      named as the anchor's shadow and explicitly distinguished from the occurrence-to-anchor distance;
      the measured widest shadow with its owning anchor, surface width, raw span and shipped-occurrence
      count; the interchangeability consequence stated with pass 10's two relocations and their distances
      and results cited; 234 kept and correctly re-described as bounding nothing; and Task 1's case named
      with its reach and its MAXIMUM-only limit stated.
    - The two replacements pasted side by side and shown to state the same figure on the same measurement
      basis, with an explicit statement that they agree; any difference recorded as a finding.
    - Every figure in both paragraphs traced to Task 1's pasted measurement, with an explicit statement
      that no figure was copied from `01-REVIEW.md`'s candidate text or from `01-VERIFICATION.md`.
    - The BOUNDED enumeration of site-identity claims in the exemption-anchoring region pasted with its
      grep and its bounds stated by identifier, reconciled against the floor `:9416`, `:10282`, `:10337`,
      `:10339`, `:10728`, with any unlisted hit recorded as a finding and EVERY hit given a one-line
      written disposition.
    - The replacement for `:10282` quoted verbatim and shown to say the construct half names a REGION of
      measured, pinned width and does not identify the occurrence's site.
    - The dated retirement bracket quoted verbatim and shown to state: that the site-identity claim is
      withdrawn as of wave 42; that the reason is the 2026-08-25 bar never required it, with the three
      criteria named; that SOLE BOUND asks for a disclosure accurate and contradicted nowhere; and that
      `01-VERIFICATION.md`'s mechanism judgement is where the decision is recorded. An explicit statement
      that nothing was deleted silently.
    - The four negative site statements shown by diff to be UNCHANGED, each with its written reason for
      surviving.
    - `git diff` shown EMPTY over `ResolverRecord.site`, over every `site:` value in `RESOLVER_REGISTRY`,
      and over every occurrence of the word outside the enumerated region — with the numeric check from
      the verify block pasted.
    - The gate suite re-run after the edits with the pinned maximum pasted. If it moved, the movement is
      attributed line-by-line to lines this task added with the diff shown and the re-derivation recorded;
      if any part is unattributable, the pin is shown UNMOVED and the movement written up as a
      first-order finding. If it did not move, that is stated with the number.
    - THE TWO-SURFACE 234 GATE PASTED WITH ITS THREE NUMBERS BEFORE AND AFTER: `R1` (surface A's old
      inference clause), `T234N` (total `234` mentions, the floor that catches deletion-instead-of-
      correction), and `R2` (`234` lines with no `shadow` within +-3 lines, the number that catches
      surface B being left alone). Reconciled against this plan's recorded arrival state `R1=1 T234N=3
      R2=3` and shown at `R1=0 T234N>=2 R2=0` after. AN EXPLICIT STATEMENT that the previous draft's gate
      checked `R1` alone and was therefore GREEN on a fix that corrected surface A and left surface B
      untouched — measured at `R1=0 T234N=3 R2=2` — which is the state warning 3 of the round-10 revision
      is about.
    - THE MISQUOTE RECORDED AS A ROUND-10 FINDING in its own SUMMARY row, with
      `grep -c 'interchangeability residual is that wide'` pasted at 1, the previous draft's claim that
      both surfaces closed on that text quoted, and the correction stated: surface B is a different
      sentence making the same inference for a different stated reason.
    - The compiler-API check, the hand-rolled-containment net, the narrowing TOKEN net and all eight
      body-scoped byte-identity pairs shown returning zero over this task's diff, with their non-vacuity
      line counts pasted — plus a per-sentence written statement that no containment was computed by any
      means, AST or hand-rolled.
    - `git diff` shown EMPTY over `constructAnchorFor`'s body, `constructTokenOf`, `exemptionKeyFor`,
      `maskQuantifiers`, `normalizeGateLine`, `nameableRemainder`, both scan bounds,
      `CONSTRUCT_TOKEN_ELLIPSIS`, `EXEMPTION_ANCHOR_SEP`, every exemption key and value, all three
      `EXCLUSIONS`, every band, every `proof` token, `CORE11_BOX_EXPECTED`, the box case, and the
      sentinel docblock and case.
    - `git status --porcelain .planning/` asserted EMPTY numerically; `requirements mark-complete` absent
      from every command run.
    - Executed `pnpm test` exit 0 with counts pasted and asserted at or above the captured baseline;
      `typecheck`, `lint`, `knip` exit 0; `build:backend && check:bundle` exactly one specifier, `crypto`;
      the real-tree walk asserted at the captured module count with zero violations; CORE-11's first
      sentence pasted and shown byte-identical.
    - A one-line no-overclaim verdict recorded for EVERY sentence added or changed, with before and after
      pasted for each rewrite and the reach claimed stated beside the reach executed.
    - Any prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses, or an
      explicit statement that there were none.
  </acceptance_criteria>

  <done>
    Limit (5) and the `constructAnchorFor` docblock both state the interchangeability residual as the
    measured width of the anchor's shadow with the pin that watches it named and its limits stated, 234
    is kept and correctly re-described as bounding nothing, and the claim that the construct half NAMES
    the occurrence is retired in the bytes — dated, reasoned against the 2026-08-25 bar, and recorded
    against a bounded enumeration in which every surviving site statement carries a written disposition.
  </done>
</task>

</tasks>

<!-- planner-discipline-allow: from "typescript" -->
<!-- planner-discipline-allow: interchangeability residual is that wide -->
<!-- planner-discipline-allow: maximum distance from an occurrence to its anchor -->
<!-- planner-discipline-allow: is 234 LINES -->
<!-- planner-discipline-allow: got 234 again -->
<!-- planner-discipline-allow: 234 -->
<!-- planner-discipline-allow: shadow -->
<!-- planner-discipline-allow: braceDepth|braceLevel|indentOf|indentLevel|enclosing[A-Z]|Enclosing|containmentOf|frameOf|depth\+\+|depth-- -->
<!-- planner-discipline-allow: site: " -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a residual stated as one quantity to the quantity that actually bounds it | Occurrence-to-anchor distance is where 29 sentences happen to sit. The residual is the anchor's shadow. The published figure is roughly five times too small, and it is the tenth instance of this phase's signature defect. |
| a corrected sentence to a corrected sentence that is WATCHED | Part 1 alone is true on the day it is written. Part 2 is what makes it drift-detectable, and pass 10 says both or the item is not closed. |
| a pin over a MAXIMUM to the shadows below it | A non-maximal shadow may grow up to the maximum with nothing reporting. That is a real limit of the chosen mechanism and it is disclosed in the bytes rather than left for the next reviewer to find. |
| a pin that bounds SIZE to a relocation it does not prevent | The residual's width becomes measured and drift-detectable. The relocation inside it stays green, which is why the relocation is re-run and its green written down beside the pin. |
| exhaustive evaluation to a sampled one | A pin computed over a sample states a reach it does not execute. Every element of `SURFACE_LINES` is read; memoization is permitted and sampling is not. |
| a pin re-derived against attributed growth to one moved to fit a number | Attribution line-by-line to the same commit's added lines is a measurement. Anything else is a pin moved to fit the number it exists to catch. |
| a deliberate retirement to a silent deletion | The site-identity claim is prose this file volunteered and the bar never required it. Withdrawing it without saying so converts one disclosure defect into another. |
| a bounded enumeration to a word sweep | `site` is a registry FIELD NAME in this file. A global sweep would edit `RESOLVER_REGISTRY` data under cover of a prose correction. |
| a disclosure correction to a mechanism change | The operator took exit 2. Any AST, parser, compiler-API or frame-identity work in this diff is exit 1 arriving unannounced, and any further narrowing is the fourth round of the pattern pass 10 named. |
| a test-only gate to the shipped bundle | Nothing here reaches shipped code. The round's whole code delta is asserted to be one test file by a diff filtered of `.spec.ts`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-264 | Repudiation | a residual published five times too small | high | mitigate | Both surfaces restate the residual as the anchor's shadow, measured with the SHIPPED builder over every surface line in this session, with the owning anchor, surface width, raw span and shipped-occurrence count all named and every figure traced to a pasted measurement rather than copied from a report. |
| T-01-265 | Tampering | a corrected number that can silently widen again | high | mitigate | The maximum shadow asserted by exact equality against the measured value, non-vacuously, with the owning anchor named at runtime — and the pin watched RED against surface lines planted inside the widest shadow, with the arithmetic predicted before the run. |
| T-01-266 | Repudiation | a pin whose reach is stated wider than it executes | high | mitigate | The case's reach paragraph states five limits in its own bytes: maximum only; non-maximal shadows unbounded up to it, illustrated with the measured second-largest; nothing inside the exclusions; no prevention of relocation; still a proximity and not a containment — plus the stated reason the distribution is not pinned as well. |
| T-01-267 | Repudiation | a pin described as closing the relocation class | high | mitigate | Pass 10's own 1,129-line relocation of `:436` is RE-RUN, observed GREEN, and that green is written into the bytes beside the pin, with an explicit requirement that a reader of that paragraph alone cannot conclude the class is closed. |
| T-01-268 | Tampering | a measurement taken over a sample and stated over a surface | high | mitigate | Every element of `SURFACE_LINES` read, with no sampling, no early exit and no break, asserted by diff; memoization permitted and named; the measured wall-clock runtime pasted. |
| T-01-269 | Tampering | the pin moved to fit the number it exists to catch | high | mitigate | Re-derivation permitted ONLY against growth attributed line-by-line to lines the same round added, with the diff shown; unattributable growth leaves the pin unmoved and becomes a first-order finding. Stated in the failure message itself so the next author reads it before editing. |
| T-01-270 | Spoofing | an exit-1 mechanism change arriving inside a disclosure correction | high | mitigate | A structural check over the diff asserts zero added or removed lines carrying the compiler-API import form or a compiler-API member access, and `git diff` over `auditSource` and the import block is EMPTY. Prohibited in frontmatter and asserted numerically, not promised. |
| T-01-271 | Tampering | a fourth narrowing round | high | mitigate | A structural check asserts zero diff lines touching the five recognisers, either scan bound, the ellipsis constant or the separator constant; `git diff` over every helper body and every exemption key asserted EMPTY. Pass 10 states plainly that a fourth narrowing round is what would produce an eleventh instance. |
| T-01-272 | Tampering | a line number folded into the key | high | mitigate | Forbidden in frontmatter; the new case groups by the TOKEN the builder returns and writes into no key; `git diff` over `exemptionKeyFor` and every exemption key asserted EMPTY. The census case's own message already names this as the tempting one and the worst. |
| T-01-273 | Information Disclosure | a claim withdrawn without a record | medium | mitigate | The retirement is a dated, marked bracket stating that the claim is withdrawn, that the 2026-08-25 bar never required it, and where the decision is recorded — produced only after a bounded enumeration in which every surviving site statement carries a written disposition. |
| T-01-274 | Tampering | a word sweep editing registry data | medium | mitigate | The enumeration is bounded by identifier to the exemption-anchoring region; `git diff` over `ResolverRecord.site`, over every `site:` value and over every occurrence outside the region asserted EMPTY by a numeric check. |
| T-01-275 | Repudiation | two surfaces stating one measurement at two values | medium | mitigate | The figure is written once with its basis and the second surface states the same figure with the same basis and a pointer; both pasted side by side and shown to agree. WR-60 is in this same round because this failed once already. |
| T-01-276 | Repudiation | this round's own prose widening the residual it just pinned | medium | mitigate | Task 2 re-runs the pin after its edits, and any movement is attributed line-by-line to lines the task added or the pin stays unmoved and the movement is a finding. The pin doing that work inside its own round is the first evidence that it works. |
| T-01-263 | Repudiation | an eleventh overclaim authored inside the fix for the tenth | high | mitigate | An `open`-status DISCLOSED-NOT-CLOSED prohibition in frontmatter, a per-sentence no-overclaim verdict in both tasks with reach-claimed beside reach-executed, and an explicit designation of the two highest-risk paragraphs — the one recording mutation two's green and the one replacing limit (5)'s closing sentence. |
| T-01-240 | Tampering | a mutation planted over uncommitted work | high | mitigate | Task 1 commits before planting, pastes `git log --oneline -1` first, and proves each restore with `git diff --exit-code`. Wave 29 destroyed its own Task 1 exactly this way. |
| T-01-214 | Tampering | CORE-11's checkbox moved | critical | mitigate | Prohibited. This plan opens no file under `.planning/`; `CORE11_BOX_EXPECTED` asserted byte-unchanged; `git status --porcelain .planning/` asserted EMPTY numerically at every boundary; `requirements mark-complete` appears in no command, preserving plan 01-41's handoff. |
| T-01-212 | Information Disclosure | the two reader surfaces no mechanism reaches | medium | accept | `.planning/STATE.md` and `.planning/WINDOWS.md` are governed by the pointer-not-a-bound prohibition with no mechanical check. Untouched here and carried forward from T-01-144. |
| T-01-51 | Information Disclosure | the accepted residual of the outbound walk | medium | mitigate | Unchanged by this plan — no registry row moves — and byte-pinned into the ledger by the two comparisons, re-run green at both task boundaries. |
| T-01-50 | Information Disclosure | `.spec.ts` files, which the walk skips | low | accept | Unchanged and carried forward, bounded by `pnpm check:bundle`, which specs never enter. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | NOT APPLICABLE: this plan installs no package. `pnpm install` is not run and no manifest appears in `files_modified`. `01-RESEARCH.md`'s Package Legitimacy Audit is present and unchanged; no `[ASSUMED]` or `[SUS]` package is introduced, so no blocking human checkpoint is required. |
</threat_model>

<verification>
1. Precondition asserted and pasted at both task boundaries, with HEAD reconciled against `483a8ee` and
   `01-41-SUMMARY.md` confirmed present.
2. The round's baseline file written from this session's measurements and its contents pasted; every
   numeric assertion in both verify blocks made against those captured values and never against a
   literal.
3. The gate file's line count re-derived and reconciled against all three disagreeing surfaces, with the
   carried-forward number stated.
4. The temporary instrumented case's full distribution pasted — ten largest shadows with anchor token,
   surface size, raw span and shipped-occurrence count — measured from inside the gate's own `describe`
   with the shipped builder, and shown by diff to have been removed afterwards.
5. The distribution reconciled against pass 10's five figures and the review's four-row table, with every
   disagreement recorded as a finding.
6. The 1,155-raw / 574-surface relationship explained by exclusion one, with the machine-owned span's two
   sentinels located by text.
7. The grouping choice stated, and the alternative grouping by producer line measured with its maximum
   pasted and an explicit statement of whether it differs.
8. The permanent case shown by diff to be exhaustive, non-vacuous on four counts before its rule, and an
   exact equality; its title, failure message and reach paragraph each quoted verbatim.
9. The reason for the exact equality over an upper bound quoted; the reason the distribution is not
   pinned quoted.
10. The case's wall-clock runtime pasted with a statement about memoization and an explicit statement
    that no sampling or early exit exists.
11. MUTATION ONE: `git log --oneline -1` first; planted lines shown by diff; the new maximum PREDICTED
    before the run; RED observed; failing title and FULL message pasted; arithmetic confirmed against the
    prediction; restored; clean diff.
12. MUTATION TWO: pass 10's `:436` relocation re-run with its destination shown; GREEN observed with its
    count; the resolved key shown byte-identical; restored; clean diff. A RED instead is written up in
    full as a first-order finding.
13. The paragraph recording mutation two's green quoted verbatim, with the explicit statement that it
    cannot be read as closing the relocation class.
14. `grep -n 234` pasted before Task 2's edit and reconciled against three expected lines.
15. Both replacement paragraphs quoted verbatim and each shown to carry all five required elements;
    pasted side by side and shown to agree on one figure with one basis.
16. Every figure in both paragraphs traced to Task 1's pasted measurement, with an explicit statement
    that none was copied from a report.
17. The bounded site-claim enumeration pasted with its grep and its bounds by identifier, reconciled
    against the five-hit floor, every hit given a one-line disposition, any unlisted hit a finding.
18. The replacement for the positive claim and the dated retirement bracket each quoted verbatim, the
    bracket shown to carry the withdrawal, the reason, the three criteria and the citation, with an
    explicit statement that nothing was deleted silently.
19. The four negative site statements shown UNCHANGED by diff, each with its written reason for surviving.
20. `git diff` EMPTY over `ResolverRecord.site`, every `site:` value and every occurrence outside the
    region, by the numeric check in the verify block.
21. The pin re-run after Task 2's prose, with the maximum pasted, any movement attributed line-by-line or
    the pin shown unmoved and the movement recorded as a finding.
22. The numeric check showing the old inference string absent from the file, output pasted.
23. The compiler-API check and the narrowing check both shown returning zero at BOTH task boundaries,
    with an explicit statement that no AST, parser or enclosing-frame identity was built and that nothing
    was narrowed.
24. `git diff` EMPTY over every out-of-scope construct named in each task's action, at that task's
    boundary.
25. `git status --porcelain .planning/` asserted EMPTY numerically at every boundary;
    `requirements mark-complete` absent from every command run; `CORE11_BOX_EXPECTED` byte-unchanged.
26. `pnpm test` exit 0 with counts asserted against the captured baseline; `typecheck`, `lint`, `knip`
    exit 0; `build:backend && check:bundle` exactly one specifier, `crypto`; the real-tree walk at the
    captured module count with zero violations; CORE-11's first sentence pasted byte-identical.
27. A per-sentence no-overclaim verdict for everything both tasks wrote, reach claimed beside reach
    executed.
28. Every prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses, or an
    explicit statement that there were none.
</verification>

<success_criteria>
- The interchangeability residual is published on both surfaces as the measured width of the anchor's
  shadow, with the owning anchor named, its raw span and surface width stated, and the shipped
  occurrences inside it counted — and 234 is kept, correctly re-described as the largest distance any
  shipped sentence happens to sit from its anchor, which bounds nothing.
- The widest shadow is pinned by an exact equality over an exhaustive, non-vacuous grouping of every
  surface line, and the pin has been watched going RED against planted lines with its arithmetic
  predicted in advance.
- What the pin does not catch is EXECUTED rather than argued: pass 10's own 1,129-line relocation is
  re-run, observed green, and that green is written into the bytes beside the pin.
- The case's reach paragraph states all five of its limits in its own bytes, including that a non-maximal
  shadow may grow up to the maximum unreported.
- The site-identity claim is retired deliberately — dated, reasoned against the 2026-08-25 bar, citing
  pass 10's mechanism judgement — against a bounded enumeration in which every surviving site statement
  carries a written disposition, and nothing is deleted silently.
- No AST, parser, compiler-API or enclosing-frame identity appears anywhere in the diff, proved by a
  structural check rather than asserted.
- Nothing was narrowed: the five recognisers, both scan bounds, the key format, every helper body and
  every exemption key are byte-unchanged, proved by diff.
- No line number was folded into any key.
- The pin was re-run after this plan's own prose edits and any movement was attributed line-by-line or
  the pin was left unmoved and the movement recorded as a finding.
- No sentence anywhere claims that measuring and pinning the shadow makes the anchor identify a site,
  closes the relocation class, or discharges criterion (3).
- CORE-11's checkbox did not move in either direction, `requirements mark-complete` was not run, no file
  under `.planning/` was opened, and whether the box may move is left to verification pass 11.
</success_criteria>

<artifacts_this_phase_produces>
| Artifact | Kind | Purpose |
|----------|------|---------|
| the anchor-shadow distribution, measured with the shipped builder | new evidence | Every line of `SURFACE_LINES` grouped by resolved anchor, the ten largest shadows with token, surface width, raw span and shipped-occurrence count, measured from inside the gate's own `describe` and reconciled against pass 10 and the review with every disagreement recorded |
| the widest-shadow pin | new mechanism | An exact equality over an exhaustive, non-vacuous grouping, with the owning anchor named at runtime, so the next widening of the interchangeability residual turns the suite red instead of passing unnoticed |
| the pin's own reach paragraph | corrected disclosure | Five stated limits — maximum only, non-maximal shadows unbounded up to it, nothing inside the exclusions, no prevention of relocation, still a proximity — plus the reason the distribution is not pinned as well |
| mutation one, watched RED | new evidence | Surface lines planted inside the widest shadow with the new maximum predicted before the run, the pin observed failing, the arithmetic confirmed, restored clean |
| mutation two, watched GREEN | new evidence | Verification pass 10's own 1,129-line relocation of `:436` re-run and observed green, recorded in the bytes as the measured statement of what the pin does not do |
| limit (5)'s replaced closing sentence | corrected disclosure | The residual restated as the measured shadow with the pin named and its limits stated, and 234 kept and correctly re-described |
| the `constructAnchorFor` docblock's replaced sentence | corrected disclosure | The same fact on the second surface, same figure, same basis, with a pointer rather than a re-measurement, pasted beside the first and shown to agree |
| the retired site-identity claim | corrected disclosure | The clause saying the construct half NAMES the occurrence replaced by one saying it names a REGION of measured, pinned width, beside a dated bracket recording the withdrawal and its reason |
| the bounded site-claim enumeration | new evidence | Every site claim in the exemption-anchoring region, region-bounded rather than word-swept, each with a one-line written disposition and the four negative statements shown surviving unchanged |
| the round's baseline file | new evidence | `BASE_GATE`, `BASE_TESTS`, `BASE_FILES`, `BASE_MODULES` and `BASE_SURFACE` captured from this session's measurements, read by every verify block in plans 01-42 and 01-43 so no count is asserted against a literal |
| `01-42-SUMMARY.md` | new evidence | The distribution, the pin, both mutations, both replaced paragraphs, the retirement, the enumeration, the per-sentence verdict table and the full gate set |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-skeleton-persistence-compatibility/01-42-SUMMARY.md` when done.

It must contain, as pasted terminal output rather than as prose: the precondition assertions with
`git rev-parse HEAD` and its reconciliation against `483a8ee`; the round's baseline file contents with
the command that produced each value; the gate file's line count from two commands and its reconciliation
against 11,079 / 11,072 / 11,071; the temporary instrumented case's full distribution and its diff in and
out; the reconciliation against pass 10's five figures and the review's four-row table with every
disagreement; the exclusion-one explanation of the raw-versus-surface gap with both sentinels located by
text; the alternative producer-line grouping with its maximum; the permanent case's diff, title, failure
message and reach paragraph; its wall-clock runtime; `git log --oneline -1` before mutation one; mutation
one's planted diff, predicted maximum, RED title and FULL message, restore and clean diff; mutation two's
relocation, GREEN count, byte-identical key, restore and clean diff; the paragraph recording that green;
`grep -n 234` before Task 2; both replacement paragraphs verbatim and side by side; the bounded site-claim
enumeration with its grep and per-hit dispositions; the replacement for the positive claim and the dated
retirement bracket verbatim; the four negative statements shown unchanged; the numeric checks for the
compiler-API, the narrowing set, the registry `site` field and the absent old inference string; the pin
re-run after Task 2 with its maximum and any attribution; the EMPTY `git status --porcelain .planning/`;
and the final `pnpm test`, `typecheck`, `lint`, `knip` and `check:bundle` results with CORE-11's first
sentence shown byte-identical.

It must state in ONE sentence what the interchangeability residual actually is, what it measures at, and
why the 234-line figure was correct as a number and wrong as a bound.

It must state in ONE sentence what the new pin bounds and, in the same sentence, the five things it does
not — the maximum only, non-maximal shadows, the excluded lines, relocation inside a shadow, and
containment.

It must state in ONE sentence that verification pass 10's own relocation was re-run in this session and
stayed GREEN, and that this is the measured statement of what the pin does not do rather than a defect
newly introduced.

It must state in ONE sentence that the site-identity claim was retired deliberately, on what date, for
what reason, and that nothing was deleted silently.

It must state in ONE sentence that no AST, parser, compiler-API or enclosing-frame identity was built,
that nothing was narrowed, and that no line number was folded into any key — each with the numeric check
that proves it.

It must state in ONE sentence that this plan closes CR-22's two parts and closes NEITHER the relocation
class NOR criterion (3), naming what remains open.

It must state in ONE sentence that CORE-11's box was not moved in either direction, that
`requirements mark-complete` was not run, that no file under `.planning/` was opened, and that whether
the box may move is verification pass 11's determination.
</output>
