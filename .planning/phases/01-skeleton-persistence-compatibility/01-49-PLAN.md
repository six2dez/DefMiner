---
phase: 01-skeleton-persistence-compatibility
plan: 49
type: execute
wave: 49
gap_closure: true
gap_ids: [CR-33]
depends_on: [01-48]
requirements: [CORE-11]
autonomous: true
files_modified:
  - packages/backend/src/outbound-prohibition.spec.ts
  - .planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

# ROUND 13'S SECOND PLAN. IT RUNS AFTER 01-48 BECAUSE THE REGION IT WIDENS IS THE REGION 01-48 EDITS.
#
# ---------------------------------------------------------------------------------------------------
# THE THIRD ITEM OF PASS 14'S `missing:` BLOCK, AND WHY IT IS NOT OPTIONAL IN PRACTICE.
# ---------------------------------------------------------------------------------------------------
# The `.planning/` mention census installed by `196d20c` discloses its own reach honestly. Its limit (a)
# reads: *"IT REACHES THE HEADER AND NO FURTHER ... Prose below the registry is not scanned by it at all."*
# Pass 14 confirmed that reach by NEGATIVE CONTROL rather than by reading it — one fabricated mechanism
# claim reds above the sentinel and is INVISIBLE below it.
#
# THAT UNSCANNED REGION IS THE ONE THAT CARRIED BOTH CR-33 BLOCKERS. Surface #2 of criterion (3) is
# "gate file prose below the registry", and it is the only one of seven that has ever failed. So the
# region with the worst measured record is the region with the least machinery over it, and the machinery
# already exists: widening costs a declaration list, not a mechanism.
#
# ---------------------------------------------------------------------------------------------------
# WHAT THIS PLAN DOES NOT CLAIM.
# ---------------------------------------------------------------------------------------------------
# It closes NO known live defect. There is no CR-33(c) sitting below the sentinel that anyone has found;
# pass 14 looked and reported none. This is a PROPHYLACTIC widening of a disclosed blind spot, and saying
# so plainly is the point — the failure mode this phase keeps reproducing is a sentence that promises more
# reach than its mechanism executes, and a plan that oversold this one would be committing that defect
# inside the commit that widens the guard against it.
#
# ---------------------------------------------------------------------------------------------------
# THE SELF-REFERENCE PROBLEM IS REAL AND IT IS THIS PLAN'S ONLY GENUINE DESIGN DECISION.
# ---------------------------------------------------------------------------------------------------
# Measured 2026-08-27 on the pre-01-48 tree: 23 lines below the derived-block sentinel name a file under
# the planning directory, and TWELVE of them are the census machinery's own bytes — its banner, its
# docblock, its eight declared line texts, its path-prefix constant and one of its case titles. A region
# that swallowed those would obligate the census to declare itself, and the entries declaring the
# declarations would be the next undeclared hits, without end. So the census section gets the same
# treatment plan 01-48 gave its own declaration block: a resolved, sentinel-bounded self-exclusion, with a
# uniqueness assertion per sentinel and non-vacuity before any rule reads the region. That is the third
# instance of the generalized "censused region" contract, and this plan is where the contract stops being
# a resemblance and becomes an asserted invariant.
#
# ---------------------------------------------------------------------------------------------------
# THE SIX HARD CONSTRAINTS OF ROUND 13 APPLY BYTE FOR BYTE AND ARE REPEATED RATHER THAN INHERITED.
# ---------------------------------------------------------------------------------------------------
# 1. `git grep -E` SILENTLY DROPS `\b` ON THIS PLATFORM — pass 14 measured a false ZERO against a real 7.
#    Every census and every verify clause here uses `grep -rE`, and every zero is preceded by a STATED
#    POSITIVE CONTROL asserted non-zero first.
# 2. NO LINE NUMBER IS A LOCATOR. Every target is named BY ITS TEXT.
# 3. THE SIX DATED-HISTORY RANGES AND PLAN 01-48's SIX DECLARATIONS SURVIVE UNCHANGED, counted both ways.
# 4. THE NINE DECLARED PHRASINGS IN `UNBOUNDED_QUANTIFIERS` GO INTO NO NEW PROSE.
# 5. NO NEW `N..M` RANGE IS WRITTEN ANYWHERE — plan 01-48's exact-six gate is now live and this plan is
#    the first thing it guards.
# 6. THE SHADOW PIN AND THE IDENTITY PIN ARE NOT MOVED TO FIT. This plan adds more lines below the
#    registry than 01-48 did, so this is the round where that hazard is most likely to fire.

must_haves:
  truths:
    - "THE POPULATION IS ENUMERATED AND CLASSIFIED BEFORE ANY RULE IS WRITTEN. Every line below the derived-block sentinel naming a file under the planning directory is listed with its text and a verdict: MACHINE-OWNED SELF-REFERENCE, TRUE-AND-POINTING, STATES-AN-ABSENCE, or MECHANISM CLAIM — and any line landing in the last bucket is a live finding, filed rather than declared away."
    - "THE CENSUS SECTION IS EXCLUDED FROM ITS OWN SCAN BY A RESOLVED BOUNDARY, NEVER BY A WRITTEN LINE NUMBER. Two full-line comment sentinels bound it, each asserted to match EXACTLY ONE line before any rule reads the region, so a sentinel that stopped matching is loud rather than silently region-collapsing."
    - "THE THIRD CENSUSED REGION IS BUILT UNDER THE SAME CONTRACT GOVERNING THE FIRST TWO AND THE CONTRACT IS NOW ASSERTED RATHER THAN RESEMBLED. One case enumerates every censused region in the file and requires each to have resolved bounds and a non-vacuity assertion, so a fourth region added without the guard goes red the moment it lands."
    - "EVERY BODY MENTION IS DECLARED WITH A REASON THAT DESCRIBES THE CLAIM, NOT THE PATH. The reason says what the sentence promises a reader — a pointer, an absence, a genuinely-read surface, or a named error path — because a reason that only restates the file name is a label and this census counts labels as thin."
    - "THE WIDENED CENSUS BITES BELOW THE SENTINEL, PROVED BY THE SAME MUTATION PASS 14 USED TO PROVE IT DID NOT. A fabricated mechanism claim naming a planning file, planted in the prose below the registry, turns the suite RED and names its own line — where before this plan it was invisible."
    - "THE HEADER CENSUS IS UNCHANGED AND STILL BITES. Its eight declarations, its region and its five rules are untouched, and the positive control above the sentinel is re-run rather than assumed to have survived the widening."
    - "THE DISCLOSED LIMITS ARE RESTATED FOR THE WIDER REACH RATHER THAN INHERITED SILENTLY. It still matches a path prefix and nothing else, so a mechanism claim naming no file is invisible; it still matches only paths under the planning directory, so the same false sentence about a file elsewhere in the repository still passes unseen; and it still does not adjudicate whether a declared reason is a good one."
    - "NOTHING LEAKED AND NOTHING ELSE MOVED. Whole suite, typecheck, lint, knip, build and bundle check all executed; the import set is still exactly one specifier; zero debt markers under either source root; plan 01-48's exact-six range gate is still green."
    - statement: "The widened census reaches every line below the derived-block sentinel except the census section's own bytes, so a false mechanism claim written INTO that section — for example inside a declared reason — is invisible to it, and the only thing standing against that is that the section's contents are themselves the thing a reviewer reads."
      verification: backstop
    - statement: "A mechanism claim naming a file outside the planning directory is invisible to this census in the body exactly as it is in the header, so widening the REGION does not widen the MATCHER, and the same shape as CR-30 in a different directory remains reachable."
      verification: backstop

  prohibitions:
    - requirement_id: CORE-11
      category: integrity
      status: unresolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT declare a line that is actually a false mechanism claim. Declaring is the LAST of the three dispositions and the file says so; a sentence promising a check this suite does not run is CR-30's shape and is DELETED, not listed."
    - requirement_id: CORE-11
      category: integrity
      status: unresolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT bound the self-exclusion by a written line number or by a count of lines. The bound is resolved by full-line sentinel equality with a uniqueness assertion, because a hand-written boundary in the commit that widens a census against hand-written geometry is the CR-33 class returning through the door it was closed at."
    - requirement_id: CORE-11
      category: integrity
      status: unresolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT weaken, rescope or silently inherit the header census's four disclosed limits. Widening the region changes limit (a) and changes nothing else, and the docblock says exactly that rather than implying the matcher grew with it."
    - requirement_id: CORE-11
      category: integrity
      status: unresolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT use `git grep` with an extended-regex flag anywhere. It drops `\\b` on this platform and returns a false zero that fails closed-looking."
    - requirement_id: CORE-11
      category: integrity
      status: unresolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT touch CORE-11's checkbox or the constant that pins it. Plan 01-50 owns the discharge and this plan is one of the things that discharge will be measured against."

  artifacts:
    - path: "packages/backend/src/outbound-prohibition.spec.ts"
      provides: "A second censused region covering the prose below the derived-block sentinel, its declaration list, and a case asserting that every censused region in the file has resolved bounds and a non-vacuity guard."
      contains: "BODY_PLANNING_MENTIONS"
    - path: ".planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md"
      provides: "Section D extended with the edges the widened region introduces, with sections A, B and C byte-unchanged and the 38-item equality intact."

  key_links:
    - from: "a disclosed blind spot below the derived-block sentinel"
      to: "a censused region under the same contract governing the header region"
      via: "a resolved, sentinel-bounded self-exclusion plus a declaration list with reasons"
    - from: "three censused regions built by resemblance"
      to: "one asserted contract every region must satisfy"
      via: "a case that enumerates the regions and requires resolved bounds and a non-vacuity guard on each"
---

<objective>
Widen the `.planning/` mention census below the derived-block sentinel, so the region that carried both
CR-33 blockers stops being the region with the least machinery over it — and turn the "censused region"
pattern from a resemblance into an asserted contract, so a fourth region cannot be added without its
bounds resolved and its non-vacuity guarded.

Purpose: item 3 of verification pass 14's `missing:` block. The header census discloses limit (a) honestly
— it reaches the header and no further — and pass 14 confirmed that by negative control: a fabricated
mechanism claim reds above the sentinel and is invisible below it. Surface #2 of criterion (3) is the prose
below the registry, and it is the only one of seven surfaces that has ever failed. The machinery already
exists; widening costs a declaration list.

Output: a second censused region with its declarations and reasons, a contract case covering all three
regions, a positive control executed below the sentinel where none could fire before, and the header
census proved unchanged and still biting.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md
@.planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md
@.planning/phases/01-skeleton-persistence-compatibility/01-48-SUMMARY.md
@packages/backend/src/outbound-prohibition.spec.ts
</context>

## Artifacts this phase produces

Every symbol below is CREATED by this plan and does not exist on the tree at plan time.

**New constants and types in `packages/backend/src/outbound-prohibition.spec.ts`:**

- `PLANNING_CENSUS_BEGIN`, `PLANNING_CENSUS_END` — the two full-line comment sentinels bounding the whole
  census section, so the census machinery's own bytes are excluded from the region it scans.
- `BODY_PLANNING_MENTIONS` — the frozen declaration list for the region below the derived-block sentinel.
- `CENSUSED_REGIONS` — the enumeration of every censused region in the file, each carrying its name, its
  resolver and its minimum size, which is what turns the pattern into an asserted contract.

**New functions:**

- `bodyRegion()` — the numbered lines after the derived-block END sentinel, minus the census section.
- `bodyPlanningHits()` — the matched subset of that region.

**New test-case titles** (each a new `it(...)` in the existing `describe` that opens with *"the residual is
DERIVED"*): the body census's non-vacuity case, its undeclared-hit case, its stale-entry case, its
count-balance case, its duplicate-declaration case, its thin-reason case, and the region-contract case.

**New planning content:** additional rows and a restated tally in the `## D.` section of
`.planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md`, appended by plan 01-48.

**Changed, not created:** `headerRegion()` keeps its behaviour and gains membership in `CENSUSED_REGIONS`;
`rangeCensusRegion()` from plan 01-48 does the same.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the body population enumerated and classified, the census section excluded by a resolved boundary, and the second region installed with its declarations and reasons</name>

  <files>packages/backend/src/outbound-prohibition.spec.ts</files>

  <precondition>Plan 01-48 is committed, `01-48-SUMMARY.md` exists, `git status --porcelain` is empty, and plan 01-48's exact-six range gate is green on the current tree — read that count off a run rather than off this plan. The whole suite passes at whatever count 01-48 left it at; read the arrival number off the run, because 01-48 added cases and every figure in this plan's comments is a measurement dated 2026-08-27.</precondition>

  <read_first>
    - `packages/backend/src/outbound-prohibition.spec.ts` — the entire census section as one unit, from the banner comment whose text names the header census through the end of the file: the docblock and its four disclosed limits, `HeaderPlanningMention`, `HEADER_PLANNING_MENTIONS` and all eight of its reasons, `PLANNING_PATH_PREFIX`, `MIN_REASON_LENGTH`, `headerRegion`, `headerPlanningHits`, the non-vacuity case and all five rules — plus everything plan 01-48 added beside it.
    - The same file's `DERIVED_MARK`, `DERIVED_BEGIN` and `DERIVED_END` declarations and the case asserting the sentinels are unique full lines, because the body region's top boundary is the END sentinel and this task must resolve it the same way rather than a second way.
    - Every line below the derived-block END sentinel that names a path under the planning directory, read IN ITS PARAGRAPH — a reason written from the line alone describes a path, and this census counts that as thin.
    - `01-VERIFICATION.md`, the paragraph quoting limit (a) and recording the negative control at a line below the sentinel, so the thing this task widens is understood as the verifier established it rather than as this plan describes it.
    - `packages/backend/src/outbound-prohibition.spec.ts`'s `UNBOUNDED_QUANTIFIERS` — the nine phrasings no new prose may contain.
  </read_first>

  <behavior>
    - With every body mention declared, the suite is green and the body census reports its matched count equal to its declaration count.
    - A fabricated mechanism claim naming a planning file, planted in the prose below the registry, turns the suite RED on the body census's undeclared case and names its own line — the exact mutation that was invisible before this plan.
    - The same fabricated line planted inside the census section leaves the suite GREEN, proving the self-exclusion.
    - The header census's own positive control still fires: the same fabricated line planted above the derived-block BEGIN sentinel still reds the header census.
    - Removing one body declaration while its line stands reds the count-balance case.
  </behavior>

  <action>
Enumerate first and write nothing until the list exists. Search the gate file for the planning path prefix
with `grep -nE` over the file path — never `git grep`, which drops word boundaries here and hands back a
false zero — and split the hits at the derived-block END sentinel, resolving that sentinel by full-line
equality rather than by a remembered line number. Print the counts before the lists.

Classify every body hit into exactly four buckets and put the table in the SUMMARY. MACHINE-OWNED
SELF-REFERENCE is a line that is part of the census machinery itself: its banner, its docblock, a declared
line text, the path-prefix constant, a case title. TRUE-AND-POINTING is a line naming a surface this suite
genuinely reads, where the sentence only points — the byte-compared surface constant's own entry is the
clearest example. STATES-AN-ABSENCE is a line whose claim is that nothing reads the named file, which is
the opposite of CR-30's shape. NAMED-ERROR-PATH is a line describing what happens when a planning ledger is
missing, which promises a failure mode rather than a check. Anything that does not fit those four and
promises a MECHANISM this suite does not run is a LIVE FINDING: do not declare it, delete it, and file it
as a Broken Windows entry with its text — declaring is the last of the three dispositions and the file's
own failure message says so.

Install the self-exclusion before the region. Declare `PLANNING_CENSUS_BEGIN` and `PLANNING_CENSUS_END` as
two strings whose values are comment lines, and place those two comment lines so that the span between
them contains the WHOLE census section — the banner, the docblocks, both declaration lists, the constants,
the resolvers and every case. Resolve both by full-line equality on the trimmed line and assert each
matches EXACTLY ONE line before anything reads the region: at zero the section is not excluded and the
census obligates itself, and above one it resolves whichever duplicate came first.

Then `bodyRegion()` is the file's numbered lines strictly after the derived-block END sentinel, minus the
census span inclusive; `bodyPlanningHits()` filters it by the same `PLANNING_PATH_PREFIX` the header census
uses. REUSE THAT CONSTANT AND THE MATCHER'S SHAPE — a second matcher spelled differently is a second thing
to keep true, which is the argument the header census's own docblock makes against inventing a key format.

Declare every remaining body hit in `BODY_PLANNING_MENTIONS`, mirroring `HEADER_PLANNING_MENTIONS` exactly:
a frozen array of records keyed on the TRIMMED RAW LINE, never an object, because a keyed record silently
overwrites a duplicate. Write a reason that describes THE CLAIM the sentence makes to a reader — what it
promises, and why that promise is one this suite keeps or one it explicitly disclaims — rather than
restating the path. Reuse `MIN_REASON_LENGTH`.

Add the same five rules the header census has, one case each, each with its own message: undeclared, with
the three dispositions in the order this file prefers them and DECLARE last; stale, with a message saying
plainly it is a different failure from an undeclared mention; count balance, with the reason the two set
checks can both pass while these differ; duplicates; thin reasons. Assert non-vacuity BEFORE the rules —
the region is large and the scan matched something — because a census that scanned nothing passes having
measured nothing, which is worse than no census because somebody is now relying on it.

RESTATE THE DISCLOSED LIMITS FOR THE WIDER REACH RATHER THAN LETTING THEM BE INHERITED. Limit (a) changes
and nothing else does: the scan now reaches the header AND the body, excluding the machine-owned span and
the census section, each bounded by resolved sentinels. Limits (b), (c) and (d) are unchanged and stay
stated in full — it matches a path prefix and nothing else, so a mechanism claim naming no file is still
invisible; it matches only paths under the planning directory, so the same false sentence about a file
elsewhere in the repository still passes unseen, which is CR-30's shape in a different directory and is
disclosed here rather than left for a later round to find by moving one path; and it does not adjudicate
whether a declared reason is a good one — it makes the mention listed and reviewable, which is what
converts an invisible claim into one a reader can disagree with, and that is the whole of what it buys.

WRITE NO `N..M` RANGE ANYWHERE. Plan 01-48's exact-six gate is live and this plan is the first thing it
guards; a range written into this docblock would be caught, which is the point of having written it.
Likewise write none of the nine `UNBOUNDED_QUANTIFIERS` phrasings and no debt marker of any kind.

WATCH IT FAIL BEFORE BELIEVING IT PASSES, THREE WAYS. Plant one fabricated mechanism claim naming a
planning file in the prose below the registry — the mutation pass 14 ran to prove the OLD census could not
see it — and read the red off a real run, pasting the message, the named line and the three dispositions.
Plant the SAME line inside the census section and confirm the suite stays green. Plant it once more above
the derived-block BEGIN sentinel and confirm the HEADER census still reds, so the widening is proved not
to have broken the guard it extends. Remove one body declaration while its line stands and confirm the
count-balance case reds.

RESTORE WITH `git checkout --` EVERY TIME, never with a copy — this environment aliases the copy command to
its interactive form and it refuses silently — and prove the restoration with a byte digest of the file
taken before the first mutation and again after the last, plus a clean `git status --porcelain`.

FINALLY, RECONCILE THE PINS RATHER THAN ASSUMING THEM. This task adds more lines below the registry than
plan 01-48 did. If the shadow pin or the identity pin goes red, do NOT adjust either constant. Attribute
the movement line by line to lines this commit added, show the diff, and only then re-derive; if it cannot
be attributed line by line, stop and report rather than fit.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner || exit 1; SP=packages/backend/src/outbound-prohibition.spec.ts; [ -f "$SP" ] || { echo "GATE FILE ABSENT"; exit 1; }; PC=$(grep -rcF '.planning/' "$SP" || true); [ "${PC:-0}" -ge 1 ] || { echo "POSITIVE CONTROL FAILED: the planning path prefix matches ZERO lines, so every count below would be a false zero. NOTE: git grep with -E silently drops \\b on this platform and is not used in this gate"; exit 1; }; echo "POSITIVE-CONTROL-PLANNING-MENTIONS=$PC"; PC2=$(grep -rcF 'DERIVED RESIDUAL' "$SP" || true); [ "${PC2:-0}" -ge 1 ] || { echo "SECOND POSITIVE CONTROL FAILED: the derived-block sentinel text is absent, so this is not the file this gate was written against"; exit 1; }; SENT=$(grep -rcE '^[[:space:]]*//[[:space:]]*PLANNING-CENSUS-(BEGIN|END)[[:space:]]*$' "$SP" || true); [ "${SENT:-0}" -eq 2 ] || { echo "THE CENSUS-SECTION SELF-SENTINELS RESOLVE TO $SENT LINE(S), EXPECTED EXACTLY 2. At any other count the section is not excluded and the census obligates its own declarations without end"; exit 1; }; RSENT=$(grep -rcE '^[[:space:]]*//[[:space:]]*GATE-RANGE-CENSUS-(BEGIN|END)[[:space:]]*$' "$SP" || true); [ "${RSENT:-0}" -eq 2 ] || { echo "PLAN 01-48's RANGE-CENSUS SENTINELS NO LONGER RESOLVE TO 2 ($RSENT); this plan does not own them"; exit 1; }; for S in BODY_PLANNING_MENTIONS bodyRegion bodyPlanningHits PLANNING_CENSUS_BEGIN PLANNING_CENSUS_END CENSUSED_REGIONS; do grep -qF "$S" "$SP" || { echo "MISSING NEW SYMBOL: $S"; exit 1; }; done; HDR=$(grep -rcF 'HEADER_PLANNING_MENTIONS' "$SP" || true); [ "${HDR:-0}" -ge 1 ] || { echo "THE HEADER CENSUS DISAPPEARED; this plan widens it, it does not replace it"; exit 1; }; HDRD=$(grep -rcE '^[[:space:]]*line: "(//|-|`|\.planning)' "$SP" || true); [ "${HDRD:-0}" -ge 8 ] || { echo "THE COMBINED DECLARATION LISTS CARRY $HDRD line-keyed entr(ies); the header census alone shipped 8, so declarations were lost"; exit 1; }; echo "LINE-KEYED-DECLARATIONS=$HDRD"; RANGES=$(grep -rcE '[0-9]+\.\.[0-9]+' "$SP" || true); [ "${RANGES:-0}" -eq 6 ] || { echo "THE GATE FILE CARRIES $RANGES RANGE LINE(S), EXPECTED EXACTLY 6. Plan 01-48 left it at six; this plan writes none and destroys none"; exit 1; }; for P in 'anywhere in the file' 'everywhere in the file' 'any depth' 'every literal' 'ANY of them' 'every spelling' 'every reachable spelling' 'ANY string literal' 'ANY-BINDING-WINS'; do C=$(grep -rcF "$P" "$SP" || true); echo "QUANTIFIER-PHRASING [$P] = $C"; done; DEBT=$(grep -rlnE '(TBD|FIXME|XXX|HACK|PLACEHOLDER)' packages/backend/src packages/engine/src 2>/dev/null | grep -c . || true); [ "${DEBT:-0}" -eq 0 ] || { echo "DEBT MARKERS APPEARED UNDER A SOURCE ROOT ON $DEBT FILE(S)"; exit 1; }; pnpm typecheck || { echo "TYPECHECK FAILED"; exit 1; }; pnpm lint || { echo "LINT FAILED"; exit 1; }; pnpm test || { echo "SUITE FAILED — if a pin went red, attribute the movement line by line to lines this commit added; do NOT adjust the constant"; exit 1; }; pnpm knip || { echo "KNIP FAILED"; exit 1; }; pnpm check:bundle | tee /dev/stderr | grep -qE '1 import specifier\(s\): crypto' || { echo "THE SHIPPED BUNDLE'S IMPORT SET IS NO LONGER EXACTLY ONE SPECIFIER"; exit 1; }; CBX=$(grep -rcF 'CORE11_BOX_EXPECTED = "- [ ] **CORE-11**"' "$SP" || true); [ "${CBX:-0}" -eq 1 ] || { echo "CORE11_BOX_EXPECTED IS NOT THE UNCHECKED FORM ($CBX); PLAN 01-50 OWNS THE FLIP"; exit 1; }; BOXROW=$(grep -rcE '^- \[ \] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md || true); [ "${BOXROW:-0}" -eq 1 ] || { echo "CORE-11's LEDGER CHECKBOX IS NOT THE UNCHECKED FORM ($BOXROW)"; exit 1; }; N=$(git status --porcelain | grep -c . || true); [ "$N" -eq 0 ] || { echo "TREE DIRTY AT EXIT: $N"; exit 1; }; echo TASK1-GREEN</automated>
  </verify>

  <acceptance_criteria>
    - The enumeration command pasted verbatim with counts printed before lists, split at the derived-block END sentinel resolved by full-line equality, and an explicit statement that `grep -rE` was used and `git grep` was not, with the reason named.
    - The four-bucket classification table pasted in full, one row per body hit, each with its text and reason.
    - An explicit statement of whether ANY body hit fell outside the four buckets as a live mechanism claim. If one did: it was DELETED rather than declared, and filed as a Broken Windows entry with its text. If none did: an explicit statement that the search was over every body line and not only the ones this plan expected.
    - Evidence that both new sentinels and both of plan 01-48's resolve to exactly one line each, pasted from the run.
    - The POSITIVE CONTROL below the sentinel executed and its red pasted: the planted line's number and text, the three dispositions, the failure message — the mutation that was invisible before this plan.
    - The NEGATIVE CONTROL executed: the same line inside the census section leaves the suite green.
    - The HEADER census re-proved still biting: the same line above the derived-block BEGIN sentinel still reds the header census, so the widening did not break the guard it extends.
    - The count-balance case watched RED by removing one declaration while its line stands.
    - Every mutation restored with `git checkout --`, never a copy, with the reason named; a byte digest of the file before the first mutation and after the last shown identical; `git status --porcelain` empty.
    - The restated limits quoted from the docblock as shipped, with limit (a) shown changed and limits (b), (c) and (d) shown unchanged and still stated in full.
    - Plan 01-48's exact-six range count shown still at six, read off a run.
    - If either pin moved: the line-by-line attribution and the diff. If neither moved: an explicit statement that both were re-read and are unchanged.
  </acceptance_criteria>

  <reversibility rating="reversible">Additive test cases, a declaration list and two comment sentinels in a test-only file; every byte is recoverable from `git` and nothing shipped changes.</reversibility>

  <done>
Every line below the derived-block sentinel naming a planning file is either machine-owned, declared with a
reason describing its claim, or deleted as a live mechanism claim and filed; the census section is excluded
from its own scan by a resolved boundary; and the mutation pass 14 used to prove the old census blind has
been re-run and now turns the suite red.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the censused-region contract asserted rather than resembled, and the probe ledger extended with the edges the widening introduces</name>

  <files>packages/backend/src/outbound-prohibition.spec.ts, .planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md</files>

  <precondition>Task 1 is committed, all three regions exist in the file, and the suite is green. This task adds no region and no declaration; it adds the case that makes the three regions one contract, plus the ledger rows. If Task 1's classification table is not in the SUMMARY, stop — the ledger rows below describe edges of a mechanism whose population was never written down.</precondition>

  <read_first>
    - `packages/backend/src/outbound-prohibition.spec.ts` — all three region resolvers side by side, plus each one's non-vacuity case, read together rather than in sequence: the contract this task asserts is the intersection of what those three already do, and a contract written without reading all three would assert something one of them does not satisfy.
    - The same file's case asserting the derived-block sentinels are unique full lines, which is the existing precedent for a uniqueness assertion and the shape this contract generalises.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md` — section B's resolution rules and its 38-item equality, and section D as plan 01-48 left it, so this task appends rows and restates the tally rather than starting a second ledger.
  </read_first>

  <behavior>
    - With all three regions satisfying the contract, the suite is green and the contract case reports three regions checked.
    - Adding a fourth region entry whose bounds are not resolved by a unique full-line sentinel turns the contract case RED and names that region.
    - Making any one region resolve to an empty span turns that region's own non-vacuity case RED, and the contract case names the region whose minimum size was not met.
    - The contract case fails loudly if `CENSUSED_REGIONS` is empty, rather than passing having checked nothing.
  </behavior>

  <action>
Declare `CENSUSED_REGIONS` as a frozen list with one entry per censused region in the file: the header
region, the range-census region plan 01-48 installed, and the body region Task 1 installed. Each entry
carries the region's NAME, a function returning its resolved lines, the sentinel texts that bound it, and
the minimum size below which the region is considered collapsed. The names are what the failure messages
print, so write them as a reader would recognise them rather than as identifiers.

Then add ONE case that asserts the contract over that list. For every region: each of its bounding
sentinels matches EXACTLY ONE line of the file by full-line equality on the trimmed line; the resolved
region is at least its declared minimum size; and the region is a contiguous, non-inverted span. The
message names the offending region, states which of the three clauses failed, and says why the clause
exists — a region whose bound stopped matching collapses or swallows silently, and every rule over it then
passes having measured nothing.

ASSERT NON-VACUITY OF THE CONTRACT ITSELF BEFORE THE CONTRACT. An empty `CENSUSED_REGIONS` would make the
case pass having checked nothing, which is the same vacuity hole in one level of indirection. Require the
list to carry at least three entries and say in the message that the count is a floor rather than a pin,
because a fourth region SHOULD be able to land without editing a number — it just cannot land without the
guard.

WATCH IT FAIL. Add a fourth entry whose sentinel text matches no line, run the suite, read the red off a
real run and paste the message with the named region. Then change one region's declared minimum to a value
its live span cannot meet and watch the size clause red separately, so the two clauses are proved to fail
independently rather than as one. Restore with `git checkout --` both times, never a copy, and prove the
restoration by digest and a clean `git status --porcelain`.

Then extend section D of `01-PROBE.md` with the edges this plan introduces, using section B's own
resolution rules: an edge with a defensible acceptance criterion is authored as a plain truth; one whose
check exists but cannot be confirmed with explicit evidence is marked `verification: backstop`; one with no
defensible criterion stays unresolved and is surfaced as a flagged assumption rather than dropped, and is
never auto-dismissed. The edges are: a censused region whose sentinel is duplicated; a region that resolves
to an inverted span; a false mechanism claim written INSIDE the census section, which the self-exclusion
makes invisible; a mechanism claim naming a file outside the planning directory, which the matcher still
cannot see; and a fourth region added without a `CENSUSED_REGIONS` entry at all, which the contract cannot
catch because a region nobody listed is a region nobody checks — say that plainly, because it is the
honest residual of this design and hiding it would be the defect this whole round is about.

SECTIONS A, B AND C STAY BYTE-UNCHANGED and the 38-item equality stays true and checkable. Section D's
tally is restated to include both plans' rows, and it says in its own words that it extends rather than
replaces.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner || exit 1; SP=packages/backend/src/outbound-prohibition.spec.ts; PB=.planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md; [ -f "$SP" ] || { echo "GATE FILE ABSENT"; exit 1; }; [ -f "$PB" ] || { echo "PROBE LEDGER ABSENT"; exit 1; }; PC=$(grep -rcF 'CENSUSED_REGIONS' "$SP" || true); [ "${PC:-0}" -ge 1 ] || { echo "POSITIVE CONTROL FAILED: CENSUSED_REGIONS is absent, so every assertion below is about a file that does not carry the contract"; exit 1; }; echo "POSITIVE-CONTROL-CONTRACT-REFS=$PC"; for S in headerRegion rangeCensusRegion bodyRegion; do grep -qF "$S" "$SP" || { echo "MISSING REGION RESOLVER: $S"; exit 1; }; done; SENTS=$(grep -rcE '^[[:space:]]*//[[:space:]]*(PLANNING|GATE-RANGE)-CENSUS-(BEGIN|END)[[:space:]]*$' "$SP" || true); [ "${SENTS:-0}" -eq 4 ] || { echo "THE FOUR CENSUS SELF-SENTINELS RESOLVE TO $SENTS LINE(S), EXPECTED EXACTLY 4"; exit 1; }; RANGES=$(grep -rcE '[0-9]+\.\.[0-9]+' "$SP" || true); [ "${RANGES:-0}" -eq 6 ] || { echo "THE GATE FILE CARRIES $RANGES RANGE LINE(S), EXPECTED EXACTLY 6"; exit 1; }; PBD=$(grep -cE '^## D\.' "$PB" || true); [ "${PBD:-0}" -eq 1 ] || { echo "PROBE SECTION D IS MISSING OR DUPLICATED: $PBD"; exit 1; }; PBA=$(grep -cE '^## A\.' "$PB" || true); PBB=$(grep -cE '^## B\.' "$PB" || true); PBC=$(grep -cE '^## C\.' "$PB" || true); [ "${PBA:-0}" -eq 1 ] && [ "${PBB:-0}" -eq 1 ] && [ "${PBC:-0}" -eq 1 ] || { echo "PROBE SECTIONS A/B/C DID NOT SURVIVE AS EXACTLY ONE EACH: $PBA/$PBB/$PBC"; exit 1; }; grep -qF '38' "$PB" || { echo "THE 38-ITEM EQUALITY IS NO LONGER STATED IN THE PROBE LEDGER"; exit 1; }; grep -qF 'backstop' "$PB" || { echo "SECTION D DECLARES NO BACKSTOP DISPOSITION; at least one edge of this design cannot be confirmed with explicit evidence and saying so is required"; exit 1; }; for P in 'anywhere in the file' 'everywhere in the file' 'any depth' 'every literal' 'ANY of them' 'every spelling' 'every reachable spelling' 'ANY string literal' 'ANY-BINDING-WINS'; do C=$(grep -rcF "$P" "$SP" || true); echo "QUANTIFIER-PHRASING [$P] = $C"; done; DEBT=$(grep -rlnE '(TBD|FIXME|XXX|HACK|PLACEHOLDER)' packages/backend/src packages/engine/src 2>/dev/null | grep -c . || true); [ "${DEBT:-0}" -eq 0 ] || { echo "DEBT MARKERS APPEARED UNDER A SOURCE ROOT ON $DEBT FILE(S)"; exit 1; }; pnpm typecheck || { echo "TYPECHECK FAILED"; exit 1; }; pnpm lint || { echo "LINT FAILED"; exit 1; }; pnpm test || { echo "SUITE FAILED"; exit 1; }; pnpm knip || { echo "KNIP FAILED"; exit 1; }; pnpm check:bundle | tee /dev/stderr | grep -qE '1 import specifier\(s\): crypto' || { echo "THE SHIPPED BUNDLE'S IMPORT SET IS NO LONGER EXACTLY ONE SPECIFIER"; exit 1; }; CBX=$(grep -rcF 'CORE11_BOX_EXPECTED = "- [ ] **CORE-11**"' "$SP" || true); [ "${CBX:-0}" -eq 1 ] || { echo "CORE11_BOX_EXPECTED IS NOT THE UNCHECKED FORM ($CBX); PLAN 01-50 OWNS THE FLIP"; exit 1; }; BASE=$(git log --format=%H -1 -- .planning/phases/01-skeleton-persistence-compatibility/01-48-SUMMARY.md); [ -n "$BASE" ] || { echo "COULD NOT RESOLVE THIS PLAN'S BASE COMMIT FROM 01-48-SUMMARY.md; every diff below would read the index and report 0"; exit 1; }; git diff --quiet "$BASE" HEAD -- .planning/REQUIREMENTS.md || { echo "REQUIREMENTS.md CHANGED SINCE PLAN 01-48's EXIT; THIS PLAN DOES NOT OWN THAT FILE — PLAN 01-50 DOES"; exit 1; }; N=$(git status --porcelain | grep -c . || true); [ "$N" -eq 0 ] || { echo "TREE DIRTY AT EXIT: $N"; exit 1; }; echo TASK2-GREEN</automated>
  </verify>

  <acceptance_criteria>
    - `CENSUSED_REGIONS` pasted as shipped, with one entry per region, each carrying its name, its resolver, its bounding sentinel texts and its minimum size.
    - The contract case's three clauses named individually, with an explicit statement of which existing behaviour of each of the three regions each clause generalises.
    - The uniqueness clause watched RED by adding a fourth entry whose sentinel text matches no line, with the failure message and the named region pasted from a real run.
    - The size clause watched RED SEPARATELY by setting one region's minimum above its live span, proving the two clauses fail independently rather than as one.
    - The contract's own non-vacuity assertion quoted, with an explicit statement that the entry count is a floor and not a pin, and the reason.
    - Both mutations restored with `git checkout --`, never a copy, with the reason named; a byte digest of the file before the first mutation and after the last shown identical; `git status --porcelain` empty.
    - Section D of `01-PROBE.md` pasted with its restated tally, every new edge carrying an explicit disposition of explicit, backstop or unresolved, and no edge dropped.
    - The residual stated plainly in section D and in the SUMMARY: a region nobody listed in `CENSUSED_REGIONS` is a region nobody checks, and the contract cannot catch it.
    - Sections A, B and C shown byte-unchanged and the 38-item equality shown still stated and still true.
    - Closing baseline executed: whole suite, typecheck, lint, knip, build and bundle check, with the suite's file and test counts read off this session's run and reconciled against plan 01-48's exit.
  </acceptance_criteria>

  <reversibility rating="reversible">One additive test case, one frozen list and an appended documentation section; every byte is recoverable from `git` and nothing shipped changes.</reversibility>

  <done>
The three censused regions are one asserted contract rather than three resemblances, the contract has been
watched failing on each of its clauses independently, and the probe ledger records every edge the widening
introduces — including the one the contract cannot catch, said plainly rather than left for pass 15.
  </done>
</task>

</tasks>

<assumption_delta_decision>
The detector fired `pluralization` on this phase's scope, and this plan is where the transition actually
completes: the file goes from ONE censused region to THREE.

**Primary noun:** *censused region* — a named span of the gate file whose bounds are resolved by full-line
sentinel equality, whose sentinels are asserted unique, and whose non-vacuity is asserted before any rule
reads it.

**Decision: `promote`.** `headerRegion()` is demoted from "the region" to one instance of the generalized
noun, and `CENSUSED_REGIONS` plus its contract case becomes the primary. Add-alongside was rejected
explicitly: three regions built by resemblance would let a fourth land without a uniqueness assertion, and
a region whose bound stopped matching is precisely the silent-collapse failure this phase has been paying
for since CR-30.

**Accepted residual, and it is recorded in the probe ledger as well as here:** the contract reaches every
region that is LISTED. A fourth region added without a `CENSUSED_REGIONS` entry is invisible to it. What
would force a further promote is a second file acquiring censused regions — at that point the list stops
being a list and has to become something the regions register themselves with.

**Companion invariant test: ACCEPTED and implemented as Task 2's contract case**, which goes red the
instant a listed region loses a resolved bound or collapses below its declared minimum.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sentinel text → resolved region | Three regions now depend on sentinel resolution; a sentinel that stops matching collapses or swallows silently and every rule over it passes having measured nothing. |
| census section → the region it scans | The census machinery's own bytes name planning files. Without a resolved self-exclusion the census obligates its own declarations without end. |
| author prose below the registry → reader belief | The region that carried both CR-33 blockers, and until this plan the region with the least machinery over it. |
| declaration list → censused population | A list drifted from its population satisfies both set checks while being wrong. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-327 | Tampering | the census-section self-exclusion | high | mitigate | Bounded by two full-line comment sentinels, each asserted to match exactly one line before any rule reads the region, and the plan forbids bounding it by a written line number. Watched failing by a sentinel that matches nothing. |
| T-01-328 | Spoofing | a false mechanism claim declared rather than deleted | high | mitigate | The four-bucket classification forces every body hit into a named bucket, and anything promising a mechanism this suite does not run is DELETED and filed as a Broken Windows entry. DECLARE is last of the three dispositions and the failure message says so. |
| T-01-329 | Tampering | the widening silently breaking the header census | high | mitigate | The header census's own positive control is re-executed above the derived-block BEGIN sentinel after the widening, rather than assumed to have survived it. |
| T-01-330 | Repudiation | limits inherited silently rather than restated | medium | mitigate | Limits (b), (c) and (d) are restated in full in the widened docblock and quoted in the acceptance criteria; limit (a) is shown changed. A sentence promising more reach than its mechanism executes is the failure this round exists to close. |
| T-01-331 | Denial of service | the widest-anchor shadow pin moving under added lines | medium | mitigate | The plan forbids fitting either pin and requires line-by-line attribution against the commit's own diff, or a stop. This plan adds more lines below the registry than 01-48, so the hazard is named where it is most likely to fire. |
| T-01-332 | Information disclosure | a region nobody listed in `CENSUSED_REGIONS` | low | accept | Disclosed in the docblock, in the probe ledger's section D and in the assumption-delta record. The contract reaches listed regions; an unlisted one is invisible, and saying so is the whole of the guarantee available at this level of indirection. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan; every manifest and the lockfile are untouched, and `pnpm check:bundle` asserts the shipped import set is still exactly one specifier. RESEARCH.md's Package Legitimacy Audit is unchanged and raises no `[ASSUMED]` or `[SUS]` entry for this work. |
</threat_model>

<verification>
- Every line below the derived-block sentinel naming a planning file is machine-owned, declared with a reason, or deleted and filed.
- The census section is excluded from its own scan by a resolved, sentinel-bounded boundary with a uniqueness assertion.
- The mutation pass 14 used to prove the old census blind below the sentinel now turns the suite red.
- The header census is unchanged and re-proved still biting above the sentinel.
- All three regions satisfy one asserted contract, watched failing on each clause independently.
- Plan 01-48's exact-six range count is still six; the nine declared phrasings are probed and reported.
- Whole suite, typecheck, lint, knip, build and bundle check green; import set exactly one specifier; zero debt markers under either source root.
- CORE-11's box and `CORE11_BOX_EXPECTED` are both untouched.
- `01-PROBE.md` sections A, B and C are byte-unchanged and the 38-item equality still holds.
</verification>

<success_criteria>
The region that carried both CR-33 blockers is no longer the region with the least machinery over it: it is
censused under the same contract governing the header, its declarations carry reasons describing claims rather
than paths, the widening has been proved to bite where it could not before and proved not to have broken
the guard it extends, and the one thing the design cannot catch is written down rather than left for
verification pass 15 to find.
</success_criteria>

<output>
Create `.planning/phases/01-skeleton-persistence-compatibility/01-49-SUMMARY.md` when done.
</output>