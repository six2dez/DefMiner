---
phase: 01-skeleton-persistence-compatibility
plan: 42
subsystem: testing
tags: [vitest, gate-discipline, core-11, cr-22, anchor-shadow, disclosure-correction]

requires:
  - phase: 01-skeleton-persistence-compatibility
    provides: "constructAnchorFor, SURFACE_LINES, the three EXCLUSIONS and the uniqueness census (waves 36-41); verification pass 10's measured shadow distribution and its `## The mechanism judgement`"
provides:
  - "A named case pinning the widest anchor shadow over SURFACE_LINES by exact equality at 574, watched RED against planted lines"
  - "The relocation the pin does NOT catch, re-run and recorded green in the bytes"
  - "Both 234 surfaces restating the interchangeability residual as the measured shadow"
  - "The site-identity claim retired deliberately, dated, against a bounded enumeration"
affects: [verification-pass-11, plan-01-43]

actuals:
  tokens: 30000
  tasks: 2
  commits: 5

tech-stack:
  added: []
  patterns:
    - "A residual published as a measured, drift-detectable number rather than as an inferred bound"
    - "A pin shipped beside an executed counter-observation of what it does NOT catch"

key-files:
  created: []
  modified:
    - packages/backend/src/outbound-prohibition.spec.ts

key-decisions:
  - "Verification pass 10's exit 2 taken: stop claiming a site, correct the disclosure. No AST, no parser, no compiler-API identity, no hand-rolled containment, no fourth narrowing."
  - "The maximum shadow is pinned by EXACT EQUALITY rather than an upper bound, so a shadow that shrank is as loud as one that grew."
  - "Only the maximum is pinned; the distribution is recorded as evidence, because a pin that must move on ordinary prose edits is a pin nobody trusts."

patterns-established:
  - "Measure with the SHIPPED builder from inside the gate's own describe, never a lifted copy"
  - "Self-exclude a temporary in-tree measurement case's own lines by marker text, since the gate reads its own bytes"

requirements-completed: []

coverage:
  - id: D1
    description: "The widest anchor shadow over SURFACE_LINES is pinned by exact equality at 574, exhaustively and non-vacuously"
    requirement: CORE-11
    verification:
      - kind: unit
        ref: "packages/backend/src/outbound-prohibition.spec.ts#the WIDEST ANCHOR SHADOW over the scanned surface is PINNED"
        status: pass
      - kind: unit
        ref: "mutation one — 7 surface lines planted inside the widest shadow; pin RED at 581 vs 574, arithmetic matched the prediction exactly"
        status: pass
    human_judgment: false
  - id: D2
    description: "What the pin does NOT catch, executed rather than argued: pass 10's 514-line relocation re-run and observed green"
    requirement: CORE-11
    verification:
      - kind: integration
        ref: "mutation two-A — occurrence relocated 514 lines to pass 10's own destination; 440 of 440 GREEN including the pin; key byte-identical; restored clean"
        status: pass
    human_judgment: false
  - id: D3
    description: "Both 234 surfaces restate the residual as the measured shadow, with 234 kept and correctly re-described"
    requirement: CORE-11
    verification:
      - kind: other
        ref: "two-surface gate: arrival R1=1 T234N=3 R2=3 -> after R1=0 T234N=3 R2=0"
        status: pass
    human_judgment: false
  - id: D4
    description: "The site-identity claim retired deliberately, dated, reasoned, against a bounded enumeration with a per-hit disposition"
    requirement: CORE-11
    verification: []
    human_judgment: true
    rationale: "Whether the retirement is deliberate, adequately reasoned against the 2026-08-25 bar, and free of an eleventh overclaim is a judgement about prose that no assertion in this file can make. Verification pass 11 is the reader it is written for."
  - id: D5
    description: "CORE-11's criterion (3) mechanism leg, and the relocation class"
    requirement: CORE-11
    verification: []
    human_judgment: true
    rationale: "NOT CLOSED BY THIS PLAN AND EXPLICITLY DISCLOSED AS SUCH. A relocation inside a shadow is still invisible — executed at 440 of 440 green in this session. Pass 11 determines whether the corrected disclosure discharges criterion (3)."

duration: 22 min
completed: 2026-08-26
status: complete
---

# Phase 01 Plan 42: The Anchor's Shadow, Measured and Pinned Summary

**CORE-11's interchangeability residual is republished as the measured 574-line width of the widest anchor shadow instead of a 234-line occurrence-to-anchor distance, pinned by an exact equality watched RED against planted lines, shipped beside the relocation it is watched NOT catching, and the site-identity claim retired dated and against a bounded enumeration.**

## Performance

- **Duration:** 22 min
- **Started:** 2026-08-26T20:44Z
- **Completed:** 2026-08-26T21:04Z
- **Tasks:** 2
- **Files modified:** 1 (`packages/backend/src/outbound-prohibition.spec.ts`)

## The six sentences the plan's `<output>` block requires

**What the interchangeability residual actually is.** It is the ANCHOR'S SHADOW — every surface line that resolves to the same anchor, so that any two of them produce the same construct half and an exemption written for one is discharged by an occurrence at the other — and measured at wave 42 with the shipped builder over all 8,846 surface lines it is **574 surface lines** at the header row `:417`, spanning raw 419..1574, already holding four shipped occurrences; the 234-line figure was correct as a NUMBER (re-measured at 234 by pass 10, and this round did not re-measure it) and wrong as a BOUND, because the distance a shipped sentence happens to sit from its anchor is a fact about where 29 sentences were written and constrains nothing about which lines are interchangeable.

**What the new pin bounds, and the five things it does not.** It bounds the MAXIMUM shadow over `SURFACE_LINES` by exact equality at 574, and it does NOT bound a non-maximal shadow (the second-widest is 283 and may grow to 573 unreported), does NOT reach any line inside the three exclusions, does NOT prevent or detect a relocation inside a shadow, and does NOT make the anchor a containment — it is still the nearest preceding line the five recognisers accept, a proximity.

**Pass 10's own relocation, re-run.** Verification pass 10's 514-line relocation of the shipped occurrence was re-run in this session and stayed **GREEN at 440 of 440, the new pin included**, with a byte-identical key and its stated reason false in its new home — and that green is the measured statement of what the pin does not do, not a defect newly introduced by this round.

**The retirement.** The claim that the construct half NAMES the occurrence was retired deliberately on **2026-08-26 (wave 42)** in both places it was written, because the 2026-08-25 re-scoped bar — DERIVED, DRIFT-DETECTABLE, SOLE BOUND — never required the anchor to identify a site (SOLE BOUND asks for a disclosure accurate and contradicted nowhere), and nothing was deleted silently: the withdrawal, its reason and its citation are in the bytes and all five surviving negative site statements are named and byte-identical.

**What was not built and not narrowed.** No AST, no parser, no compiler-API and no enclosing-frame identity was built (`AST=0` over the round diff, the import set byte-identical at 4 lines / 0 diff, `auditSource` byte-identical at 1179 lines / 0 diff), nothing was narrowed (`NAR=0`, and all six protected helper bodies byte-identical at their exact recorded lengths 47/7/12/9/5/11 with 0 diff each), and no line number was folded into any key (`exemptionKeyFor` byte-identical at 12 lines / 0 diff; the new case groups by the token the builder returns and writes into no key).

**What this closes and what it does not.** This plan closes CR-22's two parts — the corrected disclosure at both surfaces and the pin that makes it drift-detectable — and closes **NEITHER the relocation class NOR criterion (3)**: an occurrence inside a shadow can still be moved anywhere else inside it for a byte-identical key at a fully green suite, which was executed here, and what remains open is exactly that the anchor is a proximity and not a containment.

**The checkbox.** CORE-11's box was not moved in either direction (it reads `- [ ] **CORE-11**`, `CORE11_BOX_EXPECTED` byte-identical to `483a8ee`), `requirements mark-complete` was run in no command, no file under `.planning/` was opened by either task, and whether the box may move is verification pass 11's determination.

## Task Commits

1. **Housekeeping — orchestrator STATE.md write** — `868c3ff` (chore)
2. **Task 1: the shadow pin** — `5a60ec2` (test)
3. **Task 1: what the pin does NOT do, both relocations re-run** — `6ad8a81` (docs)
4. **Task 2: both surfaces + the retirement** — `013ea74` (docs)
5. **Task 2: correcting this round's own count** — `216ede3` (fix)

## Precondition, pasted

```
=== HEAD ===
2940ceb736e760abc5e0690a3a1b55e241c61471
=== ancestor check ===
ancestor rc=0                      # git merge-base --is-ancestor 483a8ee HEAD
=== spec byte-identity to 483a8ee ===
[git diff --stat 483a8ee..HEAD -- <spec> produced NO OUTPUT — EMPTY]
=== status ===
 M .planning/STATE.md
=== 01-41-SUMMARY present? ===
-rw-r--r--@ 1 six2dez staff 59922 26 Aug 16:58 .../01-41-SUMMARY.md
```

The load-bearing fact is the EMPTY spec diff, not `HEAD == 483a8ee` — HEAD is `2940ceb`, and `483a8ee` is its ancestor with the round-10 plan commits on top touching only `.planning/`. That is expected by construction and is not a finding.

**`.planning/` was NOT clean at arrival — recorded as finding F-0 and resolved before the first edit.** See Deviations.

## The round's baseline file

`${TMPDIR}/defminer-round10-baseline`, every value from a command executed in this session, none copied from a figure quoted in the plan:

```
BASE_GATE_ARRIVAL=439      # pnpm exec vitest run <spec>  -> Tests 439 passed (439), rc=0
BASE_TESTS_ARRIVAL=1379    # pnpm test -> Test Files 31 passed (31), Tests 1379 passed (1379), rc=0
BASE_FILES=46              # find packages/*/src -name '*.ts' | wc -l
BASE_MODULES=23            # find packages/*/src -name '*.ts' ! -name '*.spec.ts' | wc -l
BASE_SURFACE=8846          # SURFACE_LINES.length, read out of the suite by the temporary case
BASE_GATE_CURRENT=440      # re-written at the task-1 and task-2 boundaries
BASE_TESTS_CURRENT=1380    # re-written at the task-1 and task-2 boundaries
```

`BASE_GATE_ARRIVAL` and `BASE_TESTS_ARRIVAL` were written ONCE, by Task 1's precondition, and were re-written by **no** task in this plan; plan 01-43 compares against them unchanged. `BASE_GATE_CURRENT` / `BASE_TESTS_CURRENT` moved 439→440 and 1379→1380 at the Task 1 boundary and stayed there through Task 2.

Arrival quality gates, all executed: `tsc --build` rc=0, `typecheck` rc=0, `lint` rc=0, `knip` rc=0, `build:backend` rc=0, `check:bundle` → `packages/backend/dist/index.js: 1 import specifier(s): crypto`, real-tree walk 23 modules / 0 violations.

## The file's line count, re-derived — FINDING F-1

```
$ wc -l packages/backend/src/outbound-prohibition.spec.ts
   11071
$ awk 'END{print NR}' packages/backend/src/outbound-prohibition.spec.ts
11071
$ tail -c 1 <spec> | xxd     # 00000000: 0a   — the file ends with a newline
```

Three surfaces disagree and **the disagreement is now explained rather than merely recorded**:

| surface | figure | verdict |
| --- | --- | --- |
| `01-VERIFICATION.md` (twice) | 11,079 | **WRONG** — agrees with neither measurement |
| `01-REVIEW.md` | 11,072 | **explained** — this is `gateLines.length`, i.e. `readFileSync().split("\n")` over a file ending in a newline, which yields one trailing empty element. Measured in-suite at arrival: `gateLines.length=11072`. |
| this plan / this session | 11,071 | **CARRIED FORWARD.** `wc -l` and `awk END{NR}` agree. |

`01-REVIEW.md`'s 11,072 is not an error; it is a different (and legitimate) quantity. `01-VERIFICATION.md`'s 11,079 is an error.

## The temporary instrumented case — full output

Inserted inside the gate's own `describe` at `:10063`, so it read the SHIPPED `constructAnchorFor`, `constructTokenOf`, `quantifierOccurrences` and `SURFACE_LINES`. Diff hunk header proving it was inside:

```
@@ -10062,0 +10063,59 @@ describe("the residual is DERIVED — the registry is bound to the walk, and the
```

**The gate reads its own bytes, so the case inflated the very surface it measured** — first run reported `SURFACE_LINES.length=8905` against an 8,846 arrival. The case was therefore amended to locate its own range by marker text and exclude it. Raw output of the final run:

```
SURFACE_LINES.length(raw,with this case)=8921
SELF-EXCLUDED range=10063..10137 size=75
CLEAN surface (the file WITHOUT this case)=8846
gateLines.length=11147
distinctAnchors=988
distinctProducers=1058
producerGroupingMax=574
producerTop3: producer=417 surface=574 | producer=1 surface=283 | producer=8985 surface=115
shippedOccurrencesOnSurface=29
TOP10:
surface=574 raw=419..1574 rawspan=1156 occ=4 [436,468,523,600] token="SPELLING (operator, by POSITION) RESOLVED BY REPORTS"
surface=283 raw=2..284 rawspan=283 occ=4 [133,134,178,235] token="packages/backend/src/outbound-prohibition.spec.ts — CORE-11's wi…"
surface=202 raw=7012..8947 rawspan=1936 occ=6 [7390,7515,7538,7543,7592,7766] token="it.each(["
surface=115 raw=8986..9100 rawspan=115 occ=0 [] token="WHY THIS EXISTS, AND WHAT IT IS NOT."
surface=98 raw=2540..2637 rawspan=98 occ=0 [] token="THE LITERAL DESCENT OVER AN OPERATOR INITIALIZER — THE SECOND CO…"
surface=89 raw=1763..1851 rawspan=89 occ=0 [] token="const RULES = Object.freeze({"
surface=75 raw=9902..9976 rawspan=75 occ=0 [] token="WR-48, 2026-08-26, wave 37. THIS MATCHED THE EXACT LINE `\"]);\"`,…"
surface=67 raw=2149..2215 rawspan=67 occ=0 [] token="THE ONE DEFINITION OF HOW AN OPERATOR EXPRESSION IS READ FOR A G…"
surface=64 raw=2278..2341 rawspan=64 occ=0 [] token="THE ASSIGNMENT OPERATORS THAT BIND A NAME TO WHATEVER IS ON THEI…"
surface=62 raw=301..362 rawspan=62 occ=3 [303,305,309] token="SPELLING (rebind, receiver-key position) RESOLVED BY REPORTS"
```

`8921 - 75 = 8846` confirms `BASE_SURFACE` a second, independent way.

Removed afterwards and proved gone: `git checkout -- <spec>` then `git diff --exit-code` clean, `wc -l` back to 11071.

## Reconciliation against pass 10 and the review

**Against verification pass 10 — the five surface widths agree EXACTLY.**

| quantity | pass 10 | measured | verdict |
| --- | --- | --- | --- |
| surface widths, largest first | 574 / 283 / 202 / 115 / 98 | 574 / 283 / 202 / 115 / 98 | **exact agreement** |
| raw spans | 419..1574, 2..284, 7012..8947, 8986..9100, 2540..2637 | identical | **exact agreement** |
| `SURFACE_LINES.length` | 8,846 | 8846 | **exact agreement** |
| occurrences inside `:417`'s shadow | four (`:436`, `:468`, `:523`, `:600`) | 4, at 436/468/523/600 | **exact agreement** |
| raw width of 419..1574 | **1,155** | **1156** | **FINDING F-2** |
| file length used for the sentinel sweep | 11,079 | 11,071 | **FINDING F-1** |

**FINDING F-2 — a systematic exclusive-subtraction, inherited from the review.** 1574 − 419 = 1155; the *inclusive* span 419..1574 is 1156 lines. It reproduces on every row of the review's table (284−2=282, 362−301=61, 1574−419=1155, 3077−3023=54), so it is one defect, not four.

**Against `01-REVIEW.md`'s four-row table.**

| review row | review raw width | review surface | measured surface | verdict |
| --- | --- | --- | --- | --- |
| anchor 1, 2..284 | 282 | 283 | 283 | surface agrees; raw width off by one (F-2) |
| anchor 299, 301..362 | 61 | 62 | 62 | surface agrees; raw width off by one (F-2) |
| anchor 417, 419..1574 | 1155 | 574 | 574 | surface agrees; raw width off by one (F-2) |
| anchor 3022, 3023..3077 | 54 | 55 | — (rank 11+, below the top ten, consistent) | consistent |

**The review's raw-width column being SMALLER than its own surface column** (282 < 283, 61 < 62, 54 < 55) — which the plan flagged as impossible, since the surface is a subset of the raw span — **is confirmed and its root cause is F-2**: for a shadow that is fully contiguous surface, the exclusive subtraction yields exactly `surface − 1`. It is an arithmetic defect in the report, not a data disagreement.

**FINDING F-3 — pass 10 and the review do not enumerate the same anchors.** The review lists anchors 1 / 299 / 417 / 3022; pass 10 lists the five widest (417, 1, `it.each([`, `WHY THIS EXISTS`, `THE LITERAL DESCENT`). Neither list is the top-N of the other. The measured top-ten above is the reconciliation, and it contains both lists' members.

## The 1,156-raw / 574-surface relationship — and FINDING F-4

Exclusion one removes the machine-owned span. Both sentinels located BY TEXT:

```
983:BEGIN DERIVED RESIDUAL - generated by deriveResidual(RESOLVER_REGISTRY) in ... - MACHINE-OWNED, DO NOT HAND-EDIT
1564:END DERIVED RESIDUAL
```

and `EXCLUSIONS` derives its bounds from exactly those two lines (`lineOf(l => l.startsWith("BEGIN DERIVED RESIDUAL"))` / `"END DERIVED RESIDUAL"`). So:

```
raw span 419..1574 inclusive        = 1156
machine-owned span 983..1564        =  582   (all of it inside that range)
1156 - 582                          =  574   = the measured surface width, EXACTLY
```

Every surface line in 419..1574 resolves to `:417`; the shadow is the contiguous raw range minus exactly the excluded span, with no remainder.

**FINDING F-4 — the plan's and pass 10's span endpoints are the wrong two lines.** Both state the span as `/*` at `:952` … `*/` at `:1565`. Those are the block-comment DELIMITERS (confirmed: `:952` is `/*`, `:1565` is `*/`). The EXCLUDED range is the SENTINEL-bounded `983..1564` = 582 lines. With the plan's 614-line figure the arithmetic gives 1156 − 614 = 542 ≠ 574 and does not close; with the measured 582 it closes exactly. This finding is what makes the raw-versus-surface relationship exact rather than approximate.

## The alternative grouping, measured

Grouping by PRODUCER LINE instead of by token: `producerGroupingMax=574`, at `producer=417`.

**It does not differ. Both groupings give 574, at the same anchor.** Grouping is by the TOKEN because `constructAnchorFor` returns a token and it is the token that goes into the key, so two lines resolving to the same token are interchangeable whether or not one line produced it. (`distinctProducers=1058` exceeds `distinctAnchors=988`, i.e. some tokens do have multiple producers — that is the uniqueness census's business, not this pin's, and it does not move the maximum.)

## The permanent case

Title, verbatim:

> `the WIDEST ANCHOR SHADOW over the scanned surface is PINNED — an anchor's shadow is the set of lines interchangeable under it, so the shadow's width IS the residual and the occurrence-to-anchor distance is not`

Exhaustive, by construction and by diff — `for (const n of SURFACE_LINES)` with **no sampling, no early exit, no `break`**:

```ts
    const shadows = new Map<string, number[]>();
    for (const n of SURFACE_LINES) {
      const token = constructAnchorFor(gateLines, n);
      const at = shadows.get(token);
      if (at === undefined) shadows.set(token, [n]);
      else at.push(n);
    }
```

Four non-vacuity assertions BEFORE the rule, each with its own message: `SURFACE_LINES.length` > 1000; the shadow map non-empty; **distinct anchors > 1** (a collapse would make the maximum trivially the surface size); and the maximum > 0. Then the rule: `expect(widestSize, …).toBe(WIDEST_ANCHOR_SHADOW)` with `const WIDEST_ANCHOR_SHADOW = 574;`.

**Why the equality is exact, verbatim from the bytes:**

> `WHY THE EQUALITY IS EXACT RATHER THAN AN UPPER BOUND. An upper bound hides a shadow that SHRANK, and a shadow that shrank is information about this file's recogniser geometry that this gate should not discard in silence; an exact equality makes both directions loud.`

**The failure message, verbatim** (all four runtime-computed quantities present — measured maximum, pinned maximum, owning anchor token, raw span):

> `THE WIDEST ANCHOR SHADOW IS NOW ${widestSize} SURFACE LINES AND THIS GATE PINS IT AT ${WIDEST_ANCHOR_SHADOW}. The anchor that owns it is ${JSON.stringify(widest)}, spanning raw lines ${lo}..${hi}. A SHADOW THAT GREW IS A RESIDUAL THAT GREW: every one of those ${widestSize} lines now produces the same construct half, so an exemption written for any one of them is discharged just as well by an occurrence at any other, and the reach this file publishes for its anchoring is that wide. THE CORRECT RESPONSES ARE (a) re-site or rewrite whatever widened it, or (b) re-derive this pin ONLY against growth attributed LINE BY LINE to lines the same commit added, with the diff shown. Moving the pin to fit a number it cannot account for is decoration that reports green, and it is the exact defect this file has spent eleven waves removing. If the number FELL, that is equally reportable: the equality is exact so that a shadow which shrank is visible too, and the reason belongs in the commit that shrank it.`

**The reach paragraph, verbatim — all five limits plus the reason the distribution is not pinned:**

> `THIS CASE'S REACH, STATED NARROWLY AND IN ITS OWN BYTES.`
> `  (1) It bounds the MAXIMUM shadow over SURFACE_LINES under the grouping named above, and nothing wider.`
> `  (2) IT DOES NOT BOUND A NON-MAXIMAL SHADOW. The second-widest measured at 283 surface lines at wave 42, and it may grow to 573 without this case reporting a thing.`
> `  (3) IT REACHES NO LINE INSIDE THE THREE EXCLUSIONS. SURFACE_LINES is the scanned surface and the excluded spans are not in it.`
> `  (4) IT DOES NOT PREVENT A RELOCATION INSIDE A SHADOW. It makes the SIZE of the region that permits one measured and drift-detectable. Pass 10's relocation was re-run at wave 42 and stayed green; see the paragraph below this case.`
> `  (5) IT DOES NOT MAKE THE ANCHOR A CONTAINMENT. The anchor is still the nearest preceding line the five recognisers accept — a proximity. No syntax tree, parser, compiler API or frame identity is computed here or anywhere in wave 42's diff.`
>
> `AND WHY ONLY THE MAXIMUM IS PINNED, WHICH IS A DISCLOSURE AND NOT AN EXCUSE. Pinning the whole distribution would put a number under every one of the top shadows, and each of those numbers would have to move on ordinary prose edits inside a region hundreds of lines wide. A pin that must be re-derived on routine edits is a pin nobody trusts and everybody re-derives without reading. So the maximum is pinned here and the distribution is recorded as evidence in 01-42-SUMMARY.md instead.`

**Wall-clock runtime:** `188ms` (from `--reporter=verbose`). **No memoization was used** — it was not needed. **No sampling and no early exit were introduced**; the loop reads all 8,846 elements every run.

## Mutation one — the pin watched FAILING

`git log --oneline -1` before planting: `5a60ec2 test(01-42): pin the WIDEST ANCHOR SHADOW over the scanned surface — CR-22 part 2`

**Prediction, stated before the run:** 7 surface `//` lines planted after `:420`, inside the contiguous line-comment block (so recogniser (5) accepts only the block's opening line and the shadow GROWS rather than splits) → maximum 574 → **581**, pin unchanged at 574.

Planted diff:

```
@@ -420,2 +420,9 @@
 //        CALL-RECEIVER position                   via receiverKind     — WR-27, and it was
+//      MUTATION ONE, WAVE 42 — planted surface lines, restored in the same task.   (x7)
 //                                                                       `[]` before wave 25
```

**Observed RED.** Failing title and FULL message:

```
FAIL  packages/backend/src/outbound-prohibition.spec.ts > the residual is DERIVED — the registry is bound
      to the walk, and the shipped text to the registry > the WIDEST ANCHOR SHADOW over the scanned
      surface is PINNED — an anchor's shadow is the set of lines interchangeable under it, so the
      shadow's width IS the residual and the occurrence-to-anchor distance is not

AssertionError: THE WIDEST ANCHOR SHADOW IS NOW 581 SURFACE LINES AND THIS GATE PINS IT AT 574. The anchor
that owns it is "SPELLING (operator, by POSITION) RESOLVED BY REPORTS", spanning raw lines 419..1581. A
SHADOW THAT GREW IS A RESIDUAL THAT GREW: every one of those 581 lines now produces the same construct
half, so an exemption written for any one of them is discharged just as well by an occurrence at any
other, and the reach this file publishes for its anchoring is that wide. THE CORRECT RESPONSES ARE (a)
re-site or rewrite whatever widened it, or (b) re-derive this pin ONLY against growth attributed LINE BY
LINE to lines the same commit added, with the diff shown. Moving the pin to fit a number it cannot account
for is decoration that reports green, and it is the exact defect this file has spent eleven waves
removing. If the number FELL, that is equally reportable: the equality is exact so that a shadow which
shrank is visible too, and the reason belongs in the commit that shrank it.: expected 581 to be 574
      Tests  1 failed | 439 passed (440)
```

**Arithmetic confirmed against the prediction: 574 + 7 = 581, exactly.** No discrepancy.

Restored: `git checkout -- <spec>` then `git diff --exit-code` → **clean (rc=0)**.

**A live demonstration of the plan's revision-(5A) defect, from this very run.** The red run's own summary line is `Tests 1 failed | 439 passed (440)`. The OLD parse would have taken the parenthesised total `440`, satisfied `-gt BASE_GATE_ARRIVAL` (439), and proceeded **with this round's own new pin failing**. The corrected parse, run against that exact string:

```
[      Tests  1 failed | 439 passed (440)] -> NEW parse: 439      # 439 is NOT > 439 — the block halts
[      Tests  440 passed (440)]            -> NEW parse: 440      # correct on a green run
```

## Mutation two — and it did NOT go as predicted. FINDING F-5.

**Prediction, stated before the run:** pass 10's ~1,129-line relocation of the occurrence at `:436` (the wrapped ASCII-table cell whose entire normalized content is a declared phrasing) to the top level beside the imports lands *inside* `:417`'s shadow span, so anchor token, group membership and group size are unchanged and **GREEN** is the correct prediction.

**Observed: RED.** Per the plan's pre-commitment, this is a first-order finding, written up in full, and the pin is **NOT** described as catching relocations.

```
AssertionError: THE WIDEST ANCHOR SHADOW IS NOW 567 SURFACE LINES AND THIS GATE PINS IT AT 574. The anchor
that owns it is "SPELLING (operator, by POSITION) RESOLVED BY REPORTS", spanning raw lines 419..1567. …
: expected 567 to be 574
      Tests  1 failed | 439 passed (440)
```

**The shadow SHRANK by 7. It did not grow, and the pin did not detect a relocation.** The mechanism, diagnosed rather than assumed:

The destination places the cell under a BLANK line (`*/` at 1564, blank at 1565, cell at 1566):

```
1564	*/
1565	
1566	//                                                                       any depth
1567	import { existsSync, readdirSync, readFileSync } from "node:fs";
```

That makes the cell the **opening line of a contiguous `//` comment block**, which recogniser (5) accepts — so it becomes an accepted line where none existed. Its own `constructTokenOf` is `null` (its whole normalized content masks to one quantifier token), so the forward walk steps over it and lands on the **import line** at 1567. For `lineNumber = 1568` and below the walk therefore returns the import's token, and the 7 surface lines that used to be `:417`'s tail (raw 1568..1574) left its shadow. `574 − 7 = 567`, exactly.

**So the pin saw a SPLIT, not a move.** It reported because this particular destination happened to perturb a shadow width, not because it can observe a relocation.

**Pass 10's result is reproduced exactly, not contradicted:** `Tests 1 failed | 439 passed (440)` — **all 439 pre-existing cases stayed green**, which is precisely pass 10's `Tests 439 passed (439)`. The key is byte-identical (the stale-entry and count-equality checks fail on any key change and both passed): `"SPELLING (operator, by POSITION) RESOLVED BY REPORTS §§ {q2} :: q2"`. The entry's stated reason — *"HEADER. A wrapped cell in the same ASCII table, labelling what the keyReceiver row already carries."* — is false in the new home. Only the new pin spoke.

Restored: `git diff --exit-code` → **clean**.

### Mutation two-B — the GREEN the success criterion requires

Because 2-A reported for a reason that is not relocation-detection, the honest counter-observation was executed separately: **pass 10's OWN 514-line relocation**, to its own recorded destination (the paragraph closing *"against a walk that shrinks"*, at `:950`), which sits inside the shadow with a `//` line above it so recogniser (5) does not accept it and nothing is split.

**Prediction: GREEN. Observed: GREEN.**

```
@@ -434,5 +434,4 @@
-//                                                                       any depth
@@ -949,4 +948,5 @@
 //    against a walk that shrinks.
+//                                                                       any depth

 Test Files  1 passed (1)
      Tests  440 passed (440)
```

**440 of 440 green, the new pin included**, with a byte-identical key and a stated reason false in its new home. This is the executed statement of what the pin does not do. Restored: `git diff --exit-code` → **clean**.

### The paragraph recording both, verbatim

> `WHAT THE PIN ABOVE DOES NOT DO, EXECUTED AT WAVE 42 RATHER THAN ARGUED. Verification pass 10 relocated the shipped occurrence in the wrapped table cell twice and got 439 of 439 green both times (01-VERIFICATION.md, ### CR-20 is NOT CLOSED / CR-22). BOTH WERE RE-RUN IN THIS SESSION AGAINST THE PIN ABOVE, and the two answers are different for a reason worth writing down rather than averaging.`
>
> `  (A) PASS 10's 514-LINE MOVE, out of the table to the paragraph closing 'against a walk that shrinks' — still inside this anchor's shadow, with a // line above it so recogniser (5) does not accept it. RESULT: 440 of 440 GREEN, the pin included. The key is byte-identical and the entry's stated reason is false where the sentence now sits. THIS IS THE HONEST STATEMENT OF WHAT THE PIN DOES NOT DO: it does not prevent, detect or report a relocation inside a shadow. It makes the SIZE of the region that permits one measured and drift-detectable, and that is the whole of its contribution.`
>
> `  (B) PASS 10's ~1,130-LINE MOVE, to the top level above the imports. RESULT: the pin reported — but it reported a SHRINK, 574 -> 567, and NOT a relocation. THE MECHANISM, BECAUSE A REPORT NOBODY CAN EXPLAIN IS WORTH NOTHING: that destination puts the cell under a BLANK line, which makes it the OPENING line of a contiguous // block, which recogniser (5) accepts. Its own token is null — its whole normalized content masks to one quantifier token — so the forward walk steps over it and lands on the import line below, and the seven surface lines under that import left this anchor's shadow. The pin saw the shadow SPLIT. It did not see the sentence move. A destination that splits nothing, which is what (A) is, reports nothing at all. ALL 439 PRE-EXISTING CASES STAYED GREEN under (B), exactly as pass 10 recorded; the only case that spoke was the one above.`
>
> `SO, STATED SO THAT READING ONLY THIS PARAGRAPH CANNOT MISLEAD: THE RELOCATION CLASS IS NOT CLOSED. An occurrence inside this shadow can still be moved to another line inside it, keep a byte-identical key, keep an exemption reason that is false of its new home, and take the whole suite green with it — (A) is that, executed, at wave 42, at 440 of 440. The anchor is still a proximity and not a containment, criterion (3)'s mechanism leg is not discharged by this case, and CORE-11 is not closed by it. What changed at wave 42 is only this: the WIDTH of the region inside which that move is invisible is now a measured number the suite defends, instead of a published figure five times too small.`

**A reader of that paragraph alone cannot conclude the relocation class is closed:** its final block states in capitals that the class is NOT closed, gives the executed 440-of-440 green as the evidence, and names criterion (3) and CORE-11 as undischarged — before it says anything about what did change.

## `grep -n 234` before Task 2's edit

```
9091: *     occurrence to its anchor is 234 LINES — the occurrence on line 235 takes
9343: * occurrences taking the file's own title line as their anchor from up to 234
9345: * scan bounds and got 234 again, at the occurrence on line 235. The word
```

Exactly the three lines the plan expects. No unexpected hit.

## The two surfaces — quoted from their own bytes, before and after

**SURFACE A — limit (5), `:9090-9093`, BEFORE (verbatim):**

```
 *     RE-MEASURED AT WAVE 39 AFTER BOTH CHANGES, the maximum distance from an
 *     occurrence to its anchor is 234 LINES — the occurrence on line 235 takes
 *     this file's own title line — so `the same construct` can span a couple
 *     of hundred lines and the interchangeability residual is that wide.
```

**SURFACE B — the `constructAnchorFor` docblock, `:9342-9345`, BEFORE (verbatim):**

```
 * region, and in this file those regions are large: WR-49 measured four
 * occurrences taking the file's own title line as their anchor from up to 234
 * lines above them, and wave 39 re-measured the maximum after correcting the
 * scan bounds and got 234 again, at the occurrence on line 235. The word
```

**They share no text.** `grep -c 'interchangeability residual is that wide'` = 1 (surface A only, `:9093`); `grep -c 'got 234 again'` = 1 (surface B only, `:9345`). See FINDING F-6.

**Surface B's own scope justification, stated separately and not carried across from A:** surface B sits under the heading `IT IS THE NEAREST PRECEDING ACCEPTED LINE, WHICH IS NOT THE SAME AS THE FINEST ENCLOSING CONSTRUCT, AND THE DIFFERENCE IS MEASURED RATHER THAN ESTIMATED`, and then offers 234 as that measurement. The proximity-versus-containment gap is the WIDTH OF THE REGION one anchor covers, not the distance a shipped sentence sits from its anchor — the same wrong-quantity inference in different words, for a different stated reason.

**SURFACE A, AFTER (verbatim):**

```
 *     RE-MEASURED AT WAVE 42, AND THE PUBLISHED QUANTITY WAS THE WRONG ONE.
 *     WHAT BOUNDS INTERCHANGEABILITY IS THE ANCHOR'S SHADOW — the set of
 *     surface lines that resolve to the SAME anchor — AND NOT THE DISTANCE
 *     FROM AN OCCURRENCE TO ITS ANCHOR, which is a fact about where the 29
 *     shipped sentences happen to sit. Measured at wave 42 with the shipped
 *     builder from inside this file's own describe over all 8,846 surface
 *     lines, the widest shadow belongs to the header row at `:417`,
 *     `SPELLING (operator, by POSITION) RESOLVED BY REPORTS`: 574 SURFACE
 *     LINES, raw span 419..1574 — 1,156 raw lines, of which exactly the 582
 *     the machine-owned span covers are removed by exclusion one — and it
 *     ALREADY HOLDS FOUR SHIPPED OCCURRENCES. Any occurrence inside a shadow
 *     can be moved to another line inside that shadow and keep a
 *     BYTE-IDENTICAL key: verification pass 10 moved one 514 lines and then
 *     1,129 lines, at 439 of 439 green both times, with the entry's stated
 *     reason false in both new homes (`01-VERIFICATION.md`). THE 234 IS
 *     CORRECT AND IT IS NOT A BOUND — it is the largest distance any shipped
 *     sentence HAPPENS to sit from its anchor, and it bounds nothing. The
 *     shadow's width is what is now watched, by the case titled `the WIDEST
 *     ANCHOR SHADOW over the scanned surface is PINNED`, WHICH PINS THE
 *     MAXIMUM ONLY: a non-maximal shadow may grow up to that maximum with
 *     nothing reporting, and the pin does not prevent a relocation inside a
 *     shadow — pass 10's 514-line move was re-run at wave 42 and stayed green.
```

**SURFACE B, AFTER (verbatim):**

```
 * region, and in this file those regions are large. THE MEASURE OF THAT
 * DIFFERENCE IS THE WIDTH OF THE REGION ONE ANCHOR COVERS — THE ANCHOR'S
 * SHADOW, every surface line resolving to it — AND NOT THE DISTANCE FROM AN
 * OCCURRENCE TO ITS ANCHOR. Stated here on the same basis and to the same
 * figure as limit (5) above, which is where it is measured and where the
 * measurement is described: the widest shadow is the header row at `:417` at
 * 574 surface lines, raw 419..1574, already holding four shipped occurrences,
 * and pass 10 relocated one of them 514 lines and then 1,129 lines for a
 * byte-identical key at 439 of 439 green. WR-49's 234 — the largest distance a
 * shipped sentence happens to sit from its anchor, and still 234 when wave 39
 * re-measured it after correcting the scan bounds — is a fact about where
 * those sentences were written and bounds nothing. The shadow's width is
 * pinned by the case limit (5) names, WHICH PINS THE MAXIMUM ONLY. The word
```

**Side by side — they agree, on one figure and one basis.**

| element | surface A | surface B | agree? |
| --- | --- | --- | --- |
| residual named as the anchor's shadow, distinguished from the occurrence-to-anchor distance | yes, in capitals | yes, in capitals | ✓ |
| widest shadow, surface width | 574 | 574 | ✓ |
| owning anchor | header row `:417`, token quoted | header row `:417` | ✓ |
| raw span | 419..1574, 1,156 raw, 582 excluded by exclusion one | 419..1574 | ✓ |
| shipped occurrences inside | FOUR | four | ✓ |
| pass 10's relocations | 514 and 1,129 lines, 439/439 green, reason false in both homes | 514 and 1,129 lines, byte-identical key, 439/439 green | ✓ |
| 234 kept and re-described | "CORRECT AND IT IS NOT A BOUND … bounds nothing" | "is a fact about where those sentences were written and bounds nothing" | ✓ |
| the pin named, with its MAXIMUM-only limit | named by title, "WHICH PINS THE MAXIMUM ONLY" | "pinned by the case limit (5) names, WHICH PINS THE MAXIMUM ONLY" | ✓ |
| measurement basis | measured here, stated once | "on the same basis and to the same figure as limit (5) above" — a POINTER, not a re-measurement | ✓ by construction |

**No difference. The figure is written once, with its basis, at surface A; surface B points at it and cannot drift from it** (the WR-60 failure mode this round also carries).

**Every figure traced to Task 1's pasted measurement.** 574, 8,846, 419..1574, 1,156, 582 and "four shipped occurrences" all come from the temporary instrumented case's raw output above and the sentinel arithmetic above. **No figure was copied from `01-REVIEW.md`'s candidate replacement text or from `01-VERIFICATION.md`** — and had one been, it would have carried the review's 1,155, which this round measured at 1,156 (F-2).

## The two-surface 234 gate, before and after

```
ARRIVAL (483a8ee): R1=1 T234N=3 R2=3     # plan records R1=1 T234N=3 R2=3 — EXACT MATCH
AFTER            : R1=0 T234N=3 R2=0
```

- `R1` — surface A's old inference clause: gone.
- `T234N` — total `234` mentions: **3, unchanged**. The floor that catches deletion-instead-of-correction holds; the 234 was kept and re-described, not deleted.
- `R2` — `234` lines with no `shadow` within ±3 lines: 3 → 0.

`grep -n 234` after: `:9104`, `:9368`, `:9369` — all inside the corrected paragraphs.

**The previous draft's gate checked `R1` alone and was therefore GREEN on a fix that corrected surface A and left surface B untouched** — a state the plan measured at `R1=0 T234N=3 R2=2`. That is exactly warning 3 of the round-10 revision, and it is why `R2` exists. `R2`'s own limit, restated rather than hidden: it is a WORD-PROXIMITY gate, satisfiable by writing `shadow` beside a 234 without doing the derivation. What it detects is the ABSENCE of the correction. Presence is established by the measured distribution and the quoted paragraphs above, never by `R2`.

## The bounded site-claim enumeration

Bounded **by identifier**: from `const HEADER_QUANTIFIER_EXEMPTIONS` to the cross-construct relocation case (`it("a CROSS-CONSTRUCT relocation yields a DIFFERENT anchored key…")`). Grep: `awk 'NR>=FROM && NR<=TO && /site/'`, **18 hits**. Every hit has a written disposition; line numbers are `483a8ee`'s.

| # | line | text (abbrev.) | disposition |
| --- | --- | --- | --- |
| 1 | 9159 | `…the honest opposite…` | **NOT A HIT** — substring artifact, `oppo-site`. Untouched. |
| 2 | 9219 | ``constructAnchorFor` returns it from ONE site: the fall-through` | **OUT OF SCOPE** — a CODE site (where the sentinel is returned), not an anchor-identity claim. Byte-unchanged. |
| 3 | **9356** | `an anchor that travelled with the sentence and named no site.` | **NEGATIVE — SURVIVES.** True statement about the defect CR-20(a) closed. Byte-unchanged. |
| 4 | **9416** | `an anchor that names no site at all` | **NEGATIVE — SURVIVES.** True statement about the shape the fall-through would create. Byte-unchanged. |
| 5 | 9603/9605 | `Each anchor occurs inside its own registry row (\`site: "auditSource > …"\`)` / `the code site DELETED` | **OUT OF SCOPE** — registry FIELD NAME and ordinary prose. Byte-unchanged. |
| 6 | 9612/9613 | `row.site.split(" > ")` | **OUT OF SCOPE** — registry field read. Byte-unchanged. |
| 7 | 9617 | `records its provenance as \`${row.site}\`… the code site moved` | **OUT OF SCOPE** — registry field. Byte-unchanged. |
| 8 | 9648 | `The row was read off \`${row.site}\`` | **OUT OF SCOPE** — registry field. Byte-unchanged. |
| 9 | 9716 | `a row's \`site\` shares the same convention` | **OUT OF SCOPE** — registry field. Byte-unchanged. |
| 10 | 9757 | `records its site as \`${b.anchor}\`` | **OUT OF SCOPE** — branch provenance. Byte-unchanged. |
| 11 | 9766 | `it names the site the silence is measured AT` | **OUT OF SCOPE** — measurement site. Byte-unchanged. |
| 12 | 9789/9793/9807 | `A RESOLVER is a mechanism with a code site` (×3) | **OUT OF SCOPE** — code site prose. Byte-unchanged. |
| 13 | **10282** | `that is fine — the construct half is what names it` | **THE POSITIVE CLAIM — RETIRED.** Replaced; see below. |
| 14 | **~10272** | `its construct half is what names it.` (comment block above the case) | **SECOND INSTANCE OF THE SAME POSITIVE CLAIM — RETIRED.** See FINDING F-7. |
| 15 | **10331** | `AN ANCHOR THAT IS THE SENTINEL NAMES NO CONSTRUCT AND NO LINE` | **NEGATIVE — SURVIVES.** Byte-unchanged. |
| 16 | **10339** | `a self-anchoring key names no site` (case title) | **NEGATIVE — SURVIVES.** Byte-unchanged. |
| 17 | **10728** | `the key names no site.` | **NEGATIVE — SURVIVES.** Byte-unchanged. |
| 18 | 10200 | the new pin's own failure message (added this round) | In scope, authored this round; claims a REGION and no site. |

**Reconciliation against the plan's floor (`:9416`, `:10282`, `:10337`, `:10339`, `:10728`) — three corrections:**

- **FINDING F-8a.** The plan attributes the text *"an anchor that travelled with the sentence and named no site"* to `:9416`. Measured, that clause is at **`:9356`**; `:9416` carries a **different** negative statement (*"an anchor that names no site at all"*). They are two statements, not one.
- **FINDING F-8b.** The plan's `:10337` is measured at **`:10331`**.
- **FINDING F-7 — an UNLISTED HIT, and it is a second instance of the positive claim.** Neither the plan's floor nor pass 10 records that *"its construct half is what names it"* also appears in the **comment block immediately above** the null-anchor case, not only in its failure message. Retiring only the message would have left the claim standing four lines above it. Both were retired.

Consequently the surviving negative statements number **five**, not four.

Byte-identity of all five, base vs work:

```
[named no site]                        base=1 work=1
[names no site at all]                 base=1 work=1
[NAMES NO CONSTRUCT AND NO LINE]       base=1 work=1
[a self-anchoring key names no site]   base=1 work=1
[the key names no site]                base=1 work=1
```

**Not a word sweep:** `REGISTRY-SITE-TOUCHED=0` (`grep -cE '^[+-].*(site: "|readonly site:)'` over the whole round diff), so `ResolverRecord.site` and every `site:` value in `RESOLVER_REGISTRY` are byte-unchanged — as is every occurrence of the word outside the region, since the round's diff has **zero deletions outside the eleven intended replacement lines** (audited individually below).

## The retirement

**The replacement for the positive claim, verbatim** — failure message:

> `If the occurrence's own line genuinely normalizes to a bare declared phrasing, that is fine — the construct half then names the REGION the occurrence sits in, which is that anchor's SHADOW and is 574 surface lines wide for the one shipped occurrence in this position; it does NOT identify the occurrence's site, a claim withdrawn at wave 42 and recorded in the bracket above this case — but if BOTH halves mask away, the line is the defect: REWRITE the sentence so it says what it is about, or DELETE it.`

and comment block:

> `…has a line half that legitimately reduces to nothing, and its construct half is what names the REGION it sits in.`

**The dated retirement bracket, verbatim:**

> `[SITE-IDENTITY CLAIM WITHDRAWN — 2026-08-26, wave 42, CR-22.] Until this date this paragraph and the failure message below both said the construct half NAMES the occurrence. IT DOES NOT, AND THE CLAIM WAS FALSIFIED BY EXECUTION RATHER THAN ARGUED AWAY: verification pass 10 relocated this very occurrence 514 lines and then 1,129 lines and the key was BYTE-IDENTICAL both times at 439 of 439 green, with the entry's stated reason false in both new homes (01-VERIFICATION.md, ### CR-20 is NOT CLOSED / CR-22). What the construct half names is a REGION — the anchor's SHADOW, every surface line resolving to that anchor — whose width is measured and pinned by the case titled 'the WIDEST ANCHOR SHADOW over the scanned surface is PINNED', and which is 574 surface lines wide for THIS occurrence's anchor. WHY THE CLAIM IS RETIRED RATHER THAN REPAIRED: nothing in the 2026-08-25 re-scoped bar ever required it. That bar asks for a residual that is DERIVED, that is DRIFT-DETECTABLE, and that is the SOLE BOUND — and SOLE BOUND asks for a disclosure that is ACCURATE AND CONTRADICTED NOWHERE, not for an anchor that identifies a site. The site-identity claim was prose this file volunteered on top of the bar; it is withdrawn here, in the bytes, rather than deleted quietly. The decision to withdraw it rather than to build a containment is recorded in 01-VERIFICATION.md's '## The mechanism judgement', which put both exits to the operator; exit 2 is the one taken. NOTHING WAS DELETED SILENTLY. The bounded enumeration behind this retirement is in 01-42-SUMMARY.md; within the exemption-anchoring region it found ONE positive site claim, written in TWO places — this paragraph and the failure message below — and FIVE negative ones, and it is five rather than the four the round-10 plan listed as its floor. All five are untouched and byte-identical: an anchor that travelled with the sentence and named no site; an anchor that names no site at all; an anchor that is the sentinel and names no construct and no line; a self-anchoring key that names no site; and a key whose halves were both read off the occurrence's own line and so names no site. Each survives because each is a true statement about a DEFECT SHAPE, and none of them claims that a well-formed anchor identifies anything.`

It carries all four required elements: the withdrawal, dated to wave 42; the reason, with all three criteria named; SOLE BOUND read as asking for a disclosure accurate and contradicted nowhere; and the citation to `01-VERIFICATION.md`'s mechanism judgement. And it states explicitly that nothing was deleted silently.

## The pin re-run after Task 2's prose

```
$ grep -n 'const WIDEST_ANCHOR_SHADOW' <spec>
10152:  const WIDEST_ANCHOR_SHADOW = 574;
$ pnpm exec vitest run <spec>
      Tests  440 passed (440)
```

**The pinned maximum DID NOT MOVE. It is 574 before and after this task's prose edits, and no attribution or re-derivation was required.** This round added 218 lines and removed 11, all of them either above `:9372` (the two docblocks) or below `:10063` (the new case and the retirement) — none inside the shadow's raw span 419..1574 — so no growth was possible, and none occurred. The pin doing this work inside its own round (T-01-276) reported clean.

## Numeric checks, final state

```
ROUND DIFF CHANGED LINES=225   (>= 10 non-vacuity floor)
AST=0                          compiler-API import form / member access in the diff
HAND=0                         hand-rolled containment spellings in added non-comment lines
NAR=0                          narrowing tokens (recognisers, scan bounds, key constants)
REGISTRY-SITE-TOUCHED=0        site: " / readonly site:
R1=0  T234N=3  R2=0            two-surface 234 gate
PLANNING=0                     git status --porcelain .planning/
SHIPPED=0                      non-spec files under packages/ or scripts/ in the round diff
MODULES=23 == BASE_MODULES=23
TREE-CLEAN                     git diff --exit-code -- <spec>
```

**The eight body-scoped byte-identity pairs**, extracted BY IDENTIFIER from `git show 483a8ee:<spec>` and from the worktree with the same awk, each at its exact recorded length on BOTH sides:

```
BODY-BYTE-IDENTICAL constructAnchorFor base=47   work=47   diff=0
BODY-BYTE-IDENTICAL constructTokenOf   base=7    work=7    diff=0
BODY-BYTE-IDENTICAL exemptionKeyFor    base=12   work=12   diff=0
BODY-BYTE-IDENTICAL maskQuantifiers    base=9    work=9    diff=0
BODY-BYTE-IDENTICAL normalizeGateLine  base=5    work=5    diff=0
BODY-BYTE-IDENTICAL nameableRemainder  base=11   work=11   diff=0
BODY-BYTE-IDENTICAL auditSource        base=1179 work=1179 diff=0
BODY-BYTE-IDENTICAL imports            base=4    work=4    diff=0
STRUCTURAL-OK auditSource=1179 imports=4
```

All eight reconcile against the plan's recorded 47 / 7 / 12 / 9 / 5 / 11 / 1179 / 4. **No range came back short, so no check passed vacuously.**

**The `T234` guard fragments, re-measured at the Task 1 boundary: `1/1/1`** (`is 234 LINES`, `interchangeability residual is that wide`, `got 234 again`). A fragment returning 0 would be a guard that cannot fire — which is precisely what the previous draft's `maximum distance from an occurrence to its anchor` was, since the line wrap splits it across `:9090` and `:9091` and it therefore matches **no line of this file**. All three replacements are single-line and each matched exactly one line. (After Task 2 the first and third are correctly 0 — they were the text Task 2 replaced.)

**Every removed line of the round, audited individually — 218 added, 11 removed, and all 11 are intended replacements:**

```
- *     RE-MEASURED AT WAVE 39 AFTER BOTH CHANGES, the maximum distance from an     surface A
- *     occurrence to its anchor is 234 LINES — the occurrence on line 235 takes     surface A
- *     this file's own title line — so `the same construct` can span a couple       surface A
- *     of hundred lines and the interchangeability residual is that wide.           surface A
- * region, and in this file those regions are large: WR-49 measured four            surface B
- * occurrences taking the file's own title line as their anchor from up to 234      surface B
- * lines above them, and wave 39 re-measured the maximum after correcting the       surface B
- * scan bounds and got 234 again, at the occurrence on line 235. The word           surface B
-  // it. A key whose WHOLE anchor reduces to nothing names neither, and that is     retirement (comment)
-  // the shape a fabricated bound was driven through at 432 of 432 green.           retirement (comment)
-        `exemption key ${JSON.stringify(key)} carries an ANCHOR that reduces …      retirement (message)
```

**Nothing pre-existing was edited or deleted outside those eleven lines**, which is why every out-of-scope construct — `anchorTokenCensus`, `quantifierOccurrences`, `surfaceExemptionKeys`, `preAnchoringExemptionKeyForFixtureOnly`, `CONSTRUCT_TOKEN_ELLIPSIS`, `EXEMPTION_ANCHOR_SEP`, `NO_PRECEDING_CONSTRUCT`, every exemption key and value, all three `EXCLUSIONS`, every band, every `proof` token, `CORE11_BOX_EXPECTED` and the box case — is byte-unchanged by construction as well as by spot-check (each returned 0 diff lines).

**CORE-11's box, unmoved in both directions:**

```
$ grep -c '^- \[ \] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md      -> 1
$ grep -n 'const CORE11_BOX_EXPECTED' <spec>
11190:  const CORE11_BOX_EXPECTED = "- [ ] **CORE-11**";
$ diff <(git show 483a8ee:<spec> | grep 'const CORE11_BOX_EXPECTED') <(grep … <spec>)
IDENTICAL
```

## Final gate set, executed

```
$ pnpm test                       rc=0    Test Files 31 passed (31)   Tests 1380 passed (1380)
$ pnpm typecheck  ($ tsc --build) rc=0
$ pnpm lint       ($ eslint .)    rc=0
$ pnpm knip                       rc=0
$ pnpm build:backend              rc=0
$ pnpm check:bundle
  packages/backend/dist/index.js: 1 import specifier(s): crypto
  real-tree walk: 23 shipped non-spec modules across both SOURCE_ROOTS, 0 violations
$ git diff --exit-code -- <spec>  TREE-CLEAN
```

**No test was skipped at any point.** Every run in this session reported a bare `N passed (N)` summary with no `skipped` term — checked at each boundary, since no `<automated>` block asserts the skipped count is zero (the known advisory).

## Per-sentence no-overclaim verdicts

Reach CLAIMED beside reach EXECUTED, for every sentence this round added or changed.

| # | sentence | reach claimed | reach executed | verdict |
| --- | --- | --- | --- | --- |
| 1 | pin title — "the widest anchor shadow … is PINNED" | the widest shadow over the scanned surface | exactly that; 574 asserted by equality over all 8,846 surface lines | ✔ equal |
| 2 | "an anchor's shadow is the set of lines interchangeable under it" | interchangeability under one anchor | the key's construct half is that token; two lines with the same token give the same half | ✔ equal |
| 3 | reach (1) "MAXIMUM … and nothing wider" | maximum only | the case takes a max and asserts only it | ✔ equal |
| 4 | reach (2) "may grow to 573 without this case reporting" | non-maximal shadows unbounded | true by construction; 283 measured as the second-widest | ✔ equal |
| 5 | reach (3) "reaches no line inside the three exclusions" | scanned surface only | iterates `SURFACE_LINES` | ✔ equal |
| 6 | reach (4) "does not prevent a relocation inside a shadow" | no relocation coverage | executed: 440/440 green under a 514-line relocation | ✔ equal, and EXECUTED |
| 7 | reach (5) "does not make the anchor a containment" | still a proximity | `constructAnchorFor` byte-unchanged, 47 lines / 0 diff | ✔ equal |
| 8 | "No syntax tree, parser, compiler API or frame identity is computed here or anywhere in wave 42's diff" | whole round diff | `AST=0`, `HAND=0`, imports 4/0 diff, `auditSource` 1179/0 diff | ✔ equal (limit stated below) |
| 9 | "why only the maximum is pinned" | a disclosure of a choice | states the reason, points at the SUMMARY for the distribution | ✔ equal |
| 10 | failure message "A SHADOW THAT GREW IS A RESIDUAL THAT GREW" | growth ⇒ wider residual | true of the grouping; arithmetic shown at runtime | ✔ equal |
| 11 | failure message "re-derive … ONLY against growth attributed LINE BY LINE" | an instruction, not a claim | n/a | ✔ no claim |
| 12 | mutation paragraph (A) "it does not prevent, detect or report a relocation inside a shadow" | no relocation coverage at all | 440/440 green observed | ✔ equal, EXECUTED |
| 13 | mutation paragraph (B) "the pin saw the shadow SPLIT. It did not see the sentence move." | a mechanism claim | diagnosed to the exact 7 lines; 574−7=567 | ✔ equal, EXECUTED |
| 14 | mutation paragraph "THE RELOCATION CLASS IS NOT CLOSED" | an explicit non-claim | strengthens rather than overclaims | ✔ no overclaim |
| 15 | surface A "WHAT BOUNDS INTERCHANGEABILITY IS THE ANCHOR'S SHADOW" | the corrected quantity | measured at 574 this session | ✔ equal |
| 16 | surface A "574 SURFACE LINES, raw span 419..1574 — 1,156 raw lines, of which exactly the 582 … removed by exclusion one" | an exact arithmetic identity | 1156−582=574, sentinels located by text | ✔ equal |
| 17 | surface A "THE 234 IS CORRECT AND IT IS NOT A BOUND" | 234 true, not a bound | pass 10 re-measured 234; this round did not re-measure it and says so here | ✔ equal |
| 18 | surface A "WHICH PINS THE MAXIMUM ONLY" | the pin's limit, in the disclosure | matches reach (1)/(2) | ✔ equal |
| 19 | surface A "pass 10's 514-line move was re-run at wave 42 and stayed green" | one executed observation | executed, 440/440 | ✔ equal |
| 20 | surface B "THE MEASURE OF THAT DIFFERENCE IS THE WIDTH OF THE REGION ONE ANCHOR COVERS" | proximity-vs-containment gap = region width | the gap is what a single anchor covers; measured | ✔ equal |
| 21 | surface B "on the same basis and to the same figure as limit (5) above" | a pointer, not a measurement | it re-measures nothing | ✔ equal |
| 22 | retirement "IT DOES NOT [name the occurrence]" | a falsification | two executed relocations, byte-identical key | ✔ equal |
| 23 | retirement "whose width is measured and pinned … 574 surface lines wide" | the region's width | measured; pinned by the case | ✔ equal |
| 24 | retirement "nothing in the 2026-08-25 re-scoped bar ever required it" | a reading of the bar | matches pass 10's exit 2 verbatim | ✔ equal |
| 25 | retirement "All five are untouched and byte-identical" | five survivors | five, each verified base=1 work=1 | ✔ equal **after correction** — see F-9 |
| 26 | replacement message "it does NOT identify the occurrence's site" | an explicit non-claim | n/a | ✔ no overclaim |

**The written statement the plan requires, for every sentence above: NO CONTAINMENT WAS COMPUTED BY ANY MEANS, AST OR HAND-ROLLED.** No compiler API was used (`AST=0`; the `import ts from "typescript"` at `:1570` is untouched and `auditSource` is byte-identical at 1179 lines / 0 diff), no parser was written, no brace-depth walk, no indentation walk, and no enclosing-frame identity under any other spelling. The identifier net (`HAND=0`) matches **SPELLINGS, not shapes** — a containment helper spelled differently would pass it — and this written statement is what covers that residue; the executor states plainly that it is doing so, and that this is not a proof.

## Findings

| id | finding |
| --- | --- |
| **F-0** | **`.planning/` was NOT clean at arrival.** The prompt's baseline recorded 0 entries; the execute-phase orchestrator had rewritten `.planning/STATE.md` at dispatch. Since every `<automated>` block asserts `git status --porcelain .planning/` EMPTY numerically, this was resolved (Rule 3) by committing the orchestrator's own bookkeeping as its own housekeeping commit `868c3ff` before the first edit. No CORE-11 byte moved. |
| **F-1** | The gate file's length is 11,071 (`wc -l` and `awk END{NR}` agree). `01-VERIFICATION.md`'s **11,079 is wrong**. `01-REVIEW.md`'s 11,072 is not an error — it is `gateLines.length`, confirmed in-suite. Carried forward: 11,071. |
| **F-2** | Pass 10's and the review's raw-span widths are computed EXCLUSIVELY (`hi − lo`). The inclusive span 419..1574 is **1,156**, not 1,155. Reproduces on all four review rows — one defect, not four. |
| **F-3** | Pass 10's five-anchor list and the review's four-row table do not enumerate the same anchors; neither is the other's top-N. The measured top-ten reconciles both. |
| **F-4** | The plan and pass 10 give the machine-owned span as `/*` `:952` … `*/` `:1565`. Those are the comment DELIMITERS; `EXCLUSIONS` bounds by the SENTINELS at **983..1564 = 582 lines**. Only the measured 582 makes 1156 − 582 = 574 close exactly; the plan's implied 614 gives 542 and does not. |
| **F-5** | **Mutation two contradicted its prediction.** The ~1,130-line relocation went RED, not GREEN — but on a SHRINK (574→567) caused by the destination inserting a recogniser-(5)-accepted line that split the shadow, NOT by detecting a relocation. All 439 pre-existing cases stayed green, reproducing pass 10 exactly. Per the plan's pre-commitment the pin is **not** described as catching relocations; the required GREEN was obtained separately, from pass 10's own 514-line relocation, at 440/440. |
| **F-6** | **The plan's own earlier draft misquoted the file** — it claimed both surfaces close on the same words. Re-measured: `grep -c 'interchangeability residual is that wide'` = **1** (`:9093`, surface A only) and `grep -c 'got 234 again'` = **1** (`:9345`, surface B only). Surface B is a different sentence making the same wrong-quantity inference for a different stated reason (it offers 234 as the proximity-versus-containment measure, under a heading claiming that difference is `MEASURED RATHER THAN ESTIMATED`). Recorded because a plan whose subject is the accuracy of quoted claims must not misquote the file it corrects — the phase's signature defect one level up in the toolchain. The shipped plan carries the correction; this row records that it was verified against the bytes rather than trusted. |
| **F-7** | **An unlisted hit: the positive site-identity claim appears TWICE**, not once — in the null-anchor case's failure message (`:10282`, the only place the plan and pass 10 name) *and* in the comment block four lines above it. Retiring only the message would have left the claim standing. Both were retired. |
| **F-8a** | The plan's floor attributes *"an anchor that travelled with the sentence and named no site"* to `:9416`; it is at **`:9356`**, and `:9416` carries a **different** negative statement. Two statements, not one. |
| **F-8b** | The plan's floor gives `:10337`; the statement is at **`:10331`**. |
| **F-9** | **A defect authored by this round, in this round's own retirement bracket, and caught by the per-sentence re-read.** The bracket first read *"the two negative statements about anchors naming no site"* — the enumeration found **five**. A wrong count inside the paragraph retiring an overclaim would have been the eleventh instance of the phase's signature defect. Corrected in `216ede3`; all five are now named individually and each shown byte-identical. |

**Prediction-vs-measurement discrepancies:** two — **F-5** (mutation two predicted GREEN, measured RED, mechanism diagnosed and the correct GREEN obtained separately) and the temporary case's first run reporting `SURFACE_LINES.length=8905` against the 8,846 arrival (cause: the gate reads its own bytes; resolved by self-exclusion and confirmed twice, `8921 − 75 = 8846`). All other predictions matched exactly, including mutation one's `574 + 7 = 581`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `.planning/` dirty at arrival**
- **Found during:** Task 1 precondition
- **Issue:** The orchestrator's dispatch-time STATE.md write left `.planning/` with one modified entry, which would fail the `PLANNING DIRTY` assertion in every `<automated>` block in both tasks.
- **Fix:** Committed the orchestrator's own bookkeeping as a separate housekeeping commit before the first edit.
- **Files modified:** `.planning/STATE.md`
- **Verification:** `git status --porcelain .planning/ | grep -c .` → 0 at both task boundaries.
- **Committed in:** `868c3ff`

**2. [Rule 1 - Bug] The temporary measurement case contaminated its own measurement**
- **Found during:** Task 1
- **Issue:** The gate reads its own bytes via `readFileSync`, so the temporary case's 59 lines entered `SURFACE_LINES` and it reported 8905 against an 8,846 arrival.
- **Fix:** The case locates its own range by marker text and excludes it. An initial off-by-one (`selfFrom - 1` swallowed a genuine surface line, giving 8845) was found and corrected.
- **Verification:** `CLEAN surface = 8846`, cross-checked by `8921 − 75 = 8846`.
- **Committed in:** not committed — temporary, removed and proved gone by `git diff --exit-code`.

**3. [Rule 1 - Bug] This round's own retirement bracket stated the wrong count**
- **Found during:** Task 2, per-sentence no-overclaim re-read
- **Issue:** "the two negative statements" where the bounded enumeration found five.
- **Fix:** Corrected to five, each named individually and each shown byte-identical to `483a8ee`.
- **Verification:** the five `base=1 work=1` pairs above; suite green at 440.
- **Committed in:** `216ede3`

**4. [Scope] Mutation two run twice rather than once**
- **Found during:** Task 1
- **Issue:** The prescribed relocation went RED (F-5). The plan forbids adjusting a mutation to force a result, and requires an executed GREEN showing what the pin does not catch.
- **Fix:** The prescribed mutation was kept, observed, diagnosed and written up unchanged; a *second*, separately predicted relocation — pass 10's own 514-line move — supplied the GREEN. Both are recorded in the bytes.
- **Verification:** 2-A RED at 567 with the mechanism explained; 2-B GREEN at 440/440. Both restored, both `git diff --exit-code` clean.
- **Committed in:** `6ad8a81` (the paragraph recording both)

---

**Total deviations:** 4 (1 blocking, 2 bugs, 1 scope expansion required by an executed counter-result)
**Impact on plan:** No scope creep. Deviation 4 strengthens the plan's own success criterion rather than relaxing it: what the pin does not catch is now executed twice — once where a relocation reported for an explained non-relocation reason, and once where it did not report at all.

## Known Stubs

None. No hardcoded empty value, placeholder, TODO or unwired component was introduced. The pinned constant `WIDEST_ANCHOR_SHADOW = 574` is a measured value, not a stub, and it was watched failing.

## Issues Encountered

- Mutation two's outcome inverted its prediction (F-5). Resolved by diagnosing the mechanism to the exact seven lines before writing any description of it, and by obtaining the required GREEN from a separately predicted relocation rather than by adjusting the mutation.
- Four of the plan's reconciliation anchors (F-2, F-4, F-8a, F-8b) and one of `01-VERIFICATION.md`'s figures (F-1) disagreed with the tree. Every disagreement was recorded as a finding before anything was adjusted, and in each case the measured value is the one carried forward.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **Plan 01-43 (CR-23, WR-55, WR-58, WR-60, closing baseline) is unblocked.** The round's baseline file is at `${TMPDIR}/defminer-round10-baseline` with `BASE_GATE_ARRIVAL=439` and `BASE_TESTS_ARRIVAL=1379` **unchanged**, exactly as plan 01-43's `-gt ARRIVAL` assertions require, and `BASE_GATE_CURRENT=440` / `BASE_TESTS_CURRENT=1380` recording this plan's boundary.
- **Plan 01-43 should note F-1** when it re-derives the file length for WR-55's replacement text: the measured count is **11,071**, WR-55's prescribed text carries 11,072, and 11,072 is `gateLines.length` rather than `wc -l`.
- **Left for verification pass 11, and untouched here:** CORE-11's checkbox (`[ ]`, both surfaces byte-identical), whether the corrected disclosure discharges criterion (3)'s mechanism leg, and whether exit 2 as executed satisfies SOLE BOUND. `requirements mark-complete` was not run.
- **Open and disclosed, not closed by this plan:** the relocation class. A shipped occurrence can still be relocated inside a 574-line shadow for a byte-identical key at a fully green suite — executed in this session at 440 of 440 — the pin bounds only the MAXIMUM shadow, non-maximal shadows may grow up to it unreported, nothing inside the three exclusions is reached, and the anchor remains a proximity rather than a containment.

---
*Phase: 01-skeleton-persistence-compatibility*
*Completed: 2026-08-26*

## Self-Check: PASSED

- `packages/backend/src/outbound-prohibition.spec.ts` — FOUND
- `.planning/phases/01-skeleton-persistence-compatibility/01-42-SUMMARY.md` — FOUND
- Commits `868c3ff`, `5a60ec2`, `6ad8a81`, `013ea74`, `216ede3`, `9980ea8`, `bebc3e2` — all FOUND
- `pnpm test` — Test Files 31 passed (31), Tests 1380 passed (1380), rc=0
- `pnpm exec tsc --build` — rc=0
- gate file alone — Tests 440 passed (440)
- `git diff --exit-code -- <spec>` — SPEC CLEAN
- `.planning/` — clean

**One untracked entry left deliberately:** `.gsd/dispatch-isolation-sentinel.json`, created by the
execute-phase orchestrator as harness runtime state. It is out of this plan's scope (`packages/`,
`scripts/`, `.planning/`), is asserted by no gate in this plan, and was not created by either task,
so it was neither committed nor gitignored — logged here rather than acted on.
