---
phase: 01-skeleton-persistence-compatibility
plan: 37
type: execute
wave: 37
gap_closure: true
depends_on: [01-36]
files_modified:
  - packages/backend/src/outbound-prohibition.spec.ts
  - .planning/REQUIREMENTS.md
# THIS PLAN DOES TWO THINGS AND THE SECOND ONE IS A MEASUREMENT WHOSE RESULT NOBODY MAY PREDICT.
#
# TASK 1 IS THE OTHER HALF OF CR-17, AND IT IS MANDATORY RATHER THAN OPTIONAL. Plan 01-36 anchored the
# exemption key to its construct. The verifier's own words about what that is worth without a fixture:
# "a guard against relocation that has never been watched failing is the same species of unwatched
# assertion this file has spent nine waves removing." The executed mutation — the ASCII-table cell
# deleted and a fabricated hand-written BOUND planted 9,001 lines away, outside all three exclusions,
# passing 432 of 432 — becomes a PERMANENT failing-path fixture with its counter-probe beside it, in
# the probe-and-counter-probe shape wave 34 established for measured silences.
#
# TASK 2 IS WR-48 AND IT IS EXPLICITLY A MEASUREMENT. `closingBracketAfter` matches the exact line
# `"]);"`. `RESOLVER_REGISTRY` opens at `:4135` and closes at `:5767` with
# `] as readonly ResolverRecord[]);`, which does not match, so the scan runs on to the next exact
# `]);` at `:5884`. I confirmed all four line numbers during planning. Exclusion three is therefore
# `4135..5884` and swallows 117 lines (`5768..5884`) that are the `BRANCH_VOCABULARY` docblock and
# declaration — a DIFFERENT construct, for which the exclusion's `name` ("the RESOLVER_REGISTRY
# declaration") and its `why` ("its clause strings are already obligated") are both FALSE. The band
# `[500, 3000]` does not catch it: 1,750 lines is inside the band.
#
# IT ERRS SAFE AND THAT IS WHY IT IS A WARNING AND NOT A BLOCKER. The verifier planted a declared
# phrasing at `:5800`, inside those 117 lines, and the suite went RED — the `inRange == inClauses`
# equality covers them at the same reach as the main guard. So the residue is real, the description is
# wrong, and the enforcement is not.
#
# NARROWING EXCLUSION THREE PUTS 117 LINES BACK ON THE GUARDED SURFACE AND MAY RAISE NEW OBLIGATIONS.
# THE VERIFIER'S INSTRUCTION, IN ITS OWN WORDS: that is "the honest direction and should be taken as a
# MEASUREMENT rather than predicted." NO ACCEPTANCE CRITERION IN THIS PLAN NAMES AN EXPECTED NUMBER OF
# NEW OBLIGATIONS. The task's output is whatever the measurement says, disposed of by the guard's own
# three-choice rule, and a count of zero and a count of ten are equally successful outcomes.
#
# THE THIRD LEG OF WR-48 IS A SENTENCE, NOT A NUMBER. `.planning/REQUIREMENTS.md:154` — row 3 of wave
# 35's three-row discharge table — states "zero residue in any exclusion". That is TRUE OF PHRASINGS
# and FALSE OF LINES, and the verifier asked for it to be re-stated rather than left to be
# re-discovered a ninth time. It is superseded IN PLACE with a scoped marker, the bytes left intact,
# exactly as `:110` marks `:112`.
#
# WHAT THIS PLAN MUST NOT DO.
#   - DO NOT FLIP CORE-11's CHECKBOX. It is `[ ]` and that is correct; the box is verification pass 9's
#     call after all four findings are closed. `git diff` over the checkbox is EMPTY at both task
#     boundaries.
#   - DO NOT RE-OPEN THE 2026-08-25 RE-SCOPE. The verifier tested it for a quietly-lowered bar and
#     cleared it. The `[x]` is unearned because criterion (3) is unmet, not because the bar is wrong.
#   - DO NOT TOUCH `.planning/REQUIREMENTS.md:46`, `:60`, `:158` OR `:160`, AND DO NOT TOUCH
#     `.planning/STATE.md`. Those are CR-18 and CR-19 and belong to plan 01-38. This plan's only ledger
#     edit is a scoped supersession marker over row 3's evidence cell at `:154`.
#   - DO NOT WEAKEN THE OUTBOUND GATE TO GET GREEN. Narrowing exclusion three is the only permitted
#     direction; widening any exclusion, adding a band, or minting an exemption for this wave's own
#     prose is forbidden. If a new sentence trips the guard, REWRITE THE SENTENCE.
#
# ENVIRONMENT FACTS, MEASURED DURING PLANNING SO THE EXECUTOR DOES NOT RE-DERIVE THEM:
#   - HEAD at planning time was `4105fd0`; plan 01-36 has committed on top of it. Cross-check ANY
#     git-status snapshot against `git rev-parse HEAD` before trusting it.
#   - NO ACTIVE GIT HOOKS. Run the gate set by hand at every task boundary.
#   - COMMIT BEFORE MUTATING. Wave 29 destroyed its own Task 1 with `git checkout --`.
#   - Every exact `]);` line in the gate file at planning time: 1568, 1601, 1617, 1631, 2119, 2220,
#     2239, 2248, 2320, 2347, 2353, 5884, 5969, 6010. Exclusion TWO resolves `5959 -> 5969` and MUST
#     STILL RESOLVE THERE after any change to `closingBracketAfter`.
#   - `BRANCH_VOCABULARY` is declared at `:5789` and closes at `:5884`. That is the construct exclusion
#     three currently swallows the tail of.
#   - Wave 35's independently re-measured counts, for reconciliation: 58 line-based and 62
#     joined-and-prefix-stripped declared-phrasing occurrences; surface 29 against 29 exemption
#     entries; exclusion one 12; exclusion two 9; exclusion three's line range 12 against 12 live
#     clause occurrences. Plan 01-36 changes the KEY FORMAT, not the occurrence count, so the
#     occurrence numbers are expected to survive it — and a disagreement is a finding.
#   - Line numbers in this header are HEAD-`4105fd0` values. Plan 01-36 edits the same file, so
#     RE-DERIVE every one by anchor rather than trusting the number.

autonomous: true
requirements: [CORE-11]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 2
  confidence: low
# `estimate-calibration` reports `{"factor": 1, "applied": false, "sample_count": 0, "min_samples": 3}`,
# so `tokens` equals `raw_tokens` and the confidence is DERIVED from the sample count, never self-rated.
# Weigh the MEASUREMENT COUNT over the token figure: one permanent fixture with a counter-probe, one
# executed relocation mutation on the real tree, one exclusion re-measurement whose result is unknown
# at planning time, and one scoped supersession marker.

must_haves:
  truths:
    - "THE RELOCATION IS A PERMANENT FAILING-PATH FIXTURE, NOT A MUTATION SOMEBODY RAN ONCE. A named case reproduces the verifier's executed mutation over SYNTHETIC lines: a declared phrasing sitting under construct A with an exemption written for it, then the same phrasing relocated under construct B. The case asserts that the anchored key builder produces a DIFFERENT key, so the discharge computation yields a NON-EMPTY `missing` and a NON-EMPTY `stale`. A guard against relocation that has never been watched failing is the same species of unwatched assertion this file has spent nine waves removing, and that sentence is the verifier's."
    - "THE FIXTURE CARRIES ITS COUNTER-PROBE, IN THE SHAPE WAVE 34 ESTABLISHED FOR MEASURED SILENCES. The same synthetic relocation is run through the PRE-ANCHORING key builder — masked line anchor plus `:: qN`, retained as a fixture-only helper named so that nobody mistakes it for the live one — and the case asserts that under THAT builder the key is IDENTICAL across the relocation, `missing` and `stale` are both EMPTY and the count balances. The probe shows the new mechanism catching it; the counter-probe shows the old mechanism not catching it; together they prove the anchoring is what makes the difference rather than asserting it."
    - "THE REAL-TREE RELOCATION IS EXECUTED ONCE IN THIS SESSION, WATCHED GOING RED, AND RESTORED. The ASCII-table cell whose normalized content is exactly the declared phrasing at index 2 is deleted, and a fabricated hand-written BOUND carrying that phrasing is planted at a line OUTSIDE all three exclusions and thousands of lines away. It is planted as a SWAP — one deleted, one added — because the count equality is what forced the original bypass to balance, and a mutation that only adds proves a different and easier thing. The suite is observed RED, the failing title and FULL assertion message pasted, the tree restored and `git diff --exit-code` shown clean."
    - "EVERY SYNTHETIC DECLARED PHRASING IS BUILT BY REFERENCE, NEVER SPELLED. The fixture's strings take their phrasing from `UNBOUNDED_QUANTIFIERS` by index. This file's guard scans the file's own bytes, so a fixture that writes the phrasing as a literal raises the obligation it is testing — the same reasoning the key masking already rests on, and the single most likely way this work turns the suite red for a reason that is not a finding."
    - "`closingBracketAfter` MATCHES THE CONSTRUCT IT IS ASKED ABOUT, AND EXCLUSION TWO IS PROVED UNMOVED BY THE CHANGE. It currently matches the exact line `\"]);\"`, so it walks straight past `RESOLVER_REGISTRY`'s real closing line and lands 117 lines later on the close of a different declaration. After the fix it resolves the registry to its own closing line. `closingBracketAfter` is used by exclusion TWO as well, so exclusion two's resolution is re-measured and shown UNCHANGED — a fix that silently re-points a second exclusion while correcting the first is the same defect one construct over."
    - "EXCLUSION THREE'S TRUE RANGE IS MEASURED AND PASTED, BEFORE AND AFTER, WITH THE 117 LINES NAMED FOR WHAT THEY ACTUALLY ARE. The before range, the after range, the line count in each, the band each falls in, and the identity of the lines that leave the exclusion — the `BRANCH_VOCABULARY` docblock and declaration, a DIFFERENT construct, for which the exclusion's `name` and its `why` were both false. The exclusion's `name`, `why` and `proof` are re-read against the range it actually resolves to and corrected where they are false."
    - "WHAT THE 117 RESTORED LINES RAISE IS A MEASUREMENT, AND THIS PLAN NAMES NO EXPECTED NUMBER. Narrowing the exclusion puts those lines back on the guarded surface. Whatever obligations that raises are enumerated and disposed of by the guard's OWN three-choice rule — DELETE the sentence if it states a bound on the walk's reach, REWRITE it to say what the branch does without the universal, or add a NAMED exemption if it is a quotation, a title, an assertion message, a table label or a `QUANTIFIED_CLAUSES` value. A count of zero and a count of ten are equally successful outcomes; a count that was predicted rather than measured is not."
    - "ANY NEW EXEMPTION MINTED BY THAT DISPOSAL CARRIES A CONSTRUCT ANCHOR AND A REASON A REVIEWER CAN DISAGREE WITH. Plan 01-36's anchored key format applies to every entry added here, the fully-masked-anchor prohibition applies to every entry added here, and each reason states WHICH of the three permitted kinds the occurrence is. An exemption is a sentence a later author must keep true, so it is a cost; the SUMMARY states how many were spent and why each one was cheaper than deleting or rewriting the sentence."
    - "THE `inRange == inClauses` EQUALITY IS RE-MEASURED AGAINST THE NARROWED RANGE AND STAYS LIVE. That equality is what made this defect err safe; narrowing the range changes both what it counts and what it means. Its two numbers are pasted before and after, and the pin is NOT widened to accommodate the new range — if the two numbers disagree, that disagreement is the finding and it is written down before anything is adjusted."
    - "THE DISCHARGE'S `zero residue in any exclusion` IS RE-STATED AS THE PHRASING-LEVEL CLAIM IT ACTUALLY IS, IN PLACE AND SCOPED. `.planning/REQUIREMENTS.md:154` carries it inside row 3's evidence cell. A scoped supersession marker names WHICH clause it reaches, states that the claim is true of PHRASINGS and false of LINES, cites WR-48 and this wave's measurement, and states that the rest of the row stands. The superseded bytes are LEFT INTACT below the marker — this ledger's own precedent for falsified text — and `grep -c 'zero residue in any exclusion' .planning/REQUIREMENTS.md` still returns 1."
    - "THE ENFORCEMENT ERRED SAFE AND THIS PLAN SAYS SO WITHOUT USING IT AS AN EXCUSE. The verifier planted a declared phrasing inside the 117 lines and the suite went RED, so no bound was ever laundered through this defect and nothing leaked. That is why WR-48 is a warning. It is not a reason to leave a false `name` and a false `why` standing on an exclusion, because the next reader takes an exclusion's description at face value and this file's whole discipline is that a description match what it describes."
    - "NOT ONE THING OUTSIDE WR-48's SCOPE MOVES IN THE GATE FILE. The exemption map's 29 pre-existing entries, their reasons, plan 01-36's key format, the fully-masked-anchor case, exclusion one, exclusion two's resolved range, both pinned counts, `CORE11_BOX_EXPECTED` and the box case are all shown byte-unchanged or re-measured unchanged by diff. Narrowing an exclusion is the permitted direction; nothing else moves under cover of it."
    - "THE MUST-NOT DOES NOT MOVE AND NOTHING LEAKS, RE-ESTABLISHED BY EXECUTION. CORE-11's first sentence byte-identical by diff; `pnpm test` exit 0 with the gate walking 23 files over both `SOURCE_ROOTS` at ZERO violations; `tsc --build` exit 0; the shipped bundle's entire import set ONE specifier, `crypto`. Every finding this plan touches is a defect in a test-only gate's description of itself or one sentence in a planning document."
    - "NO SENTENCE ADDED ANYWHERE BY THIS PLAN STATES OR IMPLIES THAT THE GATE IS COMPLETE, THAT THE RELOCATION CLASS IS CLOSED, OR THAT THE EXCLUSIONS ARE NOW EXACT. Narrowing one exclusion to its real construct removes one false description. It does not make the other two exact, it does not widen the guard's phrase list, it does not reach undeclared spellings, and it does not reach the unguarded files. Every surface stating what this wave did states its reach in the same breath."
    - "EVERY PREDICTION-VS-MEASUREMENT DISCREPANCY IS RECORDED RATHER THAN ABSORBED, in the shape `01-27-SUMMARY.md` uses. This plan predicts the before range `4135..5884`, the after range ending at the registry's own closing line, exclusion two unmoved at `5959..5969`, and the occurrence counts surviving plan 01-36's key-format change. It predicts NOTHING about how many obligations the 117 restored lines raise. If any prediction is wrong, THE DIFFERENCE IS THE FINDING and it is written down before the work continues."

  artifacts:
    - path: "packages/backend/src/outbound-prohibition.spec.ts"
      provides: "A named failing-path fixture reproducing the relocation over synthetic lines with its pre-anchoring counter-probe beside it; `closingBracketAfter` corrected so the registry resolves to its own closing line with exclusion two proved unmoved; exclusion three's `name`, `why` and `proof` corrected against the range it actually resolves to; and whatever exemption entries the 117 restored lines require, each anchored and each reasoned"
      contains: "closingBracketAfter"
    - path: ".planning/REQUIREMENTS.md"
      provides: "A scoped supersession marker over row 3's `zero residue in any exclusion` clause at `:154`, naming which clause it reaches, stating the claim is true of PHRASINGS and false of LINES, citing WR-48 and this wave's measurement, and stating that the rest of the row stands — with the superseded bytes left intact below it"
      contains: "zero residue in any exclusion"
  key_links:
    - from: "plan 01-36's construct anchoring"
      to: "a permanent fixture that has been watched failing"
      via: "a synthetic relocation asserted RED under the anchored builder and asserted INVISIBLE under the pre-anchoring builder, probe and counter-probe in one case"
      pattern: "exemptionKeyFor"
    - from: "`closingBracketAfter`'s exact-string match"
      to: "exclusion three's real end at the registry's own closing line"
      via: "a matcher that recognises the registry's actual closing form or brackets rather than compares strings, with exclusion two's resolution re-measured and shown unchanged"
      pattern: "closingBracketAfter"
    - from: "117 lines returning to the guarded surface"
      to: "an enumerated, disposed obligation set of unknown size"
      via: "the guard's own three-choice rule applied occurrence by occurrence, with every minted exemption carrying a construct anchor and a reason"
      pattern: "HEADER_QUANTIFIER_EXEMPTIONS"
    - from: "the discharge's phrasing-level claim stated as a line-level one"
      to: "`.planning/REQUIREMENTS.md:154`"
      via: "a scoped supersession marker in the shape `:110` uses for `:112`, bytes left intact"
      pattern: "zero residue in any exclusion"
  prohibitions:
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "No acceptance criterion in this plan names an expected count of new obligations; the measurement's output is pasted and disposed of whatever it is"
      statement: "MUST NOT predict the result of the exclusion re-measurement. The verifier asked for it as a measurement rather than a prediction, in those words. A plan that bakes an expected number into its acceptance criteria converts an honest measurement into a target, and a target is what an executor engineers toward when the number disagrees."
    - requirement_id: CORE-11
      category: security
      status: resolved
      verification: gate
      resolution: "Narrowing is the only permitted direction; `git diff` over exclusion one, over both bands, over exclusion two's resolved range and over both pinned counts shown EMPTY or re-measured unchanged"
      statement: "MUST NOT weaken the outbound gate in the act of correcting its description. A change to `closingBracketAfter` or to an exclusion's anchors could move a second exclusion or widen a first. Exclusion two's resolution is re-measured and shown unchanged, and no band, no proof token and no pinned count is adjusted to accommodate anything this wave does."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "New prose that trips wave 33's guard is REWRITTEN and the before and after are both pasted; no exclusion is widened and no exemption is minted for this wave's own text"
      statement: "MUST NOT satisfy a guard by widening it. Wave 34 forbade this once already. An exemption minted for the plan's own prose, or an exclusion stretched over it, turns a guard into decoration in one commit while every assertion stays green."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "The synthetic fixture's phrasings are taken from `UNBOUNDED_QUANTIFIERS` by index; the whole-file guard's green run is the evidence"
      statement: "MUST NOT spell a declared phrasing as a literal in new code. The guard scans this file's own bytes, so a fixture that writes the phrasing raises the obligation it exists to test. Build it by reference, exactly as the key masking already does and for the same reason."
    - requirement_id: CORE-11
      category: safety
      status: open
      verification: gate
      resolution: "DISCLOSED, NOT CLOSED — the two remaining exclusions' coarseness, the guard's phrase-list reach, its normalization and both classes of unreached surface are named wherever this wave's result is stated"
      statement: "MUST NOT state or imply that the exclusions are now exact or the relocation class closed. One false description is corrected. The guard is still a phrase list over bytes under one named normalization; undeclared spellings and the unguarded files are still unreached; two occurrences under the same construct are still interchangeable. A `✓` that omits any of those is a stated reach exceeding an executed one."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "`git diff` over CORE-11's checkbox EMPTY; `git diff` over `.planning/STATE.md` EMPTY; `git diff` over `.planning/REQUIREMENTS.md` restricted to an insertion-only marker above row 3's evidence cell"
      statement: "MUST NOT flip CORE-11's checkbox and MUST NOT reach CR-18's or CR-19's surfaces. The box is `[ ]` and belongs to verification pass 9; rows `:46`, `:60`, `:158` and `:160` and every line of `.planning/STATE.md` belong to plan 01-38. Two waves editing one 23,290-byte line is how a correction loses its attribution."
---

<objective>
Give CR-17's anchoring the failing-path fixture that makes it trustworthy, then correct WR-48 —
`closingBracketAfter`'s string match, exclusion three's false description, and the 117 lines it
swallows — and measure what returning those lines to the guarded surface actually raises.

Purpose: plan 01-36 anchored the exemption key to its construct. An anchoring nobody has watched
failing is the same artifact as a claimed check nobody performs, and this file has spent nine waves
removing exactly that. Task 1 makes the verifier's executed mutation permanent, with a counter-probe
that shows the pre-anchoring builder NOT catching it — so the fixture proves the anchoring is what
makes the difference instead of asserting it.

Then WR-48. `closingBracketAfter` matches the exact line `"]);"`, so it walks past
`] as readonly ResolverRecord[]);` and lands 117 lines later on the close of `BRANCH_VOCABULARY`.
Exclusion three's `name` and `why` are both false of those 117 lines, and the band does not catch it.
The enforcement errs safe — the verifier planted a declared phrasing inside them and the suite went red
— so nothing was laundered and nothing leaked. That is why this is a warning. It is not a reason to
leave a false description standing on an exclusion in a file whose whole discipline is that a
description match what it describes.

Output: a permanent relocation fixture with its counter-probe; a `closingBracketAfter` that resolves
the registry to its own closing line with exclusion two proved unmoved; exclusion three's range,
`name`, `why` and `proof` corrected against what it actually covers; a pasted before-and-after
measurement of the range and of the `inRange == inClauses` equality; whatever obligations the 117
restored lines raise, enumerated and disposed of by the guard's own rule; and a scoped supersession
marker re-stating `zero residue in any exclusion` as the phrasing-level claim it is.

THE SECOND HALF IS A MEASUREMENT AND THIS PLAN NAMES NO EXPECTED RESULT FOR IT. The verifier asked for
it that way in those words. Zero new obligations and ten new obligations are equally successful
outcomes of this plan; a number that was predicted rather than measured is not.

WHAT THIS PLAN DOES NOT DO. It does not flip CORE-11's checkbox — that is verification pass 9's call.
It does not re-open the 2026-08-25 re-scope. It does not touch `.planning/REQUIREMENTS.md:46`, `:60`,
`:158` or `:160`, or any line of `.planning/STATE.md` — those are plan 01-38's. And it does not claim
that narrowing one exclusion makes the other two exact or the relocation class closed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md
@.planning/phases/01-skeleton-persistence-compatibility/01-36-PLAN.md
@packages/backend/src/outbound-prohibition.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: CR-17's failing-path fixture — the relocation reproduced over synthetic lines with its pre-anchoring counter-probe, and executed once on the real tree and watched going red</name>

  <precondition>
    Plan 01-36 has executed and its artefacts are on disk: `01-36-SUMMARY.md` exists, `exemptionKeyFor`
    produces the anchored key format, and the fully-masked-anchor case is present in
    `packages/backend/src/outbound-prohibition.spec.ts`. Assert before the first edit that `pnpm test`
    exits 0 and that both byte comparisons are green, and HALT if either is red — a fixture written on
    top of a red tree cannot tell its own failure from the one that was already there. Assert
    `git status --porcelain packages/ .planning/` is clean and cross-check any status snapshot against
    `git rev-parse HEAD`. This repo has NO active git hooks, so nothing runs the gate set for you at a
    commit boundary.
  </precondition>

  <read_first>
    - `01-36-SUMMARY.md` in full, and in particular its table of all 29 occurrences with the construct
      each resolved to, its three before/after key examples, and the fifth numbered LIMIT it added to
      the guard's docblock. This task's fixture is written against THAT key format; a fixture written
      against the format in this plan's prose would be written against a prediction.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the construct-anchor helper,
      `exemptionKeyFor`, `maskQuantifiers`, `normalizeGateLine`, `joinGateLines`,
      `quantifierOccurrences` and `surfaceExemptionKeys`, wherever plan 01-36 left them. Re-derive
      their line numbers by anchor; plan 01-36 edited this file and every number in this plan's header
      is a HEAD-`4105fd0` value.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the three discharge checks: `missing`,
      `stale` and the count equality. The fixture asserts against the SAME computation these three
      perform, over synthetic input, so read exactly how each is derived from `found` and `declared`.
    - `packages/backend/src/outbound-prohibition.spec.ts` `:9769` at HEAD-`4105fd0`, the case titled
      for `extractDerivedBlock` throwing a named error against synthetic text. THIS IS THE PRECEDENT
      AND THE SHAPE TO COPY: a failing path executed against a synthetic array rather than by mutating
      the real file. Read it before designing anything.
    - `packages/backend/src/outbound-prohibition.spec.ts` `:5959-5969` at HEAD-`4105fd0` — the
      `UNBOUNDED_QUANTIFIERS` declaration and its body, so you know which index carries which phrasing.
      Index 2 is the one the verifier's mutation used.
    - `packages/backend/src/outbound-prohibition.spec.ts` `:428-442` at HEAD-`4105fd0` — the ASCII
      table containing the cell whose normalized content is exactly the index-2 phrasing. Re-locate it
      by content, not by line number.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md` `:136-160` — the
      CR-17 section: the executed mutation, the 432/432 result, the three-row table of which check
      consults the line, and the paragraph explaining that the count equality forces a swap rather than
      an addition. The fixture reproduces THAT mutation, so read what it actually was.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-34-SUMMARY.md`'s measured-silence rows
      for the PROBE-AND-COUNTER-PROBE shape: an executed probe showing the behaviour, and an executed
      counter-probe one position over showing the contrast. This fixture uses that shape and should
      read like it.
  </read_first>

  <files>packages/backend/src/outbound-prohibition.spec.ts</files>

  <reversibility rating="reversible">A test-only fixture and one fixture-only helper, added to a spec file. No shipped code, no behaviour, no requirement text and no exclusion is touched. If the fixture proves brittle it is one revert away, and the executed real-tree mutation is restored inside the same task with `git diff --exit-code` shown clean.</reversibility>

  <action>
    BUILD THE FIXTURE OVER SYNTHETIC LINES, IN THE SHAPE THE FILE ALREADY USES FOR FAILING PATHS. The
    `extractDerivedBlock` case that throws against synthetic text is the precedent: a failing path is
    executed against a constructed array, not by mutating the real file, so the assertion is permanent
    and the tree stays clean. Construct a small array of lines containing TWO distinct constructs — a
    declaration and a fixture title, or two declarations with different names — with an occurrence of a
    declared phrasing sitting under the first. Compute the anchored key for it. Then build the SAME
    array with the occurrence relocated under the SECOND construct, compute the anchored key again, and
    assert the two keys DIFFER.

    THEN RUN THE DISCHARGE COMPUTATION OVER BOTH, BECAUSE A DIFFERENT KEY IS ONLY THE MECHANISM AND THE
    OUTCOME IS WHAT MATTERS. With a declared map holding exactly the FIRST key, assert that the
    relocated array yields a NON-EMPTY `missing` and a NON-EMPTY `stale` — the relocated occurrence has
    no entry, and the entry written for the original position matches nothing. Assert the count
    equality still balances, because that is precisely what let the original bypass through: the totals
    agreeing is not evidence of anything, and the fixture should demonstrate that rather than let a
    later reader assume it.

    ADD THE COUNTER-PROBE, AND THIS IS THE HALF THAT MAKES THE FIXTURE PROVE SOMETHING. Keep the
    PRE-ANCHORING key builder — masked normalized line anchor plus `:: qN` — as a small helper named
    unmistakably as fixture-only, with a docblock saying it is the superseded format retained to
    demonstrate what it missed and must never be used by live code. Run the same synthetic relocation
    through it and assert the OPPOSITE: the key is IDENTICAL across the relocation, `missing` is EMPTY,
    `stale` is EMPTY, and the count balances — the relocation is invisible. Probe and counter-probe in
    one case is the shape wave 34 used for its measured silences, and it is the difference between
    "the new mechanism catches this" and "the new mechanism catches this and the old one did not".

    BUILD EVERY SYNTHETIC PHRASING BY REFERENCE, NEVER AS A LITERAL. Take the phrasing from
    `UNBOUNDED_QUANTIFIERS` by index. This file's guard scans its own bytes; a fixture that spells the
    phrasing raises the obligation it exists to test. The same applies to the two synthetic construct
    headers if either would otherwise contain a declared phrasing.

    NAME THE FIXTURE FOR WHAT IT PROVES AND WHAT IT DOES NOT. Its title and its failure messages state
    that it demonstrates a CROSS-CONSTRUCT relocation being caught, and the docblock beside it repeats
    the fifth numbered LIMIT plan 01-36 added: two occurrences under the SAME construct remain
    interchangeable behind a positional ordinal, so this fixture proves the cross-construct case and
    not the general one. A fixture whose name implies more than it executes is this phase's signature
    defect wearing a test title.

    COMMIT, THEN EXECUTE THE REAL-TREE MUTATION ONCE. Paste `git log --oneline -1` first. Locate by
    CONTENT — never by the line number in this plan — the ASCII-table cell whose normalized content is
    exactly the index-2 phrasing, and delete it. Then plant a fabricated hand-written BOUND carrying
    that phrasing at a line OUTSIDE all three exclusions, computed from the exclusions' RESOLVED ranges
    at that moment, and thousands of lines from the cell. PLANT IT AS A SWAP — one occurrence removed,
    one added — because the count equality is what forced the original bypass to balance and a
    mutation that only adds proves a different and easier thing. Run, observe the suite RED, paste the
    failing title and the FULL assertion message, restore both edits, and cross-check with
    `git diff --exit-code`. Record in the SUMMARY that the verifier ran this same swap before the
    anchoring existed and got 432 passed of 432, citing `01-VERIFICATION.md` for that prior result and
    presenting this session's RED as the contrast.

    IF THE REAL-TREE MUTATION DOES NOT GO RED, STOP AND WRITE IT DOWN BEFORE DOING ANYTHING ELSE. That
    outcome would mean plan 01-36's anchoring does not reach the shape it was built for, and it is a
    finding of the first order — larger than anything else in this round. Do not adjust the mutation
    until it goes red; diagnose why the key did not change and report it.

    TOUCH NO EXCLUSION, NO BAND, NO PROOF TOKEN, NO PINNED COUNT AND NO EXISTING EXEMPTION ENTRY.
    `closingBracketAfter` is Task 2's. If the new fixture's own prose trips wave 33's guard, REWRITE
    THE PROSE.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY. State the outbound suite's new test count and state
    the floor as `no fewer than`, never as an equality.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/DefMiner && pnpm vitest run packages/backend/src/outbound-prohibition.spec.ts 2>&1 | tail -6 && pnpm test 2>&1 | tail -8 && pnpm typecheck && git diff --exit-code -- packages/backend/src/outbound-prohibition.spec.ts && echo TREE-CLEAN</automated>
  </verify>

  <acceptance_criteria>
    - The precondition asserted and pasted BEFORE the first edit: `01-36-SUMMARY.md` present, `pnpm
      test` exit 0, both byte comparisons green, `git status --porcelain packages/ .planning/` clean,
      and `git rev-parse HEAD` pasted beside any status snapshot used.
    - The fixture exists as a NAMED `it(...)` and its title is quoted in the SUMMARY. The title names a
      CROSS-CONSTRUCT relocation and does not imply the general case.
    - The synthetic probe executed and pasted: the two anchored keys for the same occurrence under two
      different constructs shown side by side and shown to DIFFER, with the differing portion
      identified.
    - The synthetic probe's discharge outcome asserted and pasted: `missing` NON-EMPTY, `stale`
      NON-EMPTY, count equality still balancing. The SUMMARY states in one sentence why the balancing
      count is not evidence of anything.
    - The counter-probe executed and pasted: the same relocation under the PRE-ANCHORING builder
      yielding an IDENTICAL key, `missing` EMPTY, `stale` EMPTY. The fixture-only helper's name and its
      docblock are quoted, and the docblock says it is the superseded format retained to demonstrate
      what it missed and must never be used by live code.
    - Every synthetic declared phrasing shown to be taken from `UNBOUNDED_QUANTIFIERS` by index rather
      than written as a literal, and the whole-file guard run green afterwards with its output pasted.
    - The docblock beside the fixture repeats plan 01-36's fifth numbered LIMIT — two occurrences under
      the SAME construct remain interchangeable — and that sentence is quoted verbatim in the SUMMARY.
    - REAL-TREE MUTATION executed after the work was committed: `git log --oneline -1` pasted first;
      the ASCII-table cell located BY CONTENT with the line number it was found at pasted; the
      fabricated bound planted outside all three exclusions with the resolved exclusion ranges pasted
      to prove it; the mutation shown to be a SWAP — one occurrence removed, one added — with the
      before and after occurrence totals pasted; the suite run and observed RED; the failing title and
      the FULL assertion message pasted; both edits restored; `git diff --exit-code` shown clean.
    - The SUMMARY states, with `01-VERIFICATION.md` cited, that the verifier ran this same swap before
      the anchoring existed and observed 432 passed of 432, and presents this session's RED as the
      contrast.
    - If the real-tree mutation did NOT go red, the task stopped, the diagnosis is written up as a
      first-order finding, and no adjustment was made to the mutation to force a red.
    - `git diff` over `closingBracketAfter`, over the `EXCLUSIONS` array with its bands and proof
      tokens, over both pinned counts, and over all pre-existing exemption entries and their reasons
      shown EMPTY, output pasted.
    - `git diff` over `.planning/REQUIREMENTS.md` and `.planning/STATE.md` shown EMPTY — this task
      touches one file.
    - Executed `pnpm test` — exit 0, file and test counts pasted, no fewer than 31 files and 1372
      tests. The outbound suite's new count stated, no fewer than its post-01-36 count.
    - Executed `pnpm typecheck`, `pnpm lint`, `pnpm knip` — all exit 0, output pasted. `knip` in
      particular is run because this task adds a fixture-only helper.
    - Executed `pnpm build:backend && pnpm check:bundle` — exactly one import specifier, `crypto`.
    - Executed the real-tree run over both `SOURCE_ROOTS` — 23 files, 0 violations.
    - Any prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses, or an
      explicit statement that there were none.
  </acceptance_criteria>

  <done>
    The relocation that passed 432 of 432 is a permanent named fixture with its pre-anchoring
    counter-probe beside it; the same swap was executed once on the real tree and watched going red and
    the tree restored clean; and the fixture states in its own docblock which relocation class it
    proves and which it does not.
  </done>
</task>

<task type="auto">
  <name>Task 2: WR-48 — closingBracketAfter matches the construct it is asked about, exclusion three's range and description are corrected against what it actually covers, and what the 117 restored lines raise is measured rather than predicted</name>

  <read_first>
    - `packages/backend/src/outbound-prohibition.spec.ts` — `lineOf`, `closingBracketAfter` and the
      whole `EXCLUSIONS` array, wherever plan 01-36 and Task 1 left them. Read all three exclusions'
      `from`, `to`, `band`, `proof`, `name` and `why`. Re-derive line numbers by anchor.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the `RESOLVER_REGISTRY` declaration's
      OPENING line and its real CLOSING line, `] as readonly ResolverRecord[]);`. At HEAD-`4105fd0`
      those were `:4135` and `:5767`. Read both, and read the next exact `]);` line after them, which
      at HEAD-`4105fd0` was `:5884` and closes `BRANCH_VOCABULARY`.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the `BRANCH_VOCABULARY` docblock and
      declaration, at HEAD-`4105fd0` `:5789-5884`. These are the 117 lines exclusion three currently
      swallows the tail of. Read enough of them to know what construct they are, because the
      exclusion's `name` and `why` currently describe a different one.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the `UNBOUNDED_QUANTIFIERS` declaration and
      its close, at HEAD-`4105fd0` `:5959` and `:5969`. Exclusion TWO resolves through the SAME
      `closingBracketAfter`, so any change to that helper must leave exclusion two resolving to exactly
      the same pair.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the three-exclusion non-vacuity case with
      its band assertions and its `proof`-token assertion, and the exclusion-three clause-count
      equality case comparing `inRange` to `inClauses`. Read the equality's failure message: it tells
      the next author to move the sentence out of the range or delete the phrasing and NOT to widen the
      pin, which is the rule this task inherits.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the three discharge checks and the guard's
      four numbered LIMITS docblock, in particular limit (4), which states that exclusion three is
      coarser than its name and is narrowed by the clause-count equality. Limit (4) is re-read against
      the corrected range and updated where it is now inaccurate.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md` `:193-201` — the
      WR-48 section, including the planted phrasing at `:5800` going RED and the sentence that the
      discharge's phrase is true of phrasings and false of lines.
    - `.planning/REQUIREMENTS.md` `:150-155` — the three-row discharge table wave 35 wrote, and in
      particular row 3's evidence cell at `:154` which contains the clause this task supersedes. Read
      the whole cell so the marker can name which clause it reaches and state that the rest stands.
    - `.planning/REQUIREMENTS.md` `:108-114` — the scoped supersession marker at `:110` and the
      paragraph at `:112` it reaches. This is the marker SHAPE: it names the sentence, names what
      supersedes it, and says the rest of the paragraph stands.
  </read_first>

  <files>packages/backend/src/outbound-prohibition.spec.ts, .planning/REQUIREMENTS.md</files>

  <reversibility rating="costly">Narrowing an exclusion is mechanically reversible, but the supersession marker in `.planning/REQUIREMENTS.md` is not: the ledger is append-only in practice and a marker can only be superseded by a further dated correction, with its bytes left on the page. That permanence is what makes the record trustworthy. And the narrowing itself may raise obligations that are then disposed of by deleting or rewriting sentences — those deletions are cheap to revert individually but the judgement behind each is re-work, which is why each disposal is recorded with its reason rather than performed silently.</reversibility>

  <action>
    MEASURE THE BEFORE STATE AND PASTE IT, WITH EVERY NUMBER RE-DERIVED BY ANCHOR. For each of the
    three exclusions: the resolved `from`, the resolved `to`, the line count, and the band it falls in.
    For exclusion three specifically: the registry's opening line, the registry's REAL closing line,
    the line `closingBracketAfter` actually lands on, and the identity of the construct that line
    closes. At HEAD-`4105fd0` those were `4135`, `5767`, `5884` and `BRANCH_VOCABULARY`; plan 01-36 and
    Task 1 edited this file, so RE-DERIVE all four and reconcile against those values, recording any
    disagreement as a finding.

    MEASURE THE 117 LINES BEFORE YOU CHANGE ANYTHING. Enumerate every declared-phrasing occurrence
    inside the tail region — the lines between the registry's real closing line and the line
    `closingBracketAfter` lands on — and paste the enumeration with the count. This is the number that
    determines what the narrowing raises, and measuring it BEFORE the change means the change's effect
    is observed rather than inferred.

    FIX `closingBracketAfter` SO IT ANSWERS ABOUT THE CONSTRUCT IT WAS ASKED ABOUT. Two routes are
    permitted and the choice is recorded with its reason: recognise the registry's real closing form
    as well as the bare one, or bracket-match from the opening line rather than string-match. Prefer
    whichever you can show, by measurement, leaves exclusion TWO resolving to exactly the same pair it
    resolved to before — that is the constraint that eliminates a wrong answer here, because a fix that
    silently re-points a second exclusion while correcting the first is the same defect one construct
    over. Paste exclusion two's resolved pair before and after and show them identical.

    RE-MEASURE AND PASTE THE AFTER STATE. Exclusion three's new `from`, `to` and line count, and the
    band it falls in. Then re-read its `name`, its `why` and its `proof` against the range it NOW
    resolves to and correct each where it is false. The current `name` and `why` describe the registry
    declaration and were false of 117 lines of a different construct; after the narrowing they may be
    true, and if they are, say so and leave them — a correction that rewrites a sentence which has
    become true is churn.

    RE-MEASURE THE `inRange == inClauses` EQUALITY AND DO NOT WIDEN IT. Paste both numbers before and
    after. That equality is what made this defect err safe, and narrowing the range changes both what
    it counts and what it means. If the two numbers disagree after the narrowing, THAT DISAGREEMENT IS
    THE FINDING: write it down, then dispose of it by moving or deleting the offending sentence, never
    by adjusting the pin. The equality's own failure message says exactly that and this task obeys it.

    NOW MEASURE WHAT RETURNED TO THE GUARDED SURFACE, AND MEASURE IT — DO NOT PREDICT IT. Run the
    guard. Enumerate every new obligation the narrowing raised, with its line, its key and the
    construct it sits under. THIS PLAN NAMES NO EXPECTED COUNT and none of its acceptance criteria
    depends on one. Dispose of each by the guard's OWN three-choice rule, in the rule's own order of
    preference: (1) DELETE the sentence if it states a bound on the walk's reach, because the reach of
    record is the generated span and nothing hand-written beside it may restate it; (2) REWRITE it to
    say what the branch does WITHOUT the universal, naming the branches, and never by swapping the
    phrasing for a synonym, which turns the guard green while keeping the bound; (3) add a NAMED
    exemption only if the occurrence is a quotation, a test title, an assertion message complaining
    about the phrasing, a table label or a `QUANTIFIED_CLAUSES` value. Record which choice each
    occurrence got and why, and state how many exemptions were spent — an exemption is a sentence a
    later author must keep true, so each one is a cost.

    EVERY EXEMPTION MINTED HERE OBEYS PLAN 01-36's RULES. Anchored key format, no fully-masked anchor,
    a reason a reviewer can execute and disagree with, and a reason long enough to clear the existing
    minimum-length check. Run the fully-masked-anchor case after minting and observe it green.

    UPDATE THE GUARD'S LIMIT (4) AGAINST WHAT IS NOW TRUE. It currently says exclusion three is coarser
    than its name and is narrowed by the clause-count equality. After this task the coarseness is
    smaller and its measured figures have changed. Re-state it with the new measured numbers, and do
    NOT delete the equality or the disclosure — the exclusion is still a LINE RANGE and still wider
    than the clause strings themselves, which remains true and remains worth saying.

    SUPERSEDE THE DISCHARGE'S PHRASING-LEVEL CLAIM IN PLACE, SCOPED TO ITS CLAUSE. `.planning/
    REQUIREMENTS.md:154` is row 3's evidence cell and it contains the clause WR-48 falsifies. Insert a
    marker in the shape `:110` uses for `:112`: name WHICH clause it reaches, state that the claim is
    true of PHRASINGS and false of LINES, cite WR-48 and this wave's measurement with the before and
    after ranges, and state that the REST OF THE ROW STANDS. Leave the superseded bytes intact below
    the marker — that is this ledger's own precedent for falsified text and the reason the record reads
    straight. Prove the scoping by diff: the marker is a pure INSERTION and the surrounding bytes are
    unchanged.

    TOUCH NOTHING ELSE IN THE LEDGER. `:46`, `:60`, `:158` and `:160` are CR-18's and belong to plan
    01-38; `.planning/STATE.md` is CR-19's and belongs to plan 01-38. Assert all of them byte-unchanged
    by diff at the end of this task. Two waves editing one 23,290-byte line is how a correction loses
    its attribution.

    REPRODUCE NEITHER SENTINEL MARKER LINE IN THE NEW MARKER'S PROSE. `extractDerivedBlock` bounds the
    span by searching the ledger's own bytes for those marker lines.

    COMMIT BEFORE YOU MUTATE, THEN RUN ONE MUTATION. Paste `git log --oneline -1` first. Plant a
    declared phrasing INSIDE the tail region that just left exclusion three — the lines that were
    swallowed and are now on the guarded surface — run, and watch the guard go RED. Paste the failing
    title and the FULL assertion message, restore, cross-check with `git diff --exit-code`. Before the
    narrowing the verifier's equivalent plant went red through the `inRange == inClauses` equality;
    after the narrowing it must go red through the MAIN GUARD, and the SUMMARY states which mechanism
    caught it in each case, because that difference is the whole content of the fix.

    STATE WHAT THIS DID NOT DO, ON EVERY SURFACE WHERE IT STATES WHAT IT DID. One false description is
    corrected. The other two exclusions are not thereby exact; the guard is still a phrase list over
    bytes under one named normalization; undeclared spellings and the unguarded files are still
    unreached; two occurrences under the same construct are still interchangeable. And the enforcement
    ERRED SAFE throughout — nothing was laundered through this defect and nothing leaked — which is why
    it was a warning, and which is not a reason to have left a false description standing.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/DefMiner && pnpm vitest run packages/backend/src/outbound-prohibition.spec.ts 2>&1 | tail -6 && pnpm test 2>&1 | tail -8 && pnpm typecheck && pnpm lint && pnpm knip && pnpm build:backend && pnpm check:bundle 2>&1 | tail -5 && grep -c 'zero residue in any exclusion' .planning/REQUIREMENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - The BEFORE state pasted for all three exclusions: resolved `from`, resolved `to`, line count and
      band. For exclusion three additionally: the registry's opening line, its real closing line, the
      line `closingBracketAfter` lands on, and the construct that line closes. Reconciled against the
      HEAD-`4105fd0` values `4135`, `5767`, `5884` and `BRANCH_VOCABULARY`, with any disagreement
      written up as a finding.
    - The tail region enumerated BEFORE the change: every declared-phrasing occurrence between the
      registry's real closing line and the line `closingBracketAfter` lands on, with the count pasted.
    - `closingBracketAfter` changed, the route chosen (real-closing-form recognition or bracket
      matching) named with its reason, and exclusion TWO's resolved pair pasted BEFORE and AFTER and
      shown IDENTICAL.
    - The AFTER state pasted for exclusion three: new `from`, new `to`, new line count and the band it
      falls in, with the band assertion re-run green.
    - Exclusion three's `name`, `why` and `proof` re-read against the corrected range; each either
      corrected with the before and after pasted, or shown to have become true and deliberately left,
      with that judgement stated.
    - The `inRange == inClauses` equality re-measured with BOTH numbers pasted before and after. If
      they disagreed after the narrowing, the disagreement is written up as a finding and disposed of
      by moving or deleting the offending sentence — the pin is shown by diff NOT to have been widened.
    - The obligations raised by the narrowing enumerated with line, key and construct for each, and the
      count pasted. NO acceptance criterion in this plan requires that count to be any particular
      value. Each obligation's disposal recorded with which of the guard's three choices it got and
      why, and the number of exemptions spent stated.
    - Every exemption minted in this task carries the anchored key format and a reason clearing the
      existing minimum-length check; the fully-masked-anchor case run after minting and observed green,
      output pasted.
    - The guard's numbered LIMIT (4) re-stated with the new measured numbers, quoted verbatim in the
      SUMMARY, and shown still to disclose that the exclusion is a LINE RANGE wider than the clause
      strings themselves.
    - The supersession marker present at `.planning/REQUIREMENTS.md:154`'s clause, shown by `git diff`
      to be a pure INSERTION with the bytes above and below unchanged. The SUMMARY quotes the marker's
      own clause naming which claim it reaches, stating that the claim is true of PHRASINGS and false
      of LINES, and stating that the rest of the row stands.
    - Executed `grep -c 'zero residue in any exclusion' .planning/REQUIREMENTS.md`, observed 1 —
      UNCHANGED. The superseded bytes are left intact and the marker does not rewrite them.
    - `git diff` over `.planning/REQUIREMENTS.md` restricted to the single insertion; rows `:46`,
      `:60`, `:158` and `:160` shown byte-unchanged, output pasted. `git diff` over `.planning/STATE.md`
      shown EMPTY.
    - Neither generated-residual sentinel marker line reproduced in the new marker's prose, confirmed
      by the extraction cases running green.
    - MUTATION executed after the work was committed: `git log --oneline -1` pasted first, a declared
      phrasing planted inside the tail region that left exclusion three, run, observed RED, the failing
      title and the FULL assertion message pasted, restored, `git diff --exit-code` shown clean. The
      SUMMARY states which mechanism caught it — the main guard now, the `inRange == inClauses`
      equality before — and why that difference is the content of the fix.
    - `git diff` over exclusion ONE, over both remaining bands, over the pre-existing exemption entries
      and their reasons, over `CORE11_BOX_EXPECTED` and over the box case shown EMPTY, output pasted.
    - CORE-11's checkbox byte-unchanged; executed
      `grep -cE '^- \[[ x]\] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md`, observed exactly 1.
    - No sentence added by this task states or implies that the exclusions are now exact, that the
      relocation class is closed, or that the gate is complete. Every added sentence is re-read against
      that prohibition and the review recorded as a one-line-per-surface verdict; any rewrite's before
      and after are both pasted.
    - Executed `pnpm test` — exit 0, file and test counts pasted, no fewer than 31 files and 1372
      tests.
    - Executed `pnpm typecheck`, `pnpm lint`, `pnpm knip` — all exit 0, output pasted.
    - Executed `pnpm build:backend && pnpm check:bundle` — exactly one import specifier, `crypto`.
    - Executed the real-tree run over both `SOURCE_ROOTS` — 23 files, 0 violations.
    - `git diff` over CORE-11's first sentence shown EMPTY, output pasted.
    - Any prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses, or an
      explicit statement that there were none.
  </acceptance_criteria>

  <done>
    `closingBracketAfter` resolves the registry to its own closing line and exclusion two is proved to
    have not moved; exclusion three's range and its description agree with the construct it covers;
    whatever the 117 restored lines raised was measured, enumerated and disposed of by the guard's own
    rule with each choice recorded; the `inRange == inClauses` equality was re-measured and not widened;
    a phrasing planted in the restored region was watched going red through the main guard; and the
    discharge's phrasing-level claim is superseded in place with its bytes intact.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a guard that was CHANGED to one that was WATCHED FAILING | Plan 01-36 anchored the key. Until a relocation has been observed going red, the anchoring is an assertion about a mechanism rather than a measurement of it — the exact artifact this file has spent nine waves removing. |
| a fixture that PROVES to one that merely PASSES | A fixture showing the new builder catching a relocation is consistent with a builder that catches everything and with one that catches nothing relevant. Only the counter-probe — the pre-anchoring builder shown NOT catching the same relocation — tells the two apart. |
| a synthetic fixture's own bytes to the surface it tests | This file's guard scans its own bytes. A fixture that spells a declared phrasing raises the obligation it exists to test, and the phrasings are therefore built by reference from the list. |
| an exclusion's NAME to the RANGE it resolves to | Exclusion three says it is the registry declaration and covers 117 lines of a different construct. A description that does not match what it describes is the defect this whole file exists to remove, applied to the file itself. |
| a fix to ONE exclusion to a silent re-point of ANOTHER | `closingBracketAfter` serves exclusions two and three. Correcting the third while quietly moving the second is the same defect one construct over, and exclusion two's resolved pair is therefore measured before and after and shown identical. |
| an enforcement that ERRS SAFE to a description that is CORRECT | The `inRange == inClauses` equality caught a phrasing planted in the swallowed lines, so nothing was laundered. Erring safe is why this is a warning; it is not a reason to leave a false name and a false why standing. |
| a MEASUREMENT to a PREDICTION wearing a measurement's clothes | Narrowing the exclusion may raise obligations. An acceptance criterion naming an expected count converts the measurement into a target, and a target is what an executor engineers toward when the number disagrees. |
| the direction an exclusion may move | Narrowing removes a false claim. Widening — including widening the `inRange == inClauses` pin to accommodate a disagreement — converts a guarded surface into an unguarded one while every assertion stays green. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-223 | Repudiation | an anchoring nobody watched failing | high | mitigate | A permanent named fixture reproducing the relocation over synthetic lines, PLUS the same swap executed once on the real tree in this session and observed RED with its full assertion message pasted and the tree restored clean. |
| T-01-224 | Spoofing | a fixture that passes without proving the anchoring is what changed | high | mitigate | A counter-probe in the same case runs the identical relocation through the retained pre-anchoring key builder and asserts the key is IDENTICAL, `missing` EMPTY and `stale` EMPTY — the shape wave 34 used for measured silences. |
| T-01-225 | Tampering | a change to `closingBracketAfter` silently re-pointing exclusion two | high | mitigate | Exclusion two's resolved pair measured BEFORE and AFTER and shown identical; the route chosen for the fix is the one that can be shown by measurement to leave it unmoved; the band and proof assertions are re-run green for all three exclusions. |
| T-01-226 | Tampering | the outbound gate weakened while its description is corrected | high | mitigate | Narrowing is the only permitted direction. `git diff` over exclusion one, over both remaining bands, over the proof tokens and over the pinned counts asserted EMPTY, and the `inRange == inClauses` pin shown by diff NOT to have been widened even if its two numbers disagree. |
| T-01-227 | Repudiation | an expected obligation count baked into the plan | high | mitigate | No acceptance criterion names a count. The enumeration's output is pasted whatever it is, each obligation disposed of by the guard's own three-choice rule with the choice and its reason recorded, and zero and ten are equally successful outcomes. |
| T-01-228 | Spoofing | an occurrence disposed of by swapping the phrasing for a synonym | medium | mitigate | The guard's own rule forbids it in its own failure message, this task's action forbids it explicitly, and each disposal records which of the three choices it took and why — a rewrite must name the branches rather than rename the universal. |
| T-01-229 | Spoofing | any sentence implying the exclusions are now exact or the class closed | high | mitigate | Prohibited in frontmatter, in the objective and in an acceptance criterion per task. Limit (4) keeps disclosing that the exclusion is a LINE RANGE wider than the clause strings; the guard's phrase-list reach, its normalization, the undeclared spellings and the unguarded files are named wherever this wave's result is stated. |
| T-01-230 | Tampering | a fixture-only helper mistaken for the live key builder | medium | mitigate | The retained pre-anchoring builder is named unmistakably as fixture-only and carries a docblock saying it is the superseded format retained to demonstrate what it missed and must never be used by live code; `knip` is run at the task boundary. |
| T-01-231 | Tampering | two waves editing one 23,290-byte ledger line | medium | mitigate | This plan's only ledger edit is a pure insertion above row 3's evidence cell at `:154`. Rows `:46`, `:60`, `:158` and `:160` and every line of `.planning/STATE.md` are asserted byte-unchanged by diff and are owned by plan 01-38. |
| T-01-232 | Repudiation | a superseded claim rewritten instead of marked | medium | mitigate | The marker is a pure INSERTION with the superseded bytes left intact below it, in the shape `:110` uses for `:112`; `grep -c 'zero residue in any exclusion' .planning/REQUIREMENTS.md` observed 1 and unchanged. |
| T-01-187 | Tampering | mutating uncommitted task work | high | mitigate | Every task commits its real work before planting any mutation and cross-checks with `git diff --exit-code`. Wave 29 destroyed its own Task 1 exactly this way. Carried forward unchanged. |
| T-01-188 | Tampering | a gate set nobody ran | medium | mitigate | No active git hooks in this repo, so the full set runs by hand at every task boundary with its output pasted. Carried forward unchanged. |
| T-01-221 | Tampering | a new synthetic fixture raising the obligation it tests | medium | mitigate | Declared phrasings in new code built BY REFERENCE from `UNBOUNDED_QUANTIFIERS` by index, never spelled. Carried forward from plan 01-36. |
| T-01-214 | Tampering | CORE-11's checkbox moved | critical | mitigate | Prohibited; `git diff` over the checkbox asserted EMPTY at both task boundaries and the state-agnostic count observed exactly 1. Carried forward from plan 01-36. |
| T-01-212 | Information Disclosure | the two reader surfaces no mechanism reaches | medium | accept | `.planning/STATE.md` and `.planning/WINDOWS.md` are governed by the pointer-not-a-bound prohibition with no mechanical check. Untouched by this plan and owned by plan 01-38. Carried forward from T-01-144 unchanged. |
| T-01-51 | Information Disclosure | the accepted residual of the outbound walk | medium | mitigate | Unchanged by this plan — no registry row moves — and byte-pinned into the ledger by the two comparisons, re-run green at both task boundaries. |
| T-01-50 | Information Disclosure | `.spec.ts` files, which the walk skips | low | accept | Unchanged and carried forward, bounded by `pnpm check:bundle`, which specs never enter. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | NOT APPLICABLE: this plan installs no package. `pnpm install` is not run and no manifest appears in `files_modified`. RESEARCH.md's Package Legitimacy Audit is present and unchanged; no `[ASSUMED]` or `[SUS]` package is introduced, so no blocking human checkpoint is required. |
</threat_model>

<verification>
1. Precondition asserted and pasted: `01-36-SUMMARY.md` present, `pnpm test` exit 0, both byte
   comparisons green, working tree clean, `git rev-parse HEAD` beside any snapshot.
2. The relocation fixture present as a named case, its title quoted, naming a CROSS-CONSTRUCT
   relocation and not the general case.
3. The synthetic probe pasted: two anchored keys shown to differ, `missing` non-empty, `stale`
   non-empty, count equality still balancing, with one sentence on why a balancing count proves nothing.
4. The counter-probe pasted: identical key under the pre-anchoring builder, `missing` and `stale` both
   empty; the fixture-only helper's name and docblock quoted.
5. Declared phrasings in new code taken from `UNBOUNDED_QUANTIFIERS` by index, with the whole-file
   guard green afterwards.
6. Plan 01-36's fifth numbered LIMIT repeated beside the fixture and quoted verbatim.
7. The real-tree relocation executed as a SWAP after commit, the cell located by CONTENT, the plant
   proved outside all three resolved exclusion ranges, the suite observed RED with title and full
   message pasted, restored, `git diff --exit-code` clean; the verifier's prior 432/432 cited as the
   contrast.
8. If the real-tree mutation did not go red, the task stopped and the diagnosis is written up as a
   first-order finding with no adjustment made to force a red.
9. All three exclusions' BEFORE `from`/`to`/size/band pasted, plus the registry's opening line, its
   real closing line, the line `closingBracketAfter` lands on, and the construct that line closes;
   reconciled against `4135`, `5767`, `5884` and `BRANCH_VOCABULARY`.
10. The tail region enumerated BEFORE the change with its declared-phrasing count pasted.
11. `closingBracketAfter` changed, the route named with its reason, and exclusion TWO's resolved pair
    pasted before and after and shown IDENTICAL.
12. Exclusion three's AFTER `from`/`to`/size/band pasted with the band assertion re-run green.
13. Exclusion three's `name`, `why` and `proof` re-read against the corrected range and each corrected
    or deliberately left with the judgement stated.
14. `inRange` and `inClauses` pasted before and after; any disagreement written up as a finding and
    disposed of by moving or deleting a sentence, with the pin shown by diff not to have been widened.
15. Every obligation the narrowing raised enumerated with line, key and construct, and each disposed of
    by one of the guard's three choices with the choice and its reason recorded and the exemption spend
    stated. NO expected count is required by any criterion.
16. Every minted exemption carrying the anchored key format and a reason clearing the minimum-length
    check; the fully-masked-anchor case re-run green.
17. LIMIT (4) re-stated with the new measured numbers, quoted verbatim, still disclosing that the
    exclusion is a line range wider than the clause strings.
18. The supersession marker at row 3's evidence cell shown by diff to be a pure INSERTION; the marker's
    own scoping clause quoted; `grep -c 'zero residue in any exclusion' .planning/REQUIREMENTS.md`
    observed 1 and unchanged.
19. `.planning/REQUIREMENTS.md` rows `:46`, `:60`, `:158`, `:160` byte-unchanged and
    `.planning/STATE.md` diff EMPTY.
20. Neither sentinel marker line reproduced in the new marker's prose, confirmed by the extraction
    cases running green.
21. The post-narrowing mutation executed after commit: a phrasing planted in the restored tail region,
    observed RED, title and full message pasted, restored, clean `git diff --exit-code`, with the
    catching mechanism named for both the before and after cases.
22. `git diff` over exclusion one, both remaining bands, the pre-existing exemption entries and their
    reasons, `CORE11_BOX_EXPECTED` and the box case — EMPTY.
23. CORE-11's checkbox byte-unchanged; `grep -cE '^- \[[ x]\] \*\*CORE-11\*\*'` observed exactly 1.
24. Every added sentence re-read against the no-overclaim prohibition with a one-line-per-surface
    verdict; any rewrite's before and after pasted.
25. `pnpm test` — exit 0, no fewer than 31 files and 1372 tests.
26. `pnpm typecheck`, `pnpm lint`, `pnpm knip` — all exit 0.
27. `pnpm build:backend && pnpm check:bundle` — exactly one import specifier, `crypto`.
28. Real-tree run over both `SOURCE_ROOTS` — 23 files, 0 violations; CORE-11's first sentence diff
    EMPTY.
29. Every prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses, or an
    explicit statement that there were none.
</verification>

<success_criteria>
- The relocation that passed 432 of 432 is now a permanent named fixture, and the same swap was watched
  going red on the real tree in this session before the tree was restored clean.
- The fixture carries a counter-probe showing the pre-anchoring builder missing the identical
  relocation, so it proves the anchoring is what changed rather than asserting it.
- `closingBracketAfter` answers about the construct it is asked about, and exclusion two is shown by
  measurement not to have moved.
- Exclusion three's resolved range and its `name`, `why` and `proof` agree with the construct it
  actually covers, and the 117 lines are named for what they are.
- What the restored lines raised was MEASURED — enumerated, disposed of by the guard's own three-choice
  rule, each choice recorded with its reason — and no number was predicted anywhere in this plan.
- The `inRange == inClauses` equality was re-measured and was not widened, whichever way its two
  numbers landed.
- A phrasing planted in the restored region was watched going red through the main guard, and the
  SUMMARY names which mechanism caught it before and after.
- The discharge's `zero residue in any exclusion` is superseded in place, scoped to its clause, with
  its bytes intact and the rest of the row standing.
- CORE-11's checkbox did not move; `.planning/STATE.md` was not touched; rows `:46`, `:60`, `:158` and
  `:160` are byte-unchanged and still belong to plan 01-38.
- No sentence anywhere claims the exclusions are now exact, the relocation class closed, or the gate
  complete; the enforcement is recorded as having erred safe throughout, without that being used as a
  reason to have left a false description standing.
- The green baseline is preserved and recorded: no fewer than 31 files / 1372 tests, typecheck / lint /
  knip clean, bundle at exactly one specifier, real tree at 23 files / 0 violations, and nothing leaks.
</success_criteria>

<artifacts_this_phase_produces>
| Artifact | Kind | Purpose |
|----------|------|---------|
| the cross-construct relocation fixture | new mechanism | The verifier's executed mutation made permanent over synthetic lines: an occurrence relocated from one construct to another yields a different anchored key, a non-empty `missing` and a non-empty `stale` |
| the pre-anchoring key builder, retained as fixture-only | new mechanism | The counter-probe half — the superseded format kept so the fixture can show it missing the identical relocation, named and docblocked so nobody mistakes it for the live builder |
| the executed real-tree relocation swap | new evidence | The cell deleted and a fabricated bound planted outside all three exclusions, observed RED in this session and restored, against the verifier's prior 432/432 as the contrast |
| the corrected `closingBracketAfter` | corrected mechanism | Resolves the registry to its own closing line instead of walking 117 lines past it, with exclusion two's resolution measured before and after and shown identical |
| exclusion three's corrected range, `name`, `why` and `proof` | corrected disclosure | A description that agrees with the construct it describes, in a file whose whole discipline is that a description match what it describes |
| the tail-region enumeration, before and after | new evidence | What the 117 lines carried before the narrowing and what returning them to the guarded surface actually raised — measured, never predicted |
| the obligation disposal record | new evidence | Every obligation the narrowing raised, with its line, its key, its construct, which of the guard's three choices it got and why, and the exemption spend stated |
| the re-measured `inRange == inClauses` pair | new evidence | Both numbers before and after the narrowing, with the pin shown not to have been widened whichever way they landed |
| the post-narrowing red mutation | new evidence | A phrasing planted in the restored region watched going red through the MAIN GUARD, where before the narrowing an equivalent plant went red through the clause-count equality |
| the re-stated LIMIT (4) | corrected disclosure | Exclusion three's remaining coarseness with its new measured numbers, still disclosing that a line range is wider than the clause strings it stands for |
| the scoped supersession marker at row 3's evidence cell | corrected disclosure | `zero residue in any exclusion` marked as true of PHRASINGS and false of LINES, bytes intact, rest of the row standing |
| `01-37-SUMMARY.md` | new evidence | The fixture with its probe and counter-probe, the executed swap, the before/after exclusion measurements, the obligation enumeration and its disposals, the re-measured equality, the two mutation proofs and the full gate set |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-skeleton-persistence-compatibility/01-37-SUMMARY.md` when done.

It must contain, as pasted terminal output rather than as prose: the precondition assertions with
`git rev-parse HEAD`; the fixture's title and both halves of its assertion — the two differing anchored
keys with the differing portion identified, and the identical pre-anchoring key with `missing` and
`stale` both empty; the fixture-only helper's name and docblock; the real-tree swap with the cell's
located line number, the resolved exclusion ranges proving the plant sat outside all three, the before
and after occurrence totals proving it was a swap, the RED title and the FULL assertion message, the
restore and a clean `git diff --exit-code`; all three exclusions' `from`/`to`/size/band BEFORE and
AFTER; the registry's opening line, its real closing line, the line `closingBracketAfter` landed on and
the construct it closes; exclusion two's resolved pair before and after shown identical; the tail-region
enumeration with its count; the obligation enumeration with line, key, construct, chosen disposal and
reason for each; `inRange` and `inClauses` before and after; the post-narrowing RED mutation with its
title, full message, restore and clean diff; the re-stated LIMIT (4) quoted verbatim; the pure-insertion
diff of the supersession marker; the EMPTY diffs over exclusion one, the remaining bands, the
pre-existing exemption entries and their reasons, `CORE11_BOX_EXPECTED`, the box case, rows `:46`,
`:60`, `:158`, `:160` and `.planning/STATE.md`; and the final `pnpm test`, `typecheck`, `lint`, `knip`
and `check:bundle` results with the real-tree 23-files / 0-violations run.

It must state in ONE sentence which mechanism caught a phrasing planted in the tail region BEFORE the
narrowing and which catches it AFTER, because that difference is the entire content of the WR-48 fix.

It must state in ONE sentence that the enforcement ERRED SAFE throughout — nothing was laundered
through this defect and nothing leaked — and in the SAME sentence that erring safe was never a reason
to leave a false `name` and a false `why` standing on an exclusion.

It must state in ONE sentence that the obligation count raised by the narrowing was MEASURED and not
predicted, giving the number that was measured and stating that this plan named no expected value for
it.

It must state in ONE sentence that narrowing one exclusion does not make the other two exact, does not
close the relocation class, does not widen the guard's phrase list, and does not reach undeclared
spellings or the unguarded files.

It must also record any PREDICTION-VS-MEASUREMENT discrepancy in the shape `01-27-SUMMARY.md` uses, and
if there were none, say so explicitly.
</output>
