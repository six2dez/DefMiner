---
phase: 01-skeleton-persistence-compatibility
plan: 27
subsystem: testing
tags: [core-11, outbound-prohibition, derived-residual, coverage-guard, mutation-proofs, typescript-ast, vitest]

requires:
  - phase: 01-skeleton-persistence-compatibility
    provides: "waves 23-26's gate — the binding-declaration-order bound, the whole-file string collectors, the collapsed operator descent, and the binding-pattern assembly reading. The registry's rows are written against the gate AS THOSE FOUR WAVES LEFT IT."
provides:
  - "`RESOLVER_REGISTRY` — 38 frozen records (31 resolvers, 7 measured silences), each with a clause, a greppable declaration anchor, a probe and a counter-probe, all MEASURED"
  - "`deriveResidual(registry)` — a pure renderer whose output is a function of its input alone"
  - "`extractDerivedBlock` + `SentinelMissingError` + `PlanningLedgerAbsentError` + `readPlanningLedger`"
  - "`enumerateResolverPopulations` and `RESOLVER_EXEMPTIONS` — the coverage guard over two enumerated populations with a named, reasoned exemption list"
  - "the sentinel-delimited generated block, byte-identical in the gate header and in `.planning/REQUIREMENTS.md` CORE-11"
  - "pointer amendments in `.planning/STATE.md` P9-D3 and `.planning/WINDOWS.md` entry 32, restating no bound"
affects: [wave-28-core-11-flip, phase-01-verification, any-future-outbound-widening]

actuals:
  tokens: 47000
  tasks: 4
  commits: 4

tech-stack:
  added: []
  patterns:
    - "A disclosure is GENERATED from a registry and byte-compared to its shipping surfaces, rather than authored"
    - "Every registry row is bound to the code by an EXECUTED probe and an EXECUTED counter-probe, and a row whose two directions agree is rejected as vacuous"
    - "Provenance is a greppable DECLARATION anchor, not a line number, and the anchor must match at the start of a line so the row cannot satisfy itself"
    - "A coverage guard enumerates populations out of the file's own source and requires each member to be REGISTERED OR LISTED; the population it cannot enumerate is declared in its docblock"
    - "Derived text ships where completion is read against it; append-only dated histories get a POINTER and restate no bound"

key-files:
  created: []
  modified:
    - packages/backend/src/outbound-prohibition.spec.ts
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .planning/WINDOWS.md

key-decisions:
  - "The residual is GENERATED by `deriveResidual(RESOLVER_REGISTRY)` and byte-compared to two shipping surfaces; the generated text is authoritative and a divergent shipped text is the defect."
  - "The coverage guard's bound is REGISTERED OR LISTED over TWO enumerated populations — not `detectable`. The third population (inline branches, differently-shaped bindings, resolvers declared inside another function) is declared in the guard's docblock and in the generated limits paragraph."
  - "`counterExpect` is an explicit list rather than a hardcoded empty one, because three mechanisms NARROW the walk; what is asserted instead is that a row's two directions DIFFER."
  - "Provenance anchors are greppable declarations matched at line start, never line numbers — a substring search would have been satisfied by the row's own text."
  - "The mechanism table STAYS HAND-WRITTEN and the derived block is AUTHORITATIVE: the two are indexed on different things (spelling vs mechanism) and neither can be rendered from the other."
  - "`.planning/REQUIREMENTS.md` is DERIVED (it is the text completion is read against and the copy that drifted); `.planning/STATE.md` and `.planning/WINDOWS.md` are POINTERS (append-only dated histories)."
  - "The suite's first dependency on `.planning/` FAILS LOUDLY on a stripped tree rather than skipping, and the reason is written beside the reader."
  - "CORE-11's box is deliberately left `[ ]` and `requirements-completed` is deliberately EMPTY; wave 28 owns the flip, against the generated block."

patterns-established:
  - "Non-vacuity before the rule, for every reader AND every enumeration, with the counts the run found pinned so a SHRINKING enumeration is visible"
  - "Six separately executed mutation proofs before a mechanism is claimed to work"

requirements-completed: []

coverage:
  - id: D1
    description: "The residual shipped in the gate header and in REQUIREMENTS.md is deriveResidual(RESOLVER_REGISTRY)'s output byte for byte"
    requirement: "CORE-11"
    verification:
      - kind: unit
        ref: "packages/backend/src/outbound-prohibition.spec.ts#the block shipped in the gate header equals deriveResidual(RESOLVER_REGISTRY), byte for byte"
        status: pass
      - kind: unit
        ref: "packages/backend/src/outbound-prohibition.spec.ts#the block shipped in .planning/REQUIREMENTS.md equals deriveResidual(RESOLVER_REGISTRY), byte for byte"
        status: pass
    human_judgment: false
  - id: D2
    description: "Every registry row is bound to the walk by an executed probe and an executed counter-probe"
    requirement: "CORE-11"
    verification:
      - kind: unit
        ref: "packages/backend/src/outbound-prohibition.spec.ts#registry row %s — its probe AND its counter-probe are executed against auditSource (38 parameterised cases)"
        status: pass
    human_judgment: false
  - id: D3
    description: "A collector or resolver function added without a registry row and without an exemption entry is a failing test, over both enumerated populations"
    requirement: "CORE-11"
    verification:
      - kind: unit
        ref: "packages/backend/src/outbound-prohibition.spec.ts#every member of BOTH populations is a registry row OR a named, reasoned exemption"
        status: pass
      - kind: unit
        ref: "packages/backend/src/outbound-prohibition.spec.ts#BOTH resolver populations enumerate a NON-EMPTY set — non-vacuity, asserted BEFORE the rule"
        status: pass
    human_judgment: false
  - id: D4
    description: "Neither the block nor the file can vanish quietly: a missing sentinel and an absent planning ledger raise distinct named errors, and the entry count is asserted against the registry's length"
    requirement: "CORE-11"
    verification:
      - kind: unit
        ref: "packages/backend/src/outbound-prohibition.spec.ts#extractDerivedBlock THROWS a NAMED error when a sentinel is missing — executed against synthetic text"
        status: pass
      - kind: unit
        ref: "packages/backend/src/outbound-prohibition.spec.ts#readPlanningLedger THROWS a NAMED error when the planning ledger is ABSENT — executed against a path that does not exist"
        status: pass
      - kind: unit
        ref: "packages/backend/src/outbound-prohibition.spec.ts#the shipped block carries exactly ONE entry per registry row — a truncated block FAILS"
        status: pass
    human_judgment: false
  - id: D5
    description: "All six divergence directions have been executed and each turned the suite red"
    requirement: "CORE-11"
    verification:
      - kind: manual_procedural
        ref: "six separately executed mutations, each RED title and message pasted in this SUMMARY, each restored with `git diff --exit-code` clean and re-run green"
        status: pass
    human_judgment: true
    rationale: "A mutation proof is destructive by construction and cannot live in the committed suite; the evidence is the pasted RED output and the verified restore, which a human reads."
  - id: D6
    description: "Two surfaces carry the derived text and two carry a pointer, with the reason written at the point of the difference in all four artifacts"
    requirement: "CORE-11"
    verification: []
    human_judgment: true
    rationale: "The pointer-not-a-bound rule is a PROHIBITION WITH NO MECHANICAL CHECK — the byte comparison reaches the gate header and REQUIREMENTS.md and no further. Nothing stops a later author writing a fresh bound into either history; that limit is named rather than guarded."

duration: 22 min
completed: 2026-08-24
status: complete
---

# Phase 01 Plan 27: The Derived Residual Summary

**CORE-11's residual is no longer written by anyone: it is rendered by `deriveResidual` from a 38-row registry whose every row is executed against `auditSource`, shipped byte-identically into the gate header and `REQUIREMENTS.md`, guarded by a coverage check over two enumerated resolver populations, and proven by six separately executed mutations.**

## Performance

- **Duration:** 22 min
- **Started:** 2026-08-24T13:29Z
- **Completed:** 2026-08-24T13:51Z
- **Tasks:** 4
- **Files modified:** 4

## Accomplishments

- The residual is GENERATED. `deriveResidual(RESOLVER_REGISTRY)` renders it; the text in the gate header and in `.planning/REQUIREMENTS.md` CORE-11 is that function's output, byte for byte, asserted against each file's own bytes at test time.
- The registry is BOUND TO THE CODE BY EXECUTION. 38 rows — 31 resolvers, 7 measured silences — each carrying a probe and a counter-probe that a parameterised case runs through `auditSource`. Removing `unwrap`'s comma arm moved that row and nothing else moved with it.
- A resolver added without a row is REGISTERED OR LISTED. The guard enumerates 11 collectors and 38 resolver functions out of the gate file's own source; each must be a registry row or a named entry on an 18-entry reasoned exemption list. Both failing paths were executed.
- Neither the block nor the FILE can vanish quietly. Three distinguishable named failures, all executed.
- SIX mutation proofs, each executed separately, each restored with `git diff --exit-code` clean and re-run green.

## Task Commits

1. **Task 1 (tracer): one resolver row, derived end to end** — `7f62ca9` (feat)
2. **Task 2a: the whole registry — 38 rows** — `d640908` (feat)
3. **Task 2b: the coverage guard over both populations** — `1002df9` (feat)
4. **Task 3: the ledger block, the two pointers, the coupling decision** — `0a8bbdd` (feat)

## THE REGISTRY — every row with the code site its clause was read from

Read off the block as it ships (`read off:` field, per row), not transcribed here by hand:

| row | read off |
|---|---|
| `constStrings` | auditSource > const constStrings = new Map<string, Set<string>>() |
| `assembledNames` | auditSource > const assembledNames = new Set<string>() |
| `receiverAliases` | auditSource > const receiverAliases = new Map<string, string>() |
| `unreadableAliases` | auditSource > const unreadableAliases = new Set<string>() |
| `fetchAliases` | auditSource > const fetchAliases = new Set<string>([FETCH_GLOBAL]) |
| `navigatorAliases` | auditSource > const navigatorAliases = new Set<string>([NAVIGATOR]) |
| `globalAliases` | auditSource > const globalAliases = new Map<string, string>() |
| `globalThisAliases` | auditSource > const globalThisAliases = new Set<string>() |
| `shadowedGlobals` | auditSource > const shadowedGlobals = new Set<string>() |
| `numericNames` | auditSource > const numericNames = new Set<string>() |
| `poisonedNumericNames` | auditSource > const poisonedNumericNames = new Set<string>() |
| `isGlobalReceiver` | auditSource > const isGlobalReceiver = (node: ts.Expression): boolean => |
| `keyReceiver` | auditSource > const keyReceiver = (key: ts.Expression): ReceiverKind => { |
| `receiverKind` | auditSource > const receiverKind = (node: ts.Expression): ReceiverKind => { |
| `literalsOf` | auditSource > function literalsOf(node: ts.Node | undefined): ReadonlySet<string> { |
| `literalOf` | auditSource > function literalOf(node: ts.Node | undefined): string | undefined { |
| `memberName` | auditSource > const memberName = ( |
| `initializerReceiver` | auditSource > const initializerReceiver = (node: ts.Expression): ReceiverKind => |
| `isFetchExpression` | auditSource > const isFetchExpression = (node: ts.Expression): boolean => { |
| `isNavigatorReceiver` | auditSource > const isNavigatorReceiver = (node: ts.Expression): boolean => { |
| `globalNameOf` | auditSource > const globalNameOf = (node: ts.Expression): string | undefined => { |
| `dynamicCodeOf` | auditSource > const dynamicCodeOf = (node: ts.Expression): string | undefined => { |
| `outboundCtorOf` | auditSource > const outboundCtorOf = (node: ts.Expression): string | undefined => { |
| `aliasedGlobalOf` | auditSource > const aliasedGlobalOf = (init: ts.Expression): string | undefined => { |
| `unwrap` | module scope > function unwrap(node: ts.Expression): ts.Expression { |
| `operatorReceiver` | module scope > const operatorReceiver = ( |
| `isProvablyNumeric` | module scope > function isProvablyNumeric( |
| `isAssembledKey` | module scope > function isAssembledKey( |
| `isGlobalReceiverIn` | module scope > function isGlobalReceiverIn( |
| `boundPropertyName` | module scope > function boundPropertyName(el: ts.BindingElement): string | undefined { |
| `destructuredInitializer` | module scope > function destructuredInitializer( |
| `silence-two-hop-key` | auditSource > const constStrings = new Map<string, Set<string>>() |
| `silence-function-boundary` | auditSource > const receiverKind = (node: ts.Expression): ReceiverKind => { |
| `silence-parameter-key` | auditSource > const keyReceiver = (key: ts.Expression): ReceiverKind => { |
| `silence-loop-binding-key` | auditSource > const keyReceiver = (key: ts.Expression): ReceiverKind => { |
| `silence-destructured-plain-literal-key` | auditSource > const constStrings = new Map<string, Set<string>>() |
| `silence-inverted-binding-order` | auditSource > const collect = (node: ts.Node): void => { |
| `silence-operator-around-global-receiver` | module scope > const operatorReceiver = ( |

**31 resolvers + 7 measured silences = 38.** Every id in the resolver half is the mechanism's identifier EXACTLY as spelled in the code, which is what lets the coverage guard match rows to populations by name.

**Was any row unwritable without reading the residual paragraph?** No. Every row's clause was read off its own branch and every row's `expect`/`counterExpect` was MEASURED by running the probe through `auditSource` before the row was written. Five predictions were wrong and the measurement won in all five; they are recorded below as discrepancies.

## PREDICTION-VS-MEASUREMENT DISCREPANCIES (recorded, not absorbed)

| # | predicted | measured | what it means |
|---|---|---|---|
| 1 | `const r = bus.requests; r.send(req)` → `[]` (counter-probe for `receiverAliases`) | `["outbound-send"]` | `receiverAliases` is anchored on the MEMBER NAME (`requests`/`net`), not on `sdk`. Any object with a `.requests` member grows a receiver alias. The counter-probe was rewritten to `const r = sdk.other`. Direction is safe (over-approximates), and it is not something any residual list had stated. |
| 2 | `const r = sdk[k]; r.send(req)` → `outbound-unanalysable` | `[]` | `k` is never bound in that fixture, so `sdk[k]` is residual (b) and never becomes an unreadable RECEIVER at all. The `unreadableAliases` probe had to be an ASSEMBLED receiver expression, `sdk["req" + "uests"]`. |
| 3 | `sdk[i + 1].send(req)` → `[]` (`isProvablyNumeric`) | `["outbound-unanalysable"]` | with `i` undeclared it is not in `numericNames`, so the numeric exemption never applies. The probe needs `let i = 0;` first. The numeric exemption is narrower than "it looks like arithmetic". |
| 4 | `function pick() { return "requests"; } sdk[pick()].send(req)` → `[]` (function boundary) | `["outbound-unanalysable"]` | a CALL RESULT is an ASSEMBLY under `assembledNames`, so the function-boundary silence cannot be probed through a call in KEY position. The silence row was rewritten to `function pick() { return sdk.requests; } pick().send(req)`. |
| 5 | the plan's own text places `isAssembledKey`, `isProvablyNumeric` and `unwrap` "inside the audit function" | all three are declared at MODULE scope | a guard reading that sentence literally would have EXCLUDED the three functions the sentence itself cited. Population 2 spans both scopes because the measurement said so. Recorded in the guard's docblock, not just here. |

## THE GENERATED BLOCK — its limits paragraph, PASTED FROM THE SHIPPED BLOCK

```
WHAT THIS TEXT ESTABLISHES, AND WHAT IT DOES NOT.
1. Each entry below is verified by EXECUTION: its probe and its counter-probe
   are run through auditSource and asserted against the rule identifiers
   recorded here, so a branch removed from the walk turns its own entry red.
2. It does NOT prove the registry enumerates every mechanism the walk has. A
   coverage guard enumerates TWO populations out of this file's own source -
   collectors matching a declared naming convention, and resolver functions
   declared inside the audit function or at module scope in the resolver
   region - and requires each member to be an entry below OR a NAMED, reasoned
   entry on an explicit exemption list. The bound is REGISTERED OR LISTED over
   those two populations. It is NOT `detectable`: a resolver written as NEITHER
   shape - an inline branch in the walk, a differently-shaped binding, a
   resolver declared inside another function - is enumerated by neither half
   and is NOT caught.
3. Each entry's probes are EXAMPLES. They prove the entry true OF ITSELF and
   do not cover that resolver's whole domain.
4. The MEASURED SILENCE entries are NOT proven exhaustive: a shape nobody
   thought of is still silent and still unlisted here.
```

The same paragraph is byte-identical in `.planning/REQUIREMENTS.md` (sha256 of the whole span in both files: `91978e313c54cc83678c3a62d6e21abed20448f55a0c943d66a636d7c6b7413e`; 295 lines, 38 entries).

## THE COVERAGE GUARD — docblock, both populations, the third one it does not reach

```
 * THE COVERAGE GUARD — WHAT IT ENUMERATES, AND WHAT IT CANNOT SEE.
 *
 * THE BOUND IS "REGISTERED OR LISTED OVER TWO ENUMERATED POPULATIONS". It is NOT
 * "every new resolver is detectable", and no sentence in this file, in the
 * generated text or in any summary may say that it is. Stating a stronger bound
 * than the mechanism has is the exact defect this whole plan exists to reduce, and
 * a guard that overclaims about itself is the sixth instance of it.
 *
 * POPULATION 1 — COLLECTORS. Every `const <name> = new Map<…>` or
 * `new Set<…>` declared at auditSource's own indentation, inside auditSource's own
 * line span. That naming convention is DECLARED here rather than inferred, so a
 * later author who renames a collector out of it is renaming out of a stated rule.
 *
 * POPULATION 2 — RESOLVER FUNCTIONS, IN BOTH SCOPES. Every `function <name>(` and
 * every `const <name> = (` declared at auditSource's own indentation inside
 * auditSource, AND every one declared at column zero in the resolver region — from
 * the first `import` to the first `describe(`.
 *
 * WHY BOTH SCOPES, RECORDED AS A DISCREPANCY RATHER THAN SMOOTHED OVER. The plan
 * that commissioned this guard described population 2 as "function declarations
 * inside the audit function" and named `isAssembledKey`, `isProvablyNumeric` and
 * `unwrap` as members of it. MEASURED, all three are declared at MODULE scope, so
 * the literal reading of that sentence would have excluded the three functions the
 * sentence itself cited. The population spans both scopes because the measurement
 * said so, not because the plan did. The half that matters most is the
 * auditSource half — WR-27 was a defect in `receiverKind`, wave 24 edited
 * `keyReceiver` and wave 25 edited `initializerReceiver` — so a guard blind to the
 * function-shaped population would have been blind exactly where this round's
 * findings came from.
 *
 * POPULATION 3 — ENUMERATED BY NEITHER HALF, AND THEREFORE NOT CAUGHT. A resolver
 * written as NEITHER a matching collector NOR a declared function: an inline branch
 * in `collect` or `visit`, a differently-shaped binding (`const f = function () {}`,
 * a class method, a resolver reached through an object literal), or a resolver
 * declared inside another function. Nothing below sees any of those. That residue
 * is named HERE rather than left to be discovered, because an undeclared blind spot
 * is the artifact this mechanism replaces.
 *
 * NON-VACUITY IS ASSERTED FOR BOTH POPULATIONS BEFORE THE RULE. A convention that
 * stops matching — because a later author renames a collector, wraps the functions
 * differently, or moves auditSource — must FAIL LOUDLY rather than pass having
 * enumerated nothing. That failure mode is the one this whole mechanism exists to
 * remove and it must not be reintroduced by its own guard.
 */
```

### The exemption list, every entry named and reasoned

```ts
const RESOLVER_EXEMPTIONS: Readonly<Record<string, string>> = Object.freeze({
  // --- declared inside auditSource ---
  add: "records a Violation into the output array; it resolves no expression and reads no binding.",
  bindString:
    "WRITES a literal into constStrings; it is the collector's setter, and what it feeds is stated by the constStrings row.",
  collect:
    "the first document-order pass. It invokes the collectors and records bindings; it decides nothing about what an expression IS.",
  visit:
    "the second document-order pass. It applies the rules to the bindings collect() produced; every resolution it performs is delegated to a registered mechanism.",
  // --- declared at module scope, in the resolver region ---
  shippedFiles:
    "enumerates the .ts files under both SOURCE_ROOTS. A filesystem walk, not an expression resolver.",
  rootHasSubdirectory:
    "a non-vacuity check on that filesystem walk — it answers whether a root has any subdirectory at all.",
  operatorOperands:
    "extracts the operands operatorReceiver descends into. Its behaviour is stated by the operatorReceiver row and it has no independent answer.",
  aliasSuffix:
    "formats the ` (an alias of \\`x\\`)` fragment of a violation message.",
  callDetail: "formats the violation detail for a call site.",
  constructionDetail: "formats the violation detail for a `new` site.",
  auditSource:
    "the entry point that HOSTS every resolver. It is the subject the registry describes, not a member of it.",
  formatRules: "renders a rule-id list into the generated text.",
  plural: "renders a count into the generated text.",
  formatEntry: "renders one registry row into the generated text.",
  deriveResidual:
    "renders the residual text from the registry. It reads no AST and resolves no expression.",
  extractDerivedBlock:
    "pulls a sentinel-delimited span out of a file's TEXT. It reads no AST and resolves no expression.",
  readPlanningLedger:
    "reads a `.planning/` ledger's TEXT behind a named-error guard. It reads no AST and resolves no expression.",
  enumerateResolverPopulations:
    "reads this file's own source TEXT to enumerate the two populations the guard checks. It is the guard's input, not a mechanism of the walk.",
});
```

### Non-vacuity, with the counts this run found

```
collectors enumerated: 11   (receiverAliases, fetchAliases, constStrings, assembledNames,
                             unreadableAliases, navigatorAliases, globalAliases,
                             shadowedGlobals, globalThisAliases, numericNames,
                             poisonedNumericNames)
functions  enumerated: 38   (17 declared inside auditSource + 21 at module scope)
registry rows:         38   (31 resolvers + 7 measured silences)
exemptions:            18
accounted for:         11 + 38 = 49 = 31 rows matching a population member + 18 exemptions
```

Both counts are asserted with `.toBe()` and not merely `> 0`, so an enumeration that SHRINKS — a renamed collector, a re-wrapped function set — fails loudly rather than passing having seen three members.

## THE `.planning/` COUPLING — declared as a decision

Pasted from the reader's own docblock in `packages/backend/src/outbound-prohibition.spec.ts`:

> THE COUPLING THIS INTRODUCES, DECLARED AS A DECISION RATHER THAN SLIPPED IN. This is the FIRST dependency in this repository from a test under `packages/backend/src` on the `.planning/` directory. `tests/pins.spec.ts` is NOT a precedent for it and must not be cited as one: it reads `pnpm-workspace.yaml`, `pnpm-lock.yaml`, three `package.json` files and `scripts/phase1/tracer-e2e.sh`, and no planning file at all. What it IS a precedent for is the repo-root relative PATH CONVENTION, which this reader follows.
>
> WHY THE LEDGER IS DERIVED RATHER THAN POINTED AT. `.planning/REQUIREMENTS.md` carries the text a requirement's COMPLETION is read against, and it is the copy that drifted through five consecutive rounds — CORE-11's `[x]` was flipped against it once and reverted twice. A pointer there would leave the ledger a reader trusts most as the one surface that can still be wrong.
>
> WHAT THE SUITE DOES ON A TREE WHERE `.planning/` IS ABSENT: IT FAILS, LOUDLY AND BY NAME. This toolchain ships a `gsd-pr-branch` workflow whose stated purpose is producing a branch with the `.planning/` commits filtered out, so that tree is not hypothetical. Failing is a DELIBERATE choice over skipping. A check that silently skips when its target is missing is exactly how a gate ends up green having read nothing, which is the failure mode this entire mechanism is built against — and it is the failure this file has now found five times. A later author reaching for a conditional skip here has to argue with this paragraph first.

**CONFIRMED, NOT ASSUMED:** `vitest.config.ts`'s `include` globs are `["tests/**/*.spec.ts", "packages/*/src/**/*.spec.ts", "scripts/ci/**/*.spec.ts"]`. The second glob already covers `packages/backend/src/outbound-prohibition.spec.ts`, so the new ledger check needs no new include. Read from that file, not inferred.

## THE THREE DISTINGUISHABLE FAILURE MESSAGES, SIDE BY SIDE

Executed, printed from the shipped code:

```
[MISSING LEDGER]   PlanningLedgerAbsentError: .planning/__this-file-does-not-exist__.md does not exist on
                   this tree, so CORE-11's derived residual could not be read. This is NOT a skip: the check
                   fails deliberately. If you are on a branch produced by the `gsd-pr-branch` workflow, the
                   `.planning/` commits were filtered out by design — run this suite on the full tree.

[MISSING SENTINEL] SentinelMissingError: the END sentinel is absent. Looked for a line containing:
                   END DERIVED RESIDUAL. The generated block is machine-owned; restore the sentinel and
                   paste deriveResidual(RESOLVER_REGISTRY)'s output between the markers.

[MISSING SENTINEL] SentinelMissingError: the BEGIN sentinel is absent. Looked for a line containing:
                   BEGIN DERIVED RESIDUAL - generated by deriveResidual(RESOLVER_REGISTRY) in
                   packages/backend/src/outbound-prohibition.spec.ts - MACHINE-OWNED, DO NOT HAND-EDIT.
                   The generated block is machine-owned; restore the sentinel and paste
                   deriveResidual(RESOLVER_REGISTRY)'s output between the markers.

[DIVERGED BLOCK]   AssertionError: the derived residual block in <path> DIVERGED from
                   deriveResidual(RESOLVER_REGISTRY). The GENERATED text is authoritative and the shipped
                   text is the defect. Replace the span between the sentinels with exactly this:
                   ----- BEGIN EXPECTED ----- <295 lines> ----- END EXPECTED -----
```

A reader of any one of these alone can tell a MISSING LEDGER from a MISSING SENTINEL from a DIVERGED BLOCK. Both extractor failing paths and the ledger-absent path are executed in committed fixtures.

## SIX MUTATION PROOFS — all six, each executed separately

| # | direction | mutation | test that went RED | assertion message it carried | restore | re-run |
|---|---|---|---|---|---|---|
| 1 | prose without code, GATE HEADER | one word inside the shipped block: `* constStrings` → `* constStringz` | `the block shipped in the gate header equals deriveResidual(RESOLVER_REGISTRY), byte for byte` | `the derived residual block in packages/backend/src/outbound-prohibition.spec.ts DIVERGED from deriveResidual(RESOLVER_REGISTRY). The GENERATED text is authoritative and the shipped text is the defect. Replace the span between the sentinels with exactly this: ----- BEGIN EXPECTED ----- …full block… ----- END EXPECTED -----` + a one-line diff naming the changed entry | `git checkout --`, `git diff --exit-code` CLEAN | 215/215 → 253/253 green |
| 2 | registry without regeneration | `constStrings` row's `expect` `["outbound-send"]` → `["outbound-net"]`, block untouched | BOTH `registry row constStrings — its probe AND its counter-probe are executed against auditSource` AND the gate-header byte comparison | `registry row \`constStrings\` says its probe reports [outbound-net]. It does not. The row was read off \`auditSource > const constStrings = new Map<string, Set<string>>()\`; either that branch changed and the row is now stale, or the row was transcribed from prose instead of from the branch.: expected [ 'outbound-send' ] to deeply equal [ 'outbound-net' ]` | `git diff --exit-code` CLEAN | green |
| 3 | unregistered COLLECTOR | `const plantedAliases = new Set<string>();` planted inside `auditSource` | `every member of BOTH populations is a registry row OR a named, reasoned exemption` (and the non-vacuity count case, `expected 12 to be 11`) | `resolver-shaped declaration(s) plantedAliases are NEITHER a RESOLVER_REGISTRY row NOR an entry on RESOLVER_EXEMPTIONS. Either add a registry row — with a probe and a counter-probe you have RUN — or add an exemption naming it with one clause saying why it is not a resolver. Silently absent is the one option this guard removes.` | `git diff --exit-code` CLEAN | 257/257 green |
| 4 | a rule with NO row | `unwrap`'s probe/expect moved off `outbound-net` so no row produces it | `every rule identifier in RULES appears in at least one registry row's expectation` (and the byte comparison) | `rule identifier(s) outbound-net appear in NO registry row. A rule with no row is a rule the derived residual says nothing about: the text would enumerate the walk's mechanisms while staying silent about a surface the gate claims to enforce. Add a row whose probe produces it.` | `git diff --exit-code` CLEAN | 253/253 green |
| 5a | prose without code, `REQUIREMENTS.md` | one word inside the LEDGER's span: `global KEY resolves` → `global TOKEN resolves` | `the block shipped in .planning/REQUIREMENTS.md equals deriveResidual(RESOLVER_REGISTRY), byte for byte` — and ONLY that surface's test; the gate-header comparison stayed green | `the derived residual block in .planning/REQUIREMENTS.md DIVERGED from deriveResidual(RESOLVER_REGISTRY).` + the one-line diff | `git diff --exit-code` CLEAN | 262/262 green |
| 5b | CODE without prose | `unwrap`'s COMMA-SEQUENCE arm DELETED; registry and both blocks untouched | `registry row unwrap — its probe AND its counter-probe are executed against auditSource` — plus the pre-existing fixture `through the COMMA SEQUENCE rule plus constStrings`. Neither byte comparison moved, which is exactly right | `registry row \`unwrap\` says its probe reports [outbound-net]. It does not. The row was read off \`module scope > function unwrap(node: ts.Expression): ts.Expression {\`; either that branch changed and the row is now stale, or the row was transcribed from prose instead of from the branch.: expected [] to deeply equal [ 'outbound-net' ]` | `git diff --exit-code` CLEAN | 262/262 green |
| 6 | unregistered, UNLISTED resolver FUNCTION | `const plantedReceiver = (node: ts.Expression): boolean => node !== undefined;` planted inside `auditSource` | `every member of BOTH populations is a registry row OR a named, reasoned exemption` (and the non-vacuity count case, `expected 38 to be 37`) | `resolver-shaped declaration(s) plantedReceiver are NEITHER a RESOLVER_REGISTRY row NOR an entry on RESOLVER_EXEMPTIONS. …` | `git diff --exit-code` CLEAN | 257/257 green |

**That is SIX rows, and the sixth is the one that covers the population WR-27 lived in.** Proof 3 plants a COLLECTOR and says nothing about the function-shaped half; proof 6 plants a FUNCTION inside `auditSource`, which is the same shape as `receiverKind` (WR-27), `keyReceiver` (wave 24) and `initializerReceiver` (wave 25).

## THE SURFACE DECISION, PASTED FROM ALL FOUR ARTIFACTS CONSECUTIVELY

**1 — `packages/backend/src/outbound-prohibition.spec.ts` (gate header, immediately above the sentinels):**

> THE SURFACE DECISION, wave 27, written here because this is one of the four places the difference is visible. The DERIVED text ships in exactly TWO surfaces - this gate header and `.planning/REQUIREMENTS.md`'s CORE-11 entry - and BOTH are byte-compared to deriveResidual(RESOLVER_REGISTRY) by this suite. `.planning/STATE.md` and `.planning/WINDOWS.md` carry a POINTER to those two and restate NO bound of their own, because they are append-only DATED HISTORIES: a block regenerated inside one would rewrite the record of what was believed and when, which is the only thing that makes a history worth keeping. THAT POINTER-NOT-A-BOUND RULE IS A PROHIBITION WITH NO MECHANICAL CHECK - the byte comparison reaches these two surfaces and no further - and it is named as an unguarded limit rather than left implicit.

**2 — `.planning/REQUIREMENTS.md` (CORE-11, above the BEGIN sentinel):**

> **THE SURFACE DECISION, wave 27, written here because this is one of the four places the difference is visible.** The DERIVED text ships in exactly TWO surfaces — the gate header and this entry — and BOTH are byte-compared to `deriveResidual(RESOLVER_REGISTRY)` by the suite. `.planning/STATE.md` and `.planning/WINDOWS.md` carry a POINTER to those two and restate NO bound of their own, because they are append-only DATED HISTORIES: a block regenerated inside one would rewrite the record of what was believed and when, which is the only thing that makes a history worth keeping. That pointer-not-a-bound rule is a PROHIBITION WITH NO MECHANICAL CHECK — the byte comparison reaches these two surfaces and no further — and it is named as an unguarded limit rather than left implicit.

**3 — `.planning/STATE.md` (P9-D3, FOURTH POINTER AMENDMENT):**

> WHY THIS DECISION LOG CARRIES A POINTER AND NOT THE TEXT, which is the reason a reader should find AT the difference rather than in a plan file: this is an append-only DATED HISTORY, and a block regenerated inside it would rewrite the record of what was believed and when, which is the only thing that makes a history worth keeping.

**4 — `.planning/WINDOWS.md` (entry 32):**

> WHY THIS LEDGER POINTS RATHER THAN COPIES: it is an append-only dated history, and a block regenerated inside it would rewrite the record of what was believed and when. THE POINTER-NOT-A-BOUND RULE HAS NO MECHANICAL CHECK — the byte comparison reaches those two surfaces and no further — and that limit is named rather than left implicit.

## THE LIMITS SENTENCE IN THE LEDGER, beside the derived block

> **THE LIMITS, IN ONE SENTENCE, BECAUSE THE LEDGER IS WHERE AN OVERCLAIM DOES ITS DAMAGE:** this makes the class MECHANICALLY DETECTABLE and does not CLOSE it — every entry below is verified by execution, but the registry is not proven complete, the coverage guard reaches two enumerated populations and names its exemptions rather than covering every shape a resolver can be written in, and each entry's probes are examples rather than a proof of that resolver's domain.

## THE MACHINE-OWNED DISTINCTION, stated inside CORE-11's entry

> **WHICH HALF OF THIS ENTRY EACH RULE APPLIES TO, STATED IN ONE SENTENCE SO NOBODY HAS TO GUESS.** Everything ABOVE the BEGIN sentinel below — the requirement's own text and every dated correction, back to the 2026-08-21 split — is HAND-WRITTEN HISTORY and stays byte-identical under append-never-rewrite; the span BETWEEN the two sentinels is MACHINE-OWNED, is regenerated in place by `deriveResidual(RESOLVER_REGISTRY)` in `packages/backend/src/outbound-prohibition.spec.ts`, and is the only part of this entry a later plan may rewrite.

## APPEND-NEVER-REWRITE, VERIFIED POSITIVELY

```
$ git diff --stat .planning/REQUIREMENTS.md
 .planning/REQUIREMENTS.md | 311 ++++++++++++++++++++++++++++++++++++++++++++++
 1 file changed, 311 insertions(+)          <- INSERTIONS ONLY, zero deletions

$ for a in "FALSIFIED 2026-08-24 (CR-09)" "CORRECTION 2026-08-24, plan 01-18" \
           "CORRECTION 2026-08-24, plan 01-19" "CORRECTION 2026-08-24, plan 01-23" \
           "CORRECTION 2026-08-22, plan 01-16" "Split out of CORE-01 on 2026-08-21"; do
      grep -c -- "$a" .planning/REQUIREMENTS.md; done
FALSIFIED 2026-08-24 (CR-09)               1
CORRECTION 2026-08-24, plan 01-18          1
CORRECTION 2026-08-24, plan 01-19          1
CORRECTION 2026-08-24, plan 01-23          1
CORRECTION 2026-08-22, plan 01-16          1
Split out of CORE-01 on 2026-08-21         1

$ grep -c "<amendment opening>" .planning/STATE.md
POINTER AMENDMENT 2026-08-24 (gap-closure round 4, plan 01-18 task 3):        => 1
SECOND POINTER AMENDMENT 2026-08-24 (gap-closure round 4, plan 01-19 task 3): => 1
THIRD POINTER AMENDMENT 2026-08-24 (gap-closure round 5, plan 01-23 task 3):  => 1
FOURTH POINTER AMENDMENT 2026-08-24 (gap-closure round 5, plan 01-27 task 3): => 1

$ git diff --stat .planning/STATE.md
 .planning/STATE.md | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)   <- P9-D3 is ONE line; the fourth
                                                    amendment is APPENDED to its end,
                                                    the three previous amendments byte-identical
```

## `gsd-tools windows` — every command with its raw output

```
$ gsd-tools windows fixed 31
{ "ok": true, "ledger": { "schema_version": 1, "open_count": 18, "waived_count": 0,
  "fixed_count": 13, "total_count": 31, "last_updated": "2026-08-24T13:49:09.560Z", … } }

$ gsd-tools windows append --kind deviation --phase 01 \
    --file packages/backend/src/outbound-prohibition.spec.ts --description "<the pointer entry>"
{ "ok": true, "ledger": { "schema_version": 1, "open_count": 19, "waived_count": 0,
  "fixed_count": 13, "total_count": 32, "last_updated": "2026-08-24T13:49:23.650Z", … } }

$ gsd-tools windows status   (entries whose file is this gate)
  13 fixed   20 fixed   22 fixed   23 fixed   25 fixed   26 fixed   27 fixed   28 fixed
  31 fixed   32 open
OPEN ENTRIES ON THIS GATE FILE: 1 -> [32]
```

`.planning/WINDOWS.md` was NOT hand-edited. Its diff is consistent with tool output — the frontmatter counters and the JSON block moved together:

```
$ git diff .planning/WINDOWS.md | grep -E "^[+-](fixed|total|last_updated)"
-fixed_count: 12          +fixed_count: 13
-total_count: 31          +total_count: 32
-last_updated: 2026-08-24T13:01:13.195Z   +last_updated: 2026-08-24T13:49:23.650Z
```

The tool did not refuse; no discrepancy to record.

## THE MECHANISM TABLE'S FATE — decided and stated

Pasted from the header, at the table:

> THE TABLE'S FATE, DECIDED 2026-08-24 (wave 27) RATHER THAN LEFT AS TWO HAND-MAINTAINED LISTS OF ONE FACT. It STAYS HAND-WRITTEN, and the derived block above it is AUTHORITATIVE. The reason is that the two are indexed on different things and neither index can be rendered from the other: this table is organised by SPELLING — what a reader arrives holding — while `RESOLVER_REGISTRY` is organised by MECHANISM, which is what the code has. Rendering this table from the registry would cost the spelling index, which is the only reason the table exists; rendering the registry from this table would make the executed probes secondary to a paragraph, which is the defect wave 27 removes. WHERE THEY DISAGREE, THE DERIVED BLOCK WINS AND A ROW HERE IS THE DEFECT — the derived block's entries are executed against `auditSource`; these rows are not. A future wave that wants one list should widen the registry to carry spellings and render this table from it; that is a separate decision needing its own measurement, and this wave changes no rule.

## Real-tree run — ZERO, with the four measured-exempt sites named

```
REAL TREE: roots=packages/backend/src + packages/engine/src files=23 violations=0
[]
EXEMPT SITE QUIET: compat.ts at() dotted-path walk — cur[key], key a for…of binding
                   -> packages/backend/src/compat.ts reports 0 violations
EXEMPT SITE QUIET: compat.ts ctx[root] — root a parameter
                   -> packages/backend/src/compat.ts reports 0 violations
EXEMPT SITE QUIET: store/observations.ts segments[i]
                   -> packages/backend/src/store/observations.ts reports 0 violations
EXEMPT SITE QUIET: store/observations.ts MIGRATIONS[MIGRATIONS.length - 1]
                   -> packages/backend/src/store/observations.ts reports 0 violations
```

No rule changed in this plan, so the zero is a regression check and nothing more.

## Decisions Made

See `key-decisions` in the frontmatter. The two worth restating in prose:

- **`counterExpect` is explicit, not hardcoded empty.** Three mechanisms in this file NARROW the walk — `shadowedGlobals`, `numericNames`, `isProvablyNumeric` — so their probe is the shape that stays QUIET and their counter-probe is the shape that REPORTS. A registry that hardcoded "the counter-probe produces none" could not express them and would have had to leave them out, which is how a register of mechanisms acquires its first blind spot. What is asserted instead is that a row's two directions DIFFER: a row that answers the same thing both ways would keep passing with its mechanism deleted.
- **The provenance anchor is matched at LINE START, and that is not a detail.** A plain `text.includes(anchor)` would have been satisfied by the row's own `site:` string and again by the generated block's `read off:` line — the provenance would have been checking itself, and it would have passed with the code site deleted. That vacuity is the same shape as everything else this round is about, so it is named in the assertion's own comment.

## Deviations from Plan

### 1. [Rule 2 — Missing Critical] The coverage guard's second population spans BOTH scopes, not only `auditSource`

- **Found during:** Task 2b
- **Issue:** the plan specifies population 2 as "function declarations inside the audit function" and names `isAssembledKey`, `isProvablyNumeric` and `unwrap` as members. Measured, all three are declared at MODULE scope. A guard built to the literal sentence would have EXCLUDED the three functions the sentence itself cited.
- **Fix:** population 2 enumerates functions declared at `auditSource`'s own indentation inside `auditSource` AND at column zero in the resolver region (first `import` → first `describe(`). This is strictly a superset of the literal criterion.
- **Files modified:** `packages/backend/src/outbound-prohibition.spec.ts`
- **Verification:** proof 6 plants a function inside `auditSource` (the half the criterion names) and it goes RED; the module-scope half is exercised by 21 real members, 7 registered and 14 exempt.
- **Committed in:** `1002df9`, and the discrepancy is written into the guard's own docblock rather than only here.

### 2. [Rule 2 — Missing Critical] The provenance check was vacuous as first written

- **Found during:** Task 2a
- **Issue:** `gateText.includes(anchor)` is satisfied by the registry row's own `site` string and by the generated block's `read off:` line, so it would pass with the code site deleted.
- **Fix:** the anchor must match at the START of a line, which only a declaration satisfies. The reasoning is in a comment beside the assertion.
- **Verification:** all 38 anchors verified unique as line-start matches before the rows were written.
- **Committed in:** `d640908`

### 3. [Rule 3 — Blocking] `readPlanningLedger` exported

- **Found during:** Task 3
- **Issue:** the three failure messages had to be shown side by side as EXECUTED output; the reader was module-private and could not be driven from a fixture.
- **Fix:** exported, consistent with `enumerateResolverPopulations`; `knip`'s `ignoreExportsUsedInFile` keeps it honest and `pnpm knip` exits 0.
- **Committed in:** `0a8bbdd` (amended)

### 4. [Deliberate, plan-mandated] `requirements-completed` is EMPTY and `requirements mark-complete` was NOT run

- **Issue:** the plan declares `requirements: [CORE-11]`, and the SUMMARY template says to copy that array verbatim into `requirements-completed`. Doing so would flip CORE-11's checkbox, which this plan's own acceptance criterion forbids in as many words: *"`grep -cE '^- \[[ x]\] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md` returns 1 and the box is `[ ]`. A flip in this plan fails this criterion."*
- **Fix:** `requirements-completed: []`, and `gsd-tools query requirements.mark-complete` deliberately NOT invoked. Wave 28 owns the flip, against the generated block.
- **Verification:** `grep -cE '^- \[[ x]\] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md` → `1`; the row is `- [ ] **CORE-11**`.

---

**Total deviations:** 3 auto-fixed (2 missing-critical, 1 blocking) + 1 deliberate plan-mandated departure from the SUMMARY template.
**Impact on plan:** all three auto-fixes make the mechanism strictly harder to fool. None widens a rule; this plan changes no rule at all.

## Issues Encountered

None that required problem-solving beyond the five recorded prediction-vs-measurement discrepancies, which are the intended output of measuring first.

**One process note, recorded rather than hidden:** regeneration of the two sentinel spans was performed by a THROWAWAY vitest spec that imported `deriveResidual` and rewrote both spans in place; it was deleted before every commit and is not part of the shipped tree. The committed contract is the one the plan specifies — the byte comparison's failure message carries the full expected block, so the fix is a paste. A future wave that regenerates often should consider committing a small regenerator; that is a separate decision and it is named here rather than assumed.

## Known Stubs

None. No hardcoded empty values, no placeholder text, no unwired component.

## User Setup Required

None — no external service configuration.

## Next Phase Readiness

- **Wave 28 can flip CORE-11 against the generated block.** The block is in `.planning/REQUIREMENTS.md` between the two sentinels, byte-compared by the suite, and CORE-11's box is `[ ]`.
- **What is NOT closed, said plainly so wave 28 does not inherit an overclaim:** this makes the class MECHANICALLY DETECTABLE and does not close it. The registry is not proven complete; the coverage guard reaches two enumerated populations and NAMES its exemptions rather than covering every shape a resolver can be written in; each row's probes are examples rather than a proof of that resolver's domain; and the measured-silence list is not proven exhaustive.
- **The unguarded limit still stands:** the pointer-not-a-bound rule for `STATE.md` and `WINDOWS.md` is a prohibition with NO mechanical check. The byte comparison reaches the gate header and `REQUIREMENTS.md` and no further.
- **Still OPEN and still UNOWNED:** the OPERATOR AROUND A GLOBAL RECEIVER — `(ok && globalThis).fetch(url)` and its twins — unchanged by this wave, now carried forward as registry row `silence-operator-around-global-receiver` with an executed probe and counter-probe rather than as a paragraph. Residual (b2), the destructured plain literal key, likewise.
- **Nothing leaked.** This plan changes no rule and adds no widening. The gate runs green over the real tree — 23 files, ZERO violations — inside a 31-file / 1200-test suite, and `pnpm check:bundle` reports the shipped bundle's entire import set as one specifier, `crypto`.

## Final battery

```
$ pnpm test         Test Files  31 passed (31)      Tests  1200 passed (1200)
$ pnpm typecheck    exit 0
$ pnpm lint         exit 0
$ pnpm knip         exit 0
$ pnpm build:backend && pnpm check:bundle
                    packages/backend/dist/index.js: 1 import specifier(s): crypto
$ grep -cE '^- \[[ x]\] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md      1   (box is `[ ]`)
$ git diff --exit-code -- 01-01..01-26 PLANs, 01-VERIFICATION.md, 01-REVIEW.md, 01-UAT.md
                    CLEAN
```

---
*Phase: 01-skeleton-persistence-compatibility*
*Completed: 2026-08-24*

## Self-Check: PASSED

All four modified files and this SUMMARY exist on disk. All five commits
(`7f62ca9`, `d640908`, `1002df9`, `0a8bbdd`, `6201435`) resolve in `git log`.
`deriveResidual` is present in the gate file, the derived block's BEGIN sentinel
is present in `.planning/REQUIREMENTS.md`, and `deriveResidual` is named as a
pointer in both `.planning/STATE.md` and `.planning/WINDOWS.md` — the four
artifacts the `must_haves.artifacts` block requires, each carrying the string it
requires. Every plan-level `<verification>` item was re-run at the end of task 3
and is recorded above: 31 files / 1200 tests green, typecheck / lint / knip exit
0, bundle at exactly one specifier `crypto`, real tree 23 files / ZERO
violations, CORE-11's box `[ ]`, and the frozen plan/verification/review/UAT
files clean under `git diff --exit-code`.
