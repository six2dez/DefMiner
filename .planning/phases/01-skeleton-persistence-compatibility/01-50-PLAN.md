---
phase: 01-skeleton-persistence-compatibility
plan: 50
type: execute
wave: 50
gap_closure: true
gap_ids: [CR-33]
depends_on: [01-49]
requirements: [CORE-11]
autonomous: true
files_modified:
  - .planning/REQUIREMENTS.md
  - packages/backend/src/outbound-prohibition.spec.ts
  - .planning/WINDOWS.md

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

# ROUND 13'S THIRD AND LAST PLAN. IT IS THE ONLY ONE ALLOWED NEAR CORE-11's CHECKBOX.
#
# ---------------------------------------------------------------------------------------------------
# THE FOURTH ITEM OF PASS 14'S `missing:` BLOCK, AND ITS PRECONDITION IS NOT NEGOTIABLE.
# ---------------------------------------------------------------------------------------------------
# "Once and only once CR-33 is closed, re-measure criterion (3) across the seven surfaces in one session
# and, if it discharges, flip the ledger row and the constant IN THE SAME COMMIT."
#
# ONCE AND ONLY ONCE. Plans 01-48 and 01-49 close CR-33 and widen the census that would have caught it.
# This plan measures whether that was enough — BY EXECUTION, in one session, over all seven surfaces —
# and it is allowed to reach either verdict. `[ ]` IS A CORRECT OUTCOME. The gate file says so in its own
# failure message: *"If ANY criterion is unmet, the box stays `[ ]`, the blocking criterion is named in the
# ledger, and this constant says `[ ]`. `[ ]` IS A CORRECT OUTCOME; an unexamined `[x]` is not."*
#
# ---------------------------------------------------------------------------------------------------
# THE BOX HAS BEEN FLIPPED AND REVERTED THREE TIMES. THAT IS THE REASON FOR EVERY CONSTRAINT BELOW.
# ---------------------------------------------------------------------------------------------------
# `e7cc4b6` after round 3, `faca607` after round 4, `4105fd0` after round 8. Until 2026-08-24 the one case
# named for the box could not even see it — its regex was a character class matching BOTH states, so it
# stayed green through two flips and two reverts. The box has now stood at `[ ]` for seven consecutive
# rounds. A fourth premature flip would be worse than an eighth `[ ]`, because a `[ ]` with a named blocker
# is a true statement and an unearned `[x]` is a false one in the ledger a requirement's completion is read
# against.
#
# ---------------------------------------------------------------------------------------------------
# THE CONSTANT IS RESOLVED BY CONTENT. NEVER BY LINE NUMBER. THIS IS NOT A STYLE PREFERENCE.
# ---------------------------------------------------------------------------------------------------
# `CORE11_BOX_EXPECTED` HAS MOVED EVERY SINGLE PASS: pass 12 cited one line, pass 13 cited a line 25 lower,
# pass 14 measured it 82 lines higher again — and plans 01-48 and 01-49 both add lines above it. ANY line
# number written into this plan would be wrong before the executor read it, and writing one would be the
# CR-33 class committed inside the plan that closes CR-33. The constant is located by its NAME and its
# VALUE, and the ledger row by its own text.
#
# ---------------------------------------------------------------------------------------------------
# THE SEVEN SURFACES, AND WHAT PASS 14 LEFT EACH OF THEM AT.
# ---------------------------------------------------------------------------------------------------
# Pass 14 discharged six and named the seventh. This plan re-measures ALL SEVEN, because a discharge
# inherited rather than re-executed is exactly the method that produced five consecutive false bounds, and
# because plans 01-48 and 01-49 changed the file that surfaces 1, 2 and 3 all read from.
#   1. gate file header — pass 14: DISCHARGED, reintroduction reds two census cases.
#   2. gate file prose below the registry — pass 14: BLOCKS, CR-33(a) and CR-33(b). Round 13's whole target.
#   3. `REQUIREMENTS.md` above the sentinel — pass 14: DISCHARGED, byte-compare proved live by mutation.
#   4. `.planning/STATE.md` — pass 14: DISCHARGED, the adjacent dated figure-free pointer amendment.
#   5. `.planning/WINDOWS.md` — pass 14: carries no bound; counters agree with row count.
#   6. `.planning/ROADMAP.md` — pass 14: DISCHARGED, the one live clause deleted.
#   7. phase artifacts — pass 14: dated history correctly marked.
#
# ---------------------------------------------------------------------------------------------------
# THE SIX HARD CONSTRAINTS OF ROUND 13 APPLY BYTE FOR BYTE.
# ---------------------------------------------------------------------------------------------------
# 1. `git grep -E` SILENTLY DROPS `\b` ON THIS PLATFORM — a false ZERO against a real 7. Every census here
#    uses `grep -rE`, and EVERY ZERO IS PRECEDED BY A STATED POSITIVE CONTROL asserted non-zero first.
#    This constraint matters more in this plan than in any other, because this plan's whole method is
#    proving absences.
# 2. NO LINE NUMBER IS A LOCATOR — see above; this plan's central symbol has moved every pass.
# 3. THE SIX DATED-HISTORY RANGES AND ALL DECLARATIONS FROM 01-48 AND 01-49 SURVIVE UNCHANGED.
# 4. THE NINE DECLARED PHRASINGS IN `UNBOUNDED_QUANTIFIERS` GO INTO NO NEW PROSE.
# 5. NO NEW `N..M` RANGE IS WRITTEN ANYWHERE, in the gate file or in `REQUIREMENTS.md`'s derived span.
# 6. NEITHER PIN IS MOVED TO FIT.

must_haves:
  truths:
    - "CRITERION (3) IS RE-MEASURED ON ALL SEVEN SURFACES BY EXECUTION IN ONE SESSION, NOT INHERITED. Six of the seven were discharged by pass 14 and three of those read from a file this round edited twice, so every one is re-run and the command and its output are recorded per surface."
    - "EVERY ZERO IS EARNED. No absence is reported without a positive control executed first and shown non-zero, and every census uses `grep -rE` because `git grep` with an extended-regex flag returns a false zero on this platform that fails closed-looking."
    - "THE VERDICT IS REACHED BEFORE THE EDIT, AND EITHER VERDICT IS A DELIVERABLE. If all seven discharge, the ledger row and the constant flip in ONE commit. If any surface blocks, the box stays `[ ]`, the blocking surface and its evidence are named in the ledger, and the constant is untouched."
    - "THE CONSTANT AND THE ROW ARE RESOLVED BY CONTENT AND MOVE TOGETHER OR NOT AT ALL. `CORE11_BOX_EXPECTED` is located by its name and current value, never by a line number, and the byte pin between it and the ledger row means a commit that changes one without the other turns the suite red."
    - "THE LEDGER RECORDS WHAT WAS MEASURED AND WHEN, IN THE FORM THIS ROW ALREADY USES. A dated correction paragraph naming this plan and this round is appended; the row's existing dated history is byte-unchanged, because this is the copy that drifted through five consecutive rounds and rewriting it is the harm the phase's own threat register exists to prevent."
    - "ANY RESIDUAL IS FILED RATHER THAN NARRATED. Whatever this session finds that is real and unclosed becomes a Broken Windows entry with its file, its text and its kind, and the ledger's frontmatter counters agree with its row count afterwards."
    - "THE CLOSING BASELINE IS EXECUTED, NOT ASSUMED. Whole suite, gate spec alone, typecheck, lint, knip, build and bundle check all run in this session, with the bundle's import set at exactly one specifier and zero debt markers under either source root."
    - statement: "The discharge condition for an append-only surface — an amendment that is adjacent, dated, figure-free and names the sole live publication — is applied as pass 14 wrote it down rather than re-litigated, and if this session disagrees with that condition it says so explicitly instead of quietly applying a different bar."
      verification: backstop

  prohibitions:
    - requirement_id: CORE-11
      category: integrity
      status: unresolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT flip CORE-11's box on any evidence short of all seven surfaces re-measured by execution in this session. The box has been flipped and reverted three times and an unexamined checked box is a false statement in the ledger a requirement's completion is read against."
    - requirement_id: CORE-11
      category: integrity
      status: unresolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT edit `CORE11_BOX_EXPECTED` without editing the ledger row in the same commit, or the reverse. Editing the constant on its own is not a way out and never was; the byte pin exists to make the pair inseparable."
    - requirement_id: CORE-11
      category: integrity
      status: unresolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT locate the constant, the ledger row or any surface by a line number. `CORE11_BOX_EXPECTED` has moved on every pass and both preceding plans in this round add lines above it; a line number written here would be wrong before it was read, and writing one would be the CR-33 class committed inside the round that closes CR-33."
    - requirement_id: CORE-11
      category: integrity
      status: unresolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT rewrite CORE-11's existing dated ledger passages. They are the record of what was believed and when, this row is the copy that drifted through five consecutive rounds, and a correction is APPENDED with its date."
    - requirement_id: CORE-11
      category: integrity
      status: unresolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT report an absence without a positive control. `git grep` with an extended-regex flag drops word boundaries on this platform and returns a false zero, and this plan's entire method is proving absences."

  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "CORE-11's row carrying the verdict this session measured, with a dated correction paragraph naming the round, the surfaces re-executed and — if the box stays unchecked — the blocking surface by name."
    - path: "packages/backend/src/outbound-prohibition.spec.ts"
      provides: "`CORE11_BOX_EXPECTED` holding the same state as the ledger row it pins, changed in the same commit as that row or not at all."
    - path: ".planning/WINDOWS.md"
      provides: "A ledger row for any residual this session finds real and unclosed, with counters that agree with the row count."

  key_links:
    - from: "seven surfaces, six of them discharged by a previous pass"
      to: "seven surfaces re-measured by execution in one session"
      via: "a per-surface command with a positive control before every zero"
    - from: "a ledger checkbox and the constant that pins it"
      to: "one commit changing both or neither"
      via: "content-resolved locators and the existing byte pin between them"
---

<objective>
Measure criterion (3) across all seven surfaces by execution in one session, reach a verdict, and act on
whichever verdict the measurement produces — flipping CORE-11's ledger row and the constant that pins it in
a single commit if all seven discharge, or leaving the box unchecked with the blocking surface named if any
does not.

Purpose: the phase's last open truth. Criteria (1) DERIVED and (2) DRIFT-DETECTABLE are discharged and were
re-proved by five independent mutations in verification pass 14. Criterion (3) — *"the disclosure is the
only bound stated on every surface a reader touches, contradicted nowhere"* — has blocked for seven
consecutive rounds. Plans 01-48 and 01-49 close the class that blocked it and widen the census that would
have caught it. This plan finds out whether that was enough, without inheriting a single one of pass 14's
six discharges, because three of those six read from a file this round edited twice.

Output: a seven-row surface table with the command and output for each, a verdict with its reasoning, one
commit carrying both halves of the flip or neither, a dated ledger correction, and a filed residual.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md
@.planning/phases/01-skeleton-persistence-compatibility/01-SECURITY.md
@.planning/phases/01-skeleton-persistence-compatibility/01-48-SUMMARY.md
@.planning/phases/01-skeleton-persistence-compatibility/01-49-SUMMARY.md
@packages/backend/src/outbound-prohibition.spec.ts
</context>

## Artifacts this phase produces

This plan creates no new code symbol. It changes the VALUE of one existing constant and the STATE of one
existing ledger checkbox, and it appends prose.

**Changed, not created:**

- `CORE11_BOX_EXPECTED` in `packages/backend/src/outbound-prohibition.spec.ts` — its value moves from the
  unchecked form to the checked form if and only if the verdict is discharge. Located by name and value,
  never by line number.
- CORE-11's row in `.planning/REQUIREMENTS.md` — its checkbox moves in the same commit, or neither moves.

**New planning content:**

- A dated correction paragraph appended to CORE-11's entry in `.planning/REQUIREMENTS.md`, in the form that
  entry already uses: a bold heading naming the requirement, the date, the plan and the round.
- Zero or more rows appended to `.planning/WINDOWS.md`, one per residual this session finds real.
- A seven-row criterion-(3) surface table, delivered in `01-50-SUMMARY.md`.

<tasks>

<task type="auto">
  <name>Task 1: criterion (3) re-measured on all seven surfaces by execution in one session, every zero earned by a positive control, and the verdict reached and written down BEFORE any box is touched</name>

  <files>.planning/WINDOWS.md</files>

  <precondition>Plans 01-48 and 01-49 are committed, both SUMMARYs exist on disk, `git status --porcelain` is empty, and the whole suite is green on the current tree. If either plan reported an unresolved residual in its SUMMARY, read it before measuring — a surface discharged while a known residual sits on it is a discharge that was not measured.</precondition>

  <read_first>
    - `01-VERIFICATION.md`, the section headed `Criterion (3), surface by surface`, for the seven surfaces and pass 14's verdict on each — read as the thing being RE-TESTED rather than as the answer.
    - `01-VERIFICATION.md`, the paragraph stating the four-part discharge condition for an append-only surface — adjacent, dated, figure-free, names the sole live publication — because pass 14 wrote it down expressly so a later round could APPLY it rather than re-litigate it, and this is that round.
    - `01-48-SUMMARY.md` and `01-49-SUMMARY.md` in full, especially each one's classification table, disclosed limits and any residual either filed.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the failure message of the case named for CORE-11's box, which states the three criteria and the terminal condition in the file's own words; and both censuses as this round left them.
    - `.planning/STATE.md`'s superseded shadow entry together with the amendment on the line immediately after it, and `.planning/ROADMAP.md`'s phase-01 rows, and `.planning/WINDOWS.md`'s frontmatter counters and its last rows — each read as it stands today rather than as pass 14 described it.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-SECURITY.md`'s frontmatter and its historical block's summary line, so surface 7 is judged against what the file actually marks as superseded.
  </read_first>

  <action>
Measure all seven. Do not inherit one. Three of pass 14's six discharges read from the gate file, and this
round rewrote parts of that file twice — a discharge carried forward across two commits that edited its
subject is not a measurement, it is a hope with a table row.

For each surface, run a command, record the command and its output verbatim, and state the verdict with
its reason. State a POSITIVE CONTROL before every zero, and assert the control non-zero before trusting the
zero beside it: a matcher that finds nothing proves nothing, and `git grep` with an extended-regex flag
silently drops word boundaries on this platform, which is how a false zero gets believed. Use `grep -rE`
throughout and say so in the SUMMARY.

Surface 1, the gate file header: confirm the planning-mention census still reaches it and still bites, by
planting one fabricated mechanism claim above the derived-block BEGIN sentinel, reading the red off a real
run, and restoring. Confirm the count balance and the set checks both still hold on the clean tree.

Surface 2, the gate file prose below the registry: this is the one that has failed every round. Run the
range census's own matcher over the file and confirm the surviving count is what plan 01-48 left it at,
with each survivor confirmed dated history rather than a live claim by reading its sentence and not its
digits. Then plant a fabricated range below the registry, read the red, and restore. Then plant a
fabricated mechanism claim below the registry — the mutation that was invisible before plan 01-49 — read
the red, and restore. A surface that only passes a census it also authored has not been tested.

Surface 3, the ledger above the sentinel: confirm the derived span in `.planning/REQUIREMENTS.md` still
byte-compares against the generated form, by mutating one byte of the shipped span and watching the case
red, then restoring. Confirm the checkbox pin still reads the state the ledger carries.

Surface 4, `.planning/STATE.md`: apply pass 14's four-part condition to the amendment as it stands today —
adjacent to the entry it supersedes, dated, spelling no figure, naming the sole live publication. Check the
figure-free clause by grepping the amendment for the shadow values with a positive control fired somewhere
that does carry one, so the zero is earned. If the condition holds, the surface discharges and this session
says it is applying pass 14's written condition rather than a fresh judgement. If this session DISAGREES
with that condition, say so explicitly and argue it — quietly applying a different bar is how a criterion
becomes unreachable.

Surface 5, `.planning/WINDOWS.md`: confirm it restates no bound, with a positive control; confirm the
frontmatter counters agree with the row count, counted on disk.

Surface 6, `.planning/ROADMAP.md`: confirm no present-tense clause publishes a figure the gate file owns.
Distinguish LIVE from DATED HISTORY by the same criteria plan 01-47 used and pass 14 endorsed, and count
the dated-history survivors so that destroying them would be as loud as leaving a live claim.

Surface 7, phase artifacts: confirm `01-SECURITY.md`'s open register is what its frontmatter says it is,
and that the historical block is still marked superseded on the container. Pass 14 recorded a structural
weakness here in its own words — the marker sits on the container and not on the rows, so a reader or a
grep meets rows that look live. Re-check whether that is still true. If it is, decide explicitly whether it
is a criterion-(3) contradiction or a disclosed limit, and give the reason either way rather than
inheriting pass 14's disposition.

THEN REACH THE VERDICT AND WRITE IT DOWN BEFORE TOUCHING ANYTHING. Seven rows, seven verdicts, one
conclusion. If all seven discharge, say so and hand Task 2 the flip. If any blocks, name it, name its
evidence, and hand Task 2 the un-flip — which is the same amount of work and the same commit discipline,
and the gate file's own message says an unchecked box with a named blocker is a correct outcome.

File anything real. A residual found here that is not closed in this session becomes a Broken Windows entry
with its file, its text and its kind, recorded through the ledger's own tooling so the frontmatter counters
stay consistent with the row count. Narrating a defect in a SUMMARY and not filing it is how a finding
survives a round without an owner.

RESTORE EVERY MUTATION WITH `git checkout --`, never with a copy — this environment aliases the copy
command to its interactive form and it refuses silently. Take a byte digest of every mutated file before
the first mutation and again after the last, show them identical, and finish with `git status --porcelain`
empty except for the ledger row this task may have added.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner || exit 1; SP=packages/backend/src/outbound-prohibition.spec.ts; RQ=.planning/REQUIREMENTS.md; ST=.planning/STATE.md; WN=.planning/WINDOWS.md; RM=.planning/ROADMAP.md; for F in "$SP" "$RQ" "$ST" "$WN" "$RM"; do [ -f "$F" ] || { echo "SURFACE FILE ABSENT: $F"; exit 1; }; done; for P in 01-48 01-49; do [ -f ".planning/phases/01-skeleton-persistence-compatibility/${P}-SUMMARY.md" ] || { echo "PRECONDITION UNMET: ${P}-SUMMARY.md is absent, so the class this plan measures was never closed"; exit 1; }; done; PC1=$(grep -rcF '.planning/' "$SP" || true); [ "${PC1:-0}" -ge 1 ] || { echo "POSITIVE CONTROL 1 FAILED: the planning path prefix matches ZERO lines of the gate file. Every zero below would be a false zero. NOTE: git grep with -E silently drops \\b on this platform and is not used in this gate"; exit 1; }; echo "PC1-PLANNING-MENTIONS=$PC1"; PC2=$(grep -rcE '[0-9]+\.\.[0-9]+' "$SP" || true); [ "${PC2:-0}" -eq 6 ] || { echo "THE GATE FILE CARRIES $PC2 RANGE LINE(S), EXPECTED EXACTLY 6 AS PLANS 01-48 AND 01-49 LEFT IT"; exit 1; }; echo "PC2-RANGE-LINES=$PC2"; PC3=$(grep -rcF 'CORE-11' "$RQ" || true); [ "${PC3:-0}" -ge 1 ] || { echo "POSITIVE CONTROL 3 FAILED: CORE-11 does not appear in the ledger at all"; exit 1; }; echo "PC3-LEDGER-CORE11-MENTIONS=$PC3"; SENTS=$(grep -rcE '^[[:space:]]*//[[:space:]]*(PLANNING|GATE-RANGE)-CENSUS-(BEGIN|END)[[:space:]]*$' "$SP" || true); [ "${SENTS:-0}" -eq 4 ] || { echo "THE FOUR CENSUS SELF-SENTINELS RESOLVE TO $SENTS LINE(S), EXPECTED 4"; exit 1; }; for S in headerRegion rangeCensusRegion bodyRegion CENSUSED_REGIONS GATE_RANGE_MENTIONS BODY_PLANNING_MENTIONS HEADER_PLANNING_MENTIONS liveGeometryReport; do grep -qF "$S" "$SP" || { echo "A MECHANISM THIS PLAN MEASURES IS ABSENT: $S"; exit 1; }; done; CN=$(grep -rcF 'CORE11_BOX_EXPECTED' "$SP" || true); [ "${CN:-0}" -ge 1 ] || { echo "CORE11_BOX_EXPECTED IS ABSENT — RESOLVED BY NAME, NEVER BY LINE NUMBER, AND IT IS NOT THERE"; exit 1; }; UNCH=$(grep -rcF 'CORE11_BOX_EXPECTED = "- [ ] **CORE-11**"' "$SP" || true); CHK=$(grep -rcF 'CORE11_BOX_EXPECTED = "- [x] **CORE-11**"' "$SP" || true); [ $((UNCH+CHK)) -eq 1 ] || { echo "CORE11_BOX_EXPECTED RESOLVES TO NEITHER STATE OR TO BOTH (unchecked=$UNCH checked=$CHK); the byte pin cannot work against an unreadable constant"; exit 1; }; echo "CONSTANT-STATE unchecked=$UNCH checked=$CHK"; RUNCH=$(grep -rcE '^- \[ \] \*\*CORE-11\*\*' "$RQ" || true); RCHK=$(grep -rcE '^- \[x\] \*\*CORE-11\*\*' "$RQ" || true); [ $((RUNCH+RCHK)) -eq 1 ] || { echo "CORE-11's LEDGER ROW RESOLVES TO NEITHER STATE OR TO BOTH (unchecked=$RUNCH checked=$RCHK)"; exit 1; }; echo "LEDGER-ROW-STATE unchecked=$RUNCH checked=$RCHK"; [ "$UNCH" -eq "$RUNCH" ] || { echo "TASK 1 MUST NOT MOVE EITHER HALF: the constant and the row disagree BEFORE Task 2 has run"; exit 1; }; BASE=$(git log --format=%H -1 -- .planning/phases/01-skeleton-persistence-compatibility/01-49-SUMMARY.md); [ -n "$BASE" ] || { echo "COULD NOT RESOLVE THIS PLAN'S BASE COMMIT FROM 01-49-SUMMARY.md; the diffs below would read the index and report 0"; exit 1; }; git merge-base --is-ancestor "$BASE" HEAD || { echo "PLAN 01-49's EXIT COMMIT IS NOT AN ANCESTOR OF HEAD"; exit 1; }; git diff --quiet "$BASE" HEAD -- "$SP" "$RQ" || { echo "TASK 1 CHANGED THE GATE FILE OR THE LEDGER SINCE PLAN 01-49's EXIT; IT OWNS NEITHER — TASK 2 DOES"; exit 1; }; git diff --quiet HEAD -- "$SP" "$RQ" || { echo "THE GATE FILE OR THE LEDGER IS DIRTY IN THE WORKING TREE; every mutation this task plants must be restored with git checkout -- before it exits"; exit 1; }; WROWS=$(grep -cE '^\| [0-9]+ \|' "$WN" || true); [ "${WROWS:-0}" -ge 47 ] || { echo "THE BROKEN WINDOWS LEDGER CARRIES $WROWS ROW(S); pass 14 counted 47 and rows are not removed"; exit 1; }; WTOT=$(grep -E '^total_count:' "$WN" | head -1 | grep -oE '[0-9]+' || true); [ -n "$WTOT" ] || { echo "WINDOWS total_count IS UNREADABLE"; exit 1; }; [ "$WTOT" -eq "$WROWS" ] || { echo "WINDOWS total_count IS $WTOT WHILE $WROWS ROW(S) ARE ON DISK; the counters must agree with the row count"; exit 1; }; echo "WINDOWS-ROWS=$WROWS TOTAL=$WTOT"; pnpm typecheck || { echo "TYPECHECK FAILED"; exit 1; }; pnpm lint || { echo "LINT FAILED"; exit 1; }; pnpm test || { echo "SUITE FAILED"; exit 1; }; pnpm check:bundle | tee /dev/stderr | grep -qE '1 import specifier\(s\): crypto' || { echo "THE SHIPPED BUNDLE'S IMPORT SET IS NO LONGER EXACTLY ONE SPECIFIER"; exit 1; }; DEBT=$(grep -rlnE '(TBD|FIXME|XXX|HACK|PLACEHOLDER)' packages/backend/src packages/engine/src 2>/dev/null | grep -c . || true); [ "${DEBT:-0}" -eq 0 ] || { echo "DEBT MARKERS APPEARED UNDER A SOURCE ROOT ON $DEBT FILE(S)"; exit 1; }; OTHER=$(git status --porcelain | grep -v 'WINDOWS.md' | grep -v '01-50-SUMMARY.md' | grep -c . || true); [ "${OTHER:-0}" -eq 0 ] || { echo "TASK 1 LEFT $OTHER FILE(S) DIRTY BEYOND THE WINDOWS LEDGER AND ITS SUMMARY: $(git status --porcelain | tr '\n' ' ')"; exit 1; }; echo TASK1-MEASURED</automated>
  </verify>

  <acceptance_criteria>
    - A seven-row table, one row per surface, each carrying the command run, its output, the verdict and the reason. No row may cite pass 14 as its evidence.
    - Every zero in the session preceded by a named positive control shown non-zero, with an explicit statement that `grep -rE` was used throughout and `git grep` was not, and the reason.
    - Surface 1: the planting mutation above the derived-block sentinel executed and its red pasted, with the restoration proved.
    - Surface 2: BOTH mutations executed — a fabricated range and a fabricated mechanism claim, each planted below the registry — with both reds pasted, and an explicit statement that the second was invisible before plan 01-49.
    - Surface 2: each surviving range read IN ITS SENTENCE and confirmed dated history, not confirmed by its digits.
    - Surface 3: the byte-compare watched failing on a one-byte mutation of the shipped span, with the restoration proved.
    - Surface 4: pass 14's four-part condition applied clause by clause with evidence for each, and an explicit statement that this session is applying that written condition — or, if it disagrees, the disagreement argued rather than silently substituted.
    - Surface 5: the counters recomputed from the row count on disk and shown to agree.
    - Surface 6: the LIVE-versus-DATED-HISTORY split executed with the criteria named, and the dated-history survivor count stated.
    - Surface 7: an explicit decision on whether the container-level supersession marker is a criterion-(3) contradiction or a disclosed limit, with the reason, reached in this session rather than inherited.
    - THE VERDICT stated in one sentence before any box is discussed, followed by its reasoning.
    - Every mutation restored with `git checkout --`, never a copy, with the reason named; byte digests before the first and after the last mutation shown identical.
    - Any residual filed as a Broken Windows row through the ledger's own tooling, with the frontmatter counters shown agreeing with the row count afterwards — or an explicit statement that none was found, with the search that established it.
  </acceptance_criteria>

  <reversibility rating="reversible">A measurement session plus, at most, appended ledger rows. Every mutation is restored in-session and proved restored by digest.</reversibility>

  <done>
All seven surfaces are re-measured by execution in one session with every zero earned by a positive
control, a verdict is stated with its reasoning before any checkbox is touched, and any residual is filed
rather than narrated.
  </done>
</task>

<task type="auto">
  <name>Task 2: act on the verdict — the ledger row and the constant flipped in ONE commit if all seven discharge, or the box left unchecked with the blocking surface named, with both locators resolved by content</name>

  <files>.planning/REQUIREMENTS.md, packages/backend/src/outbound-prohibition.spec.ts</files>

  <precondition>Task 1 is committed and its seven-row surface table and one-sentence verdict exist in the SUMMARY. If the table is absent, or if any row cites verification pass 14 as its evidence rather than a command run in this session, STOP — this task's only input is a measurement, and acting on an inherited one is the method that produced three reverted flips.</precondition>

  <read_first>
    - `01-50-SUMMARY.md` as Task 1 left it: the seven-row table and the verdict, which are this task's entire input.
    - `.planning/REQUIREMENTS.md` — CORE-11's row and every dated correction paragraph beneath it, read as a sequence so the new paragraph matches the form the row already uses and lands after the last one rather than inside them.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the declaration of `CORE11_BOX_EXPECTED` located by grepping its NAME, and the failure message of the case that pins it, which states the terminal condition, records the three reverted flips and says in its own words that an unchecked box is a correct outcome.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-SECURITY.md` — the closure-row form, so a threat row this round can now close is closed with dated evidence rather than re-numbered.
  </read_first>

  <action>
Read the verdict from Task 1's table. There are exactly two branches and both are deliverables.

BRANCH A — ALL SEVEN DISCHARGE. Flip both halves in ONE commit and no other file in that commit. In
`.planning/REQUIREMENTS.md`, CORE-11's row checkbox moves from the unchecked form to the checked form; the
rest of the row's text is byte-identical. In the gate file, `CORE11_BOX_EXPECTED`'s string literal moves
from the unchecked form to the checked form. LOCATE BOTH BY CONTENT: grep the gate file for the constant's
NAME and edit the line the grep returns, and locate the ledger row by its own leading text. Do not write a
line number into a command, a comment or the commit message as a locator — that constant has moved on
every single pass and both preceding plans in this round added lines above it, so a line number recorded
here is wrong before it is read, and recording one inside the round that closes CR-33 would be the class
returning through its own closing commit. Then append a dated correction paragraph to CORE-11's entry, in
the form that entry already uses: a bold heading naming the requirement, the date, this plan and this
round; then what was measured, on which seven surfaces, by which commands; then the statement that criteria
(1), (2) and (3) were each verified BY EXECUTION in one session, which is the terminal condition the gate
file's own message sets. Name the residuals that stay open under the discharged bar rather than implying
none remain — the disclosed limits of both censuses, and anything Task 1 filed. Do not rewrite one byte of
the row's existing dated passages; this is the copy that drifted through five consecutive rounds and the
correction is APPENDED.

BRANCH B — ANY SURFACE BLOCKS. Change neither the row nor the constant, and say so as a decision rather
than as an omission. Append a dated correction paragraph in the same form, naming the blocking surface, the
command that established it and the evidence, and stating plainly that the box stays unchecked because a
criterion is unmet. The gate file's own message already says this is a correct outcome and an unexamined
checked box is not; quote it. File the blocker as a Broken Windows entry if Task 1 has not already, and
name the round that will own it.

IN BOTH BRANCHES, prove the pairing rather than asserting it. After the edit, run the gate spec alone and
confirm the case named for the box is green — that case byte-pins the constant against the ledger row, so a
commit that moved one without the other turns it red, and reading that green is the proof that the pair
moved together. Then confirm by `git show --stat` that the flip commit touches exactly the two files it is
allowed to touch, in branch A, or neither of them in branch B.

Then close what this round earned in the security register, if anything. If a threat row in `01-SECURITY.md`
is now closable against evidence re-executed in this session, close it with a dated closure row that quotes
the row's original words and pastes what re-execution returned — the form every existing closed row in that
file already uses. Leave the historical block byte-unchanged; plan 01-47 established that its rows are a
verbatim snapshot and that scoping a gate to their status column is the misfire, not the fix.

Finish with the round's closing baseline, executed rather than assumed: whole suite, gate spec alone,
typecheck, lint, knip, build and bundle check, with counts read off this session's runs and reconciled
against plan 01-49's exit values. State the delta and attribute it.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner || exit 1; SP=packages/backend/src/outbound-prohibition.spec.ts; RQ=.planning/REQUIREMENTS.md; SU=.planning/phases/01-skeleton-persistence-compatibility/01-50-SUMMARY.md; for F in "$SP" "$RQ"; do [ -f "$F" ] || { echo "FILE ABSENT: $F"; exit 1; }; done; [ -f "$SU" ] || { echo "01-50-SUMMARY.md ABSENT — TASK 1's VERDICT IS THIS TASK'S ONLY INPUT"; exit 1; }; PC=$(grep -rcF 'CORE11_BOX_EXPECTED' "$SP" || true); [ "${PC:-0}" -ge 1 ] || { echo "POSITIVE CONTROL FAILED: CORE11_BOX_EXPECTED is absent from the gate file, so both counts below are false zeros. Resolved BY NAME, never by line number. NOTE: git grep with -E drops \\b on this platform and is not used here"; exit 1; }; echo "PC-CONSTANT-REFS=$PC"; UNCH=$(grep -rcF 'CORE11_BOX_EXPECTED = "- [ ] **CORE-11**"' "$SP" || true); CHK=$(grep -rcF 'CORE11_BOX_EXPECTED = "- [x] **CORE-11**"' "$SP" || true); [ $((UNCH+CHK)) -eq 1 ] || { echo "CORE11_BOX_EXPECTED RESOLVES TO NEITHER STATE OR BOTH (unchecked=$UNCH checked=$CHK)"; exit 1; }; RUNCH=$(grep -rcE '^- \[ \] \*\*CORE-11\*\*' "$RQ" || true); RCHK=$(grep -rcE '^- \[x\] \*\*CORE-11\*\*' "$RQ" || true); [ $((RUNCH+RCHK)) -eq 1 ] || { echo "CORE-11's LEDGER ROW RESOLVES TO NEITHER STATE OR BOTH (unchecked=$RUNCH checked=$RCHK)"; exit 1; }; [ "$UNCH" -eq "$RUNCH" ] && [ "$CHK" -eq "$RCHK" ] || { echo "THE CONSTANT AND THE LEDGER ROW DISAGREE: constant unchecked=$UNCH checked=$CHK, row unchecked=$RUNCH checked=$RCHK. THEY MOVE IN ONE COMMIT OR NOT AT ALL, AND EDITING THE CONSTANT ON ITS OWN IS NOT A WAY OUT"; exit 1; }; echo "PAIR-AGREES unchecked=$UNCH checked=$CHK"; if [ "$CHK" -eq 1 ]; then FC=$(git log --format=%H -1 -- "$SP"); RC=$(git log --format=%H -1 -- "$RQ"); [ -n "$FC" ] && [ -n "$RC" ] || { echo "COULD NOT RESOLVE THE LAST COMMIT OF ONE OR BOTH HALVES"; exit 1; }; [ "$FC" = "$RC" ] || { echo "THE FLIP DID NOT LAND IN ONE COMMIT: the gate file's last commit is $FC and the ledger's is $RC. Pass 14's instruction is IN THE SAME COMMIT, and the byte pin exists so the pair cannot drift apart"; exit 1; }; echo "SINGLE-FLIP-COMMIT=$FC"; git show --stat --format= "$FC" | grep -qE 'outbound-prohibition\.spec\.ts' || { echo "THE FLIP COMMIT DOES NOT TOUCH THE GATE FILE"; exit 1; }; git show --stat --format= "$FC" | grep -qE 'REQUIREMENTS\.md' || { echo "THE FLIP COMMIT DOES NOT TOUCH THE LEDGER"; exit 1; }; fi; NEWP=$(grep -rcE '^\*\*CORE-11 —' "$RQ" || true); [ "${NEWP:-0}" -ge 2 ] || { echo "CORE-11's ENTRY CARRIES $NEWP DATED CORRECTION HEADING(S); the row has carried several for many rounds, so this is not the file this gate was written against"; exit 1; }; git diff --quiet HEAD~1 HEAD -- "$RQ" && { echo "TASK 2 APPENDED NO DATED CORRECTION PARAGRAPH TO THE LEDGER. BOTH BRANCHES REQUIRE ONE: a verdict recorded is a deliverable, and an unchecked box with a named blocker is a correct outcome"; exit 1; }; RANGES=$(grep -rcE '[0-9]+\.\.[0-9]+' "$SP" || true); [ "${RANGES:-0}" -eq 6 ] || { echo "THE GATE FILE CARRIES $RANGES RANGE LINE(S), EXPECTED EXACTLY 6"; exit 1; }; SENTS=$(grep -rcE '^[[:space:]]*//[[:space:]]*(PLANNING|GATE-RANGE)-CENSUS-(BEGIN|END)[[:space:]]*$' "$SP" || true); [ "${SENTS:-0}" -eq 4 ] || { echo "THE FOUR CENSUS SELF-SENTINELS RESOLVE TO $SENTS LINE(S), EXPECTED 4"; exit 1; }; DEBT=$(grep -rlnE '(TBD|FIXME|XXX|HACK|PLACEHOLDER)' packages/backend/src packages/engine/src 2>/dev/null | grep -c . || true); [ "${DEBT:-0}" -eq 0 ] || { echo "DEBT MARKERS APPEARED UNDER A SOURCE ROOT ON $DEBT FILE(S)"; exit 1; }; pnpm typecheck || { echo "TYPECHECK FAILED"; exit 1; }; pnpm lint || { echo "LINT FAILED"; exit 1; }; pnpm test || { echo "SUITE FAILED — if the case named for CORE-11's box is what went red, the constant and the ledger row disagree and the pair must move together"; exit 1; }; pnpm knip || { echo "KNIP FAILED"; exit 1; }; pnpm check:bundle | tee /dev/stderr | grep -qE '1 import specifier\(s\): crypto' || { echo "THE SHIPPED BUNDLE'S IMPORT SET IS NO LONGER EXACTLY ONE SPECIFIER"; exit 1; }; N=$(git status --porcelain | grep -c . || true); [ "$N" -eq 0 ] || { echo "TREE DIRTY AT EXIT: $N"; exit 1; }; echo TASK2-GREEN</automated>
  </verify>

  <acceptance_criteria>
    - The branch taken stated in one sentence, with the verdict row from Task 1's table that determined it.
    - BRANCH A only: `git show --stat` for the flip commit pasted, showing exactly two files touched and both halves inside it.
    - BRANCH A only: the case named for CORE-11's box shown GREEN after the flip, read off a gate-spec run, with an explicit statement that this green is what proves the pair moved together.
    - BRANCH B only: an explicit statement that neither half was touched, with `git diff` on both shown empty for the code half, and the blocking surface named with the command that established it.
    - BOTH BRANCHES: the appended dated correction paragraph pasted verbatim, matching the form the entry already uses, and an explicit statement that no existing dated passage in the row was altered — proved by a diff scoped to the row's prior text.
    - BOTH BRANCHES: the residuals that remain under whatever bar was applied, named — the disclosed limits of both censuses and anything filed to the Broken Windows ledger. A discharge that implies no residual remains is an overclaim.
    - An explicit statement that both locators were resolved by CONTENT, with the grep commands pasted, and that no line number appears as a locator in any command, comment or commit message.
    - Any `01-SECURITY.md` row closed in this session shown with its dated closure row quoting the original words and pasting what re-execution returned, and the historical block shown byte-unchanged.
    - Closing baseline pasted: whole suite file and test counts, gate spec alone, typecheck, lint, knip, build, bundle check — each read off this session's run, reconciled against plan 01-49's exit, with the delta attributed to the cases this round added.
  </acceptance_criteria>

  <reversibility rating="reversible">A one-character change in a ledger checkbox and in a string constant, plus appended prose. All three prior flips of this box were reverted by a single commit each, and the byte pin between the two halves makes a half-applied state impossible to leave green.</reversibility>

  <done>
The verdict Task 1 measured has been acted on: either both halves of CORE-11's box moved in one commit with
the case that pins them green, or neither moved and the blocking surface is named with its evidence — and
in both cases a dated correction paragraph records what was measured, on which surfaces, by which commands,
with the remaining residuals named rather than implied away.
  </done>
</task>

</tasks>

<assumption_delta_decision>
`no-change`. This plan adds no case, no region and no representation — it measures existing surfaces and
changes the value of one constant and the state of one checkbox. The pluralization the detector fired on is
carried by plans 01-48 and 01-49, and their `promote` decision is recorded in those plans rather than
restated here, because two records of one decision is how an amendment loses its attribution.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measurement session → ledger claim | A checked box is the statement a requirement's completion is read against. Three prior flips crossed this boundary on evidence that did not hold. |
| constant ↔ ledger row | Two files hold the same fact. A commit moving one without the other leaves a half-applied state. |
| prior pass verdict → this session's verdict | Six discharges were inherited from pass 14; three of them read from a file this round edited twice. |
| absence claim → belief | This plan's whole method is proving absences, and the platform's `git grep` returns a false zero that fails closed-looking. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-333 | Spoofing | CORE-11's checkbox in `.planning/REQUIREMENTS.md` | high | mitigate | The flip is gated on all seven surfaces re-measured by execution in this session, with the verdict written down before any box is discussed. Task 2's precondition stops the task if any table row cites pass 14 rather than a command run here. |
| T-01-334 | Tampering | `CORE11_BOX_EXPECTED` versus the ledger row | high | mitigate | The verify block asserts the two halves agree in both directions and, when checked, that they share a single commit resolved by `git log`. The existing byte pin turns the suite red on any half-applied state. |
| T-01-335 | Spoofing | a false zero from `git grep` with an extended-regex flag | high | mitigate | Prohibited by name; every count uses `grep -rE`, and each gate fires a named positive control and asserts it non-zero before any zero beside it is trusted. |
| T-01-336 | Tampering | a line number used as a locator for the constant | high | mitigate | Prohibited by name and by acceptance criterion. The constant has moved on every pass and both preceding plans add lines above it; both locators are resolved by content and the grep commands are pasted as proof. |
| T-01-337 | Repudiation | CORE-11's existing dated ledger passages | high | mitigate | Corrections are APPENDED with a date in the form the entry already uses; the verify requires a new paragraph and the acceptance criteria require a diff proving no prior passage was altered. |
| T-01-338 | Information disclosure | a discharge implying no residual remains | medium | mitigate | Both branches must name the residuals that remain under the bar applied — the disclosed limits of both censuses and anything filed to the Broken Windows ledger. A discharge that implies none is called an overclaim in the acceptance criteria. |
| T-01-339 | Denial of service | a gate scoped so that green requires falsifying dated history | medium | mitigate | Every count gate in this plan is an equality against a measured population rather than a demand for zero, which is the correction plan 01-47's refusal established; the historical block in the security register is asserted byte-unchanged rather than status-scoped. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan; every manifest and the lockfile are untouched, and `pnpm check:bundle` asserts the shipped import set is still exactly one specifier. RESEARCH.md's Package Legitimacy Audit is unchanged and raises no `[ASSUMED]` or `[SUS]` entry for this work. |
</threat_model>

<verification>
- All seven criterion-(3) surfaces re-measured by execution in one session, no row citing a prior pass as its evidence.
- Every zero preceded by a named positive control shown non-zero, every census run with `grep -rE`.
- Surfaces 1, 2 and 3 each watched failing under a planted mutation and each restored, with restoration proved by digest.
- The verdict stated in one sentence before any checkbox is touched.
- The constant and the ledger row agree in both directions; if checked, they share exactly one commit touching exactly those two files.
- A dated correction paragraph appended in either branch, with no prior dated passage altered.
- Residuals named, not implied away; anything real filed to the Broken Windows ledger with counters agreeing with the row count.
- Closing baseline executed: whole suite, gate spec alone, typecheck, lint, knip, build, bundle check at one specifier, zero debt markers under either source root.
</verification>

<success_criteria>
CORE-11's box carries the state this session measured rather than the state anyone hoped for: both halves
moved in one commit with the case that pins them green, or neither moved and the blocking surface is named
with the command that established it — and either way the ledger records what was measured, on which
surfaces, by which commands, with the remaining residuals named.
</success_criteria>

<output>
Create `.planning/phases/01-skeleton-persistence-compatibility/01-50-SUMMARY.md` when done.
</output>
