---
phase: 01-skeleton-persistence-compatibility
plan: 13
type: execute
wave: 13
gap_closure: true
depends_on: [01-12]
files_modified:
  - packages/backend/src/store/error-redaction.spec.ts
# TRANSIENT MUTATION TARGET — planted and reverted inside task 1, restore
# cross-checked by `git diff --exit-code` in that task's acceptance criteria:
#   packages/backend/src/store/artifacts.ts
# Deliberately NOT a list item: `files_modified` is what the wave guard reads to
# decide what may run in parallel, and this file is not durably modified — but a
# reader comparing file lists must not conclude this plan never touches it.
autonomous: true
requirements: [STORE-07]
# NOTE on STORE-07: `REQUIREMENTS.md:55` reads "All SQL uses positional `?`
# parameters" and says nothing about error-render redaction. Inherited from 01-07,
# not introduced here. Plan 01-10 task 3 records this collision and the STORE-03 one
# in REQUIREMENTS.md as deferred WITH AN OWNER, so nobody reads the CORE-11 split as
# evidence that the ledger is now clean. This plan does not fix it and does not
# pretend to.
#
# CORE-11 is NOT declared here. The requirement-ledger split that used to be this
# plan's task 2 moved forward: the ledger half to plan 01-10 task 3 (wave 10), so
# 01-12 can declare CORE-11 in its own frontmatter, and the source-string retag into
# 01-12 task 1, which rewrites those exact regions. Leaving it here would have left
# the "one id, two meanings" defect standing in the frontmatter of the plan that
# owns the gate — which is the one place a verifier reads.

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "`catch (e) { return e.message; }` fails the STORE-07 gate. So do `\"x: \" + e.message`, a template interpolating `e.message`, `String(e.message)`, `e.toString()`, `JSON.stringify(e)`, `String(e as Error)`, `` `${e as any}` `` and `const x = e; String(x)` — every one of which reports clean today."
    - "`e.message` is not an exotic spelling, it is THE spelling; and because TypeScript types a catch binding as `unknown` under `useUnknownInCatchVariables`, `String(e as Error)` and `(e as Error).message` are the forms the compiler pushes an author toward. The gate is blind exactly where the codebase is most likely to drift, and after this plan it is not."
    - "A destructured error-shaped PARAMETER — `function finishAnalysis({ error }: { error: string | null })` — fails the fourth rule, which today requires `ts.isIdentifier(param.name)` and therefore reports clean."
    - "The widened rules report ZERO violations against the real store layer. If a legal site fires, the RULE is narrowed and the narrowing is recorded with its reason — never the site silenced."
    - "The negative fixtures still hold: `describeError(e)` inside a catch, a `String(x)` outside any catch, a type MEMBER named `error`, and a parameter that is not error-shaped all report clean, so the widened rule is not always-on."
    - "The gate's header discloses the reach it has after the widening, and names the three bare renders that remain OUTSIDE its `store/` scope by design, including the one inside `describeError` itself that a future scope widening would trip on."
    - "The two bare renders this gate does NOT cover — `compat.ts:317` and `hooks/passive.ts:171` — have a NAMED owner, not just a disclosure: Phase 2, whose ERR-03 and ERR-04 requirements are about catching and surfacing errors and which is where both sites will be rewritten anyway. The `<output>` OPEN item has something to write in the owner column, and a reader can check whether the owner took it."
  artifacts:
    - path: "packages/backend/src/store/error-redaction.spec.ts"
      provides: "STORE-07's gate at a reach that follows the binding rather than matching it — casts, property access, copies, returns and object-literal values"
      exports: ["auditSource"]
  key_links:
    - from: "packages/backend/src/store/error-redaction.spec.ts"
      to: "packages/backend/src/store"
      via: "the widened rules run over the real store modules and report zero, with the non-vacuity assertion naming every module including db.ts"
      pattern: "derivesFrom"
  prohibitions:
    - requirement_id: STORE-07
      category: security
      status: resolved
      verification: gate
      resolution: "packages/backend/src/store/error-redaction.spec.ts — rules that follow the binding through casts, parentheses, non-null assertions, property and element access, and a copy, with all ten executed shapes from 01-REVIEW.md CR-05 as fixtures"
      statement: "MUST NOT render a caught exception or an error-shaped parameter into a persisted or logged string without passing it through `describeError` first — and MUST NOT let a cast, a property access or a copy of that binding escape the rule, because those are the forms `useUnknownInCatchVariables` pushes an author toward."
    - requirement_id: STORE-07
      category: safety
      status: partial
      verification: gate
      resolution: "packages/backend/src/store/error-redaction.spec.ts header boundary 1 — restated to name compat.ts:317, hooks/passive.ts:171 and the String(e) inside describeError itself, each with why it is outside the store scope, and each carrying its OWNER"
      statement: "MUST NOT leave a bare render outside the gate's scope undisclosed OR unowned. Two production sites still render a caught exception bare — outside `store/`, outside the UAT gap, and recorded as T-01-37 accept — and a third sits inside `describeError`'s own implementation, where it is correct. A reader must be able to tell those three apart from a hole, AND be able to see who is expected to act: Phase 2 (ERR-03, ERR-04) owns the first two; the third is owned by nobody because it is correct."
---

<objective>
Close the third finding folded into UAT gap 2: the STORE-07 redaction gate cannot see `e.message`.

`error-redaction.spec.ts` cannot see `e.message`. All three catch rules bottom out in `isRefTo`, which
is `ts.isIdentifier(node) && node.text === name` — so the binding must appear as a BARE IDENTIFIER.
Reach through it, cast it, or copy it, and the rule is gone. Executed, not argued:

    catch(e){ return e.message; }                []      catch(e){ return "x: " + e.message; }   []
    catch(e){ return `x: ${e.message}`; }        []      catch(e){ return String(e.message); }   []
    catch(e){ return e.toString(); }             []      catch(e){ return JSON.stringify(e); }   []
    catch(e){ return String(e as Error); }       []      catch(e){ return `${e as any}`; }       []
    catch(e){ const x = e; return String(x); }   []      catch(e){ return [e].join(""); }        []
    function fin({ error }: {…}) { … }           []      (rule 4 needs ts.isIdentifier(param.name))

Purpose. `e.message` is not an exotic spelling — it is *the* spelling. And in this codebase
specifically, TypeScript types a catch binding as `unknown`, so anything richer than `String(e)`
requires a cast: `String(e as Error)` and `(e as Error).message` are the forms the compiler pushes an
author toward, and the gate is blind to both. That matters because `schema.spec.ts:62-70` justifies
keeping the `analyses.error` column in the T-01-21 allowlist ON THE STRENGTH OF THIS GATE, the column
is durable for 90 days in a file `db.ts` documents as never garbage-collected, and the gate's own
comment names Phase 2's ERR-02/ERR-04 authors as the people who "will read the gate to learn what is
already guaranteed". They will write `error: (e as Error).message` and ship it green.

**This plan used to carry a second task, the CORE-01 → CORE-11 requirement split, and it moved.** The
ledger half is now plan 01-10 task 3 (wave 10) and the gate's source-string retag is now plan 01-12
task 1. The reason is not cosmetic: 01-12 declares the outbound requirement in its own frontmatter, and
frontmatter is what a verifier reads — so a split landing in wave 13 would have left the exact "one id,
two meanings" defect standing in the frontmatter of the plan that OWNS the gate. Moving it forward
costs nothing and saves an eight-file cleanup later. Recorded here rather than silently deleted, so
somebody reading this plan against the round's history can find where the work went.

Note what that move does NOT fix, because this plan is where a reader would otherwise assume it did.
Two further id collisions remain: STORE-03 (`content-addressed by digest`) is declared by four plans
for URL redaction, and STORE-07 — this plan's own requirement, `All SQL uses positional ? parameters` —
is declared by three for error-render redaction. Neither text says either thing. Both are inherited
from 01-01/01-07, both are recorded as deferred WITH AN OWNER in `REQUIREMENTS.md` by plan 01-10 task 3,
and neither is closed by this round. The ledger is TIDIER after CORE-11; it is not clean.

Output: a gate that follows the binding instead of matching it, with all ten executed shapes as
fixtures, and a header that names — with owners — every render it does not cover.

**Why one task.** After the ledger split moved forward this plan has a single subject, and it is a
dense one: thirteen reach/cast/copy fixtures, two new scan positions, two destructured-parameter forms,
nine must-stay-quiet fixtures, a real-tree zero-violation run and a mutation. Padding it with a second
unrelated task to hit a task count would be the shape this round exists to stop.

**Why wave 13, serialised behind 01-12.** The same reason every plan from 01-09 onward has recorded,
and after the ledger split moved out it is now the ONLY reason — which makes recording it in words
matter more, not less. This plan plants an `e.message` render into a real store module while asserting
the whole suite is green. Its one durable file, `error-redaction.spec.ts`, overlaps nothing in 01-10,
01-11, 01-12 or 01-14, so the wave guard would happily run it beside all four. The coupling is one axis
over and the guard does not model it: 01-10 and 01-11 are reverting redaction branches in the same
tree, 01-12 is planting outbound calls into three shipped modules, 01-14 is reverting the bare-segment
branch under a live Caido — and every one of them asserts the WHOLE suite is green. Run concurrently,
one plan's mutation window makes another's whole-suite assertion fail for an unrelated reason,
intermittently and misattributed to whichever plan read the tree at the wrong moment. A phase whose
entire subject is gates that can fail honestly cannot afford a gate that fails dishonestly.
`depends_on: [01-12]` is a SEQUENCING edge, not a code dependency — recorded in words so nobody reads
the file list and optimises the sequence back into a shared wave.

**Why there is no leading `type="tracer"` task.** `TRACER_MODE` is on. This plan widens a static gate
over already-shipped code; there is no path through the stack to wire. The mutation run in task 1 is
the equivalent evidence and it is stronger for this kind of work.

**What this plan deliberately does NOT do.** It does not widen the gate's SCOPE beyond
`packages/backend/src/store/`. Boundary 1 of the gate's header records that decision as T-01-37
`accept`, the verifier confirmed the store scope as correct by design, and CR-05 is about REACH inside
that scope, not about the scope itself. What this plan does instead is make the disclosure accurate —
see the second prohibition above, and the residual named in task 1 — which now carries an OWNER,
Phase 2, rather than only a reason. It does not touch `01-01-PLAN.md` … `01-12-PLAN.md`: the historical
record of how the phase was planned stays as it is. And it does not touch `REQUIREMENTS.md`,
`ROADMAP.md`, `STATE.md` or `outbound-prohibition.spec.ts` — every one of those moved to 01-10 or
01-12, and a second plan editing them here is how the earlier edit gets silently reverted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md
@packages/backend/src/store/error-redaction.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the redaction gate follow the binding instead of matching it</name>

  <read_first>
    - `packages/backend/src/store/error-redaction.spec.ts` in FULL, all 415 lines. Specifically:
      `isRefTo` at :86-88 (the two-line function that is the whole defect); `isSafeRenderCall` at
      :77-83 and the "do not descend into a `describeError(...)` call" guard at :170, which is what
      keeps the widened rule from flagging the correct form; `scanFor` at :120-174 with its three
      forms; rule 4 at :205-225 and its `ts.isIdentifier(param.name)` condition; the header's TWO
      BOUNDARIES block at :22-38, which this task must restate; and every NEGATIVE fixture at :347-414,
      because those are what stop a widened rule becoming always-on and each must still pass unchanged.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-REVIEW.md` CR-05 in full — the ten
      executed shapes with their verbatim `[]` outputs, the `derivesFrom` sketch, and the sentence
      about `useUnknownInCatchVariables`. The sketch is a starting point, not a specification: it
      unwraps casts, parentheses, non-null assertions, property access and a call-on-member.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md` — the STORE-07 gate
      probe table, 8 shapes, 2 caught and 6 missed. Note which two are the CONTROLS: `catch(e){
      String(e) }` and `catch(ex){ String(ex) }`, the second proving the gate resolves the binding from
      the catch clause rather than from a hard-coded name. Both must still be caught.
    - Every module under `packages/backend/src/store/` — `analyses.ts`, `artifacts.ts`, `db.ts`,
      `migrations.ts`, `observations.ts`, `retention.ts`, `settings.ts` — reading every catch clause
      and every `describeError` call site. The widened rules must report ZERO against all of them, and
      the way to know that before writing the rule is to have read what is there: the shape to expect
      is `catch (e) { return { ok: false, error: describeError(e).slice(0, 200) }; }` and a
      `recordError(e)` call that passes the raw binding to a function which renders it safely.
    - `packages/backend/src/compat.ts` line 317 and `packages/backend/src/hooks/passive.ts` line 171 —
      the two production sites still rendering a caught exception bare, and `packages/backend/src/
      telemetry.ts` line 272, which is the `String(e)` inside `describeError`'s own implementation and
      is CORRECT. All three are outside the gate's scope; the header must be able to tell them apart.
  </read_first>

  <files>packages/backend/src/store/error-redaction.spec.ts</files>

  <behavior>
    THE EVASIONS FIRST. Write every fixture below and confirm each currently reports clean BEFORE
    changing `isRefTo`. Round 1's gates each passed fixtures written by the same reasoning that wrote
    the rule; this list comes from an adversary's execution, not from the rule's author.

    MUST FLAG — reach through the binding:
    - `function f(){ try { g(); } catch (e) { return e.message; } }`
    - `function f(){ try { g(); } catch (e) { return "x: " + e.message; } }`
    - ``function f(){ try { g(); } catch (e) { return `x: ${e.message}`; } }``
    - `function f(){ try { g(); } catch (e) { return String(e.message); } }`
    - `function f(){ try { g(); } catch (e) { return e.toString(); } }`
    - `function f(){ try { g(); } catch (e) { return JSON.stringify(e); } }`

    MUST FLAG — cast, assert, parenthesise, copy:
    - `function f(){ try { g(); } catch (e) { return String(e as Error); } }`
    - `` function f(){ try { g(); } catch (e) { return `${e as any}`; } } ``
    - `function f(){ try { g(); } catch (e) { return String((e)); } }`
    - `function f(){ try { g(); } catch (e) { return String(e!); } }`
    - `function f(){ try { g(); } catch (e) { return (e as Error).message; } }`
    - `function f(){ try { g(); } catch (e) { const x = e; return String(x); } }`
    - `function f(){ try { g(); } catch (e) { return [e].join(""); } }`

    MUST FLAG — the two new positions CR-05 names:
    - the binding, or anything derived from it, RETURNED without passing through `describeError`:
      `function f(){ try { g(); } catch (e) { return e; } }`
    - the binding as an OBJECT-LITERAL property value:
      `function f(){ try { g(); } catch (e) { return { ok: false, error: e.message }; } }`

    MUST FLAG — rule 4's destructured parameter:
    - `function fin({ error }: { error: string | null }) { return error === null ? null : String(error); }`
    - the same with a renamed binding: `function fin({ error: err }: { error: string }) { return String(err); }`

    MUST STAY QUIET — every one of these already passes and every one must still pass, because they are
    what stop the widened rule being always-on:
    - `function f(){ try { g(); } catch (e) { return describeError(e).slice(0, 200); } }`
    - `function f(){ try { g(); } catch (e) { return { ok: false, error: describeError(e).slice(0, 200) }; } }`
      — the real store shape. If this fires, the rule is wrong, not the code.
    - `function f(){ try { g(); } catch (e) { recordError(e); return null; } }` — the raw binding passed
      to a function that renders it safely is not a render.
    - `function f(){ try { g(); } catch { return null; } }` — a bare catch binds nothing.
    - `function f(x) { return String(x).slice(0, 120); }` — outside any catch.
    - `type AnalysisRow = { error: string | null }; const s = String(r.error);` — a type MEMBER named
      `error` declares a field and renders nothing.
    - `function f(label: string) { return String(label); }` and the `contentType` case — parameters that
      are not error-shaped.
    - The two existing CONTROLS: `catch(e){ String(e) }` and `catch(ex){ String(ex) }`, the second
      proving the binding is resolved from the clause and not from the name.

    AND, against the real tree:
    - Every module under `store/` reports ZERO violations under the widened rules.
  </behavior>

  <action>
    Replace `isRefTo` with a `derivesFrom(node, name)` that answers "does this expression DERIVE from
    the binding?" by unwrapping, in a loop until nothing more unwraps: parenthesised expressions, `as`
    expressions, non-null assertions and type assertions; property access and element access; and a
    call whose callee is a property access, which is what makes `e.toString()` visible. Then match the
    identifier at the bottom. Swap it into all three catch rules and into rule 4. That single change
    closes `e.message`, `String(e.message)`, `e.toString()`, `(e as Error).message` and every cast form
    at once, which is the reason CR-05 proposes it rather than a list of special cases.

    Add the copy: a `const`/`let` whose initializer derives from the binding records the new name as
    ALSO the binding, collected in document order in the same walk, so `const x = e; String(x)` is
    seen. This is one hop only and the header must say so — that is the honest boundary, and it is a
    different sentence from "an identifier that merely shares the name in an unrelated inner scope",
    which is the existing disclosure and stays true.

    Add the two positions CR-05 names to `scanFor`: the binding or anything derived from it appearing
    as a RETURN value, and as an OBJECT-LITERAL property value. The existing "do not descend into a
    `describeError(...)` call" guard is what keeps `return { ok: false, error: describeError(e) }`
    quiet, and it must be preserved exactly — verify it by running the real-store fixture from
    `<behavior>` before and after.

    Extend rule 4 to an `ObjectBindingPattern` parameter: for each element, the property name decides
    whether it is error-shaped and the bound name is what gets scanned, so both
    `function fin({ error })` and `function fin({ error: err })` are covered. Keep the
    `ParameterDeclaration`-only matching that stops a type member named `error` being flagged — that
    is `analyses.ts:87`'s real shape and the negative fixture for it must still pass.

    Add `JSON.stringify(x)` and `[x].join(…)` alongside `String(x)` as render forms. `JSON.stringify`
    on an `Error` is a common and worse-than-usual spelling, because it serialises enumerable own
    properties and a driver rejection's are exactly the bound parameters.

    Then RUN THE WIDENED RULES AGAINST THE REAL STORE LAYER and expect zero. If a legal site fires, the
    fix is to narrow the RULE and record the narrowing with its reason in the header — never to silence
    the site, never to add an exception keyed on a file name. A gate with a per-file exception is a gate
    that will grow one more every phase.

    Restate the header's boundary block to the reach the gate has afterwards: it follows the binding
    through wrappers, member access and one hop of copy; it does not build a symbol table, so a value
    crossing a function boundary or more than one hop of indirection is outside it; and it is scoped to
    `store/`. Then name the three renders outside that scope so a reader can tell a decision from a
    hole: `compat.ts:317` and `hooks/passive.ts:171` render a caught exception bare and are outside the
    store layer, outside the UAT gap and recorded as T-01-37 `accept`; and `telemetry.ts:272` is the
    `String(e)` inside `describeError`'s own implementation, which is where the safe rendering happens
    and which any future scope widening must exempt by construction rather than by a file-name check.
    Naming the third one now is what stops that widening looking like a bug to whoever does it.

    Give each of the first two an OWNER in the header, in the same sentence, because a disclosure with
    no owner is a note and this phase has enough notes. The owner is **Phase 2**, and it is not a
    guess: `compat.ts:317` renders `String(e).slice(0, 160)` into a per-surface `error` field the
    operator reads, and `hooks/passive.ts:171` renders `String(e).slice(0, 160)` into `sdk.console.log`
    on the hook's error path — which are ERR-04 ("a per-artifact failure is recorded with its reason
    and is visible to the operator") and ERR-03 ("errors thrown or rejected inside `onInterceptResponse`
    are caught and logged by us") respectively, both owned by Phase 2 in the ROADMAP's Requirement
    Traceability table. Both sites get rewritten by whoever implements those two requirements, which is
    the moment the redaction decision is theirs to make rather than deferred past them. Write the
    requirement ids into the header, not just the phase number, so the owner is checkable against the
    ROADMAP rather than asserted. The third render, `telemetry.ts:272`, is owned by NOBODY and the
    header must say so in those words: it is correct, it is where the redaction happens, and an owner
    on it would imply work that must not be done.

    Then MUTATE AND RUN. Plant `return { ok: false, error: (e as Error).message };` into a real catch
    in `packages/backend/src/store/artifacts.ts`, run the gate, capture the failing output verbatim —
    it must name the file, the rule and the form. Restore, re-run green, and confirm
    `git diff --exit-code packages/backend/src/store/artifacts.ts` is clean.
  </action>

  <acceptance_criteria>
    - `pnpm vitest run packages/backend/src/store/error-redaction.spec.ts` passes with an executed
      fixture for every shape in `<behavior>`: 13 reach/cast/copy forms, 2 new positions, 2 destructured
      parameter forms, 9 must-stay-quiet shapes, and the 2 existing controls.
    - Every one of the 6 shapes the verifier recorded as MISSED, and every one of the 10 in CR-05's
      executed table, now produces at least one violation. Re-run that probe and paste the before/after
      table into the SUMMARY.
    - The real store layer reports ZERO violations under the widened rules — the "reports ZERO
      violations across the store layer" case still passes without any per-file exception. If a rule had
      to be narrowed, the narrowing and its reason are in the header and in the SUMMARY.
    - MUTATION PROOF pasted verbatim into the SUMMARY: `(e as Error).message` planted into a real catch
      in `store/artifacts.ts` makes the gate fail naming the file and the rule; after restoring,
      `git diff --exit-code packages/backend/src/store/artifacts.ts` is clean and the gate is green.
    - The non-vacuity assertions still hold and the module list additionally names `db.ts` (IN-10).
    - The header names `compat.ts`, `hooks/passive.ts` and `telemetry.ts`'s internal `String(e)` as the
      three renders outside the gate's scope, each with why AND with its owner: Phase 2 / ERR-04 for
      `compat.ts:317`, Phase 2 / ERR-03 for `hooks/passive.ts:171`, and explicitly NO OWNER for
      `telemetry.ts:272` because it is correct. `grep -c 'describeError' packages/backend/src/store/error-redaction.spec.ts`
      returns at least 5, the header text names all three files, and
      `grep -c 'ERR-03\|ERR-04' packages/backend/src/store/error-redaction.spec.ts` returns at least 1.
      Cross-check both ids against `.planning/ROADMAP.md`'s Requirement Traceability table before
      writing them; an owner nobody can verify is worse than none.
    - `pnpm test` passes with zero failures over no fewer than 31 files and no fewer than 705 tests;
      `pnpm typecheck`, `pnpm lint`, `pnpm knip` exit 0.
  </acceptance_criteria>

  <verify>
    <automated>pnpm vitest run packages/backend/src/store/error-redaction.spec.ts &amp;&amp; pnpm test &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm knip</automated>
  </verify>

  <done>
    The STORE-07 gate sees `e.message`, every cast form, a copy, a return and an object-literal value,
    and a destructured error-shaped parameter; it still reports zero against the real store layer and
    still stays quiet on every legal shape; and its header names the three renders outside its scope so
    a reader can tell a decision from a hole.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caught exception → `analyses.error` column | A driver rejection carries the statement text and its bound parameters, and one of those parameters is the observation URL. The column is durable for 90 days in a file Caido never garbage-collects. |
| caught exception → host log | `sdk.console.log` is a channel the threat model says must not carry target-controlled content, and on a runtime whose `HANDLER_ERROR_SURFACED` is "neither" it is often the only record that will ever exist. |
| TypeScript's `unknown` catch binding → the author | `useUnknownInCatchVariables` pushes an author toward exactly the cast forms the gate cannot see, so the compiler and the gate were pulling in opposite directions. |
| disclosed residual → nobody | A residual named without an owner is read as somebody else's problem by every reader in turn. `compat.ts:317` and `hooks/passive.ts:171` have been disclosed since 01-07 and nobody has been named. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-68 | Information Disclosure | `e.message` and every reach-through form rendering a caught exception unredacted into `analyses.error` or a log | high | mitigate | Task 1 replaces `isRefTo` with `derivesFrom`, which unwraps casts, parentheses, non-null assertions, property and element access and a call-on-member; adds one hop of copy tracking; and adds the return and object-literal-value positions. All ten of CR-05's executed shapes become fixtures, and the rule is mutation-proven against a real catch in `store/artifacts.ts`. |
| T-01-69 | Information Disclosure | a destructured error-shaped parameter — `function fin({ error })` — writing the `analyses.error` column | high | mitigate | Task 1 extends rule 4 to `ObjectBindingPattern`, keyed on the property name with the bound name scanned, while keeping the `ParameterDeclaration`-only matching that stops a type member named `error` from being flagged. |
| T-01-70 | Tampering | a widened rule that becomes always-on and gets disabled | high | mitigate | Every existing negative fixture must still pass, the real store shape `return { ok: false, error: describeError(e).slice(0, 200) }` is asserted as a must-stay-quiet case, and the rule is required to report ZERO against the real store layer with no per-file exception. A gate with one file-name exception grows one more every phase. |
| T-01-37 | Information Disclosure | bare renders outside the gate's `store/` scope — `compat.ts:317`, `hooks/passive.ts:171` | medium | accept | Carried forward and RESTATED rather than silently inherited, and now OWNED. The scope decision stands (outside the store layer, outside the UAT gap). What changes is the disclosure: the header names both sites with their owner — Phase 2, ERR-04 for `compat.ts:317` and ERR-03 for `hooks/passive.ts:171`, both verifiable against the ROADMAP traceability table — plus `telemetry.ts:272`'s `String(e)` inside `describeError` itself, which is correct, has NO owner by design, and which a future scope widening must exempt by construction. Surfaced in the SUMMARY as an open item with that owner rather than left for a reader to rediscover. |
| T-01-71 | Repudiation | one requirement id meaning two different things | medium | mitigate | MOVED OUT of this plan and discharged earlier, deliberately: the ledger half is plan 01-10 task 3 (wave 10, so 01-12 can declare CORE-11 in its own frontmatter — which is what a verifier reads) and the gate's source-string retag is plan 01-12 task 1 (inside the rewrite already touching those regions). Listed here rather than deleted so the threat register does not appear to have dropped it. |
| T-01-76 | Repudiation | a partial ledger tidy reading as a complete one | medium | accept | STORE-07 — THIS plan's own declared requirement — reads "All SQL uses positional `?` parameters" and says nothing about error-render redaction; STORE-03 has the matching problem for URL redaction. Both inherited from 01-01/01-07, both recorded as deferred with an owner in `REQUIREMENTS.md` by plan 01-10 task 3, neither closed by this round. Stated here because this is the plan a reader would otherwise assume had cleaned the whole ledger. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | NOT APPLICABLE: this plan installs no package. `typescript` and `vitest` are already dependencies and `pnpm install` is not run. A dependency turning out to be needed is a scope change requiring the Package Legitimacy Gate and a `gate="blocking-human"` checkpoint before any install. |
</threat_model>

<verification>
1. `pnpm vitest run packages/backend/src/store/error-redaction.spec.ts` — green, with a fixture for
   every shape in task 1's `<behavior>` list including all nine must-stay-quiet shapes.
2. The verifier's 8-shape probe and CR-05's 10-shape table both RE-RUN, with a before/after column
   pasted into the SUMMARY: 0 missed after.
3. The `(e as Error).message` mutation RUN against the real `store/artifacts.ts`, its failure block
   pasted verbatim, reverted, and `git diff --exit-code` clean.
4. The gate's header names `compat.ts:317` (Phase 2 / ERR-04), `hooks/passive.ts:171` (Phase 2 /
   ERR-03) and `telemetry.ts:272` (no owner, correct as written), and both requirement ids are
   confirmed against `.planning/ROADMAP.md`'s Requirement Traceability table before being written.
5. `pnpm test` — zero failures, no fewer than 31 files and no fewer than 705 tests.
6. `pnpm typecheck`, `pnpm lint`, `pnpm knip` — all exit 0.
7. No historical artifact modified: `01-01-PLAN.md` … `01-12-PLAN.md`, `01-VERIFICATION.md`,
   `01-REVIEW.md` and `01-UAT.md` all clean in `git status --porcelain`. Neither
   `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md` nor
   `packages/backend/src/outbound-prohibition.spec.ts` is touched — those edits belong to 01-10 and
   01-12, and repeating them here is how the earlier edit gets reverted.
8. `git diff --exit-code packages/backend/src/store/artifacts.ts` is clean after the mutation — the one
   file this plan touches transiently and does not durably modify.
9. Before starting, `git status --porcelain packages/` must be clean.
</verification>

<success_criteria>
- The STORE-07 gate sees `e.message`, every cast form, a copy, a return, an object-literal value and a
  destructured parameter — the shapes `useUnknownInCatchVariables` pushes authors toward.
- It still reports zero against the real store layer, and still stays quiet on `describeError(e)`, on
  `recordError(e)`, on a `String(x)` outside a catch and on a type member named `error`.
- Its header names the three renders outside its scope WITH their owners, so a reader can tell a
  decision from a hole and can see who is expected to act on the two that are not yet correct.
- No historical artifact and no ledger owned by another plan was rewritten here.
- The green baseline is restored before the SUMMARY: 31 files / 705 tests, typecheck / lint / knip
  clean.
</success_criteria>

<artifacts_this_phase_produces>
| Artifact | Kind | Purpose |
|----------|------|---------|
| `derivesFrom` in `packages/backend/src/store/error-redaction.spec.ts` | new internal function | Follows the caught binding through parentheses, casts, non-null assertions, property and element access and a call-on-member, replacing the two-line `isRefTo` |
| one-hop copy tracking in the same file | new internal structure | Records a `const`/`let` initialised from the binding as also the binding, so `const x = e; String(x)` is seen |
| the RETURN and OBJECT-LITERAL-VALUE forms in `scanFor` | new rule forms | The two positions CR-05 names, with the `describeError` descent guard preserved so the correct shape stays quiet |
| `ObjectBindingPattern` handling in rule 4 | extended rule | `function fin({ error })` and `function fin({ error: err })`, keyed on the property name |
| the restated header boundary block | modified | The reach the gate has, plus `compat.ts:317` (owner Phase 2 / ERR-04), `hooks/passive.ts:171` (owner Phase 2 / ERR-03) and `telemetry.ts:272` (no owner — correct as written) named as the three renders outside its scope |
| `01-13-SUMMARY.md` | new evidence | Both re-run probe tables with before/after columns, the verbatim mutation failure block, and the OPEN-item entry with its owner |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-skeleton-persistence-compatibility/01-13-SUMMARY.md` when done.

It must contain, as pasted terminal output rather than as prose: the re-run of the verifier's 8-shape
STORE-07 probe and CR-05's 10-shape table with before/after columns, the verbatim failure block from
the `(e as Error).message` mutation, and the `pnpm test` counts.

It must also contain an explicitly labelled OPEN ITEMS section, each row carrying an owner:

| Open item | Owner | Why it is open |
|---|---|---|
| `compat.ts:317` renders `String(e).slice(0, 160)` into the per-surface `error` field | Phase 2 — ERR-04 | Outside `store/`, outside the UAT gap, T-01-37 `accept`. ERR-04 rewrites how a per-artifact failure is recorded and surfaced, so the redaction decision is that author's to make. |
| `hooks/passive.ts:171` renders `String(e).slice(0, 160)` into `sdk.console.log` | Phase 2 — ERR-03 | Same. ERR-03 owns catching and logging errors thrown inside `onInterceptResponse`, which is this exact line. |
| STORE-03 and STORE-07 declared for redaction work neither requirement's text mentions | the operator, at the next requirements pass | Marked in `REQUIREMENTS.md` by plan 01-10 task 3, with the reason for deferring rather than splitting alongside CORE-11. Same UAT route that produced STORE-08. |

`telemetry.ts:272`'s `String(e)` is deliberately NOT in that table: it is inside `describeError`'s own
implementation, it is correct, and giving it an owner would imply work that must not be done.
</output>
