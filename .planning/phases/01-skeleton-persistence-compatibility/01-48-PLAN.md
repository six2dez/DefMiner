---
phase: 01-skeleton-persistence-compatibility
plan: 48
type: execute
wave: 48
gap_closure: true
gap_ids: [CR-33]
depends_on: [01-47]
requirements: [CORE-11]
autonomous: true
files_modified:
  - packages/backend/src/outbound-prohibition.spec.ts
  - .planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low

# ROUND 13'S FIRST PLAN, AND THE ONLY ONE THAT TOUCHES THE BLOCKER.
#
# ---------------------------------------------------------------------------------------------------
# CR-33 IS A CLASS WITH TWO LIVE MEMBERS, AND THE INSTANCE FIX IS THE KNOWN-FAILING MOVE.
# ---------------------------------------------------------------------------------------------------
# Verification pass 14 (2026-08-27) leaves this phase at 8/9 truths for the twelfth consecutive pass. UAT
# is 18/18 with its one gap withdrawn, so UAT contributes nothing here. Six of criterion (3)'s seven
# surfaces DISCHARGE this round. The one that blocks is surface #2 — the gate file's own prose below the
# registry — and it blocks on TWO hand-written line ranges, each TRUE WHEN WRITTEN and each silently drifted:
#
#   MEMBER (a): the section divider grouping HEADER_QUANTIFIER_EXEMPTIONS states a header extent whose top
#   endpoint is 956. `DERIVED_BEGIN` now matches at 949 and `headerRegion()` returns `gateLines.slice(0, at)`,
#   so the live region ends at 948. OFF BY EIGHT. It was exactly right at `516ce1d` and was made eight-wrong
#   by `196d20c` — THE COMMIT THAT DELETED CR-30's NINE LINES AND INSTALLED THE EMISSION FIX DESIGNED TO
#   PREVENT THIS SHAPE. The round built `headerRegion()`, which measures the very quantity that divider gets
#   wrong, in the same file, in the same commit range, and did not connect them.
#
#   MEMBER (b): docblock point (4) asserts, present tense and unmarked, that exclusion three excludes the
#   registry declaration's whole line range at endpoints 4135 and 5767 for 1633 lines. Replicating the
#   suite's own resolvers puts the live range at 4138..5770. BOTH ENDPOINTS OFF BY THREE, WITH THE LENGTH
#   STILL CORRECT AT 1633 — which is why the sentence reads as internally consistent and why fourteen
#   passes went past it. It was true at `1330e99` and was ALREADY FALSE ON PASS 13's OWN TREE.
#
# ---------------------------------------------------------------------------------------------------
# WHY THIS PLAN DELETES AND DERIVES RATHER THAN RE-MEASURING AND CORRECTING.
# ---------------------------------------------------------------------------------------------------
# The gate file states the rule against itself, in the paragraph beginning "THEY ARE DELETED RATHER THAN
# RE-DATED": *"A corrected sentence is still an AUTHORED bound standing beside a DERIVED one, which is how
# seven consecutive waves each fixed a stale claim here and each acquired the next."* Both members of CR-33
# are proof of that sentence — each was a CORRECT number the day it was written. A plan that re-measures
# 4138..5770 and 1..948 and writes them down is instance sixteen with a fresher date on it. THAT MOVE IS
# PROHIBITED HERE BY NAME.
#
# ---------------------------------------------------------------------------------------------------
# AND WHY THE CLASS FIX IS A CENSUS RATHER THAN A ZERO.
# ---------------------------------------------------------------------------------------------------
# Pass 14's census of this file found EIGHT lines carrying an `N..M` range. SIX OF THEM ARE ACCURATE DATED
# HISTORY — past-tense narratives of the wave-37 correction, and the `500..3000` band they cite is LIVE and
# TRUE. A guard demanding zero would order the executor to choose between destroying dated history and
# stopping, which is the harm `T-01-318` exists to prevent arriving through the gate rather than the prose,
# and it is the exact clause that misfired on plan 01-47's `open`-scoped gates. So the class closes the way
# the round already proved works for `.planning/` mentions: EVERY RANGE IS EITHER DELETED, DERIVED, OR
# DECLARED WITH A REASON. There is no fourth state, and the file's own failure message at the header
# census already lists those three dispositions in that order.
#
# ---------------------------------------------------------------------------------------------------
# THE SIX HARD CONSTRAINTS OF ROUND 13, REPEATED HERE RATHER THAN INHERITED.
# ---------------------------------------------------------------------------------------------------
# 1. `git grep -E` SILENTLY DROPS `\b` ON THIS PLATFORM. Pass 14 measured it: `git grep -cE '\bWINDOWS\.md'`
#    exits 1 with ZERO hits while `grep -cE '\bWINDOWS\.md'` returns 7. A false zero that fails
#    closed-looking. EVERY census and every verify clause in this plan uses `grep -rE`, and every zero is
#    preceded by a STATED POSITIVE CONTROL. Wave 47's first census hit this and caught it only by luck.
# 2. NO LINE NUMBER IS A LOCATOR. Every edit target below is named BY ITS TEXT. A plan that says "edit
#    :9053" is itself the CR-33 class, and it would be wrong before the executor read it.
# 3. THE SIX DATED-HISTORY RANGES SURVIVE BYTE-IDENTICALLY. They are counted before and after.
# 4. THE NINE DECLARED PHRASINGS IN `UNBOUNDED_QUANTIFIERS` ARE NOT WRITTEN INTO ANY NEW PROSE. They are
#    narrow, unusual strings; ordinary prose does not trip them, and prose that does would raise an
#    exemption obligation this plan has no business creating.
# 5. THE NEW PROSE STATES NO `N..M` RANGE OF ITS OWN. The census would obligate it, and a census whose own
#    docblock is its first undeclared entry is a joke with a test id.
# 6. THE SHADOW PIN AND THE IDENTITY PIN ARE NOT MOVED TO FIT. This plan adds lines below the registry.
#    If either pin reds, the change is attributed LINE BY LINE to lines this commit added, with the diff
#    shown, exactly as the pin's own failure message demands — or the plan stops.

must_haves:
  truths:
    - "THE CLASS IS ENUMERATED AND CLASSIFIED BEFORE ANY BYTE IS EDITED. Every line of the gate file carrying an `N..M` range is listed with its text, a LIVE or DATED HISTORY verdict, its reason, and — for the LIVE ones — the value the suite's own resolvers return for the same quantity, measured by replicating those resolvers rather than by reading pass 14's report."
    - "BOTH LIVE FIGURES ARE GONE FROM THE FILE'S BYTES AND NEITHER WAS REPLACED BY A CORRECTED ONE. The header divider carries no extent and is symmetric with its two siblings, which carry none either; exclusion three's docblock point names `registryStart` and `closingBracketAfter(registryStart)` as the things that resolve its endpoints and spells no digits at all."
    - "THE EXTENTS ARE COMPUTED IN THE RUN AND EMITTED ON A PATH THAT ALREADY FAILS. `headerRegion().length`, the registry declaration's start and its resolved closing line are rendered into a diagnostic that is UNASSERTED — a second rule invented in the round that is removing one carries exactly the risk being removed."
    - "THE CENSUS REGION IS RESOLVED, NEVER WRITTEN DOWN. It is the whole file minus two spans, each bounded by FULL-LINE equality against a declared sentinel, each asserted to match EXACTLY ONE line before any rule reads the region."
    - "NON-VACUITY IS ASSERTED BEFORE THE RULES, NOT AFTER THEM. A census that resolved an empty region, or one that matched nothing, passes having measured nothing — which is worse than no census, because somebody is now relying on it."
    - "EVERY RANGE IS DELETED, DERIVED OR DECLARED — AND THE SIX ACCURATE ONES ARE DECLARED, NOT DESTROYED. The six past-tense narratives of the wave-37 correction survive byte-identically with a one-clause reason each, and the count of surviving range lines is asserted at exactly six so that both directions of error are loud."
    - "THE GUARD BITES, PROVED BY MUTATION RATHER THAN ARGUED. A fabricated range planted in the prose below the registry turns the suite RED and names its own line; the tree is restored with `git checkout --` and proved byte-identical afterwards."
    - "THE DISCLOSED LIMITS ARE WRITTEN DOWN BEFORE A LATER ROUND FINDS THEM. The census matches a digit-dot-dot-digit shape and nothing else, it does not adjudicate whether a declared range is TRUE, and it keys on the trimmed raw line — so a line carrying two ranges is discharged by one entry."
    - "NOTHING LEAKED AND NOTHING ELSE MOVED. Whole suite, typecheck, lint, knip, build and bundle check all executed; the bundle's import set is still exactly one specifier; zero debt markers under either source root; `.planning/` is the only other tree this plan touches."
    - statement: "The count balance between the declaration list and the matched population catches a list that has drifted from the population it declares, but it is over LINES rather than occurrences, so a single line carrying two ranges is discharged by one entry and a second range appearing on an already-declared line is invisible to it."
      verification: backstop
    - statement: "A range expressed in any syntax other than digit-dot-dot-digit — a hyphenated pair, or the same claim written out as prose — raises no obligation and is invisible to this census, exactly as the header census is invisible to a mechanism claim that names no file."
      verification: backstop

  prohibitions:
    - requirement_id: CORE-11
      category: integrity
      status: unresolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT re-measure a stale range and write the corrected value back into the prose. A corrected range is still a hand-written one, and this file's own deletion paragraph records that seven consecutive waves each fixed a stale claim here and each acquired the next — both members of CR-33 were correct numbers the day they were written."
    - requirement_id: CORE-11
      category: integrity
      status: unresolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT delete, re-date or reword any of the six accurate past-tense range narratives. Reaching green by destroying the dated history the gate exists to protect is the harm T-01-318 names, and it is the clause that misfired on plan 01-47."
    - requirement_id: CORE-11
      category: integrity
      status: unresolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT use `git grep` with an extended-regex flag in any census or gate. It silently drops `\\b` on this platform and returns a false zero that fails closed-looking; every count in this plan comes from `grep -rE` and every zero is preceded by a stated positive control."
    - requirement_id: CORE-11
      category: integrity
      status: unresolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT move the shadow pin or the identity pin to fit a number this commit cannot account for line by line. The pin's own failure message calls that decoration that reports green and names it as the defect this file has spent eleven waves removing."
    - requirement_id: CORE-11
      category: integrity
      status: unresolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT touch CORE-11's checkbox in `.planning/REQUIREMENTS.md` or the constant that pins it. This plan closes the blocker; plan 01-50 owns the discharge, and editing the constant on its own is not a way out and never was."

  artifacts:
    - path: "packages/backend/src/outbound-prohibition.spec.ts"
      provides: "A gate file whose prose below the registry states no line range it does not derive, and a census that makes every surviving range a listed, reasoned entry rather than an invisible claim."
      contains: "GATE_RANGE_MENTIONS"
    - path: ".planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md"
      provides: "A section D extending the existing disposition ledger with the edges this round's guard introduces, leaving sections A, B and C byte-unchanged and the 38-item equality intact."

  key_links:
    - from: "a hand-written header extent in a section divider"
      to: "`headerRegion().length`, computed in the run"
      via: "deletion of the divider's extent plus an unasserted emission on the census's failing path"
    - from: "a hand-written registry line range asserted in the present tense"
      to: "`registryStart` and `closingBracketAfter(registryStart)`, the resolvers the suite already runs"
      via: "replacing the digits with the names of the things that resolve them"
    - from: "eight range expressions nothing enumerated"
      to: "six declared entries with reasons, in a census whose region is resolved rather than written down"
      via: "the same delete-derive-declare shape the header planning census already proved"
---

<objective>
Close CR-33 as a CLASS. Delete the two present-tense line ranges the gate file states about its own
geometry, replace them with the names of the resolvers that compute those quantities plus an unasserted
emission of the live values, and install a census that makes every surviving range in the file a declared,
reasoned entry — so that the sixteenth instance names itself instead of waiting for verification pass 15.

Purpose: criterion (3) of CORE-11's discharge bar is *"the disclosure is the only bound stated on every
surface a reader touches, contradicted nowhere."* Six of the seven surfaces discharge. Surface #2 fails
because the file's own live bytes state two things about its own geometry that the same file computes and
contradicts. Fourteen passes hunted the header prose, the anchoring machinery, the exemption keys, the pin
and the reference tables; nobody censused the hand-written ranges.

Output: a gate file carrying no undeclared range, a census with non-vacuity guards and a positive control
executed, a classification table of all eight sites committed in the SUMMARY, and a probe ledger extended
rather than replaced.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md
@.planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md
@.planning/phases/01-skeleton-persistence-compatibility/01-47-SUMMARY.md
@packages/backend/src/outbound-prohibition.spec.ts
</context>

## Artifacts this phase produces

Every symbol below is CREATED by this plan and does not exist on the tree at plan time. A drift check that
flags one of these as an unacknowledged reference is reading a symbol this plan is introducing.

**New constants and types in `packages/backend/src/outbound-prohibition.spec.ts`:**

- `RANGE_EXPR` — the regular expression the census matches with.
- `RANGE_CENSUS_BEGIN`, `RANGE_CENSUS_END` — the two full-line comment sentinels bounding the census's own
  declaration block, so the declarations cannot be their own first undeclared entries.
- `GateRangeMention` — the record type, `{ line, reason }`, mirroring `HeaderPlanningMention`.
- `GATE_RANGE_MENTIONS` — the frozen declaration list, six entries after Task 2.
- `MIN_RANGE_REASON_LENGTH` — the shortest string this census accepts as a reason rather than a token.

**New functions:**

- `rangeCensusRegion()` — the scanned population: every numbered line of the gate file outside the
  machine-owned span and outside the census's own declaration block.
- `gateRangeHits()` — the matched subset, as `{ n, text }` records.
- `liveGeometryReport()` — the unasserted diagnostic string carrying the header extent and the registry
  declaration's resolved endpoints and length.

**New test-case titles** (each a new `it(...)` in the existing `describe` that opens with *"the residual is
DERIVED"*): the range census's non-vacuity case, its undeclared-hit case, its stale-entry case, its
count-balance case, its duplicate-declaration case, and its thin-reason case.

**New planning content:** a `## D.` section appended to
`.planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md`.

**Changed, not created:** the section divider that groups the header block of
`HEADER_QUANTIFIER_EXEMPTIONS`, and docblock point (4) describing exclusion three.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: the class enumerated and classified, both live figures DELETED rather than corrected, and the extents rendered from the resolvers that already compute them — one path, end to end</name>

  <files>packages/backend/src/outbound-prohibition.spec.ts</files>

  <precondition>Plan 01-47 is committed, `01-47-SUMMARY.md` exists on disk, and `git status --porcelain` is empty. Verification pass 14's baseline is the arrival contract: the whole suite runs 31 files / 1396 tests at exit 0 and the gate spec alone runs 448 of 448. Re-run both and read the arrival numbers off the run rather than off this plan — every figure quoted in this plan's comments is a measurement dated 2026-08-27 and is history the moment it is written.</precondition>

  <read_first>
    - `packages/backend/src/outbound-prohibition.spec.ts`, four regions, each located BY TEXT rather than by line number: (1) the docblock bullet that begins `EXCLUSION THREE IS A LINE RANGE, WHICH IS WIDER THAN THE CLAUSE STRINGS` and its following six lines; (2) the section divider inside `HEADER_QUANTIFIER_EXEMPTIONS` whose comment text starts `// --- HEADER` together with its two siblings whose comment texts are `// --- CODE ABOVE THE REGISTRY ---` and `// --- BELOW THE REGISTRY ---`; (3) the declarations of `CLOSES_FROZEN_ARRAY`, `closingBracketAfter`, `lineOf` and the `EXCLUSIONS` IIFE, which is where the registry's endpoints are resolved today; (4) `headerRegion`, `headerPlanningHits`, `PLANNING_PATH_PREFIX` and `MIN_REASON_LENGTH`, which is the shape this plan mirrors.
    - The same file's paragraph beginning `THEY ARE DELETED RATHER THAN RE-DATED` — read it for the rule this task is applying, in the file's own words, before applying it.
    - The same file's failure message that begins `YOU HAVE THREE DISPOSITIONS AND THEY ARE LISTED IN THE ORDER THIS FILE PREFERS THEM` — the three dispositions this task chooses between are already written down there, and this task does not invent a fourth.
    - The same file's `WIDEST_ANCHOR_SHADOW` pin case and its failure message, so that if the pin moves, the response is the one the message prescribes and not the one that is convenient.
    - The same file's `UNBOUNDED_QUANTIFIERS` declaration — the nine phrasings no new prose in this plan may contain.
    - `01-VERIFICATION.md`, the section headed `CR-33 — the blocker, and it is a class with two live members`, for the provenance of both members and for the census pass 14 already ran.
  </read_first>

  <behavior>
    - After the edits, the file carries EXACTLY SIX lines matching a digit-dot-dot-digit range, down from eight, and each of the six is one of the past-tense wave-37 narratives.
    - The header divider's comment text is `// --- HEADER ---`, carrying no extent, symmetric with the two siblings that carry none.
    - Docblock point (4) still says exclusion three is a line range wider than the clause strings, still says a line range would swallow a docblock sitting between two rows, still names `exclusionThreeCarriesOnlyClauses` as the narrowing, still records that the measurement was 12 and 12 both times — and states no endpoint, no length and no digit for the range itself.
    - Forcing the census's non-vacuity case to fail prints the header extent and the registry declaration's start, resolved close and length, every one of them computed in that run.
    - The whole suite, typecheck, lint, knip, build and the bundle import check all exit 0, and the bundle's import set is still exactly one specifier.
  </behavior>

  <action>
Run the enumeration FIRST and edit nothing until it is written down. Search the gate file for the
digit-dot-dot-digit shape with `grep -nE` over the file path — never with `git grep`, which drops word
boundaries on this platform and would hand back a false zero. Print the count before the list so the list
can be checked against it. Pass 14 reports eight lines; treat that as a prediction to confirm or refute,
not as a fact to adopt, and if the count differs say so in the SUMMARY and reconcile it before proceeding.

Classify every hit. LIVE means the sentence asserts the range in the PRESENT TENSE about the tree the
reader is on. DATED HISTORY means it names the wave, pass or plan that measured it, is past tense, and
states no current bound — this is the same criterion plan 01-47 applied across four surfaces and pass 14
endorsed, and it is quoted rather than reinvented. For every LIVE hit, MEASURE the live value by
replicating the suite's own resolvers in a scratch script: the registry's start is the line
`lineOf` finds for the predicate that tests `startsWith("export const RESOLVER_REGISTRY")`, its close is
`closingBracketAfter` of that start under `CLOSES_FROZEN_ARRAY`, and the header region is `gateLines`
sliced from the start up to but not including the line matching `DERIVED_BEGIN` by full-line equality.
Record measured-versus-stated for each. Put the whole table in the SUMMARY; it is a deliverable, not a
working note.

THEN DELETE, AND DO NOT CORRECT. This is the one instruction in this plan that the phase's entire history
argues for. The header divider loses its parenthesised extent and becomes a bare divider whose comment text
is `// --- HEADER ---`. Its two siblings already carry no extent, so the result is symmetric rather than
irregular, and nothing about the grouping is lost — a divider names a region, it does not measure one.
Docblock point (4) loses its digits: the clause that today spells an endpoint pair, a length and a wave
attribution is replaced by a clause naming `registryStart` and `closingBracketAfter(registryStart)` as the
expressions that resolve those endpoints in the run, and pointing at the emission below for the values.
Keep every other sentence of point (4) intact — the coarseness disclosure, the narrowing by
`exclusionThreeCarriesOnlyClauses`, and the 12-and-12 measurement record are all TRUE and all DATED, and
removing them would be the T-01-318 harm.

DO NOT write the corrected endpoints anywhere. Not in the prose, not in a comment, not in a commit
message body as a claim about the current tree. If the commit message mentions them at all it does so in
the past tense with today's date attached, as a record of what was measured while closing this, which is
the same discipline plan 01-47 applied to `.planning/ROADMAP.md`.

NOW WIRE THE ONE PATH END TO END, because a deletion that leaves the quantity invisible is a deletion and
not a derivation. Add `liveGeometryReport()` as a describe-level helper placed AFTER `headerRegion` so it
can call it, returning one string that states, in words plus interpolated values: the censused header
region's extent, expressed as starting at the first line and running to `headerRegion().length`; and the
registry declaration's start, its `closingBracketAfter` resolution and the difference plus one as its
length. Say inside that string that all of these figures are computed in the run and asserted nowhere, and
say that they replace hand-written prose CR-33 falsified. Interpolate every number — write no digit into
the template literal, both because a digit there would be the defect returning and because the census
installed in Task 2 would obligate it.

Add the census's REGION and its NON-VACUITY case in this task, and nothing else of the census. Declare
`RANGE_EXPR` as a regular expression matching one or more digits, two literal dots, one or more digits.
Declare `RANGE_CENSUS_BEGIN` and `RANGE_CENSUS_END` as two strings whose values are comment lines, and
place those two comment lines in the file with a short note between them recording that Task 2's
declaration list lands in that span. WRITE THAT NOTE WITHOUT ANY DEBT-MARKER WORD — the shipped-source scan
reports zero of them under both source roots today, the verify below asserts that, and a marker written
here would make this task the first to break it. Resolve both spans by FULL-LINE equality on the trimmed
line, and assert that
each of the four sentinels — the two new ones and the two derived-block ones — matches EXACTLY ONE line
before anything reads the region: at zero the region silently swallows the block it is supposed to skip,
and above one it resolves whichever duplicate came first. `rangeCensusRegion()` returns the file's numbered
lines minus those two spans inclusive; `gateRangeHits()` returns the members whose trimmed text matches
`RANGE_EXPR`. The non-vacuity case asserts the region is large — comfortably more lines than the file could
shrink to by accident — and that the scan matched at least one line, and its failure message ends with
`liveGeometryReport()`.

WATCH THE NON-VACUITY CASE FAIL BEFORE BELIEVING IT PASSES. Temporarily invert its assertion, run the gate
spec alone, read the emitted geometry off the real failing run, paste it into the SUMMARY, and restore the
assertion. That is how the pin's five figures were established in `196d20c` and it is the only way to know
the emission renders rather than compiles.

WRITE NO NEW RANGE AND NO DECLARED PHRASING. The nine strings in `UNBOUNDED_QUANTIFIERS` are narrow and
unusual; ordinary prose does not contain them, and prose that does would raise an exemption obligation
this task has no business creating. Likewise write no debt marker of any kind into the file — the shipped
source scan reports zero of them today and this plan does not change that.

FINALLY, RECONCILE THE PINS RATHER THAN ASSUMING THEM. This task adds lines below the registry, which is
the mechanism that has moved the widest anchor shadow before. Run the gate spec. If the shadow pin or the
identity pin goes red, do NOT adjust either constant to match. Attribute the movement line by line to the
lines this commit added, show the diff, and only then re-derive — and if the movement cannot be attributed
line by line, stop and report it rather than fitting the pin, because a pin moved to fit the number it
exists to catch is decoration that reports green.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner || exit 1; SP=packages/backend/src/outbound-prohibition.spec.ts; [ -f "$SP" ] || { echo "GATE FILE ABSENT"; exit 1; }; PC=$(grep -rcE '[0-9]+\.\.[0-9]+' "$SP" || true); [ "${PC:-0}" -ge 1 ] || { echo "POSITIVE CONTROL FAILED: the range matcher finds ZERO lines in the gate file, so every count below would be a false zero. NOTE: git grep with -E silently drops \\b on this platform; this gate uses grep -rE only."; exit 1; }; echo "POSITIVE-CONTROL-RANGE-MATCHER-LINES=$PC"; PC2=$(grep -rcE 'closingBracketAfter' "$SP" || true); [ "${PC2:-0}" -ge 1 ] || { echo "SECOND POSITIVE CONTROL FAILED: the resolver name is not present, so this is not the file this gate was written against"; exit 1; }; LIVEB=$(grep -rcE 'whole LINE RANGE[^0-9]*[0-9]+\.\.[0-9]+' "$SP" || true); [ "${LIVEB:-0}" -eq 0 ] || { echo "CR-33(b) SURVIVES ON $LIVEB LINE(S): exclusion three's docblock point still spells a live endpoint pair. The disposition is DELETE-AND-DERIVE, not correct"; exit 1; }; LIVEA=$(grep -rcE '^[[:space:]]*// --- HEADER \([0-9]+\.\.[0-9]+\) ---$' "$SP" || true); [ "${LIVEA:-0}" -eq 0 ] || { echo "CR-33(a) SURVIVES: the header divider still carries a parenthesised extent"; exit 1; }; BARE=$(grep -rcE '^[[:space:]]*// --- HEADER ---$' "$SP" || true); [ "${BARE:-0}" -eq 1 ] || { echo "THE HEADER DIVIDER DID NOT SURVIVE AS A BARE DIVIDER: matched $BARE line(s), expected exactly 1. Deleting the extent must not delete the divider"; exit 1; }; SIB=$(grep -rcE '^[[:space:]]*// --- (CODE ABOVE THE REGISTRY|BELOW THE REGISTRY) ---$' "$SP" || true); [ "${SIB:-0}" -eq 2 ] || { echo "THE TWO SIBLING DIVIDERS ARE NOT BOTH PRESENT ($SIB): the symmetry this deletion relies on is not there"; exit 1; }; SURV=$(grep -rcE '[0-9]+\.\.[0-9]+' "$SP" || true); [ "${SURV:-0}" -eq 6 ] || { echo "THE GATE FILE CARRIES $SURV LINE(S) WITH AN N..M RANGE, EXPECTED EXACTLY 6 — the six accurate DATED HISTORY narratives. ABOVE 6 a live figure survived or new prose wrote one; BELOW 6 dated history was destroyed, which is the harm T-01-318 names and the failure plan 01-47 refused to commit"; exit 1; }; echo "SURVIVING-RANGE-LINES=$SURV"; HIST=$(grep -rcE '(therefore ran|went from|did not catch it|sat inside it|range went from|band of) ' "$SP" || true); [ "${HIST:-0}" -ge 1 ] || { echo "POSITIVE CONTROL FAILED on the dated-history phrasing probe"; exit 1; }; for P in 'anywhere in the file' 'everywhere in the file' 'any depth' 'every literal' 'ANY of them' 'every spelling' 'every reachable spelling' 'ANY string literal' 'ANY-BINDING-WINS'; do C=$(grep -rcF "$P" "$SP" || true); echo "QUANTIFIER-PHRASING [$P] = $C"; done; DEBT=$(grep -rlnE '(TBD|FIXME|XXX|HACK|PLACEHOLDER)' packages/backend/src packages/engine/src 2>/dev/null | grep -c . || true); [ "${DEBT:-0}" -eq 0 ] || { echo "DEBT MARKERS APPEARED UNDER A SOURCE ROOT ON $DEBT FILE(S); the shipped source scan reported zero at arrival"; exit 1; }; for S in liveGeometryReport rangeCensusRegion gateRangeHits RANGE_EXPR RANGE_CENSUS_BEGIN RANGE_CENSUS_END; do grep -qF "$S" "$SP" || { echo "MISSING NEW SYMBOL: $S"; exit 1; }; done; DIG=$(grep -nE 'liveGeometryReport' "$SP" | head -1 | cut -d: -f1); [ -n "$DIG" ] || { echo "liveGeometryReport UNRESOLVABLE"; exit 1; }; pnpm typecheck || { echo "TYPECHECK FAILED"; exit 1; }; pnpm lint || { echo "LINT FAILED"; exit 1; }; pnpm test || { echo "SUITE FAILED — if the shadow pin or the identity pin is what went red, attribute the movement line by line to lines this commit added and show the diff; do NOT adjust either constant to match"; exit 1; }; pnpm knip || { echo "KNIP FAILED"; exit 1; }; pnpm check:bundle | tee /dev/stderr | grep -qE '1 import specifier\(s\): crypto' || { echo "THE SHIPPED BUNDLE'S IMPORT SET IS NO LONGER EXACTLY ONE SPECIFIER"; exit 1; }; N=$(git status --porcelain | grep -c . || true); [ "$N" -eq 0 ] || { echo "TREE DIRTY AT EXIT: $N"; exit 1; }; echo TASK1-GREEN</automated>
  </verify>

  <acceptance_criteria>
    - The enumeration command pasted verbatim, its count printed BEFORE its list, and an explicit statement that `grep -rE` was used and `git grep` was not — with the reason named.
    - The classification table pasted in full: the line's text, LIVE or DATED HISTORY, the reason, and for each LIVE hit the value the suite's own resolvers return for the same quantity, obtained by replicating those resolvers and not by quoting `01-VERIFICATION.md`.
    - An explicit statement of whether the census count matched pass 14's prediction of eight, and if not, the reconciliation.
    - The before-and-after text of both edited regions quoted, with an explicit statement that neither corrected value was written anywhere in the file, the prose, or a present-tense commit claim.
    - `liveGeometryReport()`'s output read off a REAL FAILING RUN — the non-vacuity assertion temporarily inverted, the gate spec run, the emitted string pasted, the assertion restored — with the restoration proved by a clean `git diff` on that hunk.
    - Evidence that each of the four sentinels resolves to exactly one line, pasted from the run rather than asserted.
    - The surviving range-line count shown at exactly six, with the six lines listed so a reader can confirm each is dated history rather than a live claim.
    - Arrival and exit numbers for the whole suite and for the gate spec alone, both read off runs in this session, with any delta attributed to cases this task added.
    - If either pin moved: the line-by-line attribution and the diff. If neither moved: an explicit statement that both were re-read from the file and are unchanged.
  </acceptance_criteria>

  <reversibility rating="reversible">Two prose deletions and one new helper in a test-only file; every byte is recoverable from `git`, and nothing shipped in the bundle changes.</reversibility>

  <done>
The gate file carries exactly six range expressions, all of them dated history; neither deleted figure was
replaced by a corrected one; the header extent and the registry declaration's endpoints are computed in the
run and emitted on a path that already fails, with the emission read off a real failing run; and the whole
suite, typecheck, lint, knip, build and bundle check are green with the import set at one specifier.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the six surviving ranges DECLARED with reasons, the class closed by a census whose region is resolved rather than written down, and the guard watched failing on a planted range</name>

  <files>packages/backend/src/outbound-prohibition.spec.ts, .planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md</files>

  <precondition>Task 1 is committed, its classification table exists in the SUMMARY, and `rangeCensusRegion()`, `gateRangeHits()`, `liveGeometryReport()` and both census sentinels are already in the file with the non-vacuity case green. Every entry declared below is a line Task 1's table classified; a declaration written for a line that table does not carry is a declaration written without a reason.</precondition>

  <read_first>
    - `packages/backend/src/outbound-prohibition.spec.ts` — the whole `HEADER_PLANNING_MENTIONS` section, from its banner comment through its last case, read as ONE unit: the docblock's four disclosed limits, the record type, the frozen list, `MIN_REASON_LENGTH`, the non-vacuity case, and the five rules. This task mirrors that shape, and mirroring a shape means reading all of it rather than the array.
    - The failure message beginning `YOU HAVE THREE DISPOSITIONS` — its wording is the model for this census's own undeclared message, including the ordering that puts DECLARE last.
    - The six surviving range lines identified by Task 1, each read in its surrounding paragraph so the declared reason describes the CLAIM the sentence makes rather than the digits it contains.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md` — sections A, B and C, and the tally line, so section D extends the ledger and the 38-item equality stays checkable.
  </read_first>

  <behavior>
    - With all six lines declared, the suite is green and the census reports six matched lines against six declarations.
    - Planting one fabricated range line in the prose below the registry turns the suite RED on the undeclared case, and the failure message names that line's number and text, lists the three dispositions with DECLARE last, and carries the live geometry.
    - Planting the same fabricated line INSIDE the census's own declaration block leaves the suite GREEN — the self-exclusion works, and it is proved rather than assumed.
    - Removing one declaration while its line is still present turns the count-balance case RED even if the set checks would have passed.
    - Declaring a line that no longer matches turns the stale case RED with a message distinguishing it from an undeclared mention.
    - A declaration whose reason is a bare token turns the thin-reason case RED.
  </behavior>

  <action>
Declare the six surviving range lines in `GATE_RANGE_MENTIONS`, placed between the two sentinel comment
lines Task 1 installed. Mirror `HEADER_PLANNING_MENTIONS` exactly: a frozen array of records rather than an
object keyed by line text, because a keyed record silently overwrites a duplicate and a list that can lose
an entry without saying so is the vacuity hole this file's discipline is organised against. The KEY IS THE
TRIMMED RAW LINE — no normalization, no masking, no new key format, for the reason that census's docblock
already gives: a key format is a narrowing, and a narrowing invented here is a second thing to keep true.

Write a REASON per entry, not a label. Each reason states WHY the line may keep its digits, and the answer
for all six is the same class with six different specifics: the sentence narrates a past correction, names
the wave that made it, is past tense, and asserts nothing about the tree the reader is on. Where the line
carries the live `500..3000` band rather than a superseded endpoint pair, say so — that band is a LIVE and
TRUE value read from the exclusion's own declaration, and conflating it with a stale endpoint pair in the
reasons would make the census's own record wrong. Where a single line carries TWO ranges, say that too,
because the count balance is over lines and one entry discharges both.

Then add the rules, one case each, in the same order and with the same division of labour as the header
census. An UNDECLARED case listing every matched line that no declaration covers, with the file, the line
number and the text, whose message names the three dispositions in the order this file prefers them —
DELETE the range if the sentence states a CURRENT bound, MAKE IT DERIVED if the quantity is one the suite
already computes, DECLARE it with a one-clause reason only if it is genuinely dated history — and which
ends with `liveGeometryReport()`, so a reader who arrives here because a range drifted is handed the live
geometry in the same breath. A STALE case reporting declarations that match no line, with a message saying
plainly that this is a DIFFERENT failure from an undeclared range: nothing is unlisted, a listing has
outlived its line. A COUNT-BALANCE case asserting the declaration count equals the matched-line count,
with a message saying why the two set checks can both pass while these differ. A DUPLICATE case over
declared line texts. A THIN-REASON case against `MIN_RANGE_REASON_LENGTH`.

DISCLOSE THE LIMITS IN THE DOCBLOCK, WEAKER THAN THE GUARD RATHER THAN STRONGER, on the same terms the
header census uses. It matches a digit-dot-dot-digit shape and nothing else, so a hyphenated pair or the
same claim written out in words is invisible to it. It does not adjudicate whether a declared range is
TRUE — it makes the range LISTED and REVIEWABLE, which is what converts an invisible claim into one a
reader can read and disagree with, and that is the whole of what it buys. It keys on the trimmed raw line,
so a line carrying two ranges is discharged by one entry and a second range added to an already-declared
line is invisible. And it reaches the whole file outside two derived spans, which is the one place it is
STRONGER than the header census — that census reaches the header and no further, and pass 14 confirmed by
negative control that a fabricated claim reds above the sentinel and is invisible below it. WRITE NO
RANGE INTO THIS DOCBLOCK. A census whose own docblock is its first undeclared entry is a joke with a test
id, and the surviving-count gate would catch it, which is the point.

WATCH IT FAIL, THREE WAYS, BEFORE BELIEVING IT PASSES. Plant one fabricated range line in the prose below
the registry and read the red off a real run — the message, the named line, the three dispositions and the
emitted geometry all pasted into the SUMMARY. Plant the SAME line inside the census's own declaration
block and confirm the suite stays GREEN, which proves the self-exclusion rather than assuming it, and is
the negative control the header census's limit (a) was confirmed with. Remove one declaration while its
line stands and confirm the count-balance case reds. RESTORE WITH `git checkout --` EVERY TIME, NEVER with
a copy — this environment aliases the copy command to its interactive form and it refuses silently — and
prove the restoration with a byte digest of the file taken before the first mutation and again after the
last, plus a clean `git status --porcelain`.

Then extend the probe ledger. Append a `## D.` section to `01-PROBE.md` recording the edges this guard
introduces and their dispositions, using the same rules section B declares: an edge with a defensible
acceptance criterion is authored as a plain truth; one whose check exists but cannot be confirmed with
explicit evidence is marked `verification: backstop`; one with no defensible criterion stays unresolved
and is surfaced as a flagged assumption rather than dropped. The edges are: an empty or absent
machine-owned span; a legitimate dated-history range sitting inside the censused region; a line carrying
two ranges; a range appearing inside a string literal in code rather than in prose; and a range written
in a syntax the matcher does not recognise. SECTIONS A, B AND C STAY BYTE-UNCHANGED and the 38-item
equality in section B stays true and checkable — section D is an extension with its own tally, not a
replacement, and it says so in its first line.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner || exit 1; SP=packages/backend/src/outbound-prohibition.spec.ts; PB=.planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md; [ -f "$SP" ] || { echo "GATE FILE ABSENT"; exit 1; }; [ -f "$PB" ] || { echo "PROBE LEDGER ABSENT"; exit 1; }; PC=$(grep -rcE '[0-9]+\.\.[0-9]+' "$SP" || true); [ "${PC:-0}" -ge 1 ] || { echo "POSITIVE CONTROL FAILED: the range matcher finds ZERO lines, so every count below is a false zero. git grep with -E drops \\b on this platform and is not used here"; exit 1; }; echo "POSITIVE-CONTROL-RANGE-LINES=$PC"; [ "${PC:-0}" -eq 6 ] || { echo "THE GATE FILE CARRIES $PC RANGE LINE(S), EXPECTED EXACTLY 6. Task 2 declares; it does not add or remove ranges"; exit 1; }; DECL=$(grep -rcE '^[[:space:]]*line: ' "$SP" || true); [ "${DECL:-0}" -ge 14 ] || { echo "THE TWO DECLARATION LISTS TOGETHER CARRY $DECL line-keyed entr(ies); the header census alone shipped 8, so the six new ones are not all present"; exit 1; }; for S in GATE_RANGE_MENTIONS GateRangeMention MIN_RANGE_REASON_LENGTH; do grep -qF "$S" "$SP" || { echo "MISSING NEW SYMBOL: $S"; exit 1; }; done; grep -qF 'DELETE' "$SP" || { echo "THE THREE-DISPOSITION WORDING IS ABSENT FROM THE FILE"; exit 1; }; SENT=$(grep -rcE '^[[:space:]]*//[[:space:]]*GATE-RANGE-CENSUS-(BEGIN|END)[[:space:]]*$' "$SP" || true); [ "${SENT:-0}" -eq 2 ] || { echo "THE CENSUS SELF-SENTINELS RESOLVE TO $SENT LINE(S), EXPECTED EXACTLY 2. At any other count the declaration block is not excluded and the census's own entries become its first undeclared hits"; exit 1; }; MD5A=$(md5 -q "$SP" 2>/dev/null || md5sum "$SP" | cut -d' ' -f1); echo "GATE-FILE-DIGEST=$MD5A"; PBA=$(grep -cE '^## A\.' "$PB" || true); PBB=$(grep -cE '^## B\.' "$PB" || true); PBC=$(grep -cE '^## C\.' "$PB" || true); PBD=$(grep -cE '^## D\.' "$PB" || true); [ "${PBA:-0}" -eq 1 ] && [ "${PBB:-0}" -eq 1 ] && [ "${PBC:-0}" -eq 1 ] || { echo "PROBE SECTIONS A/B/C DID NOT SURVIVE AS EXACTLY ONE EACH: $PBA/$PBB/$PBC"; exit 1; }; [ "${PBD:-0}" -eq 1 ] || { echo "PROBE SECTION D IS MISSING OR DUPLICATED: $PBD"; exit 1; }; BASE=$(git log --format=%H -1 -- .planning/phases/01-skeleton-persistence-compatibility/01-47-SUMMARY.md); [ -n "$BASE" ] || { echo "COULD NOT RESOLVE THIS PLAN'S BASE COMMIT FROM 01-47-SUMMARY.md; every diff below would read the index and report 0"; exit 1; }; git diff --quiet "$BASE" HEAD -- "$SP" && { echo "THIS PLAN CHANGED NOTHING IN THE GATE FILE SINCE PLAN 01-47's EXIT"; exit 1; }; git diff --quiet "$BASE" HEAD -- .planning/REQUIREMENTS.md || { echo "REQUIREMENTS.md CHANGED SINCE PLAN 01-47's EXIT; THIS PLAN DOES NOT OWN THAT FILE — PLAN 01-50 DOES"; exit 1; }; grep -qF '38' "$PB" || { echo "THE 38-ITEM EQUALITY IS NO LONGER STATED IN THE PROBE LEDGER"; exit 1; }; pnpm typecheck || { echo "TYPECHECK FAILED"; exit 1; }; pnpm lint || { echo "LINT FAILED"; exit 1; }; pnpm test || { echo "SUITE FAILED"; exit 1; }; pnpm knip || { echo "KNIP FAILED"; exit 1; }; pnpm check:bundle | tee /dev/stderr | grep -qE '1 import specifier\(s\): crypto' || { echo "THE SHIPPED BUNDLE'S IMPORT SET IS NO LONGER EXACTLY ONE SPECIFIER"; exit 1; }; CBX=$(grep -rcF 'CORE11_BOX_EXPECTED = "- [ ] **CORE-11**"' "$SP" || true); [ "${CBX:-0}" -eq 1 ] || { echo "CORE11_BOX_EXPECTED IS NOT THE UNCHECKED FORM ($CBX). PLAN 01-50 OWNS THE FLIP; THIS PLAN DOES NOT"; exit 1; }; BOXROW=$(grep -rcE '^- \[ \] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md || true); [ "${BOXROW:-0}" -eq 1 ] || { echo "CORE-11's LEDGER CHECKBOX IS NOT THE UNCHECKED FORM ($BOXROW)"; exit 1; }; N=$(git status --porcelain | grep -c . || true); [ "$N" -eq 0 ] || { echo "TREE DIRTY AT EXIT: $N"; exit 1; }; echo TASK2-GREEN</automated>
  </verify>

  <acceptance_criteria>
    - All six declarations pasted with their reasons, and for each an explicit statement of which classification row in Task 1's table it corresponds to.
    - The reason for the entry carrying the live band explicitly distinguishes that band from a superseded endpoint pair, naming where the band's live value is declared.
    - The entry for the line carrying two ranges explicitly says so, and states that the count balance is over lines.
    - The POSITIVE CONTROL executed and its red pasted verbatim: the planted line's number and text, the three dispositions in order, and the emitted live geometry — read off a real failing run.
    - The NEGATIVE CONTROL executed and its green stated: the same fabricated line placed inside the census's own declaration block leaves the suite passing, proving the self-exclusion rather than asserting it.
    - The count-balance case watched RED by removing one declaration while its line stands, with the message pasted.
    - Every mutation restored with `git checkout --`, never with a copy, with the reason named; the file's byte digest taken before the first mutation and again after the last and shown identical; `git status --porcelain` empty.
    - The four disclosed limits quoted from the docblock as shipped, with an explicit statement that the docblock itself contains no range and that the surviving-count gate is what enforces that.
    - Section D of `01-PROBE.md` pasted, with its own tally, an explicit statement that A, B and C are byte-unchanged, and the 38-item equality shown still stated and still true.
    - CORE-11's box and the constant that pins it both shown unchanged, with an explicit statement that plan 01-50 owns the flip.
  </acceptance_criteria>

  <reversibility rating="reversible">Additive test cases and a declaration list in a test-only file, plus an appended documentation section; every byte is recoverable from `git` and nothing shipped changes.</reversibility>

  <done>
Every range surviving in the gate file is a declared entry with a reason, the census's region is resolved
by full-line sentinel equality rather than written down, the guard has been watched failing on a planted
range and watched staying quiet on the same line inside its own declaration block, and the probe ledger
carries a section D that extends rather than replaces the 38-item disposition record.
  </done>
</task>

</tasks>

<assumption_delta_decision>
The detector fired `pluralization` on this phase's scope, and the signal is real for THIS plan even though
its quoted snippets came from ROADMAP prose: the plan introduces a SECOND censused region alongside
`headerRegion()`, and a second census alongside the header planning census.

**Primary noun:** *censused region* — a named, sentinel-bounded span of the gate file whose bounds are
resolved by full-line equality and whose non-vacuity is asserted before any rule reads it.

**Decision: `promote`.** `headerRegion()` stops being the singular and becomes one instance of the
generalized noun. This plan installs `rangeCensusRegion()` under the same contract — resolved bounds,
uniqueness assertion per sentinel, non-vacuity before the rules — rather than adding a region that is a
region by resemblance only. Plan 01-49 adds the third instance under the same contract. Adding alongside
was rejected because the failure this phase keeps reproducing is exactly a region whose bounds were
believed rather than resolved.

**Companion invariant (accepted, and it lands in plan 01-49 where the third region makes it non-trivial):**
every censused region declares its bounds by full-line sentinel equality and asserts non-vacuity before
any rule reads it — a case that goes red the instant a future region is added without the guard.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author prose → reader belief | A sentence in this file is read as a statement about the tree the reader is on. Both CR-33 members crossed here and were believed for fourteen passes. |
| declaration list → censused population | A list that has drifted from the population it declares can satisfy both set checks while being wrong, so the balance is asserted separately. |
| sentinel text → resolved region | If a sentinel stops matching, the region silently collapses or expands and every rule over it passes having measured nothing. |
| test-only source → shipped bundle | The gate file lives under a source root the outbound walk audits, so new code here is walked like any other. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-320 | Tampering | the two CR-33 members in `outbound-prohibition.spec.ts` | high | mitigate | The disposition is DELETE-and-DERIVE, prohibited from correcting. Task 1's gate greps the file for the live-claim shapes and requires zero, and separately requires the surviving range-line count to be exactly six, so a "corrected" value written back is caught in both directions. |
| T-01-321 | Tampering | `rangeCensusRegion()` sentinel resolution | high | mitigate | Each of the four sentinels is asserted to match EXACTLY ONE line before any rule reads the region, and the non-vacuity case asserts the region is large and the scan matched something. Both are watched failing by inversion rather than trusted. |
| T-01-322 | Repudiation | the six dated-history range narratives | high | mitigate | The surviving-count gate is an exact equality at six, not a maximum, so destroying dated history to reach green is as loud as leaving a live claim standing. This is the T-01-318 harm restated as a two-sided gate after plan 01-47's `open`-scoped clause misfired. |
| T-01-323 | Spoofing | `git grep -E` in any census step | high | mitigate | Prohibited by name in the plan and absent from every verify clause; every count comes from `grep -rE` and every zero is preceded by a stated positive control that is itself asserted non-zero before the zero is trusted. |
| T-01-324 | Information disclosure | `liveGeometryReport()` output | low | accept | The emitted figures describe a test file's own line geometry and reach no operator data, no target traffic and no shipped bundle. Emitted only on an already-failing path and asserted nowhere. |
| T-01-325 | Elevation of privilege | new prose tripping `UNBOUNDED_QUANTIFIERS` | medium | mitigate | The nine declared phrasings are enumerated in the plan and probed by the verify block, which prints each count so an unexpected non-zero is visible rather than silent. New prose that raised an exemption obligation would be caught by the suite. |
| T-01-326 | Denial of service | the widest-anchor shadow pin moving under added lines | medium | mitigate | The plan forbids fitting either pin and requires line-by-line attribution against the commit's own diff, or a stop. The pin's own failure message states the same rule and is quoted in `read_first`. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan; `pnpm-lock.yaml` and every manifest are untouched, and `pnpm check:bundle` asserts the shipped import set is still exactly one specifier. RESEARCH.md's Package Legitimacy Audit is unchanged and raises no `[ASSUMED]` or `[SUS]` entry for this work. |
</threat_model>

<verification>
- The gate file carries exactly six `N..M` lines, all dated history, each declared with a reason.
- Neither CR-33 member survives, and neither was replaced by a corrected value anywhere in the tree.
- The header extent and the registry declaration's endpoints are computed in the run and emitted, unasserted, on a path that already fails — read off a real failing run.
- The census's four sentinels each resolve to exactly one line, asserted before any rule reads the region.
- The guard is watched failing on a planted range and watched quiet on the same line inside its own declaration block.
- Whole suite, typecheck, lint, knip, build and bundle check green; import set exactly one specifier; zero debt markers under either source root.
- CORE-11's box and `CORE11_BOX_EXPECTED` are both untouched.
- `01-PROBE.md` sections A, B and C are byte-unchanged and the 38-item equality still holds.
</verification>

<success_criteria>
CR-33 is closed as a class rather than as two instances: the two false present-tense statements are gone
from the file's live bytes, the quantities they described are computed and emitted, every surviving range
is a listed reasoned entry, and a sixteenth instance planted anywhere outside the two derived spans names
itself and turns the suite red.
</success_criteria>

<output>
Create `.planning/phases/01-skeleton-persistence-compatibility/01-48-SUMMARY.md` when done.
</output>
