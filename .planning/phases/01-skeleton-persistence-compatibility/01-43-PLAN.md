---
phase: 01-skeleton-persistence-compatibility
plan: 43
type: execute
wave: 43
gap_closure: true
depends_on: [01-42]
files_modified:
  - packages/backend/src/outbound-prohibition.spec.ts
# ROUND 10's SECOND AND LAST PLAN. IT CARRIES THE ROUND'S OTHER BLOCKER, ITS THREE WARNINGS, AND ITS
# CLOSING BASELINE. READ THIS BLOCK BEFORE THE TASKS, AND READ PLAN 01-42's HEADER BLOCK TOO — its
# prohibitions on AST/parser/compiler-API work, on further narrowing, on folding line numbers into keys,
# on re-opening the 2026-08-25 re-scope and on CORE-11's checkbox apply here BYTE FOR BYTE and are
# repeated in this plan's own frontmatter rather than inherited by reference.
#
# CR-23 — BLOCKER. THE TWO `.from` ASSERTIONS DO NOT ASSERT. `EXCLUSIONS` derives its two opening anchors
# at `:9983-9988` with `lineOf(l => l.startsWith("export const UNBOUNDED_QUANTIFIERS"))` and
# `lineOf(l => l.startsWith("export const RESOLVER_REGISTRY"))`. The WR-54 case then RECOMPUTES THE SAME
# TWO EXPRESSIONS, CHARACTER FOR CHARACTER, over the same `gateLines`, through the same `lineOf`, at
# `:10080-10085`, and asserts `EXCLUSIONS[n].from` against them at `:10132-10134` and `:10151-10153`.
# Tautologies. They hold for every possible file, including one where the locator matches nothing and
# `-1 === -1` — the exact silent success this same case's own non-vacuity paragraph thirty lines above
# says it is organised against, and which it CORRECTLY guards for `REGISTRY_CLOSE` at `:10092-10100` and
# for each list entry at `:10104-10117`.
#   EXECUTED BY VERIFICATION PASS 10 ON THE REAL TREE: one planted line —
#   `export const RESOLVER_REGISTRY_DECOY: readonly number[] = Object.freeze([1]);` — 104 lines above the
#   real registry slid exclusion three's `from` from 4163 to 4058 and REMOVED 104 PRE-EXISTING GATE-FILE
#   LINES FROM THE SCANNED SURFACE (`SURFACE_LINES` 8846 -> 8742, measured from inside the suite), at
#   439 OF 439 GREEN — past the assertion whose own message reads `Both endpoints are pinned because a
#   width is two numbers, and pinning only the end leaves the other half free to move.` Any declared
#   phrasing living in those 104 lines is unguarded and nothing says so.
#   THE FIX PASS 10 PRESCRIBES: pin each opening endpoint against a locator derived INDEPENDENTLY of the
#   expression under test — a FULL-LINE literal, PROVED TO MATCH EXACTLY ONE LINE BEFORE USE, exactly the
#   treatment `REGISTRY_CLOSE` already gets — and correct `:9963-9967`, which claims all five locators are
#   proved unique when THREE are. DO NOT DELETE THE ASSERTIONS.
#   AND THE TRAP INSIDE THE FIX, WHICH THIS PLAN FORBIDS EXPLICITLY: DO NOT change `EXCLUSIONS`' own
#   opening locators to the full-line form. The pin is only an assertion because the two expressions are
#   INDEPENDENT — a prefix matcher on one side and a full-line equality on the other. Making both sides
#   the same expression restores the tautology in the commit that claims to remove it.
#
# WR-55 — the sentinel case's reach. `constructAnchorFor` reaches `return NO_PRECEDING_CONSTRUCT` only by
# running the backward scan off the top of the file having accepted no line. Line 1 is
# `// packages/backend/src/outbound-prohibition.spec.ts — CORE-11's wired gate.` — a `//` comment with
# nothing above it, so the fifth recogniser accepts it unconditionally and `constructTokenOf` returns a
# non-null token, which means the forward walk from `head = 1` returns on its first iteration for every
# line from 2 down. PASS 10 CONFIRMED THIS EXHAUSTIVELY by evaluating the shipped builder at every line
# of the file: the set of lines resolving to the sentinel is exactly `[1]`. So the case is GREEN BY
# CONSTRUCTION over 8,845 of the 8,846 surface lines while `:9229-9232` and `:10295-10300` both state its
# reach as the occurrences `quantifierOccurrences` finds across `SURFACE_LINES` and nothing wider. Say
# what it executes, in the bytes. THE DECLARED-KEY HALF OF CR-21 NEEDS NO CHANGE — pass 10 drove the
# exact CR-17 key through it and it fired at `Tests 3 failed | 436 passed (439)`.
#
# WR-58 — the census's `constructHalf` at `:10399-10400` is `k.slice(0, k.indexOf(EXEMPTION_ANCHOR_SEP))`
# with no `-1` guard, so a key missing the separator silently yields the key minus its last character.
# The prefix case guards exactly this at `:10348-10354`; the census, which reads the same declared keys,
# guards neither side itself. A separator-less key would enter the census's `inUse` set as a
# near-complete key, find zero producers, and fail with a message about AMBIGUITY rather than about
# MALFORMATION — a correct red for the wrong reason. Pass 10 confirmed this BY INSPECTION.
#
# WR-60 — the same measured relocation is stated as `58 lines apart` at `:10383` and as `57 lines` at
# `:10795`, 412 lines apart in the same wave's work. Pass 10 confirmed this BY GREP and calls for the
# distance to be DERIVED rather than written twice at two values. AND THERE IS A RECORD THE REVIEWER DID
# NOT CONSULT: `01-39-SUMMARY.md:531` and `:931` already record BOTH numbers and explain them — wave 39
# moved the two-line cell as a unit, deleting it before computing the insertion point, so the destination
# shifted up by two while the source stayed fixed, and pass 9 measured against the unmodified file. IF
# THAT READING HOLDS, `one of the two is wrong` is refuted: both are correct measurements of DIFFERENT
# PROCEDURES and the defect is that neither sentence states its frame. THIS IS A CLAIM TO MEASURE, NOT A
# CONCLUSION — Task 2 reads those lines and writes whichever the bytes support.
#
# UNADJUDICATED FINDINGS — DO NOT TREAT ANY OF THESE AS VERIFIED. Pass 10 records WR-56, WR-57, WR-59 and
# IN-41 … IN-44 as REVIEWER-REPORTED AND NOT RE-EXECUTED BY IT, and says so in its own `artifacts:` list.
# They are OUT OF SCOPE for this round. Where one abuts an in-scope edit — WR-57's three-times-written key
# split abuts WR-58's guard, IN-44's nine colliding `it.each([` producers abut WR-60's paragraph, IN-41's
# limit (5) heading abuts plan 01-42's replacement — the task STATES the abutment, states that the finding
# is unadjudicated, and does NOT close it. Nothing in this plan may describe any of them as closed by
# pass 10.
#
# WHAT THIS PLAN MUST NOT DO — the same list as plan 01-42, restated because a plan is read alone.
#   - NO AST, NO PARSER, NO COMPILER-API, NO ENCLOSING-FRAME IDENTITY. Exit 1 was not taken.
#   - NO FURTHER NARROWING of `constructAnchorFor`'s recognisers, either scan bound, the key format,
#     `constructTokenOf`, `CONSTRUCT_TOKEN_ELLIPSIS`, `EXEMPTION_ANCHOR_SEP`, `maskQuantifiers`,
#     `normalizeGateLine`, `nameableRemainder` or `exemptionKeyFor`. No line number in any key.
#   - DO NOT RE-OPEN THE 2026-08-25 RE-SCOPE.
#   - DO NOT FLIP OR REVERT CORE-11's CHECKBOX; `requirements mark-complete` stays UNRUN; no file under
#     `.planning/` is opened.
#   - DO NOT CHANGE PLAN 01-42's WORK: the shadow pin, its pinned value, its failure message, its reach
#     paragraph, the two replaced 234 sentences and the retirement bracket are byte-unchanged here,
#     EXCEPT that the pin's value may be re-derived under the attribution rule below if this plan's own
#     prose widens the residual — and only then, and only with the attribution recorded.
#   - DO NOT WIDEN A BAND, A `proof` TOKEN, AN EXCLUSION OR A PINNED COUNT to accommodate any new
#     assertion. Narrowing is the only permitted direction, as wave 37 established. If a new assertion
#     disagrees with a resolved value, THE DISAGREEMENT IS THE FINDING.
#
# ENVIRONMENT FACTS, MEASURED THIS SESSION AT HEAD `483a8ee` — RECONCILIATION ANCHORS, NOT ADDRESSES.
# PLAN 01-42 EDITS THIS FILE BEFORE THIS PLAN RUNS, SO EVERY NUMBER BELOW WILL HAVE MOVED. Re-derive each
# by identifier or by content and record any disagreement as a FINDING rather than adopting either
# silently.
#   - CR-23's SITES: the `EXCLUSIONS` derivation `:9980-10016` with `listStart` `:9983-9985` and
#     `registryStart` `:9986-9988`; the closing claim `:9963-9967`; the WR-54 case opening `:10079` with
#     its recomputed locators `:10080-10085`; the `REGISTRY_CLOSE` treatment that is the model,
#     `:10092-10100`; the list-entry treatment `:10104-10117`; the two `.from` assertions `:10131-10134`
#     and `:10150-10153`.
#   - THE TWO CONSTRUCTS, CONFIRMED BY CONTENT THIS SESSION: `RESOLVER_REGISTRY` opens at `:4161` on the
#     exact line `export const RESOLVER_REGISTRY: readonly ResolverRecord[] = Object.freeze([` and closes
#     at `:5793` on the exact line `] as readonly ResolverRecord[]);`. `UNBOUNDED_QUANTIFIERS` opens at
#     `:5985` on the exact line `export const UNBOUNDED_QUANTIFIERS: readonly string[] = Object.freeze([`
#     with nine entries, closing on the bare `]);` at `:5995`. NOTE THAT `:4161` AND `:5793` DISAGREE WITH
#     PLAN 01-41's RECORDED 4135 AND 5767 — round 9 added lines above them and that is the expected cause,
#     but it is re-derived and stated rather than assumed.
#   - NEITHER FULL-LINE OPENER CARRIES A DECLARED PHRASING — checked against all nine entries of
#     `UNBOUNDED_QUANTIFIERS` this session — so writing either into the file as a string constant raises
#     no new obligation on the scanned surface. RE-CHECK IT ANYWAY before writing, exactly as wave 41 did
#     when it rebuilt the entry literals with `JSON.stringify` at runtime for this reason.
#   - WR-55's SITES: the sentinel docblock's reach sentence `:9229-9232`, above the declaration at
#     `:9241`; the case's own reach paragraph `:10295-10300`; the case itself opening `:10306`. The single
#     `return NO_PRECEDING_CONSTRUCT` is at `:9419` — ONE return site, not two; plan 01-40's `:174` and
#     `:636` say `both return sites` and round 9 disclosed that as its own finding.
#   - WR-58's SITE: the census case opening `:10392`, its `constructHalf` `:10399-10400`. THE SECOND
#     `constructHalf` at `:10568-10569`, inside the cross-construct fixture, IS A DIFFERENT DECLARATION
#     and WR-57 — which proposes consolidating them — IS UNADJUDICATED. Task 2 guards the census's one,
#     MEASURES the other's exposure, and closes WR-57 not at all.
#   - WR-60's SITES: `:10382-10384` and `:10794-10796`. The record that explains both numbers is
#     `01-39-SUMMARY.md:496-535` and its discrepancy row at `:931`. The relocation's endpoints as recorded
#     there: source line 305 under the header at 299, destination header at 361 in the unmodified frame
#     and 359 after the two-line deletion, planted at 361 in the delete-first frame. IN-44's neighbouring
#     sentence at `:10378-10380` — `it.each([` produced by TEN lines, the receiver-key table header by
#     THREE — is UNADJUDICATED and is NOT edited.
#   - THE SUITE COUNTS: the round's baseline file at `"${TMPDIR:-/tmp}/defminer-round10-baseline"` is
#     written by plan 01-42 Task 1 and updated by plan 01-42 to include its new case. If it is absent,
#     RE-CREATE IT from this session's measurements before the first edit and say so. Arrival at
#     `483a8ee` was 31 files / 1379 tests, gate 439, `tsc --build` 0, ONE specifier `crypto`, 23 modules /
#     0 violations, `SURFACE_LINES.length` 8846.
#   - NO ACTIVE GIT HOOKS. COMMIT BEFORE MUTATING — every mutation here is plant-observe-restore over
#     committed work, and wave 29 destroyed its own Task 1 by not doing that.
#   - EVERY `<automated>` BLOCK OPENS WITH `set -o pipefail` AND EVERY COUNT IS ASSERTED NUMERICALLY
#     AGAINST A PRECONDITION-CAPTURED VALUE READ FROM THE BASELINE FILE, never against a literal.

autonomous: true
requirements: [CORE-11]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low
# `estimate-calibration` reports `{"factor": 1, "applied": false, "sample_count": 0, "min_samples": 3}`,
# so `tokens` equals `raw_tokens` and the confidence is DERIVED from the sample count, never self-rated.
# Weigh the MEASUREMENT COUNT over the token figure: two independent full-line locators each proved
# unique and each watched failing under pass 10's own decoy with the surface amputation measured from
# inside the suite, one permanent fixture encoding the decoy shape, one exhaustive evaluation of the
# builder at every line of the file, one guard watched failing in both directions, one distance derived
# against the record that explains it, and the round's closing baseline executed rather than carried.

must_haves:
  truths:
    - "BOTH `.from` ASSERTIONS ASSERT. Each opening endpoint is pinned against a FULL-LINE literal locator derived independently of the expression `EXCLUSIONS` was built from, and each locator is PROVED TO MATCH EXACTLY ONE LINE before it is used, with a failure message saying that a locator matching zero lines makes the pin compare nothing and one matching more than one pins to whichever came first — the treatment `REGISTRY_CLOSE` already gets. Neither assertion is deleted and `EXCLUSIONS`' own prefix locators are left exactly as they are, because making both sides the same expression would restore the tautology."
    - "THE DECOY IS WATCHED FAILING AND ITS DAMAGE IS MEASURED FROM INSIDE THE SUITE. Verification pass 10's planted `export const RESOLVER_REGISTRY_DECOY` line, 104 lines above the real registry, turns the corrected pin RED where it was green at 439 of 439, and the number of pre-existing gate-file lines it removes from the scanned surface is measured by instrumenting the surface non-vacuity assertion rather than reasoned about. The decoy shape also survives as a PERMANENT fixture over synthetic lines, so a future author who swaps the pin's locator back to a prefix matcher turns the suite red."
    - "THE CLOSING CLAIM IS CORRECTED TO WHAT IS PROVED. `:9963-9967` no longer says all five locators are proved unique. It states which are, states that `EXCLUSIONS`' own opening locators are PREFIX matchers that a longer identifier can satisfy, and states that the new pins make such a slide RED rather than preventing it — that reach and no wider."
    - "THE SENTINEL CASE'S REACH IS RESTATED TO WHAT IT EXECUTES. Both `:9229-9232` and `:10295-10300` state, from a measurement taken in this session by evaluating the shipped builder at EVERY line of the file, that exactly one line can resolve to the sentinel, that the case is therefore green by construction over every surface line but that one, and that it is kept because line 1 becoming a declared phrasing or ceasing to be a nameable `//` comment are both edits a person can make."
    - "THE CENSUS GUARDS ITS OWN INPUT. The census case's `constructHalf` fails loudly on a key carrying no separator instead of silently returning the key minus its last character, and the difference is proved by planting a separator-less key and pasting the census's message BEFORE the guard — about ambiguity — and AFTER it — about malformation."
    - "THE RELOCATION DISTANCE IS STATED ONCE, WITH ITS FRAME. Both sentences carry one number on one stated basis, with the two line numbers it was measured between named, and the record in `01-39-SUMMARY.md` that explains why two different values were both correct is read first and its reading written down."
    - "THE ROUND'S BASELINE IS EXECUTED, NOT CARRIED. The whole suite, `tsc --build`, `check:bundle`, the gate suite's own count, `SURFACE_LINES.length` and the real-tree walk are all run in this session, compared against arrival, and every difference attributed to a named plan and task; an unattributed difference is a finding. The round's entire code delta is shown to be one test file and CORE-11's first sentence is shown byte-identical."

  artifacts:
    - path: "packages/backend/src/outbound-prohibition.spec.ts"
      provides: "Two full-line opening-endpoint locators, each proved unique before use, making both `.from` assertions assert; a permanent synthetic fixture encoding the decoy shape that distinguishes a prefix locator from a full-line one; a corrected closing claim stating which locators are proved and that the exclusion's own openers are prefix matchers; both sentinel-reach paragraphs restated to the executed bound measured over every line of the file; an `indexOf` guard on the census's `constructHalf` watched failing in both directions; one relocation distance stated once with its frame and endpoints; and the round's closing baseline"
      contains: "REGISTRY_OPEN"

  key_links:
    - from: "a `.from` assertion comparing an expression against itself"
      to: "a `.from` assertion comparing it against a full-line locator proved to match exactly one line"
      via: "the `REGISTRY_CLOSE` treatment applied to both opening endpoints, with the exclusion's own prefix locators deliberately left independent so the comparison stays a comparison"
      pattern: "REGISTRY_OPEN"
    - from: "a decoy that amputates the scanned surface at 439 of 439 green"
      to: "a decoy that turns the pin RED, with the amputation counted from inside the suite"
      via: "pass 10's own planted line re-run against the corrected pin, the surface non-vacuity assertion instrumented to print its own number, and the shape kept as a permanent synthetic fixture"
      pattern: "RESOLVER_REGISTRY_DECOY"
    - from: "a case green by construction over 8,845 of 8,846 surface lines describing itself as bounding all of them"
      to: "a case that states the one line it can fire on and why it is kept anyway"
      via: "the shipped builder evaluated at every line of the file in this session, the resolving set counted, and line 1's shape named as the reason"
      pattern: "NO_PRECEDING_CONSTRUCT"
    - from: "a helper that turns a malformed key into a near-complete one"
      to: "a helper that fails on it with a message about malformation"
      via: "an `indexOf` guard, watched failing in both directions with the before and after messages pasted"
      pattern: "constructHalf"
    - from: "one measurement written twice at two values"
      to: "one measurement written once with its frame and its two endpoints"
      via: "`01-39-SUMMARY.md`'s own discrepancy record read first, and whichever reading its bytes support written down"
      pattern: "identically-headed"

  prohibitions:
    - requirement_id: CORE-11
      category: security
      status: resolved
      verification: gate
      resolution: "`git diff` over the `EXCLUSIONS` derivation asserted EMPTY by a numeric check, and the two locator forms shown side by side to be a prefix matcher and a full-line equality"
      statement: "MUST NOT change `EXCLUSIONS`' own opening locators to the full-line form. The pin asserts only because the two expressions are independent. Making both sides identical restores the tautology in the same commit that claims to remove it, which is CR-23 arriving one wave later wearing the fix."
    - requirement_id: CORE-11
      category: security
      status: resolved
      verification: gate
      resolution: "Both `.from` assertions shown present by diff after the change, with their line numbers and their messages pasted"
      statement: "MUST NOT delete either `.from` assertion. Pass 10 says so in its own words: make them assert. A tautology removed by deleting the comparison leaves the endpoint pinned by nothing at all, which is the state the finding is about."
    - requirement_id: CORE-11
      category: security
      status: resolved
      verification: gate
      resolution: "A structural check over this plan's whole diff shows ZERO added or removed lines carrying the compiler-API import form or a compiler-API member access, and ZERO touching the five recognisers, either scan bound, the ellipsis constant or the separator constant"
      statement: "MUST NOT build an AST, parser, compiler-API or enclosing-frame identity, and MUST NOT narrow the recognisers, the scan bounds or the key format. Exit 1 was not taken and a fourth narrowing round is what pass 10 expects to reproduce the signature defect."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "Each new pin planted against with pass 10's decoy and observed RED with its FULL message pasted, restored, tree proved clean; the guard planted against in both directions with both messages pasted"
      statement: "MUST NOT ship a pin that has never been watched failing. WR-54 and CR-23 both exist because a correction shipped pinned by nothing, and CR-23's own message claimed both endpoints were pinned while one was free."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "The decoy is restored and `git diff --exit-code` shown clean before the task's verify block runs; the permanent fixture is shown by diff to operate over a SYNTHETIC array and to write no unindented registry-open or registry-close line into the file"
      statement: "MUST NOT leave the decoy in the tree, and MUST NOT let the permanent fixture write a line that the real locators would match. Leaving the decoy would amputate over a hundred lines from the scanned surface; writing an unindented opener or closer into the fixture would break the exactly-one-line proofs the fix depends on."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "Every unadjudicated finding named in this plan is stated as reviewer-reported and not re-executed by pass 10 wherever it is mentioned, and none appears in any list of closed items"
      statement: "MUST NOT present WR-56, WR-57, WR-59 or IN-41 through IN-44 as verified or as closed. Pass 10 records them as reported by the reviewer and NOT independently re-executed. Anything measured about them in this round is a claim measured in this round, said so in those words."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "`git diff` asserted EMPTY over plan 01-42's case body, its pinned value, its failure message, its reach paragraph, both replaced 234 paragraphs and the retirement bracket — except for a pin re-derivation attributed line-by-line to lines this plan added, recorded with its diff"
      statement: "MUST NOT edit plan 01-42's work. The one exception is the pinned value, and only when this plan's own prose widened the residual, and only with the growth attributed line-by-line. Unattributable growth leaves the pin unmoved and becomes a first-order finding."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "`git status --porcelain .planning/` shown EMPTY numerically at every task boundary; `requirements mark-complete` present in no command; `CORE11_BOX_EXPECTED` asserted byte-unchanged and still reading the unchecked form"
      statement: "MUST NOT flip CORE-11's checkbox and MUST NOT revert it either. Round 9 did not move it, plan 01-41 left `requirements mark-complete` unrun, and pass 10 determined the box may not move. That handoff is preserved and pass 11 makes the call."
    - requirement_id: CORE-11
      category: safety
      status: open
      verification: gate
      resolution: "DISCLOSED, NOT CLOSED — stated wherever this plan's result is stated, in the bytes and in the SUMMARY, that the `from` pins DETECT a slide rather than prevent one, that `EXCLUSIONS`' openers remain prefix matchers, that the sentinel case remains green by construction over all but one line, that the census's second `constructHalf` remains unguarded and WR-57 remains open, and that round 10 closes CR-22 and CR-23 and closes neither the relocation class nor criterion (3)"
      statement: "MUST NOT state or imply that making the `from` assertions assert makes the exclusions exact, that restating a reach makes a case reach further, that guarding one helper closes WR-57, or that round 10 closes CORE-11. Every item in this round is a correction to a test-only gate's description of itself, and whether criterion (3) is discharged is verification pass 11's determination."
---

<objective>
Make CR-23's two `.from` assertions assert, restate the sentinel case's reach to the one line it can fire
on, guard the census's key split, state the one relocation distance once with its frame — and then
execute the round's closing baseline rather than carrying it.

Purpose: verification pass 10 reproduced CR-23 on the real tree. One planted line 104 lines above the
registry slid exclusion three's opening endpoint and removed 104 pre-existing gate-file lines from the
scanned surface at 439 of 439 green, straight past the assertion whose own message says both endpoints
are pinned. The two `.from` assertions compare `EXCLUSIONS[n].from` against a locator expression
recomputed character for character from the expression `EXCLUSIONS` was built from; they are true of every
possible file including one where the locator matches nothing. The fix is the treatment `REGISTRY_CLOSE`
already gets — a full-line literal, proved to match exactly one line before use — with the exclusion's own
prefix locators deliberately left alone so the comparison stays a comparison. Beside it, three warnings
pass 10 confirmed: a sentinel case that executes over one line and says it bounds the surface, a key split
that turns a malformed key into a near-complete one, and one measurement written twice at two values.

Output: two full-line opening-endpoint locators each proved unique, both `.from` assertions asserting,
pass 10's decoy watched turning them RED with the surface amputation counted from inside the suite, and
the decoy shape kept as a permanent synthetic fixture; a closing claim corrected to what is actually
proved, including that the exclusion's own openers are prefix matchers and that the pins detect a slide
rather than prevent one; both sentinel-reach paragraphs restated against an evaluation of the shipped
builder at every line of the file; an `indexOf` guard on the census's `constructHalf` watched failing in
both directions; one relocation distance stated once with its frame and its endpoints, written after
reading the record that explains why two values were both correct; and the round's baseline executed,
compared against arrival, with every difference attributed.

WHAT THIS PLAN DOES NOT DO. It does not change `EXCLUSIONS`' own opening locators, does not delete either
`.from` assertion, does not leave the decoy in the tree, and does not widen a band, a `proof` token, an
exclusion or a pinned count to accommodate anything. It builds no AST, parser, compiler-API or
enclosing-frame identity and narrows nothing. It does not edit plan 01-42's work except for a pin
re-derivation attributed line-by-line. It closes none of WR-56, WR-57, WR-59 or IN-41 through IN-44 and
presents none of them as verified. It does not flip or revert CORE-11's checkbox, does not run
`requirements mark-complete`, does not re-open the 2026-08-25 re-scope and opens no file under
`.planning/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md
@.planning/phases/01-skeleton-persistence-compatibility/01-39-SUMMARY.md
@.planning/phases/01-skeleton-persistence-compatibility/01-42-SUMMARY.md
@packages/backend/src/outbound-prohibition.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-23 — both opening endpoints pinned against full-line locators proved unique, pass 10's decoy watched turning them RED with the surface amputation counted, the decoy shape kept as a permanent fixture, and the closing claim corrected to what is proved</name>

  <precondition>
    Plan 01-42 has executed and committed both tasks and `01-42-SUMMARY.md` exists on disk. The round's
    baseline file exists at `"${TMPDIR:-/tmp}/defminer-round10-baseline"`; if it is absent, RE-CREATE it
    from this session's measurements before the first edit and say so in the SUMMARY. Assert and PASTE:
    `git rev-parse HEAD`; `git status --porcelain packages/ scripts/ .planning/` clean; the gate suite
    green at plan 01-42's recorded count; `pnpm test` exit 0 with file and test counts; `tsc --build`
    exit 0; `check:bundle`'s specifier set; plan 01-42's pinned shadow maximum re-read and pasted. HALT
    if any is red. NO ACTIVE GIT HOOKS: run the gate set by hand at the boundary.
  </precondition>

  <read_first>
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md` — the `artifacts:` entry
      beginning `CR-23 — BLOCKER, REPRODUCED AT 439/439 GREEN`, the `missing:` entry beginning
      `CR-23: MAKE THE TWO .from ASSERTIONS ASSERT`, and the body section
      `### CR-23 — CONFIRMED. One planted line, 104 lines off the surface, green.` The body section
      carries the instrumented measurement — `without decoy: expected 8846 to be -1` / `with decoy:
      expected 8742 to be -1` — and notes that pass 10's arithmetic differs from the reviewer's by one
      line and that the difference is immaterial. Re-derive both numbers; a third value is a finding.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-REVIEW.md`, `### CR-23` in full
      (~line 5721). It carries the fix in code shape and the exact decoy line. USE THE SHAPE, MEASURE THE
      NUMBERS: the review reports 8,848 -> 8,743 and 105 lines where pass 10 reports 8,846 -> 8,742 and
      104. Both are recorded; neither is adopted without re-measurement.
    - `packages/backend/src/outbound-prohibition.spec.ts` — `lineOf` `:9895`, the closing claim
      `:9963-9967`, `CLOSES_FROZEN_ARRAY` and `closingBracketAfter` `:9976-9978`, the `EXCLUSIONS`
      derivation `:9980-10016` with both prefix locators, `EXCLUDED_LINES` and `SURFACE_LINES`
      `:10018-10028`, the surface non-vacuity assertion at the end of the case opening `:10034`, and the
      whole WR-54 case `:10079-10156`. RE-DERIVE EVERY ONE BY IDENTIFIER — plan 01-42 edited this file.
    - `packages/backend/src/outbound-prohibition.spec.ts` — `:10092-10100`, the `REGISTRY_CLOSE`
      treatment. THIS IS THE MODEL AND IT IS COPIED IN STRUCTURE, NOT IN TEXT: a full-line literal, a hit
      count over `gateLines`, an assertion that the count is exactly one with a message naming both
      failure directions, and only then the `lineOf`. Read its message in full so the two new ones say
      the same thing about their own locators rather than paraphrasing it loosely.
    - `packages/backend/src/outbound-prohibition.spec.ts` — `:10104-10117`, the list-entry treatment,
      where the literals are rebuilt with `JSON.stringify` AT RUNTIME so that no declared phrasing is
      written into the scanned surface by the pin that measures it. Read the reason; it is why the two
      new full-line literals must be checked against `UNBOUNDED_QUANTIFIERS` before they are written.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the two constructs by content:
      `export const RESOLVER_REGISTRY: readonly ResolverRecord[] = Object.freeze([` (`:4161` at
      `483a8ee`), `] as readonly ResolverRecord[]);` (`:5793`),
      `export const UNBOUNDED_QUANTIFIERS: readonly string[] = Object.freeze([` (`:5985`) and its nine
      entries. All four re-located by content, never by the number.
    - `01-41-SUMMARY.md`'s mutation record and `01-42-SUMMARY.md`'s two mutations, for the
      commit-plant-observe-restore shape this task's decoy must take.
  </read_first>

  <files>packages/backend/src/outbound-prohibition.spec.ts</files>

  <reversibility rating="reversible">Two locator constants, two uniqueness assertions, two corrected comparisons, one synthetic fixture and one corrected comment in a spec file. No shipped code, no behaviour, no exclusion boundary moved, no exemption key, no ledger byte. The decoy is restored inside the task with `git diff --exit-code` shown clean.</reversibility>

  <behavior>
    - The full-line registry-open locator matches exactly one line of the gate file, asserted before use;
      the same for the quantifier-list-open locator.
    - `EXCLUSIONS[2].from` equals the line the full-line registry-open locator resolves to, and
      `EXCLUSIONS[1].from` equals the line the full-line list-open locator resolves to.
    - With pass 10's decoy planted above the registry, the exclusion-three `.from` assertion FAILS, and
      its message shows the two line numbers and the gap between them.
    - With either full-line locator matching zero lines, the uniqueness assertion FAILS before the pin
      runs, so no `-1 === -1` comparison can occur.
    - `EXCLUSIONS`' own opening locators remain prefix matchers, so the two sides of each pin stay
      independent expressions.
    - Over a synthetic array where a decoy identifier sits above a real-shaped opener, a prefix finder
      resolves to the decoy and a full-line finder resolves to the opener, and the two differ.
  </behavior>

  <action>
    MEASURE BEFORE CHANGING ANYTHING, AND PASTE IT. For all three exclusions paste `from`, `to`, the size
    and the band as they resolve right now, plus `SURFACE_LINES.length`. Paste the registry's opening line
    number located by its full-line text, the quantifier list's opening line number located by its
    full-line text, and the line each of `EXCLUSIONS`' prefix locators currently resolves to. Reconcile
    against `:4161`, `:5793`, `:5985` and against plan 01-41's recorded 4135 / 5767 / 5959, and RECORD
    EVERY DISAGREEMENT AS A FINDING with its cause named — plans 01-39 through 01-42 all edited this file
    and a shift with no attributable cause is worth stopping for.

    CHECK BOTH FULL-LINE LITERALS AGAINST THE DECLARED PHRASINGS BEFORE WRITING THEM. Compare each
    candidate literal against all nine entries of `UNBOUNDED_QUANTIFIERS` and paste the result. If either
    carried a declared phrasing it would raise an obligation on the scanned surface by the very pin that
    measures it — the reason wave 41 rebuilt the entry literals with `JSON.stringify` at runtime. State
    the result in one line either way.

    ADD THE TWO LOCATORS IN THE `REGISTRY_CLOSE` SHAPE, STRUCTURE COPIED AND TEXT WRITTEN FRESH. For each:
    a full-line literal constant; a hit count over `gateLines` by exact equality; an assertion that the
    count is EXACTLY ONE, with a message stating BOTH failure directions — at zero the pin below would
    compare -1 against -1 and pass having compared nothing, above one it would pin to whichever line came
    first — and instructing the reader to re-point the locator rather than delete the pin. Only then the
    `lineOf`.

    MAKE BOTH `.from` ASSERTIONS ASSERT. Replace the right-hand side of each with the line the full-line
    locator resolved to. Keep both assertions; keep their positions; rewrite each message so it names the
    exclusion's realized `from`, the line the construct actually opens on, the gap between them, and what
    a difference means: the exclusion's opening endpoint has slid onto a different line, so lines that
    were on the scanned surface are silently off it and any declared phrasing living in them is unguarded.
    Correct the sentence beside them that claims both endpoints are pinned so that it is true of what now
    executes.

    DO NOT CHANGE `EXCLUSIONS`' OWN OPENING LOCATORS. They stay prefix matchers. The pin asserts only
    because the two expressions are independent; making both sides identical restores the tautology in the
    commit that claims to remove it. State that reason in one sentence in the bytes, so the next author
    reads it before "simplifying" the duplication. THE VERIFY BLOCK'S CHECK FOR THIS IS ON REMOVED LINES
    ONLY, DELIBERATELY: the permanent fixture below legitimately ADDS a prefix finder over synthetic
    lines, so a check over added lines too would be green only by the fixture not existing. A prefix
    locator REMOVED from `EXCLUSIONS` is the defect, and that is what is gated.

    ADD THE PERMANENT FIXTURE THAT ENCODES THE DECOY SHAPE. Over a SYNTHETIC `readonly string[]` — never
    over `gateLines` — place a decoy line whose identifier extends the real one, some filler, then a line
    byte-identical to the real full-line opener. Apply a prefix finder and a full-line finder to it and
    assert that the prefix finder resolves to the decoy, the full-line finder resolves to the opener, and
    the two DIFFER, with a message saying that a prefix locator is satisfied by a longer identifier and
    slides the opening endpoint while a full-line locator is not — which is CR-23 exactly. NON-VACUITY
    BEFORE THE RULE: assert the synthetic array is non-empty and that both finders resolved to something.
    THE FIXTURE MUST NOT WRITE AN UNINDENTED REGISTRY OPENER OR CLOSER INTO THE FILE — every synthetic
    line is an indented array element or an indented string constant, and this is asserted by re-running
    the two uniqueness checks after the fixture lands and showing both still at one.

    COMMIT. Paste `git log --oneline -1`.

    PLANT PASS 10's DECOY AND WATCH BOTH DIRECTIONS. Insert
    `export const RESOLVER_REGISTRY_DECOY: readonly number[] = Object.freeze([1]);` 104 lines above the
    real registry, as pass 10 did. FIRST, instrument the surface non-vacuity assertion so it prints
    `SURFACE_LINES.length` — the technique pass 10 used — and paste the number WITH and WITHOUT the decoy,
    so the amputation is counted from inside the suite rather than reasoned about. Reconcile against pass
    10's 8846 -> 8742 and the reviewer's 8848 -> 8743 and record which this session measures. THEN run the
    gate suite with the corrected pins and observe RED. Paste the failing title and the FULL assertion
    message, including both line numbers and the gap. Remove the instrumentation, restore the decoy, and
    prove clean with `git diff --exit-code`. Cite `01-VERIFICATION.md` for the prior 439-of-439 green with
    this session's RED as the contrast. If it does not go red, STOP, diagnose, and write it up as a
    first-order finding without adjusting the mutation to force a red.

    WATCH THE SECOND PIN TOO. Plant the equivalent decoy above `UNBOUNDED_QUANTIFIERS` — an identifier
    extending the real one — and observe the exclusion-two `.from` assertion RED. Paste its FULL message.
    Restore; prove clean. Correcting one construct while leaving the other unwatched is the identical
    defect one construct over, which is the reasoning wave 41 already wrote into this file for the `to`
    endpoints.

    CORRECT THE CLOSING CLAIM AT `:9963-9967`. It says each locator is proved to match exactly one line
    before it is used. State instead, measured: which locators are proved unique after this change and
    which are not; that `EXCLUSIONS`' OWN opening locators are PREFIX matchers, so a longer identifier
    declared above either construct satisfies them; and that the new pins make that slide RED rather than
    preventing it. THAT REACH AND NO WIDER — it says nothing about a construct located by some third form,
    and it does not make exclusion three exact, which is still a LINE RANGE wider than the clause strings
    it stands for and narrowed only by the clause-count equality.

    DO NOT WIDEN ANYTHING. No band, no `proof` token, no exclusion boundary and no pinned count moves. If
    a new assertion disagrees with a resolved value, THE DISAGREEMENT IS THE FINDING and it is written up
    before anything is adjusted; narrowing is the only permitted direction.

    RE-RUN PLAN 01-42's SHADOW PIN AND ATTRIBUTE ANY MOVEMENT. This task adds lines to the file. If the
    pinned maximum moved, attribute it line-by-line to lines this task added, show the diff, and re-derive
    with the attribution recorded. Unattributable movement leaves the pin unmoved and becomes a
    first-order finding.

    TOUCH NOTHING OUTSIDE SCOPE. `constructAnchorFor`, `constructTokenOf`, `exemptionKeyFor`,
    `maskQuantifiers`, `normalizeGateLine`, `nameableRemainder`, `anchorTokenCensus`, both scan bounds,
    `CONSTRUCT_TOKEN_ELLIPSIS`, `EXEMPTION_ANCHOR_SEP`, `NO_PRECEDING_CONSTRUCT`, every exemption key and
    value, `CLOSES_FROZEN_ARRAY`, every band, every `proof` token, both `to` pins, plan 01-42's case and
    paragraphs, the sentinel docblock and case, the census's `constructHalf` and the two WR-60 sentences
    (all Task 2's), `CORE11_BOX_EXPECTED` and the box case all stay byte-unchanged. Nothing under
    `.planning/` is opened. If new prose trips the whole-file declared-phrasing guard, REWRITE THE PROSE.

    RE-READ EVERY SENTENCE THIS TASK WRITES AGAINST THE NO-OVERCLAIM PROHIBITION AND RECORD A VERDICT PER
    SENTENCE, reach claimed beside reach executed. The corrected closing claim is this task's likeliest
    place to author the eleventh instance, because it is a sentence about what a set of locators proves.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && B="${TMPDIR:-/tmp}/defminer-round10-baseline"; [ -f "$B" ] || { echo "MISSING BASELINE FILE"; exit 1; }; . "$B"; GT=$(pnpm exec vitest run packages/backend/src/outbound-prohibition.spec.ts 2>&1 | tee /dev/stderr | sed -n 's/.*Tests .*(\([0-9][0-9]*\))[[:space:]]*$/\1/p' | tail -1); [ -n "$GT" ] && [ "$GT" -gt "$BASE_GATE" ] || { echo "GATE COUNT $GT NOT ABOVE CAPTURED $BASE_GATE"; exit 1; }; FT=$(pnpm test 2>&1 | tee /dev/stderr | sed -n 's/.*Tests .*(\([0-9][0-9]*\))[[:space:]]*$/\1/p' | tail -1); [ -n "$FT" ] && [ "$FT" -gt "$BASE_TESTS" ] || { echo "SUITE COUNT $FT NOT ABOVE CAPTURED $BASE_TESTS"; exit 1; }; pnpm typecheck && pnpm lint && pnpm knip && pnpm build:backend && pnpm check:bundle && RB=$(git rev-parse 483a8ee) && D=$(git diff "$RB"..HEAD -- packages/backend/src/outbound-prohibition.spec.ts); AST=$(printf '%s\n' "$D" | grep -cE '^[+-].*(from "typescript"|[^A-Za-z]ts\.[A-Za-z])' || true); [ "$AST" -eq 0 ] || { echo "COMPILER-API LINES IN DIFF: $AST"; exit 1; }; NAR=$(printf '%s\n' "$D" | grep -cE '^[+-].*(DECLARATION =|FIXTURE_TITLE =|DOCBLOCK_OPEN =|isLineComment =|isRule =|CONSTRUCT_TOKEN_ELLIPSIS =|EXEMPTION_ANCHOR_SEP =|CLOSES_FROZEN_ARRAY =|lineNumber - 1|k < lineNumber)' || true); [ "$NAR" -eq 0 ] || { echo "NARROWING LINES IN DIFF: $NAR"; exit 1; }; EX=$(printf '%s\n' "$D" | grep -cE '^-.*l\.startsWith\("export const (RESOLVER_REGISTRY|UNBOUNDED_QUANTIFIERS)"' || true); [ "$EX" -eq 0 ] || { echo "EXCLUSIONS OPENING LOCATOR REMOVED OR CHANGED: $EX"; exit 1; }; FROM=$(grep -cE 'EXCLUSIONS\[[12]\]\.from,' packages/backend/src/outbound-prohibition.spec.ts || true); [ "$FROM" -eq 2 ] || { echo "FROM ASSERTIONS PRESENT: $FROM, EXPECTED 2"; exit 1; }; DEC=$(grep -c 'RESOLVER_REGISTRY_DECOY' packages/backend/src/outbound-prohibition.spec.ts || true); [ "$DEC" -eq 0 ] || { echo "DECOY LEFT IN TREE: $DEC"; exit 1; }; N=$(git status --porcelain .planning/ | grep -c . || true); [ "$N" -eq 0 ] || { echo "PLANNING DIRTY: $N"; exit 1; }; A=$(git diff --name-only "$RB"..HEAD -- packages/ scripts/ | grep -v '\.spec\.ts$' | grep -c . || true); [ "$A" -eq 0 ] || { echo "SHIPPED SOURCE TOUCHED: $A"; exit 1; }; M=$(find packages/*/src -name '*.ts' ! -name '*.spec.ts' | grep -c . || true); [ "$M" -eq "$BASE_MODULES" ] || { echo "MODULE COUNT $M != $BASE_MODULES"; exit 1; }; git diff --exit-code -- packages/backend/src/outbound-prohibition.spec.ts && echo TREE-CLEAN</automated>
  </verify>

  <acceptance_criteria>
    - The precondition asserted and PASTED, with `git rev-parse HEAD`, a clean status, the gate suite at
      plan 01-42's recorded count, plan 01-42's pinned maximum re-read, and `01-42-SUMMARY.md` confirmed
      present. If the baseline file had to be re-created, that is stated with the commands used.
    - All three exclusions' `from`, `to`, size and band pasted BEFORE the change, plus
      `SURFACE_LINES.length`, the two constructs' full-line-located opening lines, and the line each
      prefix locator currently resolves to — reconciled against `:4161`, `:5793`, `:5985` and against plan
      01-41's 4135 / 5767 / 5959, with every disagreement recorded as a finding and its cause named.
    - Both candidate full-line literals compared against all nine `UNBOUNDED_QUANTIFIERS` entries with the
      result pasted, and a one-line statement of whether either carries a declared phrasing.
    - Both locator constants present, shown by diff to be FULL-LINE equalities and not prefix matchers,
      each preceded by a hit count and an exactly-one assertion whose message is quoted verbatim and shown
      to name both failure directions.
    - Both `.from` assertions shown by diff to compare against the full-line-located line, both still
      present with the numeric check from the verify block pasted, and both messages quoted verbatim and
      shown to name the realized `from`, the construct's real opening line, the gap and what a difference
      means for the scanned surface.
    - The sentence claiming both endpoints are pinned quoted before and after, with the after form shown
      true of what executes.
    - `git diff` shown to REMOVE no line carrying `EXCLUSIONS`' own opening locators, by the numeric
      check, with the one-sentence in-file reason for keeping them independent quoted verbatim — and an
      explicit statement that the check is scoped to removals because the permanent fixture adds a prefix
      finder of its own over synthetic lines.
    - The permanent fixture present, shown by diff to operate over a SYNTHETIC array, to assert
      non-vacuity before its rule, and to write no unindented registry opener or closer — proved by
      re-running both uniqueness checks after it lands and showing both still at one.
    - `git log --oneline -1` pasted BEFORE the decoy.
    - THE REGISTRY DECOY: planted 104 lines above the real registry; `SURFACE_LINES.length` printed from
      inside the suite WITH and WITHOUT it and both pasted; the delta reconciled against pass 10's
      8846 -> 8742 and the reviewer's 8848 -> 8743 with this session's own figures stated; the suite
      observed RED; the failing title and FULL assertion message pasted with both line numbers and the
      gap; instrumentation removed; decoy restored; `git diff --exit-code` clean; the prior 439-of-439
      green cited from `01-VERIFICATION.md`.
    - THE LIST DECOY: planted above `UNBOUNDED_QUANTIFIERS`; the exclusion-two `.from` assertion observed
      RED with its FULL message pasted; restored; clean diff.
    - If either decoy did NOT go red, the task stopped, the diagnosis is written up as a first-order
      finding, and no adjustment was made to force a red.
    - The corrected closing claim quoted verbatim and shown to state which locators are proved unique,
      that `EXCLUSIONS`' own openers are prefix matchers a longer identifier satisfies, that the pins make
      a slide RED rather than preventing it, and that this does not make exclusion three exact.
    - `git diff` shown EMPTY over every band, every `proof` token, both `to` pins, `CLOSES_FROZEN_ARRAY`
      and exclusion one; any assertion-versus-resolution disagreement written up as a finding with an
      explicit statement that nothing was widened to accommodate it.
    - Plan 01-42's shadow pin re-run with its maximum pasted; any movement attributed line-by-line with
      the diff shown, or the pin shown unmoved and the movement recorded as a finding, or an explicit
      statement that it did not move.
    - The numeric check showing the decoy absent from the committed file, with its output pasted.
    - The compiler-API check, the narrowing check and the `EXCLUSIONS`-locator check all shown returning
      zero, with their output pasted.
    - `git diff` shown EMPTY over every other out-of-scope construct named in the action.
    - `git status --porcelain .planning/` asserted EMPTY numerically; `requirements mark-complete` absent
      from every command run; `CORE11_BOX_EXPECTED` byte-unchanged.
    - Executed `pnpm test` exit 0 with counts pasted and asserted above the captured baseline;
      `typecheck`, `lint`, `knip` exit 0; `build:backend && check:bundle` exactly one specifier, `crypto`;
      the real-tree walk asserted at the captured module count with zero violations; CORE-11's first
      sentence pasted byte-identical.
    - A one-line no-overclaim verdict recorded for EVERY sentence added or changed, reach claimed beside
      reach executed.
    - Any prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses, or an
      explicit statement that there were none.
  </acceptance_criteria>

  <done>
    Both opening endpoints are pinned against full-line locators proved to match exactly one line before
    use, both `.from` assertions still exist and now compare independent expressions, pass 10's decoy has
    been watched turning each of them RED with the surface amputation counted from inside the suite, the
    decoy shape survives as a permanent synthetic fixture, and the closing claim states which locators are
    proved and that the pins detect a slide rather than prevent one.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-55, WR-58 and WR-60 — the sentinel case's reach restated to the one line it executes over, the census's key split guarded and watched failing in both directions, and the one relocation distance stated once with its frame</name>

  <precondition>
    Task 1 has committed and its two decoys are restored. Assert and PASTE `git rev-parse HEAD`, a clean
    `git status --porcelain packages/ scripts/ .planning/`, the gate suite green at Task 1's recorded
    count, and plan 01-42's pinned shadow maximum re-read from the file. HALT if anything is red.
  </precondition>

  <read_first>
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md` — the `artifacts:`
      entries for WR-55, WR-58 and WR-60, and the `missing:` entries beginning `WR-55: RESTATE THE
      SENTINEL CASE'S REACH` and `WR-58, WR-60:`. Note which pass 10 confirmed EXHAUSTIVELY (WR-55), which
      BY INSPECTION (WR-58) and which BY GREP (WR-60) — the strength of each confirmation is part of what
      this task restates.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-REVIEW.md`, `### WR-55` (~line 5793),
      `### WR-58` (~line 5915) and `### WR-60` (~line 5974). WR-55 supplies a candidate replacement
      paragraph containing the figure 11,072 — DO NOT COPY IT; the file measures 11,071 this session and
      `01-VERIFICATION.md` says 11,079. Re-derive and write the measured number.
    - `01-39-SUMMARY.md:496-535` — `### REAL-TREE RELOCATION TWO`, in full, including the endpoints
      (source line 305 under the header at 299, destination header at 361 in the unmodified frame, 359
      after the two-line deletion, planted at 361) and the closing paragraph
      `**One reconciliation disagreement, recorded rather than absorbed:**`. AND `01-39-SUMMARY.md:931`,
      the discrepancy row `D-1`. THIS IS A CLAIM TO MEASURE, NOT A CONCLUSION: read whether those bytes
      support the reading that both 57 and 58 are correct measurements of different procedures, and write
      whichever the bytes support. The reviewer's `one of them is wrong` was written without this record.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the sentinel docblock's reach sentence
      `:9229-9232` above the declaration `:9241`; the case's reach paragraph `:10295-10300`; the case
      `:10306-10341`; `constructAnchorFor`'s body and its single `return NO_PRECEDING_CONSTRUCT` at
      `:9419`; line 1 of the file and the fifth recogniser that accepts it.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the census case `:10392` with its
      `constructHalf` `:10399-10400`, and the prefix case's guard at `:10348-10354` which is the shape the
      new guard follows. ALSO the SECOND `constructHalf` at `:10568-10569` inside the cross-construct
      fixture — read it, measure whether it carries the same exposure, and do NOT change it: WR-57, which
      proposes consolidating the two, is UNADJUDICATED.
    - `packages/backend/src/outbound-prohibition.spec.ts` — `:10378-10384` and `:10794-10796`, the two
      WR-60 sentences. The neighbouring sentence at `:10378-10380` about `it.each([` and TEN producers is
      IN-44's, is unadjudicated, and is NOT edited.
  </read_first>

  <files>packages/backend/src/outbound-prohibition.spec.ts</files>

  <reversibility rating="reversible">Two prose paragraphs restated, one guard added to one helper, and one number written once instead of twice, in a spec file. No shipped code, no behaviour, no exclusion boundary, no exemption key, no ledger byte. The planted key is restored inside the task with `git diff --exit-code` shown clean.</reversibility>

  <behavior>
    - Evaluating the shipped `constructAnchorFor` at every line of the gate file yields exactly one line
      whose resolution is the sentinel, and that line is line 1.
    - Blanking line 1 makes more than one line resolve to the sentinel, which is the evidence that the
      result depends on line 1's shape and not on the builder.
    - The census's `constructHalf` throws on a key carrying no separator instead of returning the key
      minus its last character.
    - With a separator-less key planted in the exemption map, the census reports MALFORMATION after the
      guard where it reported AMBIGUITY before it.
    - Both WR-60 sentences carry one number on one stated basis, with the two line numbers it was measured
      between.
  </behavior>

  <action>
    WR-55 — MEASURE THE SENTINEL'S REACH EXHAUSTIVELY, WITH THE SHIPPED BUILDER, AND PASTE IT. Insert a
    TEMPORARY case inside the gate's own `describe` that evaluates `constructAnchorFor` at EVERY line of
    the file — every line, not every surface line — and throws out the count of lines resolving to the
    sentinel, the list of those lines, and the file's total line count. Paste the raw output. Reconcile
    the total against `wc -l`, against `01-VERIFICATION.md`'s 11,079 and against `01-REVIEW.md`'s 11,072,
    and state which number is carried and why. THEN RUN THE DEPENDENCE CHECK: blank line 1 in the working
    tree, re-run the temporary case, paste the new resolving set, restore, and prove clean — this is what
    distinguishes a property of line 1's shape from a property of the builder, and the review asserts it
    without this task having watched it.

    RESTATE BOTH REACH PARAGRAPHS TO THE EXECUTED BOUND. At `:9229-9232` and at `:10295-10300`, add the
    measured bound in the bytes: that the sentinel is returned only when the backward scan exhausts the
    file; that line 1 is a `//` comment naming something, which the fifth recogniser accepts
    unconditionally, so from line 2 down the forward walk returns on its first iteration; that evaluated
    at every line of the file exactly ONE resolves to the sentinel, and it is line 1; that the case is
    therefore GREEN BY CONSTRUCTION over every surface line but that one; and that it is KEPT because a
    declared phrasing landing on line 1, or line 1 ceasing to be a nameable `//` comment, are both edits a
    person can make. Say plainly that it is not evidence about the other surface lines. KEEP the existing
    statement of what each of the two sentinel-reading cases bounds — it is correct — and add the executed
    bound beside it rather than replacing it.

    SAY WHAT NEEDS NO CHANGE, AND WHY. The DECLARED-KEY half of CR-21 is CLOSED: pass 10 planted the exact
    CR-17 key into `HEADER_QUANTIFIER_EXEMPTIONS` and three cases fired at `Tests 3 failed | 436 passed
    (439)`, where pass 9 had measured that same remainder at 20 and passing. State that in one line in the
    SUMMARY and do not edit the null-anchor case.

    WR-58 — GUARD THE CENSUS'S KEY SPLIT AND WATCH IT IN BOTH DIRECTIONS. Give the census case's
    `constructHalf` an explicit check on the separator's index, throwing with a message that names the key,
    names the separator, and says the key carries no separator so it has no construct half — the same
    thing the prefix case's guard says, written for this call site. THEN PROVE THE DIFFERENCE RATHER THAN
    ASSERTING IT: commit, plant a separator-less key into `HEADER_QUANTIFIER_EXEMPTIONS`, run with the
    guard REVERTED and paste the census's message — expected to be about ambiguity, a correct red for the
    wrong reason — then run with the guard IN and paste the census's message, expected to be about
    malformation. Both messages pasted side by side is the evidence; a single run proves nothing about the
    guard. Restore both; prove clean.

    MEASURE THE SECOND `constructHalf` AND DO NOT TOUCH IT. The cross-construct fixture at `:10568-10569`
    carries the same expression. Measure whether it has the same exposure — its inputs are built by the
    live builder, which carries the separator by construction, and whether that makes the exposure
    unreachable is a MEASUREMENT and not an assumption. State the result in one line, in the bytes beside
    the guarded one, and state explicitly that the OTHER call site is UNGUARDED and that WR-57 — which
    proposes consolidating the two — is UNADJUDICATED by pass 10 and is NOT closed here. A guard added to
    one of two identical expressions, described as "the guard", would be the eleventh instance.

    WR-60 — READ THE RECORD FIRST, THEN DERIVE. Read `01-39-SUMMARY.md:496-535` and its discrepancy row at
    `:931` and state what they support. If they support the reading that 58 was measured against the
    unmodified file and 57 against a tree where the two-line cell had already been deleted, then the
    reviewer's `one of them is wrong` is REFUTED and the real defect is that neither sentence states its
    FRAME — say exactly that, and record it as a finding this round measured rather than as something pass
    10 established. If the bytes do not support it, say so and take the reviewer's disposition instead.

    THEN WRITE THE NUMBER ONCE. State the distance a single time with its basis and its two endpoints —
    the source line, the destination line and which frame the number is measured in — and have the second
    site refer to that statement rather than restate a second number. Re-locate both identically-headed
    table headers in the file TODAY by their text, paste today's line numbers, and state explicitly
    whether today's separation matches the historical figure; if it does not, that is expected because the
    file has grown, and the sentence must be written so a reader can tell a historical measurement from a
    live one. DO NOT edit the neighbouring IN-44 sentence about `it.each([` and TEN producers.

    RE-RUN PLAN 01-42's SHADOW PIN AND ATTRIBUTE ANY MOVEMENT, under the same rule as Task 1: attributed
    line-by-line to lines this task added, or the pin stays unmoved and the movement is a finding.

    TOUCH NOTHING OUTSIDE SCOPE. `constructAnchorFor`'s body and both scan bounds, `constructTokenOf`,
    `exemptionKeyFor`, `maskQuantifiers`, `normalizeGateLine`, `nameableRemainder`,
    `CONSTRUCT_TOKEN_ELLIPSIS`, `EXEMPTION_ANCHOR_SEP`, `NO_PRECEDING_CONSTRUCT` itself, every exemption
    key and value, the null-anchor case, the prefix case, all three `EXCLUSIONS`, every band, every
    `proof` token, Task 1's locators and fixture, plan 01-42's case and paragraphs, `CORE11_BOX_EXPECTED`
    and the box case all stay byte-unchanged. Nothing under `.planning/` is opened.

    RE-READ EVERY SENTENCE THIS TASK WRITES AGAINST THE NO-OVERCLAIM PROHIBITION AND RECORD A VERDICT PER
    SENTENCE, reach claimed beside reach executed. All three items are disclosure corrections and the
    sentence describing the guard's coverage is this task's likeliest place to author the eleventh
    instance.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && B="${TMPDIR:-/tmp}/defminer-round10-baseline"; [ -f "$B" ] || { echo "MISSING BASELINE FILE"; exit 1; }; . "$B"; GT=$(pnpm exec vitest run packages/backend/src/outbound-prohibition.spec.ts 2>&1 | tee /dev/stderr | sed -n 's/.*Tests .*(\([0-9][0-9]*\))[[:space:]]*$/\1/p' | tail -1); [ -n "$GT" ] && [ "$GT" -ge "$BASE_GATE" ] || { echo "GATE COUNT $GT BELOW CAPTURED $BASE_GATE"; exit 1; }; FT=$(pnpm test 2>&1 | tee /dev/stderr | sed -n 's/.*Tests .*(\([0-9][0-9]*\))[[:space:]]*$/\1/p' | tail -1); [ -n "$FT" ] && [ "$FT" -ge "$BASE_TESTS" ] || { echo "SUITE COUNT $FT BELOW CAPTURED $BASE_TESTS"; exit 1; }; pnpm typecheck && pnpm lint && pnpm knip && pnpm build:backend && pnpm check:bundle && RB=$(git rev-parse 483a8ee) && D=$(git diff "$RB"..HEAD -- packages/backend/src/outbound-prohibition.spec.ts); AST=$(printf '%s\n' "$D" | grep -cE '^[+-].*(from "typescript"|[^A-Za-z]ts\.[A-Za-z])' || true); [ "$AST" -eq 0 ] || { echo "COMPILER-API LINES IN DIFF: $AST"; exit 1; }; NAR=$(printf '%s\n' "$D" | grep -cE '^[+-].*(DECLARATION =|FIXTURE_TITLE =|DOCBLOCK_OPEN =|isLineComment =|isRule =|CONSTRUCT_TOKEN_ELLIPSIS =|EXEMPTION_ANCHOR_SEP =|NO_PRECEDING_CONSTRUCT =|lineNumber - 1|k < lineNumber)' || true); [ "$NAR" -eq 0 ] || { echo "NARROWING LINES IN DIFF: $NAR"; exit 1; }; W60=$(grep -cE '5[78] lines (apart|away)' packages/backend/src/outbound-prohibition.spec.ts || true); [ "$W60" -le 1 ] || { echo "TWO RELOCATION DISTANCES STILL PRESENT: $W60"; exit 1; }; GUARD=$(printf '%s\n' "$D" | grep -cE '^\+.*indexOf\(EXEMPTION_ANCHOR_SEP\)' || true); [ "$GUARD" -ge 1 ] || { echo "NO GUARDED SPLIT ADDED: $GUARD"; exit 1; }; SEC=$(printf '%s\n' "$D" | grep -cE '^[+-].*preAnchoringExemptionKeyForFixtureOnly' || true); [ "$SEC" -eq 0 ] || { echo "COUNTER-PROBE TOUCHED: $SEC"; exit 1; }; N=$(git status --porcelain .planning/ | grep -c . || true); [ "$N" -eq 0 ] || { echo "PLANNING DIRTY: $N"; exit 1; }; A=$(git diff --name-only "$RB"..HEAD -- packages/ scripts/ | grep -v '\.spec\.ts$' | grep -c . || true); [ "$A" -eq 0 ] || { echo "SHIPPED SOURCE TOUCHED: $A"; exit 1; }; M=$(find packages/*/src -name '*.ts' ! -name '*.spec.ts' | grep -c . || true); [ "$M" -eq "$BASE_MODULES" ] || { echo "MODULE COUNT $M != $BASE_MODULES"; exit 1; }; git diff --exit-code -- packages/backend/src/outbound-prohibition.spec.ts && echo TREE-CLEAN</automated>
  </verify>

  <acceptance_criteria>
    - The precondition asserted and PASTED, with HEAD, a clean status, the gate suite at Task 1's count
      and plan 01-42's pinned maximum re-read.
    - The TEMPORARY exhaustive case's raw output pasted: the file's total line count, the count of lines
      resolving to the sentinel, and the list of those lines — measured with the SHIPPED builder from
      inside the gate's own `describe`, shown by diff to be inside it and shown by diff to have been
      removed afterwards.
    - The total reconciled against `wc -l`, `01-VERIFICATION.md`'s 11,079 and `01-REVIEW.md`'s 11,072,
      with the carried number stated and the disagreement recorded.
    - THE DEPENDENCE CHECK executed: line 1 blanked, the temporary case re-run, the new resolving set
      pasted, restored, clean diff — with an explicit statement that this was watched in this session
      rather than taken from the review.
    - Both restated reach paragraphs quoted VERBATIM and each shown to carry all five elements: the
      sentinel reached only by exhausting the backward scan; line 1's shape and the fifth recogniser named
      as the cause; the measured count of resolving lines with the file's total; green by construction
      over every surface line but that one; and the two edits that would make it fire, given as the reason
      it is kept. Each shown to ADD the executed bound rather than replace the existing correct statement.
    - A one-line statement in the SUMMARY that CR-21's declared-key half is closed and needs no change,
      citing pass 10's `Tests 3 failed | 436 passed (439)`, with the null-anchor case shown unchanged by
      diff.
    - The census's `constructHalf` shown by diff to carry an explicit separator-index check, with its
      throw message quoted verbatim.
    - THE GUARD WATCHED IN BOTH DIRECTIONS: `git log --oneline -1` pasted first; a separator-less key
      planted; the census's message pasted with the guard REVERTED and again with the guard IN; both
      quoted side by side and shown to differ in kind — ambiguity versus malformation; everything
      restored; `git diff --exit-code` clean.
    - The second `constructHalf`'s exposure MEASURED and the result stated in one line, with an explicit
      statement in the bytes that the other call site is UNGUARDED and that WR-57 is unadjudicated by
      pass 10 and is NOT closed here — and `git diff` shown EMPTY over that second declaration.
    - `01-39-SUMMARY.md:496-535` and `:931` read and their support STATED, with an explicit verdict on
      whether the reviewer's `one of them is wrong` is refuted, recorded as a claim measured in this round
      rather than as something pass 10 established.
    - The single derived distance quoted verbatim with its basis, its source line, its destination line
      and its frame named; the second site quoted and shown to refer to it rather than restate a number;
      the numeric check showing at most one such figure remains in the file, with its output pasted.
    - Both identically-headed table headers re-located by text TODAY with today's line numbers pasted, and
      an explicit statement of whether today's separation matches the historical figure and how a reader
      can tell a historical measurement from a live one.
    - `git diff` shown EMPTY over the neighbouring IN-44 sentence about `it.each([` and TEN producers.
    - Plan 01-42's shadow pin re-run with its maximum pasted; any movement attributed line-by-line, or the
      pin shown unmoved with the movement recorded as a finding, or an explicit statement that it did not
      move.
    - The compiler-API check, the narrowing check and the counter-probe check all shown returning zero,
      with their output pasted.
    - `git diff` shown EMPTY over every other out-of-scope construct named in the action.
    - `git status --porcelain .planning/` asserted EMPTY numerically; `requirements mark-complete` absent
      from every command run; `CORE11_BOX_EXPECTED` byte-unchanged.
    - Executed `pnpm test` exit 0 with counts pasted and asserted at or above the captured baseline;
      `typecheck`, `lint`, `knip` exit 0; `build:backend && check:bundle` exactly one specifier, `crypto`;
      the real-tree walk asserted at the captured module count with zero violations; CORE-11's first
      sentence pasted byte-identical.
    - A one-line no-overclaim verdict recorded for EVERY sentence added or changed, reach claimed beside
      reach executed, with before and after pasted for each rewrite.
    - Any prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses, or an
      explicit statement that there were none.
  </acceptance_criteria>

  <done>
    Both sentinel-reach paragraphs state the executed bound measured over every line of the file in this
    session with line 1's dependence watched rather than cited, the census's key split fails loudly on a
    malformed key with the difference proved in both directions and the second unguarded call site named,
    and the one relocation distance is stated once with its frame and its endpoints after the record that
    explains why two values were both correct has been read and its reading written down.
  </done>
</task>

<task type="auto">
  <name>Task 3: the round's closing baseline executed rather than carried, the whole round reconciled against arrival, and every ledger, probe and coverage obligation restated at the reach of a pasted command</name>

  <precondition>
    Tasks 1 and 2 have committed and every mutation is restored. Assert and PASTE `git rev-parse HEAD`, a
    clean `git status --porcelain packages/ scripts/ .planning/`, and the gate suite green at Task 2's
    recorded count. The round's baseline file is read one last time and its contents pasted beside this
    session's fresh measurements. HALT if anything is red.
  </precondition>

  <read_first>
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md` — the
      `## Everything below is a command I ran this session, on the real tree` section, which is the exact
      set of figures this task re-executes; the `regressions:` block; and the CORE-11 `prohibitions:`
      entry whose `evidence` field lists the nine `auditSource` probes pass 10 re-ran.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md` — the header stating the
      equality `38 == 27 + 11` and the 38-row table. Read it; do not edit it unless a row genuinely moves,
      and if one does, move it in the SAME COMMIT and re-state the equality with its new arithmetic.
    - `.planning/phases/01-skeleton-persistence-compatibility/COVERAGE.md` — the header and its policy
      statement. This round adds no external API surface; the declaration is CONFIRMED, and no row is
      fabricated.
    - `.planning/REQUIREMENTS.md` — CORE-11's row at `:46`, its opening bytes only, for the byte-identity
      comparison. READ ONLY. This task opens no file under `.planning/` for writing.
    - `01-41-SUMMARY.md`'s closing-baseline section, for the shape this task's baseline takes and the
      convention of attributing every difference to a named plan and task.
    - `01-42-SUMMARY.md` and this plan's Tasks 1 and 2 SUMMARY material, for the list of every figure this
      round published, so the reconciliation covers all of them rather than the ones that come to mind.
  </read_first>

  <files>packages/backend/src/outbound-prohibition.spec.ts</files>

  <reversibility rating="reversible">Measurement and reconciliation only. This task changes no mechanism; if it edits any byte at all it is to correct a figure this round itself published, and any such edit is recorded with its before and after.</reversibility>

  <action>
    EXECUTE THE BASELINE. Do not carry a single figure. Run and paste: `pnpm test` with its file and test
    counts and its exit status; `pnpm exec tsc --build`; `pnpm check:bundle` with its full specifier line;
    the gate file alone with its own count; `find packages/*/src -name '*.ts' ! -name '*.spec.ts' | wc -l`
    with the walk's violation count from inside the green suite; and `SURFACE_LINES.length` read out of
    the suite. Compare each against the arrival values in the round's baseline file AND against pass 10's
    published figures — 31 files / 1379 tests, `tsc --build` 0, ONE specifier `crypto`, gate 439, 23
    modules / 0 violations, surface 8846. ATTRIBUTE EVERY DIFFERENCE to a named plan and task. AN
    UNATTRIBUTED DIFFERENCE IS A FINDING and is written up rather than absorbed.

    RE-ESTABLISH THE NO-LEAK RESULT FOR THE WHOLE ROUND, NOT FOR THIS TASK.
    `git diff --name-only <round base>..HEAD -- packages/ scripts/` filtered of `.spec.ts` asserted EMPTY
    numerically, with the round base named by commit. `git log --stat <round base>..HEAD -- packages/`
    pasted and shown to touch exactly one file. CORE-11's first sentence pasted from `.planning/
    REQUIREMENTS.md` and shown BYTE-IDENTICAL to its form at the round's base, by a diff over those bytes
    and not by reading them twice.

    RE-EXECUTE THE PROBES RATHER THAN CITING THEM. Run through the shipped `auditSource` the nine shapes
    pass 10 re-ran and paste each result: the four `fetch` spellings, the beacon spelling, the send
    spelling, the assembled-member spelling, the clean source, and the constructor-in-receiver-position
    spelling that is correctly SILENT and carried as a named row. State in one line that the silent one is
    still carried as its named `measured-silence` row and that the removed
    `silence-operator-around-global-receiver` row is still absent, with the registry's row count pasted.

    RE-STATE THE LEDGER POSITION WITHOUT MOVING IT. `CORE11_BOX_EXPECTED` pasted and shown byte-unchanged
    and still reading the unchecked form; `.planning/REQUIREMENTS.md`'s CORE-11 checkbox pasted and shown
    unchecked; `git status --porcelain .planning/` asserted EMPTY numerically; `requirements
    mark-complete` shown absent from every command this round ran. STATE EXPLICITLY that the box was not
    moved in either direction and that whether it may move is verification pass 11's determination.

    RESTATE `01-PROBE.md`'s EQUALITY. Paste the equality `38 == 27 + 11` and the row counts that produce
    it, and state that it is unchanged. It is deterministic over the same requirement text and is NOT
    re-run; if a row genuinely moves this round, move it in the SAME COMMIT and re-state the arithmetic
    with the new numbers. Any new prohibition this round authored is DESCRIPTOR-LESS and therefore
    disposes as flagged-unverified; say so in one line. NEVER AUTO-DISMISS.

    CONFIRM `COVERAGE.md` AND RUN THE DETECTORS. Confirm `COVERAGE.md` is unedited and that its
    declaration stands; this round adds no external API surface and NO ROW IS FABRICATED. Run the
    assumption-delta detector and state its result in one line including a plain `did not fire` if that is
    what happened. No ORM file is in scope, so the schema-push step is skipped silently and that is stated
    in one line.

    RECONCILE EVERY FIGURE THIS ROUND PUBLISHED. Build one table: each number plan 01-42 and this plan
    stated about their own work, the command that produces it, and this session's re-measurement. Every
    row either agrees or is a finding. Include the shadow maximum, the surface size, the file's line
    count, the sentinel-resolving line count, the decoy's surface amputation, the relocation distance and
    both suite counts.

    RE-READ THE ROUND'S NEW PROSE AGAINST THE NO-OVERCLAIM PROHIBITION ONE LAST TIME, ACROSS BOTH PLANS.
    Produce a single verdict table covering every sentence plans 01-42 and 01-43 added or changed, with
    the reach claimed beside the reach executed. THIS IS THE ROUND'S LAST CHANCE TO CATCH THE ELEVENTH
    INSTANCE BEFORE A VERIFIER DOES, and the round exists because the tenth was written inside the
    paragraph closing the ninth.

    STATE WHAT IS STILL OPEN, IN ONE PLACE. The relocation class is not closed — pass 10's relocation was
    re-run in plan 01-42 and stayed green. Criterion (3)'s mechanism leg is not discharged by this round
    and this round does not claim it. `EXCLUSIONS`' opening locators are still prefix matchers. The
    sentinel case is still green by construction over all but one line. The second `constructHalf` is
    still unguarded. WR-56, WR-57, WR-59 and IN-41 through IN-44 are unadjudicated and untouched.
    `.planning/STATE.md` and `.planning/WINDOWS.md` are still reached by no mechanical comparison.

    CHANGE NO MECHANISM. If this task finds a figure this round published that is wrong, correct it, paste
    the before and after, and say which plan and task published it. Otherwise this task writes no byte of
    the spec file, and that is stated explicitly.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && B="${TMPDIR:-/tmp}/defminer-round10-baseline"; [ -f "$B" ] || { echo "MISSING BASELINE FILE"; exit 1; }; . "$B"; GT=$(pnpm exec vitest run packages/backend/src/outbound-prohibition.spec.ts 2>&1 | tee /dev/stderr | sed -n 's/.*Tests .*(\([0-9][0-9]*\))[[:space:]]*$/\1/p' | tail -1); [ -n "$GT" ] && [ "$GT" -gt "$BASE_GATE" ] || { echo "GATE COUNT $GT NOT ABOVE ARRIVAL $BASE_GATE"; exit 1; }; FT=$(pnpm test 2>&1 | tee /dev/stderr | sed -n 's/.*Tests .*(\([0-9][0-9]*\))[[:space:]]*$/\1/p' | tail -1); FF=$(pnpm test 2>&1 | sed -n 's/.*Test Files .*(\([0-9][0-9]*\))[[:space:]]*$/\1/p' | tail -1); [ -n "$FT" ] && [ "$FT" -gt "$BASE_TESTS" ] || { echo "SUITE COUNT $FT NOT ABOVE ARRIVAL $BASE_TESTS"; exit 1; }; [ -n "$FF" ] && [ "$FF" -eq "$BASE_FILES" ] || { echo "FILE COUNT $FF != $BASE_FILES"; exit 1; }; pnpm exec tsc --build && pnpm typecheck && pnpm lint && pnpm knip && pnpm build:backend && pnpm check:bundle && M=$(find packages/*/src -name '*.ts' ! -name '*.spec.ts' | grep -c . || true); [ "$M" -eq "$BASE_MODULES" ] || { echo "MODULE COUNT $M != $BASE_MODULES"; exit 1; }; RB=$(git rev-parse 483a8ee) && A=$(git diff --name-only "$RB"..HEAD -- packages/ scripts/ | grep -v '\.spec\.ts$' | grep -c . || true); [ "$A" -eq 0 ] || { echo "ROUND TOUCHED SHIPPED SOURCE: $A"; exit 1; }; F=$(git diff --name-only "$RB"..HEAD -- packages/ scripts/ | grep -c . || true); [ "$F" -eq 1 ] || { echo "ROUND TOUCHED $F FILES UNDER packages/scripts, EXPECTED 1"; exit 1; }; BOX=$(git diff "$RB"..HEAD -- .planning/REQUIREMENTS.md | grep -c . || true); [ "$BOX" -eq 0 ] || { echo "REQUIREMENTS.md CHANGED THIS ROUND: $BOX"; exit 1; }; N=$(git status --porcelain .planning/ | grep -c . || true); [ "$N" -eq 0 ] || { echo "PLANNING DIRTY: $N"; exit 1; }; P=$(grep -c '38 == 27 + 11' .planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md || true); [ "$P" -ge 1 ] || { echo "PROBE EQUALITY MISSING"; exit 1; }; git diff --exit-code -- packages/backend/src/outbound-prohibition.spec.ts && echo TREE-CLEAN</automated>
  </verify>

  <acceptance_criteria>
    - The precondition asserted and PASTED, with HEAD, a clean status and the gate suite at Task 2's count.
    - THE BASELINE EXECUTED IN THIS SESSION, every figure pasted with the command that produced it:
      `pnpm test` file and test counts and exit status; `tsc --build`; `check:bundle`'s full specifier
      line; the gate suite's own count; the module count with the walk's violation count from inside the
      green suite; and `SURFACE_LINES.length`. Each compared against the round's baseline file AND against
      pass 10's published 31 / 1379 / 0 / one specifier / 439 / 23-0 / 8846, with EVERY difference
      attributed to a named plan and task and every unattributed difference recorded as a finding.
    - The round-wide `git diff --name-only` over `packages/` and `scripts/` filtered of `.spec.ts` shown
      EMPTY numerically with the round base named by commit; `git log --stat` over the same range shown
      touching exactly one file; the numeric check from the verify block pasted.
    - CORE-11's first sentence shown BYTE-IDENTICAL to its form at the round's base by a diff over those
      bytes, not by reading them twice; the numeric check showing `.planning/REQUIREMENTS.md` unchanged
      across the round pasted.
    - The nine `auditSource` probes RE-EXECUTED with each result pasted, plus a one-line statement that
      the silent constructor-in-receiver-position shape is still carried as its named `measured-silence`
      row and that the removed operator-around-global-receiver row is still absent, with the registry's
      row count pasted.
    - `CORE11_BOX_EXPECTED` pasted byte-unchanged and unchecked; `.planning/REQUIREMENTS.md`'s CORE-11
      checkbox pasted unchecked; `git status --porcelain .planning/` EMPTY numerically;
      `requirements mark-complete` shown absent from every command the round ran; an explicit statement
      that the box was not moved in either direction and that pass 11 owns the question.
    - `01-PROBE.md`'s equality pasted with its row counts and stated unchanged, with the numeric check
      from the verify block; a one-line statement that any prohibition this round authored is
      descriptor-less and disposes as flagged-unverified; an explicit statement that nothing was
      auto-dismissed.
    - `COVERAGE.md` confirmed unedited with its declaration stated as standing and an explicit statement
      that no row was fabricated; the assumption-delta detector run with its result stated in one line
      including a plain `did not fire` if that is what happened; the schema-push step stated as skipped
      with its reason in one line.
    - THE RECONCILIATION TABLE pasted: every figure plans 01-42 and 01-43 published about their own work,
      the command that produces it, this session's re-measurement, and agree-or-finding — including the
      shadow maximum, the surface size, the file's line count, the sentinel-resolving line count, the
      decoy's surface amputation, the relocation distance and both suite counts.
    - THE ROUND-WIDE no-overclaim verdict table pasted, covering every sentence both plans added or
      changed, with reach claimed beside reach executed and an explicit statement of whether an eleventh
      instance was found.
    - THE OPEN LIST stated in one place: the relocation class not closed with pass 10's relocation shown
      re-run green; criterion (3)'s mechanism leg not discharged and not claimed; `EXCLUSIONS`' openers
      still prefix matchers; the sentinel case still green by construction over all but one line; the
      second `constructHalf` still unguarded; WR-56, WR-57, WR-59 and IN-41 through IN-44 unadjudicated
      and untouched; `.planning/STATE.md` and `.planning/WINDOWS.md` still reached by no mechanical
      comparison.
    - An explicit statement of whether this task wrote any byte of the spec file, with before and after
      and the publishing plan and task named for any correction made.
    - Executed `pnpm test` exit 0; `tsc --build`, `typecheck`, `lint`, `knip` exit 0;
      `build:backend && check:bundle` exactly one specifier, `crypto`.
    - Any prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses, or an
      explicit statement that there were none.
  </acceptance_criteria>

  <done>
    Every figure the round published is re-measured in this session against a command, the whole round's
    code delta is shown to be one test file with CORE-11's first sentence byte-identical and the nine
    probes re-executed rather than cited, the ledger position is restated without being moved, and the
    round's open list is stated in one place with the eleventh-instance check run across both plans.
  </done>
</task>

</tasks>

<!-- planner-discipline-allow: from "typescript" -->
<!-- planner-discipline-allow: RESOLVER_REGISTRY_DECOY -->
<!-- planner-discipline-allow: l\.startsWith\("export const (RESOLVER_REGISTRY|UNBOUNDED_QUANTIFIERS)" -->
<!-- planner-discipline-allow: 5[78] lines (apart|away) -->
<!-- planner-discipline-allow: preAnchoringExemptionKeyForFixtureOnly -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an assertion to a tautology wearing an assertion's message | Both `.from` comparisons recompute the expression `EXCLUSIONS` was built from. They hold for every possible file, including one where the locator matches nothing, beside a message claiming both endpoints are pinned. |
| a prefix locator to a longer identifier | One planted `export const RESOLVER_REGISTRY_DECOY` slid exclusion three's opening endpoint and removed over a hundred lines from the scanned surface at 439 of 439 green. Any declared phrasing in them is unguarded and nothing says so. |
| fixing a tautology to restoring it | Changing `EXCLUSIONS`' own locators to the full-line form would make both sides the same expression again. The fix depends on the two sides staying independent. |
| a detected slide to a prevented one | The pins make a slide RED. They do not stop the exclusion resolving to the wrong line, and the corrected closing claim has to say so. |
| a case green by construction to a case describing itself as bounding a surface | Exactly one line of this file can resolve to the sentinel. The two paragraphs state a reach of twenty-nine occurrences across the scanned surface. |
| a malformed key to a near-complete one | An unguarded `indexOf` turns a separator-less key into the key minus its last character, so the census fails about ambiguity when the defect is malformation — a correct red for the wrong reason. |
| one guard to two identical call sites | The cross-construct fixture carries the same expression. Guarding one and calling it "the guard" is the same overclaim in miniature, and WR-57 — which proposes consolidating them — is unadjudicated. |
| one measurement to two published values | 57 and 58 for one relocation, 412 lines apart. `01-39-SUMMARY.md` already records both and explains them; the reviewer's diagnosis was written without that record. |
| an unadjudicated finding to a verified one | Pass 10 explicitly did NOT re-execute WR-56, WR-57, WR-59 or IN-41 through IN-44. Presenting any of them as closed would launder a reviewer's report into a verifier's result. |
| a carried baseline to an executed one | Round 8 shipped a revision because a piped `tail` reported green on a red suite. Every figure here is executed in this session and compared against arrival. |
| a test-only gate to the shipped bundle | Nothing here reaches shipped code. The round's whole code delta is asserted to be exactly one file by a diff over the round's base. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-277 | Tampering | an opening endpoint pinned by an expression compared against itself | high | mitigate | Each opening endpoint pinned against a FULL-LINE literal proved to match exactly one line before use, in the `REGISTRY_CLOSE` shape, with `EXCLUSIONS`' own prefix locators deliberately left independent and that independence asserted EMPTY-diff by a numeric check. |
| T-01-278 | Spoofing | a longer identifier satisfying a prefix locator | high | mitigate | Pass 10's decoy planted above the registry and an equivalent above the quantifier list, each watched turning its `.from` assertion RED, with the surface amputation counted from inside the suite by instrumenting the non-vacuity assertion rather than reasoned about. |
| T-01-279 | Tampering | the tautology restored while removing it | high | mitigate | Prohibited in frontmatter; a numeric check asserts zero diff lines touching `EXCLUSIONS`' opening locators; the reason is written into the file beside the pins so the next author reads it before consolidating the duplication. |
| T-01-280 | Repudiation | a pin described as preventing what it detects | high | mitigate | The corrected closing claim states which locators are proved unique, that the exclusion's own openers remain prefix matchers, and that the pins make a slide RED rather than preventing it — that reach and no wider, with exclusion three still named as a line range wider than its clause strings. |
| T-01-281 | Tampering | a decoy left in the tree | high | mitigate | A numeric check over the committed file asserts the decoy identifier absent; every mutation is plant-observe-restore over committed work with `git diff --exit-code` shown clean. |
| T-01-282 | Tampering | a permanent fixture writing a line the real locators match | high | mitigate | The fixture operates over a SYNTHETIC array only; both uniqueness checks are re-run after it lands and shown still at one, so a fixture that broke the proofs the fix depends on could not ship green. |
| T-01-283 | Repudiation | a case executing over one line and stating a reach of a surface | high | mitigate | The shipped builder evaluated at EVERY line of the file in this session, the resolving set pasted, line 1 blanked and the dependence watched rather than cited, and both paragraphs restated to add the executed bound beside the correct existing statement. |
| T-01-284 | Tampering | a malformed key silently becoming a near-complete one | medium | mitigate | An explicit separator-index check on the census's `constructHalf`, watched failing in BOTH directions with the census's message pasted before the guard and after it and shown to differ in kind. |
| T-01-285 | Repudiation | one guard described as covering two call sites | medium | mitigate | The second `constructHalf`'s exposure is MEASURED, the result stated in one line in the bytes, the other call site named as UNGUARDED, and WR-57 stated as unadjudicated and not closed. `git diff` over the second declaration asserted EMPTY. |
| T-01-286 | Repudiation | one measurement published at two values | medium | mitigate | `01-39-SUMMARY.md`'s own discrepancy record read FIRST, its support stated as a claim measured this round, the distance then written once with its basis, its two endpoints and its frame, the second site made a pointer, and a numeric check asserting at most one such figure remains. |
| T-01-287 | Spoofing | a reviewer's report presented as a verifier's result | high | mitigate | Prohibited in frontmatter. Every mention of WR-56, WR-57, WR-59 or IN-41 through IN-44 states that pass 10 recorded them as reviewer-reported and not re-executed, and none appears in any list of closed items. |
| T-01-288 | Repudiation | a closing baseline carried instead of executed | high | mitigate | Every figure executed in this session with its command pasted, compared against both the round's captured arrival values and pass 10's published set, with every difference attributed to a named plan and task and unattributed differences recorded as findings. |
| T-01-289 | Repudiation | the round's own published figures never re-checked | high | mitigate | A reconciliation table covering every number both plans stated about their own work, each with its producing command and this session's re-measurement, and each row either agreeing or becoming a finding. |
| T-01-263 | Repudiation | an eleventh overclaim authored inside the fix for the tenth | high | mitigate | An `open`-status DISCLOSED-NOT-CLOSED prohibition in frontmatter, a per-sentence verdict in every task, and a round-wide verdict table in Task 3 with an explicit statement of whether an eleventh instance was found. |
| T-01-240 | Tampering | a mutation planted over uncommitted work | high | mitigate | Every task commits before planting, pastes `git log --oneline -1` first, and proves each restore with `git diff --exit-code`. Wave 29 destroyed its own Task 1 exactly this way. |
| T-01-214 | Tampering | CORE-11's checkbox moved | critical | mitigate | Prohibited. This plan opens no file under `.planning/` for writing; a numeric check asserts `.planning/REQUIREMENTS.md` unchanged across the whole round; `CORE11_BOX_EXPECTED` asserted byte-unchanged; `requirements mark-complete` appears in no command. |
| T-01-212 | Information Disclosure | the two reader surfaces no mechanism reaches | medium | accept | `.planning/STATE.md` and `.planning/WINDOWS.md` are governed by the pointer-not-a-bound prohibition with no mechanical check. Untouched here and named in Task 3's open list. Carried forward from T-01-144. |
| T-01-51 | Information Disclosure | the accepted residual of the outbound walk | medium | mitigate | Unchanged by this plan — no registry row moves — re-established by the nine probes re-executed in Task 3 and byte-pinned into the ledger by the two comparisons, green at every task boundary. |
| T-01-50 | Information Disclosure | `.spec.ts` files, which the walk skips | low | accept | Unchanged and carried forward, bounded by `pnpm check:bundle`, which specs never enter. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | NOT APPLICABLE: this plan installs no package. `pnpm install` is not run and no manifest appears in `files_modified`. `01-RESEARCH.md`'s Package Legitimacy Audit is present and unchanged; no `[ASSUMED]` or `[SUS]` package is introduced, so no blocking human checkpoint is required. |
</threat_model>

<verification>
1. Precondition asserted and pasted at all three task boundaries, with HEAD, a clean status, the prior
   task's recorded count and plan 01-42's pinned maximum re-read.
2. All three exclusions' `from`/`to`/size/band and `SURFACE_LINES.length` pasted BEFORE Task 1's change,
   with both constructs' full-line-located openings and both prefix resolutions, reconciled against
   `:4161` / `:5793` / `:5985` and against plan 01-41's 4135 / 5767 / 5959, every disagreement a finding.
3. Both full-line literals checked against all nine declared phrasings, with the result pasted.
4. Both locator constants shown by diff to be full-line equalities, each with a hit count and an
   exactly-one assertion whose message is quoted and shown to name both failure directions.
5. Both `.from` assertions shown present and shown by diff to compare against the full-line-located line,
   with both messages quoted; the both-endpoints-pinned sentence quoted before and after.
6. `git diff` EMPTY over `EXCLUSIONS`' opening locators by numeric check, with the in-file reason for
   keeping them independent quoted.
7. The permanent fixture shown by diff to be synthetic and non-vacuous, and both uniqueness checks re-run
   after it lands and shown still at one.
8. `git log --oneline -1` before each decoy; the registry decoy's `SURFACE_LINES.length` printed from
   inside the suite with and without it; the delta reconciled against 8846->8742 and 8848->8743 with this
   session's figures; RED observed with the FULL message and both line numbers; instrumentation removed;
   restored; clean diff; the prior green cited.
9. The list decoy planted and the exclusion-two `.from` assertion observed RED with its FULL message;
   restored; clean diff. A non-red on either is a first-order finding with no adjustment made.
10. The corrected closing claim quoted and shown to state what is proved, that the openers are prefix
    matchers, and that a slide is made red rather than prevented.
11. The temporary exhaustive sentinel case's output pasted with the file's total, the resolving count and
    the resolving lines, shown by diff in and out; the total reconciled against three disagreeing
    surfaces with the carried number stated.
12. The line-1 dependence check executed in this session, with the new resolving set pasted, restored and
    proved clean.
13. Both restated sentinel-reach paragraphs quoted verbatim and each shown to carry all five elements and
    to ADD the executed bound rather than replace the correct existing statement.
14. CR-21's declared-key half stated closed in one line with pass 10's counts cited, and the null-anchor
    case shown unchanged.
15. The census's `constructHalf` shown by diff to carry the separator-index check, its message quoted, and
    the guard watched in BOTH directions with both census messages pasted side by side.
16. The second `constructHalf`'s exposure measured, stated in one line in the bytes, named as unguarded,
    WR-57 stated unadjudicated and not closed, and `git diff` EMPTY over that declaration.
17. `01-39-SUMMARY.md:496-535` and `:931` read, their support stated, and an explicit verdict on the
    reviewer's `one of them is wrong` recorded as a claim measured this round.
18. The single derived distance quoted with basis, endpoints and frame; the second site shown to point
    rather than restate; the numeric check showing at most one such figure remains; both table headers
    re-located by text today with today's numbers and the historical-versus-live distinction stated.
19. `git diff` EMPTY over the neighbouring IN-44 sentence.
20. Plan 01-42's shadow pin re-run at BOTH Task 1's and Task 2's boundary, with the maximum pasted and any
    movement attributed line-by-line or the pin shown unmoved and the movement recorded as a finding.
21. The compiler-API check, the narrowing check, the `EXCLUSIONS`-locator check, the decoy-absence check
    and the counter-probe check all shown returning their expected values at their task boundaries.
22. The round's baseline EXECUTED in Task 3 with every figure and its command, compared against both the
    captured arrival values and pass 10's published set, every difference attributed, unattributed
    differences recorded as findings.
23. The round-wide `git diff --name-only` filtered of `.spec.ts` EMPTY numerically with the round base
    named; `git log --stat` shown touching exactly one file; `.planning/REQUIREMENTS.md` shown unchanged
    across the round; CORE-11's first sentence shown byte-identical by a diff over those bytes.
24. The nine `auditSource` probes re-executed with each result pasted, the silent shape's named row and
    the removed row's continued absence stated with the registry row count.
25. `CORE11_BOX_EXPECTED` byte-unchanged and unchecked; the ledger checkbox unchecked;
    `git status --porcelain .planning/` EMPTY numerically at every boundary; `requirements mark-complete`
    absent from every command the round ran.
26. `01-PROBE.md`'s equality restated unchanged with its row counts and its numeric check; new
    prohibitions stated descriptor-less and disposing as flagged-unverified; nothing auto-dismissed.
27. `COVERAGE.md` confirmed unedited with no fabricated row; the assumption-delta detector run and stated
    in one line; the schema-push step stated as skipped with its reason.
28. The reconciliation table covering every figure the round published, and the round-wide no-overclaim
    verdict table with an explicit statement of whether an eleventh instance was found.
29. The open list stated in one place, naming every item this round leaves open.
30. `pnpm test` exit 0 with counts asserted against captured values at every boundary; `tsc --build`,
    `typecheck`, `lint`, `knip` exit 0; `build:backend && check:bundle` exactly one specifier, `crypto`;
    the real-tree walk at the captured module count with zero violations.
31. Every prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses, or an
    explicit statement that there were none.
</verification>

<success_criteria>
- Both opening endpoints are pinned against full-line locators proved to match exactly one line before
  use, both `.from` assertions still exist and now compare independent expressions, and `EXCLUSIONS`' own
  prefix locators are byte-unchanged so the comparison stays a comparison.
- Verification pass 10's decoy, and its equivalent above the quantifier list, have each been watched
  turning a `.from` assertion RED where they were green at 439 of 439, with the surface amputation counted
  from inside the suite.
- The decoy shape survives as a permanent synthetic fixture that fails if the pin's locator is ever
  swapped back to a prefix matcher, and it writes no line the real locators would match.
- The closing claim states which locators are proved, that the exclusion's own openers are prefix matchers
  a longer identifier satisfies, and that the pins detect a slide rather than prevent one.
- Both sentinel-reach paragraphs state the executed bound, measured over every line of the file in this
  session, with line 1's dependence watched rather than cited.
- The census's key split fails loudly on a malformed key, proved by pasting the census's message before
  and after the guard, and the second unguarded call site is named with WR-57 stated unadjudicated.
- The relocation distance is stated once with its basis, its endpoints and its frame, written after the
  record that explains why two values were both correct has been read.
- No unadjudicated finding is presented as verified or closed.
- No AST, parser, compiler-API or enclosing-frame identity appears anywhere in the diff and nothing was
  narrowed, both proved by structural checks.
- No band, `proof` token, exclusion or pinned count was widened; narrowing remained the only permitted
  direction.
- The round's baseline was EXECUTED in this session and every difference from arrival attributed; the
  round's whole code delta is one test file; CORE-11's first sentence is byte-identical; and the nine
  probes were re-executed rather than cited.
- No sentence anywhere claims that round 10 closes the relocation class, discharges criterion (3), makes
  the exclusions exact, or closes CORE-11.
- CORE-11's checkbox did not move in either direction, `requirements mark-complete` was not run, no file
  under `.planning/` was written, and whether the box may move is left to verification pass 11.
</success_criteria>

<artifacts_this_phase_produces>
| Artifact | Kind | Purpose |
|----------|------|---------|
| the two full-line opening-endpoint locators | new mechanism | Each proved to match exactly one line before use, in the treatment `REGISTRY_CLOSE` already gets, so neither `.from` pin can pass by comparing -1 against -1 or by comparing an expression against itself |
| both corrected `.from` assertions | new mechanism | Kept, repositioned on independent expressions, and given messages naming the realized endpoint, the construct's real opening line, the gap and what a difference means for the scanned surface |
| the registry decoy, watched RED | new evidence | Pass 10's planted line re-run against the corrected pin where it was green at 439 of 439, with the surface amputation counted from inside the suite by instrumenting the non-vacuity assertion |
| the quantifier-list decoy, watched RED | new evidence | The same treatment for the second construct, because correcting one and leaving the other unwatched is the identical defect one construct over |
| the synthetic decoy fixture | new mechanism | A permanent case distinguishing a prefix locator from a full-line one over synthetic lines, so swapping the pin's locator back turns the suite red |
| the corrected closing claim | corrected disclosure | Which locators are proved unique, that the exclusion's own openers are prefix matchers, and that the pins make a slide red rather than preventing it — that reach and no wider |
| the exhaustive sentinel measurement | new evidence | The shipped builder evaluated at every line of the file with the resolving set pasted, and line 1's dependence watched by blanking it rather than cited from the review |
| both restated sentinel-reach paragraphs | corrected disclosure | The executed bound added beside the correct existing statement: one line can fire this case, it is green by construction over the rest, and it is kept because two person-made edits would change that |
| the guarded census key split | new mechanism | An explicit separator-index check so a malformed key fails about malformation rather than about ambiguity, watched failing in both directions with both messages pasted |
| the second call site, measured and named | corrected disclosure | The cross-construct fixture's identical expression measured, stated unguarded in the bytes, and WR-57 stated unadjudicated and not closed |
| the single derived relocation distance | corrected disclosure | One number, one basis, two named endpoints and its frame, written after `01-39-SUMMARY.md`'s own discrepancy record was read and its reading written down |
| the round's closing baseline and reconciliation table | new evidence | Every figure the round published re-measured in this session against a command, every difference attributed, the whole code delta shown to be one test file and the nine probes re-executed |
| `01-43-SUMMARY.md` | new evidence | The measurements, both pins, both decoys, the fixture, the corrected claim, the sentinel measurement, the guard's two messages, the derived distance, the baseline, the reconciliation and round-wide verdict tables, and the open list |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-skeleton-persistence-compatibility/01-43-SUMMARY.md` when done.

It must contain, as pasted terminal output rather than as prose: every precondition assertion with
`git rev-parse HEAD`; all three exclusions' `from`/`to`/size/band and `SURFACE_LINES.length` before Task
1's change with both constructs' full-line-located openings and both prefix resolutions and their
reconciliation; the declared-phrasing check on both candidate literals; both locator constants' diffs with
their exactly-one messages; both `.from` assertions' diffs and messages; the EMPTY diff over `EXCLUSIONS`'
opening locators; the synthetic fixture's diff and the two uniqueness checks re-run after it;
`git log --oneline -1` before each decoy; the registry decoy's surface counts with and without it and
their reconciliation, the RED title and FULL message, the restore and clean diff; the list decoy's RED and
FULL message with its restore; the corrected closing claim verbatim; the exhaustive sentinel case's output
and its diff in and out; the file's total line count reconciled against 11,079 / 11,072 / 11,071; the
line-1 dependence check's output and restore; both restated reach paragraphs verbatim; the guarded
`constructHalf`'s diff and throw message; the census's message BEFORE and AFTER the guard, side by side;
the second call site's measurement and its EMPTY diff; `01-39-SUMMARY.md`'s discrepancy record and this
round's verdict on it; the single derived distance verbatim with both sites; today's re-located table
header line numbers; the numeric checks for the compiler-API, the narrowing set, the `EXCLUSIONS`
locators, the decoy's absence, the counter-probe and the single remaining distance; plan 01-42's shadow
pin re-run at both boundaries with any attribution; the executed closing baseline with every figure and
its command and its comparison; the round-wide `git diff --name-only` and `git log --stat`; CORE-11's
first sentence shown byte-identical; the nine `auditSource` probe results; `CORE11_BOX_EXPECTED` and the
ledger checkbox; the EMPTY `git status --porcelain .planning/`; `01-PROBE.md`'s equality; the
assumption-delta detector's line; the reconciliation table; the round-wide no-overclaim verdict table; and
the final `pnpm test`, `tsc --build`, `typecheck`, `lint`, `knip` and `check:bundle` results.

It must state in ONE sentence what now holds each opening endpoint, why the previous assertions held
nothing, and why `EXCLUSIONS`' own locators were deliberately left alone.

It must state in ONE sentence what the `from` pins DETECT and, in the same sentence, that they do not
PREVENT the slide and do not make exclusion three exact.

It must state in ONE sentence how many lines of this file can resolve to the sentinel, how that was
measured, and why the case is kept anyway.

It must state in ONE sentence which `constructHalf` is guarded, which is not, and that WR-57 is
unadjudicated by verification pass 10 and is not closed here.

It must state in ONE sentence the one relocation distance, its two endpoints, its frame, and the verdict
this round reached on whether either previously published number was wrong.

It must state in ONE sentence, for the WHOLE of round 10, that the round's entire code delta is one test
file, that CORE-11's first sentence is byte-identical, and that nothing leaks.

It must state in ONE sentence that round 10 closes CR-22 and CR-23 and closes NEITHER the relocation class
NOR criterion (3), naming what remains open.

It must state in ONE sentence that no unadjudicated finding was presented as verified, naming WR-56,
WR-57, WR-59 and IN-41 through IN-44.

It must state in ONE sentence that CORE-11's box was not moved in either direction, that
`requirements mark-complete` was not run, that no file under `.planning/` was written by any plan in this
round, and that whether the box may move is verification pass 11's determination.
</output>
