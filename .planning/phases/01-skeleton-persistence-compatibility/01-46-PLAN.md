---
phase: 01-skeleton-persistence-compatibility
plan: 46
type: execute
wave: 46
gap_closure: true
gap_ids: [CR-30, CR-31]
depends_on: [01-45]
files_modified:
  - packages/backend/src/outbound-prohibition.spec.ts
  - .planning/REQUIREMENTS.md
# ROUND 12'S FIRST PLAN, AND THE ROUND IS DELIBERATELY NOT SHAPED LIKE THE ELEVEN BEFORE IT.
# READ THIS WHOLE BLOCK BEFORE THE TASKS.
#
# WHY THIS ROUND IS DIFFERENT. Twelve verification passes have each closed their own findings honestly
# and each surfaced a NEW instance of ONE family: prose in this gate file claiming more than the code
# executes. Pass 13 was asked what would close the CLASS instead of the instance and answered
# concretely, and this plan takes that answer rather than fixing instance thirteen and stopping:
#
#   "Instance 13 sits in the gate header's prose (lines 1..957) and IS MACHINE-DERIVABLE CHEAPLY — the
#    suite already holds the correct answer in two constants (`GATE_FILE`, `LEDGER`); rendering that set
#    into the header and byte-comparing it would make a sentence naming `WINDOWS.md` as checked
#    impossible to ship."
#
# THE PRECEDENT IS IN THIS SAME FILE AND IT IS THE ONLY MOVE IN THIS PHASE'S HISTORY THAT CLOSED A CLASS.
# `deriveResidual(RESOLVER_REGISTRY)` generates a machine-owned span that the suite byte-compares against
# the file's own bytes, in TWO surfaces. Wave 27 (plan 01-27) installed it; pass 13 re-audited it this
# session and found that all six items `08345c3` deleted from a hand-written list survive inside that
# generated span. READ `01-27-PLAN.md` and `01-27-SUMMARY.md` BEFORE TASK 1 — this plan is its sequel and
# reuses its machinery rather than inventing a second one.
#
# ---------------------------------------------------------------------------------------------------
# CR-30 — THE BLOCKER, AND THE FIRST FINDING IN TWELVE ROUNDS RUNNING IN THE UNSAFE DIRECTION.
# ---------------------------------------------------------------------------------------------------
# The gate file tells a reader TWICE, present tense and unmarked, that a MACHINE CHECK compares the
# residual against `.planning/WINDOWS.md`:
#   `:600-603`   — the clause ending "is a machine check and not a promise", with the follow-on
#                  "`REQUIREMENTS.md` and `STATE.md` still carry OLDER text and are deliberately
#                  untouched here" at `:603-605`
#   `:794-796`   — the same claim again, ending "stays a check rather than a promise"
# THE SUITE READS EXACTLY TWO FILES: `GATE_FILE` (`:9566`) and `LEDGER = ".planning/REQUIREMENTS.md"`
# (`:11367`). `WINDOWS.md` appears in code in FIVE places and all five are comment lines in this same
# file (`:601`, `:751`, `:794`, `:937`, `:954`). There is no renderer anywhere in `packages/`,
# `scripts/` or any `.ts`/`.mjs`/`.js` in the tree.
#   IT IS FALSE THREE SEPARATE WAYS AND EACH ONE IS INDEPENDENTLY DISQUALIFYING:
#   (a) No code reads `.planning/WINDOWS.md`. The same file states the truth 337 lines below, at
#       `:937-943`: "THAT POINTER-NOT-A-BOUND RULE IS A PROHIBITION WITH NO MECHANICAL CHECK — the byte
#       comparison reaches these two surfaces and no further." `.planning/REQUIREMENTS.md:170` agrees
#       and names it by class: "CLASS ONE, UNGUARDED FILES".
#   (b) `.planning/WINDOWS.md` has carried NO copy of the residual since wave 27. Its entry 32 reads
#       "POINTER, NOT A COPY — WAVE 27 (plan 01-27) … From this wave this ledger RESTATES NO BOUND", and
#       entries 33-41 each restate that. So the claimed agreement is between a text that exists and a
#       text deliberately removed eighteen waves ago.
#   (c) `:603-605` calls `REQUIREMENTS.md` "OLDER text … deliberately untouched" when it is ONE OF THE
#       TWO byte-compared surfaces, per `:934-936` in the same file.
#   WHY THE DIRECTION MATTERS. Every other instance had the gate reaching FURTHER than its prose
#   admitted. This one PROMISES A GUARANTEE THAT DOES NOT EXIST, on the one surface the file elsewhere
#   names as mechanically unreachable — and pass 13 identifies it as the mechanism by which WINDOWS.md
#   entries 22-31 accumulated superseded bounds nobody caught. Nobody checked, because the header said a
#   machine did.
#
# ---------------------------------------------------------------------------------------------------
# CR-31 — WARNING, INTRODUCED BY THE COMMIT THAT CLOSED CR-29.
# ---------------------------------------------------------------------------------------------------
# `:10192` reads "this session's evaluation of all five recognisers accepts only the `/**` docblock
# opener at 1573", inside a range it states as `419..1549` — which EXCLUDES 1573. The opener is at
# **1548**; line 1573 is an ordinary `//` comment. The sentence was TRUE before `08345c3` deleted 25
# lines above it (1573 − 25 = 1548) and that commit did not update it. This is WR-64's sentence,
# re-falsified by the commit that re-derived the pin. Pass 13 names the class fix rather than the
# instance fix: EMIT THE NUMBER FROM THE PIN'S OWN COMPUTATION instead of hand-writing it in a comment
# that must be re-derived by hand every time a line moves.
#
# ---------------------------------------------------------------------------------------------------
# SEVEN HARD CONSTRAINTS. EVERY ONE OF THEM IS A DEFECT THIS PHASE HAS ALREADY SHIPPED ONCE.
# ---------------------------------------------------------------------------------------------------
# 1. LINE COUNT IS LOAD-BEARING. This file's pins are computed over its own bytes. Any inserted or
#    deleted line moves anchors, exclusion endpoints and the widest anchor shadow. `WIDEST_ANCHOR_SHADOW`
#    currently reads 549 over raw span `419..1549`. A task that changes the line count MUST re-derive the
#    pin AND every live restatement of the number, and MUST attribute the delta LINE BY LINE to the lines
#    that task added or removed. FITTING A PIN TO A NUMBER IT CANNOT ACCOUNT FOR IS THE EXACT DEFECT THIS
#    PHASE HAS SPENT TWELVE WAVES REMOVING.
# 2. ONE BOUND, PUBLISHED ONCE. `08345c3` re-derived 549 in the gate file and left 574 live in STATE.md,
#    ROADMAP.md and 01-SECURITY.md, while its commit message claimed "one bound, published once". Any
#    task touching the number enumerates EVERY surface carrying it and classifies each as LIVE (update)
#    or DATED HISTORY (keep), with the classification stated PER SITE. Plan 01-47 owns the four surfaces
#    outside this plan's `files_modified`; THIS plan owns only what it edits and says so.
# 3. WATCH EVERY GUARD FAIL BEFORE TRUSTING IT. A new byte-comparison or census guard must be OBSERVED
#    going RED on a real or planted violation and then restored, with the observed failure text recorded
#    verbatim in the SUMMARY. A guard nobody watched fail is decoration.
# 4. RESTORE PROOF. `cp` is aliased to `cp -i` in this environment and SILENTLY REFUSES to overwrite.
#    Restore with `/bin/cp -f` or `git checkout --`, then VERIFY the restore rather than assuming it, and
#    prove the tree clean with `git diff --exit-code -- packages/`.
# 5. DO NOT TOUCH CORE-11's CHECKBOX. `.planning/REQUIREMENTS.md:46` stays `- [ ]` and
#    `CORE11_BOX_EXPECTED` (currently `:11389`) stays `"- [ ] **CORE-11**"`. Verification pass 14 owns
#    that decision. If this plan's work convinces you the box is earned, SAY SO IN THE SUMMARY AND STOP;
#    do not flip it. The two change in ONE commit when they change.
# 6. DELETION BEATS CORRECTION. Plan 01-44's discipline, stated in this file at `:744-746`: "THEY ARE
#    DELETED RATHER THAN RE-DATED … A corrected sentence is still an AUTHORED bound standing beside a
#    DERIVED one, which is how seven consecutive waves each fixed a stale claim here and each acquired
#    the next." Prefer DELETING a false disclosure over rewording it, and prefer a DERIVED statement over
#    both. Every correction this plan authorises is a deletion or a generated span; there are no
#    hand-written replacement bounds in it.
# 7. BASELINE, RE-ESTABLISHED RATHER THAN INHERITED. Pass 13 measured `pnpm test` at 31 files / 1390
#    tests exit 0; `pnpm typecheck` exit 0; `pnpm lint` exit 0; `pnpm check:bundle` at one import
#    specifier, `crypto`; the gate spec alone at 442/442. Task 1 RE-RUNS all of it and writes the arrival
#    values to a baseline file; every later task reads that file rather than this comment.
#
# ---------------------------------------------------------------------------------------------------
# PROHIBITIONS CARRIED FORWARD FROM PLANS 01-44 AND 01-45, BYTE FOR BYTE, NOT INHERITED BY REFERENCE.
# ---------------------------------------------------------------------------------------------------
#   - NO AST, NO PARSER, NO TYPESCRIPT COMPILER API, NO FRAME-IDENTITY COMPUTATION anywhere in this
#     round's diff. No `from "typescript"`, no `ts.<Something>` in new lines.
#   - NO HAND-ROLLED CONTAINMENT: no `braceDepth`, `indentLevel`, `enclosingX`, `containmentOf`,
#     `frameOf`, `depth++`/`depth--` in new non-comment lines.
#   - DO NOT NARROW the five recognisers inside `constructAnchorFor`, the scan bounds, or the exemption
#     key format. This round adds a census BESIDE them and changes none of them.
#   - DO NOT RE-OPEN the 2026-08-25 re-scoped bar (DERIVED / DRIFT-DETECTABLE / SOLE BOUND).
#   - `auditSource` and every resolver body stay BYTE-IDENTICAL. This round changes no rule, no fixture
#     probe and no registry row's semantics.
#
# ---------------------------------------------------------------------------------------------------
# THE ROUND BASE IS NOT THIS PLAN'S STARTING POINT, AND CONFLATING THEM IS ITSELF AN INSTANCE OF THE CLASS.
# `1cb2884` is verification pass 13's tree and every LINE NUMBER below is measured against it. But the
# plan-authoring commit `4f351d0` sits between it and this plan's first edit, and that commit ALREADY
# changed `.planning/ROADMAP.md` — it added the wave 46/47 rows and rewrote the `**Plans**:` paragraph.
# So an OWNERSHIP assertion scoped to `1cb2884..HEAD` starts at one changed non-phase planning file before
# the executor touches anything, and a gate reading `-le 1` or `-eq 1` against it would abort on correct
# work while blaming a commit the executor did not make. Task 1 therefore captures `BASE_PLAN_START` — this
# plan's own starting commit — into the baseline file, every ownership gate diffs from THAT, and the
# pre-existing `ROADMAP.md` delta is NAMED in the gate's own bytes rather than absorbed. A gate that
# asserts a number it cannot account for is the exact defect twelve rounds have been removing; it does not
# stop being that defect because it lives in a verify block instead of a comment.
#
# EVERY LINE NUMBER IN THIS PLAN IS MEASURED AT `1cb2884`. RE-DERIVE ALL OF THEM BY TEXT BEFORE EDITING.
# Task 1 moves everything below `:6018` and everything below the BEGIN sentinel; Task 2 moves everything
# below `:600`. A plan that hands an executor a line number and an executor that trusts it is how
# `08345c3` produced CR-31 inside the commit that closed CR-29.
# ---------------------------------------------------------------------------------------------------
#
# THE ARITHMETIC THIS PLAN PREDICTS, STATED IN ADVANCE SO THE MEASUREMENT CAN REFUTE IT.
#   The widest anchor's raw span is `419..1549` (1,131 raw lines). Exclusion one — the machine-owned
#   derived span, `958..1539` — removes exactly 582 of them, leaving 549 SURFACE lines. That is the pin.
#   TASK 1 ADDS K LINES INSIDE THE DERIVED SPAN AND THE SHADOW MUST NOT MOVE. Growth inside exclusion one
#   raises the raw span end by K and the excluded count by K, and 549 is invariant under that. Task 1
#   therefore asserts the pin GREEN and asserts the header (lines 1 .. the BEGIN sentinel) has EXACTLY
#   957 lines before and after, so nothing it writes leaks above the sentinel.
#   TASK 2 DELETES 9 SURFACE LINES INSIDE THE SHADOW AND OUTSIDE EXCLUSION ONE — six at `:600-605` and
#   three at `:794-796`, every one of them between 419 and 958. PREDICTED: the shadow falls 549 -> 540
#   and the raw span end falls 1549 -> 1540, both by exactly 9. IF THE OBSERVED NUMBERS ARE ANYTHING
#   ELSE, STOP AND REPORT — something other than these nine lines moved, and fitting the pin to it is
#   forbidden by constraint 1.
#   TASK 3 CHANGES NO LINE INSIDE RAW 419..1549. It edits the pin's own comment block, roughly 8,600
#   lines below the shadow. It asserts the pin unchanged at 540 as a consequence.
#
# WHERE THE PINNED VALUE IS PUBLISHED, AND WHO OWNS EACH SITE. Constraint 2 is not satisfied by moving a
# constant. At `1cb2884` the gate file publishes the pinned value on EIGHT live lines and carries the
# superseded 574 on two DATED HISTORICAL ones, and four surfaces OUTSIDE this file carry it too. The
# ownership is split so that no commit ever ships the inconsistency `08345c3` shipped:
#   TASK 2 OWNS the live restatements OUTSIDE the pin's own comment block — at `1cb2884` those are
#   `:9072-9073` (the reach paragraph in the anchoring docblock), `:9363` (limit (5)'s restatement),
#   `:10616` (the construct-half explanation) and the pinned value inside the shipped failure string near
#   `:10632`. It moves the constant AND reconciles those in ONE commit.
#   TASK 3 OWNS the pin's own comment block, roughly the seventy lines above the declaration — which at
#   `1cb2884` carries the value at `:10154`, the span arithmetic at `:10188-10191` and CR-31's locator at
#   `:10192`. One block, one task: two tasks editing one comment block is how a correction loses its
#   attribution, and this phase's own STATE.md records that rule from wave 37.
#   THE TWO 574 SITES AT `:10160` AND `:10271` ARE DATED HISTORY AND STAY. Both name the pass that
#   measured them, both are past tense, and neither states a current bound. Pass 13 adjudicated both this
#   way and its reasoning is quoted in `01-VERIFICATION.md`. Rewriting them would destroy the record.
#   THE FOUR SURFACES OUTSIDE THIS FILE — `.planning/STATE.md:408`, `.planning/ROADMAP.md:239` and
#   `01-SECURITY.md`'s five lines — ARE PLAN 01-47's AND ARE NOT TOUCHED HERE. They are named in this
#   plan so the enumeration is complete and the split is deliberate rather than an omission.
#   THE END STATE, ASSERTED: after Task 3 the digit sequence 540 appears in the gate file EXACTLY ONCE,
#   on the `WIDEST_ANCHOR_SHADOW` declaration. The round's own history — that the value moved from 549
#   and why — belongs in the SUMMARY and the commit message, not in the file. That is plan 01-44's
#   deletion-only thesis and this file's own rule that a history is a record, not a bound.
#
# TWO TRAPS THAT WILL BITE AND ARE NAMED HERE SO THEY COST A MINUTE INSTEAD OF A CYCLE.
#   TRAP A — THE ENTRY-COUNT GUARD. `ENTRY_MARK` is the two characters `* ` and a shipped case asserts
#   that the number of generated lines starting with it equals `RESOLVER_REGISTRY.length`, in BOTH
#   surfaces. Any new generated line that begins with `* ` breaks that equality. Render the surface list
#   with a prefix that is not `* `.
#   TRAP B — THE DECLARED-PHRASING QUANTIFIER GUARD. It scans `SURFACE_LINES` for the phrasings in
#   `UNBOUNDED_QUANTIFIERS` and requires each occurrence outside the three exclusions to be a declared
#   `HEADER_QUANTIFIER_EXEMPTIONS` entry. New prose written into the scanned surface — including this
#   round's new census docblock and its reasons — CAN raise a new obligation. If it fires, the preferred
#   response is to REPHRASE, not to add an exemption; if an exemption is genuinely right, it is added
#   with a reason and named in the SUMMARY. Text rendered INSIDE the derived span is not scanned, because
#   exclusion one covers it.
autonomous: true
requirements: [CORE-11]

estimate:
  tokens: 82000
  raw_tokens: 82000
  tasks: 3
  confidence: low
# `estimate-calibration` reports `{"factor": 1, "applied": false, "sample_count": 0, "min_samples": 3}`,
# so `tokens` equals `raw_tokens` and `confidence` is DERIVED from the sample count, never self-rated.
# WEIGH THE MEASUREMENT COUNT OVER THE TOKEN FIGURE: two byte-comparison guards watched RED against a
# generator change and restored, one census watched RED naming CR-30's own two lines BEFORE they are
# deleted, one census watched RED again against a planted mention placed where the shadow pin stays GREEN
# in the same run, one pin re-derived against a 9-line attribution stated in advance, and one hand-written
# locator replaced by an emitted one.

must_haves:
  truths:
    - "THE SET OF BYTE-COMPARED SURFACES IS DERIVED, NOT AUTHORED. One frozen constant names the surfaces this suite reads and byte-compares; `GATE_FILE` and `LEDGER` resolve FROM it rather than beside it; and the same constant is rendered into the generated residual text, so the statement of the comparison's reach ships in both compared surfaces and is itself byte-compared."
    - "A SENTENCE NAMING AN UNCHECKED FILE AS MACHINE-CHECKED CANNOT SURVIVE A RUN OF THE HEADER CENSUS. Every line of the gate header that names a `.planning/` file is either inside the machine-owned span or a DECLARED entry carrying a one-clause reason; an undeclared mention names itself, its line number and its three dispositions and turns the suite red — and the census's own docblock discloses BOTH of its blind spots rather than one: a mechanism claim naming no file, and a mechanism claim naming a file outside the planning directory, which the path-prefix matcher does not see either. The disclosure is weaker than the guard, never stronger."
    - "CR-30 IS CLOSED BY DELETION IN ALL THREE OF ITS FALSE READINGS. The machine-check clause at `:600-605` and its restatement at `:794-796` are GONE — not re-dated, not reworded — and with them the `REQUIREMENTS.md`-is-untouched clause. The banner lines above each are byte-unchanged and now sit immediately above the paragraphs that followed the deleted text."
    - "BOTH BYTE-COMPARISON GUARDS WERE WATCHED FAILING AND THEN RESTORED. The generator change was made FIRST and both shipped spans observed DIVERGED, with the failure text recorded, before either was regenerated; the header census was observed RED naming ten undeclared mentions with the empty list, and RED again on a planted mention while the shadow pin stayed GREEN in the same run."
    - "THE PIN IS RE-DERIVED AGAINST AN ATTRIBUTION STATED BEFORE THE MEASUREMENT. 549 -> 540 and raw `419..1549` -> `419..1540`, both deltas exactly 9, attributed line by line to the six lines at `:600-605` and the three at `:794-796`; the identity pin `WIDEST_ANCHOR_TOKEN` is byte-unchanged and green throughout."
    - "ONE BOUND, PUBLISHED ONCE, AND THE ENUMERATION IS COMPLETE RATHER THAN CONVENIENT. Every site in the gate file publishing the pinned value is enumerated and classified LIVE or DATED HISTORY with the classification stated per site; every LIVE one is reconciled in the SAME commit that moves the constant; the two dated 574 sites are quoted and kept; the four surfaces outside this file are named as plan 01-47's rather than left unmentioned. At exit the digit sequence 540 appears in the gate file exactly once, on the declaration."
    - "CR-31's HAND-WRITTEN LOCATOR AND ARITHMETIC ARE GONE, AND THE PIN EMITS THEM INSTEAD. No line of the pin's comment block states the raw span, the raw count, the excluded count, the accepted opener's line number or the shipped-occurrence count; the pin's failure message computes and prints each of them."
    - "NOTHING SHIPPED CHANGED AND NOTHING LEAKED. No file under `packages/*/src` other than the gate spec, and no file under `scripts/`, is touched; `check:bundle` reports one import specifier, `crypto`; CORE-11's checkbox and `CORE11_BOX_EXPECTED` are byte-unchanged in both surfaces."
    - "THE ROUND'S BASELINE IS EXECUTED, NOT CARRIED. The whole suite, `tsc --build`, `lint`, `knip`, `build:backend`, `check:bundle`, the gate spec's own count, the module count and `SURFACE_LINES.length` are measured at arrival, written to a baseline file, and re-measured at exit; arrival is never confused with current."

  artifacts:
    - path: "packages/backend/src/outbound-prohibition.spec.ts"
      provides: "A single frozen declaration of the byte-compared surface set that `GATE_FILE`, `LEDGER` and the generator all read; a generated statement of the comparison's reach shipped in both compared surfaces; a census that makes every `.planning/` mention in the gate header a listed, reviewable entry; a pin whose span arithmetic and accepted-opener locator are emitted rather than written down."
      contains: "BYTE_COMPARED_SURFACES"
    - path: ".planning/REQUIREMENTS.md"
      provides: "The regenerated machine-owned residual span, now carrying the derived statement of which two surfaces the byte comparison reaches — so the ledger a reader trusts most stops being reachable by a hand-written mechanism claim."
      contains: "BEGIN DERIVED RESIDUAL"

  key_links:
    - from: "a header paragraph promising that a machine check compares the residual to `.planning/WINDOWS.md`"
      to: "a generated span, byte-compared in both surfaces, that names the two files the suite actually reads and states that no other file is reached"
      via: "one frozen constant read by `GATE_FILE`, `LEDGER` and `deriveResidual`, with the two false paragraphs deleted rather than corrected"
    - from: "a `.planning/` file mention in the gate header that nothing can see"
      to: "a DECLARED entry with a one-clause reason, or a red suite"
      via: "a header-scoped census bounded by the exported `DERIVED_BEGIN` sentinel, watched RED with an empty list against CR-30's own two lines"
    - from: "an accepted-opener line number hand-written into the pin's comment and falsified by the commit that moved it"
      to: "the same fact emitted by the pin's own computation at the moment it is measured"
      via: "deleting the comment's numbers and extending the pin's failure message, with no new assertion added"
---

<objective>
Make the set of surfaces this suite byte-compares a DERIVED fact instead of an authored one, make every
`.planning/` file mention in the gate header a LISTED one, delete the two paragraphs that promise a
machine check on a file nothing reads, and stop the pin's own comment hand-writing numbers the pin
computes.

Purpose: twelve rounds have each closed an instance of one class — prose in this file claiming more than
the code executes — and each acquired the next instance. CR-30 is the thirteenth and the first that runs
in the UNSAFE direction: it promises a guarantee that does not exist. Pass 13 named the only route that
closes the class rather than the instance, and this plan takes it: the suite already knows which two
files it reads, so render that set and byte-compare it, and census what remains so an unlisted claim
about a file cannot be written without turning the suite red.

Output: one gate-spec file in which the comparison's reach is generated from the same constant the
readers use and shipped into both compared surfaces; a regenerated ledger span carrying the same bytes;
a header census whose empty-list run named CR-30's own two lines before they were deleted; a pin
re-derived 549 -> 540 against an attribution written down before it was measured; and a pin comment with
no hand-written number left in it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md
@.planning/phases/01-skeleton-persistence-compatibility/01-SECURITY.md
@.planning/phases/01-skeleton-persistence-compatibility/01-PATTERNS.md
@.planning/phases/01-skeleton-persistence-compatibility/01-27-PLAN.md
@.planning/phases/01-skeleton-persistence-compatibility/01-27-SUMMARY.md
@.planning/phases/01-skeleton-persistence-compatibility/01-45-SUMMARY.md
@packages/backend/src/outbound-prohibition.spec.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: CR-30's mechanism half — the byte-compared surface set becomes one frozen constant, rendered into the generated residual and shipped into both compared surfaces, with both byte-comparison guards watched RED before either span is regenerated</name>

  <files>packages/backend/src/outbound-prohibition.spec.ts, .planning/REQUIREMENTS.md</files>

  <precondition>`git status --porcelain .planning/ packages/` is empty at arrival, `01-45-SUMMARY.md` exists, and `HEAD` is at or after `1cb2884` (verification pass 13). If the tree is dirty at arrival, commit the arrival state as its own housekeeping commit BEFORE the first edit and name it in the SUMMARY — plan 01-42 recorded that exact situation as its finding F-0. THEN, AFTER any such housekeeping commit and BEFORE the first edit of this plan, capture `BASE_PLAN_START=$(git rev-parse HEAD)` into the round-12 baseline file alongside the other arrival values. EVERY OWNERSHIP ASSERTION IN THIS PLAN DIFFS FROM THAT COMMIT RATHER THAN FROM `1cb2884`, AND THE REASON IS THE ONE THIS ROUND EXISTS TO REMOVE: the round base is NOT this plan's starting point, because the plan-authoring commit `4f351d0` ALREADY changed `.planning/ROADMAP.md` (it added the wave 46/47 rows and rewrote the `**Plans**:` paragraph). A `1cb2884`-scoped ownership count therefore starts at ONE, not zero, and a gate asserting `-le 1` or `-eq 1` against it would either abort on correct work or credit the executor with a commit it did not make. That is a gate asserting a number it cannot account for, one level out from the defect this plan closes. Measured at `4f351d0`: `git diff --name-only 1cb2884..HEAD -- .planning/ | grep -v phases/` returns exactly `.planning/ROADMAP.md` and nothing else — the verify below re-derives that and refuses to proceed if anything ELSE pre-exists.</precondition>

  <read_first>
    - `packages/backend/src/outbound-prohibition.spec.ts` at `:5995-6030` (`DERIVED_MARK`, `DERIVED_BEGIN`, `DERIVED_END`, `ENTRY_MARK`, `formatEntry`), `:6055-6130` (`deriveResidual` and its docblock), `:6145-6250` (`extractDerivedBlock`, `readPlanningLedger` and their docblocks), `:9561-9600` (the `describe` opener, `GATE_FILE`, the non-vacuity cases), `:10020-10105` (`EXCLUSIONS`, the sentinel endpoint pins, `SURFACE_LINES`), `:11360-11380` (`LEDGER`), `:11415-11477` (both byte-comparison cases and both entry-count cases).
    - `.planning/REQUIREMENTS.md` at `:44-52` and `:195-205` and `:775-785` — CORE-11's row, the BEGIN sentinel inside its entry, and the END sentinel.
    - `01-27-SUMMARY.md` in full. It is the record of the round that installed this generator; this task extends that machinery and must not invent a second one.
  </read_first>

  <behavior>
    - With `BYTE_COMPARED_SURFACES` declared and `deriveResidual` taking it, the generated text carries a section naming each surface in the set, one line per surface, on a prefix that is NOT `ENTRY_MARK`, followed by a categorical statement that no other file in the repository is reached by this comparison.
    - `deriveResidual` stays PURE: its output is a function of its two arguments alone — no file read, no environment, no date, no ordering that depends on anything else. Called twice with the same arguments it returns byte-identical text.
    - Before either shipped span is regenerated, BOTH byte-comparison cases FAIL and their messages print the full expected text.
    - After both spans are regenerated, the whole suite is green, both entry-count cases still balance at `RESOLVER_REGISTRY.length`, and both sentinel full-line-uniqueness assertions still resolve to exactly one line.
    - The number of lines in the gate file from line 1 up to and including the `DERIVED_BEGIN` line is 957 + 1 before this task and 957 + 1 after it. Nothing this task writes lands above the sentinel except in-place rewrites of the same number of lines.
    - `WIDEST_ANCHOR_SHADOW` is untouched and its case is green at 549 both before and after.
  </behavior>

  <action>
Declare one frozen, module-scope, exported constant naming the surfaces this suite reads and
byte-compares. Its element order is the order the two comparison cases run in: the gate spec itself
first, `.planning/REQUIREMENTS.md` second. Give it a docblock that says, in this file's voice, that it is
the SINGLE definition of the comparison's reach and that `GATE_FILE`, `LEDGER` and the generator all read
it so they cannot disagree — and that says what it does NOT establish, namely that it is a statement about
which files are COMPARED and not a claim that any other file is correct.

Make `GATE_FILE` inside the `describe` at `:9561` and `LEDGER` at `:11367` resolve FROM that constant by
index rather than repeat the path literal. Both keep their existing names and their existing comments
about the repo-root-relative path convention; only their right-hand sides change. If a path literal
survives anywhere else as a second definition of the same fact, that is the drift this task exists to
remove — remove it.

Give `deriveResidual` a SECOND PARAMETER carrying that surface set, and pass the constant at both call
sites. Take the parameter rather than reading the module constant from inside the function body, and take
it for a stated reason that belongs in the docblock: the BEGIN sentinel's own text names the call a
hand-editor must re-run, so a call that grew an input while the sentinel still named one would BE the
next false disclosure in this file. Update `DERIVED_BEGIN` so the sentinel names the two-argument call.
Amend `deriveResidual`'s existing purity paragraph so it says the output is a function of ITS ARGUMENTS
rather than of the registry's order alone — that paragraph is currently true and this change would make
it false, which is the exact defect being closed.

Render into the generated text, ABOVE the `RESOLVERS -` line and below the opening paragraph that already
describes the byte comparison, a short section that states how many surfaces the comparison reaches, lists
each one on its own line, and then states categorically that no other file in the repository is reached by
it — that every other file, including every file under the planning directory that is not listed, carries
at most a pointer and is checked by nothing, and that a claim elsewhere that such a file is checked is
false. Author that sentence ONCE, in the generator, where it is rendered from the set rather than asserted
beside it. TRAP A APPLIES: do not begin any of those lines with the two characters `ENTRY_MARK` holds, or
the entry-count equality in both surfaces will break. Keep the generator's mechanical voice — its docblock
forbids smoothing it into prose and states why.

RUN THE SUITE NOW, BEFORE REGENERATING EITHER SPAN. Both byte-comparison cases must fail. Record both
failure titles and the full expected text each prints. This is the RED observation for the guard that
carries this task's whole claim, and it is a real divergence rather than a planted one. If EITHER case
passes at this point, stop: the generator change did not reach the compared text and the rest of this
task would ship a span nothing compares.

Then paste each failure message's expected block between the sentinels in its own surface — the gate
header's `/* */` block and CORE-11's entry in `.planning/REQUIREMENTS.md` — and re-run to green. Update
the sentinel line in `.planning/REQUIREMENTS.md` too; it is wrapped in an HTML comment there and in a bare
line here, and the non-vacuity case that asserts the ledger contains the BEGIN sentinel is what catches a
miss.

Three prose sites in the gate header name the one-argument call in ordinary text — inside the paragraph
above the sentinels and inside the wave-27 surface-decision note. Rewrite each to the two-argument
spelling IN PLACE AND LINE-COUNT NEUTRAL: rewrap within the same number of lines. These three sites sit
between raw 419 and the sentinel, inside the widest anchor's shadow and outside exclusion one, so a line
added or removed there moves the pin — and constraint 1 forbids moving the pin for anything this task
cannot attribute. Assert the header length before and after.

Do not touch `auditSource`, any resolver, any registry row, any fixture probe, `WIDEST_ANCHOR_SHADOW`,
`WIDEST_ANCHOR_TOKEN`, `CORE11_BOX_EXPECTED`, or CORE-11's checkbox row. Do not add an exclusion. Do not
delete anything in this task — the deletions are Task 2's and splitting them apart is what keeps each
task's line-count delta attributable.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && SP=packages/backend/src/outbound-prohibition.spec.ts; LG=.planning/REQUIREMENTS.md; TD="${TMPDIR:-/tmp}"; B="$TD/defminer-round12-baseline"; [ -f "$B" ] || { echo "MISSING ROUND-12 BASELINE FILE — TASK 1 MUST WRITE IT AT ARRIVAL BEFORE THE FIRST EDIT"; exit 1; }; . "$B"; for V in BASE_GATE_ARRIVAL BASE_TESTS_ARRIVAL BASE_FILES BASE_MODULES BASE_SURFACE_LINES BASE_PLAN_START; do eval "X=\${$V:-}"; [ -n "$X" ] || { echo "BASELINE FILE INCOMPLETE: $V IS EMPTY, AND AN EMPTY EXPECTED VALUE MAKES EVERY COMPARISON BELOW VACUOUS"; exit 1; }; done; RB=$(git rev-parse 1cb2884) || { echo "ROUND BASE UNRESOLVABLE"; exit 1; }; [ -n "$RB" ] || { echo "BASE REV EMPTY — git show WOULD READ THE INDEX, WHICH EQUALS THE WORKTREE, SO EVERY BODY DIFF BELOW WOULD BE 0"; exit 1; }; git merge-base --is-ancestor "$RB" HEAD || { echo "1cb2884 IS NOT AN ANCESTOR OF HEAD"; exit 1; }; pnpm exec vitest run "$SP" > "$TD/r12.gate1.log" 2>&1; GRC=$?; cat "$TD/r12.gate1.log"; [ "$GRC" -eq 0 ] || { echo "GATE SUITE RED (rc=$GRC) — A COUNT PARSED OUT OF A RED RUN IS THE DEFECT: THE PARENTHESISED TOTAL RISES WHEN A NEW CASE FAILS"; exit 1; }; GT=$(grep -E '^[[:space:]]*Tests[[:space:]]' "$TD/r12.gate1.log" | tail -1 | sed -n 's/.*[^0-9]\([0-9][0-9]*\) passed.*/\1/p'); [ -n "$GT" ] || { echo "GATE PASSED-COUNT UNPARSEABLE"; exit 1; }; [ "$GT" -ge "$BASE_GATE_ARRIVAL" ] || { echo "GATE COUNT $GT BELOW ARRIVAL $BASE_GATE_ARRIVAL — A CASE WAS LOST"; exit 1; }; echo "GATE-PASSED=$GT ARRIVAL=$BASE_GATE_ARRIVAL"; pnpm test > "$TD/r12.full1.log" 2>&1; FRC=$?; cat "$TD/r12.full1.log"; [ "$FRC" -eq 0 ] || { echo "WHOLE SUITE RED (rc=$FRC)"; exit 1; }; FT=$(grep -E '^[[:space:]]*Tests[[:space:]]' "$TD/r12.full1.log" | tail -1 | sed -n 's/.*[^0-9]\([0-9][0-9]*\) passed.*/\1/p'); FF=$(grep -E '^[[:space:]]*Test Files[[:space:]]' "$TD/r12.full1.log" | tail -1 | sed -n 's/.*[^0-9]\([0-9][0-9]*\) passed.*/\1/p'); [ -n "$FT" ] && [ -n "$FF" ] || { echo "SUITE COUNTS UNPARSEABLE"; exit 1; }; [ "$FT" -ge "$BASE_TESTS_ARRIVAL" ] || { echo "SUITE COUNT $FT BELOW ARRIVAL $BASE_TESTS_ARRIVAL"; exit 1; }; [ "$FF" -eq "$BASE_FILES" ] || { echo "FILE COUNT $FF != $BASE_FILES"; exit 1; }; pnpm exec tsc --build || { echo "tsc --build RED"; exit 1; }; pnpm typecheck || { echo "typecheck RED"; exit 1; }; pnpm lint || { echo "lint RED"; exit 1; }; pnpm knip || { echo "knip RED"; exit 1; }; pnpm build:backend || { echo "build:backend RED"; exit 1; }; pnpm check:bundle || { echo "check:bundle RED"; exit 1; }; M=$(find packages/*/src -name '*.ts' ! -name '*.spec.ts' | grep -c . || true); [ "$M" -eq "$BASE_MODULES" ] || { echo "MODULE COUNT $M != $BASE_MODULES"; exit 1; }; SD=$(grep -c '^export const BYTE_COMPARED_SURFACES' "$SP" || true); [ "$SD" -eq 1 ] || { echo "BYTE_COMPARED_SURFACES IS DECLARED $SD TIME(S), EXPECTED EXACTLY 1 — AT 0 NOTHING WAS ADDED AND ABOVE 1 THERE ARE TWO DEFINITIONS OF THE ONE FACT THIS TASK EXISTS TO SINGULARISE"; exit 1; }; SU=$(grep -c 'BYTE_COMPARED_SURFACES' "$SP" || true); [ "$SU" -ge 5 ] || { echo "BYTE_COMPARED_SURFACES IS REFERENCED $SU TIME(S), EXPECTED AT LEAST 5 (DECLARATION, GATE_FILE, LEDGER, THE GENERATOR PARAMETER AND BOTH CALL SITES) — THE CONSTANT EXISTS BUT NOTHING READS IT"; exit 1; }; PL=$(grep -c '"packages/backend/src/outbound-prohibition.spec.ts"' "$SP" || true); [ "$PL" -eq 1 ] || { echo "THE GATE PATH LITERAL APPEARS AS A STRING $PL TIME(S), EXPECTED EXACTLY 1 (INSIDE BYTE_COMPARED_SURFACES) — A SECOND LITERAL IS A SECOND DEFINITION"; exit 1; }; LL=$(grep -c '"\.planning/REQUIREMENTS\.md"' "$SP" || true); [ "$LL" -eq 1 ] || { echo "THE LEDGER PATH LITERAL APPEARS AS A STRING $LL TIME(S), EXPECTED EXACTLY 1 (INSIDE BYTE_COMPARED_SURFACES)"; exit 1; }; CF=$(grep -o 'deriveResidual(RESOLVER_REGISTRY[^)]*)' "$SP" | sort -u); CFN=$(printf '%s\n' "$CF" | grep -c . || true); [ "$CFN" -eq 1 ] || { echo "THE GENERATOR IS CALLED IN $CFN DISTINCT FORMS AND EXACTLY 1 IS EXPECTED — A SURVIVING ONE-ARGUMENT SPELLING IS A STALE MECHANISM CLAIM IN THE SHAPE CR-31 HAS ALREADY SHIPPED ONCE. FORMS FOUND: $CF"; exit 1; }; printf '%s\n' "$CF" | grep -q 'BYTE_COMPARED_SURFACES' || { echo "THE SINGLE CALL FORM DOES NOT PASS THE SURFACE SET: $CF"; exit 1; }; echo "CALL-FORM-SINGULAR $CF"; SBN=$(grep -n '^BEGIN DERIVED RESIDUAL' "$SP" | head -1 | cut -d: -f1); [ -n "$SBN" ] || { echo "THE BEGIN SENTINEL IS NOT A BARE FULL LINE IN THE GATE FILE — EXCLUSION ONE'S ENDPOINT PIN LOCATES IT BY FULL-LINE EQUALITY AND WOULD RESOLVE NOTHING"; exit 1; }; [ "$SBN" -eq 958 ] || { echo "THE GATE HEADER IS NOW $((SBN-1)) LINES AND WAS 957 — TASK 1 IS LINE-COUNT NEUTRAL ABOVE THE SENTINEL BY CONSTRUCTION, SO A DIFFERENCE MEANS SOMETHING LANDED IN THE WIDEST ANCHOR'S SHADOW AND THE PIN CANNOT BE TRUSTED WITHOUT AN ATTRIBUTION"; exit 1; }; echo "HEADER-LENGTH-UNCHANGED sentinel_line=$SBN"; SBG=$(grep -c 'BYTE_COMPARED_SURFACES' "$LG" || true); [ "$SBG" -eq 0 ] || { echo "THE LEDGER CARRIES THE CONSTANT NAME $SBG TIME(S); THE GENERATED TEXT MUST NAME PATHS, NOT THE IDENTIFIER"; exit 1; }; LGB=$(grep -c 'BEGIN DERIVED RESIDUAL' "$LG" || true); [ "$LGB" -eq 1 ] || { echo "THE LEDGER CARRIES $LGB BEGIN SENTINEL(S), EXPECTED 1"; exit 1; }; EM=$(grep -c '^const ENTRY_MARK = "\* ";' "$SP" || true); [ "$EM" -eq 1 ] || { echo "ENTRY_MARK WAS CHANGED OR REMOVED ($EM) — THE ENTRY-COUNT EQUALITY IN BOTH SURFACES IS DEFINED AGAINST IT AND THIS TASK MUST NOT MOVE IT"; exit 1; }; SL=$(grep -c 'WIDEST_ANCHOR_SHADOW = 549' "$SP" || true); [ "$SL" -eq 1 ] || { echo "THE SHADOW PIN IS NOT STILL 549 ($SL) — TASK 1 IS INVARIANT OVER IT BY CONSTRUCTION AND TASK 2 OWNS ITS RE-DERIVATION"; exit 1; }; CBX=$(grep -c 'CORE11_BOX_EXPECTED = "- \[ \] \*\*CORE-11\*\*"' "$SP" || true); [ "$CBX" -eq 1 ] || { echo "CORE11_BOX_EXPECTED IS NOT THE UNCHECKED FORM: $CBX"; exit 1; }; BOXROW=$(grep -c '^- \[ \] \*\*CORE-11\*\*' "$LG" || true); [ "$BOXROW" -eq 1 ] || { echo "CORE-11's CHECKBOX ROW IS NOT THE UNCHECKED FORM IN THE LEDGER: $BOXROW"; exit 1; }; D=$(git diff "$RB"..HEAD -- "$SP"); AST=$(printf '%s\n' "$D" | grep -cE '^[+-].*(from "typescript"|[^A-Za-z]ts\.[A-Za-z])' || true); [ "$AST" -eq 0 ] || { echo "COMPILER-API LINES IN ROUND DIFF: $AST"; exit 1; }; HAND=$(printf '%s\n' "$D" | grep '^+' | grep -vE '^\+[[:space:]]*(//|\*|/\*)' | grep -cE '(braceDepth|braceLevel|indentOf|indentLevel|enclosing[A-Z]|Enclosing|containmentOf|frameOf|depth\+\+|depth--)' || true); [ "$HAND" -eq 0 ] || { echo "HAND-ROLLED CONTAINMENT SPELLING IN ROUND DIFF: $HAND"; exit 1; }; AS='BEGIN{s=0} !s && /^export function auditSource\(/ {s=1; print; next} s && /^\}$/ {print; exit} s {print}'; git show "$RB":"$SP" | awk "$AS" > "$TD/r12.base.auditSource"; awk "$AS" "$SP" > "$TD/r12.work.auditSource"; ABL=$(grep -c . "$TD/r12.base.auditSource" || true); [ "$ABL" -gt 100 ] || { echo "THE auditSource EXTRACTOR COLLAPSED ON THE BASE REVISION ($ABL LINES) AND IT COLLAPSES BOTH SIDES EQUALLY, SO THE DIFF BELOW WOULD BE 0 HAVING COMPARED NOTHING"; exit 1; }; AD=$(diff "$TD/r12.base.auditSource" "$TD/r12.work.auditSource" | grep -c . || true); [ "$AD" -eq 0 ] || { echo "auditSource CHANGED: $AD DIFF LINES"; exit 1; }; echo "auditSource-BYTE-IDENTICAL lines=$ABL"; A=$(git diff --name-only "$RB"..HEAD -- packages/ scripts/ | grep -v '\.spec\.ts$' | grep -c . || true); [ "$A" -eq 0 ] || { echo "ROUND TOUCHED SHIPPED SOURCE: $A FILE(S)"; exit 1; }; git merge-base --is-ancestor "$RB" "$BASE_PLAN_START" || { echo "BASE_PLAN_START ($BASE_PLAN_START) IS NOT AT OR AFTER THE ROUND BASE — THE BASELINE FILE IS STALE FROM AN EARLIER ATTEMPT AND EVERY OWNERSHIP DIFF BELOW WOULD BE SCOPED TO THE WRONG TREE"; exit 1; }; git merge-base --is-ancestor "$BASE_PLAN_START" HEAD || { echo "BASE_PLAN_START ($BASE_PLAN_START) IS NOT AN ANCESTOR OF HEAD — THE OWNERSHIP DIFF BELOW WOULD COMPARE UNRELATED HISTORIES"; exit 1; }; PRE=$(git diff --name-only "$RB".."$BASE_PLAN_START" -- .planning/ | grep -v 'phases/' | sort -u); PREN=$(printf '%s\n' "$PRE" | grep -c . || true); echo "PRE-EXISTING-NON-PHASE-PLANNING-DELTA n=$PREN files=$(printf '%s ' $PRE) — THESE BELONG TO THE PLAN-AUTHORING COMMIT, NOT TO THIS EXECUTOR, AND ARE EXCLUDED FROM THE OWNERSHIP COUNT BELOW BY SCOPING IT TO THIS PLAN'S OWN COMMITS"; BAD=$(printf '%s\n' "$PRE" | grep -v '^$' | grep -v '^\.planning/ROADMAP\.md$' | grep -c . || true); [ "$BAD" -eq 0 ] || { echo "AN UNACCOUNTED NON-PHASE PLANNING FILE CHANGED BEFORE THIS PLAN STARTED: $(printf '%s ' $PRE). ONLY .planning/ROADMAP.md IS ACCOUNTED FOR — THE PLAN-AUTHORING COMMIT ADDED THE WAVE 46/47 ROWS AND REWROTE THE Plans PARAGRAPH. ANYTHING ELSE IS A CHANGE NOBODY HAS ATTRIBUTED; REPORT IT RATHER THAN PROCEEDING"; exit 1; }; LGD=$(git diff --name-only "$BASE_PLAN_START"..HEAD -- .planning/ | grep -v 'phases/' | grep -c . || true); [ "$LGD" -le 1 ] || { echo "THIS PLAN OWNS EXACTLY ONE NON-PHASE PLANNING FILE AND HAS CHANGED $LGD SINCE ITS OWN STARTING COMMIT $BASE_PLAN_START: $(git diff --name-only "$BASE_PLAN_START"..HEAD -- .planning/ | grep -v 'phases/' | tr '\n' ' '). THE DIFF IS SCOPED TO THIS PLAN'S OWN COMMITS RATHER THAN TO 1cb2884 ON PURPOSE: THE PLAN-AUTHORING COMMIT ALREADY CHANGED ROADMAP.md, SO A ROUND-BASE-SCOPED COUNT WOULD START AT ONE AND MISATTRIBUTE IT. STATE.md, ROADMAP.md AND WINDOWS.md ARE PLAN 01-47's"; exit 1; }; N=$(git status --porcelain packages/ | grep -c . || true); [ "$N" -eq 0 ] || { echo "PACKAGES DIRTY AFTER COMMIT: $N"; exit 1; }; git diff --exit-code -- packages/ && echo TASK1-TREE-CLEAN</automated>
  </verify>

  <acceptance_criteria>
    - The precondition asserted and PASTED, and the round-12 baseline file written at arrival with `BASE_GATE_ARRIVAL`, `BASE_TESTS_ARRIVAL`, `BASE_FILES`, `BASE_MODULES`, `BASE_SURFACE_LINES` and `BASE_PLAN_START` measured in THIS session rather than copied from `01-VERIFICATION.md`. Any difference from pass 13's figures named and explained before proceeding.
    - THE PRE-EXISTING PLANNING DELTA NAMED RATHER THAN ABSORBED: `git diff --name-only 1cb2884..$BASE_PLAN_START -- .planning/ | grep -v phases/` pasted, with an explicit statement that `.planning/ROADMAP.md` in it belongs to the plan-authoring commit (the wave 46/47 rows) and NOT to this executor, and that every ownership assertion in this plan is therefore scoped to `$BASE_PLAN_START..HEAD`. If anything other than `ROADMAP.md` appears there, the task STOPPED and that is the finding.
    - `BYTE_COMPARED_SURFACES` shown by diff to be the ONLY place either path exists as a string literal, with `GATE_FILE`, `LEDGER` and both generator call sites shown reading it.
    - THE RED OBSERVATION, WHICH IS THIS TASK'S WHOLE CLAIM: both byte-comparison cases quoted failing BEFORE either span was regenerated — case titles verbatim, and enough of each expected block to show it carries the new surfaces section. An explicit statement of what was run, in what order, and that neither span was touched before the run.
    - The surfaces section shown rendered from the constant and NOT hand-typed into either span: the diff of the generator quoted, and the two shipped spans shown to be pastes of the failure messages' expected text.
    - TRAP A discharged by measurement, not by intent: both entry-count cases quoted green after regeneration, with the counted value and `RESOLVER_REGISTRY.length` both stated.
    - TRAP B addressed explicitly: whether the declared-phrasing guard fired on anything this task wrote, and if it did, whether the response was a rephrase or an exemption, with the entry and its reason quoted if one was added.
    - Header length asserted 957 before and after, with the three in-place call-form rewrites shown line-count neutral by diff. The shadow pin quoted GREEN at 549 in the same run, and the identity pin quoted green.
    - `deriveResidual`'s purity paragraph shown amended to say "its arguments", and the BEGIN sentinel shown naming the two-argument call in BOTH surfaces.
    - CORE-11's checkbox and `CORE11_BOX_EXPECTED` shown byte-unchanged, by diff, in both files.
  </acceptance_criteria>

  <done>
The suite reads one frozen declaration of which surfaces it byte-compares; `GATE_FILE`, `LEDGER` and the
generator all resolve from it; the generated residual carries a rendered statement of that reach and a
categorical denial that any other file is covered; both shipped spans were observed DIVERGED before they
were regenerated and are byte-identical to the generator afterwards; the gate header is still 957 lines;
the shadow pin is still 549 and green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CR-30's disclosure half — a header census that makes every `.planning/` mention a listed entry, watched RED with an empty list naming CR-30's own two lines, then those nine lines deleted and the shadow pin re-derived 549 -> 540 against an attribution written down first</name>

  <files>packages/backend/src/outbound-prohibition.spec.ts</files>

  <precondition>Task 1 is committed, the suite is green, and the gate header is 957 lines. Re-derive every line number in this task by TEXT: Task 1 moved everything below the `DERIVED_BEGIN` declaration and everything below the sentinel, so the numbers written here are the `1cb2884` measurements and are already stale.</precondition>

  <read_first>
    - The three existing exemption-list mechanisms this census is modelled on, so it copies a shape rather than inventing one: `RESOLVER_EXEMPTIONS` and its docblock (near `:6240-6300`), and `HEADER_QUANTIFIER_EXEMPTIONS` with the four cases that discharge it — the undeclared-occurrence case with its three-choices message, the stale-entry half, the count equality, and the per-entry reason check (near `:9093-9230` and `:10529-10660`).
    - The two CR-30 sites and their banner lines: locate the banner reading `THE FINAL RESIDUAL, AFTER PLAN 01-25` and read the eight lines below it; locate the banner reading `THE NARROWING, AFTER PLAN 01-26` and read the five lines below it.
    - The eight surviving header mentions, each read in full before a reason is written for it. At `1cb2884` they are `:19`, `:751`, `:785`, `:908`, `:912`, `:935`, `:937`, `:954`. Re-locate them.
    - The pin case and its comment block near `:10145-10260`.
  </read_first>

  <behavior>
    - With the census installed and its declaration list EMPTY, the suite is RED and the failure names TEN header lines by number and text, two of which are CR-30's own `:601` and `:794`.
    - With CR-30's nine lines deleted and the remaining eight declared, the census is GREEN and its count equality balances at eight.
    - A declared entry matching nothing in the header turns the suite RED as a STALE ENTRY, with a message distinct from the undeclared-mention message.
    - Two declared entries carrying identical line text turn the suite RED rather than silently collapsing into one.
    - A declared entry whose reason is empty or trivially short turns the suite RED.
    - A planted `.planning/` mention added ABOVE raw line 419 turns the census RED while the shadow pin stays GREEN in the same run.
    - After the deletions the shadow pin reports 540 over raw span `419..1540` and the identity pin is byte-unchanged.
  </behavior>

  <action>
Add a census over the GATE HEADER — the lines from 1 up to but not including the line the exported
`DERIVED_BEGIN` constant matches by full-line equality. Use that exported constant as the locator, not a
prefix matcher and not a line number; WR-62 is the finding that a prefix locator on this same boundary
slid silently and no endpoint pin fired.

The census matches a header line if the line contains the literal planning-directory path prefix. The KEY
is the trimmed raw line — no normalization, no masking, no new key format. Constraint: this file forbids
inventing a narrowing, and a key format is a narrowing.

Declare the list as a FROZEN ARRAY of records, each carrying the line text and a one-clause reason. Do NOT
use a plain object keyed by line text: a record silently overwrites a duplicate key, and a list that can
lose an entry without saying so is the vacuity hole this file's own discipline exists to close. Assert the
absence of duplicates as its own case with its own message.

Give the census five discharges, each with its own message, modelled on the quantifier guard's:
every matched header line is DECLARED — and the failure names each undeclared line by number and text and
states the three dispositions in the order this file prefers them, DELETE the sentence if it states a
mechanism the code does not have, MAKE IT DERIVED if the fact is one the suite already computes, or DECLARE
it with a reason if the mention is genuinely not a mechanism claim; every declared entry MATCHES something,
so a stale entry is loud and its message says it is stale rather than undeclared; the counts BALANCE, so a
line mentioned twice cannot be discharged by one entry; no two entries carry identical text; and every
entry's reason is present and long enough to be a reason. Put non-vacuity BEFORE the rules, as
`tests/pins.spec.ts` and every case in this file do: assert the header region is non-empty and that the
scan matched at least one line, because a census that matched nothing passes having measured nothing.

Give the census a docblock that states ITS OWN REACH AND WHAT IT DOES NOT REACH, on the same terms points
5(b) and 5(c) of the derived text use: it reaches the header and no further, the prose below the registry
is not scanned by it; it matches a PATH PREFIX and nothing else, so a mechanism claim naming no file is
invisible to it, AND IT MATCHES ONLY PATHS UNDER THE PLANNING DIRECTORY, SO A MECHANISM CLAIM ABOUT A FILE
ELSEWHERE IN THE REPOSITORY IS INVISIBLE TO IT TOO — a sentence claiming this suite reads a file under the
backend package or under the scripts directory NAMES A FILE and still passes the census unseen; and it does
not adjudicate whether a declared reason is a GOOD one — it makes the mention
listed and reviewable, which is what converts an invisible claim into one a reader can disagree with. Say
that plainly. A census whose disclosure overstates it would be this file's defect installed by the guard
built to stop it.

RUN THE SUITE NOW WITH THE LIST EMPTY. It must be RED and it must name ten lines including the two CR-30
lives on. Record the full failure message verbatim. This is the RED observation, taken against the real
defect rather than a planted one, and it is the evidence that the guard would have caught CR-30 the day it
was written.

Then DELETE the two clauses. The first is the six lines that sit between the `THE FINAL RESIDUAL, AFTER
PLAN 01-25` banner and the paragraph beginning `WIDENED 2026-08-24 (WR-27)`; after the deletion those two
lines are adjacent. The second is the three lines between the `THE NARROWING, AFTER PLAN 01-26` banner and
the paragraph beginning `THE NARROWING AFTER WAVE 26`; after the deletion those two lines are adjacent.
DELETE, do not re-date and do not reword — a corrected mechanism sentence is still an authored mechanism
claim standing beside the derived one Task 1 installed, and this file's own words at the wave-33
disposition say so. Both banners stay byte-unchanged.

Then declare the eight surviving mentions. READ EACH LINE BEFORE WRITING ITS REASON and write the reason
from what the line says, not from what this plan says about it. Handing an executor pre-authored reasons it
did not verify is precisely how five consecutive rounds shipped a bound nobody re-derived.

Now re-derive the pin, and state the attribution BEFORE reading the number. Nine surface lines were removed
from inside the widest anchor's raw span and above exclusion one's start, so the predicted result is 549 −
9 = 540 with the raw span end falling 1549 → 1540. Run the gate spec, quote the pin's failure message in
full, and check it against that prediction. IF EITHER NUMBER DIFFERS, STOP AND REPORT RATHER THAN FITTING
THE PIN — an unattributable bump is not authorised and the pin's own message says so. Only then set
`WIDEST_ANCHOR_SHADOW` to the measured value. Leave `WIDEST_ANCHOR_TOKEN` alone and quote it green.

Finally, plant one line containing a planning-directory path ABOVE raw line 419 — above the widest anchor's
span start on purpose, so the census fires and the shadow pin does not, which discriminates the two guards
in one run. Quote both: the census's failure naming the planted line, and the shadow pin's tick in the SAME
run. Restore with `git checkout --` and VERIFY the restore rather than assuming it; `cp` is aliased to
`cp -i` here and refuses silently. Prove the tree clean by diff.

NOW DISCHARGE CONSTRAINT 2 IN THE SAME COMMIT THAT MOVES THE CONSTANT. Moving a pin and leaving its
restatements live is precisely what `08345c3` did while its commit message claimed "one bound, published
once", and pass 13 filed CR-32 for it. Enumerate EVERY line of this file carrying the digit sequence of
the old value or the new one, and every line carrying the superseded 574, by grep with line numbers. For
each, state LIVE or DATED HISTORY and why: LIVE means the line states the file's CURRENT geometry in the
present tense; DATED HISTORY means it names the pass or wave that measured it, is past tense, and states
no current bound. Write the classification per site into the SUMMARY as a table — the classification is
the deliverable, not a step towards one.

Reconcile every LIVE site OUTSIDE the pin's own comment block in this task. At `1cb2884` those are the
reach paragraph in the anchoring docblock, limit (5)'s restatement, the construct-half explanation, and
the value inside a shipped failure string. The preferred disposition for each is DELETION OF THE NUMBER:
each of them restates a bound the constant already publishes, and a sentence that names the reach without
naming a figure is true forever. Where a site genuinely needs the value, make it read the constant by
name rather than spell the digits. The two 574 sites are DATED HISTORY, both adjudicated that way by pass
13 for reasons quoted in `01-VERIFICATION.md`; quote them, classify them, and leave them byte-unchanged.

DO NOT TOUCH THE PIN'S OWN COMMENT BLOCK IN THIS TASK BEYOND THE CONSTANT ITSELF — roughly the seventy
lines above the declaration are Task 3's, and two tasks editing one comment block is how a correction
loses its attribution. THAT LEAVES THAT ONE BLOCK CARRYING THE SUPERSEDED FIGURE FOR EXACTLY ONE COMMIT,
AND THAT IS DELIBERATE RATHER THAN AN OVERSIGHT. Say so in the commit message and name Task 3 as its
owner, so a reader of the history sees a scheduled hand-off rather than the omission `08345c3` shipped.

Do not write this round's own history into the file — not the old value, not the delta, not the wave that
moved it. The verify asserts the pinned value appears nowhere outside that block, and a comment recording
the move would carry the new value a second time and be the fourteenth instance of the class this round is
closing, installed by the task closing the thirteenth. The record goes in the SUMMARY and the commit
message. DO NOT TOUCH `.planning/STATE.md`, `.planning/ROADMAP.md` or `01-SECURITY.md`;
those four surfaces are plan 01-47's, they are named in this plan's frontmatter so the enumeration is
complete, and reaching them from here would split one finding across two plans in the other direction.

Every deletion in this paragraph and the two above carries one more obligation: if a deleted line was the
match for a `HEADER_QUANTIFIER_EXEMPTIONS` key, that key is now STALE and its own guard will fire. Remove
the key in the SAME commit and name it in the SUMMARY. A stale exemption left behind is a declared
obligation matching nothing, which is the mirror image of the defect this round is closing.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && SP=packages/backend/src/outbound-prohibition.spec.ts; TD="${TMPDIR:-/tmp}"; B="$TD/defminer-round12-baseline"; [ -f "$B" ] || { echo "MISSING ROUND-12 BASELINE FILE"; exit 1; }; . "$B"; RB=$(git rev-parse 1cb2884) || { echo "ROUND BASE UNRESOLVABLE"; exit 1; }; git merge-base --is-ancestor "$RB" HEAD || { echo "1cb2884 IS NOT AN ANCESTOR OF HEAD"; exit 1; }; pnpm exec vitest run "$SP" > "$TD/r12.gate2.log" 2>&1; GRC=$?; cat "$TD/r12.gate2.log"; [ "$GRC" -eq 0 ] || { echo "GATE SUITE RED (rc=$GRC)"; exit 1; }; GT=$(grep -E '^[[:space:]]*Tests[[:space:]]' "$TD/r12.gate2.log" | tail -1 | sed -n 's/.*[^0-9]\([0-9][0-9]*\) passed.*/\1/p'); [ -n "$GT" ] || { echo "GATE PASSED-COUNT UNPARSEABLE"; exit 1; }; [ "$GT" -ge "$BASE_GATE_ARRIVAL" ] || { echo "GATE COUNT $GT BELOW ARRIVAL $BASE_GATE_ARRIVAL — A CASE WAS LOST"; exit 1; }; pnpm test > "$TD/r12.full2.log" 2>&1; FRC=$?; cat "$TD/r12.full2.log"; [ "$FRC" -eq 0 ] || { echo "WHOLE SUITE RED (rc=$FRC)"; exit 1; }; FT=$(grep -E '^[[:space:]]*Tests[[:space:]]' "$TD/r12.full2.log" | tail -1 | sed -n 's/.*[^0-9]\([0-9][0-9]*\) passed.*/\1/p'); FF=$(grep -E '^[[:space:]]*Test Files[[:space:]]' "$TD/r12.full2.log" | tail -1 | sed -n 's/.*[^0-9]\([0-9][0-9]*\) passed.*/\1/p'); [ "$FT" -ge "$BASE_TESTS_ARRIVAL" ] || { echo "SUITE COUNT $FT BELOW ARRIVAL $BASE_TESTS_ARRIVAL"; exit 1; }; [ "$FF" -eq "$BASE_FILES" ] || { echo "FILE COUNT $FF != $BASE_FILES"; exit 1; }; pnpm typecheck || { echo "typecheck RED"; exit 1; }; pnpm lint || { echo "lint RED"; exit 1; }; pnpm knip || { echo "knip RED"; exit 1; }; pnpm build:backend || { echo "build:backend RED"; exit 1; }; pnpm check:bundle || { echo "check:bundle RED"; exit 1; }; SBN=$(grep -n '^BEGIN DERIVED RESIDUAL' "$SP" | head -1 | cut -d: -f1); [ -n "$SBN" ] || { echo "BEGIN SENTINEL NOT A BARE FULL LINE"; exit 1; }; [ "$SBN" -eq 949 ] || { echo "THE GATE HEADER IS NOW $((SBN-1)) LINES; TASK 1 LEFT IT AT 957 AND THIS TASK DELETES EXACTLY 9, SO 948 IS THE ONLY VALUE THAT IS ATTRIBUTABLE. A DIFFERENT VALUE MEANS LINES WERE ADDED OR REMOVED BEYOND THE TWO CLAUSES AND THE PIN BELOW CANNOT BE TRUSTED"; exit 1; }; echo "HEADER-LENGTH-AFTER-DELETIONS=$((SBN-1))"; B1=$(grep -n 'THE FINAL RESIDUAL, AFTER PLAN 01-25' "$SP" | grep -c . || true); [ "$B1" -eq 1 ] || { echo "THE 01-25 BANNER RESOLVES TO $B1 LINES, EXPECTED EXACTLY 1 — AT 0 THE ADJACENCY CHECK READS NOTHING AND ABOVE 1 IT READS WHICHEVER CAME FIRST"; exit 1; }; B1N=$(grep -n 'THE FINAL RESIDUAL, AFTER PLAN 01-25' "$SP" | head -1 | cut -d: -f1); sed -n "$((B1N+1))p" "$SP" | grep -q 'WIDENED 2026-08-24 (WR-27)' || { echo "THE LINE BELOW THE 01-25 BANNER IS NOT THE WIDENED PARAGRAPH — THE SIX-LINE CLAUSE BETWEEN THEM IS NOT FULLY DELETED, OR MORE THAN IT WAS. FOUND: $(sed -n "$((B1N+1))p" "$SP")"; exit 1; }; B2=$(grep -n 'THE NARROWING, AFTER PLAN 01-26' "$SP" | grep -c . || true); [ "$B2" -eq 1 ] || { echo "THE 01-26 BANNER RESOLVES TO $B2 LINES, EXPECTED EXACTLY 1"; exit 1; }; B2N=$(grep -n 'THE NARROWING, AFTER PLAN 01-26' "$SP" | head -1 | cut -d: -f1); sed -n "$((B2N+1))p" "$SP" | grep -q 'THE NARROWING AFTER WAVE 26' || { echo "THE LINE BELOW THE 01-26 BANNER IS NOT THE WAVE-26 PARAGRAPH — THE THREE-LINE CLAUSE BETWEEN THEM IS NOT FULLY DELETED, OR MORE THAN IT WAS. FOUND: $(sed -n "$((B2N+1))p" "$SP")"; exit 1; }; echo "BOTH-CLAUSES-DELETED-BANNERS-INTACT b1=$B1N b2=$B2N"; WM=$(grep -c 'WINDOWS\.md' "$SP" || true); [ "$WM" -eq 3 ] || { echo "THE GATE FILE NAMES THE WINDOWS LEDGER ON $WM LINES AND EXACTLY 3 ARE ACCOUNTED FOR AFTER THIS TASK (THE TWO POINTER-NOT-A-BOUND STATEMENTS AND THE UNGUARDED-LIMIT STATEMENT). ABOVE 3 A NEW MENTION WAS WRITTEN; BELOW 3 A TRUE STATEMENT WAS DELETED WITH THE FALSE ONES"; exit 1; }; HM=$(awk -v s="$SBN" 'NR<s && /\.planning\//' "$SP" | grep -c . || true); [ "$HM" -eq 8 ] || { echo "THE HEADER CARRIES $HM PLANNING-PATH MENTIONS AND 8 ARE EXPECTED AFTER DELETING TWO FROM TEN. THE CENSUS BELOW IS DECLARED AGAINST THAT POPULATION"; exit 1; }; DECL=$(grep -c 'HEADER_PLANNING_MENTIONS' "$SP" || true); [ "$DECL" -ge 4 ] || { echo "HEADER_PLANNING_MENTIONS IS REFERENCED $DECL TIME(S), EXPECTED AT LEAST 4 (THE DECLARATION AND THE DISCHARGES THAT READ IT) — THE LIST EXISTS BUT THE CENSUS DOES NOT READ IT"; exit 1; }; ARR=$(grep -c 'HEADER_PLANNING_MENTIONS: readonly' "$SP" || true); [ "$ARR" -eq 1 ] || { echo "HEADER_PLANNING_MENTIONS IS NOT DECLARED AS A READONLY LIST ($ARR) — A KEYED RECORD SILENTLY DROPS A DUPLICATE ENTRY AND THIS TASK FORBIDS THAT SHAPE"; exit 1; }; PIN=$(grep -oE 'WIDEST_ANCHOR_SHADOW = [0-9]+' "$SP" | head -1 | grep -oE '[0-9]+'); [ -n "$PIN" ] || { echo "THE SHADOW PIN CONSTANT IS UNREADABLE"; exit 1; }; [ "$PIN" -eq 540 ] || { echo "THE SHADOW PIN READS $PIN AND THE ONLY ATTRIBUTABLE VALUE AFTER THIS TASK IS 540 (549 MINUS THE NINE DELETED SURFACE LINES). IF THE MEASUREMENT GENUINELY DISAGREES, THAT IS A FINDING TO REPORT, NOT A NUMBER TO ADOPT"; exit 1; }; PC=$(grep -c 'WIDEST_ANCHOR_SHADOW = ' "$SP" || true); [ "$PC" -eq 1 ] || { echo "THE SHADOW PIN IS DECLARED $PC TIMES"; exit 1; }; SZP=$(grep -c 'toBe(WIDEST_ANCHOR_SHADOW)' "$SP" || true); [ "$SZP" -eq 1 ] || { echo "THE SIZE PIN ASSERTION COUNT IS $SZP, EXPECTED 1"; exit 1; }; IDA=$(grep -c 'toBe(WIDEST_ANCHOR_TOKEN)' "$SP" || true); [ "$IDA" -eq 1 ] || { echo "THE IDENTITY PIN ASSERTION COUNT IS $IDA, EXPECTED 1 — TASK 2 MUST NOT TOUCH IT"; exit 1; }; git show "$RB":"$SP" | grep -o 'const WIDEST_ANCHOR_TOKEN =.*' > "$TD/r12.tok.base"; grep -o 'const WIDEST_ANCHOR_TOKEN =.*' "$SP" > "$TD/r12.tok.work"; [ -s "$TD/r12.tok.base" ] || { echo "THE IDENTITY-PIN DECLARATION WAS NOT FOUND ON THE BASE REVISION, SO THE DIFF BELOW WOULD COMPARE NOTHING"; exit 1; }; diff "$TD/r12.tok.base" "$TD/r12.tok.work" > /dev/null || { echo "THE IDENTITY PIN'S DECLARATION CHANGED THIS TASK"; exit 1; }; echo "IDENTITY-PIN-BYTE-UNCHANGED"; PL=$(grep -n 'const WIDEST_ANCHOR_SHADOW' "$SP" | head -1 | cut -d: -f1); [ -n "$PL" ] || { echo "THE PIN DECLARATION LINE IS EMPTY — EVERY WINDOW CHECK BELOW WOULD BE VACUOUS"; exit 1; }; LO=$((PL-70)); [ "$LO" -lt 1 ] && LO=1; VN=$(grep -nE '\b(549|540)\b' "$SP" | cut -d: -f1); VC=$(printf '%s\n' "$VN" | grep -c . || true); [ "$VC" -ge 1 ] || { echo "NO LINE IN THE FILE CARRIES THE PINNED VALUE AT ALL — THE DECLARATION ITSELF IS MISSING AND THE CHECK BELOW WOULD PASS HAVING FOUND NOTHING"; exit 1; }; OUT=$(printf '%s\n' "$VN" | awk -v a="$LO" -v b="$PL" '$1 < a || $1 > b'); OUTC=$(printf '%s\n' "$OUT" | grep -c . || true); [ "$OUTC" -eq 0 ] || { echo "THE PINNED VALUE IS STILL PUBLISHED ON $OUTC LINE(S) OUTSIDE THE PIN'S OWN COMMENT BLOCK ($LO..$PL): $(printf '%s ' $OUT). CONSTRAINT 2 IS DISCHARGED IN THE COMMIT THAT MOVES THE CONSTANT, NOT IN A LATER ONE — THAT IS THE EXACT SHAPE 08345c3 SHIPPED AND CR-32 RECORDS. LINES INSIDE THE BLOCK ARE TASK 3's."; exit 1; }; echo "PINNED-VALUE-CONFINED-TO-PIN-BLOCK window=$LO..$PL sites=$VC"; H574=$(grep -cE '\b574\b' "$SP" || true); [ "$H574" -eq 2 ] || { echo "THE SUPERSEDED VALUE APPEARS ON $H574 LINE(S) AND EXACTLY 2 ARE ACCOUNTED FOR — BOTH DATED HISTORICAL, BOTH ADJUDICATED BY PASS 13. ABOVE 2 A NEW RESTATEMENT WAS WRITTEN; BELOW 2 A RECORD WAS DESTROYED"; exit 1; }; grep -nE '\b574\b' "$SP" | cut -c1-160; CBX=$(grep -c 'CORE11_BOX_EXPECTED = "- \[ \] \*\*CORE-11\*\*"' "$SP" || true); [ "$CBX" -eq 1 ] || { echo "CORE11_BOX_EXPECTED IS NOT THE UNCHECKED FORM: $CBX"; exit 1; }; LGD=$(git diff --name-only HEAD~1..HEAD -- .planning/ | grep -v 'phases/' | grep -c . || true); [ "$LGD" -eq 0 ] || { echo "THIS TASK TOUCHED A NON-PHASE PLANNING FILE ($LGD) AND OWNS NONE"; exit 1; }; D=$(git diff "$RB"..HEAD -- "$SP"); AST=$(printf '%s\n' "$D" | grep -cE '^[+-].*(from "typescript"|[^A-Za-z]ts\.[A-Za-z])' || true); [ "$AST" -eq 0 ] || { echo "COMPILER-API LINES IN ROUND DIFF: $AST"; exit 1; }; A=$(git diff --name-only "$RB"..HEAD -- packages/ scripts/ | grep -v '\.spec\.ts$' | grep -c . || true); [ "$A" -eq 0 ] || { echo "ROUND TOUCHED SHIPPED SOURCE: $A FILE(S)"; exit 1; }; git diff --exit-code -- packages/ && echo TASK2-TREE-CLEAN</automated>
  </verify>

  <acceptance_criteria>
    - THE EMPTY-LIST RED PASTED IN FULL: the census case title and its message naming all ten header mentions by line number and text, with the two CR-30 lines identified in it. An explicit statement that this run happened BEFORE the deletions.
    - The two deletions shown by diff as PURE deletions of six and three lines, with both banner lines shown as context rather than changed lines, and the post-deletion adjacency of each banner to the paragraph that followed the deleted text quoted from the file.
    - All eight declared entries listed with their reasons, and a one-sentence statement per entry of what the line actually says — written from reading the line, with an explicit note of any case where the reason disagreed with what this plan predicted.
    - THE ATTRIBUTION WRITTEN BEFORE THE MEASUREMENT: the predicted 549 -> 540 and `419..1549` -> `419..1540` stated, then the pin's failure message pasted in full, then the reconciliation. If they matched, say so; if they did not, the task STOPPED and the discrepancy is the finding.
    - THE PLANTED RED WITH THE PIN GREEN IN THE SAME RUN: the planted line, its line number (proved to be below 419), the census's failure message, and the shadow pin's passing tick from that same run. `git log --oneline -1` pasted before the mutation; the restore performed with `git checkout --`, VERIFIED by re-reading the line rather than assumed, and the tree proved clean by `git diff --exit-code`.
    - The stale-entry, duplicate-entry and empty-reason discharges each watched RED on their own one-line mutation, with the three messages pasted and shown to be DISTINCT from one another and from the undeclared-mention message. Each restored and verified.
    - The census docblock's reach statement quoted, and an explicit answer to whether it overstates the census in any respect. It MUST disclose BOTH blind spots, not one: a mechanism claim naming no file at all, AND a mechanism claim naming a file outside the planning directory. The matcher is the planning-directory PATH PREFIX, and the verify's own population count (`awk 'NR<s && /\.planning\//'`) is the proof of that — so a false machine-check sentence naming a backend source file or a script NAMES A FILE and is equally invisible. A docblock that discloses only the no-file case would be this class's signature shape reappearing inside the guard built to stop it, and the whole point of this artifact is that the disclosure is WEAKER than what the guard does rather than stronger.
    - `WINDOWS.md` mention count stated at 3 with each surviving site named and quoted, and a statement that each is a true statement about an unguarded surface rather than a mechanism claim.
    - TRAP B addressed: whether the declared-phrasing guard fired on the census's new prose, and how it was answered.
    - CONSTRAINT 2 DISCHARGED AS A TABLE, NOT AS A CLAIM: every line of the gate file carrying the old value, the new value or the superseded 574, listed with its line number, its text, its classification (LIVE or DATED HISTORY) and the reason for that classification. Every LIVE site outside the pin's comment block shown reconciled BY DIFF in this same commit, with the disposition stated per site (number deleted, or made to read the constant by name). The two dated 574 sites quoted and shown byte-unchanged.
    - The four surfaces outside this file — `.planning/STATE.md:408`, `.planning/ROADMAP.md:239` and `01-SECURITY.md`'s five lines — named explicitly as plan 01-47's, with a statement that this task did not touch them and the diff showing it.
    - Any `HEADER_QUANTIFIER_EXEMPTIONS` key made stale by these deletions named and removed in the same commit, with the stale-entry guard's message quoted if it fired.
  </acceptance_criteria>

  <done>
Every `.planning/` mention in the gate header is either inside the machine-owned span or a declared entry
with a reason; the census was watched RED with an empty list naming CR-30's own two lines; those nine
lines are deleted and both banners are intact and adjacent to what followed them; the shadow pin reads
540 over raw `419..1540`, matching an attribution stated before the measurement; the identity pin is
byte-unchanged; the census was watched RED again on a planted mention while the pin stayed green; every
site in this file publishing the pinned value is classified LIVE or DATED HISTORY in a table, and every
LIVE one outside the pin's own comment block is reconciled in the same commit that moved the constant.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CR-31 — the pin's hand-written span arithmetic and accepted-opener locator deleted and emitted from the pin's own computation instead, then the round's closing baseline executed rather than carried</name>

  <files>packages/backend/src/outbound-prohibition.spec.ts</files>

  <precondition>Tasks 1 and 2 are committed, the suite is green, the shadow pin reads 540 and the gate header is 948 lines. Re-locate the pin's comment block by TEXT — it has moved by Task 1's generated-span growth minus Task 2's nine deletions.</precondition>

  <read_first>
    - The pin's comment block and its case in full — at `1cb2884` roughly `:10145-10260`. Read the whole block: the reach statement, the authorisation paragraph, the `MEASURED AT WAVE 42` paragraph carrying the numbers, the case body with `shadows`, `widest`, `widestSize`, `lo`, `hi`, and the two failure messages.
    - `constructAnchorFor` and its five recognisers, near `:9397`. READ THEM, DO NOT CHANGE THEM. This task adds no predicate and narrows nothing; it only prints what the case already computes.
  </read_first>

  <behavior>
    - The pin's comment block states no raw span, no raw line count, no excluded count, no accepted-opener line number and no shipped-occurrence count.
    - The pin's failure message, when the pin fails, prints the raw span, the raw line count over that span, the number of lines in it that are excluded, the surface count, and the identity of the lines inside the span the anchor walk resolves differently — every one computed in that run.
    - The number of assertions in the pin's case is unchanged: this task adds no `expect`.
    - The shadow pin still reads 540 and is green; the identity pin is byte-unchanged; `SURFACE_LINES.length` is unchanged from Task 2's exit value.
  </behavior>

  <action>
Delete from the pin's comment block every hand-written number that the case itself computes. That is the
paragraph stating the widest shadow's raw span, its raw line count, the count of lines exclusion one
removes, the subtraction that produces the pinned value, CR-31's own sentence naming a line number for the
one construct the recognisers accept inside that span, and the trailing claim about how many shipped
occurrences the shadow already holds. DELETION, not correction: CR-31 exists because a correct sentence
was re-falsified by the next commit that moved a line, and a corrected sentence would be the same sentence
waiting for the same commit. The occurrence count is already pinned by the occurrence/exemption count
equality elsewhere in this file, so deleting it removes a second publication of one fact rather than a
fact.

WRITE NONE OF THOSE PHRASINGS BACK INTO THE FILE IN ANY FORM, INCLUDING INSIDE A COMMENT DESCRIBING WHAT
WAS DELETED. The verify below scans this block for a raw span, an excluded-line count and a line-number
locator and requires zero of each; a comment recording what used to be there would satisfy those patterns
and would also be a fourth hand-written copy of the fact. The record belongs in the SUMMARY.

Delete the value from the one other place in the block that spells it: the sentence explaining why the
uniqueness census cannot reach this measurement says a shadow is wide because a stated number of lines find
the anchor. That sentence is TRUE of the shadow's width and does not need the width to make its point;
say that a shadow is wide because its whole span resolves to one anchor, and let the pin publish the
figure. That leaves the declaration as the only place in this file where the value is written down —
which is what constraint 2 means, and the verify below asserts it over the WHOLE file rather than a window.

Leave in place, and say why in one line: `WIDEST_ANCHOR_SHADOW` itself, which is the pin and must be a
literal; the reach statement's five numbered limits, which state what the case does and does not bound and
carry no measurement; the authorisation paragraph, which states what a future author may do and makes no
claim about the file's current geometry; and the two DATED HISTORICAL 574 sites, which name the pass that
measured them and state no current bound — Task 2 classified both and this task must leave both
byte-unchanged.

Write the round's own history — that the pin moved and why — into the SUMMARY and the commit message, NOT
into the file. A line in this file recording the move would carry the new value a second time and would be
the fourteenth instance of the class this round is closing, installed by the task closing the thirteenth.

Extend the pin's EXISTING failure message so the facts just deleted are printed by the run that needs them.
It already emits the measured width, the pinned value, the owner token and the span endpoints. Add the raw
line count over that span, the count of lines inside it that `EXCLUDED_LINES` holds, and the surface count,
each computed there. Label the first two in the message with the upper-case tokens `RAW LINES` and
`EXCLUDED` — the verify below greps for exactly those, so that deleting the comment's numbers without
replacing them is caught as a loss of true disclosure rather than passing as a clean-up. Then add the fact
CR-31's deleted sentence was standing in for: the lines inside the raw span that the anchor walk resolves
to a DIFFERENT anchor than the shadow's owner, as a count and — if that count is not zero — the first few
of them by number and text. Compute it by calling the same `constructAnchorFor` the case already calls; add
no new predicate, do not lift the five recognisers out of their function, and do not reimplement them.

ADD NO NEW `expect`. The message is a diagnostic on the path that already fails, not a second rule. A new
assertion here would be a new claim about the file's geometry made in the same round that is removing one,
and its risk is the risk this round exists to remove. State that decision in the comment in one line, so a
later reader knows the emission is deliberately unasserted.

Put in the block's place a single short line saying that the span arithmetic and the anchor-walk result are
printed by the pin's own failure message at the moment they are measured, and stating NO number of its own.
That line is a pointer, not a bound, and the file already uses that discipline by name.

Watch the emission actually emit. Plant a mutation that makes the pin fail — two bare comment lines inside
the shadow's raw span and outside exclusion one — and paste the full failure message, showing every new
field populated with a real value. Restore with `git checkout --`, verify the restore by re-reading the
lines rather than assuming it, and prove the tree clean by diff.

Then execute the round's closing baseline rather than carrying it: the whole suite, `tsc --build`, `lint`,
`knip`, `build:backend`, `check:bundle`, the gate spec's own count, the module count and
`SURFACE_LINES.length`, each measured in this session and reconciled against the baseline file Task 1 wrote.
Audit the WHOLE round's diff over both files: what changed, what did not, and an explicit statement that no
file under `packages/*/src` other than the gate spec and no file under `scripts/` was touched.

State plainly in the SUMMARY whether you believe criterion (3) — the disclosure is the only bound stated on
every surface a reader touches, contradicted nowhere — now discharges, and name what plan 01-47 still owns.
DO NOT FLIP CORE-11's BOX AND DO NOT TOUCH `CORE11_BOX_EXPECTED`. Verification pass 14 owns that decision
and the two change in one commit when they change.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && SP=packages/backend/src/outbound-prohibition.spec.ts; TD="${TMPDIR:-/tmp}"; B="$TD/defminer-round12-baseline"; [ -f "$B" ] || { echo "MISSING ROUND-12 BASELINE FILE"; exit 1; }; . "$B"; RB=$(git rev-parse 1cb2884) || { echo "ROUND BASE UNRESOLVABLE"; exit 1; }; git merge-base --is-ancestor "$RB" HEAD || { echo "1cb2884 IS NOT AN ANCESTOR OF HEAD"; exit 1; }; pnpm exec vitest run "$SP" > "$TD/r12.gate3.log" 2>&1; GRC=$?; cat "$TD/r12.gate3.log"; [ "$GRC" -eq 0 ] || { echo "GATE SUITE RED (rc=$GRC)"; exit 1; }; GT=$(grep -E '^[[:space:]]*Tests[[:space:]]' "$TD/r12.gate3.log" | tail -1 | sed -n 's/.*[^0-9]\([0-9][0-9]*\) passed.*/\1/p'); [ "$GT" -ge "$BASE_GATE_ARRIVAL" ] || { echo "GATE COUNT $GT BELOW ARRIVAL $BASE_GATE_ARRIVAL"; exit 1; }; pnpm test > "$TD/r12.full3.log" 2>&1; FRC=$?; cat "$TD/r12.full3.log"; [ "$FRC" -eq 0 ] || { echo "WHOLE SUITE RED (rc=$FRC)"; exit 1; }; FT=$(grep -E '^[[:space:]]*Tests[[:space:]]' "$TD/r12.full3.log" | tail -1 | sed -n 's/.*[^0-9]\([0-9][0-9]*\) passed.*/\1/p'); FF=$(grep -E '^[[:space:]]*Test Files[[:space:]]' "$TD/r12.full3.log" | tail -1 | sed -n 's/.*[^0-9]\([0-9][0-9]*\) passed.*/\1/p'); [ "$FT" -ge "$BASE_TESTS_ARRIVAL" ] || { echo "SUITE COUNT $FT BELOW ARRIVAL $BASE_TESTS_ARRIVAL"; exit 1; }; [ "$FF" -eq "$BASE_FILES" ] || { echo "FILE COUNT $FF != $BASE_FILES"; exit 1; }; pnpm exec tsc --build || { echo "tsc --build RED"; exit 1; }; pnpm typecheck || { echo "typecheck RED"; exit 1; }; pnpm lint || { echo "lint RED"; exit 1; }; pnpm knip || { echo "knip RED"; exit 1; }; pnpm build:backend || { echo "build:backend RED"; exit 1; }; pnpm check:bundle || { echo "check:bundle RED"; exit 1; }; M=$(find packages/*/src -name '*.ts' ! -name '*.spec.ts' | grep -c . || true); [ "$M" -eq "$BASE_MODULES" ] || { echo "MODULE COUNT $M != $BASE_MODULES"; exit 1; }; PIN=$(grep -oE 'WIDEST_ANCHOR_SHADOW = [0-9]+' "$SP" | head -1 | grep -oE '[0-9]+'); [ "$PIN" -eq 540 ] || { echo "THE SHADOW PIN READS $PIN; TASK 3 CHANGES NO LINE INSIDE RAW 419..1540 AND MUST LEAVE IT AT 540"; exit 1; }; SBN=$(grep -n '^BEGIN DERIVED RESIDUAL' "$SP" | head -1 | cut -d: -f1); [ "$SBN" -eq 949 ] || { echo "THE GATE HEADER IS NOW $((SBN-1)) LINES AND TASK 2 LEFT IT AT 948 — TASK 3 EDITS ONLY THE PIN'S COMMENT BLOCK, THOUSANDS OF LINES BELOW THE SHADOW"; exit 1; }; PL=$(grep -n 'const WIDEST_ANCHOR_SHADOW' "$SP" | head -1 | cut -d: -f1); [ -n "$PL" ] || { echo "THE PIN DECLARATION LINE IS EMPTY — THE WINDOW SCAN BELOW WOULD READ THE WHOLE FILE"; exit 1; }; W0=$((PL-70)); [ "$W0" -lt 1 ] && W0=1; BLK=$(awk -v a="$W0" -v b="$PL" 'NR>=a && NR<b' "$SP"); BLKN=$(printf '%s\n' "$BLK" | grep -c . || true); [ "$BLKN" -ge 40 ] || { echo "THE COMMENT WINDOW ABOVE THE PIN READ $BLKN LINES, TOO FEW FOR THE CHECKS BELOW TO MEAN ANYTHING"; exit 1; }; SPAN=$(printf '%s\n' "$BLK" | grep -cE '419\.\.1[0-9]{3}' || true); [ "$SPAN" -eq 0 ] || { echo "THE PIN'S COMMENT BLOCK STILL HAND-WRITES A RAW SPAN ON $SPAN LINE(S) — CR-31 IS THE FINDING THAT A HAND-WRITTEN SPAN LOCATOR IS FALSIFIED BY THE NEXT COMMIT THAT MOVES A LINE"; exit 1; }; EXC=$(printf '%s\n' "$BLK" | grep -cE '\b582\b' || true); [ "$EXC" -eq 0 ] || { echo "THE PIN'S COMMENT BLOCK STILL HAND-WRITES THE EXCLUDED-LINE COUNT ON $EXC LINE(S). THE RAW LINE COUNT IS CAUGHT BY THE PINNED-VALUE CHECK BELOW RATHER THAN BY A SECOND PATTERN, BECAUSE A PATTERN WIDE ENOUGH TO CATCH IT ALSO CATCHES THE DATED 1,129-LINE RELOCATION MEASUREMENT THAT IS SUPPOSED TO SURVIVE"; exit 1; }; SUB=$(printf '%s\n' "$BLK" | grep -cE '\b(549|540)\b' || true); [ "$SUB" -eq 0 ] || { echo "THE PIN'S COMMENT BLOCK STILL RESTATES THE PINNED VALUE ON $SUB LINE(S); THE CONSTANT BELOW IT IS THE ONLY PUBLICATION AUTHORISED"; exit 1; }; VN=$(grep -nE '\b(549|540)\b' "$SP" | cut -d: -f1); VC=$(printf '%s\n' "$VN" | grep -c . || true); [ "$VC" -eq 1 ] || { echo "THE PINNED VALUE IS WRITTEN ON $VC LINE(S) OF THIS FILE AND EXACTLY 1 IS AUTHORISED — THE DECLARATION. LINES: $(printf '%s ' $VN). ONE BOUND, PUBLISHED ONCE; THE ROUND'S OWN HISTORY BELONGS IN THE SUMMARY AND THE COMMIT MESSAGE"; exit 1; }; [ "$VN" -eq "$PL" ] || { echo "THE ONE LINE CARRYING THE PINNED VALUE IS $VN AND THE DECLARATION IS AT $PL — THE VALUE SURVIVES SOMEWHERE OTHER THAN THE PIN"; exit 1; }; echo "ONE-BOUND-PUBLISHED-ONCE line=$VN"; H574=$(grep -cE '\b574\b' "$SP" || true); [ "$H574" -eq 2 ] || { echo "THE SUPERSEDED VALUE APPEARS ON $H574 LINE(S), EXPECTED THE 2 DATED HISTORICAL SITES TASK 2 CLASSIFIED — ABOVE 2 A NEW RESTATEMENT WAS WRITTEN, BELOW 2 A RECORD WAS DESTROYED"; exit 1; }; LOC=$(printf '%s\n' "$BLK" | grep -cE 'opener at 1[0-9]{3}|docblock opener at' || true); [ "$LOC" -eq 0 ] || { echo "CR-31's ACCEPTED-OPENER LOCATOR IS STILL HAND-WRITTEN ON $LOC LINE(S)"; exit 1; }; MSG=$(awk -v a="$PL" 'NR>=a && NR<a+130' "$SP"); printf '%s\n' "$MSG" | grep -q 'RAW LINES' || { echo "THE PIN'S FAILURE MESSAGE DOES NOT EMIT A RAW LINE COUNT — THE COMMENT'S NUMBERS WERE DELETED WITHOUT BEING REPLACED BY EMITTED ONES, WHICH REMOVES A FALSE DISCLOSURE AND ALSO REMOVES A TRUE ONE"; exit 1; }; printf '%s\n' "$MSG" | grep -q 'EXCLUDED' || { echo "THE PIN'S FAILURE MESSAGE DOES NOT EMIT THE EXCLUDED COUNT"; exit 1; }; EXPN=$(printf '%s\n' "$MSG" | grep -c 'expect(' || true); [ "$EXPN" -le 6 ] || { echo "THE PIN'S CASE NOW CARRIES $EXPN expect( CALLS; THIS TASK ADDS NO ASSERTION AND THE ARRIVAL SHAPE HAD SIX (THREE NON-VACUITY, ONE WIDTH GUARD, THE SIZE PIN AND THE IDENTITY PIN)"; exit 1; }; ANCH=$(printf '%s\n' "$MSG" | grep -c 'constructAnchorFor' || true); [ "$ANCH" -ge 1 ] || { echo "THE EMITTED ANCHOR-WALK RESULT IS NOT COMPUTED FROM constructAnchorFor — CR-31's CLASS FIX IS THAT THE FACT IS DERIVED, NOT THAT IT IS RESTATED"; exit 1; }; NEWPRED=$(git diff "$RB"..HEAD -- "$SP" | grep '^+' | grep -vE '^\+[[:space:]]*(//|\*|/\*)' | grep -cE '(DECLARATION =|FIXTURE_TITLE =|DOCBLOCK_OPEN =|isLineComment =|isRule =)' || true); [ "$NEWPRED" -eq 0 ] || { echo "A RECOGNISER PREDICATE WAS LIFTED OR REIMPLEMENTED IN THIS ROUND'S DIFF: $NEWPRED — THE FIVE RECOGNISERS STAY INSIDE constructAnchorFor"; exit 1; }; IDA=$(grep -c 'toBe(WIDEST_ANCHOR_TOKEN)' "$SP" || true); [ "$IDA" -eq 1 ] || { echo "THE IDENTITY PIN ASSERTION COUNT IS $IDA, EXPECTED 1"; exit 1; }; SZP=$(grep -c 'toBe(WIDEST_ANCHOR_SHADOW)' "$SP" || true); [ "$SZP" -eq 1 ] || { echo "THE SIZE PIN ASSERTION COUNT IS $SZP, EXPECTED 1"; exit 1; }; CBX=$(grep -c 'CORE11_BOX_EXPECTED = "- \[ \] \*\*CORE-11\*\*"' "$SP" || true); [ "$CBX" -eq 1 ] || { echo "CORE11_BOX_EXPECTED IS NOT THE UNCHECKED FORM: $CBX"; exit 1; }; BOXROW=$(grep -c '^- \[ \] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md || true); [ "$BOXROW" -eq 1 ] || { echo "CORE-11's LEDGER CHECKBOX IS NOT THE UNCHECKED FORM: $BOXROW"; exit 1; }; WM=$(grep -c 'WINDOWS\.md' "$SP" || true); [ "$WM" -eq 3 ] || { echo "THE WINDOWS-LEDGER MENTION COUNT MOVED TO $WM; TASK 2 LEFT IT AT 3"; exit 1; }; D=$(git diff "$RB"..HEAD -- "$SP"); DN=$(printf '%s\n' "$D" | grep -cE '^([+][^+]|-[^-])' || true); [ "$DN" -ge 40 ] || { echo "ROUND DIFF OVER THE SPEC IS $DN CHANGED LINES, EXPECTED AT LEAST 40 BY THIS POINT — EVERY DIFF-SCOPED CHECK ABOVE WOULD PASS HAVING READ ALMOST NOTHING"; exit 1; }; NETADD=$(printf '%s\n' "$D" | grep -c '^+[^+]' || true); NETDEL=$(printf '%s\n' "$D" | grep -c '^-[^-]' || true); echo "ROUND-DELTA added=$NETADD removed=$NETDEL net=$((NETADD-NETDEL))"; AST=$(printf '%s\n' "$D" | grep -cE '^[+-].*(from "typescript"|[^A-Za-z]ts\.[A-Za-z])' || true); [ "$AST" -eq 0 ] || { echo "COMPILER-API LINES IN ROUND DIFF: $AST"; exit 1; }; HANDC=$(printf '%s\n' "$D" | grep '^+' | grep -vE '^\+[[:space:]]*(//|\*|/\*)' | grep -cE '(braceDepth|braceLevel|indentOf|indentLevel|enclosing[A-Z]|Enclosing|containmentOf|frameOf|depth\+\+|depth--)' || true); [ "$HANDC" -eq 0 ] || { echo "HAND-ROLLED CONTAINMENT SPELLING IN ROUND DIFF: $HANDC"; exit 1; }; AS='BEGIN{s=0} !s && /^export function auditSource\(/ {s=1; print; next} s && /^\}$/ {print; exit} s {print}'; git show "$RB":"$SP" | awk "$AS" > "$TD/r12.base.auditSource"; awk "$AS" "$SP" > "$TD/r12.work.auditSource"; ABL=$(grep -c . "$TD/r12.base.auditSource" || true); [ "$ABL" -gt 100 ] || { echo "auditSource EXTRACTOR COLLAPSED ON BASE ($ABL) AND WOULD COMPARE NOTHING"; exit 1; }; AD=$(diff "$TD/r12.base.auditSource" "$TD/r12.work.auditSource" | grep -c . || true); [ "$AD" -eq 0 ] || { echo "auditSource CHANGED ACROSS THE ROUND: $AD DIFF LINES"; exit 1; }; A=$(git diff --name-only "$RB"..HEAD -- packages/ scripts/ | grep -v '\.spec\.ts$' | grep -c . || true); [ "$A" -eq 0 ] || { echo "ROUND TOUCHED SHIPPED SOURCE: $A FILE(S)"; exit 1; }; SPECS=$(git diff --name-only "$RB"..HEAD -- packages/ | grep -c . || true); [ "$SPECS" -eq 1 ] || { echo "THE ROUND TOUCHED $SPECS FILE(S) UNDER packages/, EXPECTED EXACTLY 1"; exit 1; }; [ -n "${BASE_PLAN_START:-}" ] || { echo "BASE_PLAN_START IS EMPTY IN THE BASELINE FILE — TASK 1 MUST CAPTURE THIS PLAN'S OWN STARTING COMMIT, AND WITHOUT IT THE OWNERSHIP DIFF BELOW WOULD HAVE TO USE THE ROUND BASE, WHICH ALREADY CARRIES THE PLAN-AUTHORING COMMIT'S ROADMAP.md CHANGE"; exit 1; }; git merge-base --is-ancestor "$BASE_PLAN_START" HEAD || { echo "BASE_PLAN_START ($BASE_PLAN_START) IS NOT AN ANCESTOR OF HEAD"; exit 1; }; PLN=$(git diff --name-only "$BASE_PLAN_START"..HEAD -- .planning/ | grep -v 'phases/' | sort -u); PLNN=$(printf '%s\n' "$PLN" | grep -c . || true); [ "$PLNN" -eq 1 ] || { echo "THIS PLAN OWNS EXACTLY ONE NON-PHASE PLANNING FILE AND HAS CHANGED $PLNN SINCE ITS OWN STARTING COMMIT $BASE_PLAN_START: $PLN. THE DIFF IS SCOPED TO THIS PLAN'S OWN COMMITS RATHER THAN TO 1cb2884 BECAUSE THE PLAN-AUTHORING COMMIT ALREADY CHANGED ROADMAP.md AND A ROUND-BASE-SCOPED COUNT WOULD READ TWO WHERE THIS PLAN CHANGED ONE"; exit 1; }; printf '%s\n' "$PLN" | grep -q 'REQUIREMENTS.md' || { echo "THE ONE NON-PHASE PLANNING FILE CHANGED IS NOT REQUIREMENTS.md: $PLN"; exit 1; }; N=$(git status --porcelain packages/ .planning/ | grep -c . || true); [ "$N" -eq 0 ] || { echo "TREE DIRTY AT EXIT: $N"; exit 1; }; git diff --exit-code -- packages/ && echo TASK3-TREE-CLEAN</automated>
  </verify>

  <acceptance_criteria>
    - The deleted comment lines shown by diff, with an explicit list of every number removed and where the same fact is now obtainable.
    - THE EMISSION WATCHED EMITTING: the planted two-line mutation, its line numbers proved to be inside raw `419..1540` and outside exclusion one, and the pin's FULL failure message pasted with every new field carrying a real computed value — raw span, raw count, excluded count, surface count and the anchor-walk result. Restore performed with `git checkout --`, VERIFIED by re-reading the mutated lines, and the tree proved clean by diff.
    - An explicit statement that NO `expect` was added, with the case's assertion count stated before and after.
    - An explicit statement that `constructAnchorFor` and its five recognisers are byte-unchanged, shown by diff, and that no predicate was lifted out of it.
    - The replacement pointer line quoted, and an explicit confirmation that it states no number.
    - ONE BOUND, PUBLISHED ONCE, PROVED OVER THE WHOLE FILE: the grep output showing exactly one line carrying the pinned value and that line being the declaration, plus the two dated 574 sites quoted and shown byte-unchanged across the whole round.
    - THE ROUND'S CLOSING BASELINE, EXECUTED: whole suite, `tsc --build`, `lint`, `knip`, `build:backend`, `check:bundle`, gate count, module count and `SURFACE_LINES.length`, each measured in this session and reconciled line by line against Task 1's baseline file. Any difference attributed.
    - THE WHOLE-ROUND DIFF AUDIT: exactly one file under `packages/`, exactly one non-phase file under `.planning/` MEASURED FROM `$BASE_PLAN_START` and it is `REQUIREMENTS.md`, nothing under `scripts/`, `auditSource` byte-identical, added/removed/net line counts stated. State explicitly that the `1cb2884`-scoped count is TWO and why — the second is the plan-authoring commit's own `ROADMAP.md` row addition — so the number is accounted for rather than silently absorbed.
    - A PLAIN ANSWER ON CRITERION (3): whether the disclosure is now the only bound stated on every surface a reader touches, what plan 01-47 still owns, and — if you believe the box is earned — a statement saying so AND a statement that you did not flip it, with both surfaces shown byte-unchanged.
  </acceptance_criteria>

  <done>
No number in the pin's comment block is hand-written; every one the case computes is emitted by its own
failure message and was watched being emitted with real values; no assertion was added; the recognisers
are byte-unchanged; the round's baseline is measured rather than carried; the whole-round diff touches
one spec file and one planning ledger and nothing else; CORE-11's box is `[ ]` in both surfaces.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate prose → reader | A disclosure a reader acts on. A false one here is not caught by the runtime; it is caught only by a human re-deriving it, and twelve rounds show that does not happen reliably. |
| generator input → shipped span | The one place a machine-owned statement can be made to say something the code does not do, without any guard noticing, is a change to what the generator renders. |
| gate spec → `.planning/REQUIREMENTS.md` | The only planning file this suite reads. A change to what it byte-compares changes what the requirement ledger publishes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-300 | Repudiation | the header's mechanism claims about which files are checked | high | mitigate | CR-30's two clauses DELETED, not corrected; the true statement rendered from `BYTE_COMPARED_SURFACES` into the generated span and byte-compared in both surfaces; every remaining `.planning/` mention in the header made a declared entry by the census, watched RED with an empty list against CR-30's own lines. |
| T-01-301 | Tampering | `BYTE_COMPARED_SURFACES` as a second definition beside `GATE_FILE`/`LEDGER` | high | mitigate | Both readers resolve FROM the constant; the verify asserts each path exists as a string literal exactly once in the file, so a re-introduced literal is a red suite rather than a silent divergence. |
| T-01-302 | Repudiation | a generator change that ships to only one of the two compared surfaces | high | mitigate | Both byte-comparison cases observed RED before either span is regenerated, and the round's diff audit asserts exactly one non-phase planning file changed and that it is the ledger. |
| T-01-303 | Tampering | the entry-count equality broken by a generated line beginning with `ENTRY_MARK` | medium | mitigate | Named as TRAP A in the plan; the verify asserts `ENTRY_MARK` unchanged and both entry-count cases are required green with the counted value stated. |
| T-01-304 | Repudiation | the shadow pin moved to fit a number it cannot account for | high | mitigate | The 549 → 540 attribution is written into the plan BEFORE the measurement; the verify refuses any other value and says so; the executor is instructed to STOP and report rather than adopt a disagreeing measurement. |
| T-01-305 | Repudiation | a hand-written locator re-falsified by the next commit that moves a line (CR-31's own shape) | medium | mitigate | Every computed number deleted from the pin's comment and emitted from the computation; the verify scans the comment window for a raw span, an excluded count, the pinned value and an opener locator and requires zero of each. |
| T-01-306 | Denial of Service | a census or byte comparison that passes having measured nothing | high | mitigate | Non-vacuity asserted BEFORE every rule: the header region non-empty, the scan matching at least one line, the extractor's base-revision length checked before any diff is trusted, and every expected value in the baseline file asserted non-empty before comparison. |
| T-01-308 | Repudiation | an ownership gate asserting a count it cannot account for | high | mitigate | The plan-authoring commit's own `ROADMAP.md` change sits between the round base and this plan's first edit, so ownership is asserted from `BASE_PLAN_START` — captured at arrival — and the pre-existing delta is enumerated and named in the gate's own bytes, with anything other than `ROADMAP.md` in it a STOP-and-report. A gate that aborts on correct work while naming a commit the executor did not make is this class one level out. |
| T-01-307 | Elevation of Privilege | CORE-11's box flipped on the strength of this round's work | critical | mitigate | Both surfaces asserted `[ ]` by the verify in all three tasks; the plan states pass 14 owns the decision and instructs the executor to say so and stop rather than flip. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | NO package-manager install task exists in this plan. Every dependency is already resolved; `pnpm knip`, `pnpm lint` and `pnpm check:bundle` all run against the existing lockfile. `check:bundle` re-asserts the shipped bundle's entire import set as one specifier, `crypto`. |
</threat_model>

<verification>
- `pnpm test` exits 0 at no fewer test files and no fewer tests than the round-12 arrival baseline written by Task 1.
- `pnpm typecheck`, `pnpm lint`, `pnpm knip`, `pnpm build:backend` all exit 0.
- `pnpm check:bundle` reports exactly one import specifier, `crypto`.
- The gate spec alone exits 0 at no fewer tests than arrival.
- Both byte-comparison cases green; both entry-count cases balance at `RESOLVER_REGISTRY.length`; both sentinel full-line-uniqueness assertions resolve to exactly one line in the gate file and the ledger carries both sentinels.
- The gate header is 948 lines; the shadow pin reads 540; the identity pin's declaration is byte-identical to `1cb2884`.
- `auditSource` is byte-identical to `1cb2884`.
- Exactly one file under `packages/` changed across the round, and it is the gate spec. Nothing under `scripts/`. Exactly one non-phase file under `.planning/` changed, and it is `REQUIREMENTS.md`.
- `CORE11_BOX_EXPECTED` and `.planning/REQUIREMENTS.md`'s CORE-11 row are both the unchecked form.
- `git diff --exit-code -- packages/` is clean at the end of every task.
</verification>

<success_criteria>
- CR-30 is closed by deletion in all three of its false readings, and the true statement it displaced is generated from one constant and byte-compared in both surfaces.
- The header census was observed RED with an empty list naming ten mentions including CR-30's own two, RED again on a planted mention with the shadow pin green in the same run, and RED on each of its stale-entry, duplicate-entry and empty-reason discharges.
- CR-31 is closed by deleting every hand-written number from the pin's comment and emitting them from the pin's own computation, watched emitting with real values, with no assertion added.
- The shadow pin moved 549 → 540 against an attribution stated before the measurement and matching it exactly.
- Nothing shipped changed; nothing leaked; CORE-11's box was not touched.
</success_criteria>

<output>
Create `.planning/phases/01-skeleton-persistence-compatibility/01-46-SUMMARY.md` when done.

It MUST carry, in this order: the arrival baseline and any difference from pass 13's figures; the two
byte-comparison RED observations quoted in full; the census's empty-list RED quoted in full with all ten
named lines; the eight declared entries with the reasons written from reading each line; the 549 → 540
attribution stated before the measurement and the pin's failure message quoted against it; the planted-RED
runs with their restore proofs; the pin's emitted failure message with every new field populated; the
round's closing baseline; the whole-round diff audit; and a plain answer on criterion (3) naming what plan
01-47 still owns.
</output>
