---
phase: 01-skeleton-persistence-compatibility
plan: 39
type: execute
wave: 39
gap_closure: true
depends_on: [01-38]
files_modified:
  - packages/backend/src/outbound-prohibition.spec.ts
# THIS PLAN IS CR-20, WHICH IS THE ONE SEAM LEFT IN CRITERION (3)'s MECHANISM LEG.
#
# WHAT THE VERIFIER FOUND, BY ITS OWN EXECUTION AGAINST THE REAL FILE RATHER THAN BY INHERITING THE
# REVIEWER. `constructAnchorFor` (`:9210-9254`) resolves a masked LINE OF TEXT truncated to 64
# characters. It does not resolve a SITE. Two shapes follow from that and the verifier drove a shipped
# occurrence through each, at 434 of 434 GREEN both times:
#
#   (a) SELF-ANCHORING. The backward scan opens AT the occurrence (`for (let i = lineNumber; …)`,
#       `:9226`) and the forward walk is bounded BY the occurrence (`for (let k = head; k <= lineNumber; …)`,
#       `:9245`). For an occurrence that IS its own construct header — an `it(` title — or that is the
#       FIRST CONTENT LINE of a docblock, the forward walk lands on the occurrence's own line and the
#       construct half becomes a 64-character prefix of the line half. It carries no positional
#       information at all. THREE shipped keys are of this shape and you can read the duplication off
#       the map without running anything: the entries at `:9068`, `:9094` and `:9100`. The verifier
#       deleted the shipped occurrence at `:1838` from the docblock opened at `:1837` and planted it
#       verbatim at `:7102`, immediately above the `&&=` `it(` inside
#       `describe("an outbound receiver, however it was bound")` — 5,264 lines away, in a region about
#       the OUTBOUND walk rather than the FILE walk, making that exemption's construct-dependent reason
#       false while the key did not move.
#
#   (b) AMBIGUOUS ANCHOR TOKENS. The anchor is a line of text and lines repeat. Measured again during
#       this planning session at HEAD `b8c3d81`: `grep -c '^\s*it\.each(\[$'` returns 10, and
#       `SPELLING (in receiver-key position) RESOLVED BY REPORTS` is produced by THREE header rows, at
#       `:283`, `:299` and `:361`. FOUR keys hang off those two tokens — `:9052`, `:9054`, `:9056` and
#       `:9090`. The verifier moved the shipped cell at `:305` out of the CR-10 table headed at `:299`
#       into the wave-34 logical-assignment table headed at `:361`, 58 lines down and about `??=`
#       rather than `var`, for a byte-identical key.
#
#   3 + 4 = 7 of 29. That is the residual CR-17's fix narrowed 29 down to and did not close.
#
#   (c) THE DISCLOSURE IS WRONG RATHER THAN MERELY INCOMPLETE, WHICH IS WHAT MAKES THIS A BLOCKER.
#       Limit (5) at `:9029-9040` states flatly that an exemption written for one construct is NOT
#       discharged by an occurrence under a DIFFERENT one, and states the whole residual as
#       same-construct interchangeability. The verifier executed two CROSS-construct relocations that
#       the first sentence forbids and the second does not describe. A stated reach exceeding an
#       executed reach, inside the paragraph written to state the reach of the fix for that exact
#       defect. This phase's signature failure, two levels deep.
#
# THE OPERATOR HAS DECIDED HOW THIS IS FIXED AND IT IS NOT A REBUILD. An AST- or frame-derived identity
# was considered and REJECTED for this round. Plan the verifier's TWO CHANGES, both of which the file's
# own reviewer wrote, plus the disclosure restatement. DO NOT PLAN OR WRITE A PARSE, AN AST WALK, OR A
# RE-DERIVATION OF OCCURRENCE IDENTITY FROM THE `it`/`describe` FRAME. Every change in this plan stays
# inside the existing line-scan mechanism.
#
# CHANGE (1) IS PRESCRIBED AND ITS SUFFICIENCY IS A MEASUREMENT. The verifier prescribes opening the
# backward scan STRICTLY ABOVE the occurrence — `for (let i = lineNumber - 1; i >= 1; i--)`. That is
# necessary and this plan requires it. WHETHER IT IS SUFFICIENT IS NOT ASSERTED ANYWHERE IN THIS PLAN,
# because it is not knowable without running it: the FORWARD walk at `:9245` is still bounded by
# `lineNumber`, so for a docblock whose header names nothing the walk can still land on the
# occurrence's own line. The reviewer measured that exact property as WR-53 and reported that all seven
# live forward-walks currently land at `head + 1`. Task 1 therefore MEASURES the prefix property across
# all 29 regenerated keys and only then decides whether the second half of WR-53's own prescribed fix —
# bound the forward walk strictly above the occurrence, and CONTINUE the backward scan instead of
# returning the sentinel when a header names nothing above it — is needed. Both halves are the same
# line scan. Neither is an AST.
#
# CHANGE (2) IS A CENSUS AND WHAT IT FLAGS IS ALSO A MEASUREMENT. Census every line's token, assert
# every anchor in use is produced by exactly ONE line. NO ACCEPTANCE CRITERION IN THIS PLAN NAMES AN
# EXPECTED NUMBER OF FLAGGED ANCHORS. The verifier's numbers above are RECONCILIATION ANCHORS from its
# execution, not targets: change (1) lands first and regenerates every key, so the census runs against
# a key set nobody has seen yet. Two flagged anchors and six flagged anchors are equally successful
# outcomes; a number that was predicted rather than measured is not.
#
# WHAT THIS PLAN MUST NOT DO.
#   - DO NOT FLIP CORE-11's CHECKBOX AND DO NOT REVERT IT EITHER. It reads `[ ]`, `CORE11_BOX_EXPECTED`
#     at `:10311` already agrees, round 8 did not move it, and verification pass 9's determination is
#     that it MAY NOT MOVE. Whether it may move is pass 10's call. `requirements mark-complete` stays
#     UNRUN, exactly as plan 01-38's P38-D3 left it. `git diff` over the checkbox is EMPTY at every
#     task boundary and this plan touches no file under `.planning/`.
#   - DO NOT RE-OPEN THE 2026-08-25 RE-SCOPE. Pass 8 tested it for a quietly-lowered bar and found
#     none; pass 9 did not re-test it and does not report it as a defect, and adds that round 8 is
#     EVIDENCE FOR it. The `[x]` is unearned because criterion (3)'s mechanism is bypassable, not
#     because the bar is wrong.
#   - DO NOT WEAKEN THE OUTBOUND GATE TO GET GREEN. A change to `constructAnchorFor` that made every
#     key unique by construction — folding the line NUMBER into the key is the obvious one — would pass
#     the census having measured nothing and would break on every unrelated edit below it. Forbidden
#     outright, in frontmatter and in each task.
#   - DO NOT TOUCH `CLOSES_FROZEN_ARRAY`, `closingBracketAfter`, the `EXCLUSIONS` array, either band,
#     any `proof` token, `nameableRemainder`'s body, `NO_PRECEDING_CONSTRUCT`'s docblock or
#     `preAnchoringExemptionKeyForFixtureOnly`'s body. Those are plans 01-40 and 01-41.
#   - DO NOT WRITE A TENTH OVERCLAIM WHILE FIXING THE NINTH. Three of this round's six items are
#     overclaim corrections. Limit (5), `constructAnchorFor`'s own docblock and the WR-52 coverage
#     sentence are all restated in Task 3 and every one of them is re-read against the no-overclaim
#     prohibition with a per-sentence verdict.
#
# ENVIRONMENT FACTS, MEASURED DURING PLANNING SO THE EXECUTOR DOES NOT RE-DERIVE THEM.
#   - HEAD at planning time: `b8c3d81`. The gate file is 10,399 lines. Cross-check ANY git-status
#     snapshot against `git rev-parse HEAD` before trusting it.
#   - NO ACTIVE GIT HOOKS. Run the gate set by hand at every task boundary.
#   - COMMIT BEFORE MUTATING. Wave 29 destroyed its own Task 1 with `git checkout --`.
#   - RECONCILIATION ANCHORS, NOT ADDRESSES. `constructAnchorFor` `:9210`; backward scan `:9226`;
#     forward walk `:9245`; the two sentinel returns `:9251` and `:9253`; `nameableRemainder` `:9159`;
#     `NO_PRECEDING_CONSTRUCT` `:9167`; `EXEMPTION_ANCHOR_SEP` `:9174`; `exemptionKeyFor` `:9273`;
#     `preAnchoringExemptionKeyForFixtureOnly` `:9304`; `surfaceExemptionKeys` `:9343`;
#     `HEADER_QUANTIFIER_EXEMPTIONS` `:9041` with its 29 keys at `:9044` … `:9104`; limit (4)
#     `:8998-9027`; limit (5) `:9029-9040`; the null-anchor case `:9971`; the WR-52 fixture's coverage
#     sentence `:9998-10001`, its own-header comment `:10019-10021`, its counter-probe `:10126-10152`;
#     `CORE11_BOX_EXPECTED` `:10311`. EVERY ONE OF THOSE SHIFTS the moment this plan's first edit
#     lands. Start from them, RE-DERIVE each by its identifier or its title, and record any
#     disagreement with the value above as a FINDING rather than adopting either silently.
#   - THE THREE SELF-ANCHORING MAP ENTRIES, by their line in the map at HEAD `b8c3d81`: `:9068` (the
#     docblock-first-content-line shape, whose occurrence is `:1838`), `:9094` and `:9100` (the two
#     `it(` title shapes). Re-locate each by its key text.
#   - THE FOUR AMBIGUOUS-TOKEN MAP ENTRIES: `:9052`, `:9054`, `:9056` share the anchor produced by
#     `:283`, `:299` and `:361`; `:9090` carries the `it.each([` anchor, produced by 10 lines.
#     `SPELLING (operator, by POSITION) RESOLVED BY REPORTS` at `:417` is produced by exactly ONE line
#     and is therefore NOT in this set — do not sweep it in by pattern-matching on the word SPELLING.
#   - THE RELOCATION TARGET: `describe("an outbound receiver, however it was bound")` opens at `:7009`;
#     the `||=` case is `:7096` and the `&&=` case is `:7102`. `type Violation` is declared at `:1836`,
#     its docblock opens at `:1837` and the occurrence is `:1838`.
#   - BASELINE TO RE-MEASURE RATHER THAN ASSUME: the gate suite alone 434 passed (434); the whole suite
#     31 files / 1374 tests exit 0; `tsc --build` exit 0; `pnpm check:bundle` exactly ONE import
#     specifier, `crypto`; the real-tree walk 23 shipped non-spec modules across both `SOURCE_ROOTS` at
#     ZERO violations.
#   - EVERY `<automated>` BLOCK OPENS WITH `set -o pipefail` AND EVERY COUNT IS ASSERTED NUMERICALLY,
#     AND NEITHER IS DECORATION. Round 8 shipped a revision because `pnpm test 2>&1 | tail -8` exits
#     with TAIL's status, so a suite printing failures still exited 0 — measured on this tree. A bare
#     `grep -c` PRINTS a count without ASSERTING it. Do not strip either, and run the blocks as written
#     rather than retyping them.

autonomous: true
requirements: [CORE-11]

estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 3
  confidence: low
# `estimate-calibration` reports `{"factor": 1, "applied": false, "sample_count": 0, "min_samples": 3}`,
# so `tokens` equals `raw_tokens` and the confidence is DERIVED from the sample count, never self-rated.
# Weigh the MEASUREMENT COUNT over the token figure: one recogniser change whose sufficiency is
# measured rather than assumed, one full regeneration of 29 keys, two new permanent cases, one census
# whose flagged set is unknown at planning time, and TWO real-tree relocations each watched going RED.

must_haves:
  truths:
    - "THE ANCHOR IDENTIFIES A SITE, AND THAT IS MEASURED ON ALL 29 KEYS RATHER THAN ARGUED FROM THE DIFF. The backward scan opens STRICTLY ABOVE the occurrence. Every one of the 29 regenerated keys is then checked for the self-anchoring property — the construct half, minus any trailing ellipsis, being a PREFIX of the line half — and the count of keys still carrying it is pasted. The three entries that carried it at HEAD `b8c3d81` are named individually with their before and after anchors side by side."
    - "IF THE ONE-LINE CHANGE IS NOT SUFFICIENT, THE SECOND HALF OF THE SAME LINE SCAN LANDS, AND STILL NO AST. The forward walk at `:9245` is bounded by the occurrence, not by the construct, so a docblock header that names nothing can still return the occurrence's own line. If the measurement shows any key still self-anchoring, the forward walk is bounded STRICTLY ABOVE the occurrence and the backward scan CONTINUES to the next enclosing construct instead of returning the sentinel — WR-53's own prescribed fix, inside the existing scan. No parse, no AST walk, no derivation of identity from the `it`/`describe` frame."
    - "THE SELF-ANCHORING SHAPE IS FORBIDDEN BY A PERMANENT CASE, NOT LEFT TO THE NEXT MEASUREMENT. A named case asserts that no exemption key's CONSTRUCT half is a prefix of its LINE half, with a failure message that says what a self-anchoring key means — that the entry is discharged by the same text wherever it sits — and names `exemptionKeyFor` as the thing to rebuild with. The case is watched failing before it is trusted."
    - "EVERY ANCHOR IN USE NAMES EXACTLY ONE LINE OF THE FILE, ASSERTED BY A CENSUS OVER THE FILE'S OWN BYTES. Every line's would-be anchor token is computed in the SAME form the builder emits — masked, normalized, truncated at 64 with the same ellipsis — and counted. Each construct half in use, taken from BOTH `HEADER_QUANTIFIER_EXEMPTIONS` and from what `surfaceExemptionKeys` produces, is asserted to have a census count of exactly ONE, so neither side can drift alone."
    - "WHAT THE CENSUS FLAGS IS A MEASUREMENT AND THIS PLAN NAMES NO EXPECTED COUNT. Change (1) regenerates every key before the census runs, so the flagged set is unknown at planning time. Whatever it flags is enumerated with its token, its producing lines and the keys hanging off it, and disposed of by a stated three-choice rule — disambiguate the HEADER in its own bytes, re-site or rewrite the OCCURRENCE, or DELETE the occurrence — with the choice and its reason recorded one by one."
    - "THE CENSUS IS NOT SATISFIED BY MAKING EVERY KEY UNIQUE BY CONSTRUCTION. Folding the line NUMBER into the key, appending an ordinal, widening the census to permit more than one producing line, or exempting a token from the census would each turn the assertion green having measured nothing — and the line-number route would additionally break on every unrelated edit below it. All four are forbidden outright and the diff is shown not to contain them."
    - "BOTH OF THE VERIFIER'S RELOCATIONS ARE EXECUTED ON THE REAL TREE IN THIS SESSION AND WATCHED GOING RED. `:1838` to `:7102`, byte-identical, 5,264 lines; and `:305` out of the table headed at `:299` and into the table headed at `:361`. Each is planted after the task's own work is committed, run, observed RED with its failing title and FULL assertion message pasted, restored, and the tree shown clean by `git diff --exit-code`. The verifier's prior 434-of-434 GREEN on each is cited as the contrast."
    - "BOTH RELOCATIONS ALSO LAND AS PERMANENT FAILING-PATH FIXTURES OVER SYNTHETIC LINES. A guard against relocation that has never been watched failing is the same unwatched assertion this file has spent ten waves removing, and round 8 built one such fixture while excluding the very shape that breaks it. The own-header shape becomes a SECOND ARRAY PAIR in the existing cross-construct case, per WR-52's prescription; the ambiguous-token shape becomes a case asserting the census reports a token produced by two lines as non-unique."
    - "THE WR-52 COVERAGE SENTENCE IS RESTATED TO THE SPLIT IT ACTUALLY COVERS, AND THE SPLIT IS RE-MEASURED. Its probe side is REAL — the verifier applied the line-copy reduction and the fixture FAILED by name, refuting the reviewer's stronger reading — so this is a widening, not a rebuild. The sentence names how many shipped occurrences are of the own-header shape and how many are not, taken from a pasted count in this session rather than from the 26/3 figure quoted in the report."
    - "LIMIT (5) AND `constructAnchorFor`'s DOCBLOCK ARE RESTATED TO WHAT EXECUTES, ONCE, AT THE END. Limit (5) currently states the opposite of what the verifier executed. The docblock additionally claims the anchor is the FINEST construct enclosing the occurrence, which the reviewer measured as four occurrences anchoring to line 1 from up to 234 lines away (WR-49), and claims a forward walk bounded by the construct, which it is not (WR-53). Every sentence in both is re-read against the no-overclaim prohibition with a one-line verdict per sentence."
    - "THE MUST-NOT DOES NOT MOVE AND NOTHING LEAKS, RE-ESTABLISHED BY EXECUTION RATHER THAN CARRIED FORWARD. CORE-11's first sentence byte-identical by diff; the whole suite exit 0 with the gate walking 23 shipped modules across both `SOURCE_ROOTS` at ZERO violations; `tsc --build` exit 0; the shipped bundle's entire import set ONE specifier, `crypto`; and `git diff --name-only` over `packages/` and `scripts/` filtered of `.spec.ts` EMPTY. Every defect this plan touches is in a test-only gate's enforcement of its own description."
    - "NOTHING UNDER `.planning/` IS TOUCHED BY THIS PLAN. CORE-11's checkbox does not move, `requirements mark-complete` is not run, `01-PROBE.md`'s equality `38 == 27 + 11` stands unchanged and is re-stated in the SUMMARY, and `COVERAGE.md` is neither edited nor extended because this round integrates no external API."
    - "EVERY PREDICTION-VS-MEASUREMENT DISCREPANCY IS RECORDED RATHER THAN ABSORBED, in the shape `01-27-SUMMARY.md` uses. This plan predicts three self-anchoring keys, two ambiguous tokens with four keys hanging off them, and 29 entries surviving as 29. It predicts NOTHING about how many keys still self-anchor after change (1), how many anchors the census flags, or what the disposal costs."

  artifacts:
    - path: "packages/backend/src/outbound-prohibition.spec.ts"
      provides: "A `constructAnchorFor` whose backward scan opens strictly above the occurrence, extended only as far as the measurement requires and never beyond the existing line scan; the 29 exemption keys regenerated against it; a permanent case forbidding a construct half that is a prefix of its line half; a uniqueness census over every line's would-be anchor token with each anchor in use asserted to name exactly one line; the own-header shape added as a second array pair in the cross-construct fixture; a census fixture for the ambiguous-token shape; and limit (5), `constructAnchorFor`'s docblock and the fixture's coverage sentence restated to what executes"
      contains: "constructAnchorFor"

  key_links:
    - from: "an anchor that is a masked LINE OF TEXT"
      to: "an anchor that identifies a SITE"
      via: "a backward scan opened strictly above the occurrence, extended by a strictly-above forward walk and a continue-to-the-next-construct fallback only if the 29-key prefix measurement says the one-line change was not enough"
      pattern: "constructAnchorFor"
    - from: "an anchor token produced by ten lines"
      to: "an anchor token whose ambiguity is LOUD"
      via: "a census of every line's would-be token, with each anchor in use asserted to have exactly one producer, and whatever it flags disposed of by a three-choice rule that never widens the census"
      pattern: "HEADER_QUANTIFIER_EXEMPTIONS"
    - from: "two relocations the verifier ran at 434/434 green"
      to: "two relocations that have been watched going RED"
      via: "each executed once on the real tree after commit with its full assertion message pasted and the tree restored, and each landed as a permanent fixture over synthetic lines"
      pattern: "a CROSS-CONSTRUCT relocation"
    - from: "limit (5) stating the opposite of what executes"
      to: "a disclosure at the reach of the measurement that produced it"
      via: "one restatement at the end of the plan, after both changes are measured, with a per-sentence no-overclaim verdict covering WR-49's and WR-53's falsified sentences in the same docblock"
      pattern: "TWO OCCURRENCES UNDER THE SAME CONSTRUCT"

  prohibitions:
    - requirement_id: CORE-11
      category: security
      status: resolved
      verification: gate
      resolution: "The diff over `constructAnchorFor` and `exemptionKeyFor` is pasted in full and read against this prohibition sentence by sentence; the key format is shown to carry no line number and no positional ordinal beyond the pre-existing `#N`"
      statement: "MUST NOT weaken the outbound gate in the act of strengthening its anchoring. A key made unique by construction — the line number folded in, an ordinal appended, the census widened to permit more than one producer, or a token exempted from the census — turns the new assertion green having measured nothing, and the line-number route additionally breaks on every unrelated edit below it."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "No acceptance criterion in this plan names a count of still-self-anchoring keys or of flagged anchors; both enumerations are pasted whatever they say and disposed of by a stated rule"
      statement: "MUST NOT predict the result of either measurement. Change (1) regenerates every key before the census runs, so the flagged set does not exist until it is measured. A plan that bakes an expected number into its acceptance criteria converts an honest measurement into a target, and a target is what an executor engineers toward when the number disagrees."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "The diff is shown to contain no parser, no tokenizer, no AST library import, and no derivation of an occurrence's identity from an enclosing `it`/`describe` frame; `pnpm knip` is run at every task boundary"
      statement: "MUST NOT rebuild the mechanism. The operator considered an AST- or frame-derived identity and REJECTED it for this round. The verifier reproduced both bypasses and concluded the mechanism is one seam from correct with two small changes the file's own reviewer already wrote. Everything here stays inside the existing line scan."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "The synthetic fixtures' phrasings are taken from `UNBOUNDED_QUANTIFIERS` by index; the whole-file guard's green run at each task boundary is the evidence"
      statement: "MUST NOT spell a declared phrasing as a literal in new code or new prose. The guard scans this file's own bytes, so a fixture or a restated limit that writes a phrasing out raises the obligation it exists to test. Build it by reference, exactly as the key masking already does and for the same reason."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "New prose that trips wave 33's guard is REWRITTEN and its before and after are both pasted; no exclusion is widened and no exemption is minted for this plan's own text"
      statement: "MUST NOT satisfy a guard by widening it. Wave 34 forbade this once and wave 37 forbade it again. An exemption minted for the plan's own prose, or an exclusion stretched over it, turns a guard into decoration in one commit while every assertion stays green."
    - requirement_id: CORE-11
      category: safety
      status: open
      verification: gate
      resolution: "DISCLOSED, NOT CLOSED — the same-construct residual, the anchor's measured maximum reach, the guard's phrase-list reach, its normalization and both classes of unreached surface are named wherever this plan's result is stated"
      statement: "MUST NOT state or imply that the relocation class is closed, that the anchor is now a construct in any structural sense, or that criterion (3) is discharged. Two relocation shapes are removed. Two occurrences under the SAME construct remain interchangeable behind a positional ordinal assigned by scan order; the anchor is still the nearest preceding line five recognisers accept, which WR-49 measured reaching 234 lines above an occurrence; the guard is still a phrase list over bytes under one named normalization; undeclared spellings and the unguarded files are still unreached. A `✓` that omits any of those is a stated reach exceeding an executed one, which is the ninth instance of this phase's signature defect and this plan is written not to author the tenth."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "`git status --porcelain .planning/` shown EMPTY at every task boundary; `requirements mark-complete` not present in any command this plan runs"
      statement: "MUST NOT flip CORE-11's checkbox and MUST NOT revert it either. It reads `[ ]`, the pin at `:10311` already agrees, round 8 did not move it, and verification pass 9's determination is that it may not move. Whether it may move is pass 10's call. This plan touches no file under `.planning/` at all."
---

<objective>
Make the exemption anchor identify a SITE instead of a masked line of text, by the two changes the
verifier prescribed and the file's own reviewer wrote — a backward scan opened strictly above the
occurrence, and a census that makes an ambiguous anchor token loud — then restate the disclosure that
currently says the opposite of what executes.

Purpose: this is the one seam left in criterion (3)'s mechanism leg. Criteria (1) DERIVED and (2)
DRIFT-DETECTABLE are discharged. Criterion (3)'s DOCUMENT leg closed in round 8, against the verifier's
own measurement rather than against a summary. What remains is that `constructAnchorFor` resolves a
64-character masked LINE, so an occurrence that is its own construct header anchors to itself and an
anchor token produced by ten lines names none of them. Seven of 29 entries relocate invisibly and the
verifier drove two shipped occurrences through that hole at 434 of 434 green.

Output: a strictly-above backward scan, extended only as far as a 29-key prefix measurement says it
must be; the 29 keys regenerated against it; a permanent case forbidding a construct half that is a
prefix of its line half; a uniqueness census over every line's would-be token with each anchor in use
asserted to name exactly one line; whatever the census flags, enumerated and disposed of by a
three-choice rule; both of the verifier's relocations executed on the real tree and watched going RED
and both landed as permanent fixtures; and limit (5), `constructAnchorFor`'s docblock and the fixture's
coverage sentence restated once, at the end, to what executes.

TWO THINGS IN THIS PLAN ARE MEASUREMENTS AND IT NAMES NO EXPECTED RESULT FOR EITHER. How many keys
still self-anchor after change (1), and how many anchors the census flags. Change (1) regenerates every
key before the census runs, so the second set does not exist until it is measured. Zero and six are
equally successful outcomes of each; a number that was predicted rather than measured is not.

WHAT THIS PLAN DOES NOT DO. It does not flip or revert CORE-11's checkbox — pass 9 determined it may
not move and pass 10 owns the question. It does not run `requirements mark-complete`. It does not
re-open the 2026-08-25 re-scope. It touches no file under `.planning/`. It does not go near
`CLOSES_FROZEN_ARRAY`, `nameableRemainder`'s body, the sentinel's docblock or
`preAnchoringExemptionKeyForFixtureOnly`'s body — those are plans 01-40 and 01-41. And it does not
claim that removing two relocation shapes closes the relocation class.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md
@.planning/phases/01-skeleton-persistence-compatibility/01-36-SUMMARY.md
@.planning/phases/01-skeleton-persistence-compatibility/01-37-SUMMARY.md
@packages/backend/src/outbound-prohibition.spec.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: The backward scan opens STRICTLY ABOVE the occurrence, the 29 keys are regenerated against it, the self-anchoring shape is forbidden by a permanent case, and the `:1838` relocation is watched going RED on the real tree</name>

  <precondition>
    Round 8 is closed and the gate file is open again. `01-38-SUMMARY.md` exists on disk and
    `01-VERIFICATION.md` carries `verification_pass: 9`. Before the first edit, assert and PASTE the
    arrival baseline: `git rev-parse HEAD`, `git status --porcelain packages/ scripts/ .planning/`
    clean, the gate suite alone at its arrival count, `pnpm test` exit 0 with its file and test counts,
    `pnpm exec tsc --build` exit 0, and `pnpm check:bundle` reporting its import specifier set. HALT if
    any is red — a mechanism change written on top of a red tree cannot tell its own failure from the
    one that was already there, and plan 01-36 found exactly that at its own handoff. This repo has NO
    active git hooks, so nothing runs the gate set for you at a commit boundary.
  </precondition>

  <read_first>
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md` in full, and in
      particular the `gaps:` block's first `artifacts:` entry (CR-20), the `missing:` entry that begins
      `CR-20: MAKE THE ANCHOR IDENTIFY A SITE`, and the report body's section
      `### CR-20 — CONFIRMED. Both legs, both executed, both green.` That section states the cause read
      off the code and quotes the two loop headers. It is the contract this task is written against.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-REVIEW.md`, the `gap-closure-round-8`
      pass, sections `### CR-20`, `### WR-49` and `### WR-53`. WR-53 is the one that matters most here:
      it measured that the forward walk is bounded by the OCCURRENCE rather than by the construct, and
      that all seven live forward-walks currently land at `head + 1`. Its prescribed fix — bound the
      walk and fall through to `continue` the backward scan — is the extension this task may take IF
      the measurement requires it, and it is not an AST.
    - `packages/backend/src/outbound-prohibition.spec.ts` `:9153-9260` at HEAD `b8c3d81` —
      `nameableRemainder` `:9159`, `NO_PRECEDING_CONSTRUCT` `:9167`, `EXEMPTION_ANCHOR_SEP` `:9174`,
      `constructAnchorFor`'s docblock and body `:9176-9254` with the backward scan at `:9226`, the
      forward walk at `:9245` and the two sentinel returns at `:9251` and `:9253`. THOSE ARE
      RECONCILIATION ANCHORS, NOT ADDRESSES: round 8 proved every number in this file shifts between
      waves. Start from them, RE-DERIVE each by its identifier, and record any disagreement as a
      finding rather than adopting either silently.
    - `packages/backend/src/outbound-prohibition.spec.ts` `:9273-9360` — `exemptionKeyFor`,
      `preAnchoringExemptionKeyForFixtureOnly`, `quantifierOccurrences` and `surfaceExemptionKeys`.
      Read exactly how the construct half and the line half are joined and truncated, because the
      prefix test this task adds depends on both truncation widths and on the ellipsis character.
    - `packages/backend/src/outbound-prohibition.spec.ts` `:9041-9106` — the whole
      `HEADER_QUANTIFIER_EXEMPTIONS` map, all 29 entries with their reasons. Note the three entries
      whose construct half is visibly a prefix of their line half: `:9068`, `:9094`, `:9100`. Re-locate
      each by its key text, never by the number.
    - `packages/backend/src/outbound-prohibition.spec.ts` `:9880-9985` — the three discharge checks
      (`missing`, `stale`, the count equality), the `no exemption KEY carries a declared phrasing` case
      and the `no exemption key's ANCHOR reduces to nothing` case at `:9971`. The new prefix case sits
      beside these and must match their failure-message discipline: say what the defect means, not that
      it happened.
    - `packages/backend/src/outbound-prohibition.spec.ts` `:1834-1845` and `:7009`, `:7096-7110` — the
      source and the destination of the relocation. `type Violation` at `:1836`, the docblock opening
      at `:1837`, the occurrence at `:1838`; `describe("an outbound receiver, however it was bound")`
      at `:7009` and the `&&=` case at `:7102`.
    - `01-37-SUMMARY.md`'s account of the real-tree swap it executed and watched going red, for the
      SHAPE this task's mutation must take: commit first, plant, run, paste the full assertion message,
      restore, prove clean.
  </read_first>

  <files>packages/backend/src/outbound-prohibition.spec.ts</files>

  <reversibility rating="reversible">A recogniser change and a new case in a spec file, plus regenerated keys in a map in the same file. No shipped code, no behaviour, no requirement text, no exclusion and no ledger byte is touched. The real-tree relocation is planted and restored inside the same task with `git diff --exit-code` shown clean. One revert away if the anchoring proves brittle.</reversibility>

  <behavior>
    - An occurrence sitting ON an `it(` title line resolves to the construct ENCLOSING that title, not
      to a 64-character prefix of the title itself.
    - An occurrence that is the FIRST CONTENT LINE of a docblock resolves to the construct the docblock
      documents, not to a 64-character prefix of its own line.
    - No key's construct half, with any trailing ellipsis removed, is a prefix of its line half.
    - The same occurrence line placed under two different constructs still yields two different keys —
      the property the round-8 fixture already asserts, which must survive this change.
    - A shipped occurrence relocated verbatim into an unrelated construct 5,264 lines away raises a
      non-empty `missing` and a non-empty `stale`, and the suite goes RED.
  </behavior>

  <action>
    MEASURE THE ARRIVAL STATE BEFORE CHANGING ANYTHING, AND PASTE IT. Run the precondition set and
    paste every line of it. Then, with the file unmodified, enumerate all 29 keys and record for each
    one: its construct half, its line half, and whether the construct half — with any trailing ellipsis
    stripped — is a PREFIX of the line half. Paste that enumeration as a table. Reconcile the count of
    prefix-carrying keys against the three the verifier named and record any disagreement as a finding.
    This is the BEFORE half of the measurement and it must exist before the change, not be
    reconstructed after it.

    MAKE CHANGE (1), EXACTLY AS PRESCRIBED. Open the backward scan strictly above the occurrence:
    `for (let i = lineNumber - 1; i >= 1; i--)`. That is the whole of change (1) and it is the change
    the verifier named. Re-run the enumeration and paste the AFTER table beside the BEFORE one.

    NOW READ THE AFTER TABLE AND LET IT DECIDE, RATHER THAN DECIDING IN ADVANCE. If no key's construct
    half is a prefix of its line half, change (1) was sufficient and NOTHING FURTHER IS TOUCHED in the
    recogniser — say so in one sentence and move on. If any key still carries the property, the cause
    will be the forward walk at `:9245`, which is bounded by the occurrence rather than by the
    construct: a docblock header that names nothing on its own line walks forward onto the occurrence's
    own line. In that case take the SECOND HALF of WR-53's prescribed fix and nothing beyond it — bound
    the forward walk strictly above the occurrence, and when a resolved header names nothing above the
    occurrence, CONTINUE the backward scan to the next enclosing construct rather than returning the
    sentinel. Both halves are the same line scan over the same five recognisers. DO NOT WRITE A PARSER,
    DO NOT IMPORT AN AST LIBRARY, AND DO NOT DERIVE THE OCCURRENCE'S IDENTITY FROM AN ENCLOSING
    `it`/`describe` FRAME — the operator considered that route and rejected it for this round, and the
    verifier's finding is that the mechanism is one seam from correct rather than wrong in kind.

    WATCH FOR THE SENTINEL AND REPORT IT RATHER THAN ABSORBING IT. If the fallback route is taken, the
    number of occurrences resolving to `NO_PRECEDING_CONSTRUCT` may move. Count them before and after
    and paste both numbers. Plan 01-40 owns the sentinel and will assert it is never produced on the
    scanned surface, so a non-zero count arriving here is a HANDOFF FACT that must be written down in
    the SUMMARY in those words, not quietly left for the next wave to rediscover.

    REGENERATE THE 29 KEYS AGAINST THE NEW BUILDER AND KEEP THE REASONS WITH THEM. Every entry in
    `HEADER_QUANTIFIER_EXEMPTIONS` is rebuilt with `exemptionKeyFor`, never hand-edited into shape, and
    each key keeps the reason it already carried — the reasons are sentences a later author must keep
    true and this task has no licence to reword them. Paste the before and after key for each of the
    three formerly self-anchoring entries side by side with the differing portion identified. Assert
    the entry COUNT is unchanged at 29 and that the count equality still balances; if either moves, the
    move is the finding and it is written up before anything else is done.

    ADD THE PERMANENT CASE THAT FORBIDS THE SHAPE. A named `it(...)` asserts that for every key in the
    map, the construct half with any trailing ellipsis removed is NOT a prefix of the line half. Its
    failure message must say what the defect MEANS — that such a key names no site, so the entry is
    discharged by the same masked text wherever in the file that text sits, which is CR-20(a) exactly —
    and must name `exemptionKeyFor` as the thing to rebuild the entry with. Sit it beside the
    null-anchor case at `:9971` and match that case's message discipline. Take any declared phrasing it
    needs from `UNBOUNDED_QUANTIFIERS` BY INDEX; never spell one out.

    WATCH THE NEW CASE FAIL BEFORE TRUSTING IT. Temporarily restore the backward scan to open at the
    occurrence, run the suite, observe the new case RED, paste its full assertion message, then restore
    the fix and observe green. WR-54 exists precisely because round 8 shipped a correction with no pin;
    a pin nobody has watched failing is the same artifact.

    COMMIT, THEN EXECUTE THE REAL-TREE RELOCATION ONCE. Paste `git log --oneline -1` first. Locate the
    occurrence BY CONTENT — the sentence in the docblock above `type Violation`'s neighbour, not the
    line number in this plan — delete it from the docblock, and plant it verbatim as a `//` comment
    immediately above the `&&=` `it(` inside `describe("an outbound receiver, however it was bound")`.
    PLANT IT AS A SWAP: one occurrence removed, one added, so the count equality still balances,
    because a mutation that only adds proves a different and easier thing. Paste the before and after
    occurrence totals to prove it was a swap. Run the suite, observe RED, paste the failing title and
    the FULL assertion message, restore both edits, and prove the tree clean with
    `git diff --exit-code`. Cite `01-VERIFICATION.md` for the verifier's prior result on this exact
    swap — 434 passed of 434 — and present this session's RED as the contrast.

    IF THE RELOCATION DOES NOT GO RED, STOP AND WRITE IT DOWN BEFORE DOING ANYTHING ELSE. That outcome
    would mean change (1) does not reach the shape it was made for, and it is a finding of the first
    order — larger than anything else in this round. Do not adjust the mutation until it goes red;
    diagnose why the key did not change and report that.

    TOUCH NOTHING OUTSIDE THIS TASK'S SCOPE. `CLOSES_FROZEN_ARRAY`, `closingBracketAfter`, the
    `EXCLUSIONS` array, every band, every `proof` token, `nameableRemainder`'s body,
    `NO_PRECEDING_CONSTRUCT`'s docblock, `preAnchoringExemptionKeyForFixtureOnly`'s body,
    `CORE11_BOX_EXPECTED`, the box case, limit (5) and the fixture's coverage sentence all stay
    byte-unchanged in this task — the last two are Task 3's and the rest belong to plans 01-40 and
    01-41. Nothing under `.planning/` is opened. If new prose trips wave 33's guard, REWRITE THE PROSE.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY and state the outbound suite's new test count as
    `no fewer than` its arrival count, never as an equality.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && pnpm exec vitest run packages/backend/src/outbound-prohibition.spec.ts 2>&1 | tail -8 && pnpm test 2>&1 | tail -10 && pnpm typecheck && pnpm lint && pnpm knip && pnpm build:backend && pnpm check:bundle && N=$(git status --porcelain .planning/ | grep -c . || true); [ "$N" -eq 0 ] && A=$(git diff --name-only -- packages/ scripts/ | grep -v '\.spec\.ts$' | grep -c . || true); [ "$A" -eq 0 ] && git diff --exit-code -- packages/backend/src/outbound-prohibition.spec.ts && echo TREE-CLEAN</automated>
  </verify>

  <acceptance_criteria>
    - The precondition asserted and PASTED before the first edit: `git rev-parse HEAD`,
      `git status --porcelain packages/ scripts/ .planning/` clean, the gate suite's arrival count, the
      whole suite's file and test counts at exit 0, `tsc --build` exit 0, and `check:bundle`'s specifier
      set. HALT recorded if any was red.
    - The BEFORE enumeration of all 29 keys pasted as a table with construct half, line half and the
      prefix verdict per key, produced against the UNMODIFIED file, with the count of prefix-carrying
      keys stated and reconciled against the three the verifier named. Any disagreement recorded as a
      finding.
    - The backward scan shown by diff to open at `lineNumber - 1`, and the diff hunk pasted.
    - The AFTER enumeration pasted beside the BEFORE one, with the count of keys still carrying the
      prefix property stated. NO CRITERION HERE NAMES AN EXPECTED VALUE FOR THAT COUNT.
    - If the count is zero: one sentence stating that change (1) was sufficient and that the forward
      walk was NOT touched, with `git diff` over the forward walk shown EMPTY.
    - If the count is non-zero: the forward walk bounded strictly above the occurrence and the backward
      scan shown to CONTINUE rather than return the sentinel when a header names nothing above the
      occurrence, with the diff pasted, plus an explicit statement that this is WR-53's own prescribed
      fix and that no parser, no AST import and no `it`/`describe` frame derivation was added. `knip`
      run and clean.
    - The count of occurrences resolving to `NO_PRECEDING_CONSTRUCT` pasted BEFORE and AFTER, and the
      after value stated in the SUMMARY as a handoff fact for plan 01-40 in those words.
    - All 29 entries rebuilt with `exemptionKeyFor` rather than hand-edited, shown by the diff carrying
      no hand-authored key; the three formerly self-anchoring keys pasted before and after with the
      differing portion identified; the entry count asserted at exactly 29 and the count equality shown
      still balancing; every pre-existing reason shown byte-unchanged by diff.
    - The new prefix-forbidding case present as a NAMED `it(...)`, its title quoted in the SUMMARY, and
      its full failure message quoted and shown to say what the defect means rather than that it
      happened.
    - The new case WATCHED FAILING: the backward scan temporarily reverted to open at the occurrence,
      the suite run, the case observed RED, its FULL assertion message pasted, the fix restored, green
      observed. A pin that was never watched failing is not accepted by this criterion.
    - `grep -c` over the diff for a parser, a tokenizer, an AST dependency, a line NUMBER folded into a
      key, or a new positional ordinal — asserted numerically at zero:
      `N=$(git diff -- packages/backend/src/outbound-prohibition.spec.ts | grep -cE '\+.*(parse|tokeni|@babel|typescript\.createSourceFile|lineNumber\}|:\$\{lineNumber)' || true); [ "$N" -eq 0 ]`, with the
      command and its output pasted.
    - REAL-TREE RELOCATION executed AFTER the task's work was committed: `git log --oneline -1` pasted
      first; the occurrence located BY CONTENT with the line it was found at pasted; the plant made
      immediately above the `&&=` case inside `describe("an outbound receiver, however it was bound")`
      with the distance in lines pasted; the mutation shown to be a SWAP with before and after
      occurrence totals; the suite observed RED with the failing title and the FULL assertion message
      pasted; both edits restored; `git diff --exit-code` shown clean.
    - The SUMMARY states, citing `01-VERIFICATION.md`, that the verifier ran this same relocation before
      change (1) existed and observed 434 passed of 434, and presents this session's RED as the contrast.
    - If the relocation did NOT go red, the task stopped, the diagnosis is written up as a first-order
      finding, and no adjustment was made to the mutation to force a red.
    - `git diff` shown EMPTY over `CLOSES_FROZEN_ARRAY`, `closingBracketAfter`, the `EXCLUSIONS` array,
      every band, every `proof` token, `nameableRemainder`'s body, `NO_PRECEDING_CONSTRUCT`'s docblock,
      `preAnchoringExemptionKeyForFixtureOnly`'s body, `CORE11_BOX_EXPECTED`, the box case, limit (5)
      and the fixture's coverage sentence.
    - `git status --porcelain .planning/` asserted EMPTY numerically, output pasted; no
      `requirements mark-complete` invocation anywhere in the session log.
    - Executed `pnpm test` — exit 0, file and test counts pasted, no fewer than 31 files and 1374
      tests. The outbound suite's new count stated as `no fewer than` its arrival count.
    - Executed `pnpm typecheck`, `pnpm lint`, `pnpm knip` — all exit 0, output pasted.
    - Executed `pnpm build:backend && pnpm check:bundle` — exactly one import specifier, `crypto`.
    - Executed the real-tree walk over both `SOURCE_ROOTS` — 23 shipped non-spec modules, 0 violations;
      CORE-11's first sentence shown byte-identical by diff.
    - Any prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses, or an
      explicit statement that there were none.
  </acceptance_criteria>

  <done>
    The backward scan opens strictly above the occurrence; the prefix property was measured across all
    29 keys before and after and the extension to the forward walk was taken or declined on that
    measurement rather than in advance; the 29 keys are regenerated with their reasons intact; a
    permanent case forbids a construct half that is a prefix of its line half and has been watched
    failing; and the verifier's `:1838` relocation was executed once on the real tree and watched going
    RED where it had been green at 434 of 434, with the tree restored clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The uniqueness census — every line's would-be anchor token counted, every anchor in use asserted to name exactly ONE line, whatever it flags disposed of by a stated rule, and the `:305` relocation watched going RED</name>

  <precondition>
    Task 1 has executed and committed: the backward scan opens strictly above the occurrence and the 29
    keys are regenerated against it. Assert and paste before the first edit that `pnpm test` exits 0,
    that the gate suite is green at Task 1's recorded count, that `git status --porcelain` is clean over
    `packages/`, `scripts/` and `.planning/`, and that Task 1's prefix-forbidding case is present by
    title. HALT if any is red. THE KEY SET THIS TASK CENSUSES DID NOT EXIST BEFORE TASK 1 COMMITTED —
    re-derive every anchor from the file as it stands now, never from Task 1's pasted table.
  </precondition>

  <read_first>
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md`, section
      `#### (b) Ambiguous anchor tokens — a table cell moved between two identically-headed tables,
      invisibly`, and the `missing:` entry's second numbered change. Those two passages are this task's
      contract; the census is the verifier's own prescription in its own words.
    - `01-39-SUMMARY.md` if Task 1 wrote one, otherwise Task 1's committed diff — specifically the
      final form of `constructAnchorFor` and the regenerated key set. The census is written against the
      anchors the builder produces NOW.
    - `packages/backend/src/outbound-prohibition.spec.ts` — `constructAnchorFor`,
      `maskQuantifiers`, `normalizeGateLine`, `exemptionKeyFor`, `EXEMPTION_ANCHOR_SEP` and
      `surfaceExemptionKeys`, re-located by identifier. RECONCILIATION ANCHORS at HEAD `b8c3d81` were
      `:9210`, `:9109`, `:9079`, `:9273`, `:9174` and `:9343`; Task 1 moved all of them. Read in
      particular the exact truncation width and the exact ellipsis character the builder uses for the
      construct half, because a census that computes the token in a different form measures a different
      thing and would pass having compared nothing.
    - `packages/backend/src/outbound-prohibition.spec.ts` `:281-310`, `:355-366` and `:415-420` at HEAD
      `b8c3d81` — the three table headers that produce the same token (`:283`, `:299`, `:361`) and the
      one that produces a unique token (`:417`). Re-locate all four by content. The occurrence the
      verifier relocated is the `var` twin cell under the header at `:299`, and its destination is the
      wave-34 logical-assignment table headed at `:361`.
    - `packages/backend/src/outbound-prohibition.spec.ts` — every `it.each([` line. At HEAD `b8c3d81`
      `grep -c '^\s*it\.each(\[$'` returned 10 and exactly one exemption key hung off that token.
      Re-run the count; do not carry the 10.
    - `packages/backend/src/outbound-prohibition.spec.ts` `:9766-9812` — the `EXCLUSIONS` array and the
      non-vacuity case above the rule, for the SHAPE a census assertion should take in this file:
      non-vacuity asserted before the rule, and a failure message that states what the number means.
    - `01-37-SUMMARY.md`'s obligation-disposal record, for the shape this task's disposal record must
      take: one row per item, with the choice taken and the reason for it.
  </read_first>

  <files>packages/backend/src/outbound-prohibition.spec.ts</files>

  <reversibility rating="reversible">A new census helper and a new case in a spec file, plus whatever header prose the disposal rule touches. No shipped code, no behaviour and no ledger byte. The real-tree relocation is planted and restored inside the same task with the tree proved clean.</reversibility>

  <behavior>
    - A token produced by two or more lines of the file is reported as non-unique by the census.
    - Every construct half in use — taken from the declared map AND from what the surface produces —
      has a census count of exactly one.
    - The census computes each line's token in the SAME form the builder emits: masked, normalized, and
      truncated at the builder's own width with the builder's own ellipsis.
    - The census skips lines whose token has an empty `nameableRemainder`, because the builder never
      returns those, and it asserts it did not skip everything.
    - A shipped occurrence moved between two identically-headed tables raises a non-empty `missing` and
      a non-empty `stale`, and the suite goes RED.
  </behavior>

  <action>
    BUILD THE CENSUS OVER THE FILE'S OWN BYTES AND IN THE BUILDER'S OWN FORM. For every line of the
    gate file, compute the token that line would contribute as a construct anchor — the same
    `maskQuantifiers` over `normalizeGateLine` the builder applies, truncated at the builder's width
    with the builder's ellipsis — and count how many lines produce each token. SKIP tokens whose
    `nameableRemainder` is empty, because the builder never returns one of those, and state that
    exclusion in a comment rather than leaving a reader to infer it. DERIVE THE WIDTH AND THE ELLIPSIS
    FROM THE BUILDER rather than repeating the literals, so a later change to the truncation cannot
    leave the census measuring a form the keys no longer carry.

    ASSERT THE UNIQUENESS FROM BOTH SIDES, SO NEITHER CAN DRIFT ALONE. Take the construct half of every
    key in `HEADER_QUANTIFIER_EXEMPTIONS` and of every key `surfaceExemptionKeys` produces over the
    scanned surface, and assert each has a census count of exactly ONE. The two sets should be equal;
    asserting over both means an occurrence whose anchor is ambiguous is caught the moment it appears,
    before anyone writes an exemption for it. Put NON-VACUITY BEFORE THE RULE in the shape this file
    already uses: assert the census is non-empty, assert the set of anchors in use is non-empty, and
    say in the failure message what a zero would mean.

    WRITE THE FAILURE MESSAGE TO SAY WHAT THE DEFECT MEANS. It must name the token, name every line
    that produces it, and state that an anchor with more than one producer names none of them — so an
    exemption written for the occurrence under one producer is discharged just as well by an occurrence
    under another, which is CR-20(b) exactly. It must name the three permitted disposals and forbid the
    fourth. Match the discipline of the messages at the three discharge checks.

    RUN THE CENSUS AND PASTE WHAT IT FLAGS. THIS IS A MEASUREMENT AND THIS PLAN NAMES NO EXPECTED
    NUMBER. Task 1 regenerated every key, so the flagged set does not exist until this moment. For each
    flagged anchor paste the token, every line number that produces it, and every map key hanging off
    it. Reconcile against the verifier's own execution — two ambiguous tokens with four keys between
    them, `it.each([` from 10 lines and the receiver-key table header from 3 — and record any
    disagreement as a FINDING rather than adopting either number silently.

    DISPOSE OF EACH FLAGGED ANCHOR BY THIS RULE, ONE AT A TIME, RECORDING THE CHOICE AND THE REASON.
      (i) DISAMBIGUATE THE HEADER IN ITS OWN BYTES. Give the producing line a distinguishing clause so
          it names itself — which table of spellings it heads, which parameter set the `it.each` runs.
          `normalizeGateLine` strips only a LEADING comment marker, so a trailing clause survives into
          the token. State what now distinguishes it and re-run the census.
      (ii) RE-SITE OR REWRITE THE OCCURRENCE so it sits under a construct whose header is already
          unique — permitted only where the sentence still says what it is about afterwards.
      (iii) DELETE the occurrence, if it states a bound it should not be stating at all.
    FORBIDDEN, ALL FOUR: widening the census to permit more than one producer; exempting a token from
    the census; appending an ordinal to launder the ambiguity; and folding the line NUMBER into the key.
    The last is the tempting one and it is the worst — it would make every key unique by construction,
    pass the census having measured nothing, and break on every unrelated edit below it. If a disposal
    changes a header, EVERY key anchored to that header is regenerated with `exemptionKeyFor` in the
    same commit and its reason carried across byte-unchanged.

    ANY NEW OR EDITED PROSE IS SUBJECT TO THE GUARD IT SITS UNDER. The header region is on the scanned
    surface. If a disambiguating clause raises a new obligation, dispose of it by the guard's own
    three-choice rule — never by minting an exemption for this wave's own text and never by stretching
    an exclusion over it. Take any declared phrasing by index from `UNBOUNDED_QUANTIFIERS`; never spell
    one out.

    WATCH THE CENSUS FAIL BEFORE TRUSTING IT. After the disposals are green, plant a duplicate of one
    disambiguated header elsewhere in the file so its token has two producers again, run the suite,
    observe the census case RED, paste its FULL assertion message with the two producing line numbers
    in it, restore, observe green. WR-54 exists because a correction shipped without a pin; a census
    nobody has watched failing is the same artifact.

    COMMIT, THEN EXECUTE THE SECOND REAL-TREE RELOCATION ONCE. Paste `git log --oneline -1` first.
    Locate BY CONTENT the `var` twin cell under the CR-10 table header, move it out of that table and
    into the wave-34 logical-assignment table under its own header, paste the distance in lines and the
    two headers it moved between, run the suite, observe RED, paste the failing title and the FULL
    assertion message, restore, and prove the tree clean with `git diff --exit-code`. Cite
    `01-VERIFICATION.md` for the verifier's prior 434-of-434 GREEN on this exact move and present this
    session's RED as the contrast. If it does not go red, STOP, diagnose, and write it up as a
    first-order finding without adjusting the mutation to force a red.

    TOUCH NOTHING OUTSIDE SCOPE. `CLOSES_FROZEN_ARRAY`, `closingBracketAfter`, the `EXCLUSIONS` array,
    the bands, the `proof` tokens, `nameableRemainder`'s body, `NO_PRECEDING_CONSTRUCT`'s docblock,
    `preAnchoringExemptionKeyForFixtureOnly`'s body, `CORE11_BOX_EXPECTED`, the box case, limit (5) and
    the fixture's coverage sentence all stay byte-unchanged here. Nothing under `.planning/` is opened.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && pnpm exec vitest run packages/backend/src/outbound-prohibition.spec.ts 2>&1 | tail -8 && pnpm test 2>&1 | tail -10 && pnpm typecheck && pnpm lint && pnpm knip && pnpm build:backend && pnpm check:bundle && N=$(git status --porcelain .planning/ | grep -c . || true); [ "$N" -eq 0 ] && A=$(git diff --name-only -- packages/ scripts/ | grep -v '\.spec\.ts$' | grep -c . || true); [ "$A" -eq 0 ] && git diff --exit-code -- packages/backend/src/outbound-prohibition.spec.ts && echo TREE-CLEAN</automated>
  </verify>

  <acceptance_criteria>
    - The precondition asserted and pasted, including Task 1's case present BY TITLE and
      `git rev-parse HEAD` beside any status snapshot.
    - The census's truncation width and ellipsis shown by diff to be DERIVED from the builder rather
      than repeated as literals, and the derivation quoted in the SUMMARY.
    - The census's skip rule for empty-`nameableRemainder` tokens present in a comment and quoted, with
      the non-vacuity assertions above the rule pasted and their failure messages quoted.
    - The uniqueness asserted over BOTH the declared map's construct halves and the surface-produced
      construct halves, shown by diff, with one sentence on why asserting both prevents one side from
      drifting alone.
    - The census's FULL flagged output pasted: per flagged anchor, its token, every producing line
      number, and every map key hanging off it. NO CRITERION HERE NAMES AN EXPECTED COUNT.
    - The flagged set reconciled against the verifier's execution — two tokens, four keys, 10 producers
      and 3 producers — with any disagreement recorded as a finding in the shape `01-27-SUMMARY.md`
      uses rather than absorbed.
    - Every flagged anchor disposed of with its choice named as (i), (ii) or (iii) and its reason
      recorded, one row per anchor.
    - Every key anchored to a header a disposal changed shown regenerated with `exemptionKeyFor` in the
      same commit, with its reason shown byte-unchanged by diff and the entry count still exactly 29
      unless a disposal deleted an occurrence — in which case the new count is pasted with the deletion
      named.
    - The four forbidden routes asserted absent numerically over the diff:
      `N=$(git diff -- packages/backend/src/outbound-prohibition.spec.ts | grep -cE '\+.*(toBeLessThanOrEqual\(2|censusExempt|lineNumber\}|:\$\{lineNumber|# *\$\{n\})' || true); [ "$N" -eq 0 ]`, with the
      command and its output pasted, plus a one-line human verdict per route.
    - The census WATCHED FAILING: a duplicate of a disambiguated header planted, the case observed RED,
      its FULL assertion message pasted showing both producing line numbers, restored, green observed.
    - Any obligation raised by new or edited header prose enumerated and disposed of by the guard's own
      three-choice rule, with the choice and reason recorded, and an explicit statement that no
      exemption was minted for this wave's own text and no exclusion was stretched over it.
    - REAL-TREE RELOCATION executed AFTER commit: `git log --oneline -1` pasted first; the cell located
      BY CONTENT with its line pasted; the two table headers it moved between quoted; the distance in
      lines pasted; the suite observed RED with the failing title and FULL assertion message; restored;
      `git diff --exit-code` clean; the verifier's prior 434-of-434 cited as the contrast.
    - If the relocation did NOT go red, the task stopped, the diagnosis is written up as a first-order
      finding, and no adjustment was made to force a red.
    - `git diff` shown EMPTY over `CLOSES_FROZEN_ARRAY`, `closingBracketAfter`, `EXCLUSIONS`, the bands,
      the `proof` tokens, `nameableRemainder`'s body, `NO_PRECEDING_CONSTRUCT`'s docblock,
      `preAnchoringExemptionKeyForFixtureOnly`'s body, `CORE11_BOX_EXPECTED`, the box case, limit (5)
      and the coverage sentence.
    - `git status --porcelain .planning/` asserted EMPTY numerically.
    - Executed `pnpm test` exit 0 with counts pasted, no fewer than 31 files and 1374 tests;
      `pnpm typecheck`, `pnpm lint`, `pnpm knip` all exit 0; `pnpm build:backend && pnpm check:bundle`
      exactly one specifier, `crypto`; the real-tree walk 23 modules / 0 violations.
  </acceptance_criteria>

  <done>
    Every anchor in use is asserted to be produced by exactly one line of the file; what the census
    flagged was measured rather than predicted and disposed of one anchor at a time by a rule that
    never widens the census; the census has been watched failing; and the verifier's `:305` relocation
    was executed on the real tree and watched going RED where it had been green at 434 of 434.
  </done>
</task>

<task type="auto">
  <name>Task 3: Both relocations as permanent fixtures — the own-header shape as a second array pair in the cross-construct case and the ambiguous-token shape as a census fixture — then limit (5), `constructAnchorFor`'s docblock and the coverage sentence restated ONCE to what executes</name>

  <precondition>
    Tasks 1 and 2 have executed and committed. Assert and paste that `pnpm test` exits 0, that the gate
    suite is green at Task 2's recorded count, that both new cases from Tasks 1 and 2 are present BY
    TITLE, and that `git status --porcelain packages/ scripts/ .planning/` is clean. HALT if any is red.
    The numbers this task writes into limit (5) and into the coverage sentence come from THIS TASK's own
    re-measurement, not from Task 1's or Task 2's pasted tables and not from the verifier's report.
  </precondition>

  <read_first>
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md`, the `missing:` entry
      for WR-52 and the report section `## Where I DISAGREE with the reviewer, stated plainly`. That
      section REFUTES the reviewer's stronger reading by execution — the verifier applied the line-copy
      reduction and the fixture FAILED by name — so this task WIDENS a real fixture and must not be
      written as if it were rebuilding a vacuous one.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-REVIEW.md`, `### WR-52`, `### WR-49`
      and `### WR-53`. WR-49 measured four occurrences anchoring to line 1 of the file from up to 234
      lines away and prescribes replacing the docblock's `FINEST construct enclosing the occurrence`
      with what executes, plus stating the measured maximum in limit (5). WR-53 prescribes restating the
      forward walk's bound if it was not changed. Both sentences live in the docblock this task
      restates, so both are in scope HERE by the no-overclaim prohibition rather than as separate
      findings, and the SUMMARY must say so in exactly those terms.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the cross-construct fixture, re-located by
      its `it(...)` title. At HEAD `b8c3d81` its coverage sentence was `:9998-10001`, its own-header
      exclusion comment `:10019-10021`, its two synthetic arrays just below that, its probe assertions
      and its counter-probe at `:10126-10152`. Tasks 1 and 2 moved all of them. Read the whole case
      before touching it: the `keysUnder` helper, the `discharge` helper, the non-vacuity assertions,
      and how the phrasing is taken from `UNBOUNDED_QUANTIFIERS` by index.
    - `packages/backend/src/outbound-prohibition.spec.ts` — limit (4) and limit (5) in the guard's
      docblock, re-located by their numbering. At HEAD `b8c3d81` limit (4) was `:8998-9027` and limit
      (5) `:9029-9040`. Read limit (4) as the MODEL for how this file states a residual honestly: it
      names the coarseness, names what narrows it, and states measured numbers with the wave that
      measured them.
    - `packages/backend/src/outbound-prohibition.spec.ts` — `constructAnchorFor`'s docblock in its
      post-Task-1 form, every sentence of it.
    - `01-36-SUMMARY.md`'s account of adding limit (5), so the restatement supersedes a known text
      rather than replacing something whose history is unread.
  </read_first>

  <files>packages/backend/src/outbound-prohibition.spec.ts</files>

  <reversibility rating="reversible">Two synthetic fixture additions and three prose restatements in a spec file. No shipped code, no behaviour, no ledger byte, no exclusion. Fully revertible.</reversibility>

  <action>
    ADD THE OWN-HEADER SHAPE AS A SECOND ARRAY PAIR IN THE SAME CASE, WHICH IS WR-52's PRESCRIPTION
    VERBATIM. The existing case's first pair deliberately picks an occurrence that is NOT its own
    construct header, and says so in its own comment — precisely the shape CR-20(a) breaks. Add a
    second pair in the same case where the occurrence IS the construct header: the same phrasing
    carried by an `it(` title line under two different enclosing constructs, and separately a phrasing
    carried by the FIRST CONTENT LINE of a docblock under two different enclosing declarations. Assert
    the same things the first pair asserts — the keys DIFFER, the difference is in the construct half,
    the line half is byte-identical, `missing` and `stale` are both non-empty, and the count still
    balances. Keep the counter-probe's contrast intact for the new pair too. Build every phrasing from
    `UNBOUNDED_QUANTIFIERS` BY INDEX; never spell one out.

    ADD THE AMBIGUOUS-TOKEN SHAPE AS ITS OWN FIXTURE, over synthetic lines, in the same style. A
    constructed array with TWO identically-headed constructs and the same occurrence line under each:
    assert the two occurrences produce the SAME anchor, and assert the census reports that anchor as
    having two producers. That is the pair that makes the census meaningful — a census that only ever
    sees unique tokens is consistent with a census that cannot count.

    WATCH BOTH NEW ASSERTIONS FAIL BEFORE TRUSTING THEM. For the own-header pair, apply the line-copy
    reduction the verifier used — `constructAnchorFor` reduced to a masked normalized copy of the
    occurrence's own line — run the case by name, observe RED, paste the FULL assertion message,
    restore, observe green. For the census fixture, drop the second producing line from the synthetic
    array, observe RED, paste the message, restore. Each mutation is planted only after this task's own
    work is committed, and each is restored with `git diff --exit-code` shown clean.

    RESTATE THE COVERAGE SENTENCE TO THE SPLIT IT ACTUALLY COVERS, AND MEASURE THE SPLIT HERE. Count,
    in this session, how many shipped occurrences on the scanned surface sit ON their own construct
    header — an `it(` title line or a docblock's first content line — and how many do not. Paste both
    numbers and the total. The report quotes a 26/3 split; that is a RECONCILIATION ANCHOR from the
    verifier's execution before Tasks 1 and 2, not a target — re-derive it and record any disagreement
    as a finding. Write the restated sentence to name both numbers and to say which array pair covers
    which, and to state plainly that the fixture's probe side was REFUTED as vacuous by the verifier's
    own execution, so what was wrong was the coverage claim and not the fixture.

    RESTATE LIMIT (5) TO WHAT EXECUTES, ONCE, AND ONLY NOW THAT BOTH MEASUREMENTS EXIST. Its current
    text states flatly that an exemption written for one construct is not discharged by an occurrence
    under a different one, and states the whole residual as same-construct interchangeability. The
    verifier executed two CROSS-construct relocations that the first sentence forbids and the second
    does not describe. The new text must state, with the numbers measured in this round: what the two
    changes remove; that two occurrences under the SAME construct remain interchangeable behind a
    positional ordinal assigned by scan order rather than by line; the MEASURED MAXIMUM DISTANCE from
    an occurrence to its anchor, so the interchangeability residual is quantified rather than implied
    — WR-49 measured 234 lines and this task re-measures it after Tasks 1 and 2; and that the anchoring
    does not widen limit (1)'s phrase list, does not change limit (2)'s normalization, and reaches none
    of the surfaces limit (3) leaves out. Follow limit (4)'s shape: name the coarseness, name what
    narrows it, and date the measurement to this wave.

    RESTATE `constructAnchorFor`'s DOCBLOCK TO WHAT THE CODE NOW DOES, SENTENCE BY SENTENCE. Two of its
    sentences are false today and the reviewer measured both. It claims the anchor is the FINEST
    construct enclosing the occurrence, and WR-49 measured four occurrences taking the file's title
    line from up to 234 lines away — replace that with the nearest preceding line the recognisers
    accept, naming that for a contiguous comment region or a ruled banner that is the region's FIRST
    line, and giving the measured maximum. It describes a forward walk bounded by the CONSTRUCT, and
    WR-53 measured the bound as the OCCURRENCE — if Task 1 changed that bound, describe the new bound;
    if Task 1 did not, restate the sentence to the bound that executes. Also bring the docblock's
    enumerated recogniser list into agreement with the code, since IN-40 recorded them drifting apart.
    STATE IN THE SUMMARY, in these words, that WR-49 and WR-53 are handled HERE as consequences of the
    no-overclaim prohibition on a docblock this task is already restating, and NOT as separately
    adjudicated findings — pass 9 did not put them in its `missing:` list and this plan does not claim
    to close them.

    RE-READ EVERY SENTENCE THIS TASK WRITES AGAINST THE NO-OVERCLAIM PROHIBITION AND RECORD A VERDICT
    PER SENTENCE. Three of this round's six items are overclaim corrections; the ninth instance of this
    defect arrived inside the fix for the eighth. Any sentence that cannot be backed by a measurement
    pasted in this round's SUMMARIES is rewritten before it ships, and both its before and its after
    are pasted.

    TOUCH NOTHING OUTSIDE SCOPE. `CLOSES_FROZEN_ARRAY`, `closingBracketAfter`, `EXCLUSIONS`, the bands,
    the `proof` tokens, `nameableRemainder`'s body, `NO_PRECEDING_CONSTRUCT`'s docblock,
    `preAnchoringExemptionKeyForFixtureOnly`'s body, `CORE11_BOX_EXPECTED` and the box case all stay
    byte-unchanged. Nothing under `.planning/` is opened. If new prose trips wave 33's guard, REWRITE
    THE PROSE.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && pnpm exec vitest run packages/backend/src/outbound-prohibition.spec.ts 2>&1 | tail -8 && pnpm test 2>&1 | tail -10 && pnpm typecheck && pnpm lint && pnpm knip && pnpm build:backend && pnpm check:bundle && N=$(git status --porcelain .planning/ | grep -c . || true); [ "$N" -eq 0 ] && A=$(git diff --name-only -- packages/ scripts/ | grep -v '\.spec\.ts$' | grep -c . || true); [ "$A" -eq 0 ] && git diff --exit-code -- packages/backend/src/outbound-prohibition.spec.ts && echo TREE-CLEAN</automated>
  </verify>

  <acceptance_criteria>
    - The precondition asserted and pasted, with both prior cases confirmed present BY TITLE.
    - The own-header pair present as a SECOND array pair INSIDE the existing cross-construct case —
      shown by diff to be an addition to that case and not a new one — covering both the `it(` title
      shape and the docblock-first-content-line shape, with the same assertion set as the first pair
      and the counter-probe contrast preserved.
    - The own-header pair WATCHED FAILING under the verifier's own line-copy reduction of
      `constructAnchorFor`, run BY NAME, with the FULL assertion message pasted, restored, green
      observed.
    - The census fixture present as a named case over synthetic lines, asserting the two occurrences
      share an anchor and that the census reports two producers for it, and WATCHED FAILING with the
      second producing line dropped, FULL message pasted, restored.
    - Every synthetic phrasing shown taken from `UNBOUNDED_QUANTIFIERS` by index rather than written
      out, with the whole-file guard run green afterwards and its output pasted.
    - The own-header / not-own-header split RE-MEASURED in this session with both numbers and the total
      pasted, reconciled against the report's 26/3, and any disagreement recorded as a finding rather
      than absorbed.
    - The restated coverage sentence quoted verbatim, naming both measured numbers, naming which array
      pair covers which, and stating that the fixture's probe side was refuted as vacuous by the
      verifier's execution so the defect was the coverage claim and not the fixture.
    - Limit (5) restated ONCE and quoted verbatim, containing: what the two changes remove; the
      same-construct residual named as still unguarded; the MEASURED maximum occurrence-to-anchor
      distance re-derived in this session and pasted; and the explicit statement that the anchoring does
      not widen limit (1), does not change limit (2) and reaches none of limit (3)'s surfaces.
    - `constructAnchorFor`'s docblock restated and quoted verbatim, with the `FINEST construct` claim
      replaced by what executes and the forward walk's bound stated as it actually is after Task 1, and
      the enumerated recogniser list shown to agree with the code by a pasted side-by-side.
    - The SUMMARY states in these words that WR-49 and WR-53 are handled here as consequences of the
      no-overclaim prohibition on a docblock already being restated, and are NOT claimed as closed
      findings.
    - A one-line no-overclaim verdict recorded for EVERY sentence this task added or changed, with the
      before and after pasted for any sentence that was rewritten as a result.
    - `git diff` shown EMPTY over `CLOSES_FROZEN_ARRAY`, `closingBracketAfter`, `EXCLUSIONS`, the bands,
      the `proof` tokens, `nameableRemainder`'s body, `NO_PRECEDING_CONSTRUCT`'s docblock,
      `preAnchoringExemptionKeyForFixtureOnly`'s body, `CORE11_BOX_EXPECTED` and the box case.
    - `git status --porcelain .planning/` asserted EMPTY numerically; `01-PROBE.md`'s equality
      `38 == 27 + 11` re-stated in the SUMMARY as unchanged, with the statement that this plan resolves
      no flagged assumption and surfaces no new one; `COVERAGE.md` confirmed unedited and its
      declaration confirmed to stand because this round integrates no external API.
    - Executed `pnpm test` exit 0 with counts pasted, no fewer than 31 files and 1374 tests;
      `pnpm typecheck`, `pnpm lint`, `pnpm knip` all exit 0; `pnpm build:backend && pnpm check:bundle`
      exactly one specifier, `crypto`; the real-tree walk 23 modules / 0 violations; CORE-11's first
      sentence byte-identical by diff.
  </acceptance_criteria>

  <done>
    Both relocation shapes exist as permanent fixtures that have been watched failing; the coverage
    sentence names the split it actually covers, re-measured in this session; and limit (5) and
    `constructAnchorFor`'s docblock state what executes, with a per-sentence no-overclaim verdict behind
    every sentence written.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an anchor that is a LINE OF TEXT to one that identifies a SITE | A 64-character masked line repeats, and a line that is its own construct header anchors to itself. Seven of 29 entries relocate invisibly across that boundary and the verifier drove two shipped occurrences through it at 434/434 green. |
| a change that is PRESCRIBED to a change whose SUFFICIENCY is measured | Opening the scan one line higher is necessary. Whether it is sufficient depends on the forward walk's bound, which WR-53 measured and which no diff review can settle. Asserting sufficiency instead of measuring it is the defect this round exists to remove. |
| a fix inside the existing scan to a REBUILD of the mechanism | An AST- or frame-derived identity was considered and rejected for this round. A parse arriving under the name of a bug fix is a scope change nobody approved, in the one file the whole phase turns on. |
| a census that COUNTS to one that is unique by construction | Folding the line number into the key would pass the census having measured nothing and would break on every unrelated edit below it. The census must be able to fail, and it must have been seen failing. |
| a guard that was CHANGED to one that was WATCHED FAILING | WR-54 exists precisely because round 8 shipped a correction with no pin. Every assertion added here is planted against and observed RED before it is trusted. |
| a fixture's STATED coverage to its EXECUTED coverage | Round 8 built a real fixture and described it as covering the very shape it deliberately excluded. The coverage sentence is re-measured here rather than re-worded. |
| a disclosure that DESCRIBES the fix to one that states its reach | Limit (5) states the opposite of what executes, inside the paragraph written to state the reach of the fix for that exact defect. Two levels deep, ninth instance. |
| the file's own bytes to the surface it guards | The guard scans this file. New prose and new fixtures raise the obligations they exist to test unless every declared phrasing is taken by index from the list. |
| a test-only gate to the shipped bundle | Nothing in this plan reaches shipped code. `git diff --name-only` over `packages/` and `scripts/` filtered of `.spec.ts` is asserted EMPTY at every task boundary, and `check:bundle` is re-run regardless. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-233 | Spoofing | an exemption discharged by the same masked text sitting anywhere | critical | mitigate | The backward scan opens strictly above the occurrence, the prefix property is measured across all 29 keys before and after, and a permanent case forbids a construct half that is a prefix of its line half — watched failing under a reverted scan before it is trusted. |
| T-01-234 | Spoofing | an anchor token produced by ten lines naming none of them | critical | mitigate | A census over every line's would-be token in the builder's own form, with each anchor in use — from the map AND from the surface — asserted to have exactly one producer, watched failing against a planted duplicate header. |
| T-01-235 | Tampering | the outbound gate weakened while its anchoring is strengthened | high | mitigate | Four routes forbidden outright in frontmatter and asserted absent by a numeric grep over the diff: the line number folded into a key, an ordinal appended, the census widened past one producer, a token exempted from the census. |
| T-01-236 | Tampering | a mechanism rebuild arriving as a bug fix | high | mitigate | No parser, no tokenizer, no AST dependency and no `it`/`describe` frame derivation; asserted absent by a numeric grep over the diff and by `pnpm knip` at every task boundary. The extension available to Task 1 is WR-53's own prescribed fix inside the same line scan. |
| T-01-237 | Repudiation | a sufficiency claim that was argued rather than executed | high | mitigate | Task 1 pastes the 29-key prefix table BEFORE the change and AFTER it, and the decision to extend the forward walk is taken from that table rather than from the diff. No criterion names an expected value. |
| T-01-238 | Repudiation | an expected count baked into the census | high | mitigate | No acceptance criterion names a flagged-anchor count. The verifier's numbers are labelled RECONCILIATION ANCHORS, disagreement is a finding, and each flagged anchor is disposed of by a named choice with a recorded reason. |
| T-01-239 | Repudiation | a pin nobody watched failing | high | mitigate | Four mutations, each planted after commit and each observed RED with its FULL assertion message pasted: the reverted backward scan, the duplicated header, the line-copy reduction, the dropped producing line — plus the two real-tree relocations. |
| T-01-240 | Tampering | a real-tree mutation planted over uncommitted work | high | mitigate | Every task commits before planting, pastes `git log --oneline -1` first, and proves the restore with `git diff --exit-code`. Wave 29 destroyed its own Task 1 exactly this way. |
| T-01-241 | Spoofing | an ambiguity disposed of by renaming rather than by naming | medium | mitigate | The three permitted disposals are stated, the fourth class is forbidden, each disposal records which choice it took and why, and any header a disposal changes forces regeneration of every key anchored to it with its reason carried across byte-unchanged. |
| T-01-242 | Tampering | new prose raising the obligation it exists to test | medium | mitigate | Every declared phrasing in new code and new prose is taken from `UNBOUNDED_QUANTIFIERS` by index; the whole-file guard is run green at each boundary; any obligation raised is disposed of by the guard's own rule and never by minting an exemption for this wave's own text. |
| T-01-243 | Repudiation | a tenth overclaim written inside the fix for the ninth | high | mitigate | An `open`-status DISCLOSED-NOT-CLOSED prohibition in frontmatter, a per-sentence no-overclaim verdict required by Task 3, and limit (5) restated exactly ONCE, at the end, after both measurements exist. |
| T-01-214 | Tampering | CORE-11's checkbox moved | critical | mitigate | Prohibited. This plan opens no file under `.planning/`; `git status --porcelain .planning/` is asserted EMPTY numerically at every task boundary and `requirements mark-complete` appears in no command. Carried forward from plan 01-36. |
| T-01-212 | Information Disclosure | the two reader surfaces no mechanism reaches | medium | accept | `.planning/STATE.md` and `.planning/WINDOWS.md` are governed by the pointer-not-a-bound prohibition with no mechanical check. Untouched by this plan. Carried forward from T-01-144 unchanged. |
| T-01-51 | Information Disclosure | the accepted residual of the outbound walk | medium | mitigate | Unchanged by this plan — no registry row moves — and byte-pinned into the ledger by the two comparisons, re-run green at every task boundary. |
| T-01-50 | Information Disclosure | `.spec.ts` files, which the walk skips | low | accept | Unchanged and carried forward, bounded by `pnpm check:bundle`, which specs never enter. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | NOT APPLICABLE: this plan installs no package. `pnpm install` is not run and no manifest appears in `files_modified`. RESEARCH.md's Package Legitimacy Audit is present and unchanged; no `[ASSUMED]` or `[SUS]` package is introduced, so no blocking human checkpoint is required. |
</threat_model>

<verification>
1. Arrival baseline asserted and pasted before any edit: HEAD, clean status over `packages/`, `scripts/`
   and `.planning/`, gate-suite count, whole-suite counts at exit 0, `tsc --build` exit 0,
   `check:bundle` specifier set. HALT recorded if any was red.
2. The 29-key prefix table pasted BEFORE the change, with the count of prefix-carrying keys stated and
   reconciled against three.
3. The backward scan shown by diff to open at `lineNumber - 1`.
4. The 29-key prefix table pasted AFTER the change, with the remaining count stated and no expected
   value named anywhere.
5. The forward-walk extension taken or declined ON THAT MEASUREMENT, with the diff pasted either way,
   and — if taken — an explicit statement that it is WR-53's prescribed fix inside the same line scan.
6. `NO_PRECEDING_CONSTRUCT` occurrence counts pasted before and after, and the after value handed to
   plan 01-40 in the SUMMARY in those words.
7. All 29 entries rebuilt with `exemptionKeyFor`, reasons byte-unchanged, count asserted at 29, count
   equality still balancing; the three formerly self-anchoring keys pasted before and after.
8. The prefix-forbidding case present by title, its failure message quoted, and WATCHED FAILING under a
   reverted scan with the FULL message pasted.
9. The numeric grep over the diff for a parser, an AST dependency, a folded line number and a new
   ordinal asserted at zero, command and output pasted.
10. The `:1838` real-tree relocation executed after commit as a SWAP, RED observed, title and FULL
    message pasted, restored, `git diff --exit-code` clean, the verifier's prior 434/434 cited.
11. If either real-tree relocation did not go red, the task stopped and the diagnosis is a first-order
    finding with no adjustment made to force a red.
12. The census's width and ellipsis shown DERIVED from the builder; its skip rule quoted; its
    non-vacuity assertions pasted.
13. Uniqueness asserted from BOTH the declared map and the surface, shown by diff.
14. The census's flagged output pasted in full — token, producing lines, keys — with no expected count
    named, reconciled against the verifier's two tokens and four keys, disagreement recorded as a
    finding.
15. Every flagged anchor disposed of with its choice named (i)/(ii)/(iii) and its reason recorded; every
    key anchored to a changed header regenerated with its reason byte-unchanged.
16. The four forbidden routes asserted absent numerically and given a one-line human verdict each.
17. The census WATCHED FAILING against a planted duplicate header, FULL message with both producing
    lines pasted, restored.
18. The `:305` real-tree relocation executed after commit, RED observed, title and FULL message pasted,
    the two headers quoted, restored clean, the verifier's prior 434/434 cited.
19. The own-header pair added INSIDE the existing cross-construct case, covering the `it(` title shape
    and the docblock-first-content-line shape, WATCHED FAILING under the line-copy reduction run by
    name.
20. The census fixture present over synthetic lines and WATCHED FAILING with a producing line dropped.
21. The own-header / not-own-header split re-measured this session, both numbers and the total pasted,
    reconciled against 26/3.
22. The coverage sentence restated and quoted verbatim, naming both numbers and recording that the
    probe side was refuted as vacuous.
23. Limit (5) restated ONCE and quoted verbatim, carrying the same-construct residual, the re-measured
    maximum occurrence-to-anchor distance, and the three non-widening statements.
24. `constructAnchorFor`'s docblock restated and quoted verbatim, the `FINEST construct` claim replaced,
    the forward walk's bound stated as it executes, the recogniser list shown to agree with the code.
25. The SUMMARY states that WR-49 and WR-53 are handled as no-overclaim consequences and NOT claimed as
    closed findings.
26. A per-sentence no-overclaim verdict recorded for everything Task 3 wrote, with before and after
    pasted for each rewrite.
27. Every declared phrasing in new code and new prose shown taken from `UNBOUNDED_QUANTIFIERS` by index;
    whole-file guard green with output pasted; no exemption minted for this wave's own text.
28. `git diff` EMPTY over `CLOSES_FROZEN_ARRAY`, `closingBracketAfter`, `EXCLUSIONS`, the bands, the
    `proof` tokens, `nameableRemainder`'s body, `NO_PRECEDING_CONSTRUCT`'s docblock,
    `preAnchoringExemptionKeyForFixtureOnly`'s body, `CORE11_BOX_EXPECTED` and the box case.
29. `git status --porcelain .planning/` asserted EMPTY numerically at every task boundary;
    `requirements mark-complete` absent from every command run.
30. `01-PROBE.md`'s equality `38 == 27 + 11` re-stated unchanged; `COVERAGE.md` confirmed unedited and
    its declaration confirmed to stand.
31. `pnpm test` exit 0, no fewer than 31 files / 1374 tests; `typecheck`, `lint`, `knip` exit 0;
    `build:backend && check:bundle` exactly one specifier, `crypto`; real-tree walk 23 modules / 0
    violations; CORE-11's first sentence byte-identical.
32. Every prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses, or an
    explicit statement that there were none.
</verification>

<success_criteria>
- An occurrence sitting on its own construct header no longer anchors to itself, and that is established
  by a prefix table over all 29 keys measured before and after rather than by reading the diff.
- Whether the one-line change was sufficient was DECIDED BY THAT MEASUREMENT, and any extension taken
  was WR-53's own prescribed fix inside the existing line scan — no parser, no AST, no frame.
- The self-anchoring shape is forbidden by a permanent case that has been watched failing.
- Every anchor in use is asserted to be produced by exactly one line of the file, and the census has
  been watched failing.
- What the census flagged was MEASURED — enumerated with its producers and its keys, disposed of one at
  a time by a named choice with a recorded reason — and no number was predicted anywhere in this plan.
- The census was not satisfied by making keys unique by construction; the four forbidden routes are
  asserted absent by a numeric grep and by a human verdict each.
- Both of the verifier's relocations were executed on the real tree and watched going RED where each had
  been green at 434 of 434, and both exist as permanent fixtures over synthetic lines.
- The fixture's coverage sentence names the split it actually covers, re-measured this session, and
  records that its probe side was refuted as vacuous rather than found so.
- Limit (5) and `constructAnchorFor`'s docblock state what executes, once, with a per-sentence
  no-overclaim verdict behind every sentence — and the SUMMARY says plainly that removing two relocation
  shapes does not close the relocation class.
- CORE-11's checkbox did not move in either direction, `requirements mark-complete` was not run, and no
  file under `.planning/` was opened.
- The green baseline is preserved and RE-MEASURED rather than assumed: no fewer than 31 files / 1374
  tests, typecheck / lint / knip clean, bundle at exactly one specifier, real tree at 23 modules / 0
  violations, and nothing leaks.
</success_criteria>

<artifacts_this_phase_produces>
| Artifact | Kind | Purpose |
|----------|------|---------|
| the strictly-above backward scan | corrected mechanism | The verifier's change (1): an occurrence sitting on a title or a docblock's first content line resolves to the construct enclosing THAT, instead of to a 64-character prefix of itself |
| the 29-key prefix table, before and after | new evidence | The measurement that decides whether change (1) was sufficient, taken against the unmodified file and again against the changed one, with no expected value named |
| the forward-walk bound and continue-fallback, IF the measurement required it | corrected mechanism | WR-53's own prescribed fix inside the same line scan — taken only on evidence, never in advance, and never as a parse |
| the 29 regenerated exemption keys | corrected artifact | Every entry rebuilt with `exemptionKeyFor` against the new anchor, with every pre-existing reason carried across byte-unchanged |
| the prefix-forbidding case | new mechanism | A permanent assertion that no key's construct half is a prefix of its line half, watched failing under a reverted scan |
| the anchor uniqueness census | new mechanism | Every line's would-be token counted in the builder's own form, with each anchor in use — from the map and from the surface — asserted to have exactly one producer |
| the flagged-anchor enumeration and its disposal record | new evidence | What the census flagged, with each token's producing lines and dependent keys, and the choice and reason for each disposal — measured, never predicted |
| the two executed real-tree relocations | new evidence | `:1838` moved 5,264 lines and `:305` moved between two identically-headed tables, each watched going RED where the verifier had them green at 434 of 434, each restored clean |
| the own-header array pair inside the cross-construct fixture | new mechanism | WR-52's prescription: the shape the round-8 fixture deliberately excluded, added to the same case and watched failing under the line-copy reduction |
| the ambiguous-token census fixture | new mechanism | Two identically-headed synthetic constructs sharing one anchor, with the census asserted to report two producers — so a census that cannot count fails loudly |
| the re-measured own-header split | new evidence | How many shipped occurrences sit on their own construct header and how many do not, counted this session rather than carried from the report's 26/3 |
| the restated coverage sentence | corrected disclosure | A fixture that states the coverage it has, recording that its probe side was refuted as vacuous by execution |
| the restated limit (5) | corrected disclosure | The residual as it executes: the same-construct interchangeability, the measured maximum occurrence-to-anchor distance, and the three things the anchoring does not do |
| the restated `constructAnchorFor` docblock | corrected disclosure | The `FINEST construct` claim replaced by what the recognisers do, the forward walk's bound stated as it is, and the recogniser list brought back into agreement with the code |
| `01-39-SUMMARY.md` | new evidence | Both measurements, both disposals, four mutation REDs, two real-tree relocation REDs, the full gate set, and the sentinel-count handoff to plan 01-40 |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-skeleton-persistence-compatibility/01-39-SUMMARY.md` when done.

It must contain, as pasted terminal output rather than as prose: the arrival baseline with
`git rev-parse HEAD`; the 29-key prefix table BEFORE and AFTER with the two counts; the diff hunk for
the backward scan and, if taken, for the forward walk and the continue-fallback; the
`NO_PRECEDING_CONSTRUCT` counts before and after; the three formerly self-anchoring keys before and
after with the differing portion identified; the prefix case's title and its RED message under a
reverted scan; the numeric grep proving no parser, no AST dependency, no folded line number and no new
ordinal; the census's derivation of width and ellipsis from the builder; the census's FULL flagged
output with producing lines and dependent keys; the disposal record, one row per anchor with its choice
and reason; the census's RED against a planted duplicate header with both producing lines in the
message; both real-tree relocations with `git log --oneline -1` first, the located line, the distance,
the swap totals for the first, the two headers for the second, the RED title and FULL message for each,
the restore and a clean `git diff --exit-code`; the own-header pair's RED under the line-copy reduction
run by name; the census fixture's RED with a producing line dropped; the re-measured own-header split
with both numbers and the total; the restated coverage sentence, limit (5) and `constructAnchorFor`
docblock quoted verbatim; the per-sentence no-overclaim verdict table; the EMPTY diffs over every
out-of-scope construct listed in the verification block; the EMPTY `git status --porcelain .planning/`;
and the final `pnpm test`, `typecheck`, `lint`, `knip`, `check:bundle` and real-tree walk results.

It must state in ONE sentence whether change (1) alone was sufficient, and if not, exactly which
additional bound was taken and why that is WR-53's fix rather than a rebuild.

It must state in ONE sentence the count of occurrences resolving to `NO_PRECEDING_CONSTRUCT` after this
plan, labelled as a HANDOFF FACT for plan 01-40.

It must state in ONE sentence that WR-49 and WR-53 were handled as no-overclaim consequences of a
docblock this plan was already restating, and are NOT claimed as closed findings.

It must state in ONE sentence that removing two relocation shapes does not close the relocation class,
naming the same-construct residual, the measured maximum anchor distance, the guard's phrase-list reach
and both classes of unreached surface.

It must state in ONE sentence that CORE-11's box was not moved in either direction, that
`requirements mark-complete` was not run, and that no file under `.planning/` was opened.
</output>
