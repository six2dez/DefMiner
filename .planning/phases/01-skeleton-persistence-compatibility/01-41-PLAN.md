---
phase: 01-skeleton-persistence-compatibility
plan: 41
type: execute
wave: 41
gap_closure: true
depends_on: [01-40]
files_modified:
  - packages/backend/src/outbound-prohibition.spec.ts
# THIS PLAN CLOSES ROUND 9's LAST TWO ITEMS AND THEN RE-MEASURES THE ROUND'S OWN BASELINE.
#
# WR-54 + WR-50 — THE RECOGNISER IS PINNED BY NOTHING AT ALL. WARNING, FOUND BY THE VERIFIER'S OWN
# EXECUTION RATHER THAN BY THE REVIEWER. Round 8 corrected `closingBracketAfter` so exclusion three stops
# 117 lines short of where it used to stop — a real fix, honestly measured, and the verifier re-counted
# all nine declared phrasings across the 117 restored lines and got ZERO, confirming the docblock's own
# figure. Then it reverted `CLOSES_FROZEN_ARRAY` at `:9762` to its pre-fix form `/^\]\);$/`,
# re-introducing WR-48's EXACT over-walk, AND THE SUITE STAYED GREEN AT 434 OF 434. Exclusion three's
# WIDTH is currently pinned by nothing.
#
# AND THE CLOSING SENTENCE AT `:9758-9761` NAMES THREE MECHANISMS AND NOT ONE OF THEM HOLDS IN THE
# OVER-WALK DIRECTION. The verifier states each, measured:
#   - the `proof` token (`:9840`, `gateLines.slice(from-1, to).some(l => l.includes(proof))`) is MONOTONE
#     IN RANGE WIDTH. A wider range still contains the token. It cannot detect an over-walk, by
#     construction.
#   - the `[500, 3000]` band ALREADY FAILED on this exact defect: WR-48's over-walk was 1,750 lines,
#     comfortably inside the band.
#   - and — sharpening the reviewer's WR-50, which named the clause-count equality as "what caught it,
#     and what would catch the next one" — the CLAUSE-COUNT EQUALITY WOULD NOT HAVE CAUGHT THIS ONE
#     EITHER, because the 117 swallowed lines carry zero phrasings and zero clauses, so nothing moves.
# There is no third mechanism to name in place of the two the sentence names. The fix is to ASSERT THE
# RESOLVED RANGE DIRECTLY and then to write a sentence that names what actually holds it.
#
# THE VERIFIER GAVE THE ASSERTION IN ITS OWN WORDS:
#   `expect(closingBracketAfter(registryStart)).toBe(lineOf(l => l.startsWith('] as readonly ResolverRecord[]);')))`
#   — or pin the size. This plan takes the FIRST form for both exclusions that use `closingBracketAfter`,
#   because pinning the size alone would go green again the day the construct legitimately grows.
#
# WINDOWS 41 / P38-D2 — ROUND 8's OWN DISCLOSURE, AND THE GATE FILE IS OPEN AGAIN. Criterion (3)(d)'s
# re-run came back NON-CLEAN and said so before anyone looked. `:803-804` reads that CORE-11's box stays
# `[ ]` and attributes the flip to a wave 28 that never owned it; `:901` says the same, unmarked and
# present-tense. Ten lines below `:901`, `:911` asserts the box's state is stated in exactly two places.
# The verifier counted the sites that state it — `:803`, `:901`, `CORE11_BOX_EXPECTED` and
# `REQUIREMENTS.md:46`'s checkbox — and found FOUR, ALL AGREEING ON `[ ]`. So there is NO CONTRADICTION
# OF FACT here and this is NOT what keeps the box at `[ ]`. It is a false standing statement on a reader
# surface, which is the class criterion (3) is about. Round 8 recorded it rather than repairing it
# because the gate file was closed after 01-37, and the verifier calls that the right call.
#
# THE PRESCRIBED DISPOSITION IS A CHOICE AND EITHER BRANCH IS CORRECT. Mark the two occurrences as
# history IN THEIR OWN BYTES, per P38-D1's line-granularity rule; or delete them under wave 33's
# pointer-not-a-bound disposition. Then change the count claim at `:911` to the MEASURED count, or to a
# form that does not state a count at all.
#
# WHAT THIS PLAN MUST NOT DO.
#   - DO NOT FLIP CORE-11's CHECKBOX AND DO NOT REVERT IT EITHER. It reads `[ ]`, `CORE11_BOX_EXPECTED`
#     agrees, and verification pass 9's determination is that it may not move. Whether it may move is
#     pass 10's call. `requirements mark-complete` stays UNRUN, exactly as plan 01-38's P38-D3 left it.
#     This plan opens NO file under `.planning/` — the ledger row and `.planning/STATE.md` are not
#     touched, and the count this plan writes must therefore be stated at the reach of the surfaces this
#     plan can see, with the ones it cannot see NAMED.
#   - DO NOT RE-OPEN THE 2026-08-25 RE-SCOPE.
#   - DO NOT WIDEN THE EXCLUSION OR ITS BAND TO ACCOMMODATE THE NEW ASSERTION. If the direct range
#     assertion disagrees with the resolved range, the disagreement IS THE FINDING and it is written up
#     before anything is adjusted. Narrowing is the only permitted direction and no band, no `proof`
#     token and no pinned count is moved to make anything green.
#   - DO NOT LOOSEN `CLOSES_FROZEN_ARRAY` WHILE PINNING IT. Its reach is deliberately narrow and IN-38
#     recorded that its stated reach already omits two of its three real constraints. This plan pins
#     what it resolves to; it does not extend what it recognises.
#   - DO NOT TOUCH plan 01-39's or 01-40's work: `constructAnchorFor`, `nameableRemainder`, the
#     sentinel and its docblock, the census, the prefix-forbidding case, limit (5), the cross-construct
#     fixture, the counter-probe pin, or any regenerated exemption key.
#   - DO NOT WRITE A TENTH OVERCLAIM WHILE FIXING THE NINTH. Both items in this plan ARE overclaim
#     corrections. The replacement sentence at `:9758-9761` and the replacement for the count claim at
#     `:911` are the two most likely places in this whole round to author the next one, and each gets a
#     per-sentence verdict backed by a pasted measurement.
#
# ENVIRONMENT FACTS, MEASURED DURING PLANNING.
#   - HEAD at planning time: `b8c3d81`; the gate file 10,399 lines. Plans 01-39 and 01-40 edit the same
#     file, so RE-DERIVE every number by identifier or by content and record any disagreement as a
#     finding rather than adopting either silently.
#   - RECONCILIATION ANCHORS at HEAD `b8c3d81`: the closing sentence `:9758-9761`; `CLOSES_FROZEN_ARRAY`
#     `:9762`; `closingBracketAfter` `:9763-9764`; the `EXCLUSIONS` array `:9766-9805` with
#     `to: closingBracketAfter(listStart)` at `:9789` and `to: closingBracketAfter(registryStart)` at
#     `:9797`; the band-and-proof case `:9817-9848` with the `proof` assertion at `:9840`;
#     `CORE11_BOX_EXPECTED` `:10311`; the three WINDOWS 41 lines `:803`, `:804`, `:901` and the count
#     claim at `:911`.
#   - MEASURED CONSTRUCT ENDPOINTS AT HEAD `b8c3d81`, ALL FOUR CONFIRMED THIS SESSION:
#     `export const UNBOUNDED_QUANTIFIERS` opens at `:5959` and its close is the exact line `]);` at
#     `:5969`. `export const RESOLVER_REGISTRY` opens at `:4135` and its real closing line is
#     `] as readonly ResolverRecord[]);` at `:5767`. Exclusion three therefore resolves 4135..5767 =
#     1633 lines today, matching limit (4)'s restated figure to the line. The pre-fix recogniser landed
#     at `:5884`, the close of `BRANCH_VOCABULARY`, which is declared at `:5789`.
#   - THE THREE WINDOWS 41 SITES AND THE FOURTH THE VERIFIER COUNTED: `:803`, `:901`,
#     `CORE11_BOX_EXPECTED` at `:10311`, and `REQUIREMENTS.md:46`'s checkbox. FOUR, ALL AGREEING ON
#     `[ ]`. This plan can edit only the first two and can only READ the third; the fourth is outside
#     `files_modified` entirely and is READ-ONLY here.
#   - NO ACTIVE GIT HOOKS. COMMIT BEFORE MUTATING — wave 29 destroyed its own Task 1 with
#     `git checkout --`.
#   - BASELINE TO RE-MEASURE RATHER THAN ASSUME, AND THIS PLAN OWNS THE ROUND'S FINAL RE-MEASUREMENT:
#     whole suite 31 files / 1374 tests exit 0; `tsc --build` exit 0; `check:bundle` exactly ONE
#     specifier, `crypto`; real-tree walk 23 shipped non-spec modules across both `SOURCE_ROOTS` / 0
#     violations; the gate suite's own count as plans 01-39 and 01-40 left it.
#   - EVERY `<automated>` BLOCK OPENS WITH `set -o pipefail` AND EVERY COUNT IS ASSERTED NUMERICALLY.
#     Round 8 shipped a revision because `pnpm test 2>&1 | tail -8` exits with TAIL's status. Do not
#     strip either, and run the blocks as written.

autonomous: true
requirements: [CORE-11]

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 2
  confidence: low
# `estimate-calibration` reports `{"factor": 1, "applied": false, "sample_count": 0, "min_samples": 3}`,
# so `tokens` equals `raw_tokens` and the confidence is DERIVED from the sample count, never self-rated.
# Weigh the MEASUREMENT COUNT over the token figure: two direct range assertions with the verifier's own
# revert watched going RED, one closing sentence replaced against three measured refutations, a
# three-site sweep with the sites re-counted after it, and the round's closing baseline re-measured
# rather than carried.

must_haves:
  truths:
    - "EXCLUSION THREE'S RESOLVED END IS ASSERTED DIRECTLY AGAINST THE CONSTRUCT'S OWN CLOSING LINE, NOT INFERRED FROM A BAND. A named assertion pins `closingBracketAfter(registryStart)` to the line the registry actually closes on, located by its own text, and the same is done for the exclusion that uses `closingBracketAfter` over the quantifier list. The size band stays as it is and is NOT the mechanism — pinning the size alone goes green the day the construct legitimately grows."
    - "THE VERIFIER'S OWN REVERT IS WATCHED GOING RED. `CLOSES_FROZEN_ARRAY` is reverted to its pre-fix form, re-introducing WR-48's exact 117-line over-walk, which stayed GREEN at 434 of 434 before this plan. The new assertion must turn RED, with its FULL message pasted, the restore made, and the tree proved clean. WR-54 exists precisely because a correction shipped without a pin, so this plan does not ship one either."
    - "THE CLOSING SENTENCE NAMES WHAT ACTUALLY HOLDS THE EXCLUSION, AND SAYS WHY THE THREE PREVIOUS CANDIDATES DO NOT. The replacement states that the `proof` token is MONOTONE IN RANGE WIDTH and cannot detect an over-walk by construction; that the band did not catch 1,750 lines because 1,750 sits inside it; and that the clause-count equality would not have caught it either, because the swallowed lines carried zero clauses so nothing moved. Then it names the new direct assertion as what does hold it, and states that reach and no wider."
    - "`CLOSES_FROZEN_ARRAY`'s RECOGNISED FORMS ARE NOT EXTENDED WHILE ITS RESOLUTION IS PINNED. Its reach is deliberately narrow, and IN-38 already recorded that its stated reach omits constraints it has. This plan asserts what it resolves to; it does not widen what it matches. `git diff` over the regex itself is EMPTY at the end of the task."
    - "IF THE DIRECT ASSERTION DISAGREES WITH THE RESOLVED RANGE, THE DISAGREEMENT IS THE FINDING. No band is moved, no `proof` token is changed, no pinned count is widened and no exclusion is stretched to make anything green. Narrowing is the only permitted direction, exactly as wave 37 established."
    - "THE TWO UNMARKED STANDING STATEMENTS ARE SWEPT IN THEIR OWN BYTES, BY ONE OF THE TWO PRESCRIBED DISPOSITIONS, AND THE CHOICE IS RECORDED. Either marked as history at line granularity per P38-D1, or deleted under wave 33's pointer-not-a-bound disposition. Both attribute a flip to a wave 28 that never owned it, so a disposition that leaves the attribution standing is not one of the two."
    - "THE COUNT CLAIM IS REPLACED BY SOMETHING TRUE AT THE REACH OF A PASTED MEASUREMENT. The sites are RE-COUNTED after the sweep, in this session, and the replacement either states that measured number or states no number at all. It must also name the surfaces this plan cannot see — `.planning/REQUIREMENTS.md:46`'s checkbox and `.planning/STATE.md` are outside `files_modified` and no mechanism in this file counts them — because a count stated wider than the grep that produced it is the ninth defect wearing a tenth mask."
    - "THE SWEEP DOES NOT CREATE A NEW STANDING STATEMENT WHILE REMOVING TWO. Any replacement prose that itself states the box's state is a new site, and the re-count must include it. The safest form points at the authoritative surfaces and asserts nothing, which is the disposition wave 33 took for this exact class."
    - "THE ROUND'S CLOSING BASELINE IS RE-MEASURED RATHER THAN CARRIED FORWARD. The whole suite's file and test counts, `tsc --build`, `check:bundle`'s specifier set, the gate suite's own count and the real-tree walk over both `SOURCE_ROOTS` are all executed in this session and pasted, and each is compared against the arrival figures — 31 files / 1374 tests, exit 0, one specifier `crypto`, 23 modules / 0 violations, 434 in the gate suite before this round began — with every difference explained by a change this round made."
    - "THE MUST-NOT DOES NOT MOVE AND NOTHING LEAKS. CORE-11's first sentence byte-identical by diff; `git diff --name-only` over `packages/` and `scripts/` filtered of `.spec.ts` EMPTY for the whole round; every defect this round touched is in a test-only gate's enforcement of its own description."
    - "NOTHING UNDER `.planning/` IS TOUCHED BY THIS PLAN. CORE-11's checkbox does not move in either direction, `requirements mark-complete` is not run, `01-PROBE.md`'s equality `38 == 27 + 11` stands unchanged and is re-stated, and `COVERAGE.md` is neither edited nor extended because this round integrates no external API."
    - "EVERY PREDICTION-VS-MEASUREMENT DISCREPANCY IS RECORDED RATHER THAN ABSORBED, in the shape `01-27-SUMMARY.md` uses. This plan predicts exclusion three resolving 4135..5767 = 1633 and exclusion two resolving 5959..5969, and predicts that the sites stating the box's state number four before the sweep. It predicts NOTHING about the count after the sweep, which is measured."

  artifacts:
    - path: "packages/backend/src/outbound-prohibition.spec.ts"
      provides: "Direct assertions pinning both `closingBracketAfter` resolutions to their constructs' own closing lines located by text; a closing sentence that names why the `proof` token, the band and the clause-count equality each fail in the over-walk direction and names the new assertion as what holds it; the two unmarked standing statements at `:803-804` and `:901` swept by a recorded disposition; a replacement for the count claim at `:911` that is true at the reach of a pasted grep and names the surfaces this file cannot see; and the round's closing baseline re-measured"
      contains: "closingBracketAfter"

  key_links:
    - from: "an exclusion whose width is pinned by nothing"
      to: "an exclusion whose end is asserted against the construct's own closing line"
      via: "a direct `toBe` against the line located by `] as readonly ResolverRecord[]);`, and the same for the quantifier list, with the verifier's own revert watched going RED"
      pattern: "closingBracketAfter"
    - from: "a sentence naming three mechanisms that do not hold"
      to: "a sentence naming the one that does, and why the other three do not"
      via: "monotonicity of the `proof` check in range width, the band's measured failure on 1,750 lines, and the clause-count equality's measured blindness to lines carrying zero clauses"
      pattern: "pinned by their"
    - from: "two unmarked present-tense statements attributing a flip to a wave that never owned it"
      to: "two sites disposed of by a recorded choice"
      via: "P38-D1's line-granularity history marking, or wave 33's pointer-not-a-bound deletion, with the attribution removed either way"
      pattern: "wave 28"
    - from: "a count claim that measures four"
      to: "a claim true at the reach of the grep that produced it"
      via: "the sites re-counted after the sweep and the replacement stating that measured number or no number, with the surfaces outside this file NAMED"
      pattern: "CORE11_BOX_EXPECTED"

  prohibitions:
    - requirement_id: CORE-11
      category: security
      status: resolved
      verification: gate
      resolution: "`git diff` over `CLOSES_FROZEN_ARRAY`, over every band, over every `proof` token and over both pinned counts shown EMPTY; any disagreement between the direct assertion and the resolved range written up as a finding before anything is adjusted"
      statement: "MUST NOT widen an exclusion, a band, a proof token or a pinned count to accommodate the new assertion. Narrowing is the only permitted direction, exactly as wave 37 established. A pin adjusted to fit the number it was supposed to catch is decoration that reports green."
    - requirement_id: CORE-11
      category: security
      status: resolved
      verification: gate
      resolution: "`git diff` over the `CLOSES_FROZEN_ARRAY` regex asserted EMPTY at the end of Task 1"
      statement: "MUST NOT loosen the recogniser while pinning its resolution. Its reach is deliberately narrow and IN-38 already recorded that its stated reach omits two of its three real constraints. Extending what it matches, under cover of asserting what it resolves to, would widen an exclusion in the same commit that claims to pin it."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "The verifier's pre-fix regex is planted after commit and the new assertion observed RED with its FULL message pasted, restored, tree clean"
      statement: "MUST NOT ship a pin that has never been watched failing. WR-54 exists because round 8's correction shipped without one, and it went green under the exact revert it was written to prevent. A pin nobody has watched failing is the same unwatched assertion this file has spent ten waves removing."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "The sites are re-counted after the sweep and the replacement states the measured number or no number; the surfaces outside `files_modified` are named in the replacement text and in the SUMMARY"
      statement: "MUST NOT state a count wider than the grep that produced it. Two of the four sites the verifier counted are outside this plan's `files_modified` and no mechanism in this file counts them. A replacement claim that silently includes them is the same defect the claim it replaces committed."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "The sweep's replacement prose is included in the post-sweep re-count and the resulting number pasted; a disposition that leaves the wave-28 attribution standing is recorded as rejected"
      statement: "MUST NOT create a new standing statement while removing two. Replacement prose that itself asserts the box's state is a new site and must be counted as one. The disposition that points at the authoritative surfaces and asserts nothing is the one wave 33 took for this class, and it is available here."
    - requirement_id: CORE-11
      category: safety
      status: open
      verification: gate
      resolution: "DISCLOSED, NOT CLOSED — the two remaining exclusions' coarseness, the recogniser's narrow reach, the guard's phrase-list reach, its normalization, both classes of unreached surface and the two reader surfaces no mechanism reaches are named wherever this plan's result is stated"
      statement: "MUST NOT state or imply that pinning one resolution makes the exclusions exact, that sweeping two sentences discharges criterion (3), or that round 9 closes CORE-11. One width is pinned and two false statements are swept. Exclusion three is still a LINE RANGE wider than the clause strings it stands for; a frozen array closed in some third form is still unrecognised and would still be walked past; `.planning/STATE.md` and `.planning/WINDOWS.md` are still governed by prose with no mechanical check; and whether the box may move is verification pass 10's determination and not this plan's."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "`git status --porcelain .planning/` shown EMPTY numerically at every task boundary; `requirements mark-complete` present in no command this plan runs"
      statement: "MUST NOT flip CORE-11's checkbox and MUST NOT revert it either. It reads `[ ]`, the pin agrees, round 8 did not move it, and verification pass 9 determined it may not move. Plan 01-38's P38-D3 left `requirements mark-complete` unrun and handed the determination onward; that pattern is preserved."
---

<objective>
Pin the recogniser that WR-48's correction left unpinned, replace the sentence that names three
mechanisms none of which hold, sweep round 8's two disclosed standing statements and correct the count
claim above them — then re-measure this round's closing baseline rather than carrying it.

Purpose: round 8 corrected `closingBracketAfter` so exclusion three stops at the registry's own closing
line instead of 117 lines past it. The correction is right and the verifier re-counted all nine
phrasings across the restored lines to confirm it. Then it reverted the regex to its pre-fix form,
re-introducing the exact over-walk, and the suite stayed GREEN at 434 of 434. The `proof` token is
monotone in range width and cannot detect an over-walk; the band already failed on 1,750 lines; and the
clause-count equality would not have caught it either, because the swallowed lines carry zero clauses.
Three named mechanisms, three measured refutations, and no fourth to name — so the range gets asserted
directly.

Then WINDOWS 41, which round 8 disclosed against itself before anyone looked. Two unmarked present-tense
statements attribute CORE-11's flip to a wave 28 that never owned it, and ten lines below one of them a
sentence claims the box's state is stated in exactly two places where the verifier counted four, all
agreeing. No contradiction of fact — so this is not what keeps the box at `[ ]` — but a false standing
statement on a reader surface is precisely the class criterion (3) is about. The gate file was closed
when round 8 found it; it is open now.

Output: direct assertions pinning both `closingBracketAfter` resolutions to their constructs' own
closing lines, with the verifier's revert watched going RED; a closing sentence that names why each of
the three previous candidates fails in the over-walk direction and names what holds it now; the two
statements swept by a recorded disposition with the false attribution gone; a replacement count claim
true at the reach of a pasted grep, naming the surfaces this file cannot see; and the round's closing
baseline executed in this session and compared against arrival.

WHAT THIS PLAN DOES NOT DO. It does not widen an exclusion, a band, a proof token or a pinned count —
if the direct assertion disagrees with the resolved range, the disagreement is the finding. It does not
extend what `CLOSES_FROZEN_ARRAY` recognises while pinning what it resolves to. It does not touch plan
01-39's or plan 01-40's work. It does not flip or revert CORE-11's checkbox, does not run
`requirements mark-complete`, does not re-open the 2026-08-25 re-scope, and opens no file under
`.planning/`. And it does not claim that pinning one width and sweeping two sentences discharges
criterion (3) — whether the box may move is verification pass 10's determination.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md
@.planning/phases/01-skeleton-persistence-compatibility/01-37-SUMMARY.md
@.planning/phases/01-skeleton-persistence-compatibility/01-38-SUMMARY.md
@packages/backend/src/outbound-prohibition.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-54 and WR-50 — both `closingBracketAfter` resolutions asserted directly against their constructs' own closing lines, the verifier's revert watched going RED, and the closing sentence replaced against three measured refutations</name>

  <precondition>
    Plans 01-39 and 01-40 have executed and committed all five of their tasks, and their SUMMARYs exist
    on disk. Before the first edit assert and PASTE: `git rev-parse HEAD`;
    `git status --porcelain packages/ scripts/ .planning/` clean; the gate suite green at plan 01-40's
    recorded count; `pnpm test` exit 0 with file and test counts; `tsc --build` exit 0; `check:bundle`'s
    specifier set. HALT if any is red. NO ACTIVE GIT HOOKS: run the gate set by hand at the boundary.
  </precondition>

  <read_first>
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md` — the `artifacts:`
      entry for WR-54, the `missing:` entry beginning `WR-54 + WR-50: PIN THE RECOGNISER ITSELF`, and
      the report body section `## One finding neither pass 8 nor the reviewer has: WR-54`. The
      `missing:` entry gives the assertion in the verifier's own words and it is the shape this task
      takes.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-REVIEW.md`, `### WR-50` and `### IN-38`.
      WR-50 supplies the monotonicity argument about the `proof` check and the band's measured failure;
      the verifier SHARPENS it by adding that the clause-count equality would not have caught this one
      either. IN-38 records that `CLOSES_FROZEN_ARRAY`'s stated reach omits two of its three real
      constraints — read it so the replacement sentence does not repeat that omission, and so nobody is
      tempted to widen the regex.
    - `01-37-SUMMARY.md`'s WR-48 half in full: the before and after ranges, the 117 lines identified as
      `BRANCH_VOCABULARY`'s docblock and declaration, the `inRange == inClauses` numbers before and
      after, and the reasoning for choosing a recogniser change over bracket matching. This task pins
      that work; it must know exactly what it is pinning.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the closing sentence, `CLOSES_FROZEN_ARRAY`,
      `closingBracketAfter`, `lineOf`, the whole `EXCLUSIONS` array, the band-and-proof case with its
      `proof` assertion, and the clause-count equality case. RECONCILIATION ANCHORS at HEAD `b8c3d81`
      were `:9758-9761`, `:9762`, `:9763-9764`, `:9766-9805`, `:9817-9848` and `:9840`. PLANS 01-39 AND
      01-40 MOVED ALL OF THEM. Re-derive each by identifier and record any disagreement as a finding.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the registry's opening line
      (`export const RESOLVER_REGISTRY`, `:4135`), its real closing line
      (`] as readonly ResolverRecord[]);`, `:5767`), `BRANCH_VOCABULARY`'s declaration (`:5789`) and the
      exact `]);` at `:5884` the pre-fix recogniser landed on; and the quantifier list's opening
      (`export const UNBOUNDED_QUANTIFIERS`, `:5959`) and close (`]);`, `:5969`). All four re-located by
      content, never by the number.
    - `01-37-SUMMARY.md`'s and `01-39-SUMMARY.md`'s mutation records for the commit-plant-observe-restore
      shape this task's revert must take.
  </read_first>

  <files>packages/backend/src/outbound-prohibition.spec.ts</files>

  <reversibility rating="reversible">Two assertions added to an existing case and one comment replaced, in a spec file. No shipped code, no behaviour, no exclusion boundary moved, no ledger byte. The planted revert is restored inside the task with `git diff --exit-code` shown clean.</reversibility>

  <behavior>
    - `closingBracketAfter` applied to the registry's opening line resolves to the line located by
      `] as readonly ResolverRecord[]);` and to no other.
    - `closingBracketAfter` applied to the quantifier list's opening line resolves to that list's own
      closing line and to no other.
    - With `CLOSES_FROZEN_ARRAY` reverted to its pre-fix form, the registry assertion FAILS.
    - The two locator lines each match exactly one line of the file, asserted before they are used, so a
      locator that resolves to nothing or to the wrong construct cannot make the pin pass vacuously.
    - No band, no `proof` token and no pinned count changes value.
  </behavior>

  <action>
    MEASURE THE RESOLVED RANGES BEFORE CHANGING ANYTHING, AND PASTE THEM. For all three exclusions,
    paste `from`, `to`, the size and the band as they resolve right now, plus the registry's opening
    line number, its real closing line number, the line `closingBracketAfter` currently lands on, and
    the same three for the quantifier list. Reconcile against 4135 / 5767 / 1633 and 5959 / 5969. A
    disagreement is a FINDING, recorded before anything else — plans 01-39 and 01-40 edited this file
    and any shift in these numbers has a cause worth naming.

    ASSERT BOTH RESOLUTIONS DIRECTLY, IN THE FORM THE VERIFIER GAVE. Pin
    `closingBracketAfter(registryStart)` to the line located by the registry's own closing text, and
    pin `closingBracketAfter(listStart)` to the quantifier list's own closing line. Use a locator over
    the line's text rather than a hard-coded number, so the pin survives the file growing and fails when
    the resolution moves. ASSERT NON-VACUITY BEFORE THE RULE: each locator must be shown to match
    exactly ONE line of the file, with a failure message saying that a locator matching zero lines would
    make the pin pass having compared nothing — the failure mode this whole file is organised against.

    DO NOT PIN THE SIZE INSTEAD. The verifier offered pinning the size as an alternative; this plan
    takes the direct form because a size pin goes green again the day the construct legitimately grows,
    and the defect being fixed is precisely a width that moved without anything noticing. Leave the
    existing bands exactly as they are — they are a coarse sanity check and this task neither widens nor
    tightens them. State that choice and its reason in one sentence.

    DO NOT TOUCH WHAT THE RECOGNISER MATCHES. `CLOSES_FROZEN_ARRAY`'s reach is deliberately narrow and
    IN-38 recorded that its stated reach already omits constraints it has. This task pins what it
    RESOLVES TO. `git diff` over the regex itself is EMPTY at the end.

    IF THE DIRECT ASSERTION DISAGREES WITH THE RESOLVED RANGE, THE DISAGREEMENT IS THE FINDING. Do not
    move a band, do not change a `proof` token, do not widen a pinned count and do not stretch an
    exclusion. Write it up, and narrow if narrowing is what the measurement calls for — that is the only
    permitted direction and wave 37 established it.

    COMMIT, THEN PLANT THE VERIFIER'S OWN REVERT AND WATCH IT GO RED. Paste `git log --oneline -1`
    first. Revert `CLOSES_FROZEN_ARRAY` to its pre-fix form so the scan walks past the registry's real
    closing line and lands 117 lines later, exactly as WR-48 described. This stayed GREEN at 434 of 434
    for the verifier. Run, observe RED, paste the failing title and the FULL assertion message, and
    paste the two line numbers the message reports so a reader can see the 117-line gap in the output
    itself. Restore, and prove the tree clean with `git diff --exit-code`. Cite `01-VERIFICATION.md` for
    the prior green and present this session's RED as the contrast. If it does not go red, STOP,
    diagnose, and write it up as a first-order finding without adjusting the mutation to force a red.

    REPLACE THE CLOSING SENTENCE, AND MAKE IT DO THE WORK THE OLD ONE ONLY CLAIMED. The old text names
    the `proof` tokens and the bands as pinning these two exclusions. Write a replacement that states,
    each with the measurement behind it: that the `proof` check asks only whether the token appears
    ANYWHERE inside the resolved range, which is monotone in range width and therefore cannot detect an
    over-walk by construction; that the band did not catch WR-48 because 1,750 lines sits inside
    500..3000; and that the clause-count equality would not have caught it either, because the swallowed
    lines carried zero declared phrasings and zero clauses so neither side of the equality moved — a
    number the verifier obtained by counting all nine phrasings across all 117 lines. Then name the new
    direct assertion as what holds the resolution, and STATE ITS REACH AND NO WIDER: it pins where the
    scan lands for these two constructs, and it does nothing about a frozen array closed in some third
    form, which the recogniser still would not match and would still be walked past.

    RE-READ EVERY SENTENCE THIS TASK WRITES AGAINST THE NO-OVERCLAIM PROHIBITION AND RECORD A VERDICT
    PER SENTENCE. This replacement is itself an overclaim correction, in the paragraph whose previous
    version was an overclaim. It is one of the two most likely places in this round to author the next
    one.

    TOUCH NOTHING OUTSIDE SCOPE. `constructAnchorFor`, `nameableRemainder`, the sentinel and its
    docblock, the census, the prefix-forbidding case, limit (5), the cross-construct fixture, the
    counter-probe pin, every regenerated exemption key, `CORE11_BOX_EXPECTED`, the box case and the
    header lines at `:803-804`, `:901` and `:911` all stay byte-unchanged — the last three are Task 2's.
    Nothing under `.planning/` is opened. If new prose trips wave 33's guard, REWRITE THE PROSE.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && pnpm exec vitest run packages/backend/src/outbound-prohibition.spec.ts 2>&1 | tail -8 && pnpm test 2>&1 | tail -10 && pnpm typecheck && pnpm lint && pnpm knip && pnpm build:backend && pnpm check:bundle && R=$(git diff -- packages/backend/src/outbound-prohibition.spec.ts | grep -cE '^[+-].*CLOSES_FROZEN_ARRAY = ' || true); [ "$R" -eq 0 ] && N=$(git status --porcelain .planning/ | grep -c . || true); [ "$N" -eq 0 ] && A=$(git diff --name-only -- packages/ scripts/ | grep -v '\.spec\.ts$' | grep -c . || true); [ "$A" -eq 0 ] && git diff --exit-code -- packages/backend/src/outbound-prohibition.spec.ts && echo TREE-CLEAN</automated>
  </verify>

  <acceptance_criteria>
    - The precondition asserted and PASTED, with `git rev-parse HEAD` beside any status snapshot and
      both prior plans' SUMMARYs confirmed present. HALT recorded if anything was red.
    - All three exclusions' `from`, `to`, size and band pasted BEFORE the change, plus the registry's
      opening line, its real closing line, the line `closingBracketAfter` lands on, and the same three
      for the quantifier list — reconciled against 4135 / 5767 / 1633 and 5959 / 5969, with any
      disagreement recorded as a finding.
    - Both direct assertions present, shown by diff to use a TEXT LOCATOR rather than a hard-coded line
      number, with the non-vacuity assertion that each locator matches exactly ONE line pasted and its
      failure message quoted.
    - An explicit one-sentence statement of why the direct form was taken over pinning the size, with
      `git diff` over both bands shown EMPTY.
    - `git diff` over the `CLOSES_FROZEN_ARRAY` regex asserted EMPTY at the end of the task, by the
      numeric check in the verify block, with its output pasted.
    - `git diff` shown EMPTY over every `proof` token, over both pinned counts and over exclusion one.
    - Any disagreement between the direct assertion and the resolved range written up as a FINDING with
      an explicit statement that no band, `proof` token or pinned count was moved to accommodate it.
    - THE VERIFIER'S REVERT planted AFTER commit: `git log --oneline -1` pasted first; the regex reverted
      to its pre-fix form; the suite observed RED; the failing title and the FULL assertion message
      pasted, including the two line numbers that show the 117-line gap; restored;
      `git diff --exit-code` clean; `01-VERIFICATION.md` cited for the prior 434-of-434 green with this
      session's RED as the contrast.
    - If the revert did NOT go red, the task stopped, the diagnosis is written up as a first-order
      finding, and no adjustment was made to force a red.
    - The replacement closing sentence quoted VERBATIM and shown to contain all four elements: the
      `proof` check's monotonicity in range width; the band's measured failure at 1,750 inside
      500..3000; the clause-count equality's measured blindness because the swallowed lines carried zero
      clauses; and the new direct assertion named as what holds the resolution, with its reach stated
      and the third-form close named as still unrecognised.
    - A one-line no-overclaim verdict recorded for EVERY sentence added or changed, with before and
      after pasted for any rewrite.
    - `git diff` shown EMPTY over `constructAnchorFor`, `nameableRemainder`, the sentinel and its
      docblock, the census, the prefix-forbidding case, limit (5), the cross-construct fixture, the
      counter-probe pin, every exemption key, `CORE11_BOX_EXPECTED`, the box case and lines `:803-804`,
      `:901`, `:911`.
    - `git status --porcelain .planning/` asserted EMPTY numerically.
    - Executed `pnpm test` exit 0 with counts pasted, no fewer than 31 files and 1374 tests;
      `typecheck`, `lint`, `knip` exit 0; `build:backend && check:bundle` exactly one specifier,
      `crypto`; real-tree walk 23 modules / 0 violations; CORE-11's first sentence byte-identical.
    - Any prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses, or an
      explicit statement that there were none.
  </acceptance_criteria>

  <done>
    Both `closingBracketAfter` resolutions are asserted directly against their constructs' own closing
    lines, located by text and non-vacuously; the verifier's revert that stayed green at 434 of 434 was
    planted and watched going RED; and the closing sentence names why the `proof` token, the band and the
    clause-count equality each fail in the over-walk direction, names what holds the resolution now, and
    states that reach and no wider.
  </done>
</task>

<task type="auto">
  <name>Task 2: WINDOWS 41 — the two unmarked standing statements swept in their own bytes, the count claim replaced by something true at the reach of a pasted grep, and the round's closing baseline re-measured</name>

  <precondition>
    Task 1 has executed and committed. Assert and paste before the first edit: `git rev-parse HEAD`;
    `git status --porcelain packages/ scripts/ .planning/` clean; the gate suite green at Task 1's
    recorded count; `pnpm test` exit 0 with counts. HALT if any is red. THE GATE FILE IS OPEN — round 8
    recorded these three sites rather than repairing them because the file was closed after plan 01-37,
    and the verifier called that the right call. It is open now and this task is the repair.
  </precondition>

  <read_first>
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md` — the `artifacts:`
      entry for WINDOWS 41 / P38-D2, the `missing:` entry beginning `WINDOWS 41: SWEEP`, and the report
      body section `## Round 8's two disclosures against itself — weighed as disclosures`. Note
      carefully what the verifier concluded: FOUR sites, ALL AGREEING ON `[ ]`, no contradiction of
      fact, and therefore NOT what keeps the box at `[ ]`. This task must not describe it as more than
      it is.
    - `01-38-SUMMARY.md` in full, and in particular P38-D1's LINE-GRANULARITY rule for marking a
      superseded statement as history in its own bytes, and P38-D2's disclosure of these exact three
      sites. P38-D1 is one of the two permitted dispositions and its shape must be copied rather than
      reinvented.
    - `packages/backend/src/outbound-prohibition.spec.ts` `:795-815` and `:890-920` at HEAD `b8c3d81` —
      the two unmarked statements at `:803-804` and `:901`, the surrounding paragraphs they sit in, and
      the wave-33 note at `:905-915` whose closing clause carries the count claim at `:911`. Read the
      WHOLE of the wave-33 note before touching its count clause: it is a pointer-not-a-bound
      disposition that is otherwise correct and must survive intact.
    - `packages/backend/src/outbound-prohibition.spec.ts` — `CORE11_BOX_EXPECTED` and the box case, for
      the third site. READ-ONLY here: this task counts it and does not touch it.
    - `.planning/REQUIREMENTS.md:46` — the fourth site, READ-ONLY and outside `files_modified`. Read
      enough of round 8's repaired row to state accurately in the replacement text that this file cannot
      see it and no mechanism here counts it.
    - `01-39-SUMMARY.md` and `01-40-SUMMARY.md`, for the arrival and per-plan gate-suite counts this
      task's closing baseline must reconcile against.
  </read_first>

  <files>packages/backend/src/outbound-prohibition.spec.ts</files>

  <reversibility rating="reversible">Comment-text edits in the gate file's hand-written header, plus a re-measurement. No shipped code, no behaviour, no assertion logic, no ledger byte. Fully revertible.</reversibility>

  <action>
    COUNT THE SITES BEFORE SWEEPING ANYTHING, AND PASTE THE GREP THAT PRODUCED THE COUNT. Enumerate
    every place that STATES the box's state, across this file and across the surfaces the verifier
    counted, and paste each hit with its line and its text. Reconcile against FOUR — the two header
    statements, `CORE11_BOX_EXPECTED`, and the ledger row's checkbox — and record any disagreement as a
    finding. Label plainly which of the four this plan can EDIT, which it can only READ, and which is
    outside `files_modified` entirely.

    SWEEP THE TWO UNMARKED STATEMENTS BY ONE OF THE TWO PRESCRIBED DISPOSITIONS, AND RECORD THE CHOICE
    AND ITS REASON. Either mark each as history IN ITS OWN BYTES, at line granularity, in the shape
    P38-D1 established — a dated marker naming the wave that recorded it and the wave that swept it,
    with the superseded bytes left intact below the marker — or delete each under wave 33's
    pointer-not-a-bound disposition, the same choice wave 33 made for the paragraph that used to sit at
    this spot. BOTH ARE CORRECT AND THE CHOICE IS THE EXECUTOR'S; what is NOT permitted is a disposition
    that leaves the wave-28 flip attribution standing, because that attribution is false on its own
    terms — no wave 28 ever owned the flip — and it is the half of these two sentences that misleads a
    later planner most.

    DO NOT CREATE A NEW STANDING STATEMENT WHILE REMOVING TWO. If the disposition chosen writes
    replacement prose, and that prose itself asserts the box's state, it is a NEW SITE and must be
    counted as one below. The disposition that points at the authoritative surfaces and asserts nothing
    is available and is the one wave 33 took for this exact class.

    REPLACE THE COUNT CLAIM AT `:911` WITH SOMETHING TRUE AT THE REACH OF A PASTED GREP. Re-count the
    sites AFTER the sweep, in this session, and paste that grep. Then either state the measured number,
    or state no number at all and name the two AUTHORITATIVE surfaces instead. Whichever form is chosen,
    the sentence must NAME what this file cannot see: `.planning/REQUIREMENTS.md:46`'s checkbox is
    outside this file and outside this plan's `files_modified`, and `.planning/STATE.md` is governed by
    prose with no mechanical check. A count stated wider than the grep that produced it is the ninth
    defect wearing a tenth mask, and this clause is one of the two most likely places in the round to
    commit it. Leave the rest of the wave-33 note intact — its pointer-not-a-bound reasoning is correct
    and is not what is being corrected.

    STATE THE FINDING'S WEIGHT HONESTLY, IN THE BYTES AND IN THE SUMMARY. The verifier counted four
    sites all agreeing on `[ ]`, found no contradiction of fact, and said plainly that this is NOT what
    keeps the box at `[ ]`. Any sentence written here that implies otherwise is an overclaim in the
    opposite direction and it is just as false. What was wrong is that two statements were unmarked and
    present-tense with a false wave attribution, and that a third stated a count that measured four.

    RE-MEASURE THE ROUND'S CLOSING BASELINE RATHER THAN CARRYING IT. Execute and paste, in this session:
    `pnpm test` with its file and test counts; `pnpm exec tsc --build`; `pnpm check:bundle` with its
    specifier set; the gate suite alone with its count; and the real-tree walk over both `SOURCE_ROOTS`
    with its module count and violation count. Compare each against the round's ARRIVAL figures — 31
    files / 1374 tests, exit 0, one specifier `crypto`, 23 modules / 0 violations, and 434 in the gate
    suite before plan 01-39 — and account for every difference by naming the plan and the task that
    caused it. A test-count increase with no named cause is a finding.

    RE-ESTABLISH THAT NOTHING LEAKED, FOR THE WHOLE ROUND AND NOT JUST THIS PLAN. Paste
    `git diff --name-only <round-9 base>..HEAD -- packages/ scripts/` filtered of `.spec.ts` and assert
    it EMPTY numerically, so the round's entire code delta is shown to be one test file. Paste CORE-11's
    first sentence and show it byte-identical by diff.

    RE-STATE THE PROBE LEDGER AND THE COVERAGE DECLARATION WITHOUT EDITING EITHER. `01-PROBE.md`'s
    no-silent-drop equality stands at `38 == 27 + 11`; this round resolves no flagged assumption and
    surfaces no new one, so no row moves and the equality is re-stated rather than recomputed.
    `COVERAGE.md` is confirmed unedited and its declaration confirmed to stand, because this round
    integrates no external API. Run the assumption-delta detector; a guard-anchoring round is unlikely
    to fire a singular-to-plural transition, and if it does not fire, say so in one line rather than
    inventing a row.

    TOUCH NOTHING OUTSIDE SCOPE. Task 1's two direct assertions and its replacement sentence,
    `CLOSES_FROZEN_ARRAY`, `closingBracketAfter`, the `EXCLUSIONS` array, the bands, the `proof` tokens,
    `constructAnchorFor`, `nameableRemainder`, the sentinel and its docblock, the census, the
    prefix-forbidding case, limit (5), the cross-construct fixture, the counter-probe pin, every
    exemption key, `CORE11_BOX_EXPECTED` and the box case all stay byte-unchanged. Nothing under
    `.planning/` is opened. If new prose trips wave 33's guard, REWRITE THE PROSE.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && pnpm exec vitest run packages/backend/src/outbound-prohibition.spec.ts 2>&1 | tail -8 && pnpm test 2>&1 | tail -10 && pnpm typecheck && pnpm lint && pnpm knip && pnpm build:backend && pnpm check:bundle && P=$(grep -c 'EXACTLY TWO PLACES' packages/backend/src/outbound-prohibition.spec.ts || true); [ "$P" -eq 0 ] && W=$(grep -c 'wave 28 owns the flip' packages/backend/src/outbound-prohibition.spec.ts || true); [ "$W" -eq 0 ] && B=$(grep -cE '^  const CORE11_BOX_EXPECTED = "- \[ \] \*\*CORE-11\*\*";$' packages/backend/src/outbound-prohibition.spec.ts || true); [ "$B" -eq 1 ] && N=$(git status --porcelain .planning/ | grep -c . || true); [ "$N" -eq 0 ] && A=$(git diff --name-only -- packages/ scripts/ | grep -v '\.spec\.ts$' | grep -c . || true); [ "$A" -eq 0 ] && git diff --exit-code -- packages/backend/src/outbound-prohibition.spec.ts && echo TREE-CLEAN</automated>
  </verify>

  <acceptance_criteria>
    - The precondition asserted and pasted, with HEAD beside any status snapshot.
    - The BEFORE site enumeration pasted with the grep that produced it, every hit shown with its line
      and text, reconciled against FOUR, and each of the four labelled EDITABLE HERE, READ-ONLY HERE, or
      OUTSIDE `files_modified`.
    - The disposition chosen for each of the two unmarked statements NAMED as either P38-D1
      history-marking at line granularity or wave-33 pointer-not-a-bound deletion, with its reason
      recorded and the diff pasted.
    - The false wave-28 flip attribution shown GONE from both sites, asserted numerically by the verify
      block's `wave 28 owns the flip` count at zero, with the command and output pasted, plus a human
      read confirming no paraphrase of the attribution survives at either site.
    - The AFTER site re-count pasted with the grep that produced it, taken in this session, INCLUDING
      any replacement prose written by the sweep, with an explicit statement of whether the sweep
      created a new site.
    - The replacement for the count clause quoted VERBATIM, shown to state either the measured number or
      no number, and shown to NAME the surfaces this file cannot see —
      `.planning/REQUIREMENTS.md:46`'s checkbox and `.planning/STATE.md` — with the reason each is
      unreachable from here. The count claim asserted absent numerically by the verify block, command
      and output pasted.
    - The rest of the wave-33 note shown intact by diff, with its pointer-not-a-bound reasoning
      unchanged.
    - A sentence in the SUMMARY stating the finding's weight as the verifier stated it: four sites all
      agreeing on `[ ]`, no contradiction of fact, NOT what keeps the box at `[ ]` — and an explicit
      statement that nothing written here implies otherwise in either direction.
    - `CORE11_BOX_EXPECTED` asserted byte-unchanged and still reading `[ ]`, by the numeric check in the
      verify block, with the command and output pasted.
    - THE ROUND'S CLOSING BASELINE executed in this session and pasted: `pnpm test` file and test
      counts, `tsc --build`, `check:bundle`'s specifier set, the gate suite's count, and the real-tree
      walk's module and violation counts — each compared against the arrival figures (31 / 1374, exit 0,
      one specifier `crypto`, 23 / 0, gate suite 434) with every difference attributed to a named plan
      and task. Any unattributed difference recorded as a finding.
    - `git diff --name-only <round-9 base>..HEAD -- packages/ scripts/` filtered of `.spec.ts` asserted
      EMPTY numerically for the WHOLE ROUND, command and output pasted; CORE-11's first sentence pasted
      and shown byte-identical by diff.
    - `01-PROBE.md`'s equality `38 == 27 + 11` re-stated unchanged with the statement that this round
      resolves no flagged assumption and surfaces no new one; `COVERAGE.md` confirmed unedited and its
      declaration confirmed to stand; the assumption-delta detector run and its result stated in one
      line, including a plain "did not fire" if it did not.
    - A one-line no-overclaim verdict recorded for EVERY sentence added or changed.
    - `git diff` shown EMPTY over Task 1's assertions and replacement sentence, `CLOSES_FROZEN_ARRAY`,
      `closingBracketAfter`, `EXCLUSIONS`, the bands, the `proof` tokens, `constructAnchorFor`,
      `nameableRemainder`, the sentinel and its docblock, the census, the prefix-forbidding case, limit
      (5), the cross-construct fixture, the counter-probe pin, every exemption key and the box case.
    - `git status --porcelain .planning/` asserted EMPTY numerically; `requirements mark-complete`
      absent from every command run in the session.
    - Executed `pnpm typecheck`, `pnpm lint`, `pnpm knip` — all exit 0, output pasted.
  </acceptance_criteria>

  <done>
    The two unmarked present-tense statements are gone or marked as history in their own bytes with the
    false wave-28 attribution removed; the count claim above them is replaced by something true at the
    reach of a pasted grep that names the surfaces this file cannot see; and the round's closing baseline
    was executed in this session and reconciled against arrival with every difference attributed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a correction that is RIGHT to one that is PINNED | WR-48's fix is correct, honestly measured, and can silently revert. The verifier reverted the regex and the suite stayed green at 434 of 434 — the width is held by nothing at all. |
| a `proof` token to an over-walk | The check asks whether the token appears anywhere in the resolved range. That is monotone in range width, so a wider range still passes. It cannot detect this class of defect by construction, not by accident. |
| a band to a defect inside the band | The over-walk was 1,750 lines and the band is 500..3000. A sanity band is not a pin, and treating it as one is how WR-48 survived a wave. |
| a clause-count equality to lines carrying no clauses | The reviewer named the equality as what would catch the next one. Measured, it would not: the 117 swallowed lines carry zero phrasings and zero clauses, so neither side moves. |
| pinning WHAT IT RESOLVES TO versus WHAT IT MATCHES | Extending `CLOSES_FROZEN_ARRAY`'s recognised forms under cover of pinning its resolution would widen an exclusion in the commit that claims to pin it. |
| an unmarked present-tense statement to a marked historical one | Two header sentences state the box's state and attribute a flip to a wave that never owned it. A later planner reads a header before anything else, and this one is 8,000 lines above the pin it contradicts in attribution. |
| a count claim to the grep that produced it | The claim measures four where it says two. Its replacement can commit the identical defect one wave later if it counts surfaces this file cannot see. |
| a sweep that removes two statements to one that adds a third | Replacement prose asserting the box's state is a new site. The post-sweep re-count must include it, and the pointer-that-asserts-nothing disposition avoids the problem entirely. |
| a test-only gate to the shipped bundle | Nothing here reaches shipped code. The round's whole code delta is asserted to be one test file by a diff over the round's base. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-253 | Tampering | an exclusion width that can silently revert | high | mitigate | Both `closingBracketAfter` resolutions asserted directly against their constructs' own closing lines located by text, with non-vacuity proving each locator matches exactly one line — and the verifier's own revert planted and watched going RED where it was green at 434 of 434. |
| T-01-254 | Repudiation | three named mechanisms none of which hold | high | mitigate | The closing sentence replaced with the monotonicity argument, the band's measured failure at 1,750, and the clause-count equality's measured blindness — then the new direct assertion named as what holds it, with the third-form close named as still unrecognised. |
| T-01-255 | Tampering | the recogniser loosened while its resolution is pinned | high | mitigate | Forbidden in frontmatter; `git diff` over the regex asserted EMPTY by a numeric check in the verify block. IN-38 is read first precisely so the replacement sentence does not repeat its omission. |
| T-01-256 | Tampering | a band, proof token or pinned count moved to fit the measurement | high | mitigate | Narrowing is the only permitted direction. `git diff` over exclusion one, both bands, every `proof` token and both pinned counts asserted EMPTY; a disagreement between the direct assertion and the resolved range is written up as a finding before anything is adjusted. |
| T-01-257 | Repudiation | a pin nobody watched failing | high | mitigate | The pre-fix regex planted after commit, the RED observed with the FULL message and both line numbers pasted so the 117-line gap is visible in the output, restored with a clean `git diff --exit-code`. |
| T-01-258 | Information Disclosure | two unmarked present-tense statements with a false wave attribution | medium | mitigate | Swept by one of the two prescribed dispositions with the choice recorded; the attribution asserted GONE by a numeric grep at zero plus a human read for paraphrases. |
| T-01-259 | Repudiation | a replacement count claim stated wider than its grep | high | mitigate | The sites re-counted after the sweep with the grep pasted; the replacement states the measured number or no number and NAMES the two surfaces this file cannot see, with the reason each is unreachable from here. |
| T-01-260 | Information Disclosure | a sweep that creates a third standing statement | medium | mitigate | Any replacement prose is included in the post-sweep re-count with an explicit statement of whether a new site was created; the pointer-that-asserts-nothing disposition is named as available and is the one wave 33 took for this class. |
| T-01-261 | Repudiation | a finding described as heavier than it is | medium | mitigate | The SUMMARY states the verifier's own weighing — four sites all agreeing on `[ ]`, no contradiction of fact, NOT what keeps the box at `[ ]` — and an overclaim in the opposite direction is called out as equally false. |
| T-01-262 | Repudiation | a closing baseline carried forward instead of executed | high | mitigate | Every figure executed in this session and pasted, each compared against arrival, and every difference attributed to a named plan and task; an unattributed difference is a finding. |
| T-01-263 | Repudiation | a tenth overclaim authored inside the fix for the ninth | high | mitigate | An `open`-status DISCLOSED-NOT-CLOSED prohibition in frontmatter and a per-sentence no-overclaim verdict in both tasks. Both of this plan's items ARE overclaim corrections and both replacement sentences are flagged as the round's likeliest places to author the next one. |
| T-01-240 | Tampering | a mutation planted over uncommitted work | high | mitigate | Task 1 commits before planting, pastes `git log --oneline -1` first, and proves the restore with `git diff --exit-code`. Wave 29 destroyed its own Task 1 exactly this way. |
| T-01-214 | Tampering | CORE-11's checkbox moved | critical | mitigate | Prohibited. This plan opens no file under `.planning/`; `CORE11_BOX_EXPECTED` is asserted byte-unchanged and still reading `[ ]` by a numeric check; `git status --porcelain .planning/` asserted EMPTY; `requirements mark-complete` appears in no command, preserving plan 01-38's P38-D3 pattern. |
| T-01-212 | Information Disclosure | the two reader surfaces no mechanism reaches | medium | accept | `.planning/STATE.md` and `.planning/WINDOWS.md` are governed by the pointer-not-a-bound prohibition with no mechanical check. Untouched here and NAMED in the replacement count claim as surfaces this file cannot see. Carried forward from T-01-144. |
| T-01-51 | Information Disclosure | the accepted residual of the outbound walk | medium | mitigate | Unchanged by this plan — no registry row moves — and byte-pinned into the ledger by the two comparisons, re-run green at both task boundaries. |
| T-01-50 | Information Disclosure | `.spec.ts` files, which the walk skips | low | accept | Unchanged and carried forward, bounded by `pnpm check:bundle`, which specs never enter. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | NOT APPLICABLE: this plan installs no package. `pnpm install` is not run and no manifest appears in `files_modified`. RESEARCH.md's Package Legitimacy Audit is present and unchanged; no `[ASSUMED]` or `[SUS]` package is introduced, so no blocking human checkpoint is required. |
</threat_model>

<verification>
1. Precondition asserted and pasted at both task boundaries, with HEAD beside any status snapshot and
   both prior plans' SUMMARYs confirmed present.
2. All three exclusions' `from`/`to`/size/band pasted BEFORE the change, plus the registry's opening,
   real closing and landed-on lines and the quantifier list's three, reconciled against 4135 / 5767 /
   1633 and 5959 / 5969.
3. Both direct assertions present using TEXT LOCATORS, with the exactly-one-line non-vacuity assertion
   pasted and its message quoted.
4. The reason for taking the direct form over a size pin stated in one sentence, with both bands shown
   unchanged by diff.
5. `git diff` over the `CLOSES_FROZEN_ARRAY` regex asserted EMPTY numerically.
6. `git diff` EMPTY over every `proof` token, both pinned counts and exclusion one.
7. Any assertion-versus-resolution disagreement written up as a finding with nothing adjusted to
   accommodate it.
8. The verifier's revert planted after commit, RED observed, title and FULL message pasted with both
   line numbers showing the 117-line gap, restored, clean diff, prior 434-of-434 cited.
9. If the revert did not go red, the task stopped and the diagnosis is a first-order finding with no
   adjustment made to force a red.
10. The replacement closing sentence quoted verbatim and shown to carry all four elements.
11. The BEFORE site enumeration pasted with its grep, reconciled against four, each site labelled
    editable / read-only / outside `files_modified`.
12. The disposition for each of the two statements named as P38-D1 marking or wave-33 deletion, with its
    reason and its diff.
13. The wave-28 attribution asserted GONE numerically plus a human read confirming no paraphrase
    survives.
14. The AFTER site re-count pasted with its grep, including any replacement prose, with an explicit
    statement of whether a new site was created.
15. The replacement count clause quoted verbatim, stating the measured number or no number, and NAMING
    the two surfaces this file cannot see with the reason each is unreachable.
16. The count claim asserted absent numerically; the rest of the wave-33 note shown intact by diff.
17. The finding's weight stated as the verifier stated it, with an explicit statement that nothing
    written implies more in either direction.
18. `CORE11_BOX_EXPECTED` asserted byte-unchanged and still reading `[ ]` numerically.
19. The round's closing baseline executed in this session and pasted — suite counts, `tsc --build`,
    `check:bundle`, the gate suite's count, the real-tree walk — each compared against arrival and every
    difference attributed to a named plan and task; unattributed differences recorded as findings.
20. The round-wide `git diff --name-only` over `packages/` and `scripts/` filtered of `.spec.ts`
    asserted EMPTY numerically; CORE-11's first sentence pasted and shown byte-identical.
21. `01-PROBE.md`'s equality `38 == 27 + 11` re-stated unchanged; `COVERAGE.md` confirmed unedited and
    its declaration confirmed to stand; the assumption-delta detector run and its result stated in one
    line including a plain "did not fire".
22. A per-sentence no-overclaim verdict for everything both tasks wrote.
23. `git diff` EMPTY over every out-of-scope construct named in each task's action.
24. `git status --porcelain .planning/` asserted EMPTY numerically at every boundary;
    `requirements mark-complete` absent from every command run.
25. `pnpm test` exit 0, no fewer than 31 files / 1374 tests; `typecheck`, `lint`, `knip` exit 0;
    `build:backend && check:bundle` exactly one specifier, `crypto`; real-tree walk 23 modules / 0
    violations.
26. Every prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses, or an
    explicit statement that there were none.
</verification>

<success_criteria>
- Exclusion three's end and exclusion two's end are each asserted directly against their constructs' own
  closing lines, located by text and proved to match exactly one line — so the width can no longer move
  without something going red.
- The verifier's revert, which stayed green at 434 of 434, was planted and watched going RED, with the
  117-line gap visible in the pasted assertion message.
- The recogniser was pinned without being loosened: `git diff` over the regex is EMPTY.
- No band, `proof` token, pinned count or exclusion was widened to accommodate anything; narrowing
  remained the only permitted direction.
- The closing sentence names why the `proof` token, the band and the clause-count equality each fail in
  the over-walk direction, names the new assertion as what holds the resolution, and states that reach
  and no wider.
- The two unmarked present-tense statements are disposed of by a recorded choice and the false wave-28
  attribution is gone from both, proved by a numeric grep and by a human read for paraphrases.
- The count claim is replaced by something true at the reach of a pasted grep taken AFTER the sweep,
  naming the surfaces this file cannot see — and the sweep is shown either not to have created a new
  site, or to have counted the one it created.
- The finding's weight is stated as the verifier stated it: four sites, all agreeing, no contradiction
  of fact, and not what keeps the box at `[ ]`.
- The round's closing baseline was EXECUTED in this session, compared against arrival, and every
  difference attributed to a named plan and task; the round's whole code delta is shown to be one test
  file and CORE-11's first sentence is byte-identical.
- CORE-11's checkbox did not move in either direction, `requirements mark-complete` was not run, no file
  under `.planning/` was opened, and whether the box may move is left to verification pass 10.
- No sentence anywhere claims that pinning one width and sweeping two statements makes the exclusions
  exact or discharges criterion (3).
</success_criteria>

<artifacts_this_phase_produces>
| Artifact | Kind | Purpose |
|----------|------|---------|
| the direct range assertion for exclusion three | new mechanism | `closingBracketAfter(registryStart)` pinned to the line located by the registry's own closing text, non-vacuously, so a width that moves goes red instead of silently reverting |
| the direct range assertion for exclusion two | new mechanism | The same pin for the quantifier list, because the recogniser serves both and correcting one while leaving the other unpinned is the same defect one construct over |
| the verifier's revert, watched RED | new evidence | WR-48's exact 117-line over-walk re-introduced and observed failing, where it stayed green at 434 of 434 before this plan, with both line numbers in the pasted message |
| the replaced closing sentence | corrected disclosure | Why the `proof` token cannot detect an over-walk by construction, why the band did not catch 1,750, why the clause-count equality would not have caught it either, and what holds the resolution now — with that reach and no wider |
| the two swept standing statements | corrected disclosure | Round 8's own disclosure repaired now that the gate file is open, by P38-D1 history-marking or wave-33 deletion, with the false wave-28 flip attribution gone either way |
| the replaced count claim | corrected disclosure | A statement true at the reach of a grep pasted after the sweep, naming the surfaces this file cannot see rather than silently counting them |
| the before-and-after site enumeration | new evidence | Every place that states the box's state, counted before the sweep and again after it, with each site labelled editable, read-only or outside `files_modified` |
| the round's closing baseline | new evidence | Suite counts, `tsc --build`, `check:bundle`, the gate suite's count and the real-tree walk all executed in this session and reconciled against arrival, with every difference attributed |
| the round-wide no-leak re-establishment | new evidence | `git diff --name-only` over `packages/` and `scripts/` filtered of `.spec.ts` shown EMPTY for the whole of round 9, and CORE-11's first sentence byte-identical |
| `01-41-SUMMARY.md` | new evidence | The range measurements, both pins, the revert's RED, the replaced sentence, the two dispositions, both site counts, the replaced count claim, the closing baseline and the full gate set |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-skeleton-persistence-compatibility/01-41-SUMMARY.md` when done.

It must contain, as pasted terminal output rather than as prose: the precondition assertions with
`git rev-parse HEAD`; all three exclusions' `from`/`to`/size/band before the change; the registry's
opening, real closing and landed-on lines and the quantifier list's three, with their reconciliation;
both direct assertions' diffs and their exactly-one-line non-vacuity output; the EMPTY diff over the
`CLOSES_FROZEN_ARRAY` regex, over both bands, over every `proof` token, over both pinned counts and over
exclusion one; `git log --oneline -1` before the plant; the reverted regex, the RED title and the FULL
assertion message with both line numbers; the restore and a clean `git diff --exit-code`; the replaced
closing sentence quoted verbatim; the BEFORE site enumeration with its grep and its four-way labelling;
the diff for each of the two dispositions; the numeric grep showing the wave-28 attribution gone; the
AFTER site re-count with its grep; the replaced count clause quoted verbatim; the numeric check showing
`CORE11_BOX_EXPECTED` byte-unchanged at `[ ]`; the closing baseline with every figure and its comparison
against arrival; the round-wide `git diff --name-only` filtered of `.spec.ts` shown EMPTY; CORE-11's
first sentence shown byte-identical; the per-sentence no-overclaim verdict table; the EMPTY
`git status --porcelain .planning/`; and the final `pnpm test`, `typecheck`, `lint`, `knip` and
`check:bundle` results.

It must state in ONE sentence which mechanism holds exclusion three's width now, and why none of the
three previously named mechanisms could.

It must state in ONE sentence the disposition chosen for each of the two standing statements and why,
and whether the sweep created a new site.

It must state in ONE sentence the finding's weight exactly as verification pass 9 stated it — four
sites, all agreeing on `[ ]`, no contradiction of fact, and NOT what keeps the box at `[ ]`.

It must state in ONE sentence, for the WHOLE of round 9, that the round's entire code delta is one test
file, that CORE-11's first sentence is byte-identical, and that nothing leaks.

It must state in ONE sentence that pinning one width and sweeping two statements does not make the
exclusions exact and does not discharge criterion (3), naming exclusion three's remaining coarseness as
a line range, the third-form close the recogniser still would not match, and the two reader surfaces no
mechanism reaches.

It must state in ONE sentence that CORE-11's box was not moved in either direction, that
`requirements mark-complete` was not run, that no file under `.planning/` was opened by any plan in this
round, and that whether the box may move is verification pass 10's determination.
</output>
