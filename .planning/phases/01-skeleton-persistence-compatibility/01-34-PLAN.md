---
phase: 01-skeleton-persistence-compatibility
plan: 34
type: execute
wave: 34
gap_closure: true
depends_on: [01-33]
files_modified:
  - packages/backend/src/outbound-prohibition.spec.ts
  - packages/backend/src/store/observations.spec.ts
  - packages/backend/src/store/schema.spec.ts
  - packages/backend/src/store/observations.ts
  - packages/backend/src/store/error-redaction.spec.ts
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/WINDOWS.md
# THIS PLAN DOES NOT CLOSE CR-15 AND CR-16. IT DISCLOSES THEM. READ THAT SENTENCE AGAIN BEFORE
# EXECUTING, BECAUSE EVERY OTHER DECISION IN THIS FILE FOLLOWS FROM IT.
#
# Six rounds each closed roughly two gate blindnesses and each surfaced roughly two more. The rate is
# flat and round 6's verifier diagnosed why: CORE-11's old terminal condition — the gate goes red on
# every spelling of every enumerated clause — is UNREACHABLE over an open language. The space of
# JavaScript spellings for `invoke a function through a value` does not close. The operator has adopted
# the reachable bar instead, in the verifier's own words: the residual must be DERIVED from the code,
# drift between text and code must be MECHANICALLY DETECTABLE, and the disclosure must be THE SOLE
# BOUND stated on every surface a reader touches. Wave 33 delivers the third criterion. Wave 35 does
# the re-scope and the discharge. THIS WAVE PUTS CR-15 AND CR-16 ONTO THE DERIVED LIST.
#
# So the deliverable is a ROW, not a branch. `fetch.call(null, url)` joins the sixteen existing
# measured silences with an executed probe and an executed counter-probe, in the shape the sixteen
# already use. What earns CORE-11's `[x]` under the new bar is that the shape sits on a list nobody
# can silently edit — NOT that the gate goes red on it. A widening that happens beside the row is a
# bonus and is written as one.
#
# WIDENING IS PERMITTED, BOUNDED BY THREE CONDITIONS, ALL THREE REQUIRED. It must be CHEAP (one branch
# beside branches that already anchor on a positively identified receiver), OBVIOUSLY CORRECT (a
# structural twin already reports the intended answer, so the machinery exists and is merely not
# reached), and MEASURED (executed before and after, with everything that stays silent rowed). A
# widening whose residual is not re-measured and re-rowed is worse than no widening, because it
# converts a disclosed silence into an undisclosed one.
#
# NO SENTENCE ANYWHERE IN THIS WAVE MAY IMPLY THE CLASS IS ENDED. Not a docblock, not a fixture title,
# not an assertion message, not the ledger correction. Seven review passes have found the same defect —
# a title that promises a class while its mechanism covers one shape — and the correction for it is
# never a wider claim.
#
# THREE MEASUREMENTS TAKEN DURING PLANNING AGAINST HEAD `e193dfc`, reproducible by the commands in
# Task 1, recorded so the executor does not re-derive them:
#
#   (1) THE 510-LINE DERIVED SPAN IN `.planning/REQUIREMENTS.md` CARRIES ZERO OCCURRENCES OF `.call`,
#       `.apply`, `.bind`, `Reflect`, `navigator[` AND `require(`. Independently reproduced from the
#       verifier's search. A reader of the residual OF RECORD learns nothing about any shape in this
#       plan, which is precisely what makes the rows the deliverable.
#
#   (2) THE SPAN'S ONLY MENTION OF A TAGGED TEMPLATE IS ABOUT KEYS, NOT ABOUT `fetch`. `grep -c
#       'tagged template'` over the span returns 1, and that one is `silence-tagged-template-key`,
#       whose clause is about `const k = String.raw` building a KEY. IN-31 is a tagged-template CALL of
#       the global fetch. A reader searching the span for the words in IN-31 finds a row that does not
#       cover it and stops looking. That is a title-versus-mechanism substitution waiting inside the
#       disclosure itself, and it was found by measurement rather than by reading.
#
#   (3) SIXTEEN `kind: "measured-silence"` ROWS EXIST TODAY. That number is the one this wave moves,
#       and moving it UP is the honest direction — wave 32 moved it from 7 to 16 and the verifier
#       credited that explicitly.
#
# ON THE WAVE-33 INTERACTION, WHICH IS REAL AND WILL BITE. Wave 33 installs a whole-file, wrap-tolerant
# guard requiring every declared `UNBOUNDED_QUANTIFIERS` phrasing outside three anchored exclusions to
# carry a named `HEADER_QUANTIFIER_EXEMPTIONS` entry, with BOTH occurrence counts pinned BY EQUALITY.
# Every docblock, fixture title and assertion message this wave adds below `RESOLVER_REGISTRY` is
# inside that guard's reach and OUTSIDE its exclusions. New registry CLAUSES are inside the registry
# exclusion and are safe. So: write the new prose without a declared phrasing, and run the wave-33
# guard after every edit rather than at the end. A wave that goes red on its neighbour's equality pin
# and then widens the exclusion to get green has defeated the neighbour.
#
# TRANSIENT MUTATION TARGETS — reverted inside the task that plants them, restore cross-checked by
# `git diff --exit-code` in that task's own verify block, and COMMITTED BEFORE MUTATING because wave 29
# destroyed its own Task 1 by mutating uncommitted work:
#   packages/backend/src/outbound-prohibition.spec.ts   (each NEW branch's EFFECT neutered in turn,
#                                                        condition and anchor line left byte-identical)
#   packages/backend/src/store/observations.spec.ts     (the varied-length sweep's discriminating
#                                                        assertion inverted once)

autonomous: true
requirements: [CORE-11, STORE-03, STORE-07]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low
# `estimate-calibration` reports `{"factor": 1, "applied": false, "sample_count": 0, "min_samples": 3}`,
# so `tokens` equals `raw_tokens` and the confidence is DERIVED from the sample count, never self-rated.
# Weigh the SHAPE COUNT over the token figure: fourteen executed CR-15 shapes and twins, eight CR-16
# shapes, five WR-41 shapes, four WR-42 shapes, plus a four-length x seventy-one-offset head-side sweep.

must_haves:
  truths:
    - "CR-15 IS A NAMED, DISCLOSED RESIDUAL IN THE DERIVED SPAN, WITH EXECUTED PROBES — AND THAT IS THE DELIVERABLE. A bare global in RECEIVER rather than CALLEE position carries at least one `kind: \"measured-silence\"` row per surviving mechanism, each with a probe, an `expect`, a counter-probe and a `counterExpect`, in the shape the sixteen existing rows use. After this wave a reader of the 510-line span learns that `fetch.call(null, url)` was silent and what happened to it; before this wave the span carried ZERO occurrences of `.call`, `.apply`, `.bind` and `Reflect`, measured at HEAD `e193dfc`."
    - "CR-16 IS THE SAME KIND OF DELIVERABLE. An unreadable computed member on a positively identified `navigator` receiver is disclosed by row, with the four reporting twins (`sdk.requests[m]`, `globalThis[m]`, `window[m]`, and the navigator destructure) recorded as counter-probes, because the twins are what prove the machinery exists and is merely not reached. The span carried ZERO occurrences of `navigator[` before this wave."
    - "CLOSING THESE SHAPES IS NOT WHAT EARNS CORE-11's `[x]`, AND THE PLAN, THE CODE AND THE LEDGER ALL SAY SO IN THOSE TERMS. The bar is DERIVED + DRIFT-DETECTABLE + SOLE BOUND. Round 6 delivered the first two on the registry-owned surfaces and the verifier proved all three legs; wave 33 delivers the third; this wave puts two more shapes onto the derived list. NO sentence added by this wave — docblock, fixture title, assertion message, clause, ledger correction — may state or imply that the class of receiver-position and unreadable-member spellings is ended. The language is open and the plan says that plainly."
    - "EVERY WIDENING IS CHEAP, OBVIOUSLY CORRECT AND MEASURED — OR IT IS NOT MADE. Cheap means one branch beside branches that already anchor on a positively identified receiver. Obviously correct means a structural twin already reports the intended answer. Measured means the whole family is executed BEFORE the branch and again AFTER it, both tables pasted, and every shape still silent after the branch carries its own row. A widening that leaves an unrowed residual is a disclosed silence converted into an undisclosed one and is prohibited."
    - "WR-39 IS RE-DERIVED FROM A VARIED-LENGTH SWEEP, NOT FROM THE TEN-CHARACTER COINCIDENCE. The claimed discriminator between the two unstable head-side shapes is an artifact of `jsessionid` being ten characters, the same length as `<redacted>`. Executed by the verifier with a 29-character name, the parameter-NAME shape shrinks at NINETEEN consecutive offsets to −19 while the empty-value shape is −20. The replacement sweeps at least four name lengths across the band, states what the BRANCH does — the presence of an `=` in the final `;` segment is what selects the two branches — and lets the delta fall out of it. All THREE disclosure sites move together: `observations.spec.ts`, `schema.spec.ts` and `observations.ts`."
    - "THE DUPLICATED ASSERTION DIES. The line the discriminator comment sits on is BYTE-IDENTICAL to the assertion fourteen lines above it, so it re-asserts an existing fact and cannot detect the disagreement its own comment says it watches for. It is replaced by an assertion that the two second-pass outputs take DIFFERENT branches, and that replacement is watched failing under an inverted mutation."
    - "WR-40 AND WR-42 ARE SPLIT BY MECHANISM RATHER THAN BY TITLE. Three shapes filed under a spread mechanism are silent through THREE DIFFERENT mechanisms: a spread element is not a property assignment; a call's ARGUMENTS are never descended, which is why `Object.assign({}, e)` — a shape containing no spread at all — is silent; and a bare-identifier callee breaks the descent, which is an item already listed twenty lines above. Four further shapes in the same class are measured and disclosed, including the plainest spelling an author would actually type."
    - "WR-41, IN-31, IN-32 AND IN-33 ARE EACH COVERED OR DEFERRED WITH A STATED REASON, AND THE REASON IS RECORDED IN THE SUMMARY RATHER THAN IN THE PLANNER'S HEAD. WR-43, WR-38 and CR-14 are DEFERRED TO WAVE 33 BY OWNERSHIP — wave 33 deletes the header's bounds and installs the whole-file quantifier guard, and this wave must not touch that surface or it will be revising its own dependency."
    - "MUTATION PROOFS ARE PER BRANCH, EXECUTED SEPARATELY, NEVER COMBINED, each with its RED test title and its full assertion message pasted, `git diff --exit-code` shown clean between them and the run order stated. The mutation neuters the branch's EFFECT while leaving its CONDITION and its ANCHOR LINE byte-identical, which is the form the round-6 verifier used to defeat an anchor-line guard and the only form that proves the branch rather than the line. EVERY TASK COMMITS ITS REAL WORK BEFORE IT MUTATES — wave 29 destroyed its own Task 1 with `git checkout --` against uncommitted work, and waves 30 through 33 all used commit-then-mutate and lost nothing."
    - "EVERY PREDICTION-VS-MEASUREMENT DISCREPANCY IS RECORDED RATHER THAN ABSORBED, in the shape `01-27-SUMMARY.md` uses. Round 6 caught a wrong stated fix twice and found shapes nobody had named four times, every one of them by measuring before writing. If a shape this plan predicts silent reports, or a widening moves fewer shapes than predicted, the DIFFERENCE is the finding and it is written down."
    - "THE WAVE-33 GUARD STAYS GREEN WITHOUT BEING WEAKENED. Every docblock, fixture title and assertion message this wave adds below `RESOLVER_REGISTRY` is inside the reach of wave 33's whole-file wrap-tolerant quantifier guard and outside its three exclusions, and both of that guard's occurrence counts are pinned by equality. The new prose carries no declared phrasing, the guard is run after every edit rather than at the end, and NO exclusion and NO exemption entry is widened to accommodate this wave."
    - "EVERY FIXTURE IS TIED TO THE MECHANISM IT EXERCISES. A fixture that cannot go red under the mutation it sits beside is titled a MEASURED SILENCE and carries the probe that measured it. A fixture titled for a mechanism its own assertions do not exhibit is the defect this phase has now found five times, and it is not shipped again."
    - "THE REAL TREE IS ZERO AFTER EVERY WIDENING, with the four measured-exempt sites named: `compat.ts`'s `cur[key]` and `ctx[root]`, and `observations.ts`'s `segments[i]` and `MIGRATIONS[MIGRATIONS.length - 1]`. A widening that fires on real shipped code is a widening that gets reverted or exempted by measurement, never one that gets shipped with the tree red."
    - "NOTHING LEAKS, AND THIS PLAN DOES NOT IMPLY OTHERWISE. Zero outbound-shaped tokens in any shipped non-spec source across both roots and the frontend; the shipped bundle's entire import set is one specifier, `crypto`; `observations.ts`'s redaction code is byte-identical since round 5 and has survived 19,772 swept inputs with zero leaks; the real tree runs 23 files / 0 violations. Every finding this wave touches is a PROSPECTIVE blindness in a TEST-ONLY gate."
  artifacts:
    - path: "packages/backend/src/outbound-prohibition.spec.ts"
      provides: "New `kind: \"measured-silence\"` rows disclosing CR-15's surviving mechanisms and CR-16's, each with probe / expect / counterProbe / counterExpect; the cheap measured widenings that survived the three-condition test; the failing-path fixtures and their reporting controls; the WR-41, IN-31, IN-32 and IN-33 dispositions"
      contains: "measured-silence"
    - path: ".planning/REQUIREMENTS.md"
      provides: "The regenerated derived span carrying the new rows, and a dated CORE-11 correction stating that this wave DISCLOSED rather than ended a class, plus the STORE-03 and STORE-07 disclosure corrections"
      contains: "BEGIN DERIVED RESIDUAL"
    - path: "packages/backend/src/store/observations.spec.ts"
      provides: "The head-side sweep re-derived across at least four parameter-name lengths, the branch-selecting assertion that replaces the byte-identical duplicate, and the per-length delta profiles read out of the run rather than hard-coded"
      contains: "headUnstable"
    - path: "packages/backend/src/store/schema.spec.ts"
      provides: "The `observations.url` disclosure entry corrected so the delta is stated as name-length dependent and the branch selector is stated as the discriminator"
      contains: "observations.url"
    - path: "packages/backend/src/store/observations.ts"
      provides: "The no-separator residual paragraph corrected in the same terms — comment-only, the redaction code byte-unchanged and shown so by diff"
      contains: "redactDelimitedSegment"
    - path: "packages/backend/src/store/error-redaction.spec.ts"
      provides: "The spread residual split into its three actual mechanisms, the two pinned cases re-titled off the shape count, and the four further measured shapes disclosed with the plainest spelling pinned"
      contains: "derivesFrom"
    - path: ".planning/STATE.md"
      provides: "A pointer amendment naming the new rows and the widenings and restating no bound of its own"
      contains: "measured-silence"
    - path: ".planning/WINDOWS.md"
      provides: "The open entry for this gate file superseded and a new pointer entry appended through the tool"
      contains: "outbound-prohibition.spec.ts"
  key_links:
    - from: "`fetch.call(null, url)` and its family"
      to: "the 510-line derived span in `.planning/REQUIREMENTS.md`"
      via: "a `kind: \"measured-silence\"` row per surviving mechanism, carried through `deriveResidual(RESOLVER_REGISTRY)` and pinned by the two byte comparisons"
      pattern: "measured-silence"
    - from: "an unreadable computed member on an identified `navigator` receiver"
      to: "the same derived span"
      via: "a row whose counter-probes are the four identified receivers that already report `outbound-unanalysable`, so the asymmetry is stated rather than inferred"
      pattern: "outbound-unanalysable"
    - from: "each NEW branch this wave adds"
      to: "`BRANCH_VOCABULARY`'s per-branch probe machinery"
      via: "a registry row whose `site` names the branch's own anchor, so an effect-only mutation goes red row-granularly rather than silently"
      pattern: "BRANCH_VOCABULARY"
    - from: "the head-side unstable band"
      to: "the three disclosures that describe it"
      via: "a sweep over parameter-name LENGTH as well as offset, with the branch selector asserted and the delta derived rather than pinned"
      pattern: "headUnstable"
    - from: "this wave's new prose below `RESOLVER_REGISTRY`"
      to: "wave 33's whole-file wrap-tolerant quantifier guard"
      via: "no declared phrasing in any new docblock, title or message, verified by running that guard after every edit and never by widening an exclusion"
      pattern: "HEADER_QUANTIFIER_EXEMPTIONS"
  prohibitions:
    - requirement_id: CORE-11
      category: security
      status: open
      verification: gate
      resolution: "DISCLOSED, NOT CLOSED — `packages/backend/src/outbound-prohibition.spec.ts` carries a `kind: \"measured-silence\"` row per surviving mechanism of the bare-global-in-receiver-position family, with executed probes and executed counter-probes, and the row set is carried into the derived span in both files"
      statement: "MUST NOT be reported as closed by this wave. A bare global in RECEIVER position falsifies the enumerated clause `no global fetch by ANY RECEIVER or alias`, and the space of spellings that reach a function through a value is OPEN. What this wave changes is that the shape sits on a derived list nobody can silently edit. Any sentence claiming the class is ended is the defect, not the fix."
    - requirement_id: CORE-11
      category: security
      status: open
      verification: gate
      resolution: "DISCLOSED, NOT CLOSED — a row for an unreadable computed member on a positively identified `navigator` receiver, with the four identified receivers that DO report as counter-probes"
      statement: "MUST NOT be reported as closed by this wave. It falsifies the enumerated clause `no navigator.sendBeacon`, and it is the `could not read does not mean clean` asymmetry surviving on one receiver family after being closed twice on others. Disclosure is the deliverable; a branch beside it is a bonus."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "packages/backend/src/outbound-prohibition.spec.ts — every widening executed against the whole family before and after, both tables pasted, and every shape still silent after the branch carrying its own row"
      statement: "MUST NOT ship a widening whose residual is unmeasured. A widening that moves four of seven shapes and rows none of the remaining three has converted a disclosed silence into an undisclosed one, which is strictly worse than the silence it replaced."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "packages/backend/src/outbound-prohibition.spec.ts — new prose carries no declared `UNBOUNDED_QUANTIFIERS` phrasing, wave 33's guard is run after every edit, and no exclusion or exemption entry is widened"
      statement: "MUST NOT defeat wave 33's guard to get green. Both its occurrence counts are pinned by equality and this wave's new docblocks, titles and messages sit inside its reach. Widening an exclusion to accommodate a new sentence is how a guard becomes decoration in one commit."
    - requirement_id: STORE-03
      category: security
      status: resolved
      verification: gate
      resolution: "packages/backend/src/store/observations.spec.ts, schema.spec.ts and observations.ts — the head-side residual re-derived from a sweep over parameter-name LENGTH, the branch selector asserted, and the byte-identical duplicate assertion replaced"
      statement: "MUST NOT state a fixture coincidence as a mechanism property. `jsessionid` is ten characters and so is `<redacted>`; with a 29-character name the shape claimed not to shrink shrinks at nineteen consecutive offsets. The REDACTION is unaffected and stays unaffected — this is a disclosure defect and is corrected as one."
    - requirement_id: STORE-07
      category: security
      status: resolved
      verification: gate
      resolution: "packages/backend/src/store/error-redaction.spec.ts — the spread residual split into its three actual mechanisms, the two pinned cases re-titled off the shape count, four further shapes measured and disclosed"
      statement: "MUST NOT file a shape under a mechanism it does not exhibit. `Object.assign({}, e)` contains no spread; it is silent because a call's ARGUMENTS are never descended, which is a mechanism the list did not name at all. The prohibition itself holds and is unaffected."
    - requirement_id: CORE-11
      category: safety
      status: open
      verification: gate
      resolution: "DEFERRED BY OWNERSHIP — CR-14, WR-38 and WR-43 are wave 33's and are not touched here"
      statement: "MUST NOT edit the gate file's hand-written header in this wave. Wave 33 deletes its bounds and installs the whole-file guard; a wave that simultaneously edits that surface would be revising its own dependency, and a guard written against a header a sibling is editing is born stale."
---

<objective>
Put CR-15 and CR-16 onto the derived list, and take the cheap measured widenings that survive the
three-condition test as a bonus rather than as the close. Re-derive WR-39's head-side discriminator
from a varied-length sweep instead of a ten-character coincidence, and split the spread residual by
mechanism.

Purpose: the operator has replaced CORE-11's unreachable terminal condition — the gate goes red on
every spelling of every enumerated clause — with the reachable one the round-6 verifier named:
DERIVED, DRIFT-DETECTABLE, and THE SOLE BOUND on every surface a reader touches. CR-15 and CR-16 are
the gate's REACH and not its text, and no scan and no bar ever ends their class, because the language
is open. Under the new bar they become what the sixteen existing measured silences already are: named,
disclosed, probe-carrying rows in a block generated from the code. THAT is the deliverable of this
wave. Closing the shapes is not what earns CORE-11's `[x]`, and nothing in this wave may suggest it is.

Output: new measured-silence rows for every surviving mechanism of the receiver-position family and of
the unreadable-member-on-navigator family; the widenings that passed cheap-and-obvious-and-measured,
each with its before-and-after table and its own effect-only mutation proof; a head-side disclosure
re-derived across four parameter-name lengths on all three of its sites; a spread residual split into
the three mechanisms it actually exhibits; and dated ledger corrections that state what was disclosed
and say plainly that no class was ended.

WHAT THIS WAVE DOES NOT DO, STATED HERE SO IT CANNOT BE MISREAD LATER. It does not close CR-15 or
CR-16. It does not touch the gate file's hand-written header — CR-14, WR-38 and WR-43 are wave 33's and
editing that surface here would revise this plan's own dependency. It does not touch CORE-11's
checkbox; wave 35 owns it and the correct value today is `[ ]`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md
@.planning/phases/01-skeleton-persistence-compatibility/01-REVIEW.md
@.planning/phases/01-skeleton-persistence-compatibility/01-33-SUMMARY.md
@.planning/phases/01-skeleton-persistence-compatibility/01-32-SUMMARY.md
@packages/backend/src/outbound-prohibition.spec.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: One shape, end to end — measure the receiver-position family, disclose it by row, take the one cheap widening, carry it through the derived span into the ledger, and watch an effect-only mutation go red</name>

  <precondition>
    Wave 33 has executed and its artefacts are on disk: `01-33-SUMMARY.md` exists, the whole-file
    wrap-tolerant quantifier guard and `HEADER_QUANTIFIER_EXEMPTIONS` are present in
    `packages/backend/src/outbound-prohibition.spec.ts`, and the suite is green. Assert before the first
    edit: both byte comparisons pass — the block shipped in the gate header equals
    `deriveResidual(RESOLVER_REGISTRY)` byte for byte, and its `.planning/REQUIREMENTS.md` twin does too
    — and `pnpm test` exits 0. Halt if either comparison is already red; a row added on top of a diverged
    span ships the divergence. Also assert `git status --porcelain packages/ .planning/` is clean and
    cross-check any status snapshot against `git rev-parse HEAD` — wave 31 was handed a snapshot seven
    commits stale. This repo has NO active git hooks: `.git/hooks` holds only samples and
    `core.hooksPath` is unset, so nothing runs the gate set for you at a commit boundary.
  </precondition>

  <read_first>
    - `packages/backend/src/outbound-prohibition.spec.ts` `:3703-3782`, the member rule, in full. This
      is the whole subject of the task. Note the chain's ordering: the unreadable-receiver arm, the
      send/net arms, then `member === FETCH_GLOBAL && isGlobalReceiver(node.expression)`, then the
      beacon arm, then the dynamic-code and constructor arms, then the unreadable-member catch-all
      guarded by `isGlobalReceiver`. Every one of those anchors on the receiver being reached THROUGH a
      global, never on the receiver BEING one.
    - `:3146-3163` (`isFetchExpression`), `:3193-3215` (`bareFetchCallee`) and `:3248-3268`
      (`globalNameOf`). Read `bareFetchCallee`'s docblock in full: it explains why it is deliberately
      NARROWER than `isFetchExpression`, and that reasoning constrains the branch this task adds. A
      branch that reports `globalThis.fetch(url)` twice is a behaviour change dressed as a collapse.
    - `:2735` (`fetchAliases`) and `:3216-3246` (`isNavigatorReceiver`), so the CR-16 measurement in
      Task 2 has its context established here.
    - `01-REVIEW.md` pass 7, CR-15 in full, including the seven executed silent shapes, the six
      reporting structural twins, and the suggested branch. Read the suggestion as a STARTING POINT that
      must be measured, not as a specification — round 6 caught a wrong stated fix twice by measuring.
    - `01-VERIFICATION.md`'s `## CR-15 — confirmed, and it is the sharpest finding of the round`
      section, which carries the verifier's OWN executed output including the pair that makes the
      finding: the same identified alias reporting as a callee and silent as a receiver.
    - `RESOLVER_REGISTRY`'s measured-silence rows at `:5408-5455` — `silence-parameter-default-receiver`,
      `silence-for-of-binding-receiver`, `silence-tagged-template-key`, `silence-doubled-global-receiver`
      — and the removal note at `:5456+` explaining why a row that no longer measures a silence is a
      fiction. Those four rows are the exact shape every row this wave adds is written in.
    - `QUANTIFIED_CLAUSES` at `:5686-5700`, specifically the `isFetchExpression` entry, which exists to
      state what measurably bounds the universal it carries and today names only a function boundary, an
      array-slot binding, a class field and a parameter default.
    - Wave 33's new whole-file quantifier guard and `HEADER_QUANTIFIER_EXEMPTIONS`, wherever
      `01-33-SUMMARY.md` records them. Read the exclusion anchors and both pinned counts BEFORE writing
      any new prose, because every sentence this task adds below the registry is inside that guard's
      reach.
  </read_first>

  <files>packages/backend/src/outbound-prohibition.spec.ts, .planning/REQUIREMENTS.md</files>

  <reversibility rating="reversible">Adds registry rows, one resolver branch and fixtures; regenerates a generated span. The branch is one `else if` in a chain and reverting is a `git revert` of one commit. The real tree is asserted zero after it, so no shipped behaviour depends on it.</reversibility>

  <action>
    MEASURE BEFORE YOU CHANGE ANYTHING, AND MEASURE THE TWINS TOO. Run all seven silent shapes the
    verifier executed and all six reporting structural twins through `auditSource`, at HEAD, before any
    edit, and paste the table. The seven: a bare global fetch as the receiver of `.call`, of `.apply`
    and of `.bind`; the same fetch handed to `Reflect.apply` as an ARGUMENT; `eval` as the receiver of
    `.call`; `Function` as the receiver of `.call`; and the alias pair — an identifier bound to the bare
    global fetch, used as a receiver of `.call` on one line and as a callee on the next. The six twins
    are the SDK send member under `.call`, under `.bind` and under `Reflect.apply`, the member-qualified
    global fetch under `.call` and under `.bind`, and the navigator beacon member under `.call`. If your
    measurement disagrees with the verifier's on any row, THAT DISAGREEMENT IS THE FINDING — record it,
    do not adopt either number silently.

    NOW WRITE THE ROWS, AND WRITE THEM FIRST. This is the deliverable and the ordering is deliberate.
    Before any branch exists, add a `kind: "measured-silence"` row for the family, in the shape
    `silence-parameter-default-receiver` uses: an id, a clause that states the MECHANISM rather than the
    exemplar, a `site` naming the anchor the silence lives at, a `probe`, an `expect` of the empty
    array, a `counterProbe` that is the structural twin, and its `counterExpect`. The clause must say
    that the machinery exists and is not reached, because the twins prove exactly that. Regenerate the
    derived span, run both byte comparisons, and COMMIT. That commit alone satisfies this wave's bar for
    this family — everything after it is a bonus and the SUMMARY says so in those words.

    THEN TEST THE ONE WIDENING AGAINST ALL THREE CONDITIONS, IN WRITING. Cheap: it is one `else if`
    beside branches that already anchor on a positively identified receiver. Obviously correct: the
    member-qualified twin already reports the intended answer through the branch above it. Measured: the
    before table exists and the after table is about to. Only if all three hold, add the branch that
    consults `isFetchExpression` on the receiver when a member name is readable, and report the global
    fetch surface rather than the local spelling. Respect `bareFetchCallee`'s constraint — do NOT route
    the bare-call rule through `isFetchExpression`, and check by measurement that the member-qualified
    spelling still reports exactly once.

    RE-MEASURE ALL THIRTEEN SHAPES AND ROW EVERYTHING THAT IS STILL SILENT. The prediction to check
    rather than assume: the argument-position shape stays silent after this branch, because the global
    is handed to a call as an ARGUMENT and no member of it is mentioned anywhere — a mechanism genuinely
    different from receiver position and deserving its own row rather than a footnote on this one.
    Whatever the measurement says, the rows are rewritten to match it, and the pre-branch row is
    NARROWED to the mechanisms that survive rather than deleted wholesale. A row that no longer measures
    a silence is a fiction; a row narrowed by measurement is the honest edit.

    UPDATE THE QUANTIFIED BOUND IN THE SAME COMMIT AS THE WIDENING. `QUANTIFIED_CLAUSES`'
    `isFetchExpression` entry states what measurably bounds its universal and omits receiver position
    entirely. Add the measured probe and the measured answer. A bound narrower than the residual it
    claims to state is the specific defect CR-15 names.

    ADD THE FIXTURES, TIED TO THE MECHANISM. For this task: the alias pair as a failing-path case with
    its callee twin as the control in the same case, and the SDK twin as the reporting control. A
    fixture that cannot go red under the mutation it sits beside is titled a MEASURED SILENCE and says
    which probe measured it.

    KEEP WAVE 33's GUARD GREEN WITHOUT WEAKENING IT. Every sentence added below the registry is inside
    its reach and outside its exclusions, and both its counts are pinned by equality. Run it after each
    edit, not at the end. If a new sentence trips it, REWRITE THE SENTENCE. Do not widen an exclusion
    and do not add an exemption entry — those belong to wave 33 and taking them is how a guard becomes
    decoration in one commit.

    COMMIT BEFORE YOU MUTATE, THEN RUN ONE MUTATION. Wave 29 destroyed its own Task 1 with
    `git checkout --` against uncommitted work. Commit the real work, then neuter the NEW branch's
    EFFECT while leaving its condition and its anchor line byte-identical — that is the form the round-6
    verifier used to defeat an anchor-line guard, and the only form that proves the branch rather than
    the line. Record the red test title and the full assertion message, restore, and cross-check with
    `git diff --exit-code`. State the run order.

    RUN THE REAL TREE AFTER THE WIDENING. A branch that consults an alias set on a member receiver is
    exactly the kind that fires on ordinary code. If the tree goes non-zero, the finding is the site,
    and the disposition is a measured exemption or a reverted branch — never a shipped red tree.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/DefMiner && pnpm vitest run packages/backend/src/outbound-prohibition.spec.ts 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - The BEFORE table is pasted with all seven silent shapes and all six reporting twins, executed at
      HEAD before any edit, and reconciled against the verifier's own table with any disagreement named
      as a finding rather than absorbed.
    - Executed `grep -c 'kind: "measured-silence"' packages/backend/src/outbound-prohibition.spec.ts`
      before and after, both numbers pasted, and the after number is strictly greater. The pre-wave
      value measured at HEAD `e193dfc` is 16.
    - The row-first commit is shown by `git log --oneline` with both byte comparisons green in that
      commit, and the SUMMARY states in one sentence that this commit alone satisfies the wave's bar for
      this family.
    - The three-condition test for the widening is written out in the SUMMARY with a verdict per
      condition, and the widening was made only because all three passed.
    - The AFTER table is pasted for the same thirteen shapes, and every shape still silent carries a row
      whose id, clause, probe and counter-probe are quoted.
    - Executed `grep -c -- '\.call' ` over the 510-line span extracted from `.planning/REQUIREMENTS.md`,
      observed strictly greater than 0 — measured at 0 before this wave, so the residual OF RECORD now
      states something about receiver position that it did not state.
    - The member-qualified global fetch spelling was executed after the widening and reports EXACTLY
      ONCE, output pasted, confirming the branch did not duplicate the branch above it.
    - `QUANTIFIED_CLAUSES.isFetchExpression`'s measured bound quoted before and after, showing receiver
      position named with its probe.
    - Wave 33's whole-file quantifier guard executed after the final edit of this task, observed green,
      and `git diff` over its exclusions, its exemption map and both pinned counts shown EMPTY — the
      guard was satisfied by rewriting new prose, not by widening anything.
    - MUTATION executed: the new branch's EFFECT neutered with its condition and anchor line
      byte-identical, run, observed RED, the failing test title and the full assertion message pasted,
      restored, `git diff --exit-code` shown clean. `git log --oneline -1` pasted showing the real work
      was committed BEFORE the mutation.
    - The real-tree run over both `SOURCE_ROOTS` executed AFTER the widening, observed 23 files and 0
      violations, with the four measured-exempt sites named: `compat.ts`'s `cur[key]` and `ctx[root]`,
      and `observations.ts`'s `segments[i]` and `MIGRATIONS[MIGRATIONS.length - 1]`.
    - Both byte comparisons green at the end of the task, with the span's line count and sha256 pasted
      from both files.
  </acceptance_criteria>

  <done>
    The receiver-position family is disclosed by row in the derived span with executed probes and
    counter-probes, the one widening that passed all three conditions is in with its before-and-after
    tables and its effect-only mutation proof, everything still silent is rowed, and the real tree is
    zero.
  </done>
</task>

<task type="auto">
  <name>Task 2: The rest of the outbound surface — CR-16's dropped member, the dynamic-code receivers, WR-41's require alias, IN-31, IN-32 and IN-33, each measured, each rowed, each mutation-proved separately</name>

  <read_first>
    - Task 1's AFTER table, and `packages/backend/src/outbound-prohibition.spec.ts` `:3747-3782` again
      — specifically the final arm, which reports an unreadable computed member only when
      `isGlobalReceiver` answers true, and the beacon arm above it, which cannot match when the member
      name is `undefined` because it tests the member name.
    - `:3216-3246` (`isNavigatorReceiver`) and the navigator DESTRUCTURE arm the review places at
      `:3661-3675`. The destructure arm has an unreadable-property branch that DOES report; the member
      arm twenty lines away does not. Read both, because the two spellings of one shape disagreeing
      inside one rule is the entire content of CR-16.
    - `01-REVIEW.md` pass 7, CR-16 in full with its eight executed lines, and IN-32 and IN-33 in full.
      IN-32 is `boundPropertyName` returning undefined for a computed property name even when the key
      inside it is a plain string literal, so a readable destructure key off navigator takes the
      unreadable arm and a debugging reader gets the wrong rule id.
    - `01-REVIEW.md` pass 7, WR-41 in full, with the five executed shapes and the `RESOLVER_EXEMPTIONS`
      `visit` entry that names the specifier rule as POPULATION 3 residue. Read `:3808-3830` — the
      specifier rule's inline identity test on its callee — beside `bareFetchCallee`'s docblock, which
      calls the equivalent line a sixth copy of one question written where nobody was looking for one.
    - `01-REVIEW.md` pass 7, IN-31: the tagged-template spelling of the global fetch is not a
      `CallExpression`, so neither the bare-call rule nor `bareFetchCallee` is reached.
    - The measured-silence rows for keys at `:5244-5341` — parameter key, loop-binding key, destructured
      keys — because CR-16's parameter spelling and WR-41's alias spelling both need to say which
      EXISTING row they are or are not the same mechanism as. A new row that duplicates an existing
      mechanism is an exemption map growing without a stated cost.
    - `RESOLVER_EXEMPTIONS` in full, for the shape a named reason is written in: a statement of what was
      measured, not a justification.
  </read_first>

  <files>packages/backend/src/outbound-prohibition.spec.ts, .planning/REQUIREMENTS.md</files>

  <reversibility rating="reversible">Registry rows, at most three narrow branches, fixtures and docblock corrections. Each branch is independently revertible and each is asserted against the real tree.</reversibility>

  <action>
    TAKE EACH FINDING IN ITS OWN COMMIT, AND MEASURE EACH ONE BEFORE DECIDING ITS DISPOSITION. Five
    findings, five decisions, five records. The disposition vocabulary is exactly three words. DISCLOSE:
    a row with probe and counter-probe and no code change. DISCLOSE AND WIDEN: a row for what survives
    plus a branch that passed all three conditions from Task 1. DEFER: no row and no branch, with the
    reason stated in the SUMMARY and the finding named so round 8 does not rediscover it as new.

    CR-16 FIRST, BECAUSE ITS ASYMMETRY IS ALREADY PROVED BY ITS OWN TWINS. Execute the five silent
    shapes and the three reporting twins the reviewer ran. Write the row: the mechanism is that the
    unreadable-member catch-all is anchored on the four global receivers and `navigator` is not one of
    them, so when the member name will not reduce the beacon arm cannot match either and the shape falls
    through the whole chain. The counter-probes are the identified receivers that DO report the
    identical shape, and the sharpest counter-probe is the navigator DESTRUCTURE, because it is the same
    receiver family getting the opposite answer twenty lines away in the same rule. Then apply the
    three-condition test to widening the catch-all's guard to accept an identified navigator receiver as
    well, and re-measure all eight. The prediction to check rather than assume: the parameter spelling —
    an unreadable member whose key is a function parameter — stays silent after the widening for the
    reason `silence-parameter-key` already records, and if so it is cross-referenced to that row rather
    than given a duplicate of it.

    THE DYNAMIC-CODE RECEIVERS NEXT. Task 1's branch answers for the fetch surface. `eval` and
    `Function` in receiver position reach `globalNameOf` rather than `isFetchExpression`, so they are a
    SECOND branch and a second decision, not a footnote on the first. Measure them, apply the three
    conditions, and whatever the disposition, row what stays silent. Report the GLOBAL's name rather
    than the local spelling, which is the rule `globalNameOf`'s docblock already sets for both rules
    that read it.

    WR-41, AND DECIDE THE SCOPE QUESTION EXPLICITLY RATHER THAN LETTING IT FALL OUT OF A BRANCH. Execute
    the five shapes. The one that costs something is the last: the unanalysable-specifier arm — the
    whole `could not read does not mean clean` half of that rule — is unreachable through an alias, so
    an unreadable specifier through an aliased name is treated as clean. Two dispositions are legitimate
    and both are honest: grow the specifier rule through the alias machinery the way the dynamic-code
    globals were grown, or judge a module-loading specifier out of scope for a bundled plugin and DISCLOSE
    with a row carrying both probes. Choose ONE, state which, and state why in the row's own clause. What
    is not legitimate is leaving it in an exemption reason that records where the rule lives and records
    no silence for it — that is a residue named without being measured. Note in the SUMMARY that the
    span carried zero occurrences of the specifier keyword before this wave, measured at HEAD.

    IN-31, AND THE MEASUREMENT THAT MAKES IT SHARPER THAN THE REVIEW SAYS. The tagged-template spelling
    of the global fetch is silent because a tagged template is not a call expression. Measured during
    planning: the derived span mentions a tagged template exactly ONCE, and that mention is the KEY row
    — a tagged template building a lookup key — which is a different mechanism entirely. So a reader
    searching the residual for these words finds a row that does not cover this shape and stops looking.
    Whatever the disposition, the row or branch must distinguish the two explicitly, naming the key row
    by id so the reader is redirected rather than misled. That distinction is the deliverable here even
    if the shape itself is judged contrived.

    IN-32 AND IN-33 ARE DISCLOSURE-SHAPED AND SHOULD STAY THAT WAY. IN-32 errs in the SAFE direction —
    it reports, just with the wrong rule id — so the cost is a debugging reader learning the wrong
    mechanism, and the fix is either two lines inside the computed branch or a sentence in the beacon
    docblock retracting the claim that the class is asserted both ways. IN-33 is purely a bound the
    docblock does not state: the composition that was closed looks exactly one element deep and only at
    an object binding pattern, so a three-deep spelling and an array-nested spelling both stay silent.
    State the DEPTH and the PATTERN-KIND bound where the closure is claimed, and measure both neighbours
    rather than describing them.

    RE-DERIVE THE SPAN ONCE AT THE END OF THE TASK AND RUN BOTH BYTE COMPARISONS. Paste the row count
    and the measured-silence count before and after, and paste the sha256 and line count of the span
    from both files.

    RUN WAVE 33's GUARD AFTER EVERY COMMIT, NOT AT THE END. This task adds more prose than Task 1 and
    every sentence of it is inside that guard's reach. Rewrite any sentence that trips it. Do not touch
    its exclusions, its exemption map or its pinned counts.

    MUTATION PROOFS, ONE PER BRANCH THIS TASK ADDS, EXECUTED SEPARATELY AND NEVER COMBINED, EACH AFTER
    ITS OWN WORK IS COMMITTED. Neuter the effect, leave the condition and the anchor line
    byte-identical, record the red title and the full assertion message, restore, cross-check with
    `git diff --exit-code`, state the run order. If this task adds no branch because every disposition
    was DISCLOSE, then run instead the row-removal mutation: delete one new row while its probe fixture
    stands, observe the coverage or byte-comparison guard name it, restore. A wave whose only artefact
    is disclosure still owes one executed failing path.

    RUN THE REAL TREE AFTER EVERY WIDENING IN THIS TASK, not once at the end.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/DefMiner && pnpm vitest run packages/backend/src/outbound-prohibition.spec.ts 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - A disposition table is pasted with one row per finding — CR-16, the dynamic-code receivers, WR-41,
      IN-31, IN-32, IN-33 — each carrying its executed measurement, its disposition from the three-word
      vocabulary, and its one-line reason. No finding is absent and none is marked resolved without an
      executed probe.
    - CR-16's eight shapes executed and pasted before any edit, including the navigator destructure that
      reports and the direct beacon control, and re-executed and pasted after any widening.
    - Executed `grep -c -- 'navigator\[' ` over the span extracted from `.planning/REQUIREMENTS.md`,
      observed strictly greater than 0 — measured at 0 before this wave.
    - Every shape still silent after every widening in this task carries a row, and any row that
      duplicates an existing mechanism was instead cross-referenced to the existing row by id, with that
      decision stated.
    - WR-41's scope decision stated explicitly with its reason quoted from the row's own clause, and the
      five executed shapes pasted.
    - IN-31's disposition names the existing key row by id and states in one sentence that the two
      mechanisms are different, so a reader searching the span is redirected rather than misled.
      Executed `grep -c 'tagged template'` over the span pasted before and after; the before value
      measured at HEAD is 1.
    - IN-33's depth bound and pattern-kind bound stated where the closure is claimed, with both
      neighbouring spellings executed and their answers pasted rather than described.
    - Wave 33's whole-file quantifier guard executed after EVERY commit in this task, green each time,
      and `git diff` over its exclusions, its exemption map and its pinned counts shown EMPTY.
    - One mutation per branch added, executed SEPARATELY, each with its RED title and full assertion
      message pasted, each restored, `git diff --exit-code` clean between, run order stated, and
      `git log --oneline -1` pasted before each showing the real work committed first. If no branch was
      added, the row-removal mutation is executed instead and recorded the same way.
    - Real-tree run executed after each widening, 23 files and 0 violations each time, the four
      measured-exempt sites named.
    - Both byte comparisons green; span line count, row count, measured-silence count and sha256 pasted
      from both files.
    - Any prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses; if there
      were none, that is stated explicitly rather than left silent.
  </acceptance_criteria>

  <done>
    Every pass-7 outbound finding other than the three wave 33 owns has an executed measurement, a
    stated disposition and — where it stays silent — a row in the derived span. Every branch added has
    its own separately executed effect-only mutation proof and a zero real tree behind it.
  </done>
</task>

<task type="auto">
  <name>Task 3: The head-side discriminator re-derived from a varied-length sweep, the spread residual split by mechanism, the dated ledger corrections, both pointer surfaces, and the full gate set</name>

  <read_first>
    - `packages/backend/src/store/observations.spec.ts` `:1000-1135` in full. The existing head-side
      sweep runs one parameter name across seventy-one offsets and asserts the instability's SHAPE —
      non-empty, contiguous, strictly interior — rather than a literal band, which is the pattern to
      EXTEND rather than replace. Read the two exemplars it derives from the sweep's own output at
      `:1088-1119`, and note that the exemplar variables were renamed in wave 33's predecessor for the
      branch they actually take.
    - `packages/backend/src/store/schema.spec.ts` `:280-310`, the `observations.url` disclosure entry,
      which enumerates the three head-side shapes and states a stability property of the third.
    - `packages/backend/src/store/observations.ts` `:455-485`, the no-separator residual paragraph,
      which states a one-byte delta for the empty-value shape as a mechanism property. The verifier
      measured that delta at −20 with a longer name, so it is fixture-specific too.
    - `01-REVIEW.md` pass 7, WR-39 in full including the executed offset table with the 29-character
      name and the ten-character control, and its suggested replacement wording. And
      `01-VERIFICATION.md`'s `## WR-39` section, which carries the verifier's own executed run.
    - `packages/backend/src/store/error-redaction.spec.ts` `:141-158`, `:196-200`, `:407-461`,
      `:1474-1478`, `:1488-1534` and `:1523-1527`. `:196-200` is the file's OWN decision note on why an
      item naming an open class is deliberately not pinned — because a fixture would pin one example
      while reading as though it pinned the class. That note constrains the fix for WR-42 and is quoted
      in the SUMMARY.
    - `01-REVIEW.md` pass 7, WR-40 and WR-42 in full, with their executed silent shapes.
    - `.planning/REQUIREMENTS.md` CORE-11's entry and every dated correction above the
      `BEGIN DERIVED RESIDUAL` sentinel at `:116`, including wave 33's, which is the shape this task's
      corrections are written in. And the STORE-03 and STORE-07 entries.
    - `.planning/STATE.md`'s existing P9-D3 pointer amendments, so the new one matches their shape and
      the previous ones can be shown byte-identical.
    - `.planning/WINDOWS.md`'s current open entry for this gate file and the ledger header's
      instructions for the `gsd-tools windows` subcommands.
  </read_first>

  <files>packages/backend/src/store/observations.spec.ts, packages/backend/src/store/schema.spec.ts, packages/backend/src/store/observations.ts, packages/backend/src/store/error-redaction.spec.ts, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/WINDOWS.md</files>

  <reversibility rating="reversible">Test-fixture rewrites, comment corrections and ledger pointers. `observations.ts` changes by comment only and that is asserted by diff, so no shipped behaviour moves.</reversibility>

  <action>
    SWEEP THE NAME LENGTH, NOT JUST THE OFFSET, AND READ EVERYTHING OUT OF THE RUN. Extend the head-side
    sweep to run the existing offset band for at least four parameter-name lengths, chosen to bracket
    the redaction marker's own length from both sides — one character, the marker's exact length, and
    two clearly longer names. Nothing about the band or the deltas is hard-coded; the assertions are
    derived from the run exactly as the current sweep derives its band. Keep every existing assertion
    that HOLDS: the secret-absence check at both passes at every swept offset of every length, and the
    length ceiling. Those are the redaction half and they are unaffected by this finding.

    ASSERT THE BRANCH SELECTOR AND LET THE DELTA FALL OUT OF IT. What sends the two unstable shapes down
    two different branches is whether the final delimited segment carries an `=` at all: with one, the
    padding branch fires; without one, the whole-segment branch fires. THAT is what discriminates them,
    it is a property of the branch condition rather than of the exemplar, and it is what the replacement
    assertion tests. Assert that the two second-pass outputs take DIFFERENT branches. Delete the
    assertion that is byte-identical to the one fourteen lines above it — it re-asserts a fact already
    asserted and cannot detect the disagreement its own comment claims to watch for.

    STATE THE DELTA AS NAME-LENGTH DEPENDENT, WITH BOTH MEASURED PROFILES, ON ALL THREE SURFACES. The
    fixture's own comment, the `observations.url` entry in `schema.spec.ts`, and the no-separator
    residual paragraph in `observations.ts` all carry the same stability claim and all three move in the
    same commit or none do — that simultaneity is the rule this file set for itself in the sweep's own
    failure message. The corrected wording says what the branch does, states that the retained tail's
    length relative to the marker is what makes the delta negative, zero or clipped, and gives BOTH
    measured profiles: the marker-length name whose deltas are zero, and the long name whose deltas run
    to the negative bound the sweep found. Neither profile is presented as the mechanism; the branch
    condition is.

    WRITE THE REPLACEMENT PROSE SO IT DOES NOT CONTAIN THE SUPERSEDED CLAIM IN ANY SPELLING. The
    acceptance criteria below negative-grep three literals out of these files. A correction that quotes
    the sentence it corrects re-introduces it, and a `grep -c` cannot tell a quotation from an
    assertion. Preserve the superseded claim as history in the SUMMARY, which is the one place that is
    not a bound.

    SPLIT THE SPREAD RESIDUAL BY MECHANISM, WHICH IS THREE MECHANISMS AND NOT ONE. Execute all three
    shapes and confirm the reviewer's trace through the derivation walk. The first is silent because the
    object-literal position rule iterates property assignments and a spread element is not one — that is
    the only one the current text describes. The second is silent because the descent follows the
    callee's RECEIVER and never the ARGUMENTS, and the shape contains no spread at all; that mechanism
    is not named anywhere in the list today and naming it is the deliverable. The third is a call whose
    callee is a bare identifier, which is an item already listed twenty lines above, so it is
    cross-referenced there rather than restated here. Re-title the two pinned cases so their titles name
    a mechanism instead of counting shapes — a title that counts is a title that goes stale the moment a
    fourth shape is measured, which is what WR-42 then was.

    WR-42, AND HONOUR THE FILE'S OWN DECISION NOTE RATHER THAN OVERRIDING IT. Execute the four further
    shapes: the top-level spread of the caught binding straight into the returned literal, the array
    spread, and the two shapes that route the binding through a call whose arguments are not descended.
    The file's note says an item naming an open CLASS is deliberately not pinned, because a fixture
    would pin one example while reading as though it pinned the class. So state the spread half as a
    class WITH its mechanism, and pin exactly one shape — the top-level spread, because it is the
    plainest spelling an author would type and the one a reader will look for and not find. Say in the
    pinned case's own title that it pins one example of an open class.

    WRITE THE DATED LEDGER CORRECTIONS, ABOVE THE BEGIN SENTINEL, BELOW WAVE 33's. CORE-11's states, in
    this order: that the receiver-position family and the unreadable-member-on-navigator family are now
    DISCLOSED in the derived span with executed probes and counter-probes; which widenings were taken
    and which of the three conditions each passed; what remains silent and is rowed; and then, IN THE
    SAME PARAGRAPH rather than a later one, that this wave ENDED NO CLASS, that the space of spellings
    that reach a function through a value is open, that a disclosure is what the new bar asks for and a
    closure is not, and that CORE-11's checkbox is untouched and belongs to wave 35. A reader who takes
    this correction for the end of the finding stream has been misled by it, and it is written so they
    cannot be. STORE-03's and STORE-07's corrections state that both are DISCLOSURE defects and that
    neither prohibition moved, with the redaction's byte-identity and the swept-input count named.

    REGENERATE THE SPAN ONLY IF A REGISTRY ROW MOVED IN THIS TASK — it should not have — and otherwise
    assert byte-identity. NOTE THE HASHING CONVENTION so nobody re-derives it: the digest reproduces only
    when the extracted span is hashed WITHOUT a trailing newline, which is what the extractor returns; a
    naive pipe through a shell hasher includes the trailing newline and yields a different digest. That
    is a convention, not a divergence, and stating it is how a future session avoids mistaking one for
    the other.

    APPEND THE POINTER AMENDMENTS, RESTATING NO BOUND. `.planning/STATE.md` gets one naming the new
    rows, the widenings taken, and the fact that the class is open — as a POINTER at the derived span,
    not as a copy of it. Show the previous amendments byte-identical by diff. Supersede the open
    `.planning/WINDOWS.md` entry for this gate file and append a new pointer entry THROUGH
    `gsd-tools windows`, never by hand-editing the table, and paste every command with its raw output.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY. This repo has NO active git hooks — `.git/hooks`
    holds only samples and `core.hooksPath` is unset — so nothing runs them for you. And state the
    must-NOT plainly with its evidence: zero outbound-shaped tokens in shipped non-spec source across
    both roots and the frontend, one import specifier in the shipped bundle, the redaction code
    byte-identical, the real tree at zero. Every finding this wave touched is a prospective blindness in
    a test-only gate.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/DefMiner && pnpm test 2>&1 | tail -15 && pnpm typecheck && pnpm lint && pnpm knip && pnpm build:backend && pnpm check:bundle 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - The varied-length sweep is present and executed, with at least four parameter-name lengths
      bracketing the redaction marker's length from both sides, and the per-length band and delta
      profiles pasted as run output rather than described. No band and no delta is hard-coded.
    - Executed `grep -c 'shape (2) SHRINKS and shape (3) does not' packages/backend/src/store/observations.spec.ts`,
      observed 0. The replacement prose was written not to contain the superseded claim, so this gate is
      not satisfied by a quotation.
    - Executed `grep -c 'does NOT shrink' packages/backend/src/store/schema.spec.ts` and the same over
      `packages/backend/src/store/observations.ts`, both observed 0.
    - The byte-identical duplicate assertion is gone, shown by diff, and the assertion that replaced it
      asserts the two second-pass outputs take DIFFERENT branches. That replacement was watched failing:
      the branch selector inverted once, run, RED title and full assertion message pasted, restored,
      `git diff --exit-code` clean, with the real work committed first and shown by `git log --oneline -1`.
    - All three head-side disclosure sites show a diff in the same commit, and `git diff` over
      `packages/backend/src/store/observations.ts` shows COMMENT-ONLY changes with the non-comment
      changed line count pasted as 0.
    - Every existing assertion that holds survives: secret-absence at both passes at every swept offset
      of every length, and the length ceiling. The total swept-input count is pasted.
    - Executed `grep -c 'the three SPREAD shapes' packages/backend/src/store/error-redaction.spec.ts`,
      observed 0 — the two pinned cases are re-titled by mechanism rather than by count.
    - The three spread mechanisms are stated separately with their executed evidence, the
      arguments-are-never-descended mechanism is named explicitly as one the list did not carry before,
      and the bare-identifier-callee shape is cross-referenced to the existing item rather than restated.
    - The four further shapes are executed and pasted, exactly one is pinned, its title says it pins one
      example of an open class, and the file's own decision note is quoted in the SUMMARY as the reason.
    - CORE-11's dated correction present above the `BEGIN DERIVED RESIDUAL` sentinel, naming plan 01-34
      and wave 34, containing an explicit sentence — quoted verbatim in the SUMMARY — that this wave
      disclosed rather than ended a class, that the class is open because the language is open, and that
      wave 35 owns the checkbox. Every hand-written correction above it shown byte-identical by
      `git diff`.
    - STORE-03 and STORE-07 corrections present, each stating that the finding was a DISCLOSURE defect
      and that the prohibition did not move.
    - Executed `grep -n '^- \[[ x]\] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md`, observed exactly one
      row, and `git diff` shows that line unchanged — state-agnostic on purpose; wave 35 owns the box.
    - Both derived spans asserted byte-identical or regenerated together: line count, row count,
      measured-silence count and sha256 pasted from both files side by side, with the
      no-trailing-newline hashing convention stated and both digests shown.
    - `.planning/STATE.md` carries a new pointer amendment restating no bound; the previous ones shown
      byte-identical by diff.
    - Every `gsd-tools windows` command pasted with raw output; the open entry superseded and one new
      pointer entry appended; the resulting open-entry count for this gate file stated.
    - Executed `pnpm test`, exit 0, file and test counts pasted, neither lower than the pre-wave
      baseline of 31 files / 1345 tests.
    - Executed `pnpm typecheck`, `pnpm lint`, `pnpm knip` — all exit 0, output pasted.
    - Executed `pnpm build:backend && pnpm check:bundle`, observed exactly one import specifier,
      `crypto`, output pasted.
    - Executed the real-tree run over both `SOURCE_ROOTS`, observed 23 files and 0 violations, with the
      four measured-exempt sites named: `compat.ts`'s `cur[key]` and `ctx[root]`, and `observations.ts`'s
      `segments[i]` and `MIGRATIONS[MIGRATIONS.length - 1]`.
    - Executed `git diff --exit-code` over `01-01-PLAN.md` through `01-33-PLAN.md`, `01-VERIFICATION.md`,
      `01-REVIEW.md` and `01-UAT.md` — clean, output pasted.
  </acceptance_criteria>

  <done>
    The head-side discriminator is derived from a sweep across parameter-name length on all three of its
    surfaces, the spread residual names the three mechanisms it actually exhibits with one pinned
    example of an open class, the ledger carries dated corrections that say plainly that no class was
    ended, both append-only histories carry pointers restating no bound, and the full gate set is green
    and pasted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the gate's REACH to the gate's DESCRIPTION OF ITSELF | The reach cannot be completed over an open language; the description can be made derived, drift-detectable and singular. Six rounds spent themselves on the wrong side of this boundary. This wave works only on the description side and says so. |
| a disclosed silence to an undisclosed one | A widening that moves some shapes and rows none of the survivors converts a shape a reader could have found into one nobody can. That is the only way this wave can make things worse than it found them. |
| a structural twin to the shape beside it | The twins are what prove the machinery exists and is merely not reached. A row without its counter-probe is an assertion; a row with one is a measurement a reviewer can execute and disagree with. |
| a fixture's exemplar to the branch it claims to describe | A parameter name that happens to be the same length as the redaction marker turned a coincidence into a stated mechanism on three separate surfaces. The exemplar is not the mechanism and the two are not interchangeable. |
| a title that counts to a title that names a mechanism | A title counting shapes goes stale the moment a fourth is measured. Three separate findings across two passes are this exact substitution. |
| this wave's new prose to wave 33's equality-pinned guard | Every sentence added below the registry is inside that guard's reach and outside its exclusions. Widening an exclusion to accommodate a new sentence turns a neighbour's guard into decoration in one commit. |
| this wave's claim to CORE-11's completion | Disclosing two shapes is not closing them and is not progress on reach. A reader taking the ledger correction for the end of the finding stream has been misled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-189 (CR-15) | Information Disclosure | a bare global in RECEIVER position, silent family-wide including through a positively identified alias | high | mitigate | DISCLOSED BY ROW, not closed: one `kind: "measured-silence"` row per surviving mechanism, each with an executed probe and the structural twin as counter-probe, carried into the derived span in both files. A cheap, obviously correct, measured widening is taken beside it and framed as a bonus. |
| T-01-190 (CR-16) | Information Disclosure | an unreadable computed member on a positively identified `navigator` receiver, dropped where four other identified receivers report | high | mitigate | DISCLOSED BY ROW with the four reporting receivers as counter-probes, including the navigator destructure that gets the opposite answer twenty lines away in the same rule. Widening applied only if all three conditions hold, and the residual re-measured and rowed either way. |
| T-01-191 | Repudiation | a widening whose residual is unmeasured | high | mitigate | Every family executed BEFORE and AFTER, both tables pasted, every still-silent shape rowed, and the pre-branch row narrowed by measurement rather than deleted wholesale. |
| T-01-192 | Spoofing | any sentence implying the class is ended | high | mitigate | Prohibited in the frontmatter, in the objective, in the ledger correction and in every new docblock, title, clause and assertion message. The ledger correction states the non-closure in the SAME paragraph as the disclosure, not a later one. |
| T-01-193 (WR-39) | Repudiation | a fixture coincidence stated as a mechanism property on three surfaces | medium | mitigate | Re-derived from a sweep over parameter-name LENGTH bracketing the marker's length from both sides, the branch selector asserted instead of the delta, the byte-identical duplicate assertion deleted and its replacement watched failing under an inverted mutation. |
| T-01-194 (WR-40, WR-42) | Repudiation | three shapes filed under one mechanism two of them do not exhibit, and four siblings unlisted | medium | mitigate | Split into the three mechanisms actually exhibited, with the arguments-are-never-descended mechanism named for the first time; four further shapes measured, the class stated with its mechanism and exactly one plainest spelling pinned per the file's own class-versus-example note. |
| T-01-195 (WR-41) | Information Disclosure | the specifier rule's unanalysable arm unreachable through an alias, so an unreadable specifier is treated as clean | medium | mitigate | Measured across five shapes, one of two honest dispositions chosen explicitly and its reason written into the row's own clause. A residue named in an exemption reason without a measured silence is not an acceptable end state. |
| T-01-196 (IN-31) | Spoofing | the span's only tagged-template mention covers a different mechanism, so a searching reader stops looking | low | mitigate | The disposition names the existing key row by id and states that the two mechanisms differ, so the reader is redirected. Measured during planning: exactly one occurrence in the span, and it is the key row. |
| T-01-197 (IN-32, IN-33) | Repudiation | a claim of `asserted both ways` and a closure claim that does not state its depth bound | low | mitigate | IN-32 errs safe and is corrected or its claim retracted; IN-33's depth and pattern-kind bounds are stated where the closure is claimed, with both neighbouring spellings executed. |
| T-01-198 | Tampering | this wave weakening wave 33's guard to get green | high | mitigate | New prose is rewritten rather than exempted; the guard is run after every commit; `git diff` over its exclusions, its exemption map and both pinned counts is asserted EMPTY. |
| T-01-199 | Tampering | a widening firing on real shipped code | medium | mitigate | Real-tree run over both `SOURCE_ROOTS` after EVERY widening, 23 files / 0 violations, with the four measured-exempt sites named. A red tree means a measured exemption or a reverted branch, never a shipped red tree. |
| T-01-187 | Tampering | mutating uncommitted task work | high | mitigate | Every task commits its real work before planting any mutation and cross-checks with `git diff --exit-code` between them. Wave 29 destroyed its own Task 1 exactly this way; waves 30 through 33 lost nothing using commit-then-mutate. Carried forward unchanged. |
| T-01-188 | Tampering | a gate set nobody ran | medium | mitigate | No active git hooks in this repo — `.git/hooks` holds only samples and `core.hooksPath` is unset — so the full set runs by hand at every task boundary with its output pasted. Carried forward unchanged. |
| T-01-34 | Tampering | any guard whose failing path has never run | high | mitigate | One effect-only mutation per branch added, executed separately and never combined, condition and anchor line left byte-identical, each RED by title and message, each restored and re-run green, run order stated. If a task adds no branch, it still owes one executed row-removal failing path. |
| T-01-144 | Repudiation | a fresh copy of the bound in an append-only history | medium | mitigate | `.planning/STATE.md` and `.planning/WINDOWS.md` get POINTER amendments naming the rows and the widenings and restating no bound. The rule remains a prohibition with no mechanical check for those two files and is named as an unguarded limit. |
| T-01-51 | Information Disclosure | the accepted residual of the outbound walk | medium | mitigate | Grows by the rows this wave adds, generated rather than authored, and byte-pinned into the ledger by the two comparisons. |
| T-01-50 | Information Disclosure | `.spec.ts` files, which the walk skips | low | accept | Unchanged and carried forward, bounded by `pnpm check:bundle`, which specs never enter. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | NOT APPLICABLE: this plan installs no package. `pnpm install` is not run and no manifest appears in `files_modified`. RESEARCH.md's Package Legitimacy Audit is present and unchanged; no `[ASSUMED]` or `[SUS]` package is introduced, so no blocking human checkpoint is required. |
</threat_model>

<verification>
1. `git rev-parse HEAD` cross-checked against any git-status snapshot before the first edit, and
   `git status --porcelain packages/ .planning/` clean.
2. Wave 33 executed: `01-33-SUMMARY.md` present, its whole-file quantifier guard and exemption map
   present, the suite green, and both byte comparisons green BEFORE the first edit. Halt if either is
   red.
3. The receiver-position family measured BEFORE any edit — seven silent shapes and six reporting twins
   — and reconciled against the verifier's own table, with any disagreement recorded as a finding.
4. CR-15 disclosed by row FIRST, in its own commit, with both byte comparisons green in that commit.
5. Each widening tested against all three conditions in writing, with a verdict per condition, and made
   only if all three passed.
6. Every family re-measured AFTER every widening, and every shape still silent carrying a row with a
   probe, an `expect`, a counter-probe and a `counterExpect`.
7. The measured-silence count strictly greater than its pre-wave value of 16, both numbers pasted.
8. The span now carries the receiver-position and unreadable-member-on-navigator vocabulary, each
   confirmed by a positive grep whose pre-wave value was 0.
9. CR-16, the dynamic-code receivers, WR-41, IN-31, IN-32 and IN-33 each carrying an executed
   measurement, a disposition from the three-word vocabulary, and a one-line reason.
10. WR-43, WR-38 and CR-14 untouched, shown by an empty `git diff` over the gate file's header region —
    they are wave 33's and editing them here would revise this plan's own dependency.
11. Wave 33's guard green after every commit, and `git diff` over its exclusions, its exemption map and
    both its pinned counts EMPTY.
12. One effect-only mutation per branch added, executed separately, never combined, each RED by test
    title and full assertion message, each restored and re-run green, `git diff --exit-code` clean
    between them, run order stated, real work committed before each and shown by `git log --oneline -1`.
    If a task adds no branch, its row-removal mutation executed instead.
13. Real-tree run over both `SOURCE_ROOTS` after every widening — 23 files, 0 violations — with the four
    measured-exempt sites named.
14. The head-side sweep run across at least four parameter-name lengths bracketing the marker's length,
    with all bands and deltas read out of the run, the branch selector asserted, the byte-identical
    duplicate assertion deleted, and its replacement watched failing under an inverted mutation.
15. All three head-side disclosure sites changed in the same commit; `observations.ts` comment-only with
    its non-comment changed line count pasted as 0.
16. The spread residual split into the three mechanisms it exhibits, the two pinned cases re-titled by
    mechanism, the four further shapes measured, and exactly one pinned as an example of an open class.
17. Dated CORE-11, STORE-03 and STORE-07 corrections present above the `BEGIN DERIVED RESIDUAL`
    sentinel, with CORE-11's containing in one paragraph both what was disclosed and the explicit
    statement that no class was ended and wave 35 owns the box.
18. CORE-11's checkbox byte-unchanged, shown by a diff of that line.
19. Both derived spans byte-identical or regenerated together, with line count, row count,
    measured-silence count and both sha digests pasted and the no-trailing-newline convention stated.
20. `.planning/STATE.md` pointer amendment present with the previous ones byte-identical;
    `.planning/WINDOWS.md` entry superseded and a pointer entry appended through the tool, commands and
    raw output pasted.
21. `pnpm test` — exit 0, no fewer than 31 files and 1345 tests.
22. `pnpm typecheck`, `pnpm lint`, `pnpm knip` — all exit 0.
23. `pnpm build:backend && pnpm check:bundle` — exactly one import specifier, `crypto`.
24. `git diff --exit-code` over `01-01-PLAN.md` through `01-33-PLAN.md`, `01-VERIFICATION.md`,
    `01-REVIEW.md`, `01-UAT.md` — clean.
25. Every prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses, or an
    explicit statement that there were none.
</verification>

<success_criteria>
- A reader of the 510-line derived residual learns that a bare global in receiver position was silent
  and what happened to it, and learns the same about an unreadable computed member on an identified
  `navigator` — neither of which the span said anything about before this wave.
- Every widening taken passed cheap, obviously correct and measured, each condition answered in writing,
  and every shape still silent after it is on a row with an executed counter-probe.
- No sentence added anywhere by this wave states or implies that the receiver-position class or the
  unreadable-member class is ended, and the ledger says in one paragraph that a disclosure is what the
  new bar asks for and a closure is not.
- The head-side discriminator is a property of the branch condition rather than of a parameter name that
  happened to match the redaction marker's length, and it is asserted across four name lengths on all
  three surfaces at once.
- The spread residual names three mechanisms because there are three, including the one nobody had
  named, and its pinned cases are titled by mechanism rather than by count.
- Wave 33's guard is green and its exclusions, exemption map and pinned counts are byte-unchanged.
- The green baseline is restored and recorded: no fewer than 31 files / 1345 tests, typecheck / lint /
  knip clean, bundle at exactly one specifier, real tree at 23 files / 0 violations, and nothing leaks.
- CORE-11's box is exactly as wave 32 and wave 33 left it.
</success_criteria>

<artifacts_this_phase_produces>
| Artifact | Kind | Purpose |
|----------|------|---------|
| new `measured-silence` rows for the receiver-position family | generated disclosure | A reader of the residual OF RECORD learns that the plainest spelling in the whole finding set was silent, with an executed probe and a reporting twin |
| a new `measured-silence` row for the unreadable member on an identified `navigator` receiver | generated disclosure | The one receiver family where `could not read does not mean clean` did not hold, stated with the four receivers that do report as counter-probes |
| the widenings that passed all three conditions | new gate reach | A bonus beside the disclosure, each with a before-and-after table, its own effect-only mutation proof and a zero real tree |
| the before-and-after measurement tables | new evidence | The whole family executed at HEAD and again after each branch, so no widening ships with an unmeasured residual |
| the three-condition verdicts | new evidence | Cheap / obviously correct / measured answered in writing per widening, so a reviewer can disagree with the decision rather than only with the outcome |
| the WR-41, IN-31, IN-32 and IN-33 disposition table | new evidence | One executed measurement, one disposition and one reason per finding, so round 8 does not rediscover any of them as new |
| the varied-length head-side sweep | new gate fixture | The unstable band and its deltas derived across four parameter-name lengths, with the branch selector asserted instead of a fixture-specific delta |
| the corrected head-side disclosures on all three surfaces | corrected disclosure | The fixture coincidence removed from the mechanism statement in `observations.spec.ts`, `schema.spec.ts` and `observations.ts` in one commit |
| the spread residual split by mechanism | corrected disclosure | Three mechanisms named because there are three, including arguments-are-never-descended, which the list had never carried |
| one pinned top-level spread case | new gate fixture | One example of an open class, titled as one example, per the file's own class-versus-example decision note |
| CORE-11's dated wave-34 ledger correction | corrected disclosure | What was disclosed, which widenings were taken, and the explicit statement in the same paragraph that no class was ended |
| STORE-03 and STORE-07 dated corrections | corrected disclosure | Both findings recorded as disclosure defects with the prohibitions stated unmoved |
| `.planning/STATE.md` pointer amendment and the `.planning/WINDOWS.md` supersession | corrected ledger | Both append-only histories point at the derived span and restate no bound |
| `01-34-SUMMARY.md` | new evidence | Every measurement table, every disposition, every mutation proof, both span digests, the full gate set, and the sentence that this wave disclosed rather than closed |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-skeleton-persistence-compatibility/01-34-SUMMARY.md` when done.

It must contain, as pasted terminal output rather than as prose: the BEFORE table for the
receiver-position family with all seven silent shapes and all six reporting twins, reconciled against
the verifier's own table with any disagreement named; the row-first commit shown by `git log` with both
byte comparisons green in it, and one sentence stating that this commit alone satisfies the wave's bar
for that family; the three-condition verdict per widening; the AFTER table for every family; the
disposition table covering CR-16, the dynamic-code receivers, WR-41, IN-31, IN-32 and IN-33, each with
its executed measurement, its disposition and its reason; every new row quoted with its id, clause,
probe, expect, counterProbe and counterExpect; the pre- and post-wave measured-silence counts; the
positive greps over the span for the receiver-position and navigator-member vocabulary with their
pre-wave zeros; the varied-length sweep output with per-length bands and delta profiles; the three
corrected head-side disclosures shown by diff with `observations.ts`'s non-comment changed line count
pasted as 0; the spread mechanism split with all seven executed shapes; a TABLE of mutation proofs, one
row per branch added plus the inverted-selector mutation, each carrying the RED test title, the full
assertion message, the restore and the re-run green, with the run order stated, `git diff --exit-code`
shown clean between them and `git log --oneline -1` before each; the wave-33 guard green after every
commit with an EMPTY diff over its exclusions, its exemption map and its pinned counts; the real-tree
zero after every widening with all four measured-exempt sites named; the span's line count, row count,
measured-silence count and sha256 from both files side by side WITH the no-trailing-newline hashing
convention stated; the anchor greps proving append-never-rewrite in `REQUIREMENTS.md` and `STATE.md`;
every `gsd-tools windows` command with its raw output; and the final `pnpm test`, `typecheck`, `lint`,
`knip` and `check:bundle` results.

It must state in one sentence that this wave DISCLOSED two shapes and ENDED NO CLASS, that closing them
is not what earns CORE-11's `[x]` under the bar the operator adopted, that the class is open because the
space of JavaScript spellings is open, and that CORE-11's box is untouched and belongs to wave 35.

It must preserve as history — in the SUMMARY, which is the one place that is not a bound — every
superseded claim this wave deleted, including the head-side discriminator sentence and the spread
titles, so the rounds that believed them are not erased.

It must also record any PREDICTION-VS-MEASUREMENT discrepancy in the shape `01-27-SUMMARY.md` uses, and
if there were none, say so explicitly.
</output>
