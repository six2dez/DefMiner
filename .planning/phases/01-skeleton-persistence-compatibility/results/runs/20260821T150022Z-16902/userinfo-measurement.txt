# Measured, not assumed: can URL userinfo be exercised through this tier?
# Derived from curl's own -v request-line trace. No secret bytes here, and
# no Authorization header value either — that is base64 of user:secret.
userinfo_in_request_line=no
userinfo_sent_as_authorization_header=yes
request_lines_emitted=2
