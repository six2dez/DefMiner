---
phase: 01-skeleton-persistence-compatibility
plan: 45
type: execute
wave: 45
gap_closure: true
depends_on: [01-44]
files_modified:
  - packages/backend/src/outbound-prohibition.spec.ts
# ROUND 11's SECOND AND LAST PLAN. IT CARRIES THE ROUND'S OTHER CODE BLOCKER, ITS FOUR REMAINING
# WARNINGS AND ITS CLOSING BASELINE. READ THIS BLOCK BEFORE THE TASKS, AND READ PLAN 01-44's HEADER
# BLOCK TOO — its statement of the round's deletion-only thesis, its prohibitions on AST/parser/
# compiler-API/frame-identity work, on hand-rolled containment, on narrowing the recognisers, the scan
# bounds or the key format, on re-opening the 2026-08-25 re-scope and on CORE-11's checkbox apply here
# BYTE FOR BYTE and are repeated in this plan's own frontmatter rather than inherited by reference.
#
# WHY THIS PLAN RUNS SECOND, WHICH IS A DESIGN DECISION AND NOT AN ACCIDENT OF NUMBERING. Every item
# here writes bytes. Plan 01-44 removes bytes. Three of this plan's four regions overlap the regions
# plan 01-44 deletes from — CR-26's rationale sits eight lines above CR-25's deleted ceiling, WR-64's
# causal clause sits five lines below WR-61's deleted scope total, and WR-65's argument sits between
# them. RUNNING THE DELETIONS FIRST MEANS EVERY SENTENCE THIS PLAN WRITES IS WRITTEN ONCE, AGAINST THE
# FINAL SHORTENED STATE, rather than written and then partly deleted. RE-DERIVE EVERY LINE NUMBER BELOW:
# they are measured at `882ff17` and plan 01-44 has moved all of them.
#
# CR-26 — BLOCKER, AND PASS 11 CALLS IT ITS SHARPEST RESULT OF THE SESSION. `WIDEST_ANCHOR_SHADOW = 574`
# at `:10204` asserts `widestSize` — THE MAXIMUM OVER ALL ANCHORS. `widest`, the IDENTITY, is computed at
# `:10233-10240` and appears ONLY inside the failure message at `:10252`. Nothing pins it. The file
# admits the shape at `:9396`: `WHICH PINS THE MAXIMUM ONLY`.
#   EXECUTED BY PASS 11 AT FULL-SUITE GREEN, AND THIS IS STRICTLY WORSE THAN WHAT THE REVIEWER FILED.
#   The reviewer measured 574 -> 492 from an out-of-tree harness that computes only the pin. Pass 11 ran
#   it in the tree: 291 filler lines inserted at raw 250 to HOLD THE MAXIMUM AT 574, plus two lines at
#   raw 700 — one blank and one comment opening a new prose block — placed BELOW all four shipped
#   occurrences (`:436`, `:468`, `:523`, `:600`) so no occurrence's anchor changes. Result:
#   `Tests 441 passed (441)` and
#   `[[574,2,575,"…CORE-11'"],[292,994,1867,"a new prose block opening here"],[284,710,993,"SPELLING
#   (operator, by POSITION) RESOLVED BY REPORTS"],[202,…]]`. THE `:417` SHADOW — the one holding all four
#   shipped occurrences, the one FIVE prose sites (`:9097-9098`, `:9390`, `:10162`, `:10198-10200`,
#   `:10624`) and a SHIPPED FAILURE MESSAGE (`:10651`) name as 574 — FELL FROM 574 TO 284, a 290-line
#   collapse of more than half the published residual, AND THE ENTIRE SUITE WAS SILENT, not merely the
#   pin. The pin's own message at `:10252` compounds it: `If the number FELL, that is equally reportable:
#   the equality is exact so that a shadow which shrank is visible too` — true of the maximum, false of
#   the shadow the file names, and pass 11 measured it false.
#   THE FIX PASS 11 PRESCRIBES, IN TWO PARTS. (1) PIN THE IDENTITY AS WELL AS THE SIZE, in one line in
#   the same case, AGAINST A MASKED TOKEN RATHER THAN A LINE NUMBER, so the five prose sites and the
#   shipped failure message have something under them. (2) Correct the rationale to what it executes.
#   AND THE TRAP INSIDE PART (1), WHICH THIS PLAN NAMES BECAUSE THE FILE HAS A DISCIPLINE ABOUT IT: the
#   pinned literal is written INTO THE SCANNED SURFACE. This file already refuses to write declared
#   phrasings into its own surface from a pin that measures it — see the runtime-rebuilt entry literals
#   beside `UNBOUNDED_QUANTIFIERS`. The pin's own line must be PROVED not to perturb what it measures,
#   by measuring `widest` and `widestSize` before and after it lands.
#
# WR-62 — WARNING, EXECUTED BY PASS 11, AND THE REVIEWER'S HONESTY ON IT IS CONFIRMED. `EXCLUSIONS`'
# exclusion ONE — the largest at 582 lines — has BOTH endpoints located by unpinned prefix matchers at
# `:10060-10061`, while the F-11 enumeration at `:10028-10032` describes what was left unproved as
# `EXCLUSIONS`' own TWO opening locators — which are the OTHER two, at `:10062-10067`. FOUR PREFIX
# LOCATORS EXIST IN THAT DERIVATION AND THE ENUMERATION COUNTS TWO. Pass 11 planted a bare sentinel
# decoy inside the block comment at `:961`: it slid exclusion one's `from`, took 23 lines off the
# surface, and EXACTLY ONE case reported — the 574 pin, by luck of geometry, because exclusion one lies
# inside the widest shadow's raw span. NO ENDPOINT PIN FIRED, and nothing in the file discloses that this
# is where the protection comes from. THE CORRECT LOCATOR ALREADY EXISTS: `DERIVED_BEGIN` at `:6043`,
# whose live sentinel is `:983`, and `DERIVED_END` at `:6044`, whose live sentinel is `:1564`.
#   AND THE SAME TRAP CR-23 CLOSED: the pin must be an INDEPENDENT expression from the locator it pins.
#   `EXCLUSIONS`' own prefix matchers stay prefix matchers; the pin is a full-line equality against the
#   exported sentinel constants, run afterwards. Do NOT rewrite the derivation's own locators.
#
# WR-63 — WARNING, EXECUTED BY PASS 11, AND IT IS WR-58's OWN MISDIAGNOSIS ONE BOUNDARY VALUE OVER. The
# census guard at `:10816` is `if (at < 0)` where the round-9 fix text prescribed `at <= 0`. Pass 11
# planted a hand-written key OPENING with the separator into `HEADER_QUANTIFIER_EXEMPTIONS`:
# `Tests 3 failed | 438 passed (441)`, and the census fired with the AMBIGUITY message on an EMPTY
# construct half — the exact misdiagnosis WR-58 was raised to end. PASS 11'S OWN ADDITION: the prefix
# case at `:10737-10741` has the MIRROR defect. Its guard `toBeGreaterThan(0)` correctly catches a
# separator at index 0, but its message says the key carries no separator, which is FALSE of a key that
# OPENS with one. THE CORRECTLY-GUARDED SITE HAS THE WRONG MESSAGE AND THE CORRECTLY-MESSAGED SITE HAS
# THE WRONG GUARD, and both must name BOTH shapes.
#
# WR-64 — WARNING, CONFIRMED IN THE BYTES. `:10201-10203` says every surface line in `419..1574` resolves
# to the `:417` anchor BECAUSE between the rule at `:418` and the imports no line at all is accepted by
# any of the five recognisers. MEASURED: exactly one line in that range IS accepted — `:1573`, the `/**`
# opening the `THE SOURCE ROOTS THE PLUGIN SHIPS` docblock — and it sits BELOW the imports at
# `:1567-1571`, not between them and the rule. The real mechanism for the last two lines is the WR-53
# extension: `constructTokenOf("/**")` is null, so the forward walk falls through and the backward scan
# continues. `:10250-10254` instructs a future author to re-derive the pin against growth attributed line
# by line; an author re-deriving from the stated reason gets the geometry wrong AT THE BOUNDARY.
#
# WR-65 — WARNING, EXECUTED. `:10190-10192` argues that a pin which must be re-derived on routine edits
# is a pin nobody trusts and everybody re-derives without reading — EIGHTEEN LINES ABOVE a pin that is
# exactly that. One comment line inserted at `:500`, inside this file's opening header prose which is
# edited most rounds, gives `Tests 1 failed | 440 passed (441)`. Pass 11 is explicit that the behaviour is
# defensible and that it would KEEP the pin; what is not defensible is supplying, in the pin's own comment
# block, the argument the next round will quote while ripping it out.
#   THE SHAPE OF THE FIX, AND IT IS THE ROUND'S ONE PIECE OF GENERAL GUIDANCE: PREFER AN AUTHORISATION TO
#   A MEASUREMENT. Delete the self-undermining argument and, in its place, PRE-AUTHORISE the routine bump
#   — state what a future author is PERMITTED to do. A policy sentence makes no claim about this file's
#   contents and therefore cannot be falsified by measuring this file, so it cannot become round 12's
#   blocker. WR-64's fix is a MEASUREMENT and has no such shelter: it must carry its reach INSIDE the
#   claiming clause.
#
# THE MUST-NOT DOES NOT MOVE AND NOTHING LEAKS — RE-ESTABLISHED TWICE ON 2026-08-26, INDEPENDENTLY BY
# PASS 11 AND BY THE SECURITY AUDITOR: `pnpm test` 31 files / 1381 tests exit 0, `tsc --build` exit 0,
# bundle at ONE specifier `crypto`, gate suite 441, 23/23 shipped modules at zero violations,
# byte-identical rebuild, and the gate's scan target byte-identical to the installable artifact. EVERY
# ITEM IN THIS ROUND IS A TEST-ONLY GATE'S DESCRIPTION OF ITSELF. The one real risk is an edit that
# WEAKENS the outbound gate, and every check here is pointed at that.
#
# THE BOX DOES NOT MOVE, AND THIS PLAN IS THE LAST CHANCE TO GET THAT WRONG. `.planning/REQUIREMENTS.md:46`
# reads `- [ ] **CORE-11**`, `CORE11_BOX_EXPECTED` at `:11414` agrees, round 10 did not flip it and
# neither of its plans ran `requirements mark-complete`. PRESERVE THAT PATTERN. Pass 12 makes the call.

autonomous: true
requirements: [CORE-11]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 2
  confidence: low
# `estimate-calibration` reports `{"factor": 1, "applied": false, "sample_count": 0, "min_samples": 3}`,
# so `tokens` equals `raw_tokens` and the confidence is DERIVED from the sample count, never self-rated.
# WEIGH THE MEASUREMENT COUNT OVER THE TOKEN FIGURE: one identity pin watched RED against a 293-line
# mutation WHILE THE SIZE PIN STAYS GREEN IN THE SAME RUN, one endpoint pin watched RED against pass 11's
# own sentinel decoy, one boundary re-measured line by line across 1,156 raw lines, one guard watched
# firing with the right diagnosis instead of the wrong one, and the round's closing baseline executed
# rather than carried.

must_haves:
  truths:
    - "THE IDENTITY IS PINNED, NOT ONLY THE SIZE. The case that measures the widest anchor shadow asserts BOTH the maximum's width AND the masked token of the anchor that owns it, against a literal rather than a line number, in the same case. The five prose sites and the shipped failure message that name that shadow now have something under them."
    - "THE IDENTITY PIN IS WATCHED RED AGAINST THE EXACT MUTATION THAT WAS GREEN, AND THE SIZE PIN IS WATCHED GREEN IN THE SAME RUN. Pass 11's 293-line mutation — 291 filler lines holding the maximum at 574 plus two lines below all four shipped occurrences — gave `Tests 441 passed (441)` at `882ff17`. After this plan the identity pin is RED and the size pin is GREEN in one run, which is what proves the new pin does independent work rather than duplicating the old one."
    - "THE PIN'S OWN LINE IS PROVED NOT TO PERTURB WHAT IT MEASURES. `widest` and `widestSize` are measured before and after the pinned literal lands and both are unchanged; the uniqueness census is re-run and the anchor's producer count is still exactly one."
    - "EXCLUSION ONE'S ENDPOINTS ARE PINNED AGAINST THE EXPORTED SENTINELS, AND THE PIN IS AN INDEPENDENT EXPRESSION. `DERIVED_BEGIN` and `DERIVED_END` locate the endpoints by full-line equality in an assertion run AFTER the derivation; the derivation's own prefix matchers are untouched. Pass 11's sentinel decoy is watched turning the new pin RED rather than being caught by luck of geometry."
    - "THE CENSUS DIAGNOSES MALFORMATION AS MALFORMATION AT BOTH BOUNDARY VALUES, AND BOTH MESSAGES NAME BOTH SHAPES. The guard rejects an index of zero as well as a negative one, and pass 11's hand-written key that OPENS with the separator is watched producing the malformation diagnosis instead of the ambiguity one, with both the before and the after message pasted."
    - "THE BOUNDARY MECHANISM IS STATED FROM A MEASUREMENT TAKEN IN THIS SESSION, WITH ITS REACH INSIDE THE CLAIMING CLAUSE. Every line of the anchor's raw span is evaluated against the five recognisers, the accepted set is pasted, and the sentence written names that set and the fall-through that actually carries the boundary."
    - "THE PIN'S RATIONALE NO LONGER ARGUES AGAINST THE PIN. The self-undermining argument is removed and replaced by an AUTHORISATION — a statement of what a future author is permitted to do — which makes no claim about this file's contents and so cannot be falsified by measuring it."
    - "THE ROUND'S BASELINE IS EXECUTED, NOT CARRIED, AND ARRIVAL IS NOT CONFUSED WITH CURRENT. The whole suite, `tsc --build`, `check:bundle`, the gate suite's own count, the module walk, `SURFACE_LINES.length` and the shipped-source diff are all re-measured at the round's close and every difference from arrival is attributed to a named cause."
    - "NOTHING LEAKS AND NO SHIPPED SOURCE BYTE CHANGED. The must-NOT is re-executed rather than cited, and the whole of round 11 is shown to have touched exactly one file, none of it shipped."

  artifacts:
    - path: "packages/backend/src/outbound-prohibition.spec.ts"
      provides: "An identity pin under the residual the file publishes in six places, an endpoint pin under the largest exclusion, a census that diagnoses malformation at both boundary values, a boundary mechanism stated from a fresh measurement, and a pin rationale that authorises its own routine bump instead of arguing against it."
      contains: "WIDEST_ANCHOR_SHADOW"

  key_links:
    - from: "a pin over the maximum, with the named shadow's identity appearing only inside a failure message"
      to: "a pin over the maximum AND the masked token of the anchor that owns it"
      via: "one literal in the same case, proved not to perturb the measurement, watched RED against the 293-line mutation that collapsed the named shadow 574 -> 284 at full-suite green"
      pattern: "WIDEST_ANCHOR_SHADOW"
    - from: "the largest exclusion's two endpoints located by unpinned prefix matchers and protected only by the luck of lying inside the widest shadow"
      to: "two endpoints pinned against the exported sentinel constants by an independent full-line equality"
      via: "`DERIVED_BEGIN` and `DERIVED_END`, already exported at `:6043-6044`, asserted after the derivation rather than substituted into it, with pass 11's sentinel decoy watched turning the pin RED"
      pattern: "DERIVED_BEGIN"
    - from: "a guard that reports a hand-written key against a header that is not in the file as an AMBIGUITY"
      to: "a guard that reports it as the MALFORMATION it is, at both boundary values, from both call sites"
      via: "the index comparison widened by one value and both messages widened to name both shapes, watched failing with the planted key"
      pattern: "EXEMPTION_ANCHOR_SEP"
    - from: "a rationale supplying the argument the next round will quote while ripping the pin out"
      to: "a rationale that pre-authorises the routine bump"
      via: "the self-undermining measurement deleted and an authorisation written in its place, because a policy cannot be falsified by measuring this file"
      pattern: "re-derive"

  prohibitions:
    - requirement_id: CORE-11
      category: security
      status: resolved
      verification: gate
      resolution: "A structural check over the ROUND-BASE DIFF shows ZERO added or removed lines carrying the compiler-API import form or a compiler-API member access, ZERO added non-comment lines carrying a hand-rolled containment spelling, and ZERO added or removed lines carrying any recogniser, scan-bound or key-format token; and every body-range byte-identity pair is asserted at 0 diff lines with BOTH sides at an exact, separately re-derived length"
      statement: "MUST NOT build an AST, a parser, a compiler-API identity or an enclosing-frame identity by any spelling, MUST NOT hand-roll containment by brace depth or indentation, and MUST NOT narrow the recognisers, the scan bounds or the key format. Pass 11 explicitly did NOT report their absence as a defect and none of its prescribed fixes asks for any. `import ts from \"typescript\"` at `:1570` predates this round; every check is scoped to the round-base diff so a pre-existing line can neither satisfy nor violate them."
    - requirement_id: CORE-11
      category: security
      status: resolved
      verification: gate
      resolution: "The pinned identity literal shown by diff to be a masked token and to contain no line number; `widest` and `widestSize` measured before and after it lands and both shown unchanged; the uniqueness census re-run and the anchor's producer count shown at exactly one; the whole-file declared-phrasing guard shown green"
      statement: "MUST NOT fold a line number into the identity pin, and MUST NOT let the pin's own line change what the pin measures. Pass 11 prescribes a masked token precisely because a line number slides the first time anything above it grows, and this file already refuses to write a declared phrasing into its own scanned surface from a pin that measures that surface."
    - requirement_id: CORE-11
      category: security
      status: resolved
      verification: gate
      resolution: "`git diff` over the `EXCLUSIONS` derivation asserted to REMOVE none of its own opening locators by a numeric check, and the two forms shown side by side to be a prefix matcher and a full-line equality"
      statement: "MUST NOT change `EXCLUSIONS`' own opening locators to the full-line form and MUST NOT delete any `.from` assertion. CR-23 closed because the two sides are INDEPENDENT expressions; a pin that recomputes the expression it pins holds for any file at all, and pass 11 confirmed both of CR-23's pins go red against their decoys. WR-62 adds a pin; it does not rewrite a locator."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "Each new pin planted against with pass 11's own mutation and observed RED with its FULL message pasted, then restored and the tree proved clean; the identity pin's demonstration additionally required to show the SIZE pin GREEN in the same run; the census guard planted against and observed producing the malformation diagnosis with both the before and after messages pasted"
      statement: "MUST NOT ship a pin or a guard that has never been watched failing. Four consecutive rounds shipped a gate that passed with its own defect present. Plant the violation, watch the gate, keep only what goes red. If a planted violation does NOT go red, the task stops, the diagnosis is written up as a first-order finding, and nothing is adjusted to force a red."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "The whole round's diff over the pin's comment block asserted NET NON-POSITIVE in line count, and the deletion prohibition from plan 01-44 re-asserted by re-running its numeric greps at the close of this plan"
      statement: "MUST NOT re-author, in any form, a sentence plan 01-44 deleted. The eleven numeric greps that prove CR-24, CR-25, CR-27 and WR-61 absent are re-run at this plan's close and must still return zero. This plan writes four sentences and no more: the identity pin's rationale, the boundary mechanism, the authorisation, and the disclosure of what the new pins do not do."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "`git status --porcelain .planning/` asserted EMPTY numerically at every task boundary; `requirements mark-complete` present in no command run; `git diff` over `.planning/REQUIREMENTS.md` asserted at zero lines; `CORE11_BOX_EXPECTED` asserted byte-unchanged and still reading the unchecked form"
      statement: "MUST NOT flip CORE-11's checkbox and MUST NOT revert it either. It has stood at `[ ]` for four consecutive rounds with the pin, the ledger row and every verification agreeing. Pass 11 states the determination is its own and that the box may not move. Pass 12 owns the question, and this plan is the last place in round 11 that could get it wrong."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "Every unadjudicated finding named anywhere in this plan is stated as reviewer-reported and not independently re-executed wherever it is mentioned, and none appears in any list of closed items"
      statement: "MUST NOT present IN-45, IN-46, IN-48, IN-49 or WR-57 as verified or as closed. Pass 11 records them as reported and NOT re-executed while five blockers were open; WR-57 has been UNADJUDICATED since round 9 and the file says so at `:10811-10813`. Anything measured about them here is a NEW measurement stated as such."
    - requirement_id: CORE-11
      category: safety
      status: open
      verification: gate
      resolution: "DISCLOSED, NOT CLOSED — stated wherever this plan's result is stated, in the bytes and in the SUMMARY, that pinning the identity watches ONE shadow and leaves every other non-maximal shadow unwatched; that pinning exclusion one's endpoints DETECTS a slide rather than preventing one; that widening a diagnosis does not widen what the census reaches; that a boundary re-measured today is a measurement of this file at this commit and not a property of the builder; and that closing this list makes the currently-known contradictions zero without making criterion (3) provable"
      statement: "MUST NOT state or imply that this round closes criterion (3), that the residual is now bounded by anything other than what is measured, that an authorisation is a proof, or that `threats_open` is zero — pass 12 measures that. Pass 11's own finding is that criterion (3) as written may not be dischargeable by ANY amount of prose correction, for the reason this file documents at `:5975-5979` about a structurally identical guard. The signature defect stands at ELEVEN instances and round 10 authored five more while correcting it."
---

<objective>
Put a pin under the residual this file publishes in six places, put a pin under the largest exclusion's
two endpoints, make the census diagnose malformation as malformation at both boundary values, state the
boundary mechanism from a fresh measurement, and stop the pin's rationale arguing against the pin — then
execute the round's closing baseline rather than carrying it.

Purpose: pass 11 found that exit 2's MECHANISM half converged and its DISCLOSURE half did not. Plan 01-44
closed the disclosure half by removing bytes. This plan closes the mechanism half, which is the half
that is checkable, and it writes exactly four new sentences to do it.

Output: one gate-spec file in which the 574-line shadow the file names has an identity pin under it that
has been watched going red while the size pin stayed green, and in which every remaining warning from
pass 11 is closed by a guard that has been watched failing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md
@.planning/phases/01-skeleton-persistence-compatibility/01-SECURITY.md
@.planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md
@.planning/phases/01-skeleton-persistence-compatibility/01-44-SUMMARY.md
@packages/backend/src/outbound-prohibition.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-26, WR-64 and WR-65 — the named shadow's identity pinned against a masked token and watched RED while the size pin stays GREEN in the same run, the boundary mechanism re-measured line by line, and the rationale corrected and pre-authorised so it stops arguing against its own pin</name>

  <files>packages/backend/src/outbound-prohibition.spec.ts</files>

  <reversibility rating="costly">
    The identity pin becomes a permanent obligation on every future round: any edit that changes which
    anchor owns the maximum turns it red, and WR-65 exists precisely because this file already found that
    burden hard to carry. Reverting the pin is one commit; reverting the expectation it sets is not. Pass
    11 is explicit that it would KEEP the pin and that the burden is defensible — the mitigation is the
    pre-authorisation this task writes, not a weaker pin.
  </reversibility>

  <precondition>
    Plan 01-44 has executed and committed BOTH tasks and `01-44-SUMMARY.md` exists on disk. `git rev-parse
    HEAD` pasted and `git merge-base --is-ancestor 882ff17 HEAD` asserted true. `git status --porcelain
    packages/ scripts/ .planning/` clean and PASTED. The round baseline file at
    `"${TMPDIR:-/tmp}/defminer-round11-baseline"` exists and every variable reads back; if it is absent,
    RE-CREATE it from the round base and say so in the SUMMARY, stating explicitly which values had to be
    reconstructed from `git show 882ff17` rather than read — a drifting arrival value makes every
    comparison in this plan meaningless. PLAN 01-44's ELEVEN DELETION GREPS RE-RUN AND ALL STILL AT ZERO
    before the first edit; if any has come back, HALT and write it up, because this plan must not be the
    round that re-authors what the round deleted. Gate suite green at plan 01-44's recorded count, `pnpm
    test` exit 0 with counts, `tsc --build` exit 0, `check:bundle` at one specifier, and the current
    widest-shadow maximum re-read and pasted. HALT on any red. No active git hooks: run the gate set by
    hand at the boundary.
  </precondition>

  <read_first>
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md` — the `artifacts:` entry
      beginning `CR-26 — BLOCKER, REPRODUCED AND STRENGTHENED BY ME AT FULL-SUITE GREEN`, the entries for
      WR-64 and WR-65, the `missing:` entry beginning `CR-26 — TWO CHANGES, ONE CODE AND ONE DELETION`,
      and the body section `### CR-26 — CONFIRMED, and I have a strictly sharper instance than the one
      filed.` (~`:210-246`) IN FULL. The body carries the exact mutation — 291 filler lines at raw 250
      and two lines at raw 700 — and the resulting shadow table. RE-DERIVE the numbers; a third value is
      a finding.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md` — the body section
      `### The reviewer's central framing — adjudicated` (~`:308-336`). It records the FOUR directions
      pass 11 pushed the size pin from and could not break, and names the axis it advertises versus the
      axis it was built for. That distinction is what this task's disclosure sentence must respect.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-REVIEW.md`, the round-10 section
      (~`:6127-6890`), `### CR-26`, `### WR-64` and `### WR-65`. USE THE SHAPE, MEASURE THE NUMBERS: the
      reviewer measured 574 -> 492 from out of tree and pass 11 measured 574 -> 284 in tree.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the whole comment block and case at
      `:10145-10254`: the shadow definition `:10150-10163`, the exactness rationale `:10165-10168`, the
      reach list `:10170-10185`, the maximum-only disclosure `:10187-10193`, the wave-42 measurement
      `:10195-10203`, `WIDEST_ANCHOR_SHADOW` at `:10204`, the case at `:10206-10254` with its four
      non-vacuity counts `:10219-10231`, the `widest`/`widestSize` loop `:10233-10240`, the rule at
      `:10246-10253` and its failure message at `:10252`. PLAN 01-44 HAS DELETED FROM THIS BLOCK — reach
      statement (2)'s quantitative clause and the wave-42 scope total are gone. RE-DERIVE EVERY LINE.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the five prose sites that name the shadow as
      574 (`:9097-9098`, `:9390`, `:10162`, `:10198-10200`, `:10624`) and the shipped failure message at
      `:10651`. These are what the identity pin is being installed to support; READ THEM AND CHANGE NONE
      OF THEM in this task.
    - `packages/backend/src/outbound-prohibition.spec.ts` — `constructAnchorFor` and its docblock
      `:9375-9420`, `constructTokenOf`, and the imports at `:1567-1571` with the docblock opening at
      `:1573`. WR-64's boundary is at `:1573` and the mechanism is the WR-53 forward-walk fall-through;
      read the builder, DO NOT CHANGE IT — it is one of the eight byte-identity subjects.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-42-SUMMARY.md` — the recorded shadow
      distribution. Pass 11's `:10192-10193` points at it as the evidence the file deliberately did NOT
      inline. Read it; do not copy it into the file.
  </read_first>

  <behavior>
    - Unmodified file, after this task: the size pin is GREEN at the re-derived maximum and the identity
      pin is GREEN against the masked token of the anchor that owns it.
    - Pass 11's 293-line mutation (291 filler lines holding the maximum plus two lines below all four
      shipped occurrences): THE SIZE PIN IS GREEN AND THE IDENTITY PIN IS RED, in the same run. At
      `882ff17` this mutation gave `Tests 441 passed (441)`.
    - One comment line inserted inside the opening header prose: the SIZE pin is RED, as it already is
      today. That behaviour is kept deliberately and is what the authorisation covers.
    - The pinned literal's own line: measured before and after, `widest` and `widestSize` are unchanged
      and the anchor's producer count is still exactly one.
  </behavior>

  <action>
    RE-DERIVE EVERY LINE NUMBER IN `<read_first>` BY IDENTIFIER AND BY CASE TITLE BEFORE THE FIRST EDIT.
    Plan 01-44 removed lines from this exact block and from the docblock above it. Record every
    disagreement as a finding.

    MEASURE FIRST. Paste, from inside the suite: the full shadow table's top four entries as
    `[size, lo, hi, token]`, `SURFACE_LINES.length`, all three exclusion spans, and the current
    `WIDEST_ANCHOR_SHADOW` value. Reconcile against pass 11's `9285` surface,
    `[[983,1564],[5985,5995],[4161,5793]]` and a maximum of 574 owned by the header row at `:417`. PLAN
    01-44 CHANGED THE LINE COUNT, so the surface figure is expected to differ; attribute the difference to
    01-44's own diffstat and record it. If the MAXIMUM moved, attribute it line by line before going
    further — a pin re-derived against an unattributed number is the defect this file has spent twelve
    waves removing.

    CR-26 PART ONE, THE IDENTITY PIN. In the same case, beside the existing size assertion, add ONE
    assertion comparing the computed `widest` against a literal masked token declared beside
    `WIDEST_ANCHOR_SHADOW`. THE LITERAL IS THE MASKED TOKEN, NOT A LINE NUMBER AND NOT A RAW SPAN — pass
    11 prescribes a masked token precisely because a line number slides the first time anything above it
    grows. Give it a failure message that names the token the pin expects, the token actually found, and
    what a difference means: that the anchor owning the maximum has CHANGED IDENTITY, which is the shape
    that collapsed the named shadow by 290 lines while the maximum held.

    THEN PROVE THE PIN'S OWN LINE INNOCENT, BECAUSE IT IS WRITTEN INTO THE SURFACE IT MEASURES. Measure
    `widest` and `widestSize` immediately before and immediately after the literal lands and paste both
    pairs; re-run the uniqueness census and paste the anchor's producer count, which must be exactly one.
    If the literal perturbs either, RESHAPE THE PIN — do not adjust the expected value, and do not touch
    the recognisers. Run the whole-file declared-phrasing guard and show it green.

    CR-26 PART TWO, THE RATIONALE. The exactness rationale currently claims the equality makes both
    directions loud without saying of WHAT. Correct it to what executes, and the corrected statement now
    has THREE parts because the identity pin changes it: the equality makes both directions of THE
    MAXIMUM loud; the identity pin makes a change in WHICH anchor owns the maximum loud; and a shadow
    that is neither the maximum nor the one under the identity pin is not seen at all. Cite the executed
    collapse — the value it fell from, the value it fell to, and that the whole suite was green — as a
    dated measurement by pass 11, not as a live fact. THE WHOLE CORRECTION MUST BE NET NON-POSITIVE IN
    LINES against the block it replaces; this round's prose delta is negative and this task does not
    reverse that.

    WR-64, AND IT IS A MEASUREMENT SO IT CARRIES ITS REACH INSIDE THE CLAIMING CLAUSE. Evaluate the five
    recognisers at EVERY line of the anchor's raw span and paste the accepted set. Pass 11 measured
    exactly one accepted line, the docblock opener below the imports, and identified the real mechanism as
    the WR-53 forward-walk fall-through. RE-MEASURE IT; if the accepted set differs from pass 11's, that
    is a finding and the sentence follows YOUR measurement. Then replace the false causal clause with one
    that names the accepted set and the fall-through, with the reach inside the clause and not in a
    neighbouring sentence. Do not extend it into a claim about any other range.

    WR-65, AND IT IS AN AUTHORISATION SO IT HAS NO SUCH BURDEN. Delete the sentence in that same
    block asserting that a pin needing re-derivation on ordinary edits earns no reader's confidence and
    gets re-derived unread — the one pass 11 quotes as arguing against the pin eighteen lines below it.
    In its place write ONE sentence
    PRE-AUTHORISING the bump: state that a change to the pinned value attributable line by line to lines
    the same commit added inside the anchor's raw span is authorised, needs no finding, and requires the
    attribution shown in the commit — and that an unattributable change is not authorised. THIS IS A
    POLICY, NOT A MEASUREMENT: it makes no claim about this file's contents and therefore cannot be
    falsified by measuring this file. Keep the existing reason for pinning only the maximum, which stands
    on its own. Do not weaken the pin and do not widen it to the distribution.

    NOW WATCH THE IDENTITY PIN FAIL, AGAINST THE EXACT MUTATION THAT WAS GREEN. Plant pass 11's two
    edits together: 291 filler comment lines at raw 250, whose predecessors are already comments so the
    fifth recogniser does not accept them, to HOLD the maximum at its current value; and two lines at raw
    700 — one blank and one comment opening a new prose block — placed BELOW all four shipped occurrences
    so no occurrence's anchor changes. Run the gate suite. THE REQUIRED RESULT IS BOTH FACTS IN ONE RUN:
    the size pin GREEN and the identity pin RED. Paste the failing title, the FULL assertion message, and
    the shadow table showing two anchors at the maximum with the named one collapsed. Paste the size
    pin's tick from the same run — WITHOUT IT THE DEMONSTRATION PROVES NOTHING, because a mutation that
    reddens both pins would have been caught by the pin that already existed. Cite pass 11's
    `Tests 441 passed (441)` for the same mutation. Then restore and prove the tree clean by diff.

    EXPECT OTHER CASES TO SPEAK. Pass 11 records that at the reviewer's placement the exemption case fired
    at `Tests 2 failed | 440 passed (442)` and that at its own placement the suite was silent. Record
    exactly which cases fire under YOUR placement and do not adjust the placement to produce a cleaner
    result — an inconvenient extra failure is evidence, not noise.

    IF EITHER REQUIRED OUTCOME DOES NOT HAPPEN — the identity pin green, or the size pin red — STOP. Write
    the diagnosis up as a first-order finding and change nothing to force the result.

    THE FOURTH SENTENCE, AND THE LAST ONE THIS TASK WRITES. Disclose what the identity pin does NOT do: it
    watches ONE shadow, the one that owns the maximum, and every other non-maximal shadow remains
    unwatched in both directions. Reach inside the clause. Do not add a fifth sentence qualifying it.

    RE-RUN PLAN 01-44's ELEVEN DELETION GREPS AT THE CLOSE and show all eleven still at zero. This task
    writes in the same regions 01-44 emptied and re-authoring any of it is the round's worst outcome.

    RE-READ EVERY SENTENCE THIS TASK WRITES AGAINST THE NO-OVERCLAIM PROHIBITION AND RECORD A VERDICT PER
    SENTENCE, reach claimed beside reach executed. The corrected rationale is this task's likeliest place
    to author the twelfth instance, because it is a sentence about what a pin makes visible.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY. COMMIT SEPARATELY FROM TASK 2.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && SP=packages/backend/src/outbound-prohibition.spec.ts; TD="${TMPDIR:-/tmp}"; B="$TD/defminer-round11-baseline"; [ -f "$B" ] || { echo "MISSING ROUND-11 BASELINE FILE"; exit 1; }; . "$B"; RB=$(git rev-parse 882ff17) || { echo "ROUND BASE UNRESOLVABLE"; exit 1; }; [ -n "$RB" ] || { echo "BASE REV EMPTY — AN EMPTY REV MAKES git show READ THE INDEX, WHICH EQUALS THE WORKTREE, SO EVERY BODY DIFF BELOW WOULD BE 0"; exit 1; }; git merge-base --is-ancestor "$RB" HEAD || { echo "882ff17 IS NOT AN ANCESTOR OF HEAD"; exit 1; }; pnpm exec vitest run "$SP" > "$TD/r11.gate3.log" 2>&1; GRC=$?; cat "$TD/r11.gate3.log"; [ "$GRC" -eq 0 ] || { echo "GATE SUITE RED (rc=$GRC) — A COUNT PARSED OUT OF A RED RUN IS THE DEFECT: THE PARENTHESISED TOTAL RISES WHEN A NEW CASE FAILS"; exit 1; }; GT=$(grep -E '^[[:space:]]*Tests[[:space:]]' "$TD/r11.gate3.log" | tail -1 | sed -n 's/.*[^0-9]\([0-9][0-9]*\) passed.*/\1/p'); [ -n "$GT" ] || { echo "GATE PASSED-COUNT UNPARSEABLE"; exit 1; }; [ "$GT" -ge "$BASE_GATE_ARRIVAL" ] || { echo "GATE COUNT $GT BELOW ARRIVAL $BASE_GATE_ARRIVAL — A CASE WAS LOST"; exit 1; }; echo "GATE-PASSED=$GT ARRIVAL=$BASE_GATE_ARRIVAL"; pnpm test > "$TD/r11.full3.log" 2>&1; FRC=$?; cat "$TD/r11.full3.log"; [ "$FRC" -eq 0 ] || { echo "WHOLE SUITE RED (rc=$FRC)"; exit 1; }; FT=$(grep -E '^[[:space:]]*Tests[[:space:]]' "$TD/r11.full3.log" | tail -1 | sed -n 's/.*[^0-9]\([0-9][0-9]*\) passed.*/\1/p'); [ -n "$FT" ] || { echo "SUITE PASSED-COUNT UNPARSEABLE"; exit 1; }; FF=$(grep -E '^[[:space:]]*Test Files[[:space:]]' "$TD/r11.full3.log" | tail -1 | sed -n 's/.*[^0-9]\([0-9][0-9]*\) passed.*/\1/p'); [ -n "$FF" ] || { echo "FILE PASSED-COUNT UNPARSEABLE"; exit 1; }; [ "$FT" -ge "$BASE_TESTS_ARRIVAL" ] || { echo "SUITE COUNT $FT BELOW ARRIVAL $BASE_TESTS_ARRIVAL"; exit 1; }; [ "$FF" -eq "$BASE_FILES" ] || { echo "FILE COUNT $FF != $BASE_FILES"; exit 1; }; pnpm exec tsc --build || { echo "tsc --build RED — THE ASSERTIONS BELOW ARE NOT RUN"; exit 1; }; pnpm typecheck || { echo "typecheck RED"; exit 1; }; pnpm lint || { echo "lint RED"; exit 1; }; pnpm knip || { echo "knip RED"; exit 1; }; pnpm build:backend || { echo "build:backend RED"; exit 1; }; pnpm check:bundle || { echo "check:bundle RED"; exit 1; }; M=$(find packages/*/src -name '*.ts' ! -name '*.spec.ts' | grep -c . || true); [ "$M" -eq "$BASE_MODULES" ] || { echo "MODULE COUNT $M != $BASE_MODULES"; exit 1; }; IDP=$(grep -c 'WIDEST_ANCHOR_TOKEN' "$SP" || true); [ "$IDP" -ge 2 ] || { echo "THE IDENTITY PIN IS NOT DECLARED AND ASSERTED: WIDEST_ANCHOR_TOKEN APPEARS $IDP TIME(S), EXPECTED AT LEAST 2"; exit 1; }; IDA=$(grep -c 'toBe(WIDEST_ANCHOR_TOKEN)' "$SP" || true); [ "$IDA" -eq 1 ] || { echo "THE IDENTITY PIN IS NOT AN EXACT EQUALITY AGAINST THE DECLARED TOKEN: $IDA SITE(S)"; exit 1; }; IDN=$(grep -n 'WIDEST_ANCHOR_TOKEN' "$SP" | head -1 | cut -d: -f1); IDNUM=$(sed -n "${IDN}p" "$SP" | grep -cE '[0-9]{3,}' || true); [ "$IDNUM" -eq 0 ] || { echo "THE IDENTITY PIN'S LITERAL CARRIES A LINE-NUMBER-SHAPED VALUE — PASS 11 PRESCRIBES A MASKED TOKEN BECAUSE A LINE NUMBER SLIDES"; exit 1; }; SZP=$(grep -c 'toBe(WIDEST_ANCHOR_SHADOW)' "$SP" || true); [ "$SZP" -eq 1 ] || { echo "THE SIZE PIN WAS REPLACED RATHER THAN JOINED: $SZP SITE(S) ASSERT WIDEST_ANCHOR_SHADOW"; exit 1; }; W65=$(grep -c 'is a pin nobody trusts' "$SP" || true); [ "$W65" -eq 0 ] || { echo "WR-65's SELF-UNDERMINING ARGUMENT STILL PRESENT: $W65"; exit 1; }; W64=$(grep -c 'no line at all is accepted by any' "$SP" || true); [ "$W64" -eq 0 ] || { echo "WR-64's FALSE CAUSAL CLAUSE STILL PRESENT: $W64"; exit 1; }; C24A=$(grep -c 'Located by text TODAY' "$SP" || true); C24B=$(grep -c 'two identically-headed table headers sit at lines' "$SP" || true); C25=$(grep -cE 'grow to 573' "$SP" || true); C27A=$(grep -c 'has stopped resolving the enclosing construct' "$SP" || true); C27B=$(grep -c 'so the anchor is not resolving the enclosing construct' "$SP" || true); C27C=$(grep -c 'the anchor is not resolving the ENCLOSING construct' "$SP" || true); C27D=$(grep -c 'claims that a well-formed anchor identifies anything' "$SP" || true); W61A=$(grep -c '8,846' "$SP" || true); W61B=$(grep -c '11,417' "$SP" || true); W61C=$(grep -c '11,416' "$SP" || true); ENC=$(grep -ic 'enclos' "$SP" || true); REAUTH=$((C24A+C24B+C25+C27A+C27B+C27C+C27D+W61A+W61B+W61C)); [ "$REAUTH" -eq 0 ] || { echo "PLAN 01-44'S DELETIONS HAVE BEEN RE-AUTHORED: TOTAL $REAUTH ACROSS THE TEN LITERALS (C24A=$C24A C24B=$C24B C25=$C25 C27A=$C27A C27B=$C27B C27C=$C27C C27D=$C27D W61A=$W61A W61B=$W61B W61C=$W61C)"; exit 1; }; [ "$ENC" -eq 7 ] || { echo "CASE-INSENSITIVE POPULATION OF THE CONTRAPOSITION WORD IS $ENC, EXPECTED 7 — THIS TASK EITHER RE-AUTHORED ONE OR REMOVED ANOTHER"; exit 1; }; echo "NO-REAUTHORING-OK reauth=$REAUTH enclos=$ENC"; D=$(git diff "$RB"..HEAD -- "$SP"); DN=$(printf '%s\n' "$D" | grep -cE '^([+][^+]|-[^-])' || true); [ "$DN" -ge 30 ] || { echo "ROUND DIFF OVER THE SPEC IS $DN CHANGED LINES, EXPECTED AT LEAST 30 BY THIS POINT — EVERY DIFF-SCOPED ASSERTION BELOW WOULD PASS HAVING READ ALMOST NOTHING"; exit 1; }; NETADD=$(printf '%s\n' "$D" | grep -c '^+[^+]' || true); NETDEL=$(printf '%s\n' "$D" | grep -c '^-[^-]' || true); echo "ROUND-PROSE-DELTA added=$NETADD removed=$NETDEL net=$((NETADD-NETDEL))"; AST=$(printf '%s\n' "$D" | grep -cE '^[+-].*(from "typescript"|[^A-Za-z]ts\.[A-Za-z])' || true); [ "$AST" -eq 0 ] || { echo "COMPILER-API LINES IN ROUND DIFF: $AST"; exit 1; }; HAND=$(printf '%s\n' "$D" | grep '^+' | grep -vE '^\+[[:space:]]*(//|\*|/\*)' | grep -cE '(braceDepth|braceLevel|indentOf|indentLevel|enclosing[A-Z]|Enclosing|containmentOf|frameOf|depth\+\+|depth--)' || true); [ "$HAND" -eq 0 ] || { echo "HAND-ROLLED CONTAINMENT SPELLING IN ROUND DIFF: $HAND"; exit 1; }; NAR=$(printf '%s\n' "$D" | grep -cE '^[+-].*(DECLARATION =|FIXTURE_TITLE =|DOCBLOCK_OPEN =|isLineComment =|isRule =|CONSTRUCT_TOKEN_ELLIPSIS =|EXEMPTION_ANCHOR_SEP =|NO_PRECEDING_CONSTRUCT =|CLOSES_FROZEN_ARRAY =|lineNumber - 1|k < lineNumber)' || true); [ "$NAR" -eq 0 ] || { echo "NARROWING TOKENS IN ROUND DIFF: $NAR"; exit 1; }; XA='BEGIN{s=0} !s && $0 ~ "^const " n "[ =(]" {s=1; print; next} s && /^(\};|\/\*\*|const |export |type |interface |describe|it\()/ { if ($0 ~ /^\};/) print; exit } s {print}'; for PAIR in constructAnchorFor:$BODY_constructAnchorFor constructTokenOf:$BODY_constructTokenOf exemptionKeyFor:$BODY_exemptionKeyFor maskQuantifiers:$BODY_maskQuantifiers normalizeGateLine:$BODY_normalizeGateLine nameableRemainder:$BODY_nameableRemainder; do n=${PAIR%%:*}; EXP=${PAIR##*:}; [ -n "$EXP" ] || { echo "NO RECORDED BODY LENGTH FOR $n — THE BASELINE FILE IS INCOMPLETE AND THIS CHECK WOULD COMPARE AGAINST AN EMPTY STRING"; exit 1; }; git show "$RB":"$SP" | awk -v n="$n" "$XA" > "$TD/r11.base.$n"; awk -v n="$n" "$XA" "$SP" > "$TD/r11.work.$n"; BBL=$(grep -c . "$TD/r11.base.$n" || true); [ "$BBL" -eq "$EXP" ] || { echo "BASE RANGE FOR $n IS $BBL LINES AND THE BASELINE RECORDS $EXP — THE EXTRACTOR ITSELF COLLAPSED, AND IT COLLAPSES BOTH SIDES EQUALLY SO THE DIFF WOULD STAY 0"; exit 1; }; BL=$(grep -c . "$TD/r11.work.$n" || true); [ "$BL" -eq "$EXP" ] || { echo "BODY RANGE FOR $n IS $BL LINES, EXPECTED $EXP"; exit 1; }; BD=$(diff "$TD/r11.base.$n" "$TD/r11.work.$n" | grep -c . || true); [ "$BD" -eq 0 ] || { echo "BODY OF $n CHANGED: $BD DIFF LINES"; exit 1; }; echo "BODY-BYTE-IDENTICAL $n lines=$BL"; done; AS='BEGIN{s=0} !s && /^export function auditSource\(/ {s=1; print; next} s && /^\}$/ {print; exit} s {print}'; git show "$RB":"$SP" | awk "$AS" > "$TD/r11.base.auditSource"; awk "$AS" "$SP" > "$TD/r11.work.auditSource"; ABL=$(grep -c . "$TD/r11.base.auditSource" || true); [ "$ABL" -eq "$BODY_auditSource" ] || { echo "auditSource BASE RANGE IS $ABL, BASELINE RECORDS $BODY_auditSource"; exit 1; }; AL=$(grep -c . "$TD/r11.work.auditSource" || true); [ "$AL" -eq "$BODY_auditSource" ] || { echo "auditSource RANGE IS $AL, EXPECTED $BODY_auditSource"; exit 1; }; AD=$(diff "$TD/r11.base.auditSource" "$TD/r11.work.auditSource" | grep -c . || true); [ "$AD" -eq 0 ] || { echo "auditSource CHANGED: $AD DIFF LINES"; exit 1; }; IMPN=$(grep -c '^import ' "$SP" || true); [ "$IMPN" -eq "$BODY_imports" ] || { echo "IMPORT COUNT IS $IMPN, EXPECTED $BODY_imports"; exit 1; }; A=$(git diff --name-only "$RB"..HEAD -- packages/ scripts/ | grep -v '\.spec\.ts$' | grep -c . || true); [ "$A" -eq 0 ] || { echo "ROUND TOUCHED SHIPPED SOURCE: $A FILE(S)"; exit 1; }; BOX=$(git diff "$RB"..HEAD -- .planning/REQUIREMENTS.md | grep -c . || true); [ "$BOX" -eq 0 ] || { echo "REQUIREMENTS.md CHANGED THIS ROUND: $BOX"; exit 1; }; CBX=$(grep -c 'CORE11_BOX_EXPECTED = "- \[ \] \*\*CORE-11\*\*"' "$SP" || true); [ "$CBX" -eq 1 ] || { echo "CORE11_BOX_EXPECTED IS NOT THE UNCHECKED FORM: $CBX"; exit 1; }; N=$(git status --porcelain .planning/ | grep -c . || true); [ "$N" -eq 0 ] || { echo "PLANNING DIRTY: $N"; exit 1; }; P=$(grep -c '38 == 27 + 11' .planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md || true); [ "$P" -ge 1 ] || { echo "PROBE EQUALITY MISSING"; exit 1; }; git diff --exit-code -- "$SP" && echo TASK1-TREE-CLEAN</automated>
  </verify>

  <acceptance_criteria>
    - The precondition asserted and PASTED, including `01-44-SUMMARY.md` present, plan 01-44's commits in
      history, and ALL ELEVEN of its deletion greps re-run at zero BEFORE the first edit.
    - The pre-edit measurement pasted: the shadow table's top four entries as `[size, lo, hi, token]`,
      `SURFACE_LINES.length`, all three exclusion spans and the current pinned maximum — each reconciled
      against pass 11's figures with plan 01-44's own line delta named as the cause of any difference, and
      any movement of the MAXIMUM attributed line by line before proceeding.
    - The identity pin present and shown by diff to be an exact equality against a declared literal, with
      the numeric checks pasted: the token declared and asserted, exactly one assertion against it,
      exactly one surviving assertion against the size constant, and NO line-number-shaped value on the
      literal's line. Its failure message quoted verbatim and shown to name the expected token, the found
      token, and what a difference means.
    - THE PIN'S OWN LINE PROVED INNOCENT: `widest` and `widestSize` pasted immediately before and
      immediately after the literal lands, both unchanged; the uniqueness census re-run with the anchor's
      producer count pasted at exactly one; the whole-file declared-phrasing guard shown green. If the
      literal perturbed either measurement, the pin was RESHAPED and the reshaping is described — the
      expected value was NOT adjusted.
    - THE IDENTITY PIN WATCHED RED WITH THE SIZE PIN GREEN IN THE SAME RUN: pass 11's 293-line mutation
      planted as described, the failing case title and the FULL assertion message pasted, THE SIZE PIN'S
      TICK FROM THE SAME RUN PASTED, and the resulting shadow table showing two anchors at the maximum
      with the named one collapsed. Pass 11's `Tests 441 passed (441)` for the identical mutation cited
      from `01-VERIFICATION.md`. `git log --oneline -1` pasted BEFORE the mutation; the tree proved clean
      by diff after restore.
    - Every OTHER case that fired under this placement named, with its message, and an explicit statement
      that the placement was not adjusted to produce a cleaner result. Pass 11's own record of the
      exemption case firing at the reviewer's placement cited for contrast.
    - If the identity pin did not go red, or the size pin did not stay green, the task stopped, the
      diagnosis is a first-order finding, and nothing was adjusted to force either.
    - The corrected rationale quoted verbatim and shown to carry all three parts — the maximum's two
      directions, the identity pin's reach, and the shadows neither covers — with the executed collapse
      cited as a dated pass-11 measurement rather than a live fact. The block's net line delta pasted and
      shown NON-POSITIVE.
    - WR-64: the recogniser evaluation over every line of the anchor's raw span pasted with its accepted
      set, reconciled against pass 11's single accepted line below the imports, with any difference
      recorded as a finding and the written sentence following THIS session's measurement. The false
      causal clause shown absent by its numeric grep at zero, and the replacement quoted verbatim and
      shown to carry its reach inside the claiming clause.
    - WR-65: the self-undermining argument shown absent by its numeric grep at zero, and the
      pre-authorisation quoted verbatim and shown to be a statement of what a future author is PERMITTED
      to do — with a one-line statement of why a policy cannot be falsified by measuring this file and a
      measurement can. The existing reason for pinning only the maximum shown surviving.
    - The disclosure sentence quoted verbatim and shown to state, inside the claiming clause, that the
      identity pin watches one shadow and leaves every other non-maximal shadow unwatched in both
      directions.
    - THE NO-RE-AUTHORING CHECK PASTED: all ten of plan 01-44's deletion literals at zero and the
      case-insensitive population at seven, with the `NO-REAUTHORING-OK` line pasted in full.
    - The round prose delta pasted from the verify block's `ROUND-PROSE-DELTA` line, with a one-line
      statement of whether round 11's cumulative delta over the gate file is still negative and, if not,
      why the additions were unavoidable.
    - The compiler-API check, the hand-rolled-containment net and the narrowing-token net all shown
      returning zero over the ROUND-BASE DIFF with output pasted, AND a written per-sentence statement
      that NO CONTAINMENT WAS COMPUTED BY ANY MEANS, AST OR HAND-ROLLED. The nets match SPELLINGS, not
      shapes; this written statement covers the residue and the executor states that it is doing so.
    - All eight body-range byte-identity pairs pasted with their line counts, each at 0 diff lines and at
      an exact re-derived length on BOTH sides. `constructAnchorFor` and `constructTokenOf` are the two
      that WR-64's measurement reads; both shown unchanged.
    - `git status --porcelain .planning/` EMPTY numerically; `requirements mark-complete` absent from
      every command; `CORE11_BOX_EXPECTED` byte-unchanged; `REQUIREMENTS.md` at zero diff lines.
    - Executed and pasted: gate suite at or above arrival, `pnpm test` exit 0 at 31 files and at or above
      1381 tests, `typecheck` / `lint` / `knip` / `tsc --build` each exit 0 AS SEPARATE STATEMENTS,
      `check:bundle` at exactly one specifier `crypto`, 23 modules at zero violations.
    - A one-line no-overclaim verdict recorded for EVERY sentence added or changed, reach claimed beside
      reach executed. Exactly four sentences were written; if more, each extra is justified individually.
    - Committed SEPARATELY from Task 2, with `git log --oneline -1` pasted.
  </acceptance_criteria>

  <done>
    The anchor that owns the maximum shadow is pinned by identity as well as by size, that pin has been
    watched going RED against the exact 293-line mutation that was green at `882ff17` while the size pin
    stayed GREEN in the same run, the pin's own line is proved not to perturb what it measures, the
    boundary mechanism is stated from a measurement taken in this session with its reach inside the
    claiming clause, and the rationale pre-authorises its own routine bump instead of arguing against it.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-62 and WR-63 — exclusion one's endpoints pinned against the exported sentinels and watched RED against pass 11's decoy, the census guard corrected at its boundary value with both messages naming both shapes, and the round's closing baseline executed rather than carried</name>

  <files>packages/backend/src/outbound-prohibition.spec.ts</files>

  <reversibility rating="reversible">
    Two assertions and two message strings inside one test file, recoverable by `git revert`. No shipped
    source byte, no schema, no interface. The closing baseline writes nothing at all — it measures.
  </reversibility>

  <precondition>
    Task 1 is committed and `git log --oneline -1` shows its commit. `git diff --exit-code -- packages/`
    clean. The baseline file exists and every variable reads back. Task 1's identity pin present and
    green, and its pinned token pasted so this task can show it unmoved at the close. Plan 01-44's eleven
    deletion greps re-run and all still at zero. HALT on any red.
  </precondition>

  <read_first>
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md` — the `artifacts:`
      entries for WR-62 and WR-63 in full, the `missing:` entry beginning `WR-63:`, and the body section
      `### 5. WR-58 / WR-60` (~`:367-389`), which carries the planted key and the exact wrong message.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the `EXCLUSIONS` derivation at `:10059-10097`,
      with exclusion one's two sentinel prefix locators at `:10060-10061` and the other two at
      `:10062-10067`; `EXCLUDED_LINES` and `SURFACE_LINES` at `:10098-10112`; and the F-11 enumeration at
      `:10021-10032`, whose statement of what is left unproved is the sentence WR-62 corrects.
    - `packages/backend/src/outbound-prohibition.spec.ts` — `DERIVED_MARK` at `:6042`, `DERIVED_BEGIN` at
      `:6043` and `DERIVED_END` at `:6044`, and their LIVE SENTINEL LINES at `:983` and `:1564`. Confirm
      by measurement that each exported constant matches its live sentinel line as a FULL-LINE equality
      and that each matches exactly one line, BEFORE using either as a pin.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the prefix case's guard and message at
      `:10736-10741`, the census's `constructHalf` at `:10814-10821` with its `indexOf` comparison at
      `:10816` and its malformation message at `:10818`, the `inUse` construction at `:10822-10829`, and
      the WR-57 note at `:10811-10813` which records the second declaration as UNADJUDICATED and NOT
      closed. `EXEMPTION_ANCHOR_SEP` is a four-character string opening and closing with a space.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the CR-23 independence paragraph at
      `:10344-10352`. WR-62's pin is subject to it: the pin is an independent expression run AFTER the
      derivation, not a substitution into it.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-44-SUMMARY.md` and `01-45` Task 1's
      record — the arrival and current figures this task's closing baseline reconciles against.
  </read_first>

  <behavior>
    - Unmodified file, after this task: both new endpoint assertions are GREEN, exclusion one's `from` and
      `to` equal the lines the exported sentinels locate, and the census is GREEN over the shipped map.
    - Pass 11's sentinel decoy planted inside the block comment above the real sentinel: exclusion one's
      `from` slides and THE NEW ENDPOINT PIN IS RED, naming the realized value and the sentinel's own
      line. At `882ff17` only the size pin fired, by luck of geometry.
    - A hand-written key OPENING with the separator planted into the exemption map: the census throws the
      MALFORMATION error naming the empty-construct-half shape, not the ambiguity message. At `882ff17`
      it produced `Tests 3 failed | 438 passed (441)` with the ambiguity diagnosis.
    - A key carrying no separator at all: both sites still report, and both messages now name both shapes.
  </behavior>

  <action>
    RE-DERIVE EVERY LINE NUMBER BY IDENTIFIER BEFORE THE FIRST EDIT. Plan 01-44 and this plan's Task 1
    have both moved this file. Record every disagreement as a finding.

    WR-62, AND IT IS A PIN, NOT A REWRITE. First PROVE THE LOCATORS: measure how many lines of the file
    each exported sentinel constant equals as a FULL-LINE string, and assert exactly one for each before
    using it — the same non-vacuity treatment the registry's closer already gets, and the reason CR-23's
    pins are real. Then add, after the derivation, one assertion per endpoint comparing exclusion one's
    realized `from` and `to` against the lines those two constants locate. LEAVE THE DERIVATION'S OWN
    PREFIX MATCHERS EXACTLY AS THEY ARE: the two sides must remain independent expressions, and making
    both sides the same expression restores the tautology in the same commit that claims to remove it.
    Give each assertion a message naming the realized value, the sentinel's own line, the gap between
    them, and what a difference means for the scanned surface — that a slide of this endpoint moves the
    largest exclusion and silently removes pre-existing lines from what the gate scans.

    THEN CORRECT THE F-11 ENUMERATION, AND CORRECT IT DOWNWARD RATHER THAN UPWARD. It describes what is
    left unproved as two opening locators and there are FOUR prefix matchers in that derivation. Two of
    them are now pinned by this task. Write the corrected sentence to state what is PINNED and what is
    NOT, with the reach inside the clause, and keep it NET NON-POSITIVE in lines. DO NOT write a
    completeness claim about how many locators the derivation contains — pass 11's own count of a
    different population was incomplete and the enumeration of the four goes in the SUMMARY, dated, not
    into the file.

    WR-63, TWO SITES, AND BOTH GET BOTH SHAPES. Widen the census's index comparison so it rejects a
    separator found at the very start as well as one not found at all — the boundary value the round-9 fix
    text prescribed and the round-9 fix missed. Then widen BOTH failure messages, the census's and the
    prefix case's, so each names BOTH shapes: a key carrying no separator, and a key that OPENS with one
    and therefore has an EMPTY construct half. The prefix case's guard is already correct and is not
    touched; only its message is wrong. Leave WR-57 exactly where it is — the second declaration is
    UNADJUDICATED and this task does not close it, does not consolidate the declarations, and says so
    nowhere except by leaving the existing note intact.

    NOW WATCH BOTH GO RED. FIRST, WR-62: plant pass 11's bare sentinel decoy inside the block comment
    above the real sentinel line. Run the gate suite. THE NEW ENDPOINT PIN MUST BE RED; paste the failing
    title and the FULL message with both line numbers and the gap. Pass 11 records that at `882ff17` NO
    endpoint pin fired and only the size pin did, by the luck of exclusion one lying inside the widest
    shadow's raw span — cite that and state plainly that the new pin removes the dependence on that luck
    for THIS endpoint and for no other. Restore; prove the tree clean.
    SECOND, WR-63: plant the hand-written key that OPENS with the separator into the exemption map. Run
    the suite. THE CENSUS MUST NOW PRODUCE THE MALFORMATION DIAGNOSIS, not the ambiguity one. PASTE BOTH
    MESSAGES SIDE BY SIDE — the one `882ff17` produced, quoted from `01-VERIFICATION.md`, and the one
    produced now — because the fix is the CHANGE OF DIAGNOSIS and pasting only the new message proves
    nothing. Then plant a key carrying no separator at all and confirm both sites still report with the
    widened messages. Restore after each; prove the tree clean.

    IF ANY PLANTED VIOLATION DOES NOT GO RED, OR GOES RED WITH THE OLD DIAGNOSIS, STOP. Write the
    diagnosis up as a first-order finding and change nothing to force the result.

    THEN THE ROUND'S CLOSING BASELINE, EXECUTED AND NOT CARRIED. Re-measure and paste, each with its
    command: the whole suite's file and test counts, `tsc --build`, `typecheck`, `lint`, `knip`,
    `build:backend`, `check:bundle`'s specifier set, the gate suite's own count, the module count and the
    real-tree walk's violation count, `SURFACE_LINES.length`, `wc -l` of the gate file, and the shipped
    -source diff over the whole round. RECONCILE EVERY ONE against arrival — 31 files / 1381 tests, gate
    441, one specifier `crypto`, 23 modules / 0 violations, `wc -l` 11502 — AND ATTRIBUTE EVERY
    DIFFERENCE TO A NAMED CAUSE. A figure that moved without a cause is a finding, not a new baseline.

    RECONCILE THE WHOLE ROUND. State, in the SUMMARY: the cumulative prose delta over the gate file across
    both plans and whether it is negative as the round's thesis requires; every guard this round shipped
    and the mutation each was watched failing against; every reconciliation disagreement against pass 11's
    line numbers, including this planner's own correction that the contraposition word's case-insensitive
    population is ten and not the eight a case-sensitive grep returns; and the four unadjudicated findings
    left open.

    STATE THE SECURITY POSITION AND DO NOT OVERSTATE IT. `01-SECURITY.md` records six open blocking
    threats and all six are this round's findings. Name which are addressed by which task across both
    plans, and state that whether `threats_open` reaches zero is PASS 12's measurement and not this
    round's claim.

    RE-RUN PLAN 01-44's ELEVEN DELETION GREPS AND TASK 1's THREE at the close. All fourteen must hold.

    CORE-11's BOX IS NOT TOUCHED. `requirements mark-complete` is not run. `.planning/` stays clean.

    RE-READ EVERY SENTENCE THIS TASK WRITES AGAINST THE NO-OVERCLAIM PROHIBITION AND RECORD A VERDICT PER
    SENTENCE. The corrected F-11 enumeration is this task's likeliest place to author the twelfth
    instance, because it is a sentence about how many things are proved.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && SP=packages/backend/src/outbound-prohibition.spec.ts; TD="${TMPDIR:-/tmp}"; B="$TD/defminer-round11-baseline"; [ -f "$B" ] || { echo "MISSING ROUND-11 BASELINE FILE"; exit 1; }; . "$B"; RB=$(git rev-parse 882ff17) || { echo "ROUND BASE UNRESOLVABLE"; exit 1; }; [ -n "$RB" ] || { echo "BASE REV EMPTY"; exit 1; }; git merge-base --is-ancestor "$RB" HEAD || { echo "882ff17 IS NOT AN ANCESTOR OF HEAD"; exit 1; }; pnpm exec vitest run "$SP" > "$TD/r11.gate4.log" 2>&1; GRC=$?; cat "$TD/r11.gate4.log"; [ "$GRC" -eq 0 ] || { echo "GATE SUITE RED (rc=$GRC) — A COUNT PARSED OUT OF A RED RUN IS THE DEFECT"; exit 1; }; GT=$(grep -E '^[[:space:]]*Tests[[:space:]]' "$TD/r11.gate4.log" | tail -1 | sed -n 's/.*[^0-9]\([0-9][0-9]*\) passed.*/\1/p'); [ -n "$GT" ] || { echo "GATE PASSED-COUNT UNPARSEABLE"; exit 1; }; [ "$GT" -ge "$BASE_GATE_ARRIVAL" ] || { echo "GATE COUNT $GT BELOW ARRIVAL $BASE_GATE_ARRIVAL"; exit 1; }; echo "CLOSING-GATE-PASSED=$GT ARRIVAL=$BASE_GATE_ARRIVAL"; pnpm test > "$TD/r11.full4.log" 2>&1; FRC=$?; cat "$TD/r11.full4.log"; [ "$FRC" -eq 0 ] || { echo "WHOLE SUITE RED (rc=$FRC)"; exit 1; }; FT=$(grep -E '^[[:space:]]*Tests[[:space:]]' "$TD/r11.full4.log" | tail -1 | sed -n 's/.*[^0-9]\([0-9][0-9]*\) passed.*/\1/p'); [ -n "$FT" ] || { echo "SUITE PASSED-COUNT UNPARSEABLE"; exit 1; }; FF=$(grep -E '^[[:space:]]*Test Files[[:space:]]' "$TD/r11.full4.log" | tail -1 | sed -n 's/.*[^0-9]\([0-9][0-9]*\) passed.*/\1/p'); [ -n "$FF" ] || { echo "FILE PASSED-COUNT UNPARSEABLE"; exit 1; }; [ "$FT" -ge "$BASE_TESTS_ARRIVAL" ] || { echo "SUITE COUNT $FT BELOW ARRIVAL $BASE_TESTS_ARRIVAL"; exit 1; }; [ "$FF" -eq "$BASE_FILES" ] || { echo "FILE COUNT $FF != $BASE_FILES"; exit 1; }; echo "CLOSING-SUITE files=$FF tests=$FT"; pnpm exec tsc --build || { echo "tsc --build RED — THE ASSERTIONS BELOW ARE NOT RUN"; exit 1; }; pnpm typecheck || { echo "typecheck RED"; exit 1; }; pnpm lint || { echo "lint RED"; exit 1; }; pnpm knip || { echo "knip RED"; exit 1; }; pnpm build:backend || { echo "build:backend RED"; exit 1; }; pnpm check:bundle > "$TD/r11.bundle.log" 2>&1 || { cat "$TD/r11.bundle.log"; echo "check:bundle RED"; exit 1; }; cat "$TD/r11.bundle.log"; SPEC=$(grep -oE '[0-9]+ import specifier' "$TD/r11.bundle.log" | head -1 | grep -oE '^[0-9]+'); [ -n "$SPEC" ] || { echo "BUNDLE SPECIFIER COUNT UNPARSEABLE FROM $TD/r11.bundle.log"; exit 1; }; [ "$SPEC" -eq 1 ] || { echo "BUNDLE CARRIES $SPEC IMPORT SPECIFIERS, EXPECTED EXACTLY 1"; exit 1; }; grep -q 'crypto' "$TD/r11.bundle.log" || { echo "THE ONE SPECIFIER IS NOT crypto"; exit 1; }; M=$(find packages/*/src -name '*.ts' ! -name '*.spec.ts' | grep -c . || true); [ "$M" -eq "$BASE_MODULES" ] || { echo "MODULE COUNT $M != $BASE_MODULES"; exit 1; }; W62P=$(grep -cE 'l === DERIVED_BEGIN|l === DERIVED_END|=== DERIVED_BEGIN|=== DERIVED_END' "$SP" || true); [ "$W62P" -ge 2 ] || { echo "NO FULL-LINE EQUALITY AGAINST THE SENTINEL CONSTANTS: $W62P — WR-62 IS NOT PINNED"; exit 1; }; W62K=$(grep -c 'startsWith("BEGIN DERIVED RESIDUAL")' "$SP" || true); [ "$W62K" -eq 1 ] || { echo "EXCLUSION ONE'S OWN PREFIX LOCATOR WAS REWRITTEN RATHER THAN PINNED: $W62K — MAKING BOTH SIDES THE SAME EXPRESSION RESTORES CR-23'S TAUTOLOGY"; exit 1; }; W63G=$(grep -c 'if (at <= 0)' "$SP" || true); [ "$W63G" -eq 1 ] || { echo "THE CENSUS GUARD IS NOT AT THE PRESCRIBED BOUNDARY VALUE: $W63G SITE(S) MATCH THE WIDENED FORM"; exit 1; }; W63O=$(grep -cF 'if (at < 0)' "$SP" || true); [ "$W63O" -eq 2 ] || { echo "THE UNWIDENED FORM APPEARS $W63O TIME(S), EXPECTED EXACTLY 2 (ARRIVAL 3). THE TWO SURVIVORS AT THE TEXT AND CLAUSE SCAN LOOPS ARE indexOf BREAKS, NOT WR-63'S SITE; REMOVING THEM IS A DIFFERENT AND WRONG EDIT"; exit 1; }; W63B=$(grep -cF 'if (at < 0) break;' "$SP" || true); [ "$W63B" -eq 2 ] || { echo "A SCAN-LOOP BREAK WAS ALTERED: $W63B, EXPECTED 2"; exit 1; }; W57=$(grep -c 'UNADJUDICATED' "$SP" || true); [ "$W57" -ge 1 ] || { echo "THE WR-57 UNADJUDICATED NOTE WAS REMOVED: $W57 — THIS TASK DOES NOT CLOSE WR-57"; exit 1; }; C24A=$(grep -c 'Located by text TODAY' "$SP" || true); C24B=$(grep -c 'two identically-headed table headers sit at lines' "$SP" || true); C25=$(grep -cE 'grow to 573' "$SP" || true); C27A=$(grep -c 'has stopped resolving the enclosing construct' "$SP" || true); C27B=$(grep -c 'so the anchor is not resolving the enclosing construct' "$SP" || true); C27C=$(grep -c 'the anchor is not resolving the ENCLOSING construct' "$SP" || true); C27D=$(grep -c 'claims that a well-formed anchor identifies anything' "$SP" || true); W61A=$(grep -c '8,846' "$SP" || true); W61B=$(grep -c '11,417' "$SP" || true); W61C=$(grep -c '11,416' "$SP" || true); W65=$(grep -c 'is a pin nobody trusts' "$SP" || true); W64=$(grep -c 'no line at all is accepted by any' "$SP" || true); RO=$(grep -c 'REAL_OPENER' "$SP" || true); REAUTH=$((C24A+C24B+C25+C27A+C27B+C27C+C27D+W61A+W61B+W61C+W65+W64+RO)); [ "$REAUTH" -eq 0 ] || { echo "A ROUND-11 REMOVAL HAS COME BACK: TOTAL $REAUTH (C24A=$C24A C24B=$C24B C25=$C25 C27A=$C27A C27B=$C27B C27C=$C27C C27D=$C27D W61A=$W61A W61B=$W61B W61C=$W61C W65=$W65 W64=$W64 REAL_OPENER=$RO)"; exit 1; }; ENC=$(grep -ic 'enclos' "$SP" || true); [ "$ENC" -eq 7 ] || { echo "CASE-INSENSITIVE POPULATION OF THE CONTRAPOSITION WORD IS $ENC, EXPECTED 7"; exit 1; }; IDA=$(grep -c 'toBe(WIDEST_ANCHOR_TOKEN)' "$SP" || true); [ "$IDA" -eq 1 ] || { echo "TASK 1'S IDENTITY PIN IS GONE OR DUPLICATED: $IDA"; exit 1; }; SZP=$(grep -c 'toBe(WIDEST_ANCHOR_SHADOW)' "$SP" || true); [ "$SZP" -eq 1 ] || { echo "THE SIZE PIN IS GONE OR DUPLICATED: $SZP"; exit 1; }; echo "ROUND-CLOSE-INTEGRITY-OK reauth=$REAUTH enclos=$ENC identity=$IDA size=$SZP"; D=$(git diff "$RB"..HEAD -- "$SP"); DN=$(printf '%s\n' "$D" | grep -cE '^([+][^+]|-[^-])' || true); [ "$DN" -ge 40 ] || { echo "ROUND DIFF OVER THE SPEC IS $DN CHANGED LINES, EXPECTED AT LEAST 40 AT ROUND CLOSE"; exit 1; }; NETADD=$(printf '%s\n' "$D" | grep -c '^+[^+]' || true); NETDEL=$(printf '%s\n' "$D" | grep -c '^-[^-]' || true); echo "ROUND-11-PROSE-DELTA added=$NETADD removed=$NETDEL net=$((NETADD-NETDEL))"; AST=$(printf '%s\n' "$D" | grep -cE '^[+-].*(from "typescript"|[^A-Za-z]ts\.[A-Za-z])' || true); [ "$AST" -eq 0 ] || { echo "COMPILER-API LINES IN ROUND DIFF: $AST"; exit 1; }; HAND=$(printf '%s\n' "$D" | grep '^+' | grep -vE '^\+[[:space:]]*(//|\*|/\*)' | grep -cE '(braceDepth|braceLevel|indentOf|indentLevel|enclosing[A-Z]|Enclosing|containmentOf|frameOf|depth\+\+|depth--)' || true); [ "$HAND" -eq 0 ] || { echo "HAND-ROLLED CONTAINMENT SPELLING IN ROUND DIFF: $HAND"; exit 1; }; NAR=$(printf '%s\n' "$D" | grep -cE '^[+-].*(DECLARATION =|FIXTURE_TITLE =|DOCBLOCK_OPEN =|isLineComment =|isRule =|CONSTRUCT_TOKEN_ELLIPSIS =|NO_PRECEDING_CONSTRUCT =|CLOSES_FROZEN_ARRAY =|lineNumber - 1|k < lineNumber)' || true); [ "$NAR" -eq 0 ] || { echo "NARROWING TOKENS IN ROUND DIFF: $NAR"; exit 1; }; SEPD=$(printf '%s\n' "$D" | grep -c '^[+-].*EXEMPTION_ANCHOR_SEP = ' || true); [ "$SEPD" -eq 0 ] || { echo "THE KEY SEPARATOR'S DEFINITION WAS CHANGED: $SEPD — WR-63 WIDENS A GUARD AND TWO MESSAGES, NOT THE KEY FORMAT"; exit 1; }; XA='BEGIN{s=0} !s && $0 ~ "^const " n "[ =(]" {s=1; print; next} s && /^(\};|\/\*\*|const |export |type |interface |describe|it\()/ { if ($0 ~ /^\};/) print; exit } s {print}'; for PAIR in constructAnchorFor:$BODY_constructAnchorFor constructTokenOf:$BODY_constructTokenOf exemptionKeyFor:$BODY_exemptionKeyFor maskQuantifiers:$BODY_maskQuantifiers normalizeGateLine:$BODY_normalizeGateLine nameableRemainder:$BODY_nameableRemainder; do n=${PAIR%%:*}; EXP=${PAIR##*:}; [ -n "$EXP" ] || { echo "NO RECORDED BODY LENGTH FOR $n — THE BASELINE FILE IS INCOMPLETE"; exit 1; }; git show "$RB":"$SP" | awk -v n="$n" "$XA" > "$TD/r11.base.$n"; awk -v n="$n" "$XA" "$SP" > "$TD/r11.work.$n"; BBL=$(grep -c . "$TD/r11.base.$n" || true); [ "$BBL" -eq "$EXP" ] || { echo "BASE RANGE FOR $n IS $BBL LINES AND THE BASELINE RECORDS $EXP — THE EXTRACTOR COLLAPSED ON BOTH SIDES"; exit 1; }; BL=$(grep -c . "$TD/r11.work.$n" || true); [ "$BL" -eq "$EXP" ] || { echo "BODY RANGE FOR $n IS $BL LINES, EXPECTED $EXP"; exit 1; }; BD=$(diff "$TD/r11.base.$n" "$TD/r11.work.$n" | grep -c . || true); [ "$BD" -eq 0 ] || { echo "BODY OF $n CHANGED OVER THE WHOLE ROUND: $BD DIFF LINES"; exit 1; }; echo "ROUND-BODY-BYTE-IDENTICAL $n lines=$BL"; done; AS='BEGIN{s=0} !s && /^export function auditSource\(/ {s=1; print; next} s && /^\}$/ {print; exit} s {print}'; git show "$RB":"$SP" | awk "$AS" > "$TD/r11.base.auditSource"; awk "$AS" "$SP" > "$TD/r11.work.auditSource"; ABL=$(grep -c . "$TD/r11.base.auditSource" || true); [ "$ABL" -eq "$BODY_auditSource" ] || { echo "auditSource BASE RANGE IS $ABL, BASELINE RECORDS $BODY_auditSource"; exit 1; }; AL=$(grep -c . "$TD/r11.work.auditSource" || true); [ "$AL" -eq "$BODY_auditSource" ] || { echo "auditSource RANGE IS $AL, EXPECTED $BODY_auditSource"; exit 1; }; AD=$(diff "$TD/r11.base.auditSource" "$TD/r11.work.auditSource" | grep -c . || true); [ "$AD" -eq 0 ] || { echo "auditSource CHANGED OVER THE WHOLE ROUND: $AD DIFF LINES"; exit 1; }; IMPN=$(grep -c '^import ' "$SP" || true); [ "$IMPN" -eq "$BODY_imports" ] || { echo "IMPORT COUNT IS $IMPN, EXPECTED $BODY_imports"; exit 1; }; git show "$RB":"$SP" | grep '^import ' > "$TD/r11.base.imports"; grep '^import ' "$SP" > "$TD/r11.work.imports"; IMPD=$(diff "$TD/r11.base.imports" "$TD/r11.work.imports" | grep -c . || true); [ "$IMPD" -eq 0 ] || { echo "IMPORT SET CHANGED OVER THE WHOLE ROUND: $IMPD DIFF LINES"; exit 1; }; A=$(git diff --name-only "$RB"..HEAD -- packages/ scripts/ | grep -v '\.spec\.ts$' | grep -c . || true); [ "$A" -eq 0 ] || { echo "ROUND TOUCHED SHIPPED SOURCE: $A FILE(S)"; exit 1; }; F=$(git diff --name-only "$RB"..HEAD -- packages/ scripts/ | grep -c . || true); [ "$F" -eq 1 ] || { echo "ROUND TOUCHED $F FILES UNDER packages/scripts, EXPECTED 1"; exit 1; }; BOX=$(git diff "$RB"..HEAD -- .planning/REQUIREMENTS.md | grep -c . || true); [ "$BOX" -eq 0 ] || { echo "REQUIREMENTS.md CHANGED THIS ROUND: $BOX"; exit 1; }; CBX=$(grep -c 'CORE11_BOX_EXPECTED = "- \[ \] \*\*CORE-11\*\*"' "$SP" || true); [ "$CBX" -eq 1 ] || { echo "CORE11_BOX_EXPECTED IS NOT THE UNCHECKED FORM: $CBX"; exit 1; }; LB=$(grep -c '^- \[ \] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md || true); [ "$LB" -eq 1 ] || { echo "THE LEDGER ROW IS NOT THE UNCHECKED FORM: $LB"; exit 1; }; N=$(git status --porcelain .planning/ | grep -c . || true); [ "$N" -eq 0 ] || { echo "PLANNING DIRTY: $N"; exit 1; }; P=$(grep -c '38 == 27 + 11' .planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md || true); [ "$P" -ge 1 ] || { echo "PROBE EQUALITY MISSING"; exit 1; }; WC=$(grep -c '' "$SP"); echo "ROUND-CLOSE wc=$WC arrival=11502 modules=$M bundle=$SPEC"; git diff --exit-code -- "$SP" && echo ROUND-11-TREE-CLEAN</automated>
  </verify>

  <acceptance_criteria>
    - The precondition asserted and PASTED, including Task 1's commit, its identity pin present and green
      with its pinned token quoted, and plan 01-44's eleven deletion greps re-run at zero.
    - WR-62: each exported sentinel constant shown by measurement to equal EXACTLY ONE line of the file as
      a full-line string, with the counts pasted BEFORE either is used as a pin. Both endpoint assertions
      present and shown by diff to compare exclusion one's realized `from` and `to` against those located
      lines, with both messages quoted verbatim and shown to name the realized value, the sentinel's line,
      the gap and what a difference means for the scanned surface.
    - WR-62 INDEPENDENCE PROVED: the numeric check showing exclusion one's own prefix locator UNCHANGED
      pasted, and the two forms shown side by side as a prefix matcher and a full-line equality, with the
      in-file independence paragraph quoted.
    - WR-62 WATCHED RED: pass 11's sentinel decoy planted inside the block comment above the real
      sentinel, the new endpoint pin observed RED, the failing title and FULL message pasted with both
      line numbers and the gap; pass 11's record that at `882ff17` NO endpoint pin fired and only the size
      pin did, by geometry, cited; an explicit statement that the new pin removes that dependence for THIS
      endpoint and for no other. Restored and the tree proved clean.
    - WR-62 ENUMERATION: the F-11 sentence quoted before and after, the after form shown to state what is
      pinned and what is not with its reach inside the clause, and the block's net line delta shown
      NON-POSITIVE. The enumeration of all four prefix matchers recorded in the SUMMARY with an explicit
      statement that no completeness claim was written into the file and why.
    - WR-63: the widened guard shown by the numeric checks — the boundary-value form at exactly one site
      and the old form at zero — and BOTH messages quoted verbatim and shown to name BOTH shapes.
    - WR-63 WATCHED RED WITH THE DIAGNOSIS CHANGED: the key opening with the separator planted, the census
      observed producing the malformation diagnosis, and BOTH MESSAGES PASTED SIDE BY SIDE — `882ff17`'s
      ambiguity text quoted from `01-VERIFICATION.md` and this session's malformation text — because the
      fix is the change of diagnosis. Then the no-separator key planted and both sites shown reporting
      with the widened messages. Restored after each; tree clean.
    - WR-57 left open: the UNADJUDICATED note shown surviving by its numeric check, with an explicit
      statement that this task did not consolidate the two declarations and does not close WR-57.
    - If any planted violation did not go red, or went red with the old diagnosis, the task stopped and
      the diagnosis is a first-order finding.
    - THE CLOSING BASELINE EXECUTED, EVERY FIGURE PASTED WITH ITS COMMAND, EVERY DIFFERENCE FROM ARRIVAL
      ATTRIBUTED TO A NAMED CAUSE: whole suite files and tests, `tsc --build`, `typecheck`, `lint`,
      `knip`, `build:backend`, the bundle's specifier COUNT asserted at exactly one and its name asserted
      to be `crypto`, the gate suite count, the module count and the real-tree walk's violation count,
      `SURFACE_LINES.length`, `wc -l`, and the shipped-source diff over the whole round. A figure that
      moved without a cause is written up as a finding, not adopted as a new baseline.
    - THE ROUND-CLOSE INTEGRITY CHECK PASTED IN FULL: all thirteen removal literals at zero, the
      case-insensitive population at seven, the identity pin at one and the size pin at one.
    - `ROUND-11-PROSE-DELTA` pasted, with a written statement of whether round 11's cumulative delta over
      the gate file is negative as the round's thesis requires and, if it is not, exactly which additions
      account for it and why each was unavoidable.
    - The whole round reconciled in the SUMMARY: every guard shipped and the mutation each was watched
      failing against; every reconciliation disagreement against pass 11's line numbers INCLUDING this
      planner's correction that the contraposition word's case-insensitive population is ten where a
      case-sensitive grep returns eight; and IN-45, IN-46, IN-48, IN-49 and WR-57 listed as still open and
      NOT closed.
    - The security position stated: `01-SECURITY.md`'s six open blocking threats mapped to the tasks
      across both plans that address them, with the arithmetic written out — CLOSING THIS ROUND'S FULL
      ELEVEN-ITEM WORK LIST IS WHAT TAKES `threats_open` FROM 6 TO 0, `T-01-289`/`T-01-286`/`T-01-263` by
      plan 01-44's deletions and `T-01-280`/`T-01-264`/`T-01-239` by this plan's identity pin — AND an
      explicit statement that whether it HAS reached zero is PASS 12's measurement and not this round's
      claim. The two must appear in the same paragraph so neither can be quoted without the other.
    - All eight body-range byte-identity pairs at 0 diff lines with exact re-derived lengths on both
      sides, the import set unchanged, and the whole round shown to have touched exactly one file, none of
      it shipped.
    - `git status --porcelain .planning/` EMPTY numerically; `requirements mark-complete` absent from
      every command run in either plan; `CORE11_BOX_EXPECTED` byte-unchanged; the ledger row at
      `.planning/REQUIREMENTS.md` shown still the unchecked form by its own numeric check;
      `REQUIREMENTS.md` at zero diff lines over the whole round.
    - `01-PROBE.md`'s `38 == 27 + 11` present and unchanged, with a statement that no row moved.
    - A one-line no-overclaim verdict recorded for EVERY sentence added or changed.
  </acceptance_criteria>

  <done>
    Exclusion one's two endpoints are pinned against the exported sentinel constants by an independent
    full-line equality watched turning RED against pass 11's own decoy, the census diagnoses a
    hand-written key against a header that is not in the file as MALFORMATION at both boundary values with
    both messages naming both shapes, and the round's closing baseline has been executed with every figure
    reconciled against arrival and every difference attributed.
  </done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Produced by | What it carries |
| --- | --- | --- |
| `packages/backend/src/outbound-prohibition.spec.ts` | Task 1 | An identity pin under the 574-line shadow the file names in six places, proved not to perturb its own measurement and watched RED while the size pin stayed GREEN in one run; the boundary mechanism stated from a fresh line-by-line recogniser evaluation; and a pin rationale that pre-authorises its own routine bump instead of arguing against it |
| `packages/backend/src/outbound-prohibition.spec.ts` | Task 2 | Two endpoint pins under the largest exclusion, against the sentinel constants already exported at `:6043-6044`, watched RED against the decoy that at `882ff17` was caught only by the luck of geometry; and a census that diagnoses malformation as malformation at both boundary values from both call sites |
| `.planning/phases/01-skeleton-persistence-compatibility/01-45-SUMMARY.md` | Tasks 1-2 | The round's closing baseline with every figure reconciled against arrival and every difference attributed; the dated enumeration of the four prefix matchers, kept out of the file; round 11's cumulative prose delta; and the five findings left open and explicitly not closed |

## Multi-source coverage audit

| Source | Item | Covered by |
| --- | --- | --- |
| GOAL | ROADMAP phase goal — installs, observes without stalling, durably remembers | Unchanged. No shipped source byte is touched; the must-NOT is re-executed at every task boundary and again at the round's close |
| REQ | CORE-11 | Tasks 1-2. The box stays `[ ]`; `requirements mark-complete` stays unrun; the ledger row is asserted unchecked by its own numeric check |
| VERIFICATION | CR-26 parts (1) and (2) | Task 1 |
| VERIFICATION | WR-64, WR-65 | Task 1 |
| VERIFICATION | WR-62, WR-63 | Task 2 |
| VERIFICATION | CR-28, CR-24, CR-25, CR-27, WR-61 | Plan 01-44, and re-asserted absent at this plan's close by the no-re-authoring check |
| VERIFICATION | IN-45, IN-46, IN-48, IN-49, WR-57 | NOT COVERED BY DESIGN — pass 11 records the four INs as reviewer-reported and not re-executed; WR-57 has been UNADJUDICATED since round 9 and the file says so at `:10811-10813`. All five are listed as open in the SUMMARY and none is presented as closed |
| SECURITY | `T-01-280`, `T-01-264`, `T-01-239` | Task 1 — the mechanism half: the identity pin is what puts something under the shadow the file names |
| SECURITY | `T-01-289`, `T-01-286`, `T-01-263` | Plan 01-44 — the disclosure half. Whether `threats_open` reaches zero is pass 12's measurement |
| PROBE | `38 == 27 + 11` | Asserted unchanged at both task boundaries |
| RESEARCH | No new item — this round adds no dependency, no API and no external surface | n/a |

No CONTEXT.md, SPEC.md, AI-SPEC.md or UI-SPEC.md exists for this phase and none ever has; their absence
is not a gap. `COVERAGE.md` exists and declares no external API surface for this round's scope — no row is
fabricated. No ORM files exist, so the schema push is skipped. The assumption detector fires with four
`pluralization` signals, all of them historical ROADMAP prose about waves 36, 40 and 42; none introduces
an unstated cardinality assumption inside this round's scope.

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| editor -> gate spec | The only boundary this plan crosses. Every change lands in a test-only file; no shipped module, no bundle input and no persisted row is touched |
| gate spec -> shipped artifact | The gate's scan target must stay byte-identical to the installable artifact. An edit that narrows what the gate scans, or that moves an exclusion endpoint, is the one way this plan could weaken the outbound prohibition |
| pin literal -> scanned surface | New this plan: the identity pin's literal is written INTO the surface the pin measures. A pin that changes its own measurement is a pin that reports about itself |

## STRIDE Threat Register — ASVS level 1, blocking on `high`

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-01-280 | Repudiation | a pin asserting the maximum, not the identity | high | mitigate | Task 1 adds an exact equality on the masked token in the same case, watched RED against the 293-line mutation while the size pin stays GREEN in the same run |
| T-01-264 | Repudiation | the `:417` anchor's 574-line shadow, published in six places and pinned by nothing | high | mitigate | Task 1's identity pin is what those six sites now stand on; the five prose sites and the shipped failure message are read and left unchanged so the pin supports the existing text rather than replacing it |
| T-01-239 | Repudiation | a pin nobody watched failing | high | mitigate | Task 1 requires the RED demonstration to show BOTH the identity pin red and the size pin green in one run; a mutation reddening both would prove nothing the old pin did not already catch |
| T-01-295 | Tampering | the identity pin's own literal perturbing the surface it measures | high | mitigate | `widest` and `widestSize` measured immediately before and after the literal lands, the uniqueness census re-run with the producer count asserted at one, and the whole-file declared-phrasing guard shown green; on any perturbation the pin is RESHAPED and the expected value is not adjusted |
| T-01-296 | Tampering | exclusion one's endpoints sliding and silently removing lines from the scanned surface | high | mitigate | Task 2 pins both endpoints against the exported sentinels by an independent full-line equality, each locator proved to match exactly one line before use, and watches pass 11's decoy turn the pin red |
| T-01-297 | Repudiation | a guard reporting the right red for the wrong reason | high | mitigate | Task 2 widens the census's boundary comparison and both messages, and requires BOTH diagnoses pasted side by side — the old ambiguity text and the new malformation text — because the fix is the change of diagnosis |
| T-01-298 | Repudiation | round 11 re-authoring what round 11 deleted | high | mitigate | Both tasks re-run plan 01-44's deletion greps; Task 2's round-close integrity check asserts all thirteen removal literals at zero and the case-insensitive population at seven |
| T-01-299 | Repudiation | this round authoring instance twelve of the signature defect | high | mitigate | Task 1 writes exactly four sentences and Task 2 writes two; each carries its reach inside the claiming clause, WR-65's is an authorisation rather than a measurement, per-sentence no-overclaim verdicts are recorded, and the `open`-status DISCLOSED-NOT-CLOSED prohibition governs both tasks |
| T-01-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing. No package-manager command appears in any task, so the legitimacy gate has no input; `RESEARCH.md`'s audit table is unchanged and unconsulted |
</threat_model>

<verification>
Executed at every task boundary and again at the round's close, never carried:

- `pnpm exec vitest run packages/backend/src/outbound-prohibition.spec.ts` — exit 0, passed count at or
  above 441. The PASSED figure is parsed, never the parenthesised total, and the return code is asserted
  before any parse.
- `pnpm test` — exit 0, 31 files, passed count at or above 1381.
- `pnpm exec tsc --build`, `pnpm typecheck`, `pnpm lint`, `pnpm knip`, `pnpm build:backend`,
  `pnpm check:bundle` — each a SEPARATE statement with its own failure branch, because a short-circuited
  chain leaves the later variables empty and voids every diff-scoped assertion below it.
- `pnpm check:bundle` — the specifier COUNT parsed and asserted at exactly one, and the name asserted to
  be `crypto`. Asserting only the name would pass on a bundle carrying `crypto` plus anything else.
- 23 shipped non-spec modules across both SOURCE_ROOTS walked at zero violations.
- Round-base structural nets over the `882ff17..HEAD` diff: compiler-API at zero, hand-rolled containment
  at zero, recogniser/scan-bound/key-format tokens at zero, and the key separator's own definition at zero
  changed lines.
- Eight body-range byte-identity pairs, each asserted at an exact re-derived length on BOTH sides and at
  zero diff lines; the import set diffed and asserted unchanged over the whole round.
- The round-close integrity check: thirteen removal literals at zero, the case-insensitive contraposition
  population at seven, the identity pin at one site and the size pin at one site.
- `git status --porcelain .planning/` at zero entries; `.planning/REQUIREMENTS.md` at zero diff lines; the
  ledger row asserted unchecked by its own grep; `CORE11_BOX_EXPECTED` byte-unchanged; `requirements
  mark-complete` never run.
- `01-PROBE.md`'s `38 == 27 + 11` present and unchanged.
</verification>

<success_criteria>
- The identity pin exists as an exact equality against a masked-token literal carrying no line number,
  alongside — not instead of — the size pin.
- That pin has been watched RED against pass 11's 293-line mutation WITH THE SIZE PIN GREEN IN THE SAME
  RUN, and pass 11's `Tests 441 passed (441)` for the identical mutation is cited.
- The pin's own literal is proved not to change `widest`, `widestSize` or the anchor's producer count.
- Exclusion one's two endpoints are pinned against the exported sentinels by full-line equalities proved
  unique before use, the derivation's own prefix matchers are unchanged, and pass 11's decoy has been
  watched turning the new pin RED.
- The census guard rejects the boundary value, both messages name both shapes, and the change of
  diagnosis is proved by pasting the old and new messages side by side.
- The boundary mechanism is stated from a recogniser evaluation run in this session, and the pin
  rationale pre-authorises its bump instead of arguing against it.
- All thirteen round-11 removal literals are still at zero and the case-insensitive contraposition
  population is still seven.
- The closing baseline is executed with every figure reconciled against arrival and every difference
  attributed to a named cause.
- No shipped source byte changed; round 11 touched exactly one file.
- CORE-11's box is untouched at `[ ]` in both the ledger and the pin, and `requirements mark-complete` was
  never run in either plan.
- The SUMMARY records that closing round 11's eleven-item work list is what takes `01-SECURITY.md`'s
  `threats_open` from 6 to 0 — three threats by plan 01-44's deletions and three by this plan's identity
  pin — and, in the same paragraph, that whether it has is pass 12's measurement and not this round's
  claim.
</success_criteria>

<output>
Create `.planning/phases/01-skeleton-persistence-compatibility/01-45-SUMMARY.md` when done.

It MUST carry, in addition to the standard sections: the round's closing baseline with every figure
reconciled against arrival and every difference attributed to a named cause; the dated enumeration of the
four prefix matchers in the `EXCLUSIONS` derivation, deliberately NOT written into the gate file; round
11's cumulative prose delta over the gate file with a statement of whether it is negative as the round's
thesis requires; the full text of every watched RED, including both census diagnoses side by side; every
reconciliation disagreement against pass 11's line numbers, including the case-insensitive population
correction; IN-45, IN-46, IN-48, IN-49 and WR-57 listed as open and not closed; and the security position
mapping `01-SECURITY.md`'s six open blocking threats to the tasks that address them — without declaring
`threats_open: 0`, which is pass 12's measurement to make.
</output>
