---
phase: 01-skeleton-persistence-compatibility
plan: 33
type: execute
wave: 33
gap_closure: true
depends_on: [01-32]
files_modified:
  - packages/backend/src/outbound-prohibition.spec.ts
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/WINDOWS.md
# THE SURFACE IS DELETED, NOT INSTRUMENTED. THAT IS THE WHOLE PLAN AND IT IS COUNTER-INTUITIVE.
#
# Round 6 built a mechanism that is real — the verifier mutation-proved it at ROW granularity and
# injection-proved the coverage guard — and every guard it built reads `RESOLVER_REGISTRY[].clause`
# and nothing else. The 3,464 hand-written lines a reader reaches FIRST carry the declared phrasings
# and raise ZERO obligations. CR-14 lives there (the header says an operator around a global receiver
# is silent in every spelling, names six exemplars, and all six report), and so does WR-38 (the header
# says CORE-11 is marked complete; the ledger row and this file's own pin both say it is not).
#
# THE FIX IS NOT TO WIDEN THE SCAN. The verifier was explicit and measured the reason: widening the
# guards to read the header converts 20 stale sentences into 20 NEW OBLIGATIONS for the next author to
# keep true, on hand-written prose that will drift again. This file ALREADY applies a
# "pointer, not a bound" rule to `.planning/STATE.md` and `.planning/WINDOWS.md`, at `:931-941`, and
# wave 28's own note at `:943-955` says of itself that it "restates NO bound of its own". This plan
# applies that same rule to the gate's own header. A surface that carries no bound cannot carry a
# stale one. That removes the CLASS instead of guarding it.
#
# WHAT THE GUARD IS FOR, THEN. Deletion is a one-time act; the guard is what keeps it deleted. It is
# cheap, its obligation set is bounded, and it is the thing that makes the operator's criterion 3
# ("THE SOLE BOUND") stay true after this wave. It is NOT what makes the header correct — the deletion
# is. State that ordering plainly.
#
# TWO MEASUREMENTS TAKEN DURING PLANNING THAT CHANGE THE DESIGN, both against HEAD `e193dfc`, both
# reproducible by the commands in Task 1, and BOTH of them are the round's own defect found in the
# proposed fix before it shipped:
#
#   (1) THE REVIEWER'S SUGGESTED GUARD IS LINE-BASED AND WOULD BE BLIND TO FOUR OF THE OCCURRENCES IT
#       CLAIMS TO CATCH. Scanned line by line, the hand-written region carries the declared phrasings
#       20 times — which is exactly the verifier's number, independently reproduced. Scanned with the
#       comment prefixes stripped and the lines JOINED, so a phrasing wrapped across two comment lines
#       is still seen, it carries them 24 times. A guard that reports 20 while 24 exist is a claim
#       whose stated reach exceeds its executed reach, which is the defect this entire round is about,
#       installed AS THE FIX. The guard this plan builds is wrap-tolerant and pins BOTH numbers.
#
#   (2) THE HEADER IS NOT THE ONLY UNSCANNED SURFACE, AND THE LARGER ONE WAS NEVER MEASURED BY ANYBODY.
#       The 3,615 lines BELOW `RESOLVER_REGISTRY` — the vocabulary, `deriveResidual`, and the test
#       bodies — carry the declared phrasings 26 times line-based / 27 wrap-tolerant, MORE than the
#       header does. Nine of those are the `UNBOUNDED_QUANTIFIERS` declaration itself at `:5665-5673`,
#       which is the LIST and not an assertion. Scanning only "the header" would ship a guard whose
#       name promises the file and whose reach is a third of it — and would leave round 8 a
#       pre-written finding. This plan's guard scans the WHOLE FILE minus three principled, named,
#       non-vacuous exclusions.
#
# On `packages/backend/src/outbound-prohibition.spec.ts`: shared with plans 01-23 through 01-32 and
# with waves 34 and 35. Serialised for that reason, and because a guard written against a header that
# wave 34 is simultaneously editing would be born stale — which is this plan's own subject matter.
#
# On `.planning/REQUIREMENTS.md`: the machine-owned span between the sentinels is regenerated in place
# only if a registry row changes. This plan changes NO rule and closes NO shape. It does NOT touch
# CORE-11's checkbox; wave 35 owns it.
#
# On `.planning/STATE.md` and `.planning/WINDOWS.md`: POINTERS, restating no bound, per the rule wave
# 27 set. Both are append-only dated histories.
#
# TRANSIENT MUTATION TARGETS — reverted inside the task that plants them, restore cross-checked by
# `git diff --exit-code` in that task's own verify block, and COMMITTED BEFORE MUTATING because wave 29
# destroyed its own Task 1 by mutating uncommitted work:
#   packages/backend/src/outbound-prohibition.spec.ts   (one declared phrasing re-planted into the
#                                                        header as a single line; one re-planted
#                                                        WRAPPED across two comment lines; one
#                                                        exclusion emptied; one exemption entry
#                                                        removed while its sentence stands)

autonomous: true
requirements: [CORE-11]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low
# `estimate-calibration` reports `{"factor": 1, "applied": false, "sample_count": 0, "min_samples": 3}`,
# so `tokens` equals `raw_tokens` and the confidence is DERIVED from the sample count, not self-rated.
# Weigh the triage set over the token figure: 50 measured occurrences across three regions, of which 9
# are excluded by construction, leaving 41 to classify as DELETE or as a named exemption.

must_haves:
  truths:
    - "THE GATE'S HAND-WRITTEN HEADER STATES NO BOUND OF ITS OWN. The paragraph at `:756-772` asserting that an operator wrapping a global receiver is silent, its six exemplars, its claim to be pinned by a fixture titled as a measured silence, and the `:774-781` entry listing that shape as open and unowned, are DELETED — not corrected, not softened, not re-dated. In their place stands a pointer to the generated span between the sentinels, which is the residual OF RECORD, exactly as `:931-941` and `:943-955` already do for `.planning/STATE.md` and `.planning/WINDOWS.md`. CR-14 does not survive as a corrected sentence, because a corrected sentence is a bound and a bound is what drifts."
    - "CR-14's FALSIFIED UNIVERSAL IS RECORDED AS HISTORY IN ONE PLACE THAT IS NOT A BOUND — this plan's SUMMARY — with all six exemplars and their executed answers, so the seven waves that believed it are not erased. Nothing in the gate file restates it."
    - "WR-38 DIES WITH IT. The paragraph at `:908-916` asserting CORE-11 is marked complete in `.planning/REQUIREMENTS.md` is DELETED. After this wave the box's state is stated in exactly two places — the ledger row itself and `CORE11_BOX_EXPECTED` at `:9020` — and the gate file's header points at both rather than asserting either. The contradiction across 8,100 lines cannot recur because only one side of it still exists."
    - "A GUARD KEEPS IT DELETED, AND ITS REACH IS THE WHOLE FILE RATHER THAN THE HEADER. Every declared phrasing in `UNBOUNDED_QUANTIFIERS` must be absent from the gate file's own bytes outside three named, non-vacuous exclusions: the machine-owned span between `DERIVED_BEGIN` and `DERIVED_END`; the `UNBOUNDED_QUANTIFIERS` declaration itself, which is the list and not an assertion; and `RESOLVER_REGISTRY`'s own range, whose clauses are already obligated by the existing scan plus `QUANTIFIED_CLAUSES`. Every surviving occurrence outside those carries a NAMED, REASONED entry in a `HEADER_QUANTIFIER_EXEMPTIONS` map written in `RESOLVER_EXEMPTIONS`' shape."
    - "THE GUARD IS WRAP-TOLERANT AND BOTH COUNTS ARE PINNED BY EQUALITY, BECAUSE THE LINE-BASED VERSION WOULD HAVE SHIPPED THIS ROUND'S OWN DEFECT. Measured at HEAD `e193dfc` before any edit: the hand-written region scores 20 line-based and 24 with comment prefixes stripped and lines joined. The guard scans the joined form. Its failing path is executed TWICE — once with a phrasing re-planted on ONE line, once with a phrasing re-planted WRAPPED ACROSS TWO comment lines — and the wrapped mutation is the one that proves the wrap tolerance is real rather than claimed."
    - "THE GUARD'S OWN LIMITS ARE STATED WHEREVER IT IS CLAIMED, AND THEY ARE NOT SMALL. It is a PHRASE LIST: its reach is the declared phrasings and no further, and a universal spelled in words not on the list passes it — the same bound `UNBOUNDED_QUANTIFIERS`' own docblock already states, now stated of this guard too. It is NORMALIZATION-DEPENDENT: the count is 20 under one whitespace convention and 24 under another, and the convention it uses is named in the code rather than left to a reader. It reaches BYTES, not MEANING: a sentence that asserts a universal without using a declared phrasing is invisible to it. No sentence in this file may claim this guard makes the header correct — the DELETION does that, and the guard only keeps it deleted."
    - "THE THREE EXCLUSIONS ARE COMPUTED FROM ANCHORS, NOT FROM LINE NUMBERS, AND EACH IS ASSERTED NON-VACUOUS BEFORE THE RULE RUNS. An exclusion that silently matches nothing turns the guard green by excluding zero, and an exclusion that silently matches everything turns it green by excluding the file. Each is pinned by a measured line count and by a positive containment assertion, and the emptied-exclusion failing path is executed."
    - "THE SURFACE BELOW THE REGISTRY IS TRIAGED, NOT ASSUMED — AND NOBODY HAD MEASURED IT BEFORE. The 3,615 lines below `RESOLVER_REGISTRY` carry the declared phrasings 26 times line-based / 27 joined, of which 9 are the declaration itself. Every remaining occurrence is classified in this plan's SUMMARY as DELETED or as a named exemption with its one-line reason, with the classification table pasted. Neither the reviewer nor the verifier measured this region; had the guard scanned only the header, this would have been round 8's finding."
    - "FOUR MUTATION PROOFS, EACH EXECUTED SEPARATELY, NEVER COMBINED, EACH PASTED WITH ITS RED TEST TITLE AND ITS ASSERTION MESSAGE, `git diff --exit-code` shown clean between them and the run order stated: a declared phrasing re-planted on one line; the same re-planted wrapped across two comment lines; one exclusion emptied; one exemption entry removed while its sentence stands. EVERY TASK COMMITS BEFORE IT MUTATES — wave 29 destroyed its own Task 1 with `git checkout --` against uncommitted work, and waves 30, 31 and 32 all used commit-then-mutate."
    - "NO RULE IS WIDENED AND NO SHAPE IS CLOSED IN THIS PLAN. CR-15 and CR-16 are wave 34's; CORE-11's checkbox is wave 35's. The resolver-region diff is comment deletions, the new guard, the new exemption map and their fixtures — shown by a diff stat, not asserted."
    - "NOTHING LEAKS AND THIS PLAN DOES NOT IMPLY OTHERWISE. The must-NOT holds: zero outbound-shaped tokens in any shipped non-spec source across both roots and the frontend, the shipped bundle's entire import set is one specifier (`crypto`), and the real tree runs 23 files / 0 violations. Every finding this plan closes is a PROSPECTIVE blindness in a TEST-ONLY gate, and CR-14 and WR-38 are stale TEXT rather than unenforced behaviour."
  artifacts:
    - path: "packages/backend/src/outbound-prohibition.spec.ts"
      provides: "The header's bounds deleted and replaced by pointers; `HEADER_QUANTIFIER_EXEMPTIONS`; the whole-file wrap-tolerant quantifier guard with its three anchored exclusions, its two pinned counts, its non-vacuity assertions and its stated limits"
      contains: "HEADER_QUANTIFIER_EXEMPTIONS"
    - path: ".planning/REQUIREMENTS.md"
      provides: "A dated CORE-11 correction recording that the gate's own header now states no bound, what was deleted and why deleting beat widening; the machine-owned span byte-identical or regenerated if a row moved"
      contains: "BEGIN DERIVED RESIDUAL"
    - path: ".planning/STATE.md"
      provides: "A pointer amendment naming the deletion, the guard and its stated limits — restating no bound"
      contains: "HEADER_QUANTIFIER_EXEMPTIONS"
    - path: ".planning/WINDOWS.md"
      provides: "Entry 37 superseded and a new pointer entry appended, naming the surface deletion and restating no bound"
      contains: "outbound-prohibition.spec.ts"
  key_links:
    - from: "the gate file's own hand-written bytes"
      to: "`UNBOUNDED_QUANTIFIERS`"
      via: "a wrap-tolerant whole-file scan with three anchored, non-vacuous exclusions, every surviving hit required to be a named `HEADER_QUANTIFIER_EXEMPTIONS` entry"
      pattern: "HEADER_QUANTIFIER_EXEMPTIONS"
    - from: "the header's deleted paragraphs"
      to: "the generated span between `DERIVED_BEGIN` and `DERIVED_END`"
      via: "a pointer sentence that states no bound, in the shape `:931-941` and `:943-955` already use for `.planning/STATE.md` and `.planning/WINDOWS.md`"
      pattern: "DERIVED_BEGIN"
    - from: "CORE-11's checkbox state"
      to: "the two places that may state it"
      via: "the ledger row plus `CORE11_BOX_EXPECTED`, with the header's third copy deleted rather than corrected"
      pattern: "CORE11_BOX_EXPECTED"
    - from: "each exclusion range"
      to: "the construct it claims to exclude"
      via: "anchor-derived line bounds plus a positive containment assertion and a pinned line count, so an exclusion that matches nothing or everything fails loudly"
      pattern: "DERIVED_END"
  prohibitions:
    - requirement_id: CORE-11
      category: security
      status: resolved
      verification: gate
      resolution: "packages/backend/src/outbound-prohibition.spec.ts — the header paragraphs at `:756-772`, `:774-781` and `:908-916` DELETED and replaced by pointers to the generated span and to `CORE11_BOX_EXPECTED`, plus a whole-file wrap-tolerant guard requiring every surviving declared phrasing to be a named exemption"
      statement: "MUST NOT state a bound on any surface other than the derived span. Six rounds each corrected a stale sentence and each acquired another, because a corrected sentence is still an authored bound standing beside a derived one. The gate's own header now points and does not assert."
    - requirement_id: CORE-11
      category: security
      status: resolved
      verification: gate
      resolution: "packages/backend/src/outbound-prohibition.spec.ts — the `:908-916` completion paragraph deleted, leaving the ledger row and `CORE11_BOX_EXPECTED` as the only statements of the box's state"
      statement: "MUST NOT assert CORE-11's completion anywhere except the ledger row the suite pins. For a checkbox flipped early and reverted twice, at `e7cc4b6` and `faca607`, a header asserting completion is what a reader and a later planner meet 8,100 lines before the assertion that contradicts it."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "packages/backend/src/outbound-prohibition.spec.ts — the guard scans the joined, prefix-stripped form and its wrapped-phrasing failing path is executed as a separate mutation"
      statement: "MUST NOT ship a guard whose executed reach is narrower than its stated reach. The reviewer's suggested line-based scan reports 20 where 24 exist, because four occurrences wrap across comment lines — this round's own defect, arriving inside its own fix."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "packages/backend/src/outbound-prohibition.spec.ts — the guard's stated limits emitted beside it: phrase-list reach, normalization dependence, bytes-not-meaning, and that the deletion rather than the guard is what makes the header correct"
      statement: "MUST NOT let the quantifier guard read as though it ends the class. It is a phrase list over bytes under one whitespace convention. A universal spelled in undeclared words passes it, and saying so is the difference between this guard and the six sentences it replaces."
    - requirement_id: CORE-11
      category: safety
      status: open
      verification: gate
      resolution: "DISCLOSED, NOT CLOSED — recorded in this plan's SUMMARY and in the ledger correction as a measured, named limit"
      statement: "MUST NOT imply that deleting the header's bounds makes the gate's REACH complete. It makes the gate's DESCRIPTION OF ITSELF singular. CR-15 and CR-16 are unclosed code blindnesses on two clauses CORE-11's own first sentence enumerates, they are wave 34's, and the class they belong to is open because the space of JavaScript spellings is open."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: ".planning/STATE.md and .planning/WINDOWS.md — pointer amendments naming the deletion and the guard, restating no bound"
      statement: "MUST NOT put a fresh copy of the bound into an append-only history. The pointer-not-a-bound rule remains a PROHIBITION WITH NO MECHANICAL CHECK for those two files — the byte comparison reaches the gate header and the ledger and no further — and it is named as an unguarded limit rather than left implicit."
---

<objective>
Delete every bound the gate file states outside the generated span, and install the cheap guard that
keeps them deleted. CR-14 and WR-38 die with the paragraphs that carry them; WR-43's mechanism is
answered by removing the surface rather than instrumenting it.

Purpose: the operator has adopted a reachable bar for CORE-11 (see wave 35). Two of its three criteria
— DERIVED and DRIFT-DETECTABLE — round 6 already satisfies and the verifier mutation-proved. The third,
THE SOLE BOUND, is not satisfied: 3,464 hand-written lines ahead of `RESOLVER_REGISTRY` carry the
declared phrasings 20 times and raise zero obligations, because every guard scans
`RESOLVER_REGISTRY[].clause` and nothing else. This plan is that third criterion and nothing else.

Output: a gate file whose hand-written prose points at the derived span and asserts nothing; a
whole-file, wrap-tolerant quantifier guard with three anchored exclusions and a named exemption map;
four executed mutation proofs; and pointer amendments on both append-only histories.

ASSUMPTION-DELTA DISPOSITION, RECORDED RATHER THAN RE-RAISED. `gsd-tools query assumption-delta scan
01 --json` returns `detected: true` with a single `pluralization` signal on the term `another`, snippet
"… own code and CI-checked — rather than correcting another instance of it, plus 4 further gap-closure
plans …". That is ROADMAP narrative prose about gap-closure rounds, not a pluralization decision in
this phase's scope. Round 6 examined the identical line and dismissed it on the same grounds, recorded
in 01-29's objective. Dismissed again by citation, not re-litigated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md
@.planning/phases/01-skeleton-persistence-compatibility/01-REVIEW.md
@.planning/phases/01-skeleton-persistence-compatibility/01-32-SUMMARY.md
@.planning/phases/01-skeleton-persistence-compatibility/01-29-SUMMARY.md
@packages/backend/src/outbound-prohibition.spec.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: One phrasing, end to end — measure the real surface, build the wrap-tolerant guard with its three anchored exclusions, delete CR-14's paragraph, and watch both the single-line and the WRAPPED mutation go red</name>

  <precondition>
    Entering this plan the two derived spans are byte-identical and the suite is green. Assert before
    the first edit: the byte comparison cases `the block shipped in the gate header equals
    deriveResidual(RESOLVER_REGISTRY), byte for byte` and its `.planning/REQUIREMENTS.md` twin both
    pass, and `pnpm test` reports no fewer than 31 files / 1345 tests, exit 0. Halt if either
    comparison is already red: a guard written on top of a diverged span ships the divergence. Also
    assert the measurements below REPRODUCE AT HEAD rather than that HEAD equals a literal: they were
    taken at `e193dfc`, and the planning commits since then touch only `.planning/` plan and roadmap
    files — show that with `git diff --stat e193dfc..HEAD -- packages/ scripts/ tests/`, which must be
    EMPTY, and with `git diff e193dfc..HEAD -- packages/backend/src/outbound-prohibition.spec.ts
    .planning/REQUIREMENTS.md`, also empty, which is what makes every line reference and count in this
    plan live. Do NOT assert hash equality: this plan was reviewed and its ROADMAP entries committed
    after it was written, so a literal pin is false on arrival and would force the executor either to
    halt on a stated precondition or to silently ignore one — and silently ignoring a stated assertion
    is exactly what this phase punishes. Wave 31
    was handed a git-status snapshot seven commits stale, and this repo has NO active git hooks
    (`.git/hooks` holds only samples and `core.hooksPath` is unset), so nothing runs the gate set for
    you at a commit boundary.
  </precondition>

  <read_first>
    - `packages/backend/src/outbound-prohibition.spec.ts`, the header region in full from `:1` to
      `:955`. Read it as a whole before deleting any of it: the paragraphs are numbered sections and
      the deletion must leave the numbering coherent. Pay particular attention to `:931-941`, the
      wave-27 SURFACE DECISION, which is the exact precedent this task extends — it states that
      `.planning/STATE.md` and `.planning/WINDOWS.md` carry a POINTER and restate NO bound of their
      own, and names that rule as a prohibition with no mechanical check. And to `:943-955`, wave 28's
      note, which says of itself that it "restates NO bound of its own, deliberately". Those two
      paragraphs are the shape every replacement pointer is written in.
    - `:756-772` and `:774-781` — CR-14's two sites, to be DELETED. The first asserts that an operator
      wrapping a global receiver is still silent, names six exemplars, claims they all report empty,
      and claims to be pinned by a fixture titled as a measured silence. The second lists the same
      shape as open and unowned with no plan claiming it. Wave 32 closed all six; the fixture that
      sentence points at is now titled `… CLOSED (CR-11)` at `:7748`; and the registry row
      `silence-operator-around-global-receiver` was REMOVED with a guard at `:8853-8861` asserting it
      never returns. Read `:7748` and `:8853-8861` before deleting, so the pointer you write names
      what actually exists.
    - `:908-916` — WR-38's site, to be DELETED. Read it beside `:9020` (`CORE11_BOX_EXPECTED`) and
      beside `.planning/REQUIREMENTS.md:46` so you have seen all three statements of the box's state
      before reducing them to two.
    - `UNBOUNDED_QUANTIFIERS` at `:5664-5674` with its docblock, and `QUANTIFIED_CLAUSES` at
      `:5686-5700`. The docblock is careful about the PHRASE-LIST limit and says nothing about the
      SURFACE limit — that omission is WR-43 and this task closes it by making the surface the whole
      file and stating the remaining limits.
    - The existing quantifier scan at `:8809-8842` and the branch-vocabulary scan at `:8620-8648`.
      Both are `RESOLVER_REGISTRY.flatMap((row) => … row.clause …)`. Read them so the new guard is
      written in the same idiom and so you can state precisely what it adds rather than duplicating
      what it already does.
    - `DERIVED_BEGIN` and `DERIVED_END` at `:5722-5723`, and `extractDerivedBlock` at `:5855`. The
      sentinels are already exported and are what the first exclusion is anchored on.
    - `RESOLVER_EXEMPTIONS` at `:5954-6046` in full. `HEADER_QUANTIFIER_EXEMPTIONS` is written in its
      shape and for the argument its docblock already makes: a named, reasoned entry is something a
      reviewer can execute and disagree with, and an unnamed blind spot is not.
    - `tests/pins.spec.ts` in full — non-vacuity BEFORE the rule, a pure predicate, and the
      predicate's failing path executed against synthetic input AND against the real file's bytes.
      This task adds one enumeration and one predicate and needs that shape.
    - `01-VERIFICATION.md`'s `## CR-14 — confirmed, and the surface limit is the real finding` section
      and the `coincidental_reliance_items` entry titled "The derived residual states the whole bound
      on CORE-11", in the verifier's own words including the instruction to delete rather than widen.
    - `01-REVIEW.md` WR-43 in full, including its suggested guard — which is LINE-BASED, and which
      this task deliberately does not copy. Read it so the SUMMARY can state precisely what was
      changed about the suggestion and why.
  </read_first>

  <files>packages/backend/src/outbound-prohibition.spec.ts</files>

  <reversibility rating="reversible">Deletes hand-written comment prose and adds one guarded enumeration. No rule, no resolver and no registry row changes; the derived span is untouched. Reverting is a `git revert` of one commit.</reversibility>

  <action>
    MEASURE BEFORE YOU WRITE ANYTHING. Round 6 caught a wrong stated fix twice and found unnamed shapes
    four times, and every one of those came from measuring first. Produce a table, pasted into the
    SUMMARY, of every declared phrasing occurrence in the gate file broken down by REGION and by
    SCANNING CONVENTION. The four regions are: the header from line 1 to the line carrying
    `DERIVED_BEGIN`; the machine-owned span strictly between the sentinels; the code region from the
    line after `DERIVED_END` to the `RESOLVER_REGISTRY` declaration; the registry's own range; and the
    region from the registry's closing bracket to end of file. The two conventions are LINE-BASED — the
    reviewer's suggestion, one `grep` per line — and JOINED, in which each line has its leading comment
    prefix stripped, the lines are concatenated with a single space, and runs of whitespace are
    collapsed to one. The planning-time measurement at HEAD `e193dfc`, which you should reproduce and
    must reconcile if you do not: header 12 line-based and 14 joined; code-above-registry 8 and 10;
    hand-written region as a whole 20 and 24; registry range 12 and 12; below-registry 26 and 27;
    machine-owned span 12 and 12. If your numbers differ, the file moved and the DIFFERENCE is the
    finding — record it rather than adopting either number silently.

    THE 20-VERSUS-24 GAP IS THE POINT AND IT DECIDES THE DESIGN. The verifier's 20 and this plan's 20
    agree exactly, and both are line-based. Four occurrences are invisible to a line-based scan because
    they wrap across two comment lines. Build the guard on the JOINED convention. State the convention
    in the guard's own code as a named helper rather than leaving a reader to infer it from a regular
    expression, and pin BOTH counts so a later author who switches conventions gets a red test instead
    of a quieter number.

    DECLARE THE THREE EXCLUSIONS, ANCHORED AND NON-VACUOUS. Exclusion one is the machine-owned span,
    bounded by `DERIVED_BEGIN` and `DERIVED_END`, which legitimately carries the phrasings because it
    is the derived residual itself. Exclusion two is the `UNBOUNDED_QUANTIFIERS` declaration, which
    carries every phrasing by construction because it IS the list — anchor it on the declaration's own
    opening line and its closing bracket, never on line numbers. Exclusion three is
    `RESOLVER_REGISTRY`'s range, whose clauses are already obligated by the existing scan at `:8809`
    plus `QUANTIFIED_CLAUSES`; anchor it the same way. Each exclusion asserts, BEFORE the rule runs,
    that it is non-empty, that its line count is within a pinned band, and that it positively contains
    a token only that construct has. An exclusion that silently matches nothing turns the guard green
    by excluding zero; one that silently matches everything turns it green by excluding the file. Both
    directions must fail loudly.

    STATE THE COARSENESS OF EXCLUSION THREE RATHER THAN HIDING IT. Excluding the registry's whole line
    range also excludes any docblock sitting between rows, which is wider than "the clause strings".
    Say so in the guard's docblock, name it as a limit, and record in the SUMMARY how many occurrences
    fall in that range and how many of them are inside a clause versus inside a comment. Measure it;
    do not estimate it.

    THEN RECONCILE THAT MEASUREMENT WITH WAVE 35, BECAUSE THE TWO PLANS CURRENTLY DISAGREE. Wave 35's
    criterion-3 check (c) requires every declared-phrasing occurrence inside each exclusion to be a
    machine-owned span, the declaration itself, or a `RESOLVER_REGISTRY[].clause` — and verdicts the row
    `✗` on an exclusion that swallows one hand-written universal. A between-rows docblock occurrence is
    none of those three, so if this measurement finds any registry-range occurrence in a comment rather
    than in a clause, wave 35 reaches a `✗` that THIS wave already knew about. Do not let it be
    discovered there. Take one of two dispositions and write down which and why: NARROW exclusion three
    from the registry's whole line range to the clause strings themselves, so the coarseness stops
    existing; or KEEP the coarse range and record in this plan's SUMMARY, in as many words, that it
    hands wave 35 a known criterion-3 blocker, with the count. Either is honest; discovering it in wave
    35 after ~70k tokens is not.

    DELETE CR-14's TWO SITES AND REPLACE THEM WITH ONE POINTER. Remove the paragraph at `:756-772`
    entirely, including its six exemplars, its claim about what they report, and its closing claim
    about being pinned by a fixture. Remove the entry at `:774-781` that lists the shape as open and
    unowned. In their place write a single short sentence in the shape `:943-955` uses: that the
    residual OF RECORD is the generated span between the sentinels, that this paragraph restates no
    bound of its own, and that the history of what was believed and when lives in the SUMMARYs. Do not
    write a corrected version of the deleted claim. A corrected claim is a bound, and a bound is the
    thing that drifted through seven waves.

    PRESERVE THE HISTORY WHERE IT IS NOT A BOUND. The six exemplars and their now-reporting answers go
    into this plan's SUMMARY with their executed rule identifiers, so the seven waves that believed the
    universal are not erased. They do not go back into the file in any form.

    WRITE THE GUARD, ONE CASE, IN THE FILE'S IDIOM. Non-vacuity first: the scanned surface is non-empty
    and its line count is pinned by equality against a measured band. Then the rule: every declared
    phrasing occurrence outside the three exclusions is either absent or carries a
    `HEADER_QUANTIFIER_EXEMPTIONS` entry. The assertion message names the line number, the phrasing and
    the reason an exemption would need to give — a reader who trips this guard must learn what to do
    from the message, not from the docblock.

    THE TRACER SCOPE, STATED SO IT IS NOT MISTAKEN FOR THE WHOLE JOB. This task carries ONE region — the
    header — from measurement, through the guard, through deletion, through both mutation proofs, and
    out the other side green. The code region and the below-registry region are Task 2's, and the
    exemption entries they need are Task 2's. If the shape is wrong it is wrong on one region rather
    than on three. Task 1 leaves the guard scanning the whole file with the other two regions' surviving
    occurrences carried as exemption entries marked as Task 2's to triage, so the suite is green at the
    task boundary and the obligation is visible rather than deferred silently.

    COMMIT BEFORE YOU MUTATE. Wave 29 destroyed its own Task 1 by running `git checkout --` against
    uncommitted work; waves 30, 31 and 32 all used commit-then-mutate and none lost anything. Commit
    this task's real work first, then plant each mutation, run, record, restore, and cross-check with
    `git diff --exit-code` between them.

    RUN THE TWO MUTATIONS SEPARATELY AND NEVER TOGETHER. Mutation one: re-plant one declared phrasing
    into the header as a single line, run, record the red title and the assertion message, restore.
    Mutation two, and it is the one that matters: re-plant a declared phrasing WRAPPED across two
    comment lines so that no single line contains it, run, record. If mutation two does not go red the
    guard is line-based in effect whatever its code says, and the task is not done. State the run order
    explicitly in the SUMMARY.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/DefMiner && pnpm vitest run packages/backend/src/outbound-prohibition.spec.ts 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - Executed `grep -c "STILL SILENT" packages/backend/src/outbound-prohibition.spec.ts` restricted to
      lines 1 through the `DERIVED_BEGIN` line, observed 0 — the CR-14 paragraph is gone from the
      header rather than reworded.
    - Executed a scan of the header region for the sentence listing the operator-around-a-global-receiver
      shape as unowned, observed 0 occurrences; and executed `sed -n '7740,7760p'` observing the fixture
      still titled as closed, so the pointer that replaced the paragraph names something that exists.
    - Executed a scan of the header region for the assertion that CORE-11 is marked complete, observed
      0; and executed `grep -n 'CORE11_BOX_EXPECTED' packages/backend/src/outbound-prohibition.spec.ts`
      observing the constant still present and unchanged, and
      `grep -n '^- \[[ x]\] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md` observing exactly one row —
      state-agnostic on purpose, because `[ ]` is a legitimate outcome and wave 35 owns the box.
    - The region/convention measurement table is pasted into the SUMMARY with all five regions and both
      conventions, and the hand-written region's two numbers are stated side by side with the statement
      that the line-based one is the reviewer's suggestion and is the narrower of the two.
    - Executed `pnpm vitest run packages/backend/src/outbound-prohibition.spec.ts`, observed the new
      quantifier-surface case passing and the total case count no lower than the pre-task count, with
      the pre- and post- counts both pasted.
    - MUTATION 1 executed: one declared phrasing re-planted on a single header line, run, observed RED,
      the failing test title and the full assertion message pasted, restored, `git diff --exit-code`
      shown clean.
    - MUTATION 2 executed SEPARATELY: one declared phrasing re-planted WRAPPED across two comment lines
      such that no single line contains it, run, observed RED, title and message pasted, restored,
      `git diff --exit-code` shown clean. The SUMMARY states the run order and states in one sentence
      that had the guard been line-based this mutation would have passed.
    - Each of the three exclusions has a non-vacuity assertion that was watched failing at least in
      Task 2, and Task 1 pastes the pinned line counts each exclusion resolved to.
    - The guard's stated limits appear in its own docblock: phrase-list reach, normalization
      dependence, bytes-not-meaning, the coarseness of the registry exclusion, and the sentence that the
      deletion rather than the guard is what makes the header correct.
    - `git log --oneline -1` pasted showing the real work was committed BEFORE the first mutation.
    - THE REGISTRY-EXCLUSION DISPOSITION IS STATED AS `NARROW` OR `KEEP`, with its reason and the
      clause-versus-comment occurrence count pasted from a run rather than estimated. If `KEEP`, the
      SUMMARY says IN AS MANY WORDS that this wave hands wave 35 a known criterion-3 blocker, with the
      count — because wave 35's check (c) verdicts `✗` on an exclusion swallowing a hand-written
      universal, and a between-rows docblock occurrence is none of its three permitted classes. An
      action-only disposition with no stated choice fails this criterion: an unenforced instruction is
      an instruction that drifts, which is this phase's own repeated lesson.
  </acceptance_criteria>

  <done>
    CR-14's two sites and WR-38's site are deleted from the header and replaced by a pointer that
    states no bound. A whole-file, wrap-tolerant quantifier guard with three anchored, non-vacuous
    exclusions is green, and both its single-line and its wrapped failing paths have been watched red
    and restored.
  </done>
</task>

<task type="auto">
  <name>Task 2: The other two regions, triaged occurrence by occurrence — deletions where a sentence states a bound, named exemptions where it does not, and the emptied-exclusion and missing-exemption failing paths executed</name>

  <read_first>
    - The Task 1 measurement table, and `packages/backend/src/outbound-prohibition.spec.ts` at every
      line it names in the code region (between `DERIVED_END` and the `RESOLVER_REGISTRY` declaration)
      and in the below-registry region. The planning-time line list for the code region, to be
      reconfirmed rather than trusted: `:1755`, `:2770`, `:2788` (which carries two occurrences on one
      line), `:2795`, `:2949`, `:2950`, `:3060`. For the below-registry region: `:5643`, `:5644`,
      `:5665-5673` (the `UNBOUNDED_QUANTIFIERS` declaration itself, excluded by construction),
      `:5693`, `:5695`, `:6535`, `:6918`, `:7013`, `:7042`, `:7043`, `:7066`, `:7071`, `:7120`,
      `:7294`, `:7726`, `:8835`, `:8860`.
    - `RESOLVER_EXEMPTIONS` at `:5954-6046` and the `HEADER_QUANTIFIER_EXEMPTIONS` map Task 1 created,
      so every entry written here matches the established shape: named, reasoned, and phrased as a
      statement of what was measured rather than as a justification.
    - `QUANTIFIED_CLAUSES` at `:5686-5700`, because `:5693` and `:5695` are its own entry VALUES. An
      entry that states what BOUNDS a universal necessarily quotes the universal, which is a different
      act from asserting one — decide that deliberately and record the decision, do not let it fall out
      of a regular expression.
    - `:2788`, `:2949-2950` and `:3060` in their surrounding docblocks. These are resolver docblocks
      describing what the walk does. Read enough context around each to decide whether the sentence
      states a BOUND (delete or rewrite to point at the derived span) or merely NAMES a mechanism
      (exempt with a reason). The distinction is the whole task and it cannot be made from the matched
      line alone.
    - `:8835` and `:8860`, which sit inside the guard region added by earlier waves — check whether
      they are test titles or assertion messages quoting a clause, because a message that quotes the
      text it is complaining about is not asserting it.
  </read_first>

  <files>packages/backend/src/outbound-prohibition.spec.ts</files>

  <reversibility rating="reversible">Comment edits and additions to a frozen exemption map. No rule, resolver or registry row changes.</reversibility>

  <action>
    TRIAGE EVERY SURVIVING OCCURRENCE INTO EXACTLY ONE OF THREE DISPOSITIONS, AND RECORD THE
    DISPOSITION WITH ITS REASON IN A TABLE THE SUMMARY CARRIES. Disposition DELETE: the sentence states
    a bound on the walk's reach, and it is removed and replaced — where the paragraph still has a job —
    by a pointer to the generated span in the shape Task 1 established. Disposition REWRITE: the
    sentence describes a mechanism but reaches for a universal to do it, and it is rewritten to say
    what the branch does without the quantifier, which needs no exemption entry because the phrasing is
    gone. Disposition EXEMPT: the occurrence is not an assertion about reach at all — it quotes a
    clause, it is an assertion message complaining about the phrasing, it is a test title naming the
    shape under test, or it is a `QUANTIFIED_CLAUSES` value stating what bounds a universal — and it
    gets a named `HEADER_QUANTIFIER_EXEMPTIONS` entry whose reason a reviewer can execute and disagree
    with.

    PREFER DELETE AND REWRITE OVER EXEMPT, AND SAY WHY IN THE SUMMARY. An exemption map that grows to
    forty entries has instrumented the surface rather than removed it, which is the outcome the verifier
    told this round to avoid. Every exemption is a sentence a later author must keep true. Count them,
    state the count, and state the count as a cost rather than as a result.

    HANDLE THE `QUANTIFIED_CLAUSES` VALUES DELIBERATELY. Those entries exist to state what measurably
    bounds a clause's universal, so they necessarily contain the universal's words. Exempt them as a
    NAMED CLASS with one reason covering the class, rather than one entry per value — and state in the
    reason that the class is bounded by `QUANTIFIED_CLAUSES`' own key set, so it cannot grow silently.

    DO NOT TOUCH THE `UNBOUNDED_QUANTIFIERS` DECLARATION. It is excluded by construction in Task 1 and
    an exemption entry for it would be a second mechanism for the same fact.

    EXECUTE THE TWO REMAINING FAILING PATHS, EACH SEPARATELY, COMMITTING THIS TASK'S REAL WORK FIRST.
    Mutation three: empty one exclusion — the registry range is the clearest — run, and observe the
    guard go red on the occurrences that exclusion was carrying, confirming the exclusion is
    load-bearing rather than decorative; also observe the exclusion's own non-vacuity assertion go red,
    which is the direction that catches an exclusion silently matching nothing. Record both red cases,
    restore, cross-check with `git diff --exit-code`. Mutation four: remove one
    `HEADER_QUANTIFIER_EXEMPTIONS` entry while leaving the sentence it covers standing, run, observe the
    guard name that exact line and phrasing in its assertion message, restore, cross-check.

    RE-RUN THE MEASUREMENT AFTER THE TRIAGE AND PASTE THE BEFORE-AND-AFTER TABLE. The joined-convention
    count outside the three exclusions must be exactly the number of exemption entries, and that
    equality is itself pinned by the guard rather than checked by hand.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/DefMiner && pnpm vitest run packages/backend/src/outbound-prohibition.spec.ts 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - The triage table is pasted into the SUMMARY with one row per surviving occurrence, each carrying
      its line number, its phrasing, its disposition and its one-line reason. Every occurrence the Task
      1 measurement found outside the three exclusions appears in exactly one row.
    - The exemption count is stated as a number and stated as a cost, with one sentence naming what a
      later author now has to keep true.
    - Executed the joined-convention scan after the triage, observed the count outside the three
      exclusions equal to the `HEADER_QUANTIFIER_EXEMPTIONS` entry count, and observed the guard's own
      equality assertion enforcing that rather than a hand check.
    - MUTATION 3 executed: one exclusion emptied, run, observed RED in BOTH the guard case and that
      exclusion's non-vacuity case, both titles and both assertion messages pasted, restored,
      `git diff --exit-code` clean.
    - MUTATION 4 executed SEPARATELY: one exemption entry removed while its sentence stands, run,
      observed RED with the assertion message naming that exact line number and phrasing, pasted,
      restored, `git diff --exit-code` clean.
    - `git log --oneline -1` pasted showing this task's real work was committed BEFORE the first
      mutation of the task.
    - Executed `pnpm vitest run packages/backend/src/outbound-prohibition.spec.ts`, exit 0, count
      pasted.
    - Executed `pnpm typecheck`, `pnpm lint` and `pnpm knip`, all exit 0, output pasted — `knip` in
      particular, because a new frozen exported map is exactly what it flags when nothing reads it.
  </acceptance_criteria>

  <done>
    Every declared-phrasing occurrence in the gate file outside the three named exclusions is either
    gone or carries a named, reasoned exemption, the equality between the two counts is machine-pinned,
    and both remaining failing paths have been watched red and restored.
  </done>
</task>

<task type="auto">
  <name>Task 3: The ledger correction, both pointer surfaces, and the full gate set — with the limit this wave does NOT close stated in the same breath as what it does</name>

  <read_first>
    - `.planning/REQUIREMENTS.md` `:44-115`, CORE-11's entry and every dated correction above the
      `BEGIN DERIVED RESIDUAL` sentinel at `:116`. Read wave 32's correction at `:86` in full: it is
      the shape this task's correction is written in — dated, naming the plan and wave, stating what
      was closed and what was not, and stating the limits because the ledger is where an overclaim does
      its damage.
    - `.planning/REQUIREMENTS.md` `:116` and `:627`, the sentinels, and the span between them. This
      task does NOT hand-edit the span. If no registry row moved, the span is byte-identical and that
      is asserted rather than assumed.
    - `.planning/STATE.md`'s decision P9-D3 pointer amendments — the six that already exist — so the
      seventh matches their shape and so the six previous can be shown byte-identical.
    - `.planning/WINDOWS.md` entry 37, the current pointer entry for this gate file, and the ledger's
      header instructions for `gsd-tools windows`.
    - `01-32-SUMMARY.md`'s closing sections, for the exact form the "nothing leaks" statement and the
      final gate table take in this phase.
  </read_first>

  <files>.planning/REQUIREMENTS.md, .planning/STATE.md, .planning/WINDOWS.md</files>

  <reversibility rating="reversible">Documentation and ledger pointers only. No source file changes.</reversibility>

  <action>
    WRITE THE DATED CORRECTION INTO CORE-11's ENTRY, ABOVE THE BEGIN SENTINEL AND BELOW WAVE 32's. It
    states, in this order: that the gate file's hand-written header now states no bound of its own and
    points at the generated span; that CR-14 and WR-38 were closed by DELETION rather than by
    correction, and why deleting beat widening in the verifier's own terms — a corrected sentence is
    still an authored bound standing beside a derived one, and widening the scan would have converted
    stale sentences into new obligations; that a whole-file wrap-tolerant guard now keeps them deleted,
    with its exclusions and its exemption count named; and that the guard is a phrase list over bytes
    under a named whitespace convention, so a universal spelled in undeclared words passes it.

    AND STATE, IN THE SAME PARAGRAPH RATHER THAN IN A LATER ONE, WHAT THIS WAVE DOES NOT DO. It does
    not close CR-15 or CR-16. It does not widen the gate's reach by one shape. It does not touch
    CORE-11's checkbox. Two of the eight clauses CORE-11's own first sentence enumerates still carry a
    named blocking shape and both are CODE, and wave 34 owns them. A reader who takes this correction
    for progress on the gate's REACH has been misled by it, and the correction is written so they
    cannot.

    DO NOT REGENERATE THE SPAN UNLESS A REGISTRY ROW MOVED. Assert byte-identity instead, and paste the
    line count, the entry count and the sha256 from both files side by side. NOTE THE HASHING
    CONVENTION, measured during planning and recorded so nobody re-derives it: the span is 510 lines in
    both files, and the sha the verifier quoted — `f850802f402eabb23e6b044f3b65ae819ebdc75d57aecc89de444e7c433157fe`
    — reproduces only when the extracted span is hashed WITHOUT a trailing newline, which is what
    `extractDerivedBlock` returns. A naive `sed | shasum` includes the trailing newline and yields a
    different digest. That is a hashing convention, not a divergence; state it so a future session does
    not mistake one for the other.

    APPEND THE SEVENTH POINTER AMENDMENT TO `.planning/STATE.md`, RESTATING NO BOUND. It names the
    deletion, the guard, the exemption map and the enforcing test, points at the generated span, and
    repeats that the pointer-not-a-bound rule for `STATE.md` and `WINDOWS.md` is a prohibition with no
    mechanical check. Show the six previous amendments byte-identical by diff.

    SUPERSEDE WINDOWS ENTRY 37 AND APPEND A NEW POINTER ENTRY through `gsd-tools windows`, never by
    hand-editing the table. Paste every command with its raw output.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY. This repo has NO active git hooks — `.git/hooks`
    holds only samples and `core.hooksPath` is unset — so nothing runs them for you.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/DefMiner && pnpm test 2>&1 | tail -15 && pnpm typecheck && pnpm lint && pnpm knip && pnpm build:backend && pnpm check:bundle 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - CORE-11's new dated correction is present in `.planning/REQUIREMENTS.md` above the `BEGIN DERIVED
      RESIDUAL` sentinel, naming plan 01-33 and wave 33, and every hand-written correction above it is
      shown byte-identical by `git diff`.
    - The correction contains an explicit sentence that this wave closes no code blindness, widens the
      gate's reach by no shape, and leaves CR-15 and CR-16 open to wave 34. Quoted verbatim in the
      SUMMARY.
    - Executed `grep -n '^- \[[ x]\] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md`, observed exactly one
      row, and `git diff .planning/REQUIREMENTS.md` shows that line unchanged — state-agnostic on
      purpose; wave 35 owns the box.
    - Both spans asserted byte-identical: line count, entry count and sha256 pasted from both files side
      by side, with the no-trailing-newline hashing convention stated and both digests shown so the
      difference is explained rather than left as a discrepancy.
    - `.planning/STATE.md` carries a seventh pointer amendment restating no bound; the six previous
      shown byte-identical by diff.
    - Every `gsd-tools windows` command pasted with raw output; entry 37 superseded and one new pointer
      entry appended; the resulting open-entry count for this gate file stated.
    - Executed `pnpm test`, observed exit 0 with no fewer than 31 files and no fewer than 1345 tests,
      output pasted with both counts.
    - Executed `pnpm typecheck`, `pnpm lint`, `pnpm knip` — all exit 0, output pasted.
    - Executed `pnpm build:backend && pnpm check:bundle`, observed exactly one import specifier,
      `crypto`, output pasted.
    - Executed the real-tree run over both `SOURCE_ROOTS`, observed 23 files and 0 violations, with the
      four measured-exempt real call sites named: `compat.ts`'s `cur[key]` and `ctx[root]`, and
      `observations.ts`'s `segments[i]` and `MIGRATIONS[MIGRATIONS.length - 1]`.
    - Executed `git diff --exit-code` over `01-01-PLAN.md` through `01-32-PLAN.md`, `01-VERIFICATION.md`,
      `01-REVIEW.md` and `01-UAT.md` — clean, output pasted.
  </acceptance_criteria>

  <done>
    The ledger carries a dated correction that states what was deleted, why deletion beat widening, and
    what this wave does not close. Both append-only histories carry pointers restating no bound. The
    full gate set is green and pasted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the gate file's hand-written prose to its derived span | Seven waves wrote residuals into hand-written header prose that no guard reads. The header is what a reader and a later planner meet first; the derived span is 8,000 lines away. Every falsified universal this phase has found was written on the near side of this boundary. |
| a corrected sentence to a deleted one | Six rounds each corrected a stale sentence and each acquired another. A correction is still an authored bound; only a deletion removes the class. This boundary is the plan's central bet and it is stated as a bet. |
| the guard's scanning convention to the bytes it claims to read | A line-based scan reports 20 where 24 exist. The convention IS the reach, and a convention left implicit in a regular expression is a reach nobody stated. |
| each exclusion to the construct it claims to exclude | An exclusion matching nothing turns the guard green by excluding zero; one matching everything turns it green by excluding the file. Both are silent successes. |
| the exemption map to the sentences it covers | An exemption is a sentence a later author must keep true. A map that grows without a stated cost has instrumented the surface rather than removed it. |
| the pointer surfaces to the bound | `.planning/STATE.md` and `.planning/WINDOWS.md` are append-only dated histories the byte comparison does not reach. The rule that they restate no bound has no mechanical check and is named as an unguarded limit. |
| this plan's claim to CORE-11's reach | Deleting the header's bounds changes the gate's DESCRIPTION OF ITSELF and not its REACH by one shape. A reader taking this wave for coverage progress has been misled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-179 (CR-14) | Repudiation | a falsified universal surviving in the round that falsified it | high | mitigate | The paragraph at `:756-772` and the unowned-shape entry at `:774-781` are DELETED, not corrected, and replaced by a pointer that states no bound. The six exemplars and their executed answers are preserved in the SUMMARY, which is history rather than a bound. |
| T-01-180 (WR-38) | Spoofing | a header asserting a completion the ledger denies | high | mitigate | The `:908-916` paragraph is DELETED. After this wave the box's state is stated only by the ledger row and by `CORE11_BOX_EXPECTED`, and the contradiction cannot recur because one side of it no longer exists. |
| T-01-181 (WR-43) | Elevation of Privilege | a guard whose surface is narrower than its name | high | mitigate | The new guard scans the WHOLE FILE minus three anchored, non-vacuous exclusions, rather than the header alone. The below-registry region — 3,615 lines carrying 26 occurrences that nobody had measured — is inside its reach. |
| T-01-182 | Repudiation | a line-based scan reporting 20 where 24 exist | high | mitigate | The guard scans the joined, prefix-stripped, whitespace-collapsed form, both counts are pinned by equality, and a SEPARATE mutation re-plants a phrasing WRAPPED across two comment lines. Had the guard been line-based that mutation would have passed. |
| T-01-183 | Spoofing | an exclusion that silently matches nothing or everything | high | mitigate | Each exclusion is anchor-derived, asserts non-emptiness and a pinned line band before the rule runs, and positively asserts it contains a token only that construct has. Mutation 3 empties one and watches both the guard case and the non-vacuity case go red. |
| T-01-184 | Tampering | an exemption removed while its sentence stands | medium | mitigate | Mutation 4 removes one entry and observes the guard name that exact line and phrasing. The equality between occurrence count and entry count is machine-pinned rather than hand-checked. |
| T-01-185 | Repudiation | an exemption map that instruments instead of deletes | medium | mitigate | Triage prefers DELETE and REWRITE over EXEMPT, the entry count is stated as a cost with one sentence naming what a later author must keep true, and the `QUANTIFIED_CLAUSES` values are exempted as a bounded named class rather than one entry at a time. |
| T-01-186 | Repudiation | this plan's own claim about what it closes | high | mitigate | The ledger correction states in the same paragraph that this wave closes no code blindness and widens no reach, that CR-15 and CR-16 are open, and that wave 34 owns them. `must_haves.prohibitions` carries the same statement with status `open`. |
| T-01-187 | Tampering | mutating uncommitted task work | high | mitigate | Every task commits its real work before planting any mutation, and `git diff --exit-code` is run between mutations. Wave 29 destroyed its own Task 1 exactly this way; waves 30, 31 and 32 lost nothing using commit-then-mutate. |
| T-01-188 | Tampering | a gate set nobody ran | medium | mitigate | This repo has no active git hooks — `.git/hooks` holds only samples and `core.hooksPath` is unset — so the full set is run by hand at every task boundary and its output pasted. |
| T-01-144 | Repudiation | a fresh copy of the bound in an append-only history | medium | mitigate | `STATE.md` and `WINDOWS.md` get pointer amendments naming the deletion and the guard and restating no bound. Carried forward from wave 29 unchanged. |
| T-01-51 | Information Disclosure | the accepted residual of the outbound walk | medium | mitigate | Restated once more, generated rather than authored, and now the ONLY statement of the bound anywhere in the gate file. |
| T-01-50 | Information Disclosure | `.spec.ts` files, which the walk skips | low | accept | Unchanged and carried forward, bounded by `pnpm check:bundle`, which specs never enter. |
| T-01-34 | Tampering | any guard whose failing path has never run | high | mitigate | Four mutation directions executed separately, never combined, each RED by title and message, each restored and re-run green, run order stated. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | NOT APPLICABLE: this plan installs no package. `pnpm install` is not run and no manifest appears in `files_modified`. RESEARCH.md's Package Legitimacy Audit is present and unchanged; no `[ASSUMED]` or `[SUS]` package is introduced, so no blocking human checkpoint is required. |
</threat_model>

<verification>
1. `git rev-parse HEAD` matches the commit this plan was written against before the first edit, and
   `git status --porcelain packages/ .planning/` is clean.
2. Both byte comparisons green BEFORE the first edit; halt if either is red.
3. The five-region, two-convention measurement table produced and pasted, and reconciled against the
   planning-time numbers with any difference recorded as a finding.
4. CR-14's two header sites deleted; a pointer stating no bound in their place; the fixture the deleted
   sentence pointed at confirmed still present and still titled as closed.
5. WR-38's header site deleted; the box's state now stated only by the ledger row and
   `CORE11_BOX_EXPECTED`.
6. The whole-file guard present, wrap-tolerant, with three anchored exclusions, each non-vacuous, each
   pinned by a measured line band and a positive containment assertion.
7. Both occurrence counts pinned by equality; the exemption count equal to the surviving occurrence
   count, machine-enforced.
8. Every surviving occurrence triaged into DELETE, REWRITE or EXEMPT with a table pasted, one row per
   occurrence, covering both the code region and the below-registry region.
9. The guard's limits stated in its own docblock: phrase-list reach, normalization dependence,
   bytes-not-meaning, registry-exclusion coarseness, and that the deletion rather than the guard makes
   the header correct.
10. FOUR mutation proofs executed separately, never combined, each RED by test title and assertion
    message, each restored, each re-run green, `git diff --exit-code` clean between them, the run order
    stated. Real work committed before each mutation, shown by `git log --oneline -1`.
11. No rule widened and no shape closed: resolver-region diff stat pasted showing comment deletions,
    the guard, the exemption map and their fixtures only.
12. CORE-11's checkbox byte-unchanged, shown by a diff of that line.
13. The ledger correction present, dated, naming plan and wave, and containing the explicit sentence
    that this wave closes no code blindness and that wave 34 owns CR-15 and CR-16.
14. Both derived spans byte-identical, with line count, entry count and both sha digests pasted and the
    no-trailing-newline hashing convention stated.
15. `.planning/STATE.md` seventh pointer amendment present; six previous byte-identical.
16. `.planning/WINDOWS.md` entry 37 superseded and a pointer entry appended through the tool, commands
    and raw output pasted.
17. Real-tree run over both `SOURCE_ROOTS` reporting 23 files / zero violations, with the four
    measured-exempt sites named.
18. `pnpm test` — zero failures over no fewer than 31 files and no fewer than 1345 tests.
19. `pnpm typecheck`, `pnpm lint`, `pnpm knip` — all exit 0.
20. `pnpm build:backend && pnpm check:bundle` — exactly one import specifier, `crypto`.
21. `git diff --exit-code` over `01-01-PLAN.md` through `01-32-PLAN.md`, `01-VERIFICATION.md`,
    `01-REVIEW.md`, `01-UAT.md` — clean.
</verification>

<success_criteria>
- The gate file's hand-written prose states no bound anywhere outside the generated span, and CR-14 and
  WR-38 are gone because their paragraphs are gone rather than because their sentences were corrected.
- A whole-file, wrap-tolerant quantifier guard keeps them gone, with three anchored non-vacuous
  exclusions and a named exemption map whose size is stated as a cost.
- The wrapped-phrasing mutation goes red, which the reviewer's suggested line-based guard would not
  have caught — the round's own defect found inside its own fix and removed before it shipped.
- The below-registry region, which nobody had measured, is inside the guard's reach and its 26
  occurrences are triaged rather than inherited.
- Every claim the guard makes about itself is narrower than what a reader might assume, and the four
  limits are stated wherever the guard is claimed.
- No rule was widened, no shape was closed, and CORE-11's box is exactly as wave 32 left it.
- The green baseline is restored and recorded: no fewer than 31 files / 1345 tests, typecheck / lint /
  knip clean, bundle at exactly one specifier, real tree at 23 files / 0 violations.
</success_criteria>

<artifacts_this_phase_produces>
| Artifact | Kind | Purpose |
|----------|------|---------|
| the deleted CR-14 paragraph and unowned-shape entry | removed surface | The falsified universal and its six exemplars removed rather than corrected, so the class cannot recur on that surface |
| the deleted CORE-11 completion paragraph | removed surface | The box's state stated only by the ledger row and `CORE11_BOX_EXPECTED` |
| the replacement pointer sentences | corrected disclosure | Written in the shape `:931-941` and `:943-955` already use — point at the derived span, restate no bound |
| the whole-file wrap-tolerant quantifier guard | new gate fixture | A declared phrasing outside three named exclusions is a failing test, under a named whitespace convention, with both counts pinned |
| the three anchored exclusions with their non-vacuity assertions | new gate fixtures | An exclusion matching nothing or everything fails loudly rather than turning the guard green |
| `HEADER_QUANTIFIER_EXEMPTIONS` | new frozen data | One named, reasoned entry per surviving occurrence, in `RESOLVER_EXEMPTIONS`' disagreeable shape |
| the guard's stated limits | generated disclosure | Phrase list, normalization dependence, bytes-not-meaning, registry-exclusion coarseness, and that the deletion not the guard makes the header correct |
| the five-region two-convention measurement table | new evidence | The 20-versus-24 gap and the previously unmeasured below-registry region, both reproducible |
| the occurrence triage table | new evidence | One row per surviving occurrence with its disposition and reason |
| CORE-11's dated wave-33 ledger correction | corrected disclosure | What was deleted, why deletion beat widening, and the explicit statement that no code blindness was closed |
| `.planning/STATE.md`'s seventh P9-D3 pointer amendment | corrected decision record | Names the deletion and the guard, restates no bound, restates the unguarded pointer rule |
| `.planning/WINDOWS.md` entry 37 superseded plus a new pointer entry | corrected ledger | The tool-owned ledger points rather than copies |
| `01-33-SUMMARY.md` | new evidence | The measurement tables, the triage table, four mutation proofs, both span digests with the hashing convention, and the full gate set |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-skeleton-persistence-compatibility/01-33-SUMMARY.md` when done.

It must contain, as pasted terminal output rather than as prose: the five-region two-convention
measurement table before any edit, reconciled against the planning-time numbers with any difference
named; the six CR-14 exemplars with the rule identifiers they now produce, preserved as history in the
one place that is not a bound; the deleted paragraphs shown by diff rather than described; the pointer
sentences that replaced them, quoted; the guard's code with its three exclusions and its stated limits;
the registry-exclusion disposition stated as NARROW or KEEP with its reason and the pasted
clause-versus-comment count, and if KEEP the sentence handing wave 35 a known criterion-3 blocker with
that count; the exclusion line counts each anchor resolved to; the triage table with one row per surviving
occurrence carrying line, phrasing, disposition and reason; the exemption count stated as a cost with
one sentence naming what a later author must keep true; the after-triage measurement showing the
occurrence count equal to the exemption count and the guard's own equality assertion enforcing it; a
TABLE of FOUR mutation proofs — a phrasing on one line, a phrasing WRAPPED across two lines, an emptied
exclusion, a removed exemption — each row carrying the test that went red, the assertion message it
carried, the restore and the re-run green, with the run order stated, `git diff --exit-code` shown clean
between them, and `git log --oneline -1` showing the real work committed before each; one sentence
stating that had the guard been line-based the wrapped mutation would have passed; the resolver-region
diff stat proving no widening; the sha256, line count and entry count of the span from both files side
by side WITH the no-trailing-newline hashing convention stated and both digests shown; the anchor greps
proving append-never-rewrite in `REQUIREMENTS.md` and `STATE.md`; every `gsd-tools windows` command with
its raw output; the real-tree zero with all four measured-exempt sites named; the assumption-delta
disposition recorded with the matched line and the round-6 dismissal cited; and the final `pnpm test`,
`typecheck`, `lint`, `knip` and `check:bundle` results.

It must state in one sentence that this wave changed the gate's DESCRIPTION OF ITSELF and not its REACH,
that CR-15 and CR-16 remain open and belong to wave 34, and that CORE-11's box is exactly as wave 32
left it and wave 35 owns it.

It must also record any PREDICTION-VS-MEASUREMENT discrepancy in the shape `01-27-SUMMARY.md` used, and
if there were none, say so explicitly.
</output>
