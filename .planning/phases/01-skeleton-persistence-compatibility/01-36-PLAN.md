---
phase: 01-skeleton-persistence-compatibility
plan: 36
type: execute
wave: 36
gap_closure: true
depends_on: [01-35]
files_modified:
  - .planning/REQUIREMENTS.md
  - packages/backend/src/outbound-prohibition.spec.ts
# READ THIS BEFORE ANY TASK. THREE THINGS ARE TRUE AT PLANNING TIME AND TWO OF THEM ARE NEW.
#
# (A) THE TREE IS RED AT HEAD AND THE HANDOFF SAYS IT IS GREEN. I MEASURED IT MYSELF AT HEAD
#     `4105fd0` WHILE WRITING THIS PLAN. The stated regression floor — 31 files / 1372 tests exit 0,
#     432 in the outbound suite — is FALSE right now:
#
#       pnpm test                                  -> 31 files (1 FAILED) / 1372 tests, 2 FAILED
#       pnpm vitest run …/outbound-prohibition.spec.ts -> 432 tests, 2 FAILED / 430 passed
#
#     The operator's third revert, commit `4105fd0` ("revert CORE-11 a third time"), did the right
#     thing to the checkbox and two wrong things beside it:
#       1. It moved `.planning/REQUIREMENTS.md:46` to `- [ ] **CORE-11**` WITHOUT moving
#          `CORE11_BOX_EXPECTED` at `outbound-prohibition.spec.ts:9887`, which still reads
#          `"- [x] **CORE-11**"`. The box case at `:9947` is RED, and the same-commit rule that the
#          constant's OWN failure message states was broken by the commit that reverted the box.
#       2. It inserted FIVE blank lines and removed TWO in `.planning/REQUIREMENTS.md`, and the
#          insertions land INSIDE the machine-owned span (sentinels at `:162` and `:748`). The ledger
#          byte comparison at `:9919` is RED: the shipped span no longer equals
#          `deriveResidual(RESOLVER_REGISTRY)`.
#
#     Neither is a behavioural regression and NOTHING LEAKS. Both are exactly the drift this phase
#     exists to catch, and the mechanism caught them. Task 1 restores the floor and RECORDS the breach
#     rather than repairing it silently.
#
# (B) DO NOT FLIP CORE-11's CHECKBOX. IT IS `[ ]` AND THAT IS CORRECT. Verification pass 8 adjudicated
#     it down to four findings and the box may not move until they are closed and re-measured — that is
#     verification pass 9's call, not this round's. WHAT TASK 1 MOVES IS THE CONSTANT, NOT THE BOX:
#     `CORE11_BOX_EXPECTED` goes to `- [ ] **CORE-11**` so the pin AGREES with the ledger row that is
#     already `[ ]`. Bringing a pin into agreement with an adjudicated state is not a fourth flip, and
#     the SUMMARY must say so in one sentence so nobody later reads it as one.
#
# (C) DO NOT RE-OPEN THE RE-SCOPE. The operator re-scoped what CORE-11's `[x]` is allowed to mean on
#     2026-08-25, recorded with decider, date, reason and route. Verification pass 8 looked
#     specifically for a quietly-lowered bar and found none. THE `[x]` IS UNEARNED BECAUSE CRITERION
#     (3) IS UNMET, NOT BECAUSE THE BAR IS WRONG. No task in this plan revisits, re-derives, softens or
#     re-litigates it. The bar, verbatim, and the only thing this round is scoped to:
#       (1) DERIVED          — generated from `RESOLVER_REGISTRY`, not authored.   [DISCHARGED]
#       (2) DRIFT-DETECTABLE — text/code divergence turns the suite red.           [DISCHARGED]
#       (3) THE SOLE BOUND   — stated on every surface a reader touches.           [OPEN — this round]
#
# THE MUST-NOT DOES NOT MOVE. CORE-11's first sentence — no outbound network request in any enumerated
# spelling in shipped source — is byte-identical after this wave. Every finding in this round is a
# defect in a TEST-ONLY gate's description of itself or in two planning documents. NOTHING LEAKS, and
# Task 1 re-establishes that by execution rather than carrying it forward.
#
# THIS PHASE'S SIGNATURE DEFECT IS A STATED REACH EXCEEDING AN EXECUTED REACH. It has been caught eight
# times, and round 7 caught it INSIDE the discharge of the criterion written to end it. Any sentence in
# this plan, in generated text, in an assertion message or in an acceptance criterion that implies the
# gate is complete, or that claims a reach nobody has executed, is the ninth instance. Wave 29 forbade
# the equivalent claim about its quantifier guard; wave 35 forbade it about the re-scope; THIS WAVE
# FORBIDS IT ABOUT THE ANCHORING.
#
# ENVIRONMENT FACTS, MEASURED DURING PLANNING SO THE EXECUTOR DOES NOT RE-DERIVE THEM:
#   - HEAD at planning time is `4105fd0`. Cross-check ANY git-status snapshot against
#     `git rev-parse HEAD` before trusting it — wave 31 was handed a snapshot seven commits stale.
#   - NO ACTIVE GIT HOOKS: `.git/hooks` holds only samples and `core.hooksPath` is unset. Nothing runs
#     the gate set for you at a commit boundary. Run it by hand at every task boundary.
#   - COMMIT BEFORE MUTATING. `git checkout --` plus `git diff --exit-code` restores to HEAD and
#     destroys uncommitted task work; it destroyed wave 29's Task 1 once.
#   - `.planning/REQUIREMENTS.md` sentinels are at `:162` (BEGIN) and `:748` (END); the machine-owned
#     span is `163..747`.
#   - `HEADER_QUANTIFIER_EXEMPTIONS` opens at `:9007` and holds 29 entries; the fully-masked key
#     `"{q2} :: q2"` is at `:9024`; `exemptionKeyFor` is at `:9127`; `surfaceExemptionKeys` at `:9160`;
#     the three discharge checks at `:9667-9730`.
#   - `UNBOUNDED_QUANTIFIERS` is declared at `:5959` and closes at `:5969`. `q2` is `"any depth"`.
#   - The ASCII-table cell the fully-masked key was written for is at `:436`.

autonomous: true
requirements: [CORE-11]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 2
  confidence: low
# `estimate-calibration` reports `{"factor": 1, "applied": false, "sample_count": 0, "min_samples": 3}`,
# so `tokens` equals `raw_tokens` and the confidence is DERIVED from the sample count, never self-rated.
# Weigh the MEASUREMENT COUNT over the token figure: one floor restoration across two files, one
# mechanism change with 29 keys regenerated, and two separately executed mutation proofs.

must_haves:
  truths:
    - "THE FLOOR IS RESTORED AND IT WAS MEASURED BEFORE IT WAS REPAIRED, NOT ASSUMED FROM THE HANDOFF. The executor runs `pnpm test` and the outbound suite BEFORE the first edit and pastes both, reconciling against the planning-time measurement at HEAD `4105fd0`: 31 files with ONE FAILED and 1372 tests with TWO FAILED, and 432 outbound tests with TWO FAILED. Any disagreement with those numbers is recorded AS A FINDING rather than absorbed. This is the load-bearing ordering of the task: a round that begins by trusting a stated green it did not execute is the same defect as a gate that states a reach it did not execute."
    - "THE MACHINE-OWNED SPAN IS BYTE-EQUAL TO `deriveResidual(RESOLVER_REGISTRY)` AGAIN, AND IT WAS RESTORED FROM THE GENERATOR RATHER THAN REPAIRED BY EYE. Commit `4105fd0` inserted five blank lines and removed two in `.planning/REQUIREMENTS.md`, and the insertions land inside the span at `163..747`. The failing assertion at `:9919` prints the authoritative text between its own BEGIN EXPECTED and END EXPECTED markers; the span is replaced with exactly that. Hand-repairing whitespace by eye is forbidden — the generator is authoritative and the shipped text is the defect, which is what that assertion's message says in its own words."
    - "`CORE11_BOX_EXPECTED` AGREES WITH THE LEDGER ROW, AND THE SUMMARY STATES IN ONE SENTENCE THAT THIS IS NOT A FOURTH FLIP. The ledger row is `[ ]`, adjudicated correct by verification pass 8; the constant at `:9887` still pins `[x]` and is brought to `- [ ] **CORE-11**`. THE CHECKBOX DOES NOT MOVE IN THIS PLAN — `git diff` over CORE-11's checkbox is EMPTY. What moves is the pin, into agreement with an adjudicated state, and the distinction is written down so a later reader cannot take it for a flip."
    - "THE SAME-COMMIT-RULE BREACH THAT `4105fd0` CREATED IS RECORDED AS A FINDING, NOT REPAIRED SILENTLY. `CORE11_BOX_EXPECTED`'s own failure message states that the constant changes in the SAME COMMIT as the ledger row. The revert moved the row alone. Task 1 repairs the pairing after the fact and says so — because a rule broken and quietly re-satisfied is a rule nobody can tell was ever enforced, which is this phase's own standard applied to itself."
    - "THE BLOCKING CRITERION IS NAMED IN THE LEDGER, IN THE LEDGER'S OWN CORRECTION SHAPE. A correction dated 2026-08-26 and attributed to plan 01-36 and wave 36 states: that verification pass 8 found criteria (1) DERIVED and (2) DRIFT-DETECTABLE discharged with NO evidence against either; that criterion (3) THE SOLE BOUND is unmet on three legs, named CR-17, CR-18 and CR-19, with WR-48 beside them; that the box therefore reads `[ ]`; and that `[ ]` here is a MEASURED RESULT with a named blocking criterion, not a deferral. It quotes the three criteria as the 2026-08-25 decision states them and CITES that decision rather than re-deriving it."
    - "AN EXEMPTION IS ANCHORED TO THE CONSTRUCT IT WAS WRITTEN FOR, SO A BOUND CANNOT BE RELOCATED INTO IT. `exemptionKeyFor` gains a CONSTRUCT ANCHOR — a token derived by scanning BACKWARD from the occurrence to the nearest preceding declaration or fixture title — exactly as each of the three EXCLUSIONS one layer up carries a `proof` token pinning it to the construct it excludes. The anchor is masked through `maskQuantifiers` like the rest of the key, so the existing `no exemption KEY carries a declared phrasing` assertion stays green and the map still cannot generate the obligations it exists to discharge."
    - "A FULLY-MASKED ANCHOR IS FORBIDDEN OUTRIGHT, AS AN INDEPENDENT SECOND MECHANISM. A key whose entire anchor reduces to `{qN}` tokens and punctuation names no construct and is the one shape that can float anywhere in the file — `\"{q2} :: q2\"` at `:9024` is that shape and it is what a fabricated bound was driven through at 432/432 green. The rule is asserted in its own named case with its own failure message, not folded into the anchoring, because two independent mechanisms fail independently and one mechanism wearing two names fails once."
    - "THE ANCHORING'S OWN RESIDUAL IS NAMED WHEREVER THE ANCHORING IS CLAIMED, AS AN UNGUARDED LIMIT RATHER THAN A SATISFIED CHECK. A construct anchor pins an occurrence to its NEAREST PRECEDING DECLARATION OR FIXTURE TITLE and no further: TWO OCCURRENCES UNDER THE SAME CONSTRUCT REMAIN INTERCHANGEABLE, distinguished only by the positional `#N` ordinal. That is strictly narrower than 'an exemption cannot be discharged by a different occurrence' and the docblock says so in those terms. A `✓` that omits it is a stated reach exceeding an executed reach installed as the fix for exactly that defect."
    - "ALL 29 EXEMPTION ENTRIES ARE REGENERATED FROM THE GUARD'S OWN OUTPUT, NOT REWRITTEN BY HAND. Changing the key format invalidates every key. The guard's failure message already prints the found keys with their lines; the map is rebuilt from that output and every REASON string is carried across BYTE-IDENTICAL to its old entry, so the diff is keys-only and a reviewer can see that no justification was quietly re-authored while the mechanism moved."
    - "THE COUNT EQUALITY AND BOTH DIRECTIONAL CHECKS STAY GREEN AND ARE RE-MEASURED, NOT ASSUMED. `foundKeys.length === declared.length` holds at the new key format; `missing` is empty; `stale` is empty. The entry count is re-measured over the file's own bytes rather than read from this plan — the planning-time count is 29 and a disagreement with it is a finding."
    - "NOT ONE EXCLUSION, BAND, PROOF TOKEN OR PINNED COUNT MOVES IN THIS PLAN. `closingBracketAfter` and the three EXCLUSIONS belong to plan 01-37 (WR-48). `git diff` over `closingBracketAfter`, over the `EXCLUSIONS` array and over both pinned counts is EMPTY at the end of this plan. Taking an exclusion to make a guard green is how a guard becomes decoration in one commit, and wave 34 already forbade it once."
    - "TWO MUTATIONS ARE EXECUTED SEPARATELY, EACH AFTER ITS OWN WORK IS COMMITTED, EACH WATCHED GOING RED AND EACH RESTORED. The box pin moved away from the ledger row; and one exemption key edited to a fully-masked anchor. Each carries its RED test title and its FULL assertion message, each is restored with `git diff --exit-code` shown clean, and `git log --oneline -1` is pasted before each showing the real work committed first. A guard nobody watched fail in this session is a guard this session may not rely on."
    - "NOTHING LEAKS, RE-ESTABLISHED BY EXECUTION RATHER THAN CARRIED FORWARD. `pnpm test` exit 0 with the gate walking 23 files over both `SOURCE_ROOTS` at ZERO violations; `tsc --build` exit 0; `pnpm check:bundle` reporting the shipped bundle's entire import set as ONE specifier, `crypto`. CORE-11's first sentence byte-identical, shown by an EMPTY diff. Every finding this plan touches is a defect in a test-only gate's description of itself or in one planning document."
    - "NO SENTENCE ADDED ANYWHERE BY THIS PLAN STATES OR IMPLIES THAT THE ANCHORING CLOSES THE CLASS, OR THAT THE GATE IS COMPLETE. The anchoring removes one shape — an exemption discharged by an occurrence under a DIFFERENT construct. It does not remove the same-construct residual, it does not widen the guard's phrase list, and it does not reach undeclared spellings or the unguarded files. Every surface stating what the anchoring does states its reach in the same breath."
    - "EVERY PREDICTION-VS-MEASUREMENT DISCREPANCY IS RECORDED RATHER THAN ABSORBED, in the shape `01-27-SUMMARY.md` uses. This plan predicts the two RED cases at `:9919` and `:9947`, an exemption entry count of 29, and that no exclusion needs to move. If any prediction is wrong, THE DIFFERENCE IS THE FINDING and it is written down before the work continues."

  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "The machine-owned span at `163..747` restored byte-equal to `deriveResidual(RESOLVER_REGISTRY)`; a correction dated 2026-08-26 and attributed to plan 01-36 naming criterion (3) as the blocking criterion with its three legs (CR-17, CR-18, CR-19) and WR-48 beside them, stating that (1) and (2) were found discharged, and recording that `[ ]` here is a measured result; and CORE-11's checkbox byte-unchanged at `[ ]`"
      contains: "BEGIN DERIVED RESIDUAL"
    - path: "packages/backend/src/outbound-prohibition.spec.ts"
      provides: "`CORE11_BOX_EXPECTED` brought to `- [ ] **CORE-11**` so the pin agrees with the adjudicated row; `exemptionKeyFor` carrying a masked CONSTRUCT ANCHOR derived by backward scan; a named case forbidding a fully-masked anchor; all 29 exemption keys regenerated with their reasons carried across byte-identical; and the anchoring's same-construct residual stated in the guard's own docblock"
      contains: "HEADER_QUANTIFIER_EXEMPTIONS"
  key_links:
    - from: "commit `4105fd0`'s two side effects"
      to: "a green tree with the box still `[ ]`"
      via: "the span restored from the generator's own printed output and the constant brought into agreement with the adjudicated row, both recorded as a repair of a broken pairing rather than performed silently"
      pattern: "CORE11_BOX_EXPECTED"
    - from: "verification pass 8's adjudication"
      to: "`.planning/REQUIREMENTS.md`"
      via: "a dated, plan-attributed correction naming criterion (3) as the blocking criterion and its three legs, citing the 2026-08-25 decision rather than re-deriving the bar"
      pattern: "CORRECTION 2026-08-26"
    - from: "the three EXCLUSIONS' `proof` tokens, which pin an exclusion to its construct"
      to: "`HEADER_QUANTIFIER_EXEMPTIONS`, one layer down, which pinned nothing"
      via: "a masked CONSTRUCT ANCHOR carried inside the key itself, so the three flat-string discharge checks become anchor-sensitive without changing their logic"
      pattern: "exemptionKeyFor"
    - from: "a key whose entire anchor is `{qN}`"
      to: "a named, independently failing assertion"
      via: "a second mechanism asserted in its own case, so the one shape that can float anywhere in the file cannot reappear even if the anchor derivation degrades"
      pattern: "maskQuantifiers"
  prohibitions:
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "`git diff` over CORE-11's checkbox on `.planning/REQUIREMENTS.md:46` shown EMPTY at both task boundaries; only `CORE11_BOX_EXPECTED` moves, into agreement with the already-`[ ]` row"
      statement: "MUST NOT flip CORE-11's checkbox. It is `[ ]` and verification pass 8 adjudicated that correct; whether it may move is verification pass 9's call after all four findings are closed and re-measured. A plan that flips it is the fourth unearned flip. Bringing the PIN into agreement with an adjudicated row is a different act and the SUMMARY names the difference in one sentence."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: ".planning/REQUIREMENTS.md — the 2026-08-26 correction CITES the 2026-08-25 operator decision and quotes its three criteria; it derives nothing"
      statement: "MUST NOT re-open, re-derive, soften or re-litigate the 2026-08-25 re-scope. It is the operator's decision, recorded with decider, date, reason and route, and verification pass 8 tested it for a quietly-lowered bar and found none. The `[x]` is unearned because criterion (3) is unmet, not because the bar is wrong."
    - requirement_id: CORE-11
      category: safety
      status: open
      verification: gate
      resolution: "DISCLOSED, NOT CLOSED — the same-construct residual is stated in the guard's docblock, in the new case's failure message and in the SUMMARY, wherever the anchoring is claimed"
      statement: "MUST NOT state or imply that construct anchoring closes the relocation class. It pins an occurrence to its NEAREST PRECEDING declaration or fixture title, so two occurrences under the SAME construct remain interchangeable behind a positional ordinal. That is narrower than the sentence a reader will infer, and this phase's signature defect is exactly the gap between the two."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "`git diff` over `closingBracketAfter`, over the `EXCLUSIONS` array, over its three `band` pairs and `proof` tokens, and over both pinned counts shown EMPTY at the end of this plan"
      statement: "MUST NOT widen, move or re-anchor any exclusion to make a guard green. `closingBracketAfter` and exclusion three's real end belong to plan 01-37 and are a MEASUREMENT there, not a convenience here. New prose that trips the whole-file guard is REWRITTEN, never exempted and never excluded."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "Every exemption REASON string carried across byte-identical, shown by a diff that touches keys only"
      statement: "MUST NOT re-author a justification while moving a mechanism. Changing the key format invalidates 29 keys; it does not license rewriting 29 reasons. A reviewer must be able to see from the diff alone that the reasons are the ones a previous wave wrote and defended."
    - requirement_id: CORE-11
      category: security
      status: resolved
      verification: gate
      resolution: "CORE-11's first sentence byte-identical, shown by an EMPTY diff; 23 files over both `SOURCE_ROOTS` at ZERO violations; one import specifier in the shipped bundle"
      statement: "MUST NOT let a test-only anchoring change read as a change to the prohibition. What moves is a gate's description of itself. No outbound call exists in any non-spec source under either root, and this plan re-establishes that by execution rather than carrying it forward."
---

<objective>
Restore a floor that is RED at HEAD and was handed over as green, then close CR-17 by anchoring every
exemption to the construct it was written for.

Purpose: verification pass 8 adjudicated CORE-11 down to four findings and CR-17 is the blocker among
them — `HEADER_QUANTIFIER_EXEMPTIONS` holds a key that is the bare string `"{q2} :: q2"`, fully masked
and therefore anchored to no construct at all, and the three discharge checks match keys as flat
STRINGS without ever consulting `f.line`. The verifier deleted the ASCII-table cell the key was written
for and planted a fabricated hand-written bound 9,001 lines away, outside all three exclusions, and the
suite passed 432 of 432. The count equality is what forces the relocation to be a swap rather than an
addition, so every number balances. One layer up, each of the three EXCLUSIONS carries a `proof` token
pinning it to its construct; the exemption map one layer down carried no equivalent.

And before any of that: the tree does not build the story the handoff tells. I measured HEAD `4105fd0`
myself while writing this plan and it is RED. The operator's third revert moved the ledger checkbox to
`[ ]` — correctly — but left `CORE11_BOX_EXPECTED` pinning `[x]`, and it inserted five blank lines
inside the machine-owned span. Two cases fail. Nothing leaks and no behaviour regressed; both are
exactly the drift this mechanism exists to catch, and it caught them.

Output: a green tree with the box still `[ ]` and the pin agreeing with it; a dated correction naming
criterion (3) as the blocking criterion with its three legs; an exemption key that carries a masked
CONSTRUCT ANCHOR; an independent named case forbidding a fully-masked anchor; 29 keys regenerated with
their reasons carried across byte-identical; and two mutation proofs executed separately and watched
going red.

WHAT THIS PLAN DOES NOT DO, STATED HERE SO IT CANNOT BE MISREAD LATER. It does not flip CORE-11's
checkbox — the box is `[ ]`, that is correct, and whether it may move is verification pass 9's call. It
does not re-open the 2026-08-25 re-scope, which the verifier tested for a lowered bar and cleared. It
does not touch `closingBracketAfter` or any exclusion, which are plan 01-37's and are a MEASUREMENT
there. It does not touch `.planning/STATE.md`, which is plan 01-38's. And it does not claim that
anchoring closes the relocation class: two occurrences under the SAME construct remain interchangeable,
and that residual is named wherever the anchoring is claimed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md
@.planning/phases/01-skeleton-persistence-compatibility/01-35-PLAN.md
@packages/backend/src/outbound-prohibition.spec.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: The floor, end to end — measure the RED before repairing it, restore the machine-owned span from its generator, bring the pin into agreement with the adjudicated row without flipping it, name the blocking criterion in the ledger, and watch the pin fail</name>

  <precondition>
    `git rev-parse HEAD` resolves and is cross-checked against any git-status snapshot before it is
    trusted — wave 31 was handed a snapshot seven commits stale. `git status --porcelain packages/
    .planning/REQUIREMENTS.md` is clean. This repo has NO active git hooks (`.git/hooks` holds only
    samples, `core.hooksPath` unset), so nothing runs the gate set at a commit boundary and it runs by
    hand. DO NOT assert a green suite as a precondition: the suite is expected RED at HEAD `4105fd0`
    and measuring that RED is this task's first act. HALT only if the measured failure set is NEITHER
    of the two predicted cases, because then the diagnosis in this plan's header is wrong and the
    repair written against it would be a repair of something else.
  </precondition>

  <read_first>
    - `packages/backend/src/outbound-prohibition.spec.ts` `:9860-9970` — the ledger-binding block. Read
      `CORE11_BOX_EXPECTED`'s declaration at `:9887` and its FULL failure message inside the box case
      at `:9947-9965`. That message names both revert hashes `e7cc4b6` and `faca607`, explains why
      WR-34's character class stayed green through both flips and both reverts, states the three
      criteria as the current terminal condition, states that the constant changes IN THE SAME COMMIT
      as the ledger row, and states that `[ ]` is a correct outcome while an unexamined `[x]` is not.
      Every one of those clauses survives this task unchanged; only the constant's VALUE moves.
    - `packages/backend/src/outbound-prohibition.spec.ts` `:9919-9930` — the ledger byte comparison.
      Its failure message prints the authoritative generated text between its own BEGIN EXPECTED and
      END EXPECTED markers. That printed text, not your judgement, is what the span is restored to.
    - `packages/backend/src/outbound-prohibition.spec.ts` `:6017-6018` — `DERIVED_BEGIN` and
      `DERIVED_END`, and `extractDerivedBlock`'s use of them, so you know the extractor bounds the span
      by searching the file's OWN bytes for those marker lines.
    - `.planning/REQUIREMENTS.md` `:162` and `:748` — the two sentinels bounding the machine-owned span
      at `163..747`. Read enough of the span's opening to recognise its shape.
    - `.planning/REQUIREMENTS.md` `:46` in full — CORE-11's row, one line of 23,290 bytes, currently
      `- [ ] **CORE-11**`. Read it for the CORRECTION SHAPE its dated entries use — the dated,
      plan-attributed prefix — because the correction this task appends must match that shape. DO NOT
      EDIT THIS ROW'S PROSE IN THIS TASK; its residue is plan 01-38's (CR-18) and touching it here
      would put two waves' edits on one 23,290-byte line.
    - `.planning/REQUIREMENTS.md` `:108-114` — the scoped supersession marker at `:110` and the
      standing paragraph at `:112` it reaches. This is the marker SHAPE the ledger uses: it names which
      sentence it supersedes, names what supersedes it, and states that the rest of the paragraph
      stands. Read it before writing any new correction.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md` `:105-130` and
      `:284-299` — the verdict and the gaps summary. The correction this task writes states what THOSE
      say: (1) and (2) discharged with no evidence against either, (3) unmet on three legs. It cites
      them; it does not re-derive them.
    - `01-35-PLAN.md` `:96-111` and `:836-852` — the `must_haves.truths` voice and the
      "Artifacts this phase produces" table shape, so this wave's SUMMARY matches its predecessors.
    - `git show --stat 4105fd0` and `git show 4105fd0 -- .planning/REQUIREMENTS.md` — the revert
      commit. Six insertions and three deletions; five of the insertions are blank lines and two of the
      deletions are. Identify precisely which insertions fall inside `163..747`.
  </read_first>

  <files>.planning/REQUIREMENTS.md, packages/backend/src/outbound-prohibition.spec.ts</files>

  <reversibility rating="costly">The span restoration and the constant's value are both trivially reversible — one is regenerated from `deriveResidual(RESOLVER_REGISTRY)` on demand and the other is a one-line string. What is costly is the LEDGER CORRECTION: `.planning/REQUIREMENTS.md` is append-only in practice, so a correction cannot be undone by deletion, only superseded by a further dated correction with the superseded text left on the page. That permanence is the property that makes the record trustworthy, and it is why the correction states only what verification pass 8 measured and cites the 2026-08-25 decision rather than restating a bar.</reversibility>

  <action>
    MEASURE BEFORE YOU REPAIR, AND PASTE BOTH MEASUREMENTS. Run `pnpm vitest run
    packages/backend/src/outbound-prohibition.spec.ts` and `pnpm test` BEFORE the first edit. Measured
    during planning at HEAD `4105fd0`: the outbound suite reports 432 tests with TWO failed and 430
    passed; `pnpm test` reports 31 files with ONE failed and 1372 tests with TWO failed. Reconcile your
    numbers against those four and record any disagreement AS A FINDING rather than adopting either set
    silently. Paste both failing test titles and both FULL assertion messages — they are the evidence
    that the repair below addresses the actual defect and not a guessed one.

    DIAGNOSE THE TWO FAILURES AGAINST THE COMMIT THAT CAUSED THEM, AND WRITE THE DIAGNOSIS DOWN. Commit
    `4105fd0` did the right thing to the checkbox and two wrong things beside it. FIRST, it moved the
    ledger row to `[ ]` without moving `CORE11_BOX_EXPECTED` at `:9887`, which still pins `[x]`, so the
    box case at `:9947` is red and the same-commit rule stated in that constant's OWN failure message
    was broken by the very commit that reverted the box. SECOND, it inserted five blank lines and
    removed two in `.planning/REQUIREMENTS.md`, and the insertions land inside the machine-owned span
    bounded by the sentinels at `:162` and `:748`, so the ledger byte comparison at `:9919` is red.
    Confirm both by reading the commit rather than by trusting this paragraph.

    RESTORE THE SPAN FROM THE GENERATOR, NEVER BY EYE. The failing assertion at `:9919` prints the
    authoritative generated text between its own BEGIN EXPECTED and END EXPECTED markers. Replace the
    content between the sentinels with exactly that text. Do not hand-repair blank lines by comparing
    diffs; the generated form is authoritative and the shipped form is the defect, which is the rule
    that assertion's message already states. Then re-run and observe that case green.

    BRING THE PIN INTO AGREEMENT WITHOUT FLIPPING THE BOX, AND SAY WHICH ONE MOVED. Set
    `CORE11_BOX_EXPECTED` to `- [ ] **CORE-11**`. THE LEDGER CHECKBOX DOES NOT MOVE: it is already
    `[ ]`, verification pass 8 adjudicated that correct, and `git diff` over the checkbox must be EMPTY
    at the end of this task. Every other clause of that constant's failure message stays byte-identical
    — both revert hashes, the WR-34 explanation, the three criteria, the same-commit rule, and the
    sentence that `[ ]` is a correct outcome. Record in the SUMMARY, in one sentence, that this is a
    PIN brought into agreement with an adjudicated row and not a fourth flip of the box.

    RECORD THE BROKEN PAIRING RATHER THAN QUIETLY RE-SATISFYING IT. The constant's message requires the
    constant and the row to move in the same commit; `4105fd0` moved the row alone. This task repairs
    the pairing after the fact and the SUMMARY states that plainly, with the commit named. A rule
    broken and silently re-satisfied is a rule nobody can tell was ever enforced.

    APPEND THE CORRECTION, DATED AND ATTRIBUTED, IN THE LEDGER'S OWN SHAPE. Place it below wave 35's
    correction and above the generated-residual begin sentinel, dated 2026-08-26 and attributed to plan
    01-36 and wave 36. It states, in one continuous passage: that verification pass 8 found criteria
    (1) DERIVED and (2) DRIFT-DETECTABLE discharged with NO evidence against either; that criterion (3)
    THE SOLE BOUND is unmet on three legs, named CR-17, CR-18 and CR-19, with WR-48 recorded beside
    them as a warning that the enforcement errs safe while the description is wrong; that CORE-11's box
    therefore reads `[ ]`; and that this `[ ]` is a MEASURED RESULT with a named blocking criterion, in
    the terms waves 19, 28 and 32 used, not a deferral. QUOTE the three criteria as the 2026-08-25
    decision states them and CITE that decision by its date and plan; do not re-derive, re-scope or
    soften it, and add no sentence implying the gate is complete or that any reach was executed that
    was not.

    REPRODUCE NEITHER SENTINEL MARKER LINE IN THE CORRECTION'S PROSE. `extractDerivedBlock` bounds the
    span by searching this file's own bytes for those marker lines, so quoting one above the span turns
    the suite red — which is what happened when wave 28's correction was first written. Refer to the
    span descriptively, exactly as the surviving corrections do.

    DO NOT TOUCH ROW 46's PROSE, `.planning/STATE.md`, `closingBracketAfter` OR ANY EXCLUSION. Row 46's
    residue is CR-18 and belongs to plan 01-38. `.planning/STATE.md`'s three stale blocker lines are
    CR-19 and belong to plan 01-38. `closingBracketAfter` and exclusion three's real end are WR-48 and
    belong to plan 01-37, where they are a MEASUREMENT. Assert by diff at the end of this task that all
    four are byte-unchanged.

    RUN WAVE 33's WHOLE-FILE QUANTIFIER GUARD AFTER EVERY EDIT, NOT AT THE END. The correction prose
    and any new sentence sit inside its reach. If a new sentence trips it, REWRITE THE SENTENCE — do
    not widen an exclusion and do not add an exemption entry. Taking either is how a guard becomes
    decoration in one commit.

    COMMIT BEFORE YOU MUTATE, THEN RUN ONE MUTATION. Wave 29 destroyed its own Task 1 with
    `git checkout --` against uncommitted work. Commit the restoration, the constant and the
    correction; paste `git log --oneline -1`; then move CORE-11's ledger row away from what
    `CORE11_BOX_EXPECTED` now pins, run, watch the box case go RED, paste the failing title and the
    FULL assertion message showing both `e7cc4b6` and `faca607`, restore, and cross-check with
    `git diff --exit-code`. That mutation is this task's end-to-end proof: a ledger row, a constant that
    pins it, a machine-owned span between them, and a suite that can see all three.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY AND PASTE EVERY RESULT. `pnpm test`, `pnpm
    typecheck`, `pnpm lint`, `pnpm knip`, `pnpm build:backend && pnpm check:bundle`, and the real-tree
    run over both `SOURCE_ROOTS`. State the must-NOT plainly with its evidence rather than leaving it
    inferred: 23 files and ZERO violations, one import specifier in the shipped bundle, CORE-11's first
    sentence byte-identical by diff. Every finding this plan touches is a defect in a test-only gate's
    description of itself or in one planning document.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/DefMiner && pnpm vitest run packages/backend/src/outbound-prohibition.spec.ts 2>&1 | tail -6 && pnpm test 2>&1 | tail -8 && grep -cE '^- \[[ x]\] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - `git rev-parse HEAD` pasted before the first edit, beside any git-status snapshot used, and
      `git status --porcelain packages/ .planning/REQUIREMENTS.md` shown clean.
    - Executed `pnpm vitest run packages/backend/src/outbound-prohibition.spec.ts` BEFORE the first
      edit, output pasted. Planning-time measurement: 432 tests, 2 failed, 430 passed. Executed
      `pnpm test` BEFORE the first edit, output pasted. Planning-time measurement: 31 files with 1
      failed, 1372 tests with 2 failed. Any disagreement with those four numbers is written up as a
      finding rather than absorbed.
    - Both pre-repair failing test titles and both FULL assertion messages pasted. One is the ledger
      byte comparison; one is the box case, whose message shows `PINNED : - [x] **CORE-11**` against
      `SHIPPED : - [ ] **CORE-11**`.
    - `git show --stat 4105fd0` pasted, and the specific inserted blank lines falling inside
      `163..747` identified by line number.
    - The machine-owned span replaced with the text the `:9919` assertion printed between its BEGIN
      EXPECTED and END EXPECTED markers, and that case re-run and observed GREEN with its output
      pasted. The SUMMARY states that the span was restored from the generator and not hand-repaired.
    - `CORE11_BOX_EXPECTED` shown by `git diff` to have changed from `"- [x] **CORE-11**"` to
      `"- [ ] **CORE-11**"` and NOTHING ELSE in that declaration or in the case's failure message: the
      before and after of the full message are both pasted and both revert hashes, the WR-34
      explanation, the three criteria, the same-commit rule and the `[ ]`-is-a-correct-outcome sentence
      are all present in the after form.
    - `git diff .planning/REQUIREMENTS.md` shows CORE-11's checkbox BYTE-UNCHANGED — the row still
      begins `- [ ] **CORE-11**` and no character of that checkbox moved in this task. Executed
      `grep -cE '^- \[[ x]\] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md`, observed exactly 1. That gate
      is STATE-AGNOSTIC on purpose, and the SUMMARY says so in one sentence naming WR-34 and stating
      that the SUITE's pin stays state-sensitive through `CORE11_BOX_EXPECTED`.
    - The SUMMARY contains one sentence, quoted verbatim, stating that what moved is the PIN into
      agreement with an adjudicated row and that CORE-11's checkbox did not move, and one sentence
      naming commit `4105fd0` as having broken the same-commit rule that the constant's own message
      states.
    - The 2026-08-26 correction is present below wave 35's correction and above the generated-residual
      begin sentinel, and the SUMMARY quotes VERBATIM the sentences in it that: state criteria (1) and
      (2) were found discharged with no evidence against either; name criterion (3) as the blocking
      criterion with its three legs CR-17, CR-18 and CR-19 and WR-48 beside them; and state that `[ ]`
      here is a measured result with a named blocking criterion rather than a deferral.
    - The correction CITES the 2026-08-25 decision by date and plan and quotes its three criteria; no
      sentence in it re-derives, re-scopes or softens the bar. The SUMMARY records a one-line verdict
      confirming that re-read.
    - Neither generated-residual sentinel marker line is reproduced anywhere in the new correction
      prose, confirmed by running the suite and observing the extraction cases green rather than by
      inspection.
    - Wave 33's whole-file quantifier guard executed after EVERY edit in this task and green each time,
      with `git diff` over `closingBracketAfter`, over the `EXCLUSIONS` array and over both pinned
      counts shown EMPTY. If any new sentence tripped the guard, the before and after of the REWRITE
      are both pasted; no exclusion was widened and no exemption entry was added to satisfy it.
    - `git diff` over `.planning/STATE.md` shown EMPTY — that file is plan 01-38's.
    - MUTATION executed after the work was committed: `git log --oneline -1` pasted first, then
      CORE-11's ledger row moved away from what the constant pins, run, observed RED, the failing title
      and the FULL assertion message pasted showing both `e7cc4b6` and `faca607`, restored, and
      `git diff --exit-code` shown clean.
    - Executed `pnpm test` at the end of the task — exit 0, file and test counts pasted, no fewer than
      31 files and 1372 tests.
    - Executed `pnpm typecheck`, `pnpm lint`, `pnpm knip` — all exit 0, output pasted.
    - Executed `pnpm build:backend && pnpm check:bundle` — observed exactly one import specifier,
      `crypto`, output pasted.
    - Executed the real-tree run over both `SOURCE_ROOTS` — 23 files, 0 violations — output pasted.
    - `git diff` over CORE-11's first sentence shown EMPTY, output pasted.
    - Any prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses; if there
      were none, that is stated explicitly rather than left silent.
  </acceptance_criteria>

  <done>
    The suite is green again with CORE-11's checkbox still `[ ]`; the machine-owned span is byte-equal
    to its generator; the pin agrees with the adjudicated row and the SUMMARY says which one moved and
    which did not; the blocking criterion is named in the ledger in the ledger's own correction shape;
    and the pin was watched failing in this session.
  </done>
</task>

<task type="auto">
  <name>Task 2: CR-17 — the exemption key carries the construct it was written for, and a fully-masked anchor is forbidden by a second, independent assertion</name>

  <read_first>
    - `packages/backend/src/outbound-prohibition.spec.ts` `:8945-9006` — the guard's docblock, in
      particular its four numbered LIMITS. Limit (1) says the guard is a PHRASE LIST; limit (2) names
      the normalization convention; limit (3) says it reaches bytes and not meaning; limit (4) says
      exclusion three is coarser than its name and is NARROWED by the clause-count equality. The
      anchoring's own residual is added to this list in the same voice, as a fifth limit.
    - `packages/backend/src/outbound-prohibition.spec.ts` `:9007-9106` — `HEADER_QUANTIFIER_EXEMPTIONS`
      in full, all 29 entries with their section comments (`--- HEADER (1..956) ---`,
      `--- CODE ABOVE THE REGISTRY ---`, `--- BELOW THE REGISTRY ---`). The fully-masked key
      `"{q2} :: q2"` is at `:9024` and its reason names an ASCII-table cell. Every REASON string in
      this map is carried across byte-identical by this task.
    - `packages/backend/src/outbound-prohibition.spec.ts` `:9107-9180` — `normalizeGateLine`,
      `joinGateLines`, `maskQuantifiers`, `exemptionKeyFor` at `:9127`, `quantifierOccurrences`, and
      `surfaceExemptionKeys` at `:9160`. Read `surfaceExemptionKeys` closely: it ALREADY computes
      `line` and `endLine` per occurrence and already ordinal-suffixes duplicate keys with `#N`. The
      information the anchoring needs is already being computed and thrown away at the key boundary.
    - `packages/backend/src/outbound-prohibition.spec.ts` `:9536-9600` — `lineOf`,
      `closingBracketAfter` at `:9544`, and the `EXCLUSIONS` array. Read the three exclusions' `proof`
      tokens and `band` pairs. THIS IS THE PATTERN THE ANCHORING COPIES: each exclusion carries a token
      that only its construct contains, so an exclusion that silently relocates onto a different
      construct fails loudly. DO NOT EDIT ANY OF IT — `closingBracketAfter` and exclusion three's real
      end are plan 01-37's, where they are a measurement.
    - `packages/backend/src/outbound-prohibition.spec.ts` `:9601-9665` — the non-vacuity case and the
      exclusion-three clause-count equality. Read them for the ASSERTION VOICE this file uses: a
      failure message that names the defect, names the direction it errs in, and tells the next author
      what to do about it.
    - `packages/backend/src/outbound-prohibition.spec.ts` `:9666-9732` — the three discharge checks.
      `found.filter((f) => !declaredSet.has(f.key))` for missing, `declared.filter((k) =>
      !foundKeys.includes(k))` for stale, `foundKeys.length === declared.length` for the count, plus
      the no-verbatim-phrasing check over keys and the minimum-reason-length check. Not one of the
      three consults `f.line`. Read the whole case: the fix must leave the count equality intact and
      must not weaken the no-verbatim-phrasing check.
    - `packages/backend/src/outbound-prohibition.spec.ts` `:428-442` — the ASCII-table region
      containing the cell at `:436` that normalizes to exactly the declared phrasing at index 2. This
      is the construct `"{q2} :: q2"` was written for and the construct its anchored replacement must
      name.
    - `packages/backend/src/outbound-prohibition.spec.ts` `:5959-5969` — the `UNBOUNDED_QUANTIFIERS`
      declaration and its ten-line body, so you know which index each phrasing carries.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md` `:136-160` — the
      CR-17 section, including the three-row table showing which check consults the line and the
      paragraph explaining why the count equality forces a swap rather than an addition.
  </read_first>

  <files>packages/backend/src/outbound-prohibition.spec.ts</files>

  <reversibility rating="reversible">A key-format change inside a test-only guard, with every reason string preserved and every exclusion untouched. If the anchor derivation proves wrong, the previous key format is one revert away and the map regenerates from the guard's own failure output in either direction. No shipped code, no behaviour and no requirement text is touched.</reversibility>

  <action>
    BUILD THE CONSTRUCT ANCHOR AS THE THREE EXCLUSIONS' `proof` TOKEN, ONE LAYER DOWN. Add a helper
    beside `exemptionKeyFor` that takes the gate file's lines and an occurrence's 1-based line number
    and scans BACKWARD for the nearest preceding CONSTRUCT HEADER — a top-level or nested declaration
    (`export const`, `const`, `function`, `class`), a `describe(` or `it(` title, or the opening line
    of a fenced ASCII table or docblock, whichever comes first. Return a short, stable token derived
    from that line. Choose the recognisers by reading the constructs the 29 existing occurrences
    actually sit under rather than by guessing; if an occurrence resolves to NO preceding construct at
    all, that is a finding and the helper must say so in a named way rather than returning an empty
    string, because an empty anchor is the very shape this task exists to forbid.

    MASK THE ANCHOR THROUGH `maskQuantifiers`, AND THIS IS NOT OPTIONAL. A construct title may itself
    contain a declared phrasing — several `it(` titles in this file do. An unmasked construct token
    would put a phrasing verbatim into a key, the scan would find it inside the map, and the map would
    generate the obligations it exists to discharge. The existing check at `:9681` asserts exactly that
    and must stay green afterwards. Truncate the anchor the way the line anchor is truncated so keys
    stay readable.

    FOLD THE ANCHOR INTO THE KEY RATHER THAN BESIDE IT. Extend `exemptionKeyFor` so the key is the
    masked CONSTRUCT anchor, then the masked normalized-line anchor, then `:: qN`, with a separator
    that cannot occur in either part. Folding it into the key is the cheaper of the verifier's two
    options and it is cheaper for a structural reason worth stating: the three discharge checks match
    keys as flat strings, so a key that carries its construct makes all three anchor-sensitive with NO
    change to their logic and with the count equality still balancing exactly one entry per occurrence.
    Do not add a fourth check that consults `f.line` separately; that would be a second mechanism for
    the same fact.

    FORBID A FULLY-MASKED ANCHOR IN ITS OWN NAMED CASE, AS AN INDEPENDENT SECOND MECHANISM. Assert that
    no declared key's anchor portion reduces to nothing once its `{qN}` tokens, whitespace and
    punctuation are removed. This is the verifier's option (2) and it is taken IN ADDITION to option
    (1), not instead of it: a key whose entire anchor is `{qN}` names no construct and is the one shape
    that can float anywhere in the file. Two independent mechanisms fail independently; one mechanism
    wearing two names fails once. Give the case a failure message in this file's voice that names the
    offending key, says why a fully-masked anchor names nothing, and tells the next author to rebuild
    it with the key builder.

    REGENERATE ALL 29 KEYS FROM THE GUARD'S OWN OUTPUT AND CARRY EVERY REASON ACROSS BYTE-IDENTICAL.
    Changing the key format invalidates every key at once: `missing` will list 29 and `stale` will list
    29. Run the guard, take the found keys with their lines from the failure message, and rebuild the
    map in the same order with the same three section comments. Each new key pairs with the reason its
    old key carried, character for character — the diff is keys-only. A reviewer must be able to see
    from the diff alone that no justification was quietly re-authored while the mechanism moved. Where
    the pairing is ambiguous because two old keys were identical up to their ordinal, resolve it by the
    line the guard printed and say in the SUMMARY which entries needed that.

    RECONCILE THE COUNT RATHER THAN ASSUMING IT. Measured during planning: 29 entries in the map.
    Re-measure over the file's own bytes and reconcile; a disagreement is a finding and is written down
    before the map is rebuilt.

    ADD THE ANCHORING'S RESIDUAL TO THE DOCBLOCK'S NUMBERED LIMITS, IN THE SAME VOICE AS THE FOUR
    ALREADY THERE. The anchor pins an occurrence to its NEAREST PRECEDING declaration or fixture title
    and no further, so TWO OCCURRENCES UNDER THE SAME CONSTRUCT REMAIN INTERCHANGEABLE behind the
    positional `#N` ordinal. State that as a limit, not as a satisfied check. A reader will otherwise
    infer the sentence "an exemption cannot be discharged by a different occurrence", which is strictly
    wider than what the mechanism executes, and installing that inference as the fix for a stated reach
    exceeding an executed reach would be the ninth instance of this phase's signature defect.

    TOUCH NO EXCLUSION, NO BAND, NO PROOF TOKEN AND NO PINNED COUNT. `closingBracketAfter` and
    exclusion three's real end are WR-48 and belong to plan 01-37, where narrowing exclusion three is a
    MEASUREMENT whose result is whatever it is. If the new prose or the new case trips wave 33's guard,
    REWRITE THE PROSE. Do not widen an exclusion and do not mint an exemption for your own new text.

    KEEP THE NEW CASE'S OWN BYTES OFF THE GUARDED SURFACE. Any synthetic string this task writes must
    build a declared phrasing BY REFERENCE — from `UNBOUNDED_QUANTIFIERS` by index — never as a
    literal, so the file's own bytes never carry the phrasing verbatim and the case does not raise the
    obligation it is testing. This is the same reasoning the key masking already rests on and it is the
    single most likely way this task turns the suite red for a reason that is not a finding.

    COMMIT BEFORE YOU MUTATE, THEN RUN ONE MUTATION. Paste `git log --oneline -1` first. Then edit ONE
    exemption key so its anchor portion is fully masked, run, watch the new named case go RED, paste
    the failing title and the FULL assertion message, restore, and cross-check with
    `git diff --exit-code`. The relocation mutation — the cell deleted and a bound planted 9,001 lines
    away — is plan 01-37 Task 1's and is deliberately NOT run here: it is a fixture in its own right,
    not a smoke test bolted to a mechanism change.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY. No git hooks run it for you. The outbound suite's
    test count rises above 432 by the number of cases this task adds; state the new count and state
    that the floor is `no fewer than`, never an equality.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/DefMiner && pnpm vitest run packages/backend/src/outbound-prohibition.spec.ts 2>&1 | tail -6 && pnpm test 2>&1 | tail -8 && pnpm typecheck && pnpm lint</automated>
  </verify>

  <acceptance_criteria>
    - The exemption entry count re-measured over the file's own bytes and pasted; the planning-time
      count is 29 and any disagreement is written up as a finding before the map is rebuilt.
    - The construct-anchor helper exists beside `exemptionKeyFor`, its recogniser set is listed in the
      SUMMARY, and the SUMMARY pastes a table of all 29 occurrences with the construct each one
      resolved to. Any occurrence resolving to NO preceding construct is named and disposed of, not
      silently defaulted.
    - `exemptionKeyFor` produces a key of the form masked-construct-anchor, separator,
      masked-line-anchor, `:: qN`. Three example keys pasted BEFORE and AFTER, including the
      replacement for the fully-masked `"{q2} :: q2"` at `:9024`, whose AFTER form names the ASCII
      table it was written for.
    - The existing no-verbatim-phrasing check at `:9681` executed and observed GREEN, output pasted —
      the construct anchor is masked through `maskQuantifiers` and no key carries a declared phrasing.
    - The three discharge checks executed and all green: `missing` empty, `stale` empty, and
      `foundKeys.length === declared.length` holding at the new key format. The final counts pasted.
    - The new fully-masked-anchor case exists as its OWN named `it(...)`, separate from the discharge
      case, and its full failure message is pasted. The SUMMARY states in one sentence that this is the
      verifier's option (2) taken IN ADDITION to option (1), not instead of it.
    - `git diff` over the exemption map shows keys changed and REASONS BYTE-IDENTICAL. The SUMMARY
      states the number of reason strings that changed and that number is 0; if any entry needed its
      pairing resolved by printed line number, those entries are named.
    - A fifth numbered LIMIT is present in the guard's docblock stating that the anchor reaches the
      nearest preceding construct and no further, and that two occurrences under the SAME construct
      remain interchangeable behind the positional ordinal. That limit is quoted verbatim in the
      SUMMARY.
    - No sentence added by this task states or implies that anchoring closes the relocation class, that
      the guard's reach widened, or that the gate is complete. Every added sentence is re-read against
      that prohibition and the review is recorded as a one-line verdict. If any sentence had to be
      rewritten to satisfy it, the before and after are both pasted — that rewrite is itself a finding.
    - Every synthetic declared phrasing in new code is built BY REFERENCE from `UNBOUNDED_QUANTIFIERS`
      by index, never written as a literal. The SUMMARY states this in one sentence and the whole-file
      guard's green run is the evidence.
    - `git diff` over `closingBracketAfter`, over the `EXCLUSIONS` array with its three `band` pairs
      and `proof` tokens, and over both pinned counts shown EMPTY, output pasted.
    - `git diff` over `.planning/REQUIREMENTS.md` and `.planning/STATE.md` shown EMPTY — this task
      touches one file.
    - MUTATION executed after the work was committed: `git log --oneline -1` pasted first, then ONE
      exemption key edited to a fully-masked anchor, run, observed RED, the failing title and the FULL
      assertion message pasted, restored, `git diff --exit-code` shown clean.
    - Executed `pnpm test` — exit 0, file and test counts pasted, no fewer than 31 files and 1372
      tests. The outbound suite's new count stated, no fewer than 432.
    - Executed `pnpm typecheck`, `pnpm lint`, `pnpm knip` — all exit 0, output pasted.
    - Executed `pnpm build:backend && pnpm check:bundle` — exactly one import specifier, `crypto`.
    - Executed the real-tree run over both `SOURCE_ROOTS` — 23 files, 0 violations.
    - Any prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses, or an
      explicit statement that there were none.
  </acceptance_criteria>

  <done>
    Every exemption key names the construct its occurrence sits under, masked the way the rest of the
    key is masked; a fully-masked anchor fails its own named case; all 29 reasons survive byte-identical
    while their keys move; the anchoring's same-construct residual is stated as a limit wherever the
    anchoring is claimed; and the new rule was watched failing in this session.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a handed-over green to a MEASURED green | The handoff states 31 files / 1372 tests exit 0 and 432 in the outbound suite. HEAD is RED on two cases. A round that begins by trusting a stated green it did not execute commits, at its own first step, the defect it exists to correct. |
| the BOX to the PIN that watches the box | The ledger checkbox is `[ ]` and adjudicated correct; `CORE11_BOX_EXPECTED` still pins `[x]`. Moving the pin into agreement is a repair; moving the box would be a fourth unearned flip. The two acts look identical in a diff summary and are separated only by which file's line moved. |
| a rule BROKEN AND RECORDED to a rule broken and quietly re-satisfied | `4105fd0` moved the row without the constant, breaking the same-commit rule that constant's own message states. Repairing the pairing while saying nothing would leave no trace that the rule was ever enforced. |
| an exclusion pinned by a `proof` token to an exemption pinned by nothing | Each of the three EXCLUSIONS carries a token only its construct contains. The exemption map one layer down carried no equivalent, and a fabricated bound was driven through that gap at 432/432 green. |
| an anchor that names a CONSTRUCT to an anchor that names NOTHING | `"{q2} :: q2"` is fully masked: its whole anchor is a token, so any line whose entire normalized content is the declared phrasing discharges it. That is the one key shape that can float anywhere in the file. |
| the anchoring's CLAIMED reach to its EXECUTED reach | A construct anchor pins an occurrence to its nearest preceding declaration. Two occurrences under the SAME construct stay interchangeable. The sentence a reader infers is wider than the mechanism, and installing that inference as the fix for exactly this defect is how the defect reaches its ninth instance. |
| a KEY format change to a REASON rewrite | Invalidating 29 keys does not license re-authoring 29 justifications. The diff must be keys-only or a reviewer cannot tell which claims a previous wave defended. |
| a guard satisfied by REWRITING prose to one satisfied by WIDENING an exclusion | Wave 34 forbade the second once already. Exclusion three's real end is a MEASUREMENT owned by plan 01-37 and must not be taken here as a convenience. |
| a synthetic fixture's own bytes to the surface it tests | This file's guard scans its own bytes. A test that writes a declared phrasing as a literal raises the obligation it is testing. Phrasings are built by reference from the list, never spelled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-213 | Repudiation | a round built on a stated green nobody executed | high | mitigate | Both suites run BEFORE the first edit with output pasted and reconciled against the planning-time measurement (432 with 2 failed; 31 files with 1 failed and 1372 tests with 2 failed). Disagreement is a finding, not an absorbed number. |
| T-01-214 | Tampering | CORE-11's checkbox moved a fourth time | critical | mitigate | Prohibited in frontmatter, in the objective and in an acceptance criterion per task. `git diff` over the checkbox asserted EMPTY at both task boundaries; the task-level verify is state-agnostic so an honest `[ ]` cannot fail its own gate. Only `CORE11_BOX_EXPECTED` moves, and the SUMMARY states which one moved in one sentence. |
| T-01-215 | Repudiation | the same-commit-rule breach at `4105fd0` repaired silently | high | mitigate | The breach is named with its commit hash in the SUMMARY and in the ledger correction, and the repair is described as a repair rather than performed as routine housekeeping. |
| T-01-216 | Tampering | the machine-owned span hand-repaired by eye | high | mitigate | The span is replaced with exactly the text the `:9919` assertion prints between its own BEGIN EXPECTED and END EXPECTED markers, and that case is re-run green with its output pasted. |
| T-01-217 | Tampering | an exemption discharged by an occurrence under a different construct | critical | mitigate | The key carries a masked CONSTRUCT ANCHOR derived by backward scan, so all three flat-string discharge checks become anchor-sensitive with no change to their logic and a relocated occurrence produces a different key — non-empty `missing` AND non-empty `stale`. |
| T-01-218 | Spoofing | a key whose entire anchor is a mask token | high | mitigate | An independent named case asserts that no declared key's anchor reduces to nothing once its tokens, whitespace and punctuation are removed. Taken IN ADDITION to the anchoring, so the two mechanisms fail independently. |
| T-01-219 | Spoofing | any sentence implying the anchoring closes the relocation class | high | mitigate | A fifth numbered LIMIT in the guard's docblock states that the anchor reaches the nearest preceding construct and no further and that two occurrences under the same construct remain interchangeable. Every added sentence is re-read against the prohibition and the review recorded as a one-line verdict. |
| T-01-220 | Tampering | a reason string re-authored while its key moved | medium | mitigate | Every reason carried across byte-identical; the diff is keys-only; the count of changed reason strings is stated and is 0; ambiguous pairings resolved by the printed line number and named. |
| T-01-221 | Tampering | a new synthetic fixture raising the obligation it tests | medium | mitigate | Declared phrasings in new code are built BY REFERENCE from `UNBOUNDED_QUANTIFIERS` by index, never spelled as literals, so the file's own bytes never carry them verbatim. The whole-file guard's green run is the evidence. |
| T-01-222 | Tampering | an exclusion widened to absorb this wave's own prose | high | mitigate | `git diff` over `closingBracketAfter`, the `EXCLUSIONS` array, its bands, its proof tokens and both pinned counts asserted EMPTY. New prose that trips the guard is REWRITTEN and the before/after pasted. Carried forward from T-01-198. |
| T-01-187 | Tampering | mutating uncommitted task work | high | mitigate | Every task commits its real work before planting any mutation and cross-checks with `git diff --exit-code`. Wave 29 destroyed its own Task 1 exactly this way. Carried forward unchanged. |
| T-01-188 | Tampering | a gate set nobody ran | medium | mitigate | No active git hooks in this repo — `.git/hooks` holds only samples and `core.hooksPath` is unset — so the full set runs by hand at every task boundary with its output pasted. Carried forward unchanged. |
| T-01-212 | Information Disclosure | the two reader surfaces no mechanism reaches | medium | accept | `.planning/STATE.md` and `.planning/WINDOWS.md` are governed by the pointer-not-a-bound prohibition with no mechanical check. Untouched by this plan and owned by plan 01-38. Carried forward from T-01-144 unchanged. |
| T-01-51 | Information Disclosure | the accepted residual of the outbound walk | medium | mitigate | Unchanged by this plan — no registry row moves — and byte-pinned into the ledger by the two comparisons, both re-run green at the end of Task 1. |
| T-01-50 | Information Disclosure | `.spec.ts` files, which the walk skips | low | accept | Unchanged and carried forward, bounded by `pnpm check:bundle`, which specs never enter. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | NOT APPLICABLE: this plan installs no package. `pnpm install` is not run and no manifest appears in `files_modified`. RESEARCH.md's Package Legitimacy Audit is present and unchanged; no `[ASSUMED]` or `[SUS]` package is introduced, so no blocking human checkpoint is required. |
</threat_model>

<verification>
1. `git rev-parse HEAD` pasted and cross-checked against any status snapshot; working tree clean over
   `packages/` and `.planning/REQUIREMENTS.md`.
2. Both suites executed BEFORE the first edit and pasted, reconciled against 432/2-failed and
   31-files-1-failed/1372-tests-2-failed; any disagreement recorded as a finding.
3. Both pre-repair failing titles and both FULL assertion messages pasted.
4. `git show --stat 4105fd0` pasted and the inserted blank lines inside `163..747` identified by number.
5. The machine-owned span restored from the `:9919` assertion's own printed text and that case re-run
   green.
6. `CORE11_BOX_EXPECTED` moved from `[x]` to `[ ]` and NOTHING else in its declaration or message; the
   message quoted before and after with both revert hashes, the WR-34 explanation, the three criteria,
   the same-commit rule and the `[ ]`-is-correct sentence all surviving.
7. CORE-11's checkbox BYTE-UNCHANGED, shown by diff; `grep -cE '^- \[[ x]\] \*\*CORE-11\*\*'` observed
   exactly 1, state-agnostic on purpose with the WR-34 distinction stated.
8. One sentence in the SUMMARY stating the PIN moved and the BOX did not, and one naming `4105fd0` as
   having broken the same-commit rule.
9. The 2026-08-26 correction present, dated and plan-attributed, naming criterion (3) as blocking with
   its three legs and WR-48 beside them, citing the 2026-08-25 decision and re-deriving nothing.
10. Neither sentinel marker line reproduced in the correction prose, confirmed by the extraction cases
    running green.
11. The exemption entry count re-measured over the file's bytes and reconciled against 29.
12. The construct-anchor helper present, its recogniser set listed, and all 29 occurrences tabulated
    with the construct each resolved to; any unresolved occurrence named.
13. Keys in the new form with three before/after examples pasted, including the replacement for
    `"{q2} :: q2"`.
14. The no-verbatim-phrasing check green; `missing` empty; `stale` empty; the count equality holding.
15. The fully-masked-anchor rule present as its OWN named case with its full failure message pasted.
16. Exemption REASONS byte-identical; the count of changed reason strings stated and 0.
17. A fifth numbered LIMIT present in the docblock stating the same-construct residual, quoted verbatim.
18. Every added sentence re-read against the no-overclaim prohibition, recorded as a one-line verdict;
    any rewrite's before and after pasted.
19. Declared phrasings in new code built by reference from `UNBOUNDED_QUANTIFIERS`, never spelled.
20. `git diff` over `closingBracketAfter`, the `EXCLUSIONS` array, its bands and proof tokens, and both
    pinned counts — EMPTY.
21. `git diff` over `.planning/STATE.md` — EMPTY.
22. Two mutations executed separately, each after its own work was committed, each with
    `git log --oneline -1` before it, its RED title, its FULL assertion message, its restore and a clean
    `git diff --exit-code`.
23. `pnpm test` — exit 0, no fewer than 31 files and 1372 tests; the outbound suite no fewer than 432.
24. `pnpm typecheck`, `pnpm lint`, `pnpm knip` — all exit 0.
25. `pnpm build:backend && pnpm check:bundle` — exactly one import specifier, `crypto`.
26. Real-tree run over both `SOURCE_ROOTS` — 23 files, 0 violations.
27. `git diff` over CORE-11's first sentence — EMPTY.
28. Every prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses, or an
    explicit statement that there were none.
</verification>

<success_criteria>
- The tree is green again and it was measured RED first, by execution, before anything was repaired.
- CORE-11's checkbox is still `[ ]` and no character of it moved; the pin agrees with it; and a reader
  can tell from one sentence which of the two moved and why that is not a fourth flip.
- The breach commit `4105fd0` created is on the record with its hash, not quietly re-satisfied.
- A reader opening `.planning/REQUIREMENTS.md` cold is told which criterion blocks CORE-11, on which
  three legs, and that `[ ]` is a measured result — from the ledger alone, without reading a plan.
- An exemption cannot be discharged by an occurrence sitting under a different construct, and that is
  proved by the mechanism's own key format rather than asserted about it.
- A fully-masked anchor fails its own named case, independently of the anchoring.
- The anchoring's residual — two occurrences under the same construct remain interchangeable — is
  stated as a limit everywhere the anchoring is claimed, and no sentence anywhere claims the class is
  closed.
- All 29 reasons survive byte-identical while their keys move, so a reviewer can see the mechanism
  changed and the justifications did not.
- Not one exclusion, band, proof token or pinned count moved; exclusion three's real end is still an
  open measurement owned by plan 01-37.
- The green baseline is restored and recorded: no fewer than 31 files / 1372 tests, typecheck / lint /
  knip clean, bundle at exactly one specifier, real tree at 23 files / 0 violations, and nothing leaks.
</success_criteria>

<artifacts_this_phase_produces>
| Artifact | Kind | Purpose |
|----------|------|---------|
| the pre-repair RED measurement of both suites | new evidence | The handoff's stated floor executed rather than trusted, with the two failing titles and full assertion messages pasted — the round's first act is a measurement that contradicts its own brief |
| the machine-owned span restored from `deriveResidual(RESOLVER_REGISTRY)` | corrected artifact | Commit `4105fd0`'s five inserted blank lines removed from inside `163..747`, restored from the generator's own printed output rather than repaired by eye |
| `CORE11_BOX_EXPECTED` at `- [ ] **CORE-11**` | corrected pin | The pin brought into agreement with an adjudicated `[ ]` row — a repair of the pairing `4105fd0` broke, explicitly not a fourth flip of the box |
| the 2026-08-26 CORRECTION in `.planning/REQUIREMENTS.md` | corrected disclosure | Criterion (3) named as the blocking criterion with its three legs CR-17, CR-18, CR-19 and WR-48 beside them; criteria (1) and (2) recorded as discharged; `[ ]` recorded as a measured result |
| the construct-anchor helper beside `exemptionKeyFor` | new mechanism | Derives a masked token for the nearest preceding declaration or fixture title, giving an exemption the `proof`-token pinning the three exclusions one layer up already carry |
| the anchored key format in `exemptionKeyFor` | new mechanism | Masked construct anchor, separator, masked line anchor, `:: qN` — making all three flat-string discharge checks anchor-sensitive without changing their logic |
| the fully-masked-anchor prohibition case | new mechanism | An independent second assertion that no declared key's anchor reduces to nothing — the verifier's option (2), taken in addition to option (1) |
| the 29 regenerated exemption keys | corrected artifact | Rebuilt from the guard's own failure output with every reason string carried across byte-identical, so the diff is keys-only |
| the fifth numbered LIMIT in the guard's docblock | corrected disclosure | The anchoring's own residual — two occurrences under the same construct remain interchangeable — stated as an unguarded limit rather than a satisfied check |
| the 29-occurrence construct table | new evidence | Every occurrence with the construct it resolved to, so the anchor derivation is reviewable rather than trusted |
| the box-pin mutation proof | new evidence | The ledger row moved away from the constant, watched RED, the full assertion message pasted naming both `e7cc4b6` and `faca607`, restored |
| the fully-masked-anchor mutation proof | new evidence | One key edited to a fully-masked anchor, the new named case watched RED, restored — the rule watched failing in the session that added it |
| `01-36-SUMMARY.md` | new evidence | Both pre-repair measurements, the diagnosis against `4105fd0`, the correction quoted verbatim, the construct table, the keys-only diff verdict, the two mutation proofs, and the full gate set |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-skeleton-persistence-compatibility/01-36-SUMMARY.md` when done.

It must contain, as pasted terminal output rather than as prose: `git rev-parse HEAD`; the PRE-REPAIR
runs of `pnpm vitest run packages/backend/src/outbound-prohibition.spec.ts` and `pnpm test` with both
failing titles and both FULL assertion messages; `git show --stat 4105fd0` with the inserted blank
lines inside `163..747` identified by number; the `:9919` case re-run green after the span restoration;
`CORE11_BOX_EXPECTED`'s declaration and full failure message quoted BEFORE and AFTER; the EMPTY diff
over CORE-11's checkbox and over CORE-11's first sentence; the exemption entry count re-measured; the
table of all 29 occurrences with the construct each resolved to; three key examples before and after
including the replacement for the fully-masked one; the three discharge checks green with their final
counts; the new fully-masked-anchor case's full failure message; the keys-only diff verdict with the
number of changed reason strings stated; the fifth numbered LIMIT quoted verbatim; the EMPTY diffs over
`closingBracketAfter`, the `EXCLUSIONS` array with its bands and proof tokens, both pinned counts, and
`.planning/STATE.md`; the TWO mutation proofs as ONE TABLE with the run order stated, each carrying its
RED title, its full assertion message, its restore and its re-run green, with `git diff --exit-code`
shown clean between them and `git log --oneline -1` before each; the real-tree run at 23 files and 0
violations; and the final `pnpm test`, `typecheck`, `lint`, `knip` and `check:bundle` results.

It must quote VERBATIM, from the 2026-08-26 ledger correction, the sentences stating that criteria (1)
and (2) were found discharged with no evidence against either, naming criterion (3) as the blocking
criterion with its three legs, and stating that `[ ]` here is a measured result with a named blocking
criterion rather than a deferral.

It must state in ONE sentence that what moved is the PIN into agreement with an adjudicated row and
that CORE-11's checkbox did not move, and in ONE further sentence that commit `4105fd0` broke the
same-commit rule the constant's own message states and that this wave repaired the pairing rather than
performing it as routine housekeeping.

It must state in ONE sentence that construct anchoring removes the cross-construct relocation shape and
NOT the same-construct one, that two occurrences under the same construct remain interchangeable behind
a positional ordinal, and that the guard's phrase-list reach, its normalization and its unreached
surfaces are all unchanged by this wave.

It must state in ONE sentence that the must-NOT did not move — CORE-11's first sentence byte-identical,
23 files over both `SOURCE_ROOTS` at zero violations, one import specifier in the shipped bundle — and
that every finding this wave touched is a defect in a test-only gate's description of itself or in one
planning document.

It must also record any PREDICTION-VS-MEASUREMENT discrepancy in the shape `01-27-SUMMARY.md` uses, and
if there were none, say so explicitly.
</output>
