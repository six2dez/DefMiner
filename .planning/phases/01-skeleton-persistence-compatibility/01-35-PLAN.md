---
phase: 01-skeleton-persistence-compatibility
plan: 35
type: execute
wave: 35
gap_closure: true
depends_on: [01-34]
files_modified:
  - .planning/REQUIREMENTS.md
  - packages/backend/src/outbound-prohibition.spec.ts
  - .planning/STATE.md
  - .planning/WINDOWS.md
# THIS PLAN RECORDS AN OPERATOR DECISION AND THEN DISCHARGES AGAINST IT. IT DOES NOT MAKE THE DECISION,
# DOES NOT RE-DERIVE IT AND MUST NOT SOFTEN IT. READ THAT BEFORE ANYTHING ELSE, BECAUSE EVERY OTHER
# CHOICE IN THIS FILE FOLLOWS FROM IT.
#
# THE DECISION, IN THE OPERATOR'S TERMS. Six gap-closure rounds each closed roughly two gate blindnesses
# and each surfaced roughly two more. The rate is flat. Round 6's verifier diagnosed why, and the
# diagnosis is not that any round underperformed: CORE-11's OLD acceptance bar — the gate goes red on
# every spelling of every enumerated clause — is UNREACHABLE over an open language, not merely unmet.
# The space of JavaScript spellings for `invoke a function through a value` does not close, so no
# widening ever reaches that bar. The operator has adopted a REACHABLE bar instead, in the verifier's
# own words. CORE-11's `[x]` may now mean, and may ONLY mean, that the requirement's residual is:
#
#   (1) DERIVED          — generated from `RESOLVER_REGISTRY`, not authored (satisfied since wave 27)
#   (2) DRIFT-DETECTABLE — divergence between text and code turns the suite red (wave 27/29)
#   (3) THE SOLE BOUND   — stated on every surface a reader touches, contradicted nowhere (wave 33)
#
# CRITERIA 1 AND 2 WERE SATISFIED IN EARLIER WAVES AND ARE RE-VERIFIED HERE BY EXECUTION, NOT BY
# CITATION. A discharge that cites rather than executes is exactly what this phase has rejected six
# times, and both of those criteria were established under a bar that has since changed.
#
# THE NEW BAR IS NARROWER THAN THE OLD ONE. It does not claim the gate catches everything. It claims the
# gate's DESCRIPTION OF ITSELF is derived, drift-detectable and singular. The gate's REACH remains
# bounded and openly disclosed: CR-15, CR-16 and the sixteen-plus measured silences are NAMED RESIDUALS
# under the new bar, not defects it waves away. Any sentence implying the new bar makes the gate complete
# — in this plan, in `.planning/REQUIREMENTS.md`, in generated text, in an assertion message, or in the
# SUMMARY — is the eighth instance of this phase's signature defect committed in the very act of closing
# it. Wave 29 forbade the equivalent claim about its quantifier guard; this wave forbids it here.
#
# THE MUST-NOT DOES NOT MOVE, AND THIS IS THE SENTENCE THAT KEEPS IT FROM BEING MISREAD. The re-scope
# narrows what the CHECKBOX is allowed to mean. It does NOT narrow the PROHIBITION. CORE-11's first
# sentence — no outbound network request in any enumerated spelling in shipped source — is byte-identical
# after this wave and still holds: 23 files over both `SOURCE_ROOTS`, ZERO violations, the shipped
# bundle's whole import set one specifier (`crypto`), `observations.ts`'s redaction byte-identical since
# round 5 and 19,772 swept inputs with zero leaks. A reader who takes this decision for a weakened
# security requirement has been misled by it, and it is written so they cannot be.
#
# `[ ]` MUST REMAIN A REACHABLE HONEST OUTCOME. If wave 33 did not actually achieve criterion 3 — if a
# declared phrasing still states a bound outside `RESOLVER_REGISTRY[].clause`, if an exclusion swallowed
# more than it should, or if a standing bound survives on a reader surface wave 33's guard cannot see —
# THE BOX STAYS OPEN AND THAT IS THE CORRECT RESULT, not a failure of this plan. Waves 19, 28 and 32 all
# held it open correctly. The automated verify below is deliberately STATE-AGNOSTIC so the honest outcome
# cannot fail its own gate. That is NOT a repeat of WR-34: WR-34 was a state-agnostic character class in
# the SUITE's own pin, which is why `CORE11_BOX_EXPECTED` exists. The suite stays state-sensitive; only
# this plan's task-level verify is agnostic.
#
# CORE-11 HAS BEEN REVERTED FROM `[x]` TO `[ ]` TWICE — `e7cc4b6` after round 3 and `faca607` after round
# 4 — each time because its Complete status rested on a disclosure that later proved false. Both hashes
# are named in the box-flip mutation proof's pasted assertion message, as wave 32 did.
#
# THE TWO LONG-DEFERRED LEDGER COLLISIONS ARE NOT TOUCHED. STORE-03's and STORE-07's text-versus-usage
# collisions remain DEFERRED WITH THEIR OWNER — the operator, at the next requirements pass, by the
# STORE-01 → STORE-08 route. This decision does not reach them and NO SENTENCE THIS WAVE ADDS MAY CLAIM
# IT DOES. One flipped box is not a clean ledger.
#
# ENVIRONMENT FACTS, MEASURED DURING PLANNING SO THE EXECUTOR DOES NOT RE-DERIVE THEM:
#   - HEAD at planning time is `5e7f0d9`. Cross-check ANY git-status snapshot against `git rev-parse HEAD`
#     before trusting it — wave 31 was handed a snapshot seven commits stale.
#   - This repo has NO ACTIVE GIT HOOKS: `.git/hooks` holds only samples and `core.hooksPath` is unset.
#     Nothing runs the gate set for you at a commit boundary. Run it by hand at every task boundary.
#   - COMMIT BEFORE MUTATING. `git checkout --` plus `git diff --exit-code` restores to HEAD and destroys
#     uncommitted task work; it destroyed wave 29's Task 1 once. Waves 30 through 34 all use
#     commit-then-mutate and lost nothing.
#   - `grep -c 'probes every surface' packages/backend/src/outbound-prohibition.spec.ts` returns 1 today.
#     That single occurrence is inside `CORE11_BOX_EXPECTED`'s failure message and it states the OLD bar.
#   - `grep -c 'When this box is eventually' .planning/REQUIREMENTS.md` returns 2 today. Both are
#     statements of the OLD bar and BOTH are handled in Task 1's enumeration — one is dated, attributed
#     history and stays byte-identical; one is a standing statement and gets a scoped supersession marker.

autonomous: true
requirements: [CORE-11]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low
# `estimate-calibration` reports `{"factor": 1, "applied": false, "sample_count": 0, "min_samples": 3}`,
# so `tokens` equals `raw_tokens` and the confidence is DERIVED from the sample count, never self-rated.
# Weigh the MEASUREMENT COUNT over the token figure: one enumeration across four reader surfaces, three
# criterion re-verifications each by execution, and five separately executed mutation proofs.

must_haves:
  truths:
    - "THE RE-SCOPE IS RECORDED AS A DATED OPERATOR DECISION WITH ITS REASON, AND A READER CAN TELL IT WAS THE REQUIREMENT'S OWNER WHO MADE IT. `.planning/REQUIREMENTS.md` carries a correction dated 2026-08-25, attributed to plan 01-35 and wave 35, stating: that the OPERATOR made this decision; that the OLD bar — the gate goes red on every spelling of every enumerated clause — was UNREACHABLE over an open language rather than merely unmet; that six rounds of flat find-rate are the evidence for that; and that the route is the same one this project has always used for a requirement re-scope, the STORE-01 → STORE-08 precedent, named by id in the correction's own text. This is the load-bearing half of the wave: the phase's whole discipline is that a claim match its evidence, and a re-scope recorded as anything less than an owner's decision would be that same defect in a new place."
    - "THE DECISION NARROWS THE CHECKBOX AND NOT THE PROHIBITION, AND THE LEDGER SAYS SO IN THE SAME PARAGRAPH. CORE-11's first sentence is byte-identical after this wave, shown by diff. The correction states in its own text that the must-NOT is unchanged and still holds, with its evidence named: 23 files over both `SOURCE_ROOTS` and ZERO violations, one import specifier in the shipped bundle, `observations.ts`'s redaction byte-identical since round 5 across 19,772 swept inputs. A reader who takes this for a weakened security requirement has been misled, and the paragraph is written so they cannot be."
    - "CORE-11's `[x]` MAY NOW MEAN, AND MAY ONLY MEAN, THREE THINGS — DERIVED, DRIFT-DETECTABLE, THE SOLE BOUND — AND THE THREE ARE WRITTEN WHERE THE MECHANISM CAN SEE THEM. The bar is stated in the ledger correction AND in `CORE11_BOX_EXPECTED`'s failure message in the gate file, so a future session reading either surface is told the CURRENT terminal condition rather than the superseded one. The message's existing promise of a discharge against every enumerated surface is replaced, not appended to, because two bars standing side by side is the exact failure criterion 3 forbids."
    - "EVERY STANDING STATEMENT OF THE OLD BAR ON EVERY READER SURFACE IS ENUMERATED BY EXECUTION AND THEN DISPOSED OF, AND THE ENUMERATION IS PASTED. The four surfaces are `.planning/REQUIREMENTS.md`, `packages/backend/src/outbound-prohibition.spec.ts`, `.planning/STATE.md` and `.planning/WINDOWS.md`. The disposition rule is exactly two-valued: an occurrence that carries a DATE AND A PLAN ATTRIBUTION is HISTORY and stays byte-identical; an occurrence written as a STANDING bound, undated and unattributed, is SUPERSEDED by a scoped marker that names what it supersedes and leaves the superseded bytes intact below it. Measured during planning: `grep -c 'When this box is eventually' .planning/REQUIREMENTS.md` is 2, one of each kind."
    - "THE SUPERSESSION MARKERS ARE SCOPED TO THE SENTENCE THEY SUPERSEDE AND NOT TO THE PARAGRAPH THEY SIT IN. The standing paragraph that states the old bar ALSO carries two things that remain TRUE and must survive untouched: the limits list, and the sentence recording that STORE-03's and STORE-07's collisions remain DEFERRED WITH AN OWNER. A marker that falsifies the whole paragraph would delete a true deferral record in the act of correcting a superseded bar. Each marker names which sentence it reaches and says the rest stands."
    - "STORE-03 AND STORE-07 ARE UNTOUCHED, AND NO SENTENCE ADDED BY THIS WAVE CLAIMS OTHERWISE. Both remain LEDGER COLLISIONS deferred with their owner — the operator, at the next requirements pass, by the STORE-01 → STORE-08 route. `git diff` over both entries is EMPTY. The correction states in its own words that one flipped box is not a clean ledger, in the same terms wave 28's correction used, so a reader cannot take CORE-11's disposition for the ledger's."
    - "CRITERION 1 IS RE-VERIFIED BY EXECUTION, NOT BY CITATION. Both derived spans are re-hashed and shown byte-identical to `deriveResidual(RESOLVER_REGISTRY)`, with line count, entry count and sha256 pasted from the generated form and from both files side by side, and the one-entry-per-registry-row check green on both copies. Then DERIVATION ITSELF is proved: one registry row is mutated, the suite is watched going red because the shipped spans no longer equal the generated one, and the RED test title and full assertion message are pasted. A span that merely matches is consistent; a span that FOLLOWS THE REGISTRY is derived, and only the mutation tells the two apart."
    - "CRITERION 2 IS RE-VERIFIED BY EXECUTION ON BOTH COPIES SEPARATELY. Drift is planted inside the machine-owned span in `.planning/REQUIREMENTS.md` and the ledger byte comparison is watched going red; the drift is restored; drift is then planted inside the gate header's copy and that byte comparison is watched going red. Two mutations, executed separately and never combined, each with its RED title and full assertion message pasted, each restored with `git diff --exit-code` shown clean. One mutation covering both copies would prove neither guard individually."
    - "CRITERION 3 IS VERIFIED AS WAVE 33's OUTCOME RATHER THAN ASSUMED FROM WAVE 33's PLAN, AND IT IS THE ONE CRITERION THAT CAN HONESTLY FAIL. Three executed checks, all three required: wave 33's whole-file wrap-tolerant quantifier guard is RUN and observed green and its declared-phrasing counts are INDEPENDENTLY re-measured rather than read out of `01-33-SUMMARY.md`; each of its three exclusions is checked for over-breadth by measuring what it actually swallows and confirming every excluded occurrence is machine-owned span, the declaration itself, or a `RESOLVER_REGISTRY[].clause`; and a declared phrasing is re-planted outside the exclusions and the guard watched going red. AND THE VERDICT IS SCOPED TO WHAT THAT MECHANISM CAN EXECUTE, BECAUSE THE GUARD'S REACH IS NARROWER THAN THE CRITERION IT DISCHARGES. Wave 33 states the limit and this wave inherits it rather than dropping it: the guard is a PHRASE LIST over BYTES under one named normalization, so a hand-written bound spelled in words it does not declare passes all four checks unseen. Criterion 3 is therefore met UP TO THE GUARD'S PHRASE-LIST REACH UNDER ITS NAMED NORMALIZATION, and TWO classes of unreached surface are named together wherever the verdict is stated: unguarded FILES — `.planning/STATE.md` and `.planning/WINDOWS.md`, which no byte comparison reaches — and undeclared SPELLINGS inside the guarded files. A `✓` that omits either class is a claim whose stated reach exceeds its executed reach, which is the exact defect this round exists to correct, installed as the fix. If any check fails, CRITERION 3 IS NOT MET, the box stays `[ ]`, and the failing check is named in the ledger as the blocking row."
    - "THE DISCHARGE IS A TABLE WITH ONE ROW PER CRITERION, IN THE COLUMN SHAPE `01-32-SUMMARY.md` USED FOR ITS THIRTY-FIVE PROBES. Each row carries the criterion, the EXECUTED evidence that satisfies it, the artifact the evidence was measured against, the mutation that proved the evidence live, and a verdict. The old eight-row enumerated-surface discharge is NOT re-run as the acceptance test — it is superseded — but its most recent result is cited once as context so a reader can see which bar changed."
    - "THE BOX MOVES ONLY IF ALL THREE ROWS VERDICT `✓`, AND IT MOVES WITH `CORE11_BOX_EXPECTED` IN THE SAME COMMIT. Flipping the constant on its own is prohibited by the constant's own failure message and by this plan. If any row is `✗`, the box stays `[ ]`, `CORE11_BOX_EXPECTED` stays `[ ]`, the blocking criterion is named in the ledger, and the wave is a SUCCESS — `[ ]` is a measured result, not a deferral and not a failure of this plan."
    - "THE BOX-FLIP MUTATION IS MANDATORY IN EITHER OUTCOME. Whatever the box's final state, the ledger row is moved away from `CORE11_BOX_EXPECTED` once, the pin is watched going RED, the full assertion message is pasted — it contains both revert hashes, `e7cc4b6` and `faca607` — and the mutation is restored with `git diff --exit-code` shown clean. A pin nobody watched fail in this session is a pin this session may not rely on."
    - "NO SENTENCE ANYWHERE STATES OR IMPLIES THAT THE NEW BAR MAKES THE GATE COMPLETE. Not in this plan, not in the ledger correction, not in `CORE11_BOX_EXPECTED`'s message, not in either pointer amendment, not in the SUMMARY. The new bar is NARROWER than the old one and every surface that states it says so in the same breath: CR-15, CR-16 and the measured silences are NAMED RESIDUALS under the new bar, disclosed and unclosed, and the class is open because the space of JavaScript spellings is open. Wave 29 forbade the equivalent claim about its quantifier guard; this wave forbids it about the bar itself."
    - "MUTATION PROOFS ARE EXECUTED SEPARATELY, NEVER COMBINED, EACH AFTER ITS OWN WORK IS COMMITTED. Five of them: the registry-row divergence, the ledger-copy drift, the header-copy drift, the re-planted declared phrasing, and the box pin. Each carries its RED test title and its full assertion message, each is restored, `git diff --exit-code` is shown clean between them, the run order is stated, and `git log --oneline -1` is pasted before each showing the real work committed first."
    - "EVERY PREDICTION-VS-MEASUREMENT DISCREPANCY IS RECORDED RATHER THAN ABSORBED, in the shape `01-27-SUMMARY.md` uses. This wave predicts that criteria 1 and 2 re-verify green and that criterion 3 turns on wave 33's exclusions being non-vacuous and not over-broad. If any prediction is wrong, THE DIFFERENCE IS THE FINDING and it is written down before any box is touched."
    - "NOTHING LEAKS, AND THIS PLAN SAYS SO PLAINLY RATHER THAN LEAVING IT INFERRED. Zero outbound-shaped tokens in any shipped non-spec source across both roots and the frontend; the shipped bundle's entire import set is one specifier, `crypto`; `observations.ts`'s redaction code byte-identical since round 5 and survived 19,772 swept inputs with zero leaks; the real tree runs 23 files / 0 violations with the four measured-exempt sites named. Every finding this wave touches is a PROSPECTIVE blindness in a TEST-ONLY gate or a DISCLOSURE defect, never a live exposure."

  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "The dated 2026-08-25 OPERATOR DECISION re-scoping what CORE-11's `[x]` is allowed to mean, with its reason, the STORE-01 → STORE-08 route named, the must-NOT stated unchanged, the three criteria stated, the STORE-03 and STORE-07 deferral restated as untouched, and the explicit sentence that the new bar is narrower and ends no class; the scoped supersession markers over standing statements of the old bar; the three-row discharge table's verdict and, if and only if all three verdict `✓`, CORE-11's box"
      contains: "STORE-08"
    - path: "packages/backend/src/outbound-prohibition.spec.ts"
      provides: "`CORE11_BOX_EXPECTED` moved only in the same commit as the ledger row, and its failure message rewritten to state the THREE-criterion bar in place of the superseded enumerated-surface discharge promise, still naming both revert hashes and still stating that `[ ]` is a correct outcome"
      contains: "CORE11_BOX_EXPECTED"
    - path: ".planning/STATE.md"
      provides: "A pointer amendment naming the operator decision, its date and its three criteria, and restating no bound of its own — a pointer at the derived span, never a copy of it"
      contains: "DERIVED"
    - path: ".planning/WINDOWS.md"
      provides: "The open entry for this gate file superseded and a new pointer entry appended through `gsd-tools windows`, never by hand-editing the table"
      contains: "outbound-prohibition.spec.ts"
  key_links:
    - from: "the operator's re-scope of CORE-11's acceptance bar"
      to: "`.planning/REQUIREMENTS.md`"
      via: "a dated, attributed correction naming the operator as the decider, the unreachability of the old bar as the reason, and the STORE-01 → STORE-08 precedent as the route"
      pattern: "STORE-08"
    - from: "the three criteria the new bar states"
      to: "`CORE11_BOX_EXPECTED`'s failure message"
      via: "a rewrite that replaces the superseded enumerated-surface discharge promise rather than appending beside it, so no future session is told two different terminal conditions"
      pattern: "CORE11_BOX_EXPECTED"
    - from: "criterion 1, DERIVED"
      to: "`deriveResidual(RESOLVER_REGISTRY)` and both shipped spans"
      via: "byte comparison plus a registry-row mutation that makes the shipped spans diverge, so derivation is proved rather than consistency observed"
      pattern: "deriveResidual"
    - from: "criterion 2, DRIFT-DETECTABLE"
      to: "the two byte-comparison cases"
      via: "drift planted separately inside each machine-owned span and each comparison watched going red on its own"
      pattern: "extractDerivedBlock"
    - from: "criterion 3, THE SOLE BOUND"
      to: "wave 33's whole-file wrap-tolerant quantifier guard and its three anchored exclusions"
      via: "the guard run, its counts independently re-measured, each exclusion checked for over-breadth against what it actually swallows, and a declared phrasing re-planted outside them and watched going red"
      pattern: "HEADER_QUANTIFIER_EXEMPTIONS"
    - from: "CORE-11's checkbox state"
      to: "the only two places allowed to state it"
      via: "the ledger row and `CORE11_BOX_EXPECTED`, moved in the SAME COMMIT or not at all, with the pin watched failing in this session"
      pattern: "CORE11_BOX_EXPECTED"
  prohibitions:
    - requirement_id: CORE-11
      category: security
      status: resolved
      verification: gate
      resolution: ".planning/REQUIREMENTS.md — a correction dated 2026-08-25 and attributed to plan 01-35 stating that the OPERATOR re-scoped the acceptance bar, that the old bar was unreachable over an open language rather than merely unmet, and that the route is the STORE-01 → STORE-08 precedent named by id"
      statement: "MUST NOT record the re-scope as anything less than a dated decision by the requirement's owner. A planner quietly lowering a bar it could not clear is the same defect this phase has spent seven rounds correcting, committed in the act of closing it. A reader must be able to tell from the ledger alone who decided, when, and why."
    - requirement_id: CORE-11
      category: security
      status: resolved
      verification: gate
      resolution: ".planning/REQUIREMENTS.md — CORE-11's first sentence byte-identical, shown by diff, with the must-NOT's evidence restated in the correction's own paragraph"
      statement: "MUST NOT let the re-scope read as a weakened prohibition. What narrows is the meaning of the CHECKBOX, not the meaning of the requirement. No outbound call exists in any non-spec source under either root, and the correction says so in the same paragraph as the re-scope so the two cannot be conflated."
    - requirement_id: CORE-11
      category: safety
      status: open
      verification: gate
      resolution: "DISCLOSED, NOT CLOSED — the new bar is stated with its narrowness on every surface that states it, AND every criterion verdict is stated with the reach of the mechanism that produced it, and CR-15, CR-16 and the measured silences are named as residuals under it"
      statement: "MUST NOT state or imply that the new bar makes the gate complete, AND MUST NOT STATE A CRITERION VERDICT WHOSE REACH EXCEEDS THE MECHANISM THAT PRODUCED IT. The bar makes the gate's DESCRIPTION OF ITSELF derived, drift-detectable and singular; the gate's REACH stays bounded and openly disclosed. The second half is the one a closing wave gets wrong: forbidding an overclaiming BAR while leaving the DISCHARGE free to overclaim is the same defect with its evidence removed. Criterion 3's mechanism is a phrase list over bytes under one normalization, so its `✓` is scoped to that reach with both classes of unreached surface — unguarded files and undeclared spellings — named beside it. The class is open because the space of JavaScript spellings is open, and every surface stating the bar OR A VERDICT states its own reach in the same breath."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "packages/backend/src/outbound-prohibition.spec.ts and .planning/REQUIREMENTS.md — the box and `CORE11_BOX_EXPECTED` move together or not at all, after a three-row discharge in which every row's evidence was EXECUTED in this session"
      statement: "MUST NOT flip the box against a citation. Criteria 1 and 2 were satisfied in earlier waves and are re-verified here by running the comparisons and by watching mutations go red. A discharge that cites rather than executes is what `e7cc4b6` and `faca607` each reverted, and the second of those cited a residual three of whose sentences the next verifier falsified."
    - requirement_id: CORE-11
      category: safety
      status: open
      verification: gate
      resolution: "The task-level automated verify is state-agnostic on purpose; the SUITE's pin stays state-sensitive through `CORE11_BOX_EXPECTED`"
      statement: "MUST NOT make `[ ]` fail its own gate. If criterion 3 is not met, the honest outcome is an open box with the blocking check named, and no acceptance criterion in this plan may punish that outcome. This is NOT a repeat of WR-34 — WR-34 was a state-agnostic character class in the suite's own pin, which is precisely why `CORE11_BOX_EXPECTED` exists and stays sensitive here."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: ".planning/REQUIREMENTS.md — scoped supersession markers that name the sentence they reach and leave the superseded bytes intact, with every dated and attributed occurrence left byte-identical as history"
      statement: "MUST NOT rewrite history to make the new bar look like it was always the bar. The old bar's statements are preserved; the standing ones are marked superseded in place and the dated ones are left alone. And a marker MUST NOT reach past the sentence it supersedes into the true limits list or the STORE-03/STORE-07 deferral that shares its paragraph."
    - requirement_id: CORE-11
      category: safety
      status: open
      verification: gate
      resolution: "UNTOUCHED AND DEFERRED WITH THEIR OWNER — `git diff` over both entries EMPTY"
      statement: "MUST NOT claim this decision reaches STORE-03 or STORE-07. Both remain ledger collisions deferred to the operator at the next requirements pass by the STORE-01 → STORE-08 route. One flipped box is not a clean ledger, and the correction says that in its own words."
---

<objective>
Write the operator's re-scope of CORE-11 into `.planning/REQUIREMENTS.md` as a dated decision by the
requirement's owner, with its reason, by the route this project has always used. Then re-execute
CORE-11's discharge against the THREE criteria that re-scope installs, and move the box if and only if
all three are met.

Purpose: six gap-closure rounds each closed roughly two gate blindnesses and each surfaced roughly two
more, and round 6's verifier diagnosed why. CORE-11's old acceptance bar — the gate goes red on every
spelling of every enumerated clause — is UNREACHABLE over an open language, not merely unmet, because
the space of JavaScript spellings for reaching a function through a value does not close. The operator
has adopted the reachable bar the verifier named: the residual must be DERIVED from the code,
DRIFT-DETECTABLE between text and code, and THE SOLE BOUND stated on every surface a reader touches.
Criteria 1 and 2 have been satisfied since waves 27 and 29 and are re-verified here BY EXECUTION rather
than by citation, because they were established under a bar that has since changed and because a
discharge that cites is exactly what this phase has rejected six times. Criterion 3 is wave 33's
deliverable and is verified as an OUTCOME rather than assumed from a plan.

Output: a dated 2026-08-25 operator decision in the ledger naming the operator as the decider, the
unreachability of the old bar as the reason and the STORE-01 → STORE-08 precedent as the route; scoped
supersession markers over every standing statement of the old bar with the dated ones preserved as
history; `CORE11_BOX_EXPECTED`'s failure message rewritten to state the three-criterion bar; a three-row
discharge table whose every row carries executed evidence and a mutation that proved that evidence live;
five separately executed mutation proofs including the mandatory box-flip; and pointer amendments on
both append-only histories.

THE NEW BAR IS NARROWER THAN THE OLD ONE AND THIS PLAN SAYS SO REPEATEDLY AND PLAINLY. It does not
claim the gate catches everything. It claims the gate's DESCRIPTION OF ITSELF is derived,
drift-detectable and singular — which is the only promise a static gate over an open language can
actually keep. CR-15, CR-16 and the sixteen-plus measured silences are NAMED RESIDUALS under the new
bar, disclosed and unclosed. Any sentence implying otherwise is the eighth instance of this phase's
signature defect.

WHAT THIS WAVE DOES NOT DO, STATED HERE SO IT CANNOT BE MISREAD LATER. It does not weaken CORE-11's
prohibition — the must-NOT is byte-identical and still holds. It does not touch STORE-03 or STORE-07,
which remain deferred with their owner. It does not widen a resolver, close a shape or edit the gate
file's header, all of which belong to waves 33 and 34. And it does not guarantee an `[x]`: if criterion
3 was not achieved, the box stays `[ ]` with the blocking check named, and that is the correct result.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md
@.planning/phases/01-skeleton-persistence-compatibility/01-33-SUMMARY.md
@.planning/phases/01-skeleton-persistence-compatibility/01-34-SUMMARY.md
@.planning/phases/01-skeleton-persistence-compatibility/01-32-SUMMARY.md
@packages/backend/src/outbound-prohibition.spec.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: The operator decision, end to end — write the re-scope into the ledger as a dated owner's decision, carry the three criteria into the one constant the suite reads, supersede every standing statement of the old bar, and watch the pin go red</name>

  <precondition>
    Waves 33 and 34 have executed and their artefacts are on disk: `01-33-SUMMARY.md` and
    `01-34-SUMMARY.md` both exist, wave 33's whole-file wrap-tolerant quantifier guard and
    `HEADER_QUANTIFIER_EXEMPTIONS` are present in `packages/backend/src/outbound-prohibition.spec.ts`,
    and `pnpm test` exits 0. Assert before the first edit that BOTH byte comparisons pass — the block
    shipped in the gate header equals `deriveResidual(RESOLVER_REGISTRY)` byte for byte, and the
    `.planning/REQUIREMENTS.md` twin does too — and HALT if either is already red, because a decision
    written on top of a diverged span records a bar the code does not carry. Assert
    `git status --porcelain packages/ .planning/` is clean and cross-check any status snapshot against
    `git rev-parse HEAD`; wave 31 was handed a snapshot seven commits stale. This repo has NO active git
    hooks — `.git/hooks` holds only samples and `core.hooksPath` is unset — so nothing runs the gate set
    for you at a commit boundary.
  </precondition>

  <read_first>
    - `.planning/REQUIREMENTS.md` line 46, CORE-11's entry, in full, and every dated correction it
      carries from plan 01-12 through plan 01-28. Read it for the CORRECTION SHAPE — the dated,
      plan-attributed prefix — because that shape is what makes a statement history rather than a
      standing bound, and this task's disposition rule turns entirely on that distinction.
    - `.planning/REQUIREMENTS.md` STORE-01 at `:632`, STORE-03 at `:634`, STORE-07 at `:638` and
      STORE-08 at `:639`. STORE-01's parenthetical is the PRECEDENT this decision is written in: it
      states what was re-scoped, on what date, in what forum, and why. STORE-03's and STORE-07's each
      end with the sentence naming the operator as OWNER and the STORE-01 → STORE-08 route. Those two
      are the entries this task must leave byte-identical.
    - `.planning/REQUIREMENTS.md` the standing bolded paragraphs immediately above the generated-residual
      begin sentinel, in particular the one beginning with the restated limits. It carries THREE things
      in one paragraph: a statement of the OLD bar in the future tense, a limits list that remains true,
      and the STORE-03 / STORE-07 deferral that remains true. Read all three before writing any marker,
      because a marker that reaches past the first would delete a true deferral record.
    - `packages/backend/src/outbound-prohibition.spec.ts` `:9020` and the case at `:9080-9098`. Read
      `CORE11_BOX_EXPECTED`'s full failure message. It names both reverts, it explains why WR-34's
      character class stayed green through both flips and both reverts, it states that `[ ]` is a correct
      outcome and an unexamined `[x]` is not, and it states the OLD terminal condition as the one
      legitimate way out. The first three of those survive this task; the fourth is replaced.
    - `01-VERIFICATION.md` `:114-135`, the verifier's own answer, including the paragraph that names the
      old condition unreachable and the block-quoted sentence stating the three criteria. The decision
      recorded here uses the verifier's own words for the three criteria and cites that section.
    - `01-VERIFICATION.md` `:269-285`, the CORE-11 section and the severity paragraph, for the must-NOT
      evidence to restate: zero outbound-shaped tokens across both roots and the frontend, one import
      specifier in the shipped bundle, real tree green.
    - `01-32-SUMMARY.md` `:528-560`, the ordering paragraph and the discharge table's column shape. The
      three-row table Task 2 builds uses these columns, and the ordering rule — byte comparisons green
      BEFORE the first discharge probe, not after — is the rule Task 2 inherits.
    - `01-28-SUMMARY.md`'s record of the guard catching its own reader when a correction reproduced a
      sentinel marker line in prose above the span. `extractDerivedBlock` bounds the span by searching
      this file's own bytes for those marker lines, so reproducing one above the span turns the suite
      red. The correction this task writes refers to the span descriptively and reproduces neither
      marker line.
  </read_first>

  <files>.planning/REQUIREMENTS.md, packages/backend/src/outbound-prohibition.spec.ts</files>

  <reversibility rating="costly">Recording a re-scope in an append-only ledger cannot be undone by deletion — it would have to be superseded by a further dated correction, and the superseded text would stay on the page. The decision itself is the operator's and is already made; what is costly here is that the RECORD of it is permanent, which is exactly the property that makes it trustworthy. No code behaviour moves in this task beyond one constant's failure message.</reversibility>

  <action>
    ENUMERATE BEFORE YOU WRITE, ACROSS ALL FOUR READER SURFACES, AND PASTE THE ENUMERATION. The four
    surfaces are `.planning/REQUIREMENTS.md`, `packages/backend/src/outbound-prohibition.spec.ts`,
    `.planning/STATE.md` and `.planning/WINDOWS.md`. Grep each for statements of the OLD bar — the
    future-tense phrasing about what the box will mean, and the phrasing about discharging against every
    clause the requirement's own first sentence enumerates. Measured during planning at HEAD `5e7f0d9`:
    the ledger carries the future-tense phrasing twice and the gate file carries the discharge phrasing
    once. Reconcile your counts against those three numbers and record any disagreement AS A FINDING
    rather than adopting either number silently. Then classify EVERY occurrence with the two-valued rule
    and paste the table: DATED AND PLAN-ATTRIBUTED is HISTORY and stays byte-identical; STANDING —
    undated, unattributed, written as a bound rather than as a record — is SUPERSEDED IN PLACE.

    WRITE THE DECISION FIRST, AND WRITE IT AS THE OWNER'S. This is the load-bearing half of the wave and
    the ordering is deliberate. Append a correction dated 2026-08-25 and attributed to plan 01-35 and
    wave 35, below wave 34's correction and above the generated-residual begin sentinel. It states, in
    this order and in one continuous passage rather than scattered: that THE OPERATOR made this decision;
    WHY — the old acceptance bar was UNREACHABLE over an open language rather than merely unmet, because
    the space of JavaScript spellings for reaching a function through a value does not close, with six
    rounds of flat find-rate cited as the evidence and `01-VERIFICATION.md`'s answer section cited as the
    diagnosis; the ROUTE, named by id — the same STORE-01 → STORE-08 precedent this project used for its
    only previous requirement re-scope, and the same route STORE-03 and STORE-07 are still queued on; and
    the THREE CRITERIA in the verifier's own words, numbered, each with the wave that delivered it.

    STATE THE NARROWNESS IN THE SAME PASSAGE, NOT IN A LATER ONE. The new bar is NARROWER than the old
    one. It does not claim the gate catches everything; it claims the gate's description of itself is
    derived, drift-detectable and singular, which is the only promise a static gate over an open language
    can keep. Name CR-15, CR-16 and the measured silences as RESIDUALS UNDER THE NEW BAR — disclosed and
    unclosed — and say that the class is open because the space of spellings is open. A reader who takes
    this correction for the end of the finding stream has been misled by it, and it is written so they
    cannot be.

    STATE THAT THE PROHIBITION DID NOT MOVE, WITH ITS EVIDENCE, IN THE SAME PASSAGE. What narrows is the
    meaning of the CHECKBOX. CORE-11's first sentence is unchanged and still holds: no outbound call
    exists in any non-spec source under either root, 23 files over both `SOURCE_ROOTS` and zero
    violations, the shipped bundle's whole import set one specifier, and `observations.ts`'s redaction
    byte-identical since round 5 across 19,772 swept inputs. Prove the first sentence unchanged by diff
    rather than asserting it.

    STATE THAT STORE-03 AND STORE-07 ARE UNTOUCHED, IN THE WORDS WAVE 28's CORRECTION USED. Both remain
    ledger collisions deferred with their owner — the operator, at the next requirements pass, by the
    STORE-01 → STORE-08 route — and one flipped box is not a clean ledger. Then show it: `git diff` over
    both entries EMPTY, pasted.

    REPRODUCE NEITHER SENTINEL MARKER LINE IN THE CORRECTION'S PROSE. `extractDerivedBlock` bounds the
    span by searching this file's own bytes for those marker lines, so quoting one above the span turns
    the suite red — which is what happened when wave 28's correction was first written, recorded in
    `01-28-SUMMARY.md`. Refer to the span descriptively, exactly as the surviving corrections do.

    NOW PLANT THE SUPERSESSION MARKERS, SCOPED TO THE SENTENCE AND NOT TO THE PARAGRAPH. For each
    occurrence the enumeration classified STANDING, insert a marker that names WHICH sentence it
    supersedes, names the 2026-08-25 decision as what supersedes it, and states that the remainder of the
    paragraph stands. The superseded bytes stay intact below the marker — that is this phase's own
    precedent for falsified text and it is the reason the record reads straight. The standing paragraph
    above the sentinel also carries a limits list and the STORE-03 / STORE-07 deferral, BOTH OF WHICH
    REMAIN TRUE; a marker that reaches past the first sentence into either of those deletes a true record
    in the act of correcting a superseded one. Prove the scoping by diff: each marker is an insertion and
    the surrounding bytes are unchanged.

    CARRY THE THREE CRITERIA INTO THE ONE CONSTANT THE SUITE READS. `CORE11_BOX_EXPECTED`'s failure
    message currently offers a single legitimate way out, and the way out it names is the SUPERSEDED bar.
    Replace that promise with the three criteria — REPLACE, never append beside, because two terminal
    conditions in one message is the exact contradiction criterion 3 forbids. Keep everything in that
    message that survives: both revert hashes `e7cc4b6` and `faca607`, the WR-34 explanation of why the
    character class stayed green through both flips, the rule that the constant and the ledger row move
    in the SAME COMMIT, and the sentence that `[ ]` is a correct outcome while an unexamined `[x]` is
    not. Add nothing that states or implies the new bar makes the gate complete.

    KEEP WAVE 33's GUARD GREEN WITHOUT WEAKENING IT. That message sits below `RESOLVER_REGISTRY`, inside
    the whole-file guard's reach and outside its three exclusions, and both its occurrence counts are
    pinned by equality. Run the guard after every edit rather than at the end. If a new sentence trips it,
    REWRITE THE SENTENCE — do not widen an exclusion and do not add a `HEADER_QUANTIFIER_EXEMPTIONS`
    entry. Those belong to wave 33, and taking one is how a guard becomes decoration in one commit.

    DO NOT TOUCH THE BOX IN THIS TASK. The box moves in Task 2 or not at all, and only after the
    three-row discharge. Assert here that CORE-11's row is byte-unchanged and that `CORE11_BOX_EXPECTED`
    still pins the state it pinned at HEAD.

    COMMIT BEFORE YOU MUTATE, THEN RUN ONE MUTATION. Wave 29 destroyed its own Task 1 with
    `git checkout --` against uncommitted work. Commit the decision and the message rewrite, then plant
    drift INSIDE the machine-owned span in `.planning/REQUIREMENTS.md` — one byte is enough — run the
    suite, watch the ledger byte comparison go RED, paste the failing test title and the full assertion
    message, restore, and cross-check with `git diff --exit-code`. That single mutation is this task's
    end-to-end proof: a decision written into the ledger, carried to the constant the suite reads, and a
    guard that can see the page it was written on.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/DefMiner && grep -cE '^- \[[ x]\] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md && pnpm vitest run packages/backend/src/outbound-prohibition.spec.ts 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - The precondition was asserted and pasted BEFORE the first edit: `01-33-SUMMARY.md` and
      `01-34-SUMMARY.md` present, both byte comparisons green, `pnpm test` exit 0,
      `git status --porcelain packages/ .planning/` clean, and `git rev-parse HEAD` pasted beside any
      status snapshot used.
    - The four-surface enumeration is pasted as a table, one row per occurrence, each carrying its file,
      its line, the phrasing it matched, its classification (HISTORY or STANDING) and the evidence for
      that classification — a date and a plan attribution, or their absence. The planning-time counts
      were 2 in `.planning/REQUIREMENTS.md` and 1 in `packages/backend/src/outbound-prohibition.spec.ts`;
      any disagreement with those numbers is recorded as a finding rather than absorbed.
    - The 2026-08-25 correction is present above the generated-residual begin sentinel and below wave
      34's, and the SUMMARY quotes VERBATIM the sentences in it that: name the OPERATOR as the decider;
      state the old bar was unreachable over an open language rather than merely unmet; name the
      STORE-01 → STORE-08 precedent by id as the route; and number the three criteria.
    - The correction contains, in the SAME passage rather than a later one, an explicit sentence that the
      new bar is NARROWER than the old one, that it ends no class, and that CR-15, CR-16 and the measured
      silences are residuals under it. That sentence is quoted verbatim in the SUMMARY.
    - The correction states that the must-NOT did not move, and `git diff` over CORE-11's first sentence
      is shown EMPTY, output pasted.
    - `git diff` over the STORE-03 and STORE-07 entries shown EMPTY, output pasted, and the correction's
      own sentence restating their deferral with its owner and route quoted in the SUMMARY.
    - Executed `grep -c 'probes every surface' packages/backend/src/outbound-prohibition.spec.ts`,
      observed 0. The pre-wave value is 1. The replacement prose was WRITTEN not to contain the
      superseded literal in any form — a correction that quotes the sentence it corrects re-introduces
      it, and `grep -c` cannot tell a quotation from an assertion.
    - Executed `grep -c 'When this box is eventually' .planning/REQUIREMENTS.md`, observed 2 — UNCHANGED.
      Both statements of the old bar are preserved; neither is rewritten. The STANDING one carries a
      scoped supersession marker above it and the HISTORY one does not, shown by diff.
    - Each supersession marker is shown by `git diff` to be a pure INSERTION, with the bytes above and
      below it unchanged, and the SUMMARY quotes the marker's own clause naming which sentence it reaches
      and stating that the remainder of the paragraph stands.
    - `CORE11_BOX_EXPECTED`'s failure message quoted BEFORE and AFTER in full. The after form states the
      three criteria, still names `e7cc4b6` and `faca607`, still carries the WR-34 explanation, still
      requires the constant and the ledger row to move in the same commit, and still says `[ ]` is a
      correct outcome. Nothing was appended beside the superseded promise; it was replaced.
    - Neither generated-residual sentinel marker line is reproduced anywhere in the new correction prose,
      confirmed by running the suite and observing the extraction cases green rather than by inspection.
    - CORE-11's checkbox and `CORE11_BOX_EXPECTED`'s value both byte-unchanged in this task, shown by
      diff — the box is Task 2's.
    - Wave 33's whole-file quantifier guard executed after EVERY edit in this task, green each time, and
      `git diff` over its three exclusions, its `HEADER_QUANTIFIER_EXEMPTIONS` map and both its pinned
      counts shown EMPTY. The guard was satisfied by rewriting new prose, never by widening anything.
    - MUTATION executed: drift planted inside the machine-owned span in `.planning/REQUIREMENTS.md`, run,
      observed RED, the failing test title and the full assertion message pasted, restored,
      `git diff --exit-code` shown clean. `git log --oneline -1` pasted BEFORE the mutation showing the
      real work was committed first.
    - Both byte comparisons green at the end of the task, with the span's line count, entry count and
      sha256 pasted from the generated form and from both files.
  </acceptance_criteria>

  <done>
    The operator's re-scope stands in the ledger as a dated, attributed decision carrying its reason and
    its route; every standing statement of the old bar is superseded in place with the dated ones
    preserved; the three criteria are stated in the one constant the suite reads; and a planted drift was
    watched turning that page's own guard red.
  </done>
</task>

<task type="auto">
  <name>Task 2: CORE-11's discharge re-executed against the three new criteria — each verified by execution rather than by citation, the box moved only if all three pass, the pin watched failing, and both pointer histories amended</name>

  <read_first>
    - Task 1's four-surface enumeration table and the 2026-08-25 correction it wrote. The discharge in
      this task is against THOSE three criteria and nothing else; the old eight-row enumerated-surface
      discharge is superseded and is cited once as context, never re-run as the acceptance test.
    - `packages/backend/src/outbound-prohibition.spec.ts` `:5767` (`deriveResidual`), `:5722-5723`
      (`DERIVED_BEGIN` and `DERIVED_END`) and `:5855` (`extractDerivedBlock`). Read `deriveResidual`'s
      body closely enough to know which registry field a one-row mutation must touch for the generated
      output to change — that is criterion 1's mutation and a mutation that changes no output proves
      nothing.
    - The four byte-comparison cases at `:8980`, `:9052`, `:9065` and `:9100`. Two are one-entry-per-row
      checks and two are byte equalities, one pair per copy. Criterion 2's two mutations target the two
      byte equalities, one each, and knowing which case guards which copy is what makes the two mutations
      separable.
    - `:9080-9098`, the box case, with the message as Task 1 left it. This is criterion 3's contradiction
      surface and the target of the mandatory box-flip mutation.
    - Wave 33's whole-file wrap-tolerant quantifier guard, its three anchored exclusions, its
      non-vacuity assertions and both its pinned counts, wherever `01-33-SUMMARY.md` records them — AND
      the guard's own code. Read the code, not only the summary: criterion 3 is verified as wave 33's
      OUTCOME and a summary is a citation.
    - `01-33-SUMMARY.md`'s classification table for the region below `RESOLVER_REGISTRY`, and
      `01-34-SUMMARY.md`'s record of the prose it added inside that guard's reach. Between them they say
      what the exclusions are expected to swallow, which is the prediction this task measures against.
    - `01-32-SUMMARY.md` `:528-560` again for the ordering rule and the table columns, and `:613-633` for
      how a `[ ]` decision is written up when it is a measured result rather than a deferral.
    - `01-27-SUMMARY.md`'s prediction-versus-measurement section, for the shape any discrepancy in this
      task is recorded in.
    - `.planning/STATE.md`'s existing P9-D3 pointer amendments, so the new one matches their shape and
      the previous ones can be shown byte-identical.
    - `.planning/WINDOWS.md`'s current open entry for this gate file and the ledger header's instructions
      for the `gsd-tools windows` subcommands.
  </read_first>

  <files>.planning/REQUIREMENTS.md, packages/backend/src/outbound-prohibition.spec.ts, .planning/STATE.md, .planning/WINDOWS.md</files>

  <reversibility rating="costly">CORE-11's box has been flipped early and reverted twice, at `e7cc4b6` and `faca607`, and each revert cost a gap-closure round. Reverting a third time would be mechanically simple and reputationally expensive in exactly the way the two previous reverts were. That is why the flip happens only after three executed criterion rows and why `[ ]` is written into this task as a first-class outcome rather than a fallback.</reversibility>

  <action>
    ESTABLISH THE ORDERING FIRST, THE WAY WAVE 32 DID. Run both byte comparisons and paste their output
    with a timestamp BEFORE the first criterion row is written, not after. A discharge whose consistency
    check ran after its conclusions was written to fit them. Paste the generated form's line count, entry
    count and sha256 beside both files', and state the hashing convention so nobody re-derives it: the
    digest reproduces only when the extracted span is hashed WITHOUT a trailing newline, which is what
    the extractor returns.

    ROW 1 — DERIVED. VERIFY BY EXECUTION, NOT BY CITATION, EVEN THOUGH WAVE 27 SATISFIED IT. Two steps
    and both are required. First, the equality: both shipped spans byte-identical to
    `deriveResidual(RESOLVER_REGISTRY)`, and both one-entry-per-registry-row checks green. Second — and
    this is the step a citation cannot supply — prove the spans FOLLOW the registry rather than merely
    matching it. Commit, then mutate ONE registry row in a field `deriveResidual` actually emits, run,
    and watch both byte comparisons go red because the generated form moved and the shipped forms did
    not. Paste the RED titles and the full assertion messages, restore, cross-check with
    `git diff --exit-code`. A row mutation that changes no generated output proves nothing and must be
    replaced with one that does.

    ROW 2 — DRIFT-DETECTABLE. TWO COPIES, TWO SEPARATE MUTATIONS, NEVER COMBINED. Task 1 already planted
    drift inside the machine-owned span in `.planning/REQUIREMENTS.md` and watched the ledger comparison
    go red in this session; cite that proof by its pasted title and message rather than re-running it.
    Here, plant drift inside the gate header's copy — one byte is enough — run, watch THAT comparison go
    red on its own, paste the title and the full message, restore, cross-check. One mutation covering
    both copies would prove neither guard individually, which is the whole reason there are two cases.

    ROW 3 — THE SOLE BOUND. THIS IS THE CRITERION THAT CAN HONESTLY FAIL, AND IT IS VERIFIED AS WAVE 33's
    OUTCOME RATHER THAN ASSUMED FROM WAVE 33's PLAN. Four checks, all four required, each executed and
    pasted. (a) RUN wave 33's whole-file wrap-tolerant quantifier guard and observe it green. (b)
    INDEPENDENTLY RE-MEASURE its declared-phrasing occurrence counts over the file's own bytes rather
    than reading them out of `01-33-SUMMARY.md`, in both the line-based and the joined,
    prefix-stripped normalizations, and reconcile against wave 33's pinned pair; a disagreement is a
    finding and blocks the row. (c) CHECK EACH OF THE THREE EXCLUSIONS FOR OVER-BREADTH by measuring what
    it actually swallows: enumerate every declared-phrasing occurrence that falls inside each exclusion
    and confirm each one is genuinely machine-owned span, the declaration itself, or a
    `RESOLVER_REGISTRY[].clause`. An exclusion that swallows one hand-written universal has converted a
    guarded surface into an unguarded one and the row is `✗`. (d) COMMIT, then re-plant a declared
    phrasing outside all three exclusions, run, watch the guard go RED, paste the title and the full
    message, restore, cross-check.

    ROW 3 ALSO CARRIES THE CROSS-SURFACE CONTRADICTION CHECK, BECAUSE `SOLE` MEANS SOLE EVERYWHERE A
    READER LOOKS. Re-run Task 1's four-surface enumeration and confirm that no STANDING statement of a
    superseded bar survives unmarked on any of the four surfaces, and that the box's state is asserted in
    exactly two places — the ledger row and `CORE11_BOX_EXPECTED` — which agree. Then NAME BOTH CLASSES
    OF UNREACHED SURFACE IN ONE SENTENCE, as UNGUARDED LIMITS rather than as satisfied checks, because
    they are two different holes and naming only the first is what makes a `✓` overclaim. CLASS ONE,
    UNGUARDED FILES: `.planning/STATE.md` and `.planning/WINDOWS.md` are reached by no mechanical
    comparison at all — the byte comparisons reach the gate header and the ledger and no further — so the
    pointer-not-a-bound rule remains a prohibition with no check for those two files. CLASS TWO,
    UNDECLARED SPELLINGS INSIDE THE GUARDED FILES: wave 33's guard is a PHRASE LIST over BYTES under one
    named normalization, and it says so itself; a hand-written bound spelled in words it does not declare
    stands in the gate file and passes all four checks above unseen. So the verdict is written as
    CRITERION 3 IS MET UP TO THE GUARD'S PHRASE-LIST REACH UNDER ITS NAMED NORMALIZATION, ON THE SURFACES
    AND SPELLINGS A MECHANISM CAN SEE, WITH BOTH CLASSES OF UNREACHED SURFACE NAMED — never as an
    unqualified `✓`. That qualified form is true and checkable; the unqualified form would flip CORE-11's
    box on evidence strictly narrower than the criterion it discharges, which is a stated reach exceeding
    an executed reach arriving inside the fix for exactly that defect.

    BUILD THE TABLE IN WAVE 32's COLUMN SHAPE — ONE ROW PER CRITERION, NOT ONE PER ENUMERATED CLAUSE.
    Columns: the criterion; the executed evidence that satisfies it; the artifact the evidence was
    measured against; the mutation that proved that evidence live; the verdict. Cite the old eight-row
    enumerated-surface discharge exactly once, as context showing which bar changed, and state plainly
    that it is superseded and is not the acceptance test here.

    READ THE BOX OFF THE TABLE. If and only if all three rows verdict `✓`, move CORE-11's ledger row and
    `CORE11_BOX_EXPECTED` in the SAME COMMIT — the constant's own message forbids moving it alone, and
    moving it alone is how a pin becomes decoration. If ANY row is `✗`, the box stays `[ ]`,
    `CORE11_BOX_EXPECTED` stays `[ ]`, the blocking criterion is named in the ledger with its failing
    check, and the wave is a success: `[ ]` is a MEASURED RESULT with a named blocking row, not a
    deferral and not a failure of this plan. Waves 19, 28 and 32 each wrote that outcome up correctly and
    each was later judged right to. Whichever way it goes, append the verdict to the 2026-08-25
    correction as a continuation of it rather than as a separate later paragraph, so a reader meets the
    decision and its discharge together.

    THE BOX-FLIP MUTATION IS MANDATORY IN EITHER OUTCOME. Commit first. Then move the ledger row away
    from whatever `CORE11_BOX_EXPECTED` now pins, run, watch the box case go RED, and paste the FULL
    assertion message — it contains both revert hashes, `e7cc4b6` and `faca607`, and wave 32 pasted it
    for the same reason. Restore, cross-check with `git diff --exit-code`. A pin nobody watched fail in
    this session is a pin this session may not rely on, and this is the pin that exists because the box
    was flipped early twice.

    STATE THE NARROWNESS ON EVERY SURFACE THAT STATES THE BAR. Wherever the three criteria appear — the
    ledger correction, the constant's message, both pointer amendments — the sentence saying the new bar
    is narrower than the old one and ends no class appears with them. No sentence added by this task may
    state or imply that the new bar makes the gate complete; CR-15, CR-16 and the measured silences are
    named residuals under it.

    APPEND THE POINTER AMENDMENTS, RESTATING NO BOUND. `.planning/STATE.md` gets one naming the operator
    decision, its date, its three criteria and the box's resulting state — as a POINTER at the ledger and
    the derived span, never as a copy of either. Show the previous amendments byte-identical by diff.
    Supersede the open `.planning/WINDOWS.md` entry for this gate file and append a new pointer entry
    THROUGH `gsd-tools windows`, never by hand-editing the table, and paste every command with its raw
    output.

    RUN WAVE 33's GUARD AFTER EVERY COMMIT IN THIS TASK, NOT AT THE END, and touch none of its
    exclusions, its exemption map or its pinned counts.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY. This repo has no active git hooks, so nothing runs it
    for you. And state the must-NOT plainly with its evidence rather than leaving it inferred: zero
    outbound-shaped tokens in shipped non-spec source across both roots and the frontend, one import
    specifier in the shipped bundle, `observations.ts`'s redaction byte-identical since round 5 across
    19,772 swept inputs, and the real tree at 23 files and 0 violations with the four measured-exempt
    sites named. Every finding this wave touched is a prospective blindness in a test-only gate or a
    disclosure defect.

    RECORD EVERY PREDICTION-VS-MEASUREMENT DISCREPANCY IN THE SHAPE `01-27-SUMMARY.md` USES. This plan
    predicts rows 1 and 2 re-verify green and that row 3 turns on the exclusions being non-vacuous and
    not over-broad. If any prediction is wrong, THE DIFFERENCE IS THE FINDING, it is written down before
    the box is touched, and the box decision is read off the measurement rather than off the prediction.
  </action>

  <verify>
    <automated>cd /Users/six2dez/Tools/DefMiner && grep -cE '^- \[[ x]\] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md && pnpm test 2>&1 | tail -15 && pnpm typecheck && pnpm lint && pnpm knip && pnpm build:backend && pnpm check:bundle 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Both byte comparisons executed and pasted WITH A TIMESTAMP before the first criterion row was
      written, and again at the end of the task. Line count, entry count and sha256 pasted for the
      generated form and for both files, with the no-trailing-newline hashing convention stated.
    - ROW 1 executed in both steps: the equality shown green, AND a one-row registry mutation planted in a
      field `deriveResidual` emits, run, both byte comparisons observed RED, both RED titles and full
      assertion messages pasted, restored, `git diff --exit-code` clean. If the first field chosen
      produced no change in generated output, that is stated and a field that does was used instead.
    - ROW 2 executed on BOTH copies separately: Task 1's ledger-copy drift proof cited by its pasted title
      and message, and a NEW header-copy drift mutation run in this task with its own RED title and full
      assertion message pasted, restored, `git diff --exit-code` clean. The two are never combined.
    - ROW 3 executed in all four checks with output pasted for each: (a) wave 33's guard run and green;
      (b) its declared-phrasing counts INDEPENDENTLY re-measured in both normalizations and reconciled
      against wave 33's pinned pair, with any disagreement named as a finding; (c) every declared-phrasing
      occurrence falling inside each of the three exclusions enumerated and classified as machine-owned
      span, the declaration itself, or a `RESOLVER_REGISTRY[].clause` — with the count per exclusion
      pasted; (d) a declared phrasing re-planted outside all three exclusions, run, observed RED, title
      and full message pasted, restored, `git diff --exit-code` clean.
    - ROW 3's cross-surface contradiction check pasted: Task 1's four-surface enumeration re-run showing
      zero unmarked STANDING statements of a superseded bar, and the box's state asserted in exactly two
      places which agree. BOTH CLASSES OF UNREACHED SURFACE named in one sentence as unguarded limits
      rather than as satisfied checks: unguarded FILES — `.planning/STATE.md` and `.planning/WINDOWS.md`,
      reached by NO mechanical comparison — and undeclared SPELLINGS inside the guarded files, which
      wave 33's phrase-list guard cannot see under any normalization.
    - ROW 3's verdict is written in its SCOPED form and quoted verbatim in the SUMMARY: met UP TO THE
      GUARD'S PHRASE-LIST REACH UNDER ITS NAMED NORMALIZATION, with both classes of unreached surface
      named. An unqualified `✓` on Row 3 is a FAILING outcome for this criterion even when all four
      checks passed, because it would flip the box on evidence strictly narrower than the criterion it
      discharges.
    - The three-row discharge table is pasted in wave 32's column shape, one row per criterion, each with
      its executed evidence, the artifact measured, the mutation that proved it live, and a verdict. The
      superseded eight-row enumerated-surface discharge is cited exactly once as context and is not used
      as the acceptance test.
    - The box decision is READ OFF the table. If all three verdict `✓`, `git show` proves CORE-11's ledger
      row and `CORE11_BOX_EXPECTED` moved in the SAME COMMIT. If any row is `✗`, the box and the constant
      are shown byte-unchanged by diff and the blocking criterion is named in the ledger with its failing
      check quoted.
    - Executed `grep -cE '^- \[[ x]\] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md`, observed exactly 1.
      This gate is STATE-AGNOSTIC ON PURPOSE so an honest `[ ]` cannot fail it, and the SUMMARY says so
      in one sentence naming WR-34 and stating that the SUITE's pin stays state-sensitive through
      `CORE11_BOX_EXPECTED`.
    - THE BOX-FLIP MUTATION executed regardless of outcome: the ledger row moved away from what the
      constant pins, run, observed RED, the FULL assertion message pasted showing both `e7cc4b6` and
      `faca607`, restored, `git diff --exit-code` clean, and `git log --oneline -1` pasted before it
      showing the real work committed first.
    - Every surface stating the three criteria also carries the narrowness sentence, confirmed by a
      positive grep per surface with its count pasted, and the SUMMARY quotes the sentence verbatim from
      the ledger correction and from the constant's message.
    - NO SENTENCE ADDED ANYWHERE BY THIS WAVE STATES OR IMPLIES THAT THE NEW BAR MAKES THE GATE COMPLETE.
      Every sentence this wave added to `.planning/REQUIREMENTS.md`,
      `packages/backend/src/outbound-prohibition.spec.ts`, `.planning/STATE.md` and
      `.planning/WINDOWS.md` is re-read against that prohibition and the review is recorded as a
      one-line-per-surface verdict in the SUMMARY. If any sentence had to be rewritten to satisfy it, the
      before and after are both pasted — that rewrite is a finding, because it is this phase's signature
      defect arriving inside its own closing wave.
    - `git diff` over the STORE-03 and STORE-07 entries shown EMPTY at the end of the task, output pasted,
      and no sentence added by this wave claims the decision reaches either.
    - Wave 33's guard executed after EVERY commit in this task, green each time, and `git diff` over its
      exclusions, its exemption map and its pinned counts shown EMPTY.
    - The five mutation proofs are presented as one TABLE with the run order stated, showing for each: the
      mutation, the RED test title, the full assertion message, the restore, the re-run green, and the
      `git diff --exit-code` between it and the next. None is combined with another.
    - `.planning/STATE.md` carries a new pointer amendment restating no bound; the previous ones shown
      byte-identical by diff.
    - Every `gsd-tools windows` command pasted with raw output; the open entry for this gate file
      superseded and one new pointer entry appended; the resulting open-entry count stated.
    - Executed `pnpm test`, exit 0, file and test counts pasted, neither lower than the pre-wave baseline
      of 31 files / 1345 tests.
    - Executed `pnpm typecheck`, `pnpm lint`, `pnpm knip` — all exit 0, output pasted.
    - Executed `pnpm build:backend && pnpm check:bundle`, observed exactly one import specifier, `crypto`.
    - Executed the real-tree run over both `SOURCE_ROOTS`, observed 23 files and 0 violations, with the
      four measured-exempt sites named: `compat.ts`'s `cur[key]` and `ctx[root]`, and `observations.ts`'s
      `segments[i]` and `MIGRATIONS[MIGRATIONS.length - 1]`.
    - Executed `git diff --exit-code` over `01-01-PLAN.md` through `01-34-PLAN.md`, `01-VERIFICATION.md`,
      `01-REVIEW.md` and `01-UAT.md` — clean, output pasted.
    - Any prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses; if there
      were none, that is stated explicitly rather than left silent.
  </acceptance_criteria>

  <done>
    CORE-11's discharge is a three-row table against the criteria the operator adopted, every row proved
    by a mutation executed in this session rather than by a citation; the box is whatever that table says
    and its two statements agree; the pin was watched failing; and both append-only histories point at
    the decision without restating a bound.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a bar the OWNER re-scoped to a bar a PLANNER lowered | The load-bearing boundary of this wave. The same edit, recorded two ways, is either a legitimate scope decision or the phase's signature defect committed in the act of closing it. What separates them is a date, an attribution, a stated reason and a named precedent — and nothing else. |
| the CHECKBOX's meaning to the PROHIBITION's meaning | The re-scope narrows the first and leaves the second byte-identical. A reader who conflates them reads a weakened security requirement where none exists, and the ledger correction is written so they cannot. |
| the gate's REACH to the gate's DESCRIPTION OF ITSELF | The reach cannot be completed over an open language; the description can be made derived, drift-detectable and singular. Six rounds spent themselves on the wrong side of this boundary and the new bar sits deliberately on the right side of it. |
| a criterion VERIFIED to a criterion CITED | Criteria 1 and 2 were satisfied in earlier waves under a bar that has since changed. Re-running the comparisons and watching mutations go red is verification; quoting `01-27-SUMMARY.md` is citation, and citation is what `faca607` reverted. |
| a span that MATCHES the registry to a span DERIVED from it | Byte equality is consistency. Only a registry mutation that moves the generated form and leaves the shipped forms behind tells derivation from coincidence. |
| criterion 3 as wave 33's PLAN to criterion 3 as wave 33's OUTCOME | A plan states what a guard will do; only running it, re-measuring its counts and enumerating what its exclusions swallow states what it does. |
| an exclusion that is NON-VACUOUS to one that is NOT OVER-BROAD | Wave 33 asserts the first. The second is a different property and this wave measures it: an exclusion swallowing one hand-written universal converts a guarded surface into an unguarded one while every assertion stays green. |
| the surfaces a mechanism can see to the surfaces it cannot | The byte comparisons reach the gate header and the ledger and no further. `.planning/STATE.md` and `.planning/WINDOWS.md` are governed by a prohibition with no check, and criterion 3 is met on the guarded surfaces with the unguarded two named rather than counted. |
| a `[ ]` that is a MEASURED RESULT to one that is a DEFERRAL | Waves 19, 28 and 32 wrote the first and were each judged right. An acceptance criterion that punishes an open box would convert this wave's honest outcome into a failure it must engineer around. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-200 | Repudiation | the re-scope recorded as anything less than a dated owner's decision | critical | mitigate | A correction dated 2026-08-25, attributed to plan 01-35 and wave 35, naming THE OPERATOR as the decider, the unreachability of the old bar over an open language as the reason, six rounds of flat find-rate as the evidence, and the STORE-01 → STORE-08 precedent by id as the route. Quoted verbatim in the SUMMARY. |
| T-01-201 | Spoofing | the re-scope read as a weakened prohibition | critical | mitigate | CORE-11's first sentence byte-identical, shown by an EMPTY diff, and the correction states in the same passage that what narrows is the checkbox and not the must-NOT, with the must-NOT's evidence named. |
| T-01-202 | Spoofing | any sentence implying the new bar makes the gate complete | high | mitigate | Prohibited in frontmatter, in the objective, in an acceptance criterion per task, and on every surface stating the bar — each of which carries the narrowness sentence beside it, verified by a positive grep per surface. CR-15, CR-16 and the measured silences are named residuals under the new bar. |
| T-01-203 | Repudiation | a criterion discharged by citation rather than execution | high | mitigate | Row 1 proved by a registry-row divergence mutation; row 2 by two separate drift mutations, one per copy; row 3 by running the guard, independently re-measuring its counts, enumerating what each exclusion swallows, and re-planting a phrasing outside them. Five mutations, executed separately, never combined. |
| T-01-204 | Tampering | an over-broad wave-33 exclusion turning a guarded surface unguarded while every assertion stays green | high | mitigate | Every declared-phrasing occurrence inside each of the three exclusions enumerated and classified as machine-owned span, the declaration itself, or a `RESOLVER_REGISTRY[].clause`, with the per-exclusion count pasted. One unexplained occurrence makes row 3 `✗`. |
| T-01-205 | Tampering | `CORE11_BOX_EXPECTED` moved without the ledger row, or the ledger row without the constant | high | mitigate | Both move in the SAME COMMIT, proved by `git show`, and only after all three rows verdict `✓`. The constant's own failure message forbids the alternative and that clause survives Task 1's rewrite. |
| T-01-206 | Repudiation | a box flipped a third time on evidence that later proves false | high | mitigate | Three executed criterion rows read off a table, the mandatory box-flip mutation pasting an assertion message that names `e7cc4b6` and `faca607`, and `[ ]` written into the plan as a first-class outcome so the flip is never the path of least resistance. |
| T-01-207 | Spoofing | two terminal conditions standing side by side after the re-scope | high | mitigate | The constant's superseded promise is REPLACED rather than appended to; every STANDING statement of the old bar is superseded in place by a scoped marker; the four-surface enumeration is re-run in Task 2 to confirm none survives unmarked. |
| T-01-208 | Tampering | a supersession marker reaching past the sentence it supersedes | medium | mitigate | Each marker names which sentence it reaches and states the remainder stands; each is shown by diff to be a pure insertion. The paragraph it sits in also carries a true limits list and the true STORE-03 / STORE-07 deferral, both of which must survive intact. |
| T-01-209 | Spoofing | this decision read as reaching STORE-03 or STORE-07 | medium | mitigate | Both entries shown byte-identical by an EMPTY diff at both task boundaries, and the correction restates their deferral with its owner and route in wave 28's own words: one flipped box is not a clean ledger. |
| T-01-210 | Repudiation | history rewritten to make the new bar look like it was always the bar | medium | mitigate | Two-valued disposition rule: dated and plan-attributed occurrences stay byte-identical as history; standing ones are marked in place with their bytes preserved below the marker. `grep -c 'When this box is eventually'` stays at 2. |
| T-01-211 | Denial of Service | an acceptance criterion that punishes an honest open box | medium | mitigate | The task-level automated verify is state-agnostic (`grep -cE '^- \[[ x]\] \*\*CORE-11\*\*'`), the box criterion is conditional on the table, and the box-flip mutation is mandatory in either outcome. Named as NOT a repeat of WR-34, whose defect was a state-agnostic class in the SUITE's pin. |
| T-01-212 | Information Disclosure | the two reader surfaces no mechanism reaches | medium | accept | `.planning/STATE.md` and `.planning/WINDOWS.md` are governed by the pointer-not-a-bound prohibition with no mechanical check; the byte comparisons reach the gate header and the ledger and no further. Stated as an unguarded limit wherever criterion 3 is claimed, never counted as a passing check. Carried forward from T-01-144 unchanged. |
| T-01-187 | Tampering | mutating uncommitted task work | high | mitigate | Every task commits its real work before planting any mutation and cross-checks with `git diff --exit-code` between them. Wave 29 destroyed its own Task 1 exactly this way; waves 30 through 34 lost nothing using commit-then-mutate. Carried forward unchanged. |
| T-01-188 | Tampering | a gate set nobody ran | medium | mitigate | No active git hooks in this repo — `.git/hooks` holds only samples and `core.hooksPath` is unset — so the full set runs by hand at every task boundary with its output pasted. Carried forward unchanged. |
| T-01-198 | Tampering | this wave weakening wave 33's guard to get green | high | mitigate | New prose is rewritten rather than exempted; the guard runs after every edit and after every commit; `git diff` over its exclusions, its exemption map and both pinned counts asserted EMPTY. Carried forward from wave 34 unchanged. |
| T-01-51 | Information Disclosure | the accepted residual of the outbound walk | medium | mitigate | Unchanged by this wave — no registry row moves outside a reverted mutation — and byte-pinned into the ledger by the two comparisons, which are run before the first criterion row and again at the end. |
| T-01-50 | Information Disclosure | `.spec.ts` files, which the walk skips | low | accept | Unchanged and carried forward, bounded by `pnpm check:bundle`, which specs never enter. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | NOT APPLICABLE: this plan installs no package. `pnpm install` is not run and no manifest appears in `files_modified`. RESEARCH.md's Package Legitimacy Audit is present and unchanged; no `[ASSUMED]` or `[SUS]` package is introduced, so no blocking human checkpoint is required. |
</threat_model>

<verification>
1. `git rev-parse HEAD` cross-checked against any git-status snapshot before the first edit, and
   `git status --porcelain packages/ .planning/` clean.
2. Waves 33 and 34 executed: both SUMMARYs present, wave 33's guard and exemption map present, the suite
   green, and BOTH byte comparisons green BEFORE the first edit. Halt if either is red.
3. The four-surface enumeration executed and pasted, every occurrence classified HISTORY or STANDING by
   the two-valued rule, with the planning-time counts (2 in the ledger, 1 in the gate file) reconciled
   and any disagreement named as a finding.
4. The 2026-08-25 correction present above the generated-residual begin sentinel and below wave 34's,
   naming the OPERATOR, the reason, the STORE-01 → STORE-08 route by id, and the three numbered criteria.
5. The narrowness sentence present in the same passage and on every other surface stating the bar,
   confirmed by a positive grep per surface.
6. CORE-11's first sentence byte-identical, shown by an EMPTY diff; the must-NOT's evidence restated.
7. STORE-03 and STORE-07 entries byte-identical, shown by an EMPTY diff at both task boundaries.
8. `grep -c 'When this box is eventually' .planning/REQUIREMENTS.md` observed 2 — unchanged; each
   occurrence disposed of by rule, the standing one carrying a scoped insertion-only marker.
9. `grep -c 'probes every surface' packages/backend/src/outbound-prohibition.spec.ts` observed 0, with
   the replacement prose written not to contain the superseded literal in any form.
10. `CORE11_BOX_EXPECTED`'s message quoted before and after, the three criteria replacing the superseded
    promise, both revert hashes and the WR-34 explanation and the same-commit rule surviving.
11. Neither sentinel marker line reproduced in the new correction prose, confirmed by the extraction
    cases running green.
12. ROW 1 executed: equality green AND a registry-row divergence mutation observed RED on both
    comparisons, titles and full messages pasted, restored.
13. ROW 2 executed on both copies separately: Task 1's ledger-copy drift proof cited by its pasted title
    and message, plus a new header-copy drift mutation with its own RED title and full message.
14. ROW 3 executed in all four checks: guard green; counts independently re-measured in both
    normalizations and reconciled; every declared-phrasing occurrence inside each exclusion enumerated
    and classified with per-exclusion counts; a phrasing re-planted outside the exclusions observed RED.
15. ROW 3's cross-surface contradiction check: no unmarked STANDING statement of a superseded bar on any
    of the four surfaces, the box's state asserted in exactly two agreeing places, and BOTH classes of
    unreached surface named — unguarded FILES (`.planning/STATE.md`, `.planning/WINDOWS.md`, reached by
    NO mechanical comparison) and undeclared SPELLINGS inside the guarded files, invisible to a
    phrase-list guard under any normalization. ROW 3's verdict recorded in its SCOPED form; an
    unqualified `✓` fails this item.
16. The three-row discharge table pasted in wave 32's column shape, the superseded eight-row discharge
    cited once as context and not used as the acceptance test.
17. The box read off the table. All three `✓` → ledger row and `CORE11_BOX_EXPECTED` moved in the SAME
    COMMIT, proved by `git show`. Any `✗` → both byte-unchanged and the blocking criterion named in the
    ledger with its failing check quoted.
18. `grep -cE '^- \[[ x]\] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md` observed exactly 1 — state-agnostic
    on purpose, with the WR-34 distinction stated in one sentence.
19. THE BOX-FLIP MUTATION executed in either outcome, its full assertion message pasted showing both
    `e7cc4b6` and `faca607`, restored, `git diff --exit-code` clean.
20. Five mutation proofs presented as ONE TABLE with the run order stated, each with its RED title, full
    assertion message, restore and re-run green, none combined, `git log --oneline -1` before each.
21. Wave 33's guard green after every edit and every commit, and `git diff` over its exclusions, its
    exemption map and both pinned counts EMPTY.
22. `.planning/STATE.md` pointer amendment present with the previous ones byte-identical;
    `.planning/WINDOWS.md` entry superseded and a pointer entry appended through the tool, commands and
    raw output pasted.
23. `pnpm test` — exit 0, no fewer than 31 files and 1345 tests.
24. `pnpm typecheck`, `pnpm lint`, `pnpm knip` — all exit 0.
25. `pnpm build:backend && pnpm check:bundle` — exactly one import specifier, `crypto`.
26. Real-tree run over both `SOURCE_ROOTS` — 23 files, 0 violations — with the four measured-exempt sites
    named.
27. `git diff --exit-code` over `01-01-PLAN.md` through `01-34-PLAN.md`, `01-VERIFICATION.md`,
    `01-REVIEW.md`, `01-UAT.md` — clean.
28. Every prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses, or an
    explicit statement that there were none.
</verification>

<success_criteria>
- A reader opening `.planning/REQUIREMENTS.md` cold can tell WHO re-scoped CORE-11's acceptance bar, WHEN,
  WHY, and BY WHAT ROUTE, from the ledger alone and without reading a plan or a summary.
- The reason recorded is the true one — the old bar was unreachable over an open language, not merely
  unmet — and six rounds of flat find-rate are cited as the evidence for it.
- CORE-11's prohibition is byte-identical and a reader is told so in the same passage as the re-scope, so
  the decision cannot be mistaken for a weakened security requirement.
- Each of the three criteria carries evidence EXECUTED in this session and a mutation that proved that
  evidence live, not a citation of the wave that first satisfied it.
- Criterion 3 was verified as wave 33's outcome — its guard run, its counts independently re-measured,
  its exclusions enumerated for over-breadth — and was free to fail.
- Criterion 3's verdict is stated with the reach of the mechanism that produced it: met UP TO THE
  GUARD'S PHRASE-LIST REACH UNDER ITS NAMED NORMALIZATION, with BOTH classes of unreached surface named
  beside it — unguarded FILES (`.planning/STATE.md`, `.planning/WINDOWS.md`) and undeclared SPELLINGS
  inside the guarded files. No verdict anywhere in this wave claims a reach its mechanism does not have.
- The box is whatever the three-row table says. If it is `[x]`, it moved with `CORE11_BOX_EXPECTED` in one
  commit after three `✓` rows. If it is `[ ]`, the blocking criterion is named and that is a correct
  outcome, recorded as a measured result rather than as a deferral.
- No sentence anywhere states or implies that the new bar makes the gate complete; every surface stating
  the bar states its narrowness beside it, and CR-15, CR-16 and the measured silences are named residuals
  under it.
- STORE-03 and STORE-07 are byte-identical and still deferred with their owner, and the ledger says in its
  own words that one flipped box is not a clean ledger.
- The green baseline is restored and recorded: no fewer than 31 files / 1345 tests, typecheck / lint /
  knip clean, bundle at exactly one specifier, real tree at 23 files / 0 violations, and nothing leaks.
</success_criteria>

<artifacts_this_phase_produces>
| Artifact | Kind | Purpose |
|----------|------|---------|
| the 2026-08-25 OPERATOR DECISION in `.planning/REQUIREMENTS.md` | corrected disclosure | The re-scope of CORE-11's acceptance bar recorded as its owner's, with its date, its reason and the STORE-01 → STORE-08 route — so a reader can tell a deliberate scope decision from a quietly lowered bar |
| the three criteria, stated in the ledger and in `CORE11_BOX_EXPECTED`'s message | corrected disclosure | DERIVED, DRIFT-DETECTABLE, THE SOLE BOUND — the current terminal condition, stated where a future session reads it, replacing rather than standing beside the superseded one |
| the four-surface enumeration table | new evidence | Every standing statement of the old bar on every reader surface, found by grep and classified HISTORY or STANDING by a two-valued rule, so none survives unmarked by accident |
| the scoped supersession markers | corrected disclosure | The old bar superseded in place with its bytes intact and its paragraph's true limits list and true STORE-03 / STORE-07 deferral left standing |
| the three-row discharge table | new evidence | One row per criterion in wave 32's column shape, each carrying executed evidence, the artifact measured, the mutation that proved it live, and a verdict |
| the registry-row divergence mutation | new evidence | Proves the spans are DERIVED from the registry rather than merely consistent with it — the one step a citation cannot supply |
| the two drift mutations, one per copy | new evidence | Proves each byte comparison individually; a single mutation covering both copies would prove neither |
| the exclusion over-breadth enumeration | new evidence | Wave 33 asserts its exclusions non-vacuous; this measures whether they are also not OVER-BROAD, which is a different property and the one that can fail |
| the re-planted declared phrasing mutation | new evidence | Criterion 3's keeper watched going red in this session rather than cited from wave 33's summary |
| the box-flip mutation proof | new evidence | Mandatory in either outcome, pasting the assertion message that names both `e7cc4b6` and `faca607` — the pin that exists because the box was flipped early twice |
| CORE-11's checkbox, moved or held | corrected ledger | Read off the table, moved with `CORE11_BOX_EXPECTED` in one commit if and only if all three rows verdict `✓`, held with its blocking criterion named otherwise |
| `.planning/STATE.md` pointer amendment and the `.planning/WINDOWS.md` supersession | corrected ledger | Both append-only histories point at the decision and the derived span and restate no bound of their own |
| `01-35-SUMMARY.md` | new evidence | Every enumeration, every criterion row, the five mutation proofs as one table, both span digests, the full gate set, and the sentence that the new bar is narrower and ends no class |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-skeleton-persistence-compatibility/01-35-SUMMARY.md` when done.

It must contain, as pasted terminal output rather than as prose: the precondition assertions with
`git rev-parse HEAD`; both byte comparisons with their timestamp BEFORE the first criterion row and
again at the end, with line count, entry count and sha256 from the generated form and both files and the
no-trailing-newline hashing convention stated; the four-surface enumeration table with every occurrence
classified and the planning-time counts reconciled; the EMPTY diffs over CORE-11's first sentence and
over the STORE-03 and STORE-07 entries; `CORE11_BOX_EXPECTED`'s message quoted in full before and after;
the three-row discharge table in wave 32's column shape; ROW 3's four checks with the per-exclusion
occurrence counts; ROW 3's cross-surface contradiction check with `.planning/STATE.md` and
`.planning/WINDOWS.md` named as reached by no mechanical comparison; the FIVE mutation proofs as ONE
TABLE with the run order stated, each carrying its RED test title, its full assertion message, its
restore and its re-run green, with `git diff --exit-code` shown clean between them and
`git log --oneline -1` before each; the wave-33 guard green after every commit with an EMPTY diff over
its exclusions, its exemption map and its pinned counts; the real-tree zero with all four measured-exempt
sites named; every `gsd-tools windows` command with its raw output; and the final `pnpm test`,
`typecheck`, `lint`, `knip` and `check:bundle` results.

It must quote VERBATIM, from the ledger correction, the sentences naming the OPERATOR as the decider,
stating the old bar unreachable over an open language rather than merely unmet, naming the
STORE-01 → STORE-08 precedent as the route, and numbering the three criteria.

It must state in one sentence that the new bar is NARROWER than the old one, that it ends no class, that
CR-15, CR-16 and the measured silences are named residuals under it, and that the class is open because
the space of JavaScript spellings is open. And in one further sentence that what narrowed is the
CHECKBOX's meaning and not the PROHIBITION's, which is byte-identical and still holds.

It must state in one sentence that STORE-03 and STORE-07 remain DEFERRED WITH THEIR OWNER by the
STORE-01 → STORE-08 route, untouched by this decision, and that one flipped box is not a clean ledger.

It must record the box decision as read off the table — and if the box stayed `[ ]`, it must say plainly
that this is a MEASURED RESULT with a named blocking criterion, not a deferral and not a failure of this
plan, in the terms waves 19, 28 and 32 used. It must also state in one sentence why this plan's own
verify is state-agnostic while the SUITE's pin stays state-sensitive, naming WR-34 so the two are not
confused.

It must preserve as history — in the SUMMARY, which is the one place that is not a bound — every
superseded claim this wave marked, including the old acceptance bar in its original wording, so the six
rounds that worked toward it are not erased.

It must also record any PREDICTION-VS-MEASUREMENT discrepancy in the shape `01-27-SUMMARY.md` uses, and
if there were none, say so explicitly.
</output>
