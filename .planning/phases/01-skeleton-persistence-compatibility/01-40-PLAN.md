---
phase: 01-skeleton-persistence-compatibility
plan: 40
type: execute
wave: 40
gap_closure: true
depends_on: [01-39]
files_modified:
  - packages/backend/src/outbound-prohibition.spec.ts
# THIS PLAN IS CR-21 AND WR-51, AND BOTH ARE ABOUT AN ASSERTION THAT CANNOT FAIL.
#
# CR-21 — THE SENTINEL DEFEATS THE CASE ADDED IN THE SAME COMMIT TO FORBID IT. BLOCKER.
# `NO_PRECEDING_CONSTRUCT = "!NO-PRECEDING-CONSTRUCT!"` (`:9167` at HEAD `b8c3d81`) is made of letters,
# so `nameableRemainder` strips its punctuation and returns `NOPRECEDINGCONSTRUCT`, length 20. The case
# titled `no exemption key's ANCHOR reduces to nothing` (`:9971`) asserts
# `nameableRemainder(anchor).length` is greater than zero — so it PASSES the sentinel key. That case was
# added in the SAME WAVE as the sentinel, to forbid exactly the shape the sentinel produces. The
# verifier ran the arithmetic rather than reading it, then mutated in two steps against the real file:
#   STEP 1 — planted a bare docblock whose occurrence line masks entirely to `{q2}` so the forward walk
#            finds nothing nameable and the builder returns the sentinel, plus the matching entry
#            `"!NO-PRECEDING-CONSTRUCT! §§ {q2} :: q2"`. RESULT: 434 passed (434).
#   STEP 2 — with the exemption entry UNCHANGED, moved that docblock ~1,800 lines into the middle of an
#            `it.each` parameter table, a completely unrelated construct. RESULT: 434 passed (434).
# `"!NO-PRECEDING-CONSTRUCT! §§ {q2} :: q2"` is precisely CR-17's `"{q2} :: q2"` shape with twenty
# letters of decoration bolted on. It names no construct and no line, and it is laundered through the
# ONE case that exists to catch it.
#
# AND NOTHING OBSERVES THE SENTINEL. `grep -n NO_PRECEDING_CONSTRUCT` returns exactly FOUR hits: the
# declaration and the two `return` sites. No `expect`, no assertion. So the constant's docblock is
# wrong on TWO counts, and the verifier named both: it does not describe the `:9251` return, which
# fires when a construct DID resolve but named nothing; and it claims an observability the code does
# not have.
#
# WR-51 — THE COUNTER-PROBE PINS NOTHING. WARNING, CONFIRMED BY MUTATION.
# The verifier replaced the body of `preAnchoringExemptionKeyForFixtureOnly` (`:9304-9315`) with
# `return "CONSTANT";` and ran the gate suite: 434 passed (434). The counter-probe at `:10126-10152`
# asserts only that the pre-anchoring builder produces the SAME key across the relocation and that
# `missing` and `stale` are both empty — all of which any constant function satisfies. So the
# docblock's structural claim at `:9297-9303` — that the helper is `exemptionKeyFor` with the construct
# half removed — is asserted NOWHERE, and nothing keeps the two builders in step. The prescribed fix is
# to pin the counter-probe to THE LIVE BUILDER'S LINE HALF, so the contrast is against the format CR-17
# was actually measured in rather than against any constant.
#
# BOTH OF CR-21's FIXES ARE GREEN ON ARRIVAL AND THAT IS THE POINT, NOT AN OBJECTION. The verifier
# measured ZERO shipped occurrences resolving to the sentinel today. So excluding the sentinel from
# `nameableRemainder`'s notion of naming something, and asserting the sentinel is never produced on the
# scanned surface, cost NOTHING now and are a real guard from the next sentence somebody writes. A
# guard that is green on arrival is not a guard that does nothing — but it IS a guard nobody has
# watched failing, so every one of them is planted against in this plan before it is trusted.
#
# ONE HANDOFF FACT ARRIVES FROM PLAN 01-39 AND MUST BE READ BEFORE THE FIRST EDIT. If plan 01-39's
# Task 1 took WR-53's forward-walk extension, the population of occurrences resolving to the sentinel
# MAY HAVE MOVED. `01-39-SUMMARY.md` is required to state that count in one sentence, labelled as a
# handoff fact. THIS PLAN RE-MEASURES IT ANYWAY AND DOES NOT TRUST THE NUMBER. If it is non-zero, the
# `sentinel is never produced` assertion is NOT green on arrival, and that is a finding to be written
# up and disposed of by rewriting or re-siting the occurrence — NEVER by softening the assertion.
#
# WHAT THIS PLAN MUST NOT DO.
#   - DO NOT FLIP CORE-11's CHECKBOX AND DO NOT REVERT IT EITHER. It reads `[ ]`, `CORE11_BOX_EXPECTED`
#     already agrees, and pass 9's determination is that it may not move. `requirements mark-complete`
#     stays UNRUN. This plan opens no file under `.planning/`.
#   - DO NOT RE-OPEN THE 2026-08-25 RE-SCOPE.
#   - DO NOT DELETE THE SENTINEL OR REPLACE IT WITH AN EMPTY STRING. An empty anchor is the shape the
#     mechanism exists to forbid; the sentinel is NAMED on purpose so a produced empty anchor is
#     visible rather than silent. What is wrong is that `nameableRemainder` counts its letters as
#     naming something, and that nothing observes it. Fix those two things, keep the constant.
#   - DO NOT WEAKEN THE OUTBOUND GATE TO GET GREEN. `nameableRemainder` is consulted in TWO places —
#     the forward walk inside `constructAnchorFor` and the null-anchor case — so a change to it reaches
#     the anchor builder plan 01-39 just corrected. Its effect on the 29 keys is measured before and
#     after, and a key that moves is a finding.
#   - DO NOT TOUCH `constructAnchorFor`'s scan or its docblock, limit (5), the census, the
#     prefix-forbidding case, `CLOSES_FROZEN_ARRAY`, `closingBracketAfter`, the `EXCLUSIONS` array, any
#     band, any `proof` token, `CORE11_BOX_EXPECTED`, the box case, or the header lines at `:803-804`,
#     `:901` and `:911`. Those belong to plans 01-39 and 01-41.
#   - DO NOT WRITE A TENTH OVERCLAIM WHILE FIXING THE NINTH. The sentinel docblock correction is
#     itself an overclaim correction. Every sentence written here gets a no-overclaim verdict.
#
# ENVIRONMENT FACTS, MEASURED DURING PLANNING.
#   - HEAD at planning time: `b8c3d81`; the gate file 10,399 lines. Plan 01-39 edits the same file, so
#     RE-DERIVE every number by identifier and record any disagreement as a finding.
#   - RECONCILIATION ANCHORS at HEAD `b8c3d81`: `nameableRemainder` `:9159-9160`;
#     `NO_PRECEDING_CONSTRUCT`'s docblock `:9163-9166` and its declaration `:9167`;
#     `constructAnchorFor`'s forward-walk test `:9247`; the two sentinel returns `:9251` and `:9253`;
#     `exemptionKeyFor` `:9273`; `preAnchoringExemptionKeyForFixtureOnly`'s docblock `:9288-9303` and
#     its body `:9304-9315`; `EXEMPTION_ANCHOR_SEP` `:9174`; the null-anchor case `:9971-9979`; the
#     cross-construct fixture's counter-probe `:10126-10152`.
#   - `grep -c NO_PRECEDING_CONSTRUCT` returned FOUR at HEAD `b8c3d81`. After this plan it must be
#     strictly greater, because something finally observes the value. Assert the direction, not a
#     target number.
#   - NO ACTIVE GIT HOOKS. COMMIT BEFORE MUTATING — wave 29 destroyed its own Task 1 with
#     `git checkout --`.
#   - BASELINE TO RE-MEASURE RATHER THAN ASSUME: whole suite 31 files / 1374 tests exit 0;
#     `tsc --build` exit 0; `check:bundle` exactly ONE specifier, `crypto`; real-tree walk 23 shipped
#     non-spec modules / 0 violations. The gate suite's own count is whatever plan 01-39 left it at —
#     read it from `01-39-SUMMARY.md` and RE-MEASURE it.
#   - EVERY `<automated>` BLOCK OPENS WITH `set -o pipefail` AND EVERY COUNT IS ASSERTED NUMERICALLY.
#     Round 8 shipped a revision because `pnpm test 2>&1 | tail -8` exits with TAIL's status. Do not
#     strip either, and run the blocks as written.

autonomous: true
requirements: [CORE-11]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low
# `estimate-calibration` reports `{"factor": 1, "applied": false, "sample_count": 0, "min_samples": 3}`,
# so `tokens` equals `raw_tokens` and the confidence is DERIVED from the sample count, never self-rated.
# Weigh the MEASUREMENT COUNT over the token figure: one change to a helper consulted in two places
# with its effect on 29 keys measured, one new surface-wide assertion, two docblock corrections, one
# counter-probe pinned to the live builder, and THREE planted mutations each watched going RED.

must_haves:
  truths:
    - "THE SENTINEL NO LONGER NAMES ANYTHING, AND THE CASE ADDED TO FORBID THE EMPTY-ANCHOR SHAPE FINALLY CATCHES IT. `nameableRemainder` excludes the sentinel from its notion of naming something, so an anchor consisting of the sentinel plus mask tokens reduces to nothing and the `no exemption key's ANCHOR reduces to nothing` case rejects it. The exclusion is written by REFERENCE to the constant, never by re-spelling its letters, so renaming the constant cannot silently un-fix this."
    - "THE CHANGE TO `nameableRemainder` IS MEASURED AGAINST THE 29 KEYS BEFORE IT IS TRUSTED, BECAUSE IT REACHES THE ANCHOR BUILDER. `nameableRemainder` is consulted in two places: the forward-walk test inside `constructAnchorFor`, and the null-anchor case. The 29 keys are regenerated and compared before and after, and any key that moves is a FINDING written up before anything else, not an accepted side effect."
    - "THE SENTINEL IS ASSERTED NEVER TO BE PRODUCED BY AN OCCURRENCE ON THE SCANNED SURFACE. A named case walks every occurrence `quantifierOccurrences` finds over `SURFACE_LINES`, resolves each through the live builder, and asserts none resolves to the sentinel — with non-vacuity asserted before the rule so an empty occurrence set cannot pass it. The verifier measured zero such occurrences today; this plan RE-MEASURES rather than inheriting that, and a non-zero count is a finding disposed of by rewriting or re-siting the occurrence and never by softening the assertion."
    - "BOTH SENTINEL GUARDS ARE WATCHED FAILING, BECAUSE GREEN ON ARRIVAL IS EXACTLY WHEN A GUARD GOES UNTESTED. The verifier's own two-step mutation is reproduced: a sentinel-producing occurrence planted with its matching exemption entry, which went 434/434 green before this plan and must now go RED through the null-anchor case; and the same occurrence relocated into an unrelated construct with the entry unchanged, which must go RED through the surface assertion. Each RED is observed with its FULL message pasted, restored, and the tree proved clean."
    - "THE SENTINEL'S DOCBLOCK IS CORRECTED ON BOTH COUNTS THE VERIFIER NAMED. It must describe the `:9251` return, which fires when a construct DID resolve but named nothing — a case its current text does not cover at all — as well as the exhausted-scan return. And it must no longer claim an observability the code did not have; after this plan the observability EXISTS, so the sentence states which two assertions provide it and stops there."
    - "THE COUNTER-PROBE IS PINNED TO THE LIVE BUILDER'S LINE HALF, SO A CONSTANT FUNCTION FAILS IT. The fixture asserts that the pre-anchoring builder's output equals the LINE HALF of what `exemptionKeyFor` produces for the same input — the portion after `EXEMPTION_ANCHOR_SEP` — so the contrast is drawn against the format CR-17 was actually measured in. The docblock's structural claim that the helper is `exemptionKeyFor` with the construct half removed becomes an assertion rather than a sentence."
    - "THE COUNTER-PROBE PIN IS WATCHED FAILING BY THE VERIFIER'S OWN MUTATION. `preAnchoringExemptionKeyForFixtureOnly`'s body is replaced with a constant, which passed 434 of 434 before this plan, and the new pin must turn RED. Observed, FULL message pasted, restored, tree clean."
    - "`grep -c NO_PRECEDING_CONSTRUCT` STRICTLY EXCEEDS ITS ARRIVAL VALUE, AND THE DIRECTION IS WHAT IS ASSERTED. Four hits at HEAD `b8c3d81`, none of them an observation. The assertion is that the count grew and that at least one of the new hits sits inside an `expect`; no target number is named, because a target number is a thing an executor pads."
    - "THE MUST-NOT DOES NOT MOVE AND NOTHING LEAKS, RE-ESTABLISHED BY EXECUTION. CORE-11's first sentence byte-identical by diff; whole suite exit 0 with the gate walking 23 shipped modules across both `SOURCE_ROOTS` at ZERO violations; `tsc --build` exit 0; the bundle's entire import set ONE specifier, `crypto`; `git diff --name-only` over `packages/` and `scripts/` filtered of `.spec.ts` EMPTY. Every defect here is in a test-only gate's enforcement of its own description."
    - "NOTHING UNDER `.planning/` IS TOUCHED. CORE-11's checkbox does not move in either direction, `requirements mark-complete` is not run, `01-PROBE.md`'s equality `38 == 27 + 11` stands unchanged and is re-stated, and `COVERAGE.md` is neither edited nor extended."
    - "EVERY PREDICTION-VS-MEASUREMENT DISCREPANCY IS RECORDED RATHER THAN ABSORBED, in the shape `01-27-SUMMARY.md` uses. This plan predicts that the sentinel arithmetic scores 20 before the change and 0 after, that the 29 keys survive the `nameableRemainder` change unmoved, and that both sentinel guards are green on arrival. It predicts NOTHING about plan 01-39's handoff sentinel count, which it re-measures."

  artifacts:
    - path: "packages/backend/src/outbound-prohibition.spec.ts"
      provides: "A `nameableRemainder` that excludes the sentinel by reference to the constant; a named case asserting the sentinel is never produced by an occurrence on the scanned surface, with non-vacuity above the rule; a corrected sentinel docblock describing both returns and claiming only the observability that now exists; a counter-probe pinned to the live builder's line half so a constant function fails it; and three planted mutations each watched going RED"
      contains: "NO_PRECEDING_CONSTRUCT"

  key_links:
    - from: "a sentinel scoring 20 on the check written to forbid it"
      to: "a sentinel that reduces to nothing"
      via: "an exclusion in `nameableRemainder` written by reference to the constant rather than by re-spelling its letters, with its effect on the 29 keys measured before and after"
      pattern: "nameableRemainder"
    - from: "a value nothing observes"
      to: "a value two assertions observe"
      via: "a surface-wide case asserting no occurrence resolves to the sentinel, plus the null-anchor case now able to reject a sentinel-bearing key, both watched failing"
      pattern: "NO_PRECEDING_CONSTRUCT"
    - from: "a counter-probe satisfied by any constant function"
      to: "a counter-probe pinned to the live builder"
      via: "asserting the pre-anchoring builder's output equals the portion of `exemptionKeyFor`'s key after `EXEMPTION_ANCHOR_SEP`, watched failing under the verifier's constant-body mutation"
      pattern: "preAnchoringExemptionKeyForFixtureOnly"

  prohibitions:
    - requirement_id: CORE-11
      category: security
      status: resolved
      verification: gate
      resolution: "The 29 keys regenerated and compared before and after the `nameableRemainder` change, with the comparison pasted and any movement written up as a finding before anything else proceeds"
      statement: "MUST NOT weaken the anchor builder while correcting the helper it shares. `nameableRemainder` is consulted by `constructAnchorFor`'s forward walk as well as by the null-anchor case, so a change to it reaches the mechanism plan 01-39 just corrected. Its blast radius is measured, not reasoned about."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "The exclusion is shown by diff to reference the constant identifier; a numeric grep asserts the sentinel's literal letters are not re-spelled anywhere in `nameableRemainder`"
      statement: "MUST NOT re-spell the sentinel's letters inside the exclusion. A literal copy means renaming the constant silently un-fixes CR-21 while every assertion stays green — the same class of defect as a description that no longer matches what it describes."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "The constant remains declared and both return sites still reference it; `git diff` shows no deletion of the declaration and no empty-string substitution"
      statement: "MUST NOT delete the sentinel or replace it with an empty string. It is NAMED on purpose so that a produced empty anchor is visible rather than silent. What was wrong is that its letters counted as naming something and that nothing observed it."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "Three mutations planted after commit — the verifier's step 1, its step 2, and the constant-body substitution — each observed RED with its FULL assertion message pasted and each restored with a clean `git diff --exit-code`"
      statement: "MUST NOT ship a guard that is green on arrival without watching it fail. All three additions here are green the moment they land, which is precisely when a guard goes untested. WR-54 exists because round 8 shipped a correction with no pin."
    - requirement_id: CORE-11
      category: safety
      status: open
      verification: gate
      resolution: "DISCLOSED, NOT CLOSED — the same-construct residual, the anchor's measured reach, the guard's phrase-list reach, its normalization and both classes of unreached surface are named wherever this plan's result is stated"
      statement: "MUST NOT state or imply that closing the sentinel closes the empty-anchor class, or that CR-21 plus CR-20 discharges criterion (3). One laundering route is removed and one unobserved value is observed. A hand-written key can still be constructed to survive both checks; two occurrences under the same construct remain interchangeable; the guard is still a phrase list over bytes under one named normalization; and the unguarded files and undeclared spellings are still unreached. A `✓` that omits any of those is a stated reach exceeding an executed one, and this plan is written not to author the tenth instance while fixing the ninth."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "`git status --porcelain .planning/` shown EMPTY numerically at every task boundary; `requirements mark-complete` present in no command"
      statement: "MUST NOT flip CORE-11's checkbox and MUST NOT revert it either. It reads `[ ]`, the pin agrees, and verification pass 9 determined it may not move. Whether it may move is pass 10's call."
---

<objective>
Close CR-21 — the sentinel that defeats the case added in the same commit to forbid it, and the value
nothing observes — and WR-51, the counter-probe any constant function satisfies.

Purpose: `nameableRemainder` scores `!NO-PRECEDING-CONSTRUCT!` at twenty letters, so the one case that
exists to reject an anchor naming nothing passes the anchor that by construction names nothing. The
verifier drove CR-17's exact `"{q2} :: q2"` shape back through it twice, at 434 of 434 green each time,
with twenty letters of decoration bolted on. Separately, `grep` finds four references to the sentinel
and not one of them is an assertion, so the docblock's claim that the shape cannot be produced silently
names a mechanism that does not exist. And one layer over, the counter-probe that is supposed to prove
the anchoring is what made the difference is satisfied by `return "CONSTANT";` — measured, 434 of 434.

Output: a `nameableRemainder` that excludes the sentinel by reference to the constant; a named case
asserting no occurrence on the scanned surface resolves to it; a sentinel docblock that describes both
returns and claims only the observability that now exists; a counter-probe pinned to the live builder's
line half; and three planted mutations, each watched going RED where each was green before.

ALL THREE ADDITIONS ARE GREEN ON ARRIVAL AND THAT IS THE REASON EACH IS PLANTED AGAINST. The verifier
measured zero shipped occurrences resolving to the sentinel today, so these guard the next sentence
somebody writes rather than a defect standing now. A guard that has never been watched failing is the
same unwatched assertion this file has spent ten waves removing.

WHAT THIS PLAN DOES NOT DO. It does not delete the sentinel — a named token for a produced empty anchor
is the right design and the defect is that its letters counted. It does not touch `constructAnchorFor`'s
scan, its docblock, limit (5), the census or the prefix case — plan 01-39 owns all of those and edited
them one wave ago. It does not touch `CLOSES_FROZEN_ARRAY` or any exclusion — plan 01-41's. It does not
flip or revert CORE-11's checkbox, does not run `requirements mark-complete`, does not re-open the
2026-08-25 re-scope, and opens no file under `.planning/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md
@.planning/phases/01-skeleton-persistence-compatibility/01-39-PLAN.md
@packages/backend/src/outbound-prohibition.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-21 — the sentinel excluded from `nameableRemainder` by reference, asserted never to be produced on the scanned surface, its docblock corrected on both counts, and the verifier's two-step mutation watched going RED</name>

  <precondition>
    Plan 01-39 has executed and committed all three of its tasks. `01-39-SUMMARY.md` exists on disk and
    states in one sentence the count of occurrences resolving to `NO_PRECEDING_CONSTRUCT` after that
    plan, labelled as a handoff fact. READ THAT SENTENCE AND THEN RE-MEASURE THE COUNT YOURSELF — this
    task does not trust the number, it reconciles against it. Before the first edit assert and PASTE:
    `git rev-parse HEAD`; `git status --porcelain packages/ scripts/ .planning/` clean; the gate suite
    green at plan 01-39's recorded count; `pnpm test` exit 0 with file and test counts; `tsc --build`
    exit 0; `check:bundle`'s specifier set. HALT if any is red — a guard written on a red tree cannot
    tell its own failure from the one already there. NO ACTIVE GIT HOOKS: run the gate set by hand.
  </precondition>

  <read_first>
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md` — the `gaps:` block's
      second `artifacts:` entry (CR-21), the `missing:` entry beginning `CR-21: EXCLUDE THE SENTINEL`,
      and the report body section `### CR-21 — CONFIRMED. Two steps, both green, plus the grep.` That
      section carries the arithmetic the verifier ran and the two mutations it planted. It is this
      task's contract and the two mutations are reproduced here verbatim.
    - `01-39-SUMMARY.md` in full, and in particular: whether Task 1 took WR-53's forward-walk extension,
      the final form of `constructAnchorFor`, the sentinel-count handoff sentence, and the 29 regenerated
      keys. This task changes a helper the anchor builder consults, so it must know exactly what that
      builder now does.
    - `packages/backend/src/outbound-prohibition.spec.ts` — `nameableRemainder`, the
      `NO_PRECEDING_CONSTRUCT` docblock and declaration, `constructAnchorFor`'s forward-walk test and
      both sentinel returns, `quantifierOccurrences`, `surfaceExemptionKeys` and the `SURFACE_LINES`
      derivation. RECONCILIATION ANCHORS at HEAD `b8c3d81` were `:9159-9160`, `:9163-9167`, `:9247`,
      `:9251`, `:9253`, `:9316`, `:9343` and the `EXCLUSIONS`/`SURFACE_LINES` block at `:9766-9812`.
      PLAN 01-39 MOVED ALL OF THEM. Re-derive each by identifier and record any disagreement as a
      finding rather than adopting either silently.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the case titled
      `no exemption key's ANCHOR reduces to nothing`, in full, including its failure message. That
      message already tells a reader to rebuild the entry with `exemptionKeyFor`; the correction here
      must leave that guidance intact and true.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the non-vacuity assertion above the
      exclusions rule, for the SHAPE the new surface-wide case must take: assert the population is
      non-empty and say in the failure message what a zero would mean, before asserting anything about
      its members.
    - `01-34-SUMMARY.md`'s measured-silence rows for the probe-and-counter-probe shape, and
      `01-37-SUMMARY.md`'s real-tree swap for the commit-plant-observe-restore shape.
  </read_first>

  <files>packages/backend/src/outbound-prohibition.spec.ts</files>

  <reversibility rating="reversible">One clause added to a helper, one new case, one docblock corrected — all in a spec file. No shipped code, no behaviour, no exclusion, no ledger byte. Both planted mutations are restored inside the task with `git diff --exit-code` shown clean.</reversibility>

  <behavior>
    - `nameableRemainder` applied to an anchor consisting of the sentinel plus mask tokens, whitespace
      and punctuation returns the empty string.
    - `nameableRemainder` applied to a real anchor that merely CONTAINS letters resembling the
      sentinel's words, without being the constant, is unaffected.
    - The `no exemption key's ANCHOR reduces to nothing` case REJECTS a key whose anchor is the sentinel
      plus a mask token.
    - No occurrence found by `quantifierOccurrences` over `SURFACE_LINES` resolves through the live
      builder to the sentinel.
    - The surface-wide case fails loudly, rather than passing, if the occurrence set is empty.
  </behavior>

  <action>
    RUN THE ARITHMETIC BEFORE CHANGING ANYTHING, AND PASTE IT. Compute and paste `nameableRemainder`
    applied to an anchor formed from the `NO_PRECEDING_CONSTRUCT` constant, the separator and a single
    mask token, and paste its length, against the file as it stands. Build the sentinel half by
    REFERENCE to the constant rather than by typing its letters — the same discipline the change itself
    is held to. The verifier measured 20. Reconcile; a disagreement is a finding. This is the BEFORE
    half and it must exist before the change.

    RE-MEASURE THE SENTINEL POPULATION BEFORE CHANGING ANYTHING. Walk every occurrence
    `quantifierOccurrences` finds over `SURFACE_LINES`, resolve each through the live builder, and count
    how many resolve to the sentinel. Paste the count and reconcile it against plan 01-39's handoff
    sentence. IF IT IS NON-ZERO, STOP AND WRITE IT UP FIRST: the surface assertion is not green on
    arrival, each such occurrence is named with its line and its construct, and each is disposed of by
    REWRITING the sentence so it says what it is about or RE-SITING it under a construct that names
    something — never by softening the assertion, never by exempting the occurrence, and never by
    special-casing the sentinel in the new check.

    EXCLUDE THE SENTINEL FROM `nameableRemainder`'s NOTION OF NAMING SOMETHING, BY REFERENCE. Remove
    occurrences of the sentinel from the text before the letters and digits are counted, referencing the
    CONSTANT rather than re-spelling its letters, so renaming the constant cannot silently un-fix this.
    Say in one comment line why the sentinel is excluded: it is the token the builder emits when nothing
    was named, so counting its letters as naming something is the arithmetic that let CR-17's shape back
    in. Then re-run the arithmetic and paste the AFTER value.

    MEASURE THE BLAST RADIUS ON THE 29 KEYS, BECAUSE THIS HELPER IS CONSULTED BY THE ANCHOR BUILDER.
    `nameableRemainder` gates the forward walk inside `constructAnchorFor` as well as the null-anchor
    case. Regenerate all keys before and after the change and paste a comparison. THE EXPECTED RESULT IS
    THAT NOTHING MOVES — no line's token is the sentinel — but that is a prediction and it is recorded
    as one. If any key moves, that is the finding and it is written up before anything else is done.

    ADD THE SURFACE-WIDE ASSERTION, WITH NON-VACUITY BEFORE THE RULE. A named `it(...)` walks every
    occurrence over `SURFACE_LINES`, resolves each through the live builder, and asserts none resolves
    to the sentinel. Assert FIRST that the occurrence set is non-empty, with a failure message saying
    that an empty set would make the rule pass having read nothing — the failure mode this whole file is
    organised against. The rule's own failure message must name the offending line, quote its normalized
    text, and state what a sentinel anchor MEANS: the entry is discharged by any occurrence whose
    normalized form masks to the same shape, wherever in the file it sits, which is CR-17 verbatim. It
    must direct the author to rewrite or re-site the sentence, and must say explicitly that softening
    this assertion is not one of the options.

    CORRECT THE SENTINEL'S DOCBLOCK ON BOTH COUNTS THE VERIFIER NAMED, AND NO MORE. First: it must
    describe BOTH returns. One fires when the backward scan is exhausted; the other fires when a
    construct DID resolve but named nothing above the occurrence — a case the current text does not
    cover at all, and one whose frequency may have changed if plan 01-39 took the forward-walk
    extension, so read the code as it now stands rather than as this plan describes it. Second: the
    current text claims the shape may not be produced silently, and before this task nothing observed
    the value. After this task two assertions do. Write the sentence to NAME those two assertions and
    to stop there — a claim of observability is only as wide as the assertions that provide it.

    COMMIT, THEN REPRODUCE THE VERIFIER'S TWO-STEP MUTATION AND WATCH BOTH GO RED. Paste
    `git log --oneline -1` first.
      STEP 1 — plant a bare docblock whose occurrence line masks entirely, so the forward walk finds
        nothing nameable and the builder returns the sentinel, plus the matching exemption entry whose
        anchor is the sentinel plus the mask token. This went 434/434 GREEN for the verifier. It must
        now go RED through the null-anchor case. Observe, paste the failing title and the FULL assertion
        message, and note whether the surface assertion fired as well.
      STEP 2 — with the exemption entry UNCHANGED, relocate that docblock roughly 1,800 lines into the
        middle of an unrelated construct. This also went 434/434 GREEN. It must now go RED. Observe,
        paste the failing title and the FULL message.
      Restore both, and prove the tree clean with `git diff --exit-code`. Cite `01-VERIFICATION.md` for
      the two prior 434-of-434 results and present this session's two REDs as the contrast. Build the
      planted exemption key's sentinel half BY REFERENCE and its phrasing BY INDEX from
      `UNBOUNDED_QUANTIFIERS`; never spell either out.

    IF EITHER STEP DOES NOT GO RED, STOP AND WRITE IT DOWN BEFORE DOING ANYTHING ELSE. That outcome
    means the fix does not reach the shape it was made for and it is a first-order finding. Do not
    adjust the mutation until it goes red; diagnose and report.

    ASSERT THE OBSERVATION COUNT GREW, AS A DIRECTION AND NOT A TARGET.
    `grep -c NO_PRECEDING_CONSTRUCT` returned four at HEAD `b8c3d81`, none of them an observation. Paste
    the arrival count and the final count, assert the final is strictly greater, and paste at least one
    new hit shown to sit inside an `expect`. Do not name a target number.

    TOUCH NOTHING OUTSIDE SCOPE. `constructAnchorFor`'s scan and docblock, limit (5), the census, the
    prefix-forbidding case, `CLOSES_FROZEN_ARRAY`, `closingBracketAfter`, `EXCLUSIONS`, every band,
    every `proof` token, `CORE11_BOX_EXPECTED`, the box case and the header lines at `:803-804`, `:901`
    and `:911` all stay byte-unchanged. `preAnchoringExemptionKeyForFixtureOnly` is Task 2's. Nothing
    under `.planning/` is opened. If new prose trips wave 33's guard, REWRITE THE PROSE.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY and state the outbound suite's new count as `no fewer
    than` plan 01-39's, never as an equality.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && pnpm exec vitest run packages/backend/src/outbound-prohibition.spec.ts 2>&1 | tail -8 && pnpm test 2>&1 | tail -10 && pnpm typecheck && pnpm lint && pnpm knip && pnpm build:backend && pnpm check:bundle && G=$(grep -c 'NO_PRECEDING_CONSTRUCT' packages/backend/src/outbound-prohibition.spec.ts || true); [ "$G" -gt 4 ] && N=$(git status --porcelain .planning/ | grep -c . || true); [ "$N" -eq 0 ] && A=$(git diff --name-only -- packages/ scripts/ | grep -v '\.spec\.ts$' | grep -c . || true); [ "$A" -eq 0 ] && git diff --exit-code -- packages/backend/src/outbound-prohibition.spec.ts && echo TREE-CLEAN</automated>
  </verify>

  <acceptance_criteria>
    - The precondition asserted and PASTED, including plan 01-39's handoff sentence quoted and
      `git rev-parse HEAD` beside any status snapshot. HALT recorded if anything was red.
    - The sentinel arithmetic pasted BEFORE the change with its length, built by REFERENCE to the
      constant, and reconciled against the verifier's 20; any disagreement recorded as a finding.
    - The sentinel population over `SURFACE_LINES` RE-MEASURED before the change and pasted, reconciled
      against plan 01-39's handoff count. If non-zero: every such occurrence named with its line and
      construct, and each disposed of by rewriting or re-siting, with an explicit statement that the
      assertion was not softened and no occurrence was exempted.
    - The `nameableRemainder` exclusion shown by diff to reference the CONSTANT, with a numeric grep
      asserting the sentinel's letters are not re-spelled inside it:
      `N=$(git diff -- packages/backend/src/outbound-prohibition.spec.ts | grep -E '^\+' | grep -c 'NO-PRECEDING-CONSTRUCT' || true); [ "$N" -le 1 ]`, command and output pasted, with the one permitted
      hit accounted for.
    - The arithmetic re-run AFTER the change and pasted, showing the sentinel-bearing anchor now
      reducing to the empty string.
    - The 29-key blast-radius comparison pasted before and after the `nameableRemainder` change, with
      the prediction that nothing moves recorded AS a prediction and any movement written up as a
      finding before anything else proceeded.
    - The surface-wide case present as a NAMED `it(...)`, its title quoted, with its non-vacuity
      assertion pasted and its rule failure message quoted in full and shown to name the line, quote the
      text, state what a sentinel anchor means, and say that softening the assertion is not an option.
    - The sentinel docblock corrected and quoted verbatim, shown to describe BOTH returns including the
      construct-resolved-but-named-nothing case, and to name the two assertions that now provide the
      observability rather than claiming observability in general.
    - STEP 1 of the verifier's mutation reproduced after commit, observed RED, the failing title and FULL
      assertion message pasted, and whether the surface assertion also fired recorded.
    - STEP 2 reproduced with the exemption entry UNCHANGED, observed RED, the failing title and FULL
      assertion message pasted, the relocation distance pasted.
    - Both mutations restored and `git diff --exit-code` shown clean; `git log --oneline -1` pasted
      before the first plant; `01-VERIFICATION.md` cited for the two prior 434-of-434 results with this
      session's two REDs presented as the contrast.
    - If either step did NOT go red, the task stopped, the diagnosis is written up as a first-order
      finding, and no adjustment was made to force a red.
    - The planted exemption key's sentinel half shown built BY REFERENCE and its phrasing BY INDEX from
      `UNBOUNDED_QUANTIFIERS`, with the whole-file guard run green afterwards and its output pasted.
    - `grep -c NO_PRECEDING_CONSTRUCT` pasted at arrival and at the end, the final asserted strictly
      greater, and at least one new hit pasted shown to sit inside an `expect`. No target number named.
    - The constant shown still declared and both return sites still referencing it — no deletion, no
      empty-string substitution — by a pasted diff.
    - `git diff` shown EMPTY over `constructAnchorFor`'s scan and docblock, limit (5), the census, the
      prefix-forbidding case, `preAnchoringExemptionKeyForFixtureOnly`, `CLOSES_FROZEN_ARRAY`,
      `closingBracketAfter`, `EXCLUSIONS`, the bands, the `proof` tokens, `CORE11_BOX_EXPECTED`, the box
      case, and lines `:803-804`, `:901`, `:911`.
    - `git status --porcelain .planning/` asserted EMPTY numerically.
    - Executed `pnpm test` exit 0 with counts pasted, no fewer than 31 files and 1374 tests;
      `typecheck`, `lint`, `knip` exit 0; `build:backend && check:bundle` exactly one specifier,
      `crypto`; real-tree walk 23 modules / 0 violations; CORE-11's first sentence byte-identical.
    - Any prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses, or an
      explicit statement that there were none.
  </acceptance_criteria>

  <done>
    The sentinel reduces to nothing under `nameableRemainder`, is asserted never to be produced by an
    occurrence on the scanned surface, and is finally observed by two assertions rather than by none;
    its docblock describes both returns and claims only the observability that now exists; and the
    verifier's two-step mutation, green at 434 of 434 twice, was reproduced and watched going RED twice.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-51 — the counter-probe pinned to the live builder's line half, with the verifier's constant-body mutation watched going RED</name>

  <precondition>
    Task 1 has executed and committed. Assert and paste before the first edit: `git rev-parse HEAD`;
    `git status --porcelain packages/ scripts/ .planning/` clean; the gate suite green at Task 1's
    recorded count; `pnpm test` exit 0 with counts; Task 1's surface-wide case present BY TITLE. HALT if
    any is red.
  </precondition>

  <read_first>
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md` — the `artifacts:`
      entry for WR-51 and the `missing:` entry beginning `WR-51: PIN THE COUNTER-PROBE`. The
      prescription is one sentence and this task implements exactly it: pin the counter-probe to the
      LIVE BUILDER'S LINE HALF, so the contrast is against the format CR-17 was actually measured in
      rather than against any constant.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-REVIEW.md`, `### WR-51`, for the
      enumeration of which five assertions a constant function satisfies. The new pin must be the one
      assertion a constant function cannot satisfy.
    - `packages/backend/src/outbound-prohibition.spec.ts` — `preAnchoringExemptionKeyForFixtureOnly`,
      its docblock, `exemptionKeyFor`, `EXEMPTION_ANCHOR_SEP`, and the cross-construct fixture's
      counter-probe section including the `keysUnder` and `discharge` helpers and the `lineHalf` /
      `constructHalf` helpers the fixture already defines. RECONCILIATION ANCHORS at HEAD `b8c3d81`
      were `:9288-9315`, `:9273`, `:9174` and `:10126-10152`; plans 01-39 and 01-40 Task 1 moved all of
      them. Re-derive by identifier.
    - `01-39-SUMMARY.md`'s account of the own-header array pair it added to the same case, because the
      new pin must hold for BOTH array pairs and not only for the original one.
    - `01-37-SUMMARY.md`'s account of why the counter-probe exists at all: a fixture showing the new
      builder catching a relocation is equally consistent with a builder that catches everything and
      with one that catches nothing relevant. That reasoning is why a counter-probe satisfied by a
      constant is worth nothing, and it belongs in the corrected docblock.
  </read_first>

  <files>packages/backend/src/outbound-prohibition.spec.ts</files>

  <reversibility rating="reversible">One assertion added to an existing fixture and one docblock sentence corrected, in a spec file. No shipped code, no behaviour. The planted constant-body mutation is restored inside the task with the tree proved clean.</reversibility>

  <behavior>
    - For the same synthetic input, `preAnchoringExemptionKeyForFixtureOnly`'s output equals the portion
      of `exemptionKeyFor`'s key that follows `EXEMPTION_ANCHOR_SEP`.
    - That equality holds for BOTH array pairs in the cross-construct case, including the own-header
      pair plan 01-39 added.
    - A `preAnchoringExemptionKeyForFixtureOnly` whose body returns a constant FAILS the fixture.
    - The counter-probe's existing five assertions — identical keys across the relocation, empty
      `missing`, empty `stale`, balancing count — continue to hold and are not replaced by the new pin.
  </behavior>

  <action>
    PIN THE COUNTER-PROBE TO THE LIVE BUILDER, WHICH IS THE ONE ASSERTION A CONSTANT CANNOT SATISFY. For
    each synthetic input the fixture already uses, assert that
    `preAnchoringExemptionKeyForFixtureOnly`'s output is EQUAL to the portion of `exemptionKeyFor`'s key
    that follows `EXEMPTION_ANCHOR_SEP` — the line half — using the fixture's own `lineHalf` helper
    rather than re-deriving the split, so a change to the separator cannot leave the two computations
    disagreeing silently. ADD the pin; do not replace the five assertions already there. A relocation
    that is invisible to the old builder is still what the counter-probe is FOR; the new pin is what
    makes that observation about the old BUILDER rather than about an arbitrary function.

    HOLD THE PIN OVER BOTH ARRAY PAIRS. Plan 01-39 added an own-header pair to the same case. The pin
    must be asserted for that pair too, so the contrast is drawn in both shapes the fixture now covers,
    and the SUMMARY must state that it was.

    CORRECT THE HELPER'S DOCBLOCK TO A CLAIM THAT IS NOW ASSERTED. Its structural sentence — that the
    helper is `exemptionKeyFor` with the construct half removed — was true and asserted nowhere, which
    is WR-51 exactly. Rewrite it to name the assertion that now holds it, in one clause, and leave the
    rest of the docblock's reasoning about why the old builder is KEPT rather than described intact,
    because that reasoning is correct and is the reason the counter-probe exists.

    COMMIT, THEN REPRODUCE THE VERIFIER'S MUTATION AND WATCH IT GO RED. Paste `git log --oneline -1`
    first. Replace the body of `preAnchoringExemptionKeyForFixtureOnly` with a constant return — the
    exact mutation that passed 434 of 434 — run the suite, observe RED, paste the failing title and the
    FULL assertion message, restore, and prove the tree clean with `git diff --exit-code`. Cite
    `01-VERIFICATION.md` for the prior 434-of-434 result and present this session's RED as the contrast.
    If it does not go red, STOP, diagnose, and write it up as a first-order finding without adjusting
    the mutation to force a red.

    RE-READ EVERY SENTENCE THIS TASK WRITES AGAINST THE NO-OVERCLAIM PROHIBITION AND RECORD A VERDICT
    PER SENTENCE. In particular, do not write that the counter-probe is now sound in general: it pins
    the pre-anchoring builder to the live builder's line half over the fixture's synthetic inputs, which
    is what was asked for and is narrower than a general equivalence over the file.

    TOUCH NOTHING OUTSIDE SCOPE. `nameableRemainder`, `NO_PRECEDING_CONSTRUCT` and its docblock, Task 1's
    surface-wide case, `constructAnchorFor`, limit (5), the census, the prefix-forbidding case,
    `CLOSES_FROZEN_ARRAY`, `closingBracketAfter`, `EXCLUSIONS`, the bands, the `proof` tokens,
    `CORE11_BOX_EXPECTED`, the box case and the header lines at `:803-804`, `:901` and `:911` all stay
    byte-unchanged. Nothing under `.planning/` is opened.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && pnpm exec vitest run packages/backend/src/outbound-prohibition.spec.ts 2>&1 | tail -8 && pnpm test 2>&1 | tail -10 && pnpm typecheck && pnpm lint && pnpm knip && pnpm build:backend && pnpm check:bundle && N=$(git status --porcelain .planning/ | grep -c . || true); [ "$N" -eq 0 ] && A=$(git diff --name-only -- packages/ scripts/ | grep -v '\.spec\.ts$' | grep -c . || true); [ "$A" -eq 0 ] && git diff --exit-code -- packages/backend/src/outbound-prohibition.spec.ts && echo TREE-CLEAN</automated>
  </verify>

  <acceptance_criteria>
    - The precondition asserted and pasted, with Task 1's case confirmed present BY TITLE.
    - The new pin present, shown by diff to use the fixture's own `lineHalf` helper and
      `EXEMPTION_ANCHOR_SEP` rather than a re-derived split, and shown to be an ADDITION — the five
      pre-existing counter-probe assertions confirmed still present by diff.
    - The pin asserted over BOTH array pairs, including the own-header pair plan 01-39 added, with the
      SUMMARY stating that in one sentence.
    - The helper's docblock corrected and quoted verbatim, naming the assertion that now holds the
      structural claim, with the reasoning about why the old builder is kept shown intact by diff.
    - The verifier's mutation reproduced AFTER commit — the helper's body replaced with a constant
      return — observed RED, the failing title and FULL assertion message pasted, restored,
      `git diff --exit-code` clean, `git log --oneline -1` pasted before the plant, and
      `01-VERIFICATION.md` cited for the prior 434-of-434 with this session's RED as the contrast.
    - If the mutation did NOT go red, the task stopped, the diagnosis is a first-order finding, and no
      adjustment was made to force a red.
    - A one-line no-overclaim verdict per sentence written, with an explicit statement that the pin
      holds over the fixture's synthetic inputs and is narrower than a general equivalence.
    - `git diff` shown EMPTY over `nameableRemainder`, `NO_PRECEDING_CONSTRUCT` and its docblock, Task
      1's case, `constructAnchorFor`, limit (5), the census, the prefix case, `CLOSES_FROZEN_ARRAY`,
      `closingBracketAfter`, `EXCLUSIONS`, the bands, the `proof` tokens, `CORE11_BOX_EXPECTED`, the box
      case and lines `:803-804`, `:901`, `:911`.
    - `git status --porcelain .planning/` asserted EMPTY numerically; `01-PROBE.md`'s equality
      `38 == 27 + 11` re-stated unchanged in the SUMMARY with the statement that this plan resolves no
      flagged assumption and surfaces no new one; `COVERAGE.md` confirmed unedited and its declaration
      confirmed to stand.
    - Executed `pnpm test` exit 0 with counts pasted, no fewer than 31 files and 1374 tests;
      `typecheck`, `lint`, `knip` exit 0; `build:backend && check:bundle` exactly one specifier,
      `crypto`; real-tree walk 23 modules / 0 violations; CORE-11's first sentence byte-identical.
  </acceptance_criteria>

  <done>
    The counter-probe asserts the pre-anchoring builder produces exactly the live builder's line half
    over both array pairs, so the contrast is drawn against the format CR-17 was measured in rather than
    against any constant — and the constant-body mutation that passed 434 of 434 has been watched going
    RED.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a sentinel that NAMES NOTHING to one that SCORES TWENTY | The check written to reject an anchor naming nothing counts the sentinel's letters as naming something, so the one case that exists to catch CR-17's shape passes it with twenty letters of decoration bolted on. |
| a value that is RETURNED to one that is OBSERVED | Four references, none of them an assertion. A named token whose production nothing checks is a comment with a type. |
| a docblock's claimed mechanism to the mechanism that exists | The sentinel's docblock claims the empty-anchor shape cannot be produced silently. Before this plan nothing observed it, so the claim named a mechanism that did not exist — the ninth instance of this phase's signature defect. |
| a counter-probe that CONTRASTS to one any function satisfies | `return "CONSTANT";` passes all five of the counter-probe's assertions at 434 of 434. A contrast drawn against an arbitrary function is not a contrast. |
| a helper shared by a check and by the BUILDER | `nameableRemainder` gates `constructAnchorFor`'s forward walk as well as the null-anchor case, so this plan reaches the mechanism plan 01-39 just corrected. Blast radius is measured, not reasoned about. |
| a guard that is GREEN ON ARRIVAL to one that has been WATCHED FAILING | All three additions are green the moment they land, which is exactly when a guard goes untested. WR-54 exists because round 8 shipped a correction with no pin. |
| a test-only gate to the shipped bundle | Nothing here reaches shipped code. `git diff --name-only` over `packages/` and `scripts/` filtered of `.spec.ts` is asserted EMPTY, and `check:bundle` is re-run regardless. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-244 | Spoofing | CR-17's key shape laundered through the case added to forbid it | critical | mitigate | The sentinel is excluded from `nameableRemainder` by reference to the constant, so a sentinel-bearing anchor reduces to nothing and the null-anchor case rejects it; watched failing by the verifier's own step-1 mutation, which was green at 434 of 434. |
| T-01-245 | Repudiation | a named token whose production nothing observes | high | mitigate | A surface-wide case asserts no occurrence resolves to the sentinel, with non-vacuity above the rule; `grep -c` asserted strictly greater than its arrival value with a new hit shown inside an `expect`. |
| T-01-246 | Spoofing | a sentinel-anchored exemption relocated with the entry unchanged | critical | mitigate | The verifier's step-2 mutation is reproduced verbatim — the same occurrence moved ~1,800 lines with the entry untouched — and must be observed RED where it was green at 434 of 434. |
| T-01-247 | Tampering | the anchor builder weakened by a change to the helper it shares | high | mitigate | The 29 keys are regenerated and compared before and after the `nameableRemainder` change; the prediction that nothing moves is recorded AS a prediction and any movement is a finding written up before anything else proceeds. |
| T-01-248 | Tampering | the sentinel's letters re-spelled inside the exclusion | medium | mitigate | The exclusion references the constant; a numeric grep over the added lines asserts the literal is not re-spelled, so renaming the constant cannot silently un-fix CR-21. |
| T-01-249 | Tampering | the sentinel deleted or replaced with an empty string | high | mitigate | Forbidden in frontmatter. The constant is shown still declared and both return sites still referencing it by a pasted diff. An empty anchor produced silently is the shape the whole mechanism exists to forbid. |
| T-01-250 | Spoofing | a counter-probe satisfied by any constant function | high | mitigate | The pre-anchoring builder's output is pinned to the LIVE builder's line half over both array pairs using the fixture's own `lineHalf` helper, and the verifier's constant-body mutation is watched going RED. |
| T-01-251 | Repudiation | a guard green on arrival that nobody watched failing | high | mitigate | Three mutations planted after commit — step 1, step 2, and the constant body — each observed RED with its FULL assertion message pasted and each restored with a clean `git diff --exit-code`. |
| T-01-252 | Repudiation | a tenth overclaim written inside the fix for the ninth | high | mitigate | An `open`-status DISCLOSED-NOT-CLOSED prohibition in frontmatter; a per-sentence no-overclaim verdict in Task 2; the corrected observability sentence limited to naming the two assertions that provide it. |
| T-01-240 | Tampering | a real-tree mutation planted over uncommitted work | high | mitigate | Every task commits before planting, pastes `git log --oneline -1` first, and proves the restore with `git diff --exit-code`. Wave 29 destroyed its own Task 1 exactly this way. |
| T-01-242 | Tampering | new prose or a planted key raising the obligation it exists to test | medium | mitigate | The planted exemption's sentinel half is built by reference and its phrasing by index from `UNBOUNDED_QUANTIFIERS`; the whole-file guard is run green at each boundary; no exemption is minted for this wave's own text. |
| T-01-214 | Tampering | CORE-11's checkbox moved | critical | mitigate | Prohibited. This plan opens no file under `.planning/`; `git status --porcelain .planning/` asserted EMPTY numerically at every boundary and `requirements mark-complete` appears in no command. Carried forward from plan 01-36. |
| T-01-212 | Information Disclosure | the two reader surfaces no mechanism reaches | medium | accept | `.planning/STATE.md` and `.planning/WINDOWS.md` are governed by the pointer-not-a-bound prohibition with no mechanical check. Untouched by this plan. Carried forward from T-01-144 unchanged. |
| T-01-51 | Information Disclosure | the accepted residual of the outbound walk | medium | mitigate | Unchanged by this plan — no registry row moves — and byte-pinned into the ledger by the two comparisons, re-run green at both task boundaries. |
| T-01-50 | Information Disclosure | `.spec.ts` files, which the walk skips | low | accept | Unchanged and carried forward, bounded by `pnpm check:bundle`, which specs never enter. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | NOT APPLICABLE: this plan installs no package. `pnpm install` is not run and no manifest appears in `files_modified`. RESEARCH.md's Package Legitimacy Audit is present and unchanged; no `[ASSUMED]` or `[SUS]` package is introduced, so no blocking human checkpoint is required. |
</threat_model>

<verification>
1. Precondition asserted and pasted at both task boundaries, with plan 01-39's handoff sentence quoted
   and HEAD pasted beside any status snapshot.
2. The sentinel arithmetic pasted BEFORE the change, built by reference, reconciled against 20.
3. The sentinel population over `SURFACE_LINES` re-measured BEFORE the change and reconciled against
   plan 01-39's handoff count; any non-zero disposed of by rewriting or re-siting, never by softening.
4. The `nameableRemainder` exclusion shown to reference the constant, with the numeric grep proving the
   literal is not re-spelled.
5. The arithmetic re-run AFTER the change, showing the empty string.
6. The 29-key blast-radius comparison pasted before and after, with the no-movement expectation recorded
   AS a prediction.
7. The surface-wide case present by title, its non-vacuity assertion pasted, its rule message quoted in
   full.
8. The sentinel docblock corrected and quoted, describing BOTH returns and naming the two assertions
   that provide the observability.
9. STEP 1 reproduced after commit, RED observed, title and FULL message pasted.
10. STEP 2 reproduced with the entry unchanged, RED observed, title and FULL message pasted, distance
    pasted.
11. Both restored, `git diff --exit-code` clean, the verifier's two prior 434-of-434 results cited.
12. If either step did not go red, the task stopped and the diagnosis is a first-order finding with no
    adjustment made to force a red.
13. `grep -c NO_PRECEDING_CONSTRUCT` pasted at arrival and at the end, asserted strictly greater, one new
    hit shown inside an `expect`, no target named.
14. The constant shown still declared with both returns intact.
15. The counter-probe pin present, using the fixture's own `lineHalf` helper, shown to be an ADDITION
    with the five pre-existing assertions intact.
16. The pin asserted over BOTH array pairs including plan 01-39's own-header pair.
17. The helper's docblock corrected and quoted, its keep-rather-than-describe reasoning intact.
18. The constant-body mutation reproduced after commit, RED observed, title and FULL message pasted,
    restored clean, the prior 434-of-434 cited.
19. A per-sentence no-overclaim verdict for everything written, including the explicit statement that
    the pin holds over synthetic inputs and is narrower than a general equivalence.
20. `git diff` EMPTY over every out-of-scope construct named in each task's action.
21. `git status --porcelain .planning/` asserted EMPTY numerically at every boundary;
    `requirements mark-complete` absent from every command run.
22. `01-PROBE.md`'s equality `38 == 27 + 11` re-stated unchanged; `COVERAGE.md` confirmed unedited and
    its declaration confirmed to stand.
23. `pnpm test` exit 0, no fewer than 31 files / 1374 tests; `typecheck`, `lint`, `knip` exit 0;
    `build:backend && check:bundle` exactly one specifier, `crypto`; real-tree walk 23 modules / 0
    violations; CORE-11's first sentence byte-identical.
24. Every prediction-vs-measurement discrepancy recorded in the shape `01-27-SUMMARY.md` uses, or an
    explicit statement that there were none.
</verification>

<success_criteria>
- An anchor made of the sentinel plus mask tokens reduces to nothing, so the case added in the same
  commit to forbid the empty-anchor shape finally rejects it — and the exclusion is written by reference
  to the constant, so renaming it cannot silently un-fix this.
- No occurrence on the scanned surface resolves to the sentinel, asserted with non-vacuity above the
  rule and re-measured in this session rather than inherited from the report.
- The sentinel is observed by two assertions where it was observed by none, and `grep -c` is shown to
  have grown with at least one new hit inside an `expect`.
- The sentinel's docblock describes both returns, including the construct-resolved-but-named-nothing
  case, and claims only the observability the two new assertions provide.
- The counter-probe is pinned to the live builder's line half over both array pairs, so the contrast is
  against the format CR-17 was measured in rather than against any constant.
- All three additions were green on arrival and all three were watched failing anyway: the verifier's
  two-step mutation, twice green at 434 of 434, went RED twice; its constant-body mutation, green at 434
  of 434, went RED.
- The `nameableRemainder` change's blast radius on the anchor builder was MEASURED across all 29 keys
  before and after, not reasoned about.
- No sentence anywhere claims the empty-anchor class is closed or that criterion (3) is discharged; the
  same-construct residual, the guard's phrase-list reach and both classes of unreached surface are named
  wherever this plan's result is stated.
- CORE-11's checkbox did not move in either direction, `requirements mark-complete` was not run, and no
  file under `.planning/` was opened.
- The green baseline is preserved and RE-MEASURED rather than assumed: no fewer than 31 files / 1374
  tests, typecheck / lint / knip clean, bundle at exactly one specifier, real tree at 23 modules / 0
  violations, and nothing leaks.
</success_criteria>

<artifacts_this_phase_produces>
| Artifact | Kind | Purpose |
|----------|------|---------|
| the sentinel exclusion in `nameableRemainder` | corrected mechanism | An anchor made of the sentinel plus mask tokens reduces to nothing, so the null-anchor case rejects the shape CR-17 was filed about — written by reference to the constant, never by re-spelling it |
| the 29-key blast-radius comparison | new evidence | The measured effect of changing a helper the anchor builder consults, taken before and after rather than reasoned about |
| the surface-wide sentinel assertion | new mechanism | No occurrence on the scanned surface resolves to the sentinel, with non-vacuity above the rule and a failure message that forbids softening itself |
| the re-measured sentinel population | new evidence | Plan 01-39's handoff count re-derived in this session rather than inherited, with any non-zero disposed of by rewriting or re-siting |
| the corrected sentinel docblock | corrected disclosure | Both returns described, including the construct-resolved-but-named-nothing case, and an observability claim no wider than the two assertions that provide it |
| the verifier's two-step mutation, watched RED | new evidence | The sentinel-producing occurrence with its matching entry, and the same occurrence relocated with the entry unchanged — both green at 434 of 434 before this plan |
| the counter-probe pinned to the live builder's line half | corrected mechanism | The contrast drawn against the format CR-17 was measured in, over both array pairs, using the fixture's own `lineHalf` helper |
| the constant-body mutation, watched RED | new evidence | `return "CONSTANT";` passed 434 of 434 for the verifier and must now fail, which is the whole of WR-51 |
| the corrected `preAnchoringExemptionKeyForFixtureOnly` docblock | corrected disclosure | A structural claim that names the assertion holding it, with the keep-rather-than-describe reasoning left intact |
| `01-40-SUMMARY.md` | new evidence | The arithmetic before and after, the blast-radius comparison, the re-measured population, three mutation REDs with full messages, the grep direction, and the full gate set |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-skeleton-persistence-compatibility/01-40-SUMMARY.md` when done.

It must contain, as pasted terminal output rather than as prose: the precondition assertions with
`git rev-parse HEAD` and plan 01-39's handoff sentence quoted; the sentinel arithmetic before and after
with both lengths; the re-measured sentinel population over `SURFACE_LINES` with its reconciliation
against the handoff count; the 29-key blast-radius comparison; the numeric grep proving the sentinel's
letters are not re-spelled inside the exclusion; the surface-wide case's title, non-vacuity assertion
and full rule message; the corrected sentinel docblock quoted verbatim; `git log --oneline -1` before
each plant; STEP 1's RED title and FULL message; STEP 2's RED title, FULL message and relocation
distance; the constant-body mutation's RED title and FULL message; all three restores with clean
`git diff --exit-code`; the `grep -c NO_PRECEDING_CONSTRUCT` arrival and final counts with one new hit
shown inside an `expect`; the diff showing the constant still declared with both returns intact; the
counter-probe pin's diff over both array pairs; the corrected helper docblock quoted verbatim; the
per-sentence no-overclaim verdict table; the EMPTY diffs over every out-of-scope construct; the EMPTY
`git status --porcelain .planning/`; and the final `pnpm test`, `typecheck`, `lint`, `knip`,
`check:bundle` and real-tree walk results.

It must state in ONE sentence that all three additions were green on arrival and that each was watched
failing anyway, naming the three mutations and the 434-of-434 result each had before this plan.

It must state in ONE sentence whether the `nameableRemainder` change moved any of the 29 keys, and if it
did, that the movement was written up as a finding before anything else proceeded.

It must state in ONE sentence that closing the sentinel does not close the empty-anchor class, naming
the hand-written key that could still be constructed to survive both checks, the same-construct
residual, the guard's phrase-list reach and both classes of unreached surface.

It must state in ONE sentence that CORE-11's box was not moved in either direction, that
`requirements mark-complete` was not run, and that no file under `.planning/` was opened.
</output>
