---
phase: 01-skeleton-persistence-compatibility
plan: 47
type: execute
wave: 47
gap_closure: true
gap_ids: [CR-32]
depends_on: [01-46]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/01-skeleton-persistence-compatibility/01-SECURITY.md
  - .planning/WINDOWS.md
# ROUND 12'S SECOND AND LAST PLAN. IT RUNS SECOND FOR A REASON THAT IS A DESIGN DECISION AND NOT AN
# ACCIDENT OF NUMBERING: every number it reconciles is a number plan 01-46 moves. Reconciling them first
# would write each figure twice — once against the value 01-46 supersedes and once against the value it
# lands on — and the second write is where the attribution is lost.
#
# THIS PLAN TOUCHES NO CODE. It edits three planning surfaces and, conditionally, the Broken Windows
# ledger. `packages/` and `scripts/` are byte-unchanged across all of its commits, and the verify asserts
# that rather than assuming it. That is also its cheapest guarantee: because it changes no line of
# `packages/backend/src/outbound-prohibition.spec.ts`, the widest anchor shadow, every exclusion endpoint
# and every anchor in that file are unchanged BY CONSTRUCTION, and the plan asserts the pin still reads
# what plan 01-46 left it reading.
#
# ---------------------------------------------------------------------------------------------------
# CR-32 — WARNING, AND IT IS CR-25's SHAPE ACROSS FILES RATHER THAN INSIDE ONE.
# ---------------------------------------------------------------------------------------------------
# `.planning/STATE.md:408` publishes the shadow bound PRESENT TENSE, UNDATED AND UNPOINTED:
#   "The interchangeability residual IS republished as the measured width of the anchor's SHADOW — 574
#    surface lines at the header row :417, raw 419..1574 — and pinned by EXACT EQUALITY"
# The gate file published 549 at eight live sites before this round and plan 01-46 moves it again. The
# entry carries no date, no wave marker and no pointer. `d3a139d` DID append a dated pointer amendment to
# a sibling entry at `:307` for CR-29 and is the correct precedent for the FORM; it did not reach `:408`.
#
# TWO SIBLING SURFACES CARRY THE SAME STALE FIGURE AND PASS 13 NAMES BOTH:
#   `.planning/ROADMAP.md:239` — "measured with the live builder over all 8,846 surface lines the widest
#   is `:417`'s at 574 surface lines / 1,155 raw". TWO stale numbers in one clause: the width, and a
#   surface total WR-61 already re-measured to 9,277.
#   `01-SECURITY.md:99` — "or deleted line moves anchors, exclusion endpoints and the 574-line shadow".
#
# AND A FINDING THIS PLAN EXPECTS TO CONFIRM RATHER THAN ONE PASS 13 FILED. `01-SECURITY.md` carries
# THREE threat rows whose status is `open` and whose text was falsified by waves 42 through 45:
#   T-01-264 at `:170` states of the `:417` shadow "**Nothing pins it.**" — `WIDEST_ANCHOR_SHADOW` has
#   pinned its size since wave 42 and `WIDEST_ANCHOR_TOKEN` has pinned its identity since wave 45.
#   T-01-280 at `:171` quotes the pin's constant at a superseded value and cites a superseded line.
#   T-01-286 at `:174` rests on a sentence `01-SECURITY.md:152` itself records as DELETED.
# All three also cite gate-file line numbers that `08345c3` moved by −25 and that plan 01-46 moves again.
# A SECURITY REGISTER WHOSE OPEN ROWS DESCRIBE A TREE THAT NO LONGER EXISTS IS THE SAME DEFECT THIS PHASE
# HAS SPENT TWELVE ROUNDS REMOVING FROM THE GATE FILE, SITTING IN THE FILE THAT ADJUDICATES IT. VERIFY
# EACH OF THE THREE BY EXECUTION BEFORE TOUCHING IT — this comment block is a prediction, not evidence,
# and adopting it unverified would be the exact method that produced five consecutive false bounds.
#
# ---------------------------------------------------------------------------------------------------
# THE DISPOSITION RULE, AND IT IS NOT THE SAME FOR ALL FOUR SURFACES.
# ---------------------------------------------------------------------------------------------------
# `.planning/STATE.md` IS AN APPEND-ONLY DATED HISTORY AND MUST NOT BE REWRITTEN. The gate file states
# the rule at the paragraph beginning "THE SURFACE DECISION, wave 27": a block regenerated inside a
# history "would rewrite the record of what was believed and when, which is the only thing that makes a
# history worth keeping." So `:408` gets an APPENDED, DATED POINTER AMENDMENT and its existing bytes stay.
#   AND THE AMENDMENT MUST POINT WITHOUT RESTATING. `.planning/WINDOWS.md`'s entry 32 established the
#   form in wave 27 — "POINTER, NOT A COPY … this ledger RESTATES NO BOUND" — and the reason is
#   mechanical rather than stylistic: a pointer that spells the current figure is a FOURTH hand-written
#   copy of a number nothing compares, and it goes stale on the next commit that moves a line. THE
#   AMENDMENT NAMES `WIDEST_ANCHOR_SHADOW` AS THE SOLE PUBLICATION AND SPELLS NO DIGITS. `:307`'s
#   existing amendment does spell a figure; that is a dated record of what pass 12 found and stays, but
#   it is not the model for the new one and this plan says so rather than copying it by proximity.
# `.planning/ROADMAP.md` IS A LIVE INDEX, NOT AN APPEND-ONLY HISTORY. Its rows are edited in place as
# phases progress. So a stale figure there is DELETED, not amended around — plan 01-44's discipline:
# a deletion removes a false claim and establishes no true one, whereas a corrected sentence is still an
# authored bound. The row keeps its description of what the plan delivered and loses its figures.
# `01-SECURITY.md` IS A LIVE REGISTER WITH DATED CLOSURE RECORDS MIXED INTO IT. Its `open` rows are
# CURRENT claims and are corrected or re-adjudicated in place with the correction dated; its `CLOSED`
# rows name the pass that closed them and are DATED HISTORY that stays byte-unchanged.
# `.planning/WINDOWS.md` CARRIES NO COPY OF THE SHADOW FIGURE — measured, zero occurrences — and CR-30
# is the finding that the gate file falsely claimed a machine check on it. This plan does not rewrite it.
# Whether the round records a new POINTER entry is a decision Task 3 makes explicitly, with its reason.
#
# ---------------------------------------------------------------------------------------------------
# THE SEVEN HARD CONSTRAINTS OF ROUND 12 APPLY HERE BYTE FOR BYTE AND ARE REPEATED RATHER THAN INHERITED.
# ---------------------------------------------------------------------------------------------------
# 1. LINE COUNT IS LOAD-BEARING — in the GATE FILE. This plan changes no line of it, so the pin is
#    invariant by construction and the verify asserts the file is byte-identical to plan 01-46's exit.
#    A plan that "just fixes some prose" and touches that file is how a pin moves unattributed.
# 2. ONE BOUND, PUBLISHED ONCE. Enumerate EVERY surface carrying the figure and classify each LIVE or
#    DATED HISTORY, with the classification stated PER SITE. Task 1 produces that table for all four
#    surfaces before any of them is edited.
# 3. WATCH EVERY GUARD FAIL BEFORE TRUSTING IT. This plan installs no new guard — it has no code to put
#    one in. What it does instead is stated plainly rather than dressed up: its claims are verified by
#    grep census and by re-execution of the claims the security rows make, and the SUMMARY says which of
#    its assertions have a mechanical check behind them and which are unguarded prose. Saying so is the
#    whole of the discipline available here.
# 4. RESTORE PROOF. `cp` is aliased to `cp -i` in this environment and SILENTLY REFUSES to overwrite.
#    Restore with `/bin/cp -f` or `git checkout --`, VERIFY the restore rather than assuming it, and
#    prove the tree clean with `git diff --exit-code`.
# 5. DO NOT TOUCH CORE-11's CHECKBOX. `.planning/REQUIREMENTS.md` is NOT in this plan's `files_modified`
#    at all; plan 01-46 owns it and its only change there is the regenerated span. `CORE11_BOX_EXPECTED`
#    is likewise untouched. Verification pass 14 owns the flip. If this plan's work convinces you the box
#    is earned, SAY SO IN THE SUMMARY AND STOP.
# 6. DELETION BEATS CORRECTION, subject to the per-surface rule above: delete in the live index, amend by
#    dated pointer in the append-only history, re-adjudicate in the live register.
# 7. BASELINE, RE-ESTABLISHED RATHER THAN INHERITED. Even though this plan touches no code, Task 3 runs
#    the whole suite, `typecheck`, `lint`, `knip`, `build:backend` and `check:bundle` and reconciles them
#    against plan 01-46's exit values. A round that stops measuring because it "only edited markdown" is
#    how a broken tree ships behind a clean-looking diff.
#
# EVERY LINE NUMBER IN THIS PLAN IS MEASURED AT `1cb2884` AND PLAN 01-46 HAS NOT MOVED ANY OF THEM — it
# touches neither STATE.md nor ROADMAP.md nor 01-SECURITY.md. RE-DERIVE THEM ANYWAY, BY TEXT. The one
# number that WILL have moved is the gate file's pinned value, and reading it from the file rather than
# from this plan is the point.
autonomous: true
requirements: [CORE-11]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
# `estimate-calibration` reports `{"factor": 1, "applied": false, "sample_count": 0, "min_samples": 3}`,
# so `tokens` equals `raw_tokens` and `confidence` is DERIVED from the sample count, never self-rated.
# WEIGH THE MEASUREMENT COUNT OVER THE TOKEN FIGURE: one complete four-surface census classified site by
# site, one append-only amendment written to point rather than restate, three open security rows
# re-adjudicated against executed evidence rather than re-numbered, and one closing baseline executed on
# a round that touched no code.

must_haves:
  truths:
    - "THE ENUMERATION IS COMPLETE BEFORE ANYTHING IS EDITED. Every site on every surface outside the gate file carrying the shadow figure or the superseded one is listed with its file, line, text and a LIVE / DATED HISTORY classification carrying its reason — and the census is run repo-wide rather than over the four files this plan expected, so a fifth surface would be found rather than assumed absent."
    - "STATE.md IS AMENDED BY POINTER AND ITS EXISTING BYTES ARE UNCHANGED. `:408` gains a dated amendment recording that its figure is superseded and naming `WIDEST_ANCHOR_SHADOW` in the gate file as the sole publication; the amendment SPELLS NO FIGURE, and the original entry is byte-identical."
    - "ROADMAP.md's STALE FIGURES ARE DELETED, NOT CORRECTED. The phase-01 rows keep their description of what each plan delivered and lose every number the gate file publishes — including the surface total WR-61 already falsified — so no row states a bound that can go stale again."
    - "01-SECURITY.md's OPEN ROWS ARE RE-ADJUDICATED AGAINST EXECUTED EVIDENCE, NOT RE-NUMBERED. Each of T-01-264, T-01-280 and T-01-286 is checked by running the claim it makes against the live tree; a row whose claim is now false is closed with its closing evidence, and one whose claim survives is restated without a line number that slides. The dated CLOSED rows are byte-unchanged."
    - "NO CODE CHANGED AND THE PIN IS INVARIANT BY CONSTRUCTION. `packages/` and `scripts/` are byte-identical across every commit in this plan; the gate spec is byte-identical to plan 01-46's exit; the shadow pin and the identity pin read exactly what plan 01-46 left them reading."
    - "THE COUNTER DISCIPLINE IS FOLLOWED AND ANY HANDLER DRIFT IS RECORDED RATHER THAN QUIETLY FIXED. STATE.md's counters are set from the PLAN and SUMMARY file count on disk, which is the only number that cannot drift, and any disagreement with `state.advance-plan` or `state.update-progress` is written down — the rule these notes have followed since 01-17."
    - "THE ROUND'S CLOSING BASELINE IS EXECUTED ON A ROUND THAT TOUCHED NO CODE. Whole suite, typecheck, lint, knip, build and bundle check all run and are reconciled against plan 01-46's exit values rather than assumed unchanged."
    - "CRITERION (3) IS ANSWERED PLAINLY AND THE BOX IS NOT TOUCHED. The SUMMARY states whether the disclosure is now the only bound stated on every surface a reader touches, names anything still outstanding, and shows both box surfaces byte-unchanged."

  artifacts:
    - path: ".planning/STATE.md"
      provides: "A dated pointer amendment on the superseded shadow entry that names the gate file's constant as the sole publication and restates no figure of its own — the append-only discipline applied correctly rather than by proximity to a precedent that restates one."
      contains: "POINTER AMENDMENT"
    - path: ".planning/ROADMAP.md"
      provides: "Phase-01 plan rows that describe what each plan delivered and publish no figure the gate file owns."
    - path: ".planning/phases/01-skeleton-persistence-compatibility/01-SECURITY.md"
      provides: "A threat register whose open rows describe the tree that exists, each re-adjudicated against evidence executed in this session, with the dated closure records left intact."

  key_links:
    - from: "a shadow bound published present-tense, undated and unpointed in an append-only history"
      to: "a dated pointer amendment naming the gate file's constant as the sole publication and spelling no figure"
      via: "the wave-27 pointer-not-a-copy form, applied so the amendment cannot itself go stale"
    - from: "three open threat rows asserting facts about a tree four waves have superseded"
      to: "rows re-adjudicated against claims re-executed in this session, closed or restated with their evidence"
      via: "running each row's own claim rather than re-numbering its citations"
---

<objective>
Reconcile the shadow bound on every surface outside the gate file, using the discipline each surface
actually calls for — a dated pointer amendment in the append-only history, deletion in the live index,
re-adjudication against executed evidence in the live security register — and answer criterion (3)
plainly without touching CORE-11's box.

Purpose: `08345c3` re-derived the bound in the gate file and left it live in three other places while its
commit message claimed "one bound, published once". Pass 13 filed CR-32 for it, and plan 01-46 moves the
bound again — so the same failure is available to this round unless the enumeration is completed and
every site is classified before any of them is edited. The register that adjudicates this phase's threats
is itself carrying open rows about a tree that stopped existing four waves ago, which is the same defect
sitting in the file that is supposed to catch it.

Output: three planning surfaces on which the shadow bound is either absent or pointed at, a security
register whose open rows were re-run rather than re-numbered, a complete four-surface census committed as
a table, and a closing baseline executed on a round that changed no code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md
@.planning/phases/01-skeleton-persistence-compatibility/01-SECURITY.md
@.planning/phases/01-skeleton-persistence-compatibility/01-46-SUMMARY.md
@packages/backend/src/outbound-prohibition.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: the complete four-surface census, classified site by site before anything is edited, and STATE.md amended by a dated pointer that names the gate file's constant and spells no figure</name>

  <files>.planning/STATE.md</files>

  <precondition>Plan 01-46 is committed, `01-46-SUMMARY.md` exists, `git status --porcelain` is empty, and the gate spec's shadow pin reads the value plan 01-46 left it reading — read that value FROM THE FILE, not from this plan or from 01-46's. Every number below is measured at `1cb2884` and plan 01-46 did not touch these three surfaces; re-derive them by text anyway.</precondition>

  <read_first>
    - `.planning/STATE.md` at `:400-415`, and the amendment `d3a139d` appended to `:307` — read the latter for its FORM and note explicitly where this plan departs from it.
    - `.planning/WINDOWS.md` entry 32, for the pointer-not-a-copy rule in its own words.
    - `01-VERIFICATION.md`'s CR-32 section and its adjudication of the two surviving gate-file 574 sites, so the classification criteria used here are the ones pass 13 used rather than new ones.
    - `01-46-SUMMARY.md`'s constraint-2 table, so this census extends that enumeration rather than starting a second one with different criteria.
  </read_first>

  <action>
Run the census REPO-WIDE, not over the four files this plan names. Search every tracked file for the
shadow figure, for the superseded one, and for the raw-span spellings that carry either. The plan names
four surfaces; a census that only looks where the plan pointed proves the plan right and nothing else,
and a fifth surface found here is worth more than the four confirmed.

Classify every hit. LIVE means the line states the CURRENT geometry in the present tense and a reader
takes it as a fact about the tree they are on. DATED HISTORY means it names the pass, wave or plan that
measured it, is past tense, and states no current bound. Use pass 13's criteria, quoted from
`01-VERIFICATION.md`, rather than inventing new ones — it adjudicated two gate-file sites this way and its
reasoning is on the record. Where a single line carries BOTH a dated past-tense measurement and a
present-tense claim, say so and classify the CLAIM, because that is the half a reader acts on.

Write the census into the SUMMARY as a table with one row per hit: file, line, the text, the
classification, the reason, and the owning task. THE TABLE IS THE DELIVERABLE. Constraint 2 is not
discharged by editing the sites this plan happened to name.

Then amend `.planning/STATE.md`. Append a dated POINTER AMENDMENT to the entry that publishes the shadow
width — the one beginning "The interchangeability residual is republished". DO NOT REWRITE THE ENTRY. Its
existing bytes stay byte-identical, and the verify below asserts that against the base revision. The gate
file's own wave-27 surface decision is the rule and it is quoted there: regenerating a block inside an
append-only history rewrites the record of what was believed and when.

THE AMENDMENT POINTS AND SPELLS NO FIGURE. Record that the entry's stated width and raw span were
superseded, name the commits and rounds that superseded them, name `WIDEST_ANCHOR_SHADOW` in the gate spec
as the SOLE publication of the live value, and stop. Do not write the current number. A pointer that
spells a figure is a fourth hand-written copy of a number nothing compares, and it goes stale on the next
commit that moves a line — which is precisely the mechanism that produced CR-31 inside the commit closing
CR-29. The amendment at `:307` DOES spell a figure; it is a dated record of what pass 12 found, it stays
byte-unchanged, and it is explicitly NOT the model for this one. Say that in the amendment so a later
reader does not harmonise the two.

Record any counter drift rather than fixing it quietly. If `state.advance-plan` or `state.update-progress`
disagrees with the PLAN and SUMMARY file count on disk, set the counters FROM THE FILE COUNT — the only
number that cannot drift — and write the disagreement down. That rule has been in these notes since 01-17
and every wave that followed it produced a shorter correction than every wave that did not.

Touch no other file in this task. ROADMAP.md and 01-SECURITY.md are Task 2's, and splitting one finding's
surface across two tasks in the same plan is how a correction loses its attribution.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && SP=packages/backend/src/outbound-prohibition.spec.ts; ST=.planning/STATE.md; TD="${TMPDIR:-/tmp}"; RB=$(git rev-parse 1cb2884) || { echo "ROUND BASE UNRESOLVABLE"; exit 1; }; [ -n "$RB" ] || { echo "BASE REV EMPTY — git show WOULD READ THE INDEX AND EVERY DIFF BELOW WOULD BE 0"; exit 1; }; git merge-base --is-ancestor "$RB" HEAD || { echo "1cb2884 IS NOT AN ANCESTOR OF HEAD"; exit 1; }; P46=$(git log --oneline "$RB"..HEAD -- .planning/phases/01-skeleton-persistence-compatibility/01-46-SUMMARY.md | grep -c . || true); [ "$P46" -ge 1 ] || { echo "01-46-SUMMARY.md HAS NO COMMIT IN THIS ROUND — PLAN 01-46 IS NOT COMPLETE AND EVERY FIGURE THIS PLAN RECONCILES IS THE ONE 01-46 SUPERSEDES"; exit 1; }; [ -f .planning/phases/01-skeleton-persistence-compatibility/01-46-SUMMARY.md ] || { echo "01-46-SUMMARY.md ABSENT"; exit 1; }; H46=$(git log --format=%H -1 -- .planning/phases/01-skeleton-persistence-compatibility/01-46-SUMMARY.md); [ -n "$H46" ] || { echo "COULD NOT RESOLVE PLAN 01-46's EXIT COMMIT"; exit 1; }; git diff --quiet "$H46"..HEAD -- packages/ scripts/ || { echo "THIS PLAN HAS CHANGED CODE SINCE PLAN 01-46's EXIT. IT OWNS NO CODE, AND THE PIN'S INVARIANCE BELOW IS ASSERTED ON THE STRENGTH OF THAT"; exit 1; }; echo "CODE-BYTE-IDENTICAL-SINCE-01-46 $H46"; PIN=$(grep -oE 'WIDEST_ANCHOR_SHADOW = [0-9]+' "$SP" | head -1 | grep -oE '[0-9]+'); [ -n "$PIN" ] || { echo "THE SHADOW PIN IS UNREADABLE FROM THE GATE FILE"; exit 1; }; PIN46=$(git show "$H46":"$SP" | grep -oE 'WIDEST_ANCHOR_SHADOW = [0-9]+' | head -1 | grep -oE '[0-9]+'); [ -n "$PIN46" ] || { echo "THE SHADOW PIN IS UNREADABLE AT PLAN 01-46's EXIT, SO THE COMPARISON BELOW WOULD BE VACUOUS"; exit 1; }; [ "$PIN" -eq "$PIN46" ] || { echo "THE SHADOW PIN READS $PIN AND PLAN 01-46 LEFT IT AT $PIN46"; exit 1; }; echo "PIN-INVARIANT=$PIN"; A408=$(git show "$RB":"$ST" | grep -c 'The interchangeability residual is republished' || true); [ "$A408" -eq 1 ] || { echo "THE TARGET ENTRY RESOLVES TO $A408 LINES ON THE BASE REVISION, EXPECTED EXACTLY 1 — AT 0 THE BYTE-IDENTITY CHECK BELOW COMPARES NOTHING"; exit 1; }; git show "$RB":"$ST" | grep 'The interchangeability residual is republished' > "$TD/r12.s408.base"; grep 'The interchangeability residual is republished' "$ST" > "$TD/r12.s408.work"; [ -s "$TD/r12.s408.base" ] || { echo "BASE EXTRACT EMPTY"; exit 1; }; diff "$TD/r12.s408.base" "$TD/r12.s408.work" > /dev/null || { echo "THE SUPERSEDED ENTRY'S OWN BYTES CHANGED. THIS IS AN APPEND-ONLY DATED HISTORY: THE ENTRY IS AMENDED BY AN APPENDED POINTER, NEVER REWRITTEN"; exit 1; }; echo "SUPERSEDED-ENTRY-BYTE-UNCHANGED"; AM=$(grep -c 'POINTER AMENDMENT' "$ST" || true); [ "$AM" -ge 2 ] || { echo "STATE.md CARRIES $AM POINTER AMENDMENT(S), EXPECTED AT LEAST 2 — d3a139d's ON THE P9-D3 ENTRY AND THIS ROUND'S NEW ONE. AT 1 NOTHING WAS APPENDED"; exit 1; }; NEWAM=$(git diff "$H46"..HEAD -- "$ST" | grep '^+' | grep -c 'POINTER AMENDMENT' || true); [ "$NEWAM" -eq 1 ] || { echo "THIS TASK ADDED $NEWAM POINTER AMENDMENT LINE(S), EXPECTED EXACTLY 1"; exit 1; }; ADD=$(git diff "$H46"..HEAD -- "$ST" | grep '^+[^+]'); ADDN=$(printf '%s\n' "$ADD" | grep -c . || true); [ "$ADDN" -ge 1 ] || { echo "NOTHING WAS APPENDED TO STATE.md"; exit 1; }; FIG=$(printf '%s\n' "$ADD" | grep -cE "\\b(${PIN}|549|574)\\b" || true); [ "$FIG" -eq 0 ] || { echo "THE AMENDMENT SPELLS A SHADOW FIGURE ON $FIG ADDED LINE(S). A POINTER THAT RESTATES A BOUND IS A FOURTH HAND-WRITTEN COPY OF A NUMBER NOTHING COMPARES, AND IT GOES STALE ON THE NEXT COMMIT THAT MOVES A LINE"; exit 1; }; printf '%s\n' "$ADD" | grep -q 'WIDEST_ANCHOR_SHADOW' || { echo "THE AMENDMENT DOES NOT NAME THE SOLE PUBLICATION. A POINTER THAT POINTS AT NOTHING IS A DELETION WITH EXTRA WORDS"; exit 1; }; printf '%s\n' "$ADD" | grep -qE '2026-08-2[0-9]' || { echo "THE AMENDMENT CARRIES NO DATE. AN UNDATED AMENDMENT INSIDE A DATED HISTORY IS THE SHAPE CR-32 IS ABOUT"; exit 1; }; echo "AMENDMENT-POINTS-AND-STATES-NO-FIGURE"; OTHER=$(git diff --name-only "$H46"..HEAD | grep -v '^\.planning/STATE\.md$' | grep -v '01-4[67]-SUMMARY\.md$' | grep -c . || true); [ "$OTHER" -eq 0 ] || { echo "TASK 1 TOUCHED $OTHER FILE(S) BEYOND STATE.md AND ITS SUMMARY; ROADMAP.md AND 01-SECURITY.md ARE TASK 2's: $(git diff --name-only "$H46"..HEAD | grep -v '^\.planning/STATE\.md$' | grep -v '01-4[67]-SUMMARY\.md$' | tr '\n' ' ')"; exit 1; }; CBX=$(grep -c 'CORE11_BOX_EXPECTED = "- \[ \] \*\*CORE-11\*\*"' "$SP" || true); [ "$CBX" -eq 1 ] || { echo "CORE11_BOX_EXPECTED IS NOT THE UNCHECKED FORM: $CBX"; exit 1; }; BOXROW=$(grep -c '^- \[ \] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md || true); [ "$BOXROW" -eq 1 ] || { echo "CORE-11's LEDGER CHECKBOX IS NOT THE UNCHECKED FORM: $BOXROW"; exit 1; }; git diff --quiet "$H46"..HEAD -- .planning/REQUIREMENTS.md || { echo "REQUIREMENTS.md CHANGED SINCE PLAN 01-46's EXIT; THIS PLAN DOES NOT OWN IT"; exit 1; }; N=$(git status --porcelain | grep -c . || true); [ "$N" -eq 0 ] || { echo "TREE DIRTY AT EXIT: $N"; exit 1; }; git diff --exit-code -- packages/ scripts/ && echo TASK1-TREE-CLEAN</automated>
  </verify>

  <acceptance_criteria>
    - THE CENSUS RUN REPO-WIDE, with the exact command pasted, and the full hit list before classification. An explicit statement of whether any surface was found that this plan did not name — and if none was, that the search was wider than the four.
    - The classification table pasted in full: file, line, text, LIVE or DATED HISTORY, reason, owning task. Pass 13's criteria quoted from `01-VERIFICATION.md` rather than paraphrased, and every line carrying both a dated measurement and a present-tense claim flagged as such.
    - The amendment quoted verbatim, with an explicit statement that it names `WIDEST_ANCHOR_SHADOW`, carries a date, and spells NO figure — and the grep output proving the last of those.
    - The superseded entry shown byte-identical to the base revision by diff, and an explicit statement that it was amended rather than rewritten.
    - An explicit note of where this amendment DEPARTS from `d3a139d`'s form at `:307` and why, so a later reader does not harmonise the two into one that restates a bound.
    - Any `state.advance-plan` or `state.update-progress` disagreement recorded verbatim with the file count on disk that was used instead.
    - The pin read FROM THE GATE FILE and shown identical to plan 01-46's exit value, with `packages/` and `scripts/` shown byte-identical since that commit.
  </acceptance_criteria>

  <done>
The census covers every tracked file rather than the four this plan named, every hit is classified with
its reason, and STATE.md carries a dated pointer amendment that names the gate file's constant as the
sole publication and spells no figure, appended beside an entry whose own bytes are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: ROADMAP.md's stale figures deleted rather than corrected, and 01-SECURITY.md's three open threat rows re-adjudicated against claims re-executed in this session rather than re-numbered</name>

  <files>.planning/ROADMAP.md, .planning/phases/01-skeleton-persistence-compatibility/01-SECURITY.md</files>

  <precondition>Task 1 is committed and its classification table exists in `01-46`/`01-47` summary form. Every site edited here is a site that table classifies; a site edited without a classification row is a site edited without a reason.</precondition>

  <read_first>
    - `.planning/ROADMAP.md` at `:65-75`, `:235-255` — the phase-01 plans block and every row Task 1's census flagged.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-SECURITY.md` at `:95-105`, `:145-180` — the rule statement, the dated CLOSED rows and the three open threat rows.
    - The gate spec's pin case and its two constants, so each security row's claim can be checked against the code rather than against a memory of it.
  </read_first>

  <action>
Take ROADMAP.md first. Every figure Task 1 classified LIVE in it is DELETED, not corrected. The rows exist
to say what each plan delivered; a row that also republishes a measured bound is a second publication of a
number the gate file's constant owns, and correcting it just moves the staleness one commit down the line.
Keep each row's description. Remove the width, the raw-span figure and the surface total — that last one
matters on its own terms, because WR-61 already re-measured it and the row still carries the superseded
value, so the row has been wrong about it for longer than it has been wrong about the width. Where a row
genuinely needs to say the measurement happened, say it happened and name where the value lives.

ROADMAP.md is a LIVE INDEX and is edited in place. Do not append an amendment to it: that discipline
belongs to the append-only history and applying it here would leave the false figure standing beside a
note about it, which gives a skimmer two statements and no way to tell which is live — this file's own
words about why superseded clauses are named rather than requoted.

Then 01-SECURITY.md, and this is the substantive half. Three rows carry `open` status and assert facts
about a tree that waves 42 through 45 superseded. CHECK EACH ONE BY EXECUTION BEFORE TOUCHING IT. This
plan's frontmatter predicts what you will find; a prediction adopted without execution is exactly the
method that produced five consecutive false bounds in this phase, and the prediction being right would not
change that.

For the row asserting that nothing pins the named shadow: read the gate spec and determine what pins it
now, in size and in identity, and by which assertions. If the claim is false, CLOSE the row with the
closing evidence — the constants, the assertions, and the waves and plans that installed them — rather
than editing the claim into a weaker true one. A threat row that is no longer true is closed with
evidence; that is what the CLOSED rows already in this file look like and they are the model.

For the row quoting the pin's constant at a superseded value and the row resting on a sentence this same
file records as deleted: establish what each says today, then close, restate or keep, and state which and
why per row. A row that survives must be restated WITHOUT a gate-file line number: every line number in
these rows has now been moved twice, and a citation that slides is a citation that will be wrong again
within one wave. Cite the constant, the assertion title or the case title — names do not slide.

The dated CLOSED rows stay BYTE-UNCHANGED. They name the pass that closed them, they are past tense, and
they are the record of what was believed and when. The verify asserts their bytes against the base
revision. Date every correction you make, so this file's own live/history split stays readable.

Do not touch `.planning/STATE.md` (Task 1's), `.planning/REQUIREMENTS.md` (plan 01-46's) or any file under
`packages/` or `scripts/` (nobody's, this round).
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && SP=packages/backend/src/outbound-prohibition.spec.ts; RM=.planning/ROADMAP.md; SC=.planning/phases/01-skeleton-persistence-compatibility/01-SECURITY.md; TD="${TMPDIR:-/tmp}"; RB=$(git rev-parse 1cb2884) || { echo "ROUND BASE UNRESOLVABLE"; exit 1; }; git merge-base --is-ancestor "$RB" HEAD || { echo "1cb2884 IS NOT AN ANCESTOR OF HEAD"; exit 1; }; H46=$(git log --format=%H -1 -- .planning/phases/01-skeleton-persistence-compatibility/01-46-SUMMARY.md); [ -n "$H46" ] || { echo "COULD NOT RESOLVE PLAN 01-46's EXIT COMMIT"; exit 1; }; git diff --quiet "$H46"..HEAD -- packages/ scripts/ || { echo "THIS PLAN HAS CHANGED CODE SINCE PLAN 01-46's EXIT AND IT OWNS NONE"; exit 1; }; PIN=$(grep -oE 'WIDEST_ANCHOR_SHADOW = [0-9]+' "$SP" | head -1 | grep -oE '[0-9]+'); [ -n "$PIN" ] || { echo "THE SHADOW PIN IS UNREADABLE"; exit 1; }; RMB=$(git show "$RB":"$RM" | grep -cE '\b(549|574)\b' || true); [ "$RMB" -ge 1 ] || { echo "THE ROADMAP CARRIED NO SHADOW FIGURE ON THE BASE REVISION, SO THE CHECK BELOW WOULD PASS HAVING MEASURED NOTHING — RE-DERIVE THE CENSUS"; exit 1; }; echo "ROADMAP-BASE-FIGURE-SITES=$RMB"; RMN=$(grep -nE "\\b(${PIN}|549|574)\\b" "$RM" | grep -c . || true); [ "$RMN" -eq 0 ] || { echo "THE ROADMAP STILL PUBLISHES A SHADOW FIGURE ON $RMN LINE(S): $(grep -nE "\\b(${PIN}|549|574)\\b" "$RM" | cut -d: -f1 | tr '\n' ' '). IT IS A LIVE INDEX AND THE DISPOSITION IS DELETION"; exit 1; }; RMS=$(grep -cE '\b(8,846|8846|9,277|9277)\b' "$RM" || true); [ "$RMS" -eq 0 ] || { echo "THE ROADMAP STILL PUBLISHES A SURFACE-LINE TOTAL ON $RMS LINE(S) — WR-61 ALREADY FALSIFIED THE ONE IT CARRIED AND THE REPLACEMENT WOULD GO STALE THE SAME WAY"; exit 1; }; SCB=$(git show "$RB":"$SC" | grep -cE '\b574\b' || true); [ "$SCB" -ge 5 ] || { echo "01-SECURITY.md CARRIED $SCB FIGURE SITES ON THE BASE REVISION AND AT LEAST 5 WERE CENSUSED — THE EXTRACTOR OR THE PATH IS WRONG AND THE CHECKS BELOW WOULD BE VACUOUS"; exit 1; }; for T in T-01-264 T-01-280 T-01-286; do R=$(grep -c "$T" "$SC" || true); [ "$R" -ge 1 ] || { echo "THREAT ROW $T IS ABSENT FROM 01-SECURITY.md — A ROW DELETED IS NOT A ROW ADJUDICATED"; exit 1; }; done; OPEN=$(grep -E 'T-01-(264|280|286)' "$SC" | grep -c '| open' || true); NOPIN=$(grep -c 'Nothing pins it' "$SC" || true); [ "$NOPIN" -eq 0 ] || { echo "THE ROW ASSERTING THAT NOTHING PINS THE NAMED SHADOW IS STILL PRESENT ON $NOPIN LINE(S). WIDEST_ANCHOR_SHADOW HAS PINNED ITS SIZE SINCE WAVE 42 AND WIDEST_ANCHOR_TOKEN ITS IDENTITY SINCE WAVE 45 — VERIFY THAT BY EXECUTION AND THEN CLOSE THE ROW WITH ITS EVIDENCE"; exit 1; }; STALE=$(grep -E 'T-01-(264|280|286)' "$SC" | grep -cE ':(9[0-9]{3}|10[0-9]{3}|11[0-9]{3})' || true); [ "$STALE" -eq 0 ] || { echo "$STALE OF THE RE-ADJUDICATED ROWS STILL CITE A GATE-FILE LINE NUMBER. EVERY ONE HAS MOVED TWICE; CITE THE CONSTANT, THE ASSERTION OR THE CASE TITLE — NAMES DO NOT SLIDE"; exit 1; }; SCN=$(grep -nE "\\b${PIN}\\b" "$SC" | grep -c . || true); [ "$SCN" -eq 0 ] || { echo "01-SECURITY.md NOW PUBLISHES THE LIVE PINNED VALUE ON $SCN LINE(S) — RE-ADJUDICATION MEANS RUNNING THE CLAIM, NOT RE-NUMBERING IT"; exit 1; }; CLOSEDB=$(git show "$RB":"$SC" | grep -E 'T-01-(280|286)' | grep -c '\*\*CLOSED\*\*' || true); [ "$CLOSEDB" -ge 1 ] || { echo "NO DATED CLOSED ROW FOUND ON THE BASE REVISION FOR THE BYTE-IDENTITY CHECK BELOW, WHICH WOULD THEREFORE COMPARE NOTHING"; exit 1; }; git show "$RB":"$SC" | grep '\*\*CLOSED\*\*' > "$TD/r12.sc.base"; grep '\*\*CLOSED\*\*' "$SC" > "$TD/r12.sc.work"; BCL=$(grep -c . "$TD/r12.sc.base" || true); WCL=$(grep -c . "$TD/r12.sc.work" || true); [ "$WCL" -ge "$BCL" ] || { echo "THE FILE CARRIED $BCL DATED CLOSED ROW(S) AND NOW CARRIES $WCL — A DATED CLOSURE RECORD WAS DESTROYED"; exit 1; }; diff "$TD/r12.sc.base" <(head -n "$BCL" "$TD/r12.sc.work") > /dev/null || { echo "THE PRE-EXISTING DATED CLOSED ROWS ARE NOT BYTE-IDENTICAL. THEY NAME THE PASS THAT CLOSED THEM AND ARE THE RECORD OF WHAT WAS BELIEVED AND WHEN"; exit 1; }; echo "DATED-CLOSED-ROWS-INTACT base=$BCL work=$WCL"; DT=$(git diff "$H46"..HEAD -- "$SC" | grep '^+[^+]' | grep -cE '2026-08-2[0-9]' || true); [ "$DT" -ge 1 ] || { echo "NO CORRECTION IN 01-SECURITY.md CARRIES A DATE. AN UNDATED CORRECTION INSIDE A FILE WITH A LIVE/HISTORY SPLIT IS UNREADABLE AS EITHER"; exit 1; }; OTHER=$(git diff --name-only "$H46"..HEAD | grep -vE '^\.planning/(STATE|ROADMAP)\.md$' | grep -v '01-SECURITY\.md$' | grep -v '01-4[67]-SUMMARY\.md$' | grep -c . || true); [ "$OTHER" -eq 0 ] || { echo "THIS PLAN TOUCHED $OTHER FILE(S) IT DOES NOT OWN: $(git diff --name-only "$H46"..HEAD | grep -vE '^\.planning/(STATE|ROADMAP)\.md$' | grep -v '01-SECURITY\.md$' | grep -v '01-4[67]-SUMMARY\.md$' | tr '\n' ' ')"; exit 1; }; git diff --quiet "$H46"..HEAD -- .planning/REQUIREMENTS.md || { echo "REQUIREMENTS.md CHANGED; THIS PLAN DOES NOT OWN IT"; exit 1; }; CBX=$(grep -c 'CORE11_BOX_EXPECTED = "- \[ \] \*\*CORE-11\*\*"' "$SP" || true); [ "$CBX" -eq 1 ] || { echo "CORE11_BOX_EXPECTED IS NOT THE UNCHECKED FORM: $CBX"; exit 1; }; N=$(git status --porcelain | grep -c . || true); [ "$N" -eq 0 ] || { echo "TREE DIRTY AT EXIT: $N"; exit 1; }; git diff --exit-code -- packages/ scripts/ && echo TASK2-TREE-CLEAN</automated>
  </verify>

  <acceptance_criteria>
    - Every ROADMAP.md edit shown by diff, with the deleted figures listed and an explicit statement that each row still says what its plan delivered.
    - The surface-line total addressed on its own terms: what the row said, what WR-61 measured, and why the disposition was deletion rather than the corrected value.
    - EACH OF THE THREE OPEN THREAT ROWS RE-EXECUTED BEFORE IT WAS TOUCHED, with the check run and its output pasted per row. For the "nothing pins it" row specifically: what pins the size, what pins the identity, which assertions do it, and which waves installed them — read from the gate spec in this session, not taken from this plan's frontmatter.
    - The disposition stated PER ROW — closed with evidence, restated, or kept — with the reason, and every surviving row shown to cite names rather than line numbers.
    - The dated CLOSED rows shown byte-identical to the base revision by diff.
    - Every correction shown carrying a date.
    - An explicit statement that this task edited only sites Task 1's classification table lists, and that no site was edited without a classification row.
  </acceptance_criteria>

  <done>
ROADMAP.md publishes no figure the gate file's constant owns and no surface-line total; each of
01-SECURITY.md's three open rows was re-executed before it was touched and is closed with evidence or
restated without a line number that slides; the dated closure records are byte-unchanged; every
correction is dated.
  </done>
</task>

<task type="auto">
  <name>Task 3: the round's closing baseline executed on a round that touched no code, the Broken Windows decision made explicitly, and criterion (3) answered plainly without touching CORE-11's box</name>

  <files>.planning/WINDOWS.md</files>

  <precondition>Tasks 1 and 2 are committed and the tree is clean. `packages/` and `scripts/` are byte-identical to plan 01-46's exit commit.</precondition>

  <read_first>
    - `.planning/WINDOWS.md`'s frontmatter counters and its entries 32 and 44, for the pointer-not-a-copy rule and the current tail of the ledger.
    - `01-46-SUMMARY.md`'s closing baseline, so this task's figures are reconciled against measured values rather than against `01-VERIFICATION.md`'s.
    - `01-VERIFICATION.md`'s statement of the three criteria and of what criterion (3) requires.
  </read_first>

  <action>
Run the closing baseline even though this plan changed no code, and reconcile every figure against plan
01-46's exit values rather than assuming they carried: the whole suite, `tsc --build`, `lint`, `knip`,
`build:backend`, `check:bundle`, the gate spec's own count, the module count and the gate file's two pin
constants. A round that stops measuring because it only edited markdown is how a broken tree ships behind
a clean-looking diff, and this repository has a `pretest` hook that builds the backend, so "markdown only"
is not even true of what the commands touch.

Make the Broken Windows decision EXPLICITLY rather than by omission. The ledger's entries from 32 onward
are POINTERS rather than copies, by the rule entry 32 states. The DEFAULT for this round is NO NEW ENTRY:
the ledger already points at the gate file's derived residual, and this round did not change what it
points at — it changed which files claim to be compared to it, and the ledger makes no claim about that.
If you conclude an entry IS warranted, record it THROUGH `gsd-tools windows` rather than by hand-editing
the table: entry 26 exists because entry 25 was hand-written and had to be superseded through the tool,
and that is on the record in this file. Either way, state the decision and its reason in the SUMMARY. A
decision made by not making it is the shape this whole round is about.

Audit the whole round's diff across BOTH plans — 01-46 and 01-47 together. State what changed and what did
not: exactly one file under `packages/`, nothing under `scripts/`, exactly four planning files outside the
phase directory at most, and the phase directory's own plan and summary files. Confirm `auditSource` and
every resolver body byte-identical to `1cb2884`.

Then answer criterion (3) plainly, in the SUMMARY, in your own words: is the disclosure now the only bound
stated on every surface a reader touches, contradicted nowhere? Walk the surfaces pass 13 enumerated — the
gate header, the gate prose below the registry, REQUIREMENTS.md above the sentinel, STATE.md, WINDOWS.md,
ROADMAP.md and the phase artifacts — and say for each what this round did to it and what remains
unguarded. NAME WHAT IS STILL UNGUARDED RATHER THAN OMITTING IT: an enumeration that grows silently is
one nobody can audit for completeness in either direction, and this file's own residual records that as a
finding.

DO NOT FLIP CORE-11's BOX AND DO NOT TOUCH `CORE11_BOX_EXPECTED`. Verification pass 14 owns that decision.
If your criterion-(3) walk concludes the box is earned, SAY SO EXPLICITLY AND STOP — write down that you
believe it discharges, what you would flip, and that you did not flip it. The two change in ONE commit
when they change, and the box has already been flipped early and reverted twice.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && SP=packages/backend/src/outbound-prohibition.spec.ts; TD="${TMPDIR:-/tmp}"; B="$TD/defminer-round12-baseline"; [ -f "$B" ] || { echo "MISSING ROUND-12 BASELINE FILE WRITTEN BY PLAN 01-46 TASK 1"; exit 1; }; . "$B"; RB=$(git rev-parse 1cb2884) || { echo "ROUND BASE UNRESOLVABLE"; exit 1; }; git merge-base --is-ancestor "$RB" HEAD || { echo "1cb2884 IS NOT AN ANCESTOR OF HEAD"; exit 1; }; H46=$(git log --format=%H -1 -- .planning/phases/01-skeleton-persistence-compatibility/01-46-SUMMARY.md); [ -n "$H46" ] || { echo "COULD NOT RESOLVE PLAN 01-46's EXIT COMMIT"; exit 1; }; git diff --quiet "$H46"..HEAD -- packages/ scripts/ || { echo "PLAN 01-47 CHANGED CODE AND OWNS NONE"; exit 1; }; pnpm test > "$TD/r12.full4.log" 2>&1; FRC=$?; cat "$TD/r12.full4.log"; [ "$FRC" -eq 0 ] || { echo "WHOLE SUITE RED (rc=$FRC) — A ROUND THAT EDITED ONLY MARKDOWN STILL RUNS pretest, WHICH BUILDS THE BACKEND"; exit 1; }; FT=$(grep -E '^[[:space:]]*Tests[[:space:]]' "$TD/r12.full4.log" | tail -1 | sed -n 's/.*[^0-9]\([0-9][0-9]*\) passed.*/\1/p'); FF=$(grep -E '^[[:space:]]*Test Files[[:space:]]' "$TD/r12.full4.log" | tail -1 | sed -n 's/.*[^0-9]\([0-9][0-9]*\) passed.*/\1/p'); [ -n "$FT" ] && [ -n "$FF" ] || { echo "SUITE COUNTS UNPARSEABLE"; exit 1; }; [ "$FT" -ge "$BASE_TESTS_ARRIVAL" ] || { echo "SUITE COUNT $FT BELOW ARRIVAL $BASE_TESTS_ARRIVAL"; exit 1; }; [ "$FF" -eq "$BASE_FILES" ] || { echo "FILE COUNT $FF != $BASE_FILES"; exit 1; }; pnpm exec vitest run "$SP" > "$TD/r12.gate4.log" 2>&1; GRC=$?; cat "$TD/r12.gate4.log"; [ "$GRC" -eq 0 ] || { echo "GATE SUITE RED (rc=$GRC)"; exit 1; }; GT=$(grep -E '^[[:space:]]*Tests[[:space:]]' "$TD/r12.gate4.log" | tail -1 | sed -n 's/.*[^0-9]\([0-9][0-9]*\) passed.*/\1/p'); [ "$GT" -ge "$BASE_GATE_ARRIVAL" ] || { echo "GATE COUNT $GT BELOW ARRIVAL $BASE_GATE_ARRIVAL"; exit 1; }; pnpm exec tsc --build || { echo "tsc --build RED"; exit 1; }; pnpm typecheck || { echo "typecheck RED"; exit 1; }; pnpm lint || { echo "lint RED"; exit 1; }; pnpm knip || { echo "knip RED"; exit 1; }; pnpm build:backend || { echo "build:backend RED"; exit 1; }; pnpm check:bundle > "$TD/r12.bundle.log" 2>&1 || { cat "$TD/r12.bundle.log"; echo "check:bundle RED"; exit 1; }; cat "$TD/r12.bundle.log"; grep -q '1 import specifier(s): crypto' "$TD/r12.bundle.log" || { echo "THE SHIPPED BUNDLE'S IMPORT SET IS NOT THE SINGLE SPECIFIER crypto"; exit 1; }; M=$(find packages/*/src -name '*.ts' ! -name '*.spec.ts' | grep -c . || true); [ "$M" -eq "$BASE_MODULES" ] || { echo "MODULE COUNT $M != $BASE_MODULES"; exit 1; }; PIN=$(grep -oE 'WIDEST_ANCHOR_SHADOW = [0-9]+' "$SP" | head -1 | grep -oE '[0-9]+'); PIN46=$(git show "$H46":"$SP" | grep -oE 'WIDEST_ANCHOR_SHADOW = [0-9]+' | head -1 | grep -oE '[0-9]+'); [ -n "$PIN" ] && [ -n "$PIN46" ] || { echo "A PIN VALUE IS UNREADABLE ON ONE SIDE, SO THE COMPARISON WOULD BE VACUOUS"; exit 1; }; [ "$PIN" -eq "$PIN46" ] || { echo "THE SHADOW PIN MOVED FROM $PIN46 TO $PIN IN A PLAN THAT TOUCHES NO CODE"; exit 1; }; VN=$(grep -nE "\\b${PIN}\\b" "$SP" | cut -d: -f1); VC=$(printf '%s\n' "$VN" | grep -c . || true); [ "$VC" -eq 1 ] || { echo "THE PINNED VALUE IS WRITTEN ON $VC LINE(S) OF THE GATE FILE AND EXACTLY 1 IS AUTHORISED; PLAN 01-46 LEFT IT AT 1"; exit 1; }; REPO=$(grep -rlnE "\\b${PIN}\\b" .planning/ --include=*.md 2>/dev/null | grep -v 'phases/01-skeleton-persistence-compatibility/01-4[567]-' | grep -v '01-VERIFICATION.md' | grep -c . || true); [ "$REPO" -eq 0 ] || { echo "THE LIVE PINNED VALUE IS PUBLISHED IN $REPO PLANNING FILE(S) OUTSIDE THIS ROUND'S OWN PLANS AND SUMMARIES: $(grep -rlnE "\\b${PIN}\\b" .planning/ --include=*.md 2>/dev/null | grep -v 'phases/01-skeleton-persistence-compatibility/01-4[567]-' | grep -v '01-VERIFICATION.md' | tr '\n' ' '). ONE BOUND, PUBLISHED ONCE"; exit 1; }; echo "ONE-BOUND-PUBLISHED-ONCE-REPO-WIDE pin=$PIN gate_sites=$VC planning_sites=0"; AS='BEGIN{s=0} !s && /^export function auditSource\(/ {s=1; print; next} s && /^\}$/ {print; exit} s {print}'; git show "$RB":"$SP" | awk "$AS" > "$TD/r12.base.auditSource"; awk "$AS" "$SP" > "$TD/r12.work.auditSource"; ABL=$(grep -c . "$TD/r12.base.auditSource" || true); [ "$ABL" -gt 100 ] || { echo "auditSource EXTRACTOR COLLAPSED ON BASE ($ABL) AND WOULD COMPARE NOTHING"; exit 1; }; AD=$(diff "$TD/r12.base.auditSource" "$TD/r12.work.auditSource" | grep -c . || true); [ "$AD" -eq 0 ] || { echo "auditSource CHANGED ACROSS THE ROUND: $AD DIFF LINES"; exit 1; }; echo "auditSource-BYTE-IDENTICAL-ACROSS-ROUND lines=$ABL"; SPECS=$(git diff --name-only "$RB"..HEAD -- packages/ | grep -c . || true); [ "$SPECS" -eq 1 ] || { echo "THE ROUND TOUCHED $SPECS FILE(S) UNDER packages/, EXPECTED EXACTLY 1"; exit 1; }; SCR=$(git diff --name-only "$RB"..HEAD -- scripts/ | grep -c . || true); [ "$SCR" -eq 0 ] || { echo "THE ROUND TOUCHED $SCR FILE(S) UNDER scripts/"; exit 1; }; PLN=$(git diff --name-only "$RB"..HEAD -- .planning/ | grep -v 'phases/' | sort -u); PLNN=$(printf '%s\n' "$PLN" | grep -c . || true); [ "$PLNN" -le 4 ] || { echo "THE ROUND CHANGED $PLNN NON-PHASE PLANNING FILES AND AT MOST 4 ARE OWNED: $PLN"; exit 1; }; echo "ROUND-PLANNING-FILES: $(printf '%s ' $PLN)"; WC=$(git diff --name-only "$H46"..HEAD -- .planning/WINDOWS.md | grep -c . || true); if [ "$WC" -gt 0 ]; then WOPEN=$(sed -n 's/^open_count: *//p' .planning/WINDOWS.md | head -1); WTOT=$(sed -n 's/^total_count: *//p' .planning/WINDOWS.md | head -1); [ -n "$WOPEN" ] && [ -n "$WTOT" ] || { echo "THE WINDOWS LEDGER WAS EDITED AND ITS FRONTMATTER COUNTERS ARE UNREADABLE — AN ENTRY ADDED BY HAND DOES NOT RECOMPUTE THEM AND THAT IS WHY ENTRY 26 EXISTS"; exit 1; }; ROWS=$(grep -cE '^\| [0-9]+ \|' .planning/WINDOWS.md || true); [ "$ROWS" -eq "$WTOT" ] || { echo "THE WINDOWS LEDGER CARRIES $ROWS ENTRY ROWS AND ITS total_count SAYS $WTOT — THE ENTRY WAS HAND-EDITED RATHER THAN RECORDED THROUGH gsd-tools windows"; exit 1; }; echo "WINDOWS-ENTRY-RECORDED open=$WOPEN total=$WTOT rows=$ROWS"; else echo "WINDOWS-LEDGER-UNCHANGED — THE DECISION MUST STILL BE STATED IN THE SUMMARY WITH ITS REASON"; fi; CBX=$(grep -c 'CORE11_BOX_EXPECTED = "- \[ \] \*\*CORE-11\*\*"' "$SP" || true); [ "$CBX" -eq 1 ] || { echo "CORE11_BOX_EXPECTED IS NOT THE UNCHECKED FORM: $CBX"; exit 1; }; BOXROW=$(grep -c '^- \[ \] \*\*CORE-11\*\*' .planning/REQUIREMENTS.md || true); [ "$BOXROW" -eq 1 ] || { echo "CORE-11's LEDGER CHECKBOX IS NOT THE UNCHECKED FORM: $BOXROW"; exit 1; }; git diff --quiet "$H46"..HEAD -- .planning/REQUIREMENTS.md || { echo "REQUIREMENTS.md CHANGED IN PLAN 01-47; IT OWNS NONE OF IT"; exit 1; }; N=$(git status --porcelain | grep -c . || true); [ "$N" -eq 0 ] || { echo "TREE DIRTY AT EXIT: $N"; exit 1; }; git diff --exit-code -- packages/ scripts/ && echo TASK3-TREE-CLEAN</automated>
  </verify>

  <acceptance_criteria>
    - THE CLOSING BASELINE EXECUTED AND RECONCILED LINE BY LINE against plan 01-46's exit values, with every command's output pasted and any difference attributed. `check:bundle`'s single-specifier line quoted verbatim.
    - The two pin constants read from the gate file and shown identical to plan 01-46's exit, with `packages/` and `scripts/` shown byte-identical since that commit.
    - ONE BOUND, PUBLISHED ONCE, PROVED REPO-WIDE: the grep showing the live pinned value on exactly one line of the gate file and on no planning file outside this round's own plans and summaries.
    - THE BROKEN WINDOWS DECISION STATED EXPLICITLY with its reason, whichever way it went. If an entry was recorded, the `gsd-tools windows` invocation pasted and the frontmatter counters shown consistent with the row count; if not, the reason.
    - THE WHOLE-ROUND DIFF AUDIT across both plans: exactly one file under `packages/`, nothing under `scripts/`, the non-phase planning files listed, `auditSource` byte-identical to `1cb2884`.
    - THE CRITERION-(3) WALK, surface by surface, over the seven pass 13 enumerated: what this round did to each and what remains unguarded on it. Anything still unguarded NAMED rather than omitted.
    - A plain statement of whether you believe the box discharges. If you believe it does: an explicit statement that you did not flip it, what you would flip, and both surfaces shown byte-unchanged.
  </acceptance_criteria>

  <done>
The baseline is measured rather than carried; both pins are unchanged and the live value is published on
exactly one line repo-wide; the Broken Windows decision is stated with its reason; the whole-round diff is
audited across both plans; criterion (3) is answered surface by surface with everything still unguarded
named; CORE-11's box is `[ ]` in both surfaces.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning prose → reader | These four surfaces are CLASS ONE UNGUARDED per `REQUIREMENTS.md:170`: no mechanical comparison reaches them. A false statement here is caught only by a human re-deriving it, and CR-32 is the record of that not happening. |
| security register → phase verdict | `01-SECURITY.md` is the file that adjudicates this phase's threats. Open rows describing a superseded tree corrupt the adjudication itself. |
| append-only history → correction | The one way to lose a history is to rewrite it. Every correction here has to be an append or a deletion in a live index, never a regeneration inside the record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-310 | Repudiation | `.planning/STATE.md`'s superseded shadow entry | medium | mitigate | Amended by an APPENDED dated pointer that names `WIDEST_ANCHOR_SHADOW` and spells no figure; the entry's own bytes asserted identical to the base revision, so the record of what was believed and when survives. |
| T-01-311 | Repudiation | a pointer amendment that itself restates the bound | medium | mitigate | The verify greps the ADDED lines for any shadow figure and fails on one; the plan states the mechanical reason rather than a stylistic one, and names `:307` as a dated record that is explicitly not the model. |
| T-01-312 | Repudiation | `.planning/ROADMAP.md` republishing a bound and a surface total the gate file owns | medium | mitigate | Deleted rather than corrected, in a live index rather than an append-only history; the verify fails on any surviving figure and on any surface-line total, including the replacement value. |
| T-01-313 | Tampering | open threat rows describing a tree four waves have superseded | high | mitigate | Each of T-01-264, T-01-280 and T-01-286 re-executed before it is touched; the false "nothing pins it" claim asserted absent; surviving rows required to cite names rather than gate-file line numbers, which have moved twice. |
| T-01-314 | Repudiation | a dated closure record destroyed while correcting the live rows beside it | high | mitigate | The verify extracts every dated CLOSED row from the base revision and requires byte-identity, and requires the count not to fall. |
| T-01-315 | Denial of Service | a census that passes having measured nothing | high | mitigate | Non-vacuity asserted BEFORE every rule: the base revision's figure sites counted on each surface before the post-edit count is trusted, the extractor's base length checked before any diff is believed, and every expected value asserted non-empty. |
| T-01-316 | Elevation of Privilege | CORE-11's box flipped on the strength of a documentation round | critical | mitigate | Both surfaces asserted `[ ]` in all three tasks; `.planning/REQUIREMENTS.md` asserted byte-unchanged since plan 01-46's exit; the plan instructs the executor to state the belief and stop. |
| T-01-317 | Tampering | a Broken Windows entry hand-written into the table instead of recorded through the tool | low | mitigate | If the ledger changed, the verify requires its frontmatter `total_count` to equal the entry row count — the consistency a hand edit does not produce, and the reason entry 26 exists. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | NO package-manager install task exists in this plan, and no code file is in its `files_modified`. `check:bundle` is still executed at close and re-asserts the shipped bundle's entire import set as one specifier, `crypto`. |
</threat_model>

<verification>
- `pnpm test` exits 0 at no fewer test files and no fewer tests than the round-12 arrival baseline.
- `pnpm typecheck`, `pnpm lint`, `pnpm knip`, `pnpm build:backend` all exit 0.
- `pnpm check:bundle` prints exactly one import specifier, `crypto`.
- The gate spec alone exits 0 at no fewer tests than arrival.
- `packages/` and `scripts/` are byte-identical to plan 01-46's exit commit across every commit in this plan.
- Both pin constants read the values plan 01-46 left them reading; the live pinned value appears on exactly one line of the gate file and on no planning file outside this round's own plans and summaries.
- `.planning/STATE.md`'s superseded entry is byte-identical to `1cb2884`; a new dated pointer amendment is appended; the added lines carry no shadow figure and do name `WIDEST_ANCHOR_SHADOW`.
- `.planning/ROADMAP.md` carries no shadow figure and no surface-line total.
- `01-SECURITY.md` carries no assertion that nothing pins the named shadow; its re-adjudicated rows cite no gate-file line number; its dated CLOSED rows are byte-identical to `1cb2884` and none was lost.
- `auditSource` is byte-identical to `1cb2884`.
- `CORE11_BOX_EXPECTED` and `.planning/REQUIREMENTS.md`'s CORE-11 row are both the unchecked form, and `REQUIREMENTS.md` is byte-unchanged since plan 01-46's exit.
- `git diff --exit-code -- packages/ scripts/` is clean at the end of every task.
</verification>

<success_criteria>
- The census is repo-wide and complete, and every hit is classified LIVE or DATED HISTORY with its reason before any surface is edited.
- CR-32 is closed by the discipline each surface calls for: a dated pointer amendment that states no figure, deletion in the live index, re-adjudication against executed evidence in the live register.
- The three open threat rows were re-run before they were touched, and every surviving row cites a name rather than a line number.
- No code changed, both pins are invariant, and the live bound is published on exactly one line repo-wide.
- Criterion (3) is answered surface by surface with everything still unguarded named, and CORE-11's box was not touched.
</success_criteria>

<output>
Create `.planning/phases/01-skeleton-persistence-compatibility/01-47-SUMMARY.md` when done.

It MUST carry, in this order: the repo-wide census command and its full hit list; the classification table
with one row per hit; the STATE.md amendment quoted with the proof it spells no figure and the note of
where it departs from `d3a139d`; the ROADMAP.md deletions by diff; each of the three security rows'
re-execution output and its disposition with reason; the dated CLOSED rows proved byte-identical; the
Broken Windows decision and its reason; the closing baseline reconciled against plan 01-46's exit values;
the whole-round diff audit across both plans; and the criterion-(3) walk over all seven surfaces with
everything still unguarded named — ending with a plain statement about CORE-11's box and the fact that it
was not flipped.
</output>
