---
phase: 01-skeleton-persistence-compatibility
plan: 44
type: execute
wave: 44
gap_closure: true
depends_on: [01-43]
files_modified:
  - packages/backend/src/outbound-prohibition.spec.ts
# ROUND 11's FIRST PLAN, AND ROUND 11 IS A DELETION ROUND. READ THIS BLOCK BEFORE THE TASKS.
#
# THE OPERATOR DECISION THAT DEFINES THIS ROUND, IN ITS OWN WORDS FROM `01-VERIFICATION.md`'s
# `## Is exit 2 converging?`: exit 2's MECHANISM half converged and its DISCLOSURE half did not, and the
# blocker rate across rounds 8, 9 and 10 is 1 -> 2 -> 5 and is NOT decaying. FIVE OF FIVE BLOCKERS THIS
# ROUND ARE DEFECTS IN PROSE ROUND 10 ITSELF WROTE. ZERO ARE PRE-EXISTING. The structural reason is
# stated by the verifier and it is not about discipline: under this file's own standard a sentence
# stating a measured reach IS an assertable claim held to the same bar as the code, so every corrective
# sentence is new attack surface and THE CORRECTIVE ACT IS THE GENERATIVE ACT. Rounds 8, 9 and 10 each
# closed their finding by writing a longer, truer paragraph, and each of those paragraphs became the
# next round's blocker.
#
# THEREFORE: CR-24, CR-25 AND CR-27 CLOSE BY REMOVING BYTES. Adding a longer, truer sentence in their
# place is PROHIBITED — pass 11 says so in those words and this plan's Task 2 gate enforces it
# mechanically. Removing bytes cannot create a new assertable claim; that is the round's entire thesis.
#
# THE RULE FOR EVERY SENTENCE THIS ROUND *DOES* WRITE, AND IT IS THE ONE PIECE OF NEW GUIDANCE HERE:
# PREFER AN AUTHORISATION TO A MEASUREMENT. A sentence that states a measured reach can be falsified by
# a later measurement and becomes the next round's blocker. A sentence that states a POLICY — what a
# future author is permitted or required to do — cannot be falsified by measuring this file, because it
# makes no claim about this file's contents. WR-65's fix is an authorisation. WR-64's is a measurement
# and must therefore carry its reach INSIDE the claiming clause, not in a neighbouring sentence.
#
# CR-28 — BLOCKER, AND PASS 11 SAYS DO IT FIRST. It is the only one of the five that is a code fix.
# The fixture at `:10440-10500` is introduced at `:10433-10434` with its reason for existing: *a future
# author who swaps either pin above back to a prefix matcher makes that case a tautology again; this one
# turns red instead of arguing*. IT CANNOT. It declares its OWN `REAL_OPENER` (`:10441-10442`), its OWN
# prefix finder (`:10461-10463`) and its OWN full-line finder (`:10464`) over a frozen seven-element
# literal three lines above, and reads NOTHING from the case it claims to protect — not `REGISTRY_OPEN`,
# not `registryOpen`, not `EXCLUSIONS`. Its four assertions are facts about a hardcoded array and about
# `String.prototype.startsWith`.
#   EXECUTED BY PASS 11 IN TWO STEPS, BOTH GREEN. Step 1: `:10362` `lineOf((l) => l === REGISTRY_OPEN)`
#   swapped to `lineOf((l) => l.startsWith("export const RESOLVER_REGISTRY"))` -> `Tests 441 passed
#   (441)`. Step 2: with that in place, a `RESOLVER_REGISTRY_SHADOW` declaration planted at line 4057 ->
#   `Tests 441 passed (441)`. Measured from inside the suite by instrumenting `SURFACE_LINES`:
#   `{"surface":9285,"excl":[[983,1564],[5985,5995],[4161,5793]]}` without the decoy,
#   `{"surface":9181,...[4057,5794]]}` with it — ONE HUNDRED AND FOUR pre-existing gate-file lines
#   silently off the scanned surface, at 441 of 441 green. CR-23's tautology restored THROUGH the
#   fixture written to make it red.
#   THE FIX PASS 11 PRESCRIBES: make the fixture read the SHIPPED expression instead of re-declaring it
#   — hoist `REGISTRY_OPEN` and its full-line RESOLUTION to the enclosing `describe` (the one opening at
#   `:9588`, where `gateLines` `:9594` and `lineOf` `:9946` already live) and have the fixture assert
#   THAT, so swapping the shipped resolution to a prefix matcher makes `fullLineHit` equal `prefixHit`
#   and the case goes red. A permanent case whose justification is false is worse than no case, because
#   the next reviewer reads the justification.
#   AND THE RESIDUE THE FIX DOES NOT CLOSE, WHICH THIS PLAN REQUIRES DISCLOSED RATHER THAN CLAIMED: the
#   fixture turns red on an edit to the SHARED RESOLUTION. An author who leaves the shared binding alone
#   and inlines a fresh prefix predicate at the pin's own call site still bypasses it. That is a
#   DIFFERENT edit from the one pass 11 executed, it is not closed here, and the bytes must say so with
#   the reach inside the claiming clause.
#
# CR-24 — BLOCKER, CLOSES BY DELETION AT BOTH SITES. `:10777-10780` and `:11225-11227` assert, present
# tense and dated *today, 2026-08-26 wave 43*, that the two identically-headed table headers sit at two
# named lines. MEASURED BY PASS 11 WITH THE SHIPPED BUILDER: `:299` normalizes to `SPELLING (rebind,
# receiver-key position) RESOLVED BY REPORTS` and `:361` to `SPELLING (??=, receiver-key position)
# RESOLVED BY REPORTS` — NOT identical — and `constructAnchorFor` gives one anchor at 304/305 and a
# DIFFERENT one at 363, so a cell moved between them would NOT keep a byte-identical key, which is the
# entire property the sentence exists to illustrate. A sweep over all 25 recogniser-(4) table headers
# found ZERO non-trivial duplicates: the only repeats are `}`, `});` and `);`, none of which is a table
# and none of which is 57 or 58 lines from another. THE 57/58 PAIR STANDS ON `01-39-SUMMARY.md:531` AND
# `:931` ALONE AND NEEDS NO LIVE RE-CHECK. Delete the live re-check; KEEP the record pointer.
#
# CR-25 — BLOCKER, CLOSES BY DELETION. Reach statement (2) at `:10173-10175` publishes a silent-growth
# ceiling implying growth one line further would report. Pass 11 inserted 291 filler `//` lines into the
# second-widest shadow's region and took it to exactly the maximum: TWO shadows at 574, one DOUBLED in
# width, and the pin reported a tick at `Tests 441 passed (441)`. Limit (5) at `:9110-9111` states the
# same fact CORRECTLY — *a non-maximal shadow may grow up to that maximum with nothing reporting* —
# 1,065 lines away. TWO NUMBERS FOR ONE MEASUREMENT INSIDE ONE ROUND IS WR-60's EXACT SHAPE, IN THE
# ROUND THAT CLOSED WR-60 FOR IT. A second statement of one measurement IS the defect, so the second
# statement goes and the correct one stays.
#
# CR-27 — BLOCKER (disclosure completeness), CLOSES BY DELETION. The retirement bracket at `:10634-10645`
# claims a bounded enumeration found ONE positive site claim in TWO places and FIVE negative ones, and
# closes with a universal over all of them. Three shipped assertion messages assert by contraposition
# that the anchor's normal behaviour IS to resolve the enclosing construct — a containment reach
# statement (5) at `:10182-10184` denies in its own bytes and the anchoring docblock disowns BY NAME at
# `:9396-9398`. Two of the three sit inside SHIPPED FAILURE MESSAGES a reader sees while the suite is
# red. AN ENUMERATION WHOSE STATED COMPLETENESS EXCEEDS ITS EXECUTED COMPLETENESS IS THE SIGNATURE
# DEFECT ONE META-LEVEL UP, and it is load-bearing: that bracket IS the evidence that exit 2 was taken
# cleanly.
#   AND A CORRECTION TO PASS 11 FOUND BY THIS PLANNER AND OWED BACK TO IT, BECAUSE IT IS THE SAME
#   FAILURE IN MINIATURE: a CASE-SENSITIVE grep for the lowercase word finds 8 sites and MISSES
#   `:11136`, which spells it in capitals. THE CASE-INSENSITIVE POPULATION AT `882ff17` IS TEN:
#   `:9242 :9382 :9397 :9411 :9465 :10756 :10985 :10999 :11128 :11136`. Pass 11 named five of the ten
#   (three to delete, two as further instances) and did not report `:9465`, `:10756` or `:11128`. Wave
#   41's own case-insensitive sweep found `:981`, invisible to every case-sensitive grep in the plan, in
#   the review and in pass 9 — THIS IS THAT, AGAIN, AND IT IS WHY TASK 2 RE-DERIVES THE POPULATION
#   RATHER THAN CITING IT. The three prescribed for deletion stay exactly three; the enumeration of what
#   survives goes in the SUMMARY, NOT into this file, because a sentence in the file is an assertable
#   claim and a dated sentence in a SUMMARY is a record.
#
# WR-61 — WARNING, AND THE DELETION-SHAPED OPTION IS TAKEN. Both published sweep scopes were STALE IN
# THE COMMIT THAT WROTE THEM. `:9095` and `:10196` state a surface-line total measured today at 9,277
# (the difference is round 10's own net line count). `:9270-9271` and `:10680-10681` state a `gateLines`
# total and a `wc -l` measured today at 11,503 and 11,502 (the difference is commit `137c427`'s own
# diffstat — the sweep ran, then the 86 lines describing it were added to the file it had just swept).
# BOTH RESULTS STILL HOLD; pass 11 re-ran both. What is wrong is the STATED SCOPE of two sweeps whose
# whole value is that they were exhaustive. Pass 11 offers two closes and this plan takes the second:
# STOP PUBLISHING TOTALS AND PUBLISH ONLY RESULTS. A total that moves with every edit and is re-derived
# at the end of every round is a standing invitation to the same defect; the exhaustiveness the sentences
# need is already carried by the cases' own exhaustive-by-construction paragraphs.
#
# PROHIBITED THIS ROUND, AND PASS 11 EXPLICITLY DID NOT REPORT THEIR ABSENCE AS A DEFECT: any AST,
# parser, `typescript`-API or enclosing-frame identity work; any hand-rolled containment by brace depth
# or indentation; any further narrowing of `constructAnchorFor`'s recognisers, its scan bounds or the key
# format. `import ts from "typescript"` SITS IN THIS FILE AT `:1570` — it is a temptation, not a hurdle,
# and it predates this round. Every structural check in this plan is scoped to the ROUND-BASE DIFF for
# exactly that reason; a whole-file grep would fire on a line no one here wrote.
#
# THE BOX DOES NOT MOVE. `.planning/REQUIREMENTS.md:46` reads `- [ ] **CORE-11**`, `CORE11_BOX_EXPECTED`
# at `:11414` agrees, round 10 did not flip it and neither of its plans ran `requirements mark-complete`.
# PRESERVE THAT PATTERN EXACTLY. Pass 12 makes the call. Do not flip it and do not revert it.

autonomous: true
requirements: [CORE-11]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low
# `estimate-calibration` reports `{"factor": 1, "applied": false, "sample_count": 0, "min_samples": 3}`,
# so `tokens` equals `raw_tokens` and the confidence is DERIVED from the sample count, never self-rated.
# WEIGH THE MEASUREMENT COUNT OVER THE TOKEN FIGURE: one shipped resolution hoisted and read by the
# fixture that claims to guard it, one two-step swap watched RED, four items closed by REMOVING BYTES in
# a single commit, and one removals-only gate itself watched RED against a planted word.

must_haves:
  truths:
    - "THE FIXTURE READS THE SHIPPED EXPRESSION. The full-line resolution of the registry's opening line exists ONCE, at the enclosing `describe` scope, and the pin case's uniqueness count, the pin case's resolution and the fixture's full-line answer all route through that one binding. Swapping it to a prefix matcher makes `fullLineHit` equal `prefixHit` and the fixture RED — which is what `:10433-10434` already says happens and could not do."
    - "THE TWO-STEP SWAP IS WATCHED FAILING, NOT READ. Pass 11's step 1 (the shipped resolution swapped to a prefix matcher) is planted against the corrected fixture and the suite is observed RED with the failing title and the FULL assertion message pasted; step 2 (the shadow declaration planted 104 lines above the real opener) is run on top of it and the surface amputation is counted from inside the suite. Both are restored and `git diff --exit-code` is clean before the task closes."
    - "WHAT THE FIXTURE DOES NOT CATCH IS IN THE BYTES, WITH ITS REACH INSIDE THE CLAIMING CLAUSE. The file states that the fixture turns red on an edit to the SHARED RESOLUTION and that an author who inlines a fresh prefix predicate at the pin's own call site bypasses it. That residue is DISCLOSED, not closed."
    - "CR-24, CR-25, CR-27 AND WR-61 ARE CLOSED BY REMOVING BYTES, IN ONE COMMIT THAT ADDS NO WORD IT DID NOT REMOVE. The commit touches one file; its removed-line count exceeds its added-line count; and the set of word tokens on its added lines is a SUBSET of the set on its removed lines, asserted by a numeric check at zero difference. The check is itself watched RED against one planted word before it is trusted."
    - "THE LIVE RE-CHECK GOES AND THE RECORD STAYS. Neither present-tense locator survives at either site, by numeric grep at zero, and the pointer to `01-39-SUMMARY.md:531` and `:931` — the only thing the 57/58 pair now stands on — survives, by numeric grep at one or more."
    - "ONE MEASUREMENT IS STATED ONCE. The silent-growth ceiling is gone from reach statement (2) by numeric grep at zero, and limit (5)'s correct statement of the same fact survives by numeric grep at one or more."
    - "THE THREE CONTRAPOSITION MESSAGES NO LONGER CLAIM A CONTAINMENT, AND THE BRACKET NO LONGER CLAIMS A COMPLETENESS IT DID NOT EXECUTE. The case-INSENSITIVE population of the word is re-derived at the task's start and at its close, both counts pasted, the disposition of every survivor recorded IN THE SUMMARY AND NOT IN THIS FILE, and the bracket's numeric completeness clause removed rather than restated at a larger number."
    - "NO TOTAL THAT MOVES WITH EVERY EDIT IS PUBLISHED. The two stale surface-line figures and the two stale `gateLines`/`wc -l` figures are absent by numeric grep at zero, and each sweep's RESULT survives at its site."
    - "NOTHING LEAKS AND THE MUST-NOT DOES NOT MOVE. `pnpm test` exit 0 at or above 31 files / 1381 tests, `tsc --build` exit 0, the shipped bundle at exactly ONE import specifier `crypto`, the gate suite at or above 441, 23 shipped modules walked at zero violations, and no shipped source byte changed — each EXECUTED at every task boundary, none carried."

  artifacts:
    - path: "packages/backend/src/outbound-prohibition.spec.ts"
      provides: "One describe-scope full-line resolution of the registry's opening line, consumed by the uniqueness count, the pin and the fixture alike, so the fixture's stated reason for existing becomes executable; and four disclosure defects closed by a single removals-only commit that introduces no word it did not remove."
      contains: "REGISTRY_OPEN"

  key_links:
    - from: "a fixture that declares its own opener, its own prefix finder and its own full-line finder over a frozen literal and reads nothing from the case it guards"
      to: "a fixture that applies the SHIPPED full-line resolution to its synthetic array, so an edit to that resolution turns it red"
      via: "the resolution hoisted to the enclosing `describe` at `:9588` and consumed by the pin case's hit count, the pin case's resolution and the fixture alike, with pass 11's own two-step swap watched RED against it"
      pattern: "REGISTRY_OPEN"
    - from: "a correction that closes a wrong sentence by writing a longer, truer one and generates the next round's blocker"
      to: "a correction that closes it by removing bytes and can generate nothing"
      via: "one commit carrying all four deletions, gated on added word tokens being a subset of removed word tokens, with the gate watched RED against a single planted word"
      pattern: "DELETION-ONLY"
    - from: "an enumeration whose stated completeness exceeds its executed completeness, written into the file as a live claim"
      to: "a dated enumeration recorded in the SUMMARY, with the file carrying no completeness claim at all"
      via: "the case-INSENSITIVE population re-derived at both ends of the task and its disposition written where a record belongs rather than where a claim would be"
      pattern: "enclos"

  prohibitions:
    - requirement_id: CORE-11
      category: security
      status: resolved
      verification: gate
      resolution: "The deletion commit resolved by its subject, asserted to be exactly one commit touching exactly one file, its added-line count asserted strictly below its removed-line count, and the set difference of added word tokens minus removed word tokens asserted at ZERO — with that check watched RED against one word planted on an added line and then restored"
      statement: "MUST NOT close CR-24, CR-25, CR-27 or WR-61 by replacement. No sentence may be written in place of a deleted one. Pass 11's convergence finding is that the corrective act IS the generative act and that rounds 8, 9 and 10 produced 1, then 2, then 5 blockers doing exactly this. A plan that replaces any of these three blockers with new prose has misunderstood the round."
    - requirement_id: CORE-11
      category: security
      status: resolved
      verification: gate
      resolution: "A structural check over the ROUND-BASE DIFF shows ZERO added or removed lines carrying the compiler-API import form or a compiler-API member access, ZERO added non-comment lines carrying a hand-rolled containment spelling, and ZERO added or removed lines carrying any recogniser, scan-bound or key-format token; and every body-range byte-identity pair is asserted at 0 diff lines with BOTH sides asserted at an exact, non-vacuous, separately re-derived length"
      statement: "MUST NOT build an AST, a parser, a compiler-API identity or an enclosing-frame identity by any spelling, MUST NOT hand-roll containment by brace depth or indentation, and MUST NOT narrow the recognisers, the scan bounds or the key format. Exit 1 was not taken. `import ts from \"typescript\"` at `:1570` predates this round; the checks are scoped to the round-base diff so that a pre-existing line cannot satisfy or violate them."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "The hoisted resolution shown by diff to be a FULL-LINE equality and not a prefix matcher; `EXCLUSIONS`' own opening locators shown by a numeric check to be unremoved and unchanged; both uniqueness assertions re-run after the hoist and both shown still at one"
      statement: "MUST NOT change `EXCLUSIONS`' own opening locators to the full-line form, and MUST NOT delete either `.from` assertion. CR-23 closed because the two sides are INDEPENDENT expressions — a prefix matcher on one side and a full-line equality on the other. Making both sides the same expression restores the tautology in the same commit that claims to remove it, and pass 11 confirmed CR-23's pins are real by watching both decoys go red."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "Pass 11's step-1 swap planted against the corrected fixture and the suite observed RED with the failing title and full message pasted; step 2 run on top and the surface delta counted from inside the suite; both restored; `git diff --exit-code` clean; the prior 441-of-441 green cited from `01-VERIFICATION.md`; and the removals-only gate observed RED against one planted word"
      statement: "MUST NOT ship a guard that has never been watched failing. Four consecutive rounds shipped a gate that passed with its own defect present. Plant the violation, watch the gate, keep only what goes red. If a planted violation does NOT go red, the task stops and the diagnosis is written up as a first-order finding; nothing is adjusted to force a red."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "`git status --porcelain .planning/` asserted EMPTY numerically at every task boundary; `requirements mark-complete` present in no command run; `git diff` over `.planning/REQUIREMENTS.md` asserted at zero lines; `CORE11_BOX_EXPECTED` asserted byte-unchanged and still reading the unchecked form"
      statement: "MUST NOT flip CORE-11's checkbox and MUST NOT revert it either. It has stood at `[ ]` for four consecutive rounds with the pin, the ledger row and every verification agreeing, and pass 11 states the determination is its own to make and that the box may not move. Pass 12 owns the question."
    - requirement_id: CORE-11
      category: safety
      status: resolved
      verification: gate
      resolution: "Every unadjudicated finding named anywhere in this plan is stated as reviewer-reported and not independently re-executed wherever it is mentioned, and none appears in any list of closed items"
      statement: "MUST NOT present IN-45, IN-46, IN-48 or IN-49 as verified or as closed. Pass 11 records them as reported by the reviewer and NOT independently re-executed while five blockers were open. IN-47's element-five instance is the one exception — pass 11 confirmed it by reading `:10448` and says so — and it is closed here by deletion because it sits inside the block this plan is already opening."
    - requirement_id: CORE-11
      category: safety
      status: open
      verification: gate
      resolution: "DISCLOSED, NOT CLOSED — stated wherever this plan's result is stated, in the bytes and in the SUMMARY, that the fixture turns red on an edit to the SHARED RESOLUTION and not on a fresh prefix predicate inlined at the pin's own call site; that a deletion removes a false claim and adds no true one, so nothing is proved by it; that removing a stale total does not make either sweep more exhaustive than it was; and that the round's own count of the contraposition word is a measurement of this file at this commit and not a property of any grep"
      statement: "MUST NOT state or imply that making the fixture read the shipped expression closes the tautology CLASS, that deleting a false sentence establishes the true one, that removing a stale scope figure re-runs the sweep, or that this round closes criterion (3). The signature defect stands at ELEVEN instances and round 10 authored five more while correcting it. Every sentence this round writes must carry its reach inside the claiming clause."
---

<objective>
Make CR-28's fixture read the shipped expression it claims to guard, and then close CR-24, CR-25, CR-27
and WR-61 by removing bytes in a single commit that introduces no word it did not remove.

Purpose: pass 11 found five blockers, every one of them in prose round 10 itself wrote, and judged that
the residue is bounded and closable in one more round ONLY under a deletion-only discipline. This plan
carries the round's tracer — the one code fix pass 11 says to do first — and the whole of its deletion
half, in that order, so that the additions in plan 01-45 are written once, against a file from which the
false sentences have already gone.

Output: one gate-spec file in which the CR-23 fixture is executable rather than decorative, four
disclosure defects are absent by numeric grep, and the commit that removed them is provably additive of
nothing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md
@.planning/phases/01-skeleton-persistence-compatibility/01-SECURITY.md
@.planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md
@.planning/phases/01-skeleton-persistence-compatibility/01-43-SUMMARY.md
@packages/backend/src/outbound-prohibition.spec.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: CR-28 — the registry's full-line resolution hoisted to the enclosing describe and consumed by the fixture that claims to guard it, with pass 11's two-step swap watched turning it RED and the residue it does not catch written into the bytes</name>

  <files>packages/backend/src/outbound-prohibition.spec.ts</files>

  <reversibility rating="reversible">
    A hoist and a re-pointed assertion inside one test file, recoverable by `git revert` with no data,
    schema or interface consequence. No shipped source byte is touched.
  </reversibility>

  <precondition>
    `01-43-SUMMARY.md` exists on disk and plan 01-43's commits are in history. `git rev-parse HEAD`
    pasted, and `git merge-base --is-ancestor 882ff17 HEAD` asserted true — `882ff17` is THIS ROUND'S
    BASE and every structural check in both plans is scoped to a diff against it. `git status --porcelain
    packages/ scripts/ .planning/` asserted clean and PASTED. The round baseline file at
    `"${TMPDIR:-/tmp}/defminer-round11-baseline"` exists; if absent, CREATE it at the round base before
    the first edit and say so in the SUMMARY. It must carry, each MEASURED in this session and not
    carried from round 10: `BASE_GATE_ARRIVAL`, `BASE_TESTS_ARRIVAL`, `BASE_FILES`, `BASE_MODULES`,
    `BASE_GATE_CURRENT`, `BASE_TESTS_CURRENT`, and one body-length variable per byte-identity subject —
    `constructAnchorFor`, `constructTokenOf`, `exemptionKeyFor`, `maskQuantifiers`, `normalizeGateLine`,
    `nameableRemainder`, `auditSource` and the import count. Round 10 recorded 47 / 7 / 12 / 9 / 5 / 11 /
    1179 and 4; those are RECONCILIATION ANCHORS measured at `483a8ee`, and a disagreement at `882ff17`
    is a finding to write up, not a number to adopt silently. Pass 11's arrival figures — gate suite 441,
    whole suite 31 files / 1381 tests, `tsc --build` exit 0, bundle at one specifier `crypto`, 23 modules
    at zero violations, `wc -l` 11502 — are RE-MEASURED here, not assumed. HALT on any red. No active git
    hooks: run the gate set by hand at the boundary.
  </precondition>

  <read_first>
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md` — the `artifacts:` entry
      beginning `CR-28 — BLOCKER, REPRODUCED IN TWO STEPS AT 441/441 GREEN BOTH TIMES`, the `missing:`
      entry beginning `CR-28 — THE ONLY ONE OF THE FIVE THAT IS A CODE FIX`, and the body section
      `### CR-28 — CONFIRMED. Two steps, both green. The most important of the five.` (~`:174-208`). The
      body carries the two swap steps verbatim and the instrumented `SURFACE_LINES` measurement
      `9285 -> 9181` with the exclusion-three endpoint sliding `4161 -> 4057`. RE-DERIVE BOTH NUMBERS
      against this tree; a third value is a finding to write up, not a number to adopt.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-REVIEW.md`, the round-10 section
      (~`:6127-6890`), `### CR-28` in full. It carries the fix in code shape. USE THE SHAPE, MEASURE THE
      NUMBERS.
    - `packages/backend/src/outbound-prohibition.spec.ts` — the enclosing `describe` opening at `:9588`
      (`the residual is DERIVED — the registry is bound to the walk, and the shipped text to the
      registry`), `gateText`/`gateLines` at `:9592-9594`, `lineOf` at `:9946-9951` (note it returns a
      ONE-BASED line number and `-1` when nothing matches), the pin case opening at `:10113`, the
      `REGISTRY_OPEN` declaration at `:10353-10354`, its hit-count assertion at `:10355-10361`, the
      resolution at `:10362`, the `LIST_OPEN` block at `:10364-10371`, the fixture's introduction at
      `:10427-10439`, and the whole fixture case at `:10440-10500` including `REAL_OPENER` at
      `:10441-10442`, the synthetic array at `:10443-10450`, the prefix finder at `:10461-10463` and the
      full-line finder at `:10464`. RE-DERIVE EVERY ONE BY IDENTIFIER, not by line number — this file is
      11,502 lines and every prior round moved these.
    - `packages/backend/src/outbound-prohibition.spec.ts` — `:10344-10352`, the paragraph that states why
      the two sides must stay INDEPENDENT expressions and forbids "simplifying" the duplication. THE
      HOIST MUST NOT VIOLATE IT: `EXCLUSIONS`' own openers at `:10060-10067` stay prefix matchers and are
      not touched by this task at all.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-43-PLAN.md`, its Task 1 `<verify>` block.
      The suite-parsing idiom, the body-range byte-identity loop and the structural nets in this plan are
      that block's, kept deliberately: they are the ones pass 11 exercised.
  </read_first>

  <behavior>
    - With the hoisted resolution in place and the file otherwise unmodified: the fixture is GREEN, its
      prefix answer and its full-line answer over the synthetic array DIFFER, and the whole gate suite is
      green at or above the recorded arrival count.
    - With the shipped resolution swapped to a prefix matcher (pass 11's step 1) and nothing else changed:
      the fixture's full-line answer over the synthetic array becomes EQUAL to its prefix answer and the
      fixture case is RED. At `882ff17` this same swap gave `Tests 441 passed (441)`.
    - With that swap in place AND a registry-extending declaration planted 104 lines above the real opener
      (pass 11's step 2): the exclusion-three opening endpoint slides and the scanned surface shrinks by
      the measured amount. At `882ff17` this gave `Tests 441 passed (441)`; after this task at least the
      fixture is RED.
    - With the shared binding untouched but a fresh prefix predicate inlined at the pin's own call site:
      the fixture stays GREEN. THIS IS THE RESIDUE AND IT IS DISCLOSED, NOT CLOSED.
  </behavior>

  <action>
    RE-DERIVE EVERY LINE NUMBER IN `<read_first>` BEFORE THE FIRST EDIT, by identifier and by case title,
    and record any disagreement with this plan as a finding in the SUMMARY. Pass 11 measured them at
    `882ff17` and nothing under `packages/` has changed since, so agreement is expected and a
    disagreement means something moved that no one recorded.

    MEASURE FIRST, EDIT SECOND. Before touching anything, paste: all three exclusions' `from`, `to` and
    size; `SURFACE_LINES.length`; the line each of the two full-line locators resolves to; the line each
    of `EXCLUSIONS`' own prefix locators resolves to; and the widest-shadow pin's current maximum. These
    are the reconciliation set for everything below and for plan 01-45.

    THE HOIST. Introduce, at the scope of the `describe` opening at `:9588` and beside the existing
    `lineOf` helper, ONE binding that carries the registry's opening line as a FULL-LINE equality and ONE
    resolution over an arbitrary array of lines that uses it — a function of `readonly string[]` returning
    a zero-based index, so that the SAME expression can be applied both to `gateLines` and to the
    fixture's synthetic array. Keep the existing constant's name, `REGISTRY_OPEN`, so that every prose
    reference to it in this file stays true. Then re-point THREE consumers at that one binding: the hit
    count that proves the locator matches exactly one line, the pin case's own resolution at `:10362`
    (preserving `lineOf`'s one-based semantics and its `-1`-on-miss behaviour, which the uniqueness
    assertion above it already guards), and the fixture's full-line answer. AFTER THE HOIST THE FULL-LINE
    RESOLUTION EXISTS EXACTLY ONCE IN THIS FILE.

    DO NOT hoist or alter `LIST_OPEN`, its hit count or its resolution — CR-28 names one construct and
    widening the edit widens the surface this round has to defend. DO NOT touch `EXCLUSIONS`' own opening
    locators; the paragraph at `:10344-10352` says why in its own bytes and CR-23 closed because the two
    sides are different expressions.

    THE FIXTURE. Delete its private full-line finder and its private opener declaration and have it apply
    the hoisted resolution to its synthetic array instead. The synthetic array's decoy element and its
    real-opener element must both survive; the element that carried the private constant now carries the
    hoisted one. Keep the prefix finder exactly as it is — it is the independent side and it must stay
    independent. Keep the non-vacuity assertion above the rule and keep all four existing assertions'
    messages, re-pointed only where they name a binding that no longer exists.

    IN-47, CLOSED BY DELETION BECAUSE THIS TASK IS ALREADY IN THE BLOCK. The introduction at
    `:10437-10439` asserts a universal about every line of the synthetic array that pass 11 confirmed
    false of element five by reading `:10448`, and the hoist does not make it true. REMOVE that universal
    rather than restating it at a narrower scope. Everything else in `:10427-10439` stays, including the
    sentence at `:10433-10434` that states the fixture's reason for existing — THAT SENTENCE BECOMES TRUE
    FOR THE FIRST TIME IN THIS TASK, which is the whole point of the fix, and pass 11's fallback of
    deleting it is NOT taken because the hoist is done.

    THE ONE SENTENCE THIS TASK WRITES, AND IT CARRIES ITS REACH INSIDE THE CLAIMING CLAUSE. State, beside
    the fixture, that it turns red on an edit to the shared resolution and that an author who leaves the
    shared binding alone and inlines a fresh prefix predicate at the pin's own call site still bypasses
    it. Do not write a second sentence qualifying the first. Do not write a completeness claim about how
    many call sites exist — that is the CR-27 shape and it is the defect this round is closing elsewhere.

    THEN WATCH IT FAIL, IN THE ORDER PASS 11 RAN IT. Step 1: swap the shipped resolution to a prefix
    matcher over the registry identifier's prefix. Run the gate suite. The fixture MUST be RED. Paste the
    failing title and the FULL assertion message. Step 2: with the swap still in place, plant the
    registry-extending declaration 104 lines above the real opener and re-run; instrument
    `SURFACE_LINES.length` so the amputation is counted FROM INSIDE THE SUITE and paste the figure with
    and without the plant, reconciled against pass 11's `9285 -> 9181` and the exclusion-three endpoint's
    `4161 -> 4057`. Then remove the instrumentation, remove the plant, restore the resolution, and prove
    the tree clean by diff before the verify block runs. IF EITHER STEP DOES NOT GO RED, STOP: write the
    diagnosis up as a first-order finding and change nothing to force a red.

    RE-RUN BOTH UNIQUENESS ASSERTIONS AFTER THE HOIST and show both still at one, because the hoist moves
    lines and a moved line is a changed surface. RE-RUN THE WIDEST-SHADOW PIN and paste its maximum: the
    hoist moves lines around `:9950` and `:10353`, both far below the `:417` anchor's raw span, so the
    maximum is expected UNMOVED. If it moved, attribute the movement line by line with the diff shown and
    record it as a finding — DO NOT move the pin to fit it. That pin belongs to plan 01-42 and plan 01-45
    owns the one authorised exception to it.

    IF NEW BYTES TRIP THE WHOLE-FILE DECLARED-PHRASING GUARD, REWRITE THE BYTES. Never widen the guard.

    RE-READ EVERY SENTENCE THIS TASK WRITES OR CHANGES AGAINST THE NO-OVERCLAIM PROHIBITION AND RECORD A
    VERDICT PER SENTENCE, reach claimed beside reach executed. The disclosure sentence is this task's
    likeliest place to author the twelfth instance, because it is a sentence about what a guard catches.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY. COMMIT SEPARATELY FROM TASK 2 — Task 2's gate reads
    its own commit and a mixed commit voids it.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && SP=packages/backend/src/outbound-prohibition.spec.ts; TD="${TMPDIR:-/tmp}"; B="$TD/defminer-round11-baseline"; [ -f "$B" ] || { echo "MISSING ROUND-11 BASELINE FILE"; exit 1; }; . "$B"; RB=$(git rev-parse 882ff17) || { echo "ROUND BASE UNRESOLVABLE"; exit 1; }; [ -n "$RB" ] || { echo "BASE REV EMPTY — AN EMPTY REV MAKES git show READ THE INDEX, WHICH EQUALS THE WORKTREE, SO EVERY BODY DIFF BELOW WOULD BE 0"; exit 1; }; git merge-base --is-ancestor "$RB" HEAD || { echo "882ff17 IS NOT AN ANCESTOR OF HEAD"; exit 1; }; pnpm exec vitest run "$SP" > "$TD/r11.gate.log" 2>&1; GRC=$?; cat "$TD/r11.gate.log"; [ "$GRC" -eq 0 ] || { echo "GATE SUITE RED (rc=$GRC) — SEE THE LOG ABOVE. A COUNT PARSED OUT OF A RED RUN IS THE DEFECT: VITEST'S PARENTHESISED TOTAL RISES WHEN A NEW CASE FAILS"; exit 1; }; GT=$(grep -E '^[[:space:]]*Tests[[:space:]]' "$TD/r11.gate.log" | tail -1 | sed -n 's/.*[^0-9]\([0-9][0-9]*\) passed.*/\1/p'); [ -n "$GT" ] || { echo "GATE PASSED-COUNT UNPARSEABLE — THE SUMMARY LINE CARRIED NO passed FIGURE"; exit 1; }; [ "$GT" -ge "$BASE_GATE_ARRIVAL" ] || { echo "GATE COUNT $GT BELOW ARRIVAL $BASE_GATE_ARRIVAL — A CASE WAS LOST"; exit 1; }; echo "GATE-PASSED=$GT ARRIVAL=$BASE_GATE_ARRIVAL"; pnpm test > "$TD/r11.full.log" 2>&1; FRC=$?; cat "$TD/r11.full.log"; [ "$FRC" -eq 0 ] || { echo "WHOLE SUITE RED (rc=$FRC)"; exit 1; }; FT=$(grep -E '^[[:space:]]*Tests[[:space:]]' "$TD/r11.full.log" | tail -1 | sed -n 's/.*[^0-9]\([0-9][0-9]*\) passed.*/\1/p'); [ -n "$FT" ] || { echo "SUITE PASSED-COUNT UNPARSEABLE"; exit 1; }; FF=$(grep -E '^[[:space:]]*Test Files[[:space:]]' "$TD/r11.full.log" | tail -1 | sed -n 's/.*[^0-9]\([0-9][0-9]*\) passed.*/\1/p'); [ -n "$FF" ] || { echo "FILE PASSED-COUNT UNPARSEABLE"; exit 1; }; [ "$FT" -ge "$BASE_TESTS_ARRIVAL" ] || { echo "SUITE COUNT $FT BELOW ARRIVAL $BASE_TESTS_ARRIVAL"; exit 1; }; [ "$FF" -eq "$BASE_FILES" ] || { echo "FILE COUNT $FF != $BASE_FILES"; exit 1; }; pnpm exec tsc --build || { echo "tsc --build RED — THE ASSERTIONS BELOW ARE NOT RUN"; exit 1; }; pnpm typecheck || { echo "typecheck RED"; exit 1; }; pnpm lint || { echo "lint RED"; exit 1; }; pnpm knip || { echo "knip RED"; exit 1; }; pnpm build:backend || { echo "build:backend RED"; exit 1; }; pnpm check:bundle || { echo "check:bundle RED"; exit 1; }; M=$(find packages/*/src -name '*.ts' ! -name '*.spec.ts' | grep -c . || true); [ "$M" -eq "$BASE_MODULES" ] || { echo "MODULE COUNT $M != $BASE_MODULES"; exit 1; }; D=$(git diff "$RB"..HEAD -- "$SP"); DN=$(printf '%s\n' "$D" | grep -cE '^([+][^+]|-[^-])' || true); [ "$DN" -ge 8 ] || { echo "ROUND DIFF OVER THE SPEC IS $DN CHANGED LINES, EXPECTED AT LEAST 8 — EVERY DIFF-SCOPED ASSERTION BELOW WOULD PASS HAVING READ NOTHING"; exit 1; }; AST=$(printf '%s\n' "$D" | grep -cE '^[+-].*(from "typescript"|[^A-Za-z]ts\.[A-Za-z])' || true); [ "$AST" -eq 0 ] || { echo "COMPILER-API LINES IN ROUND DIFF: $AST"; exit 1; }; HAND=$(printf '%s\n' "$D" | grep '^+' | grep -vE '^\+[[:space:]]*(//|\*|/\*)' | grep -cE '(braceDepth|braceLevel|indentOf|indentLevel|enclosing[A-Z]|Enclosing|containmentOf|frameOf|depth\+\+|depth--)' || true); [ "$HAND" -eq 0 ] || { echo "HAND-ROLLED CONTAINMENT SPELLING IN ROUND DIFF: $HAND"; exit 1; }; NAR=$(printf '%s\n' "$D" | grep -cE '^[+-].*(DECLARATION =|FIXTURE_TITLE =|DOCBLOCK_OPEN =|isLineComment =|isRule =|CONSTRUCT_TOKEN_ELLIPSIS =|EXEMPTION_ANCHOR_SEP =|NO_PRECEDING_CONSTRUCT =|CLOSES_FROZEN_ARRAY =|lineNumber - 1|k < lineNumber)' || true); [ "$NAR" -eq 0 ] || { echo "NARROWING TOKENS IN ROUND DIFF: $NAR"; exit 1; }; XA='BEGIN{s=0} !s && $0 ~ "^const " n "[ =(]" {s=1; print; next} s && /^(\};|\/\*\*|const |export |type |interface |describe|it\()/ { if ($0 ~ /^\};/) print; exit } s {print}'; for PAIR in constructAnchorFor:$BODY_constructAnchorFor constructTokenOf:$BODY_constructTokenOf exemptionKeyFor:$BODY_exemptionKeyFor maskQuantifiers:$BODY_maskQuantifiers normalizeGateLine:$BODY_normalizeGateLine nameableRemainder:$BODY_nameableRemainder; do n=${PAIR%%:*}; EXP=${PAIR##*:}; [ -n "$EXP" ] || { echo "NO RECORDED BODY LENGTH FOR $n — THE BASELINE FILE IS INCOMPLETE AND THIS CHECK WOULD COMPARE AGAINST AN EMPTY STRING"; exit 1; }; git show "$RB":"$SP" | awk -v n="$n" "$XA" > "$TD/r11.base.$n"; awk -v n="$n" "$XA" "$SP" > "$TD/r11.work.$n"; BBL=$(grep -c . "$TD/r11.base.$n" || true); [ "$BBL" -eq "$EXP" ] || { echo "BASE RANGE FOR $n IS $BBL LINES AND THE BASELINE RECORDS $EXP — THE EXTRACTOR ITSELF COLLAPSED, AND IT COLLAPSES BOTH SIDES EQUALLY SO THE DIFF WOULD STAY 0"; exit 1; }; BL=$(grep -c . "$TD/r11.work.$n" || true); [ "$BL" -eq "$EXP" ] || { echo "BODY RANGE FOR $n IS $BL LINES, EXPECTED $EXP"; exit 1; }; BD=$(diff "$TD/r11.base.$n" "$TD/r11.work.$n" | grep -c . || true); [ "$BD" -eq 0 ] || { echo "BODY OF $n CHANGED: $BD DIFF LINES"; exit 1; }; echo "BODY-BYTE-IDENTICAL $n lines=$BL"; done; AS='BEGIN{s=0} !s && /^export function auditSource\(/ {s=1; print; next} s && /^\}$/ {print; exit} s {print}'; git show "$RB":"$SP" | awk "$AS" > "$TD/r11.base.auditSource"; awk "$AS" "$SP" > "$TD/r11.work.auditSource"; ABL=$(grep -c . "$TD/r11.base.auditSource" || true); [ "$ABL" -eq "$BODY_auditSource" ] || { echo "auditSource BASE RANGE IS $ABL, BASELINE RECORDS $BODY_auditSource — EXTRACTOR COLLAPSED ON BOTH SIDES"; exit 1; }; AL=$(grep -c . "$TD/r11.work.auditSource" || true); [ "$AL" -eq "$BODY_auditSource" ] || { echo "auditSource RANGE IS $AL, EXPECTED $BODY_auditSource"; exit 1; }; AD=$(diff "$TD/r11.base.auditSource" "$TD/r11.work.auditSource" | grep -c . || true); [ "$AD" -eq 0 ] || { echo "auditSource CHANGED: $AD DIFF LINES"; exit 1; }; IMPN=$(grep -c '^import ' "$SP" || true); [ "$IMPN" -eq "$BODY_imports" ] || { echo "IMPORT COUNT IS $IMPN, EXPECTED $BODY_imports — AN ADDED OR REMOVED IMPORT IS ITSELF THE FINDING"; exit 1; }; git show "$RB":"$SP" | grep '^import ' > "$TD/r11.base.imports"; grep '^import ' "$SP" > "$TD/r11.work.imports"; IMPD=$(diff "$TD/r11.base.imports" "$TD/r11.work.imports" | grep -c . || true); [ "$IMPD" -eq 0 ] || { echo "IMPORT SET CHANGED: $IMPD DIFF LINES"; exit 1; }; RO=$(grep -c 'REAL_OPENER' "$SP" || true); [ "$RO" -eq 0 ] || { echo "THE FIXTURE STILL DECLARES ITS OWN OPENER: $RO OCCURRENCE(S) OF REAL_OPENER — CR-28 IS NOT CLOSED"; exit 1; }; FX="$TD/r11.fixture.body"; awk '/it\("a PREFIX locator and a FULL-LINE locator disagree/,/^  \}\);$/' "$SP" > "$FX"; FXL=$(grep -c . "$FX" || true); [ "$FXL" -ge 20 ] || { echo "THE FIXTURE CASE EXTRACTED TO $FXL LINES — THE EXTRACTOR COLLAPSED AND THE TWO CHECKS BELOW WOULD READ NOTHING"; exit 1; }; FXR=$(grep -c 'REGISTRY_OPEN' "$FX" || true); [ "$FXR" -ge 1 ] || { echo "THE FIXTURE CASE DOES NOT READ THE HOISTED BINDING: REGISTRY_OPEN APPEARS $FXR TIME(S) INSIDE IT (ARRIVAL 0) — IT STILL PINS NOTHING"; exit 1; }; FXO=$(grep -c 'REAL_OPENER' "$FX" || true); [ "$FXO" -eq 0 ] || { echo "THE FIXTURE STILL DECLARES ITS OWN OPENER INSIDE ITS OWN BODY: $FXO (ARRIVAL 4)"; exit 1; }; echo "FIXTURE-READS-SHIPPED lines=$FXL registry_open=$FXR real_opener=$FXO"; FLE=$(grep -cE 'l === REGISTRY_OPEN|=== REGISTRY_OPEN' "$SP" || true); [ "$FLE" -ge 1 ] || { echo "NO FULL-LINE EQUALITY AGAINST REGISTRY_OPEN SURVIVES — THE HOIST TURNED THE PIN INTO A PREFIX MATCHER"; exit 1; }; EXO=$(printf '%s\n' "$D" | grep -c '^-.*startsWith("export const \(UNBOUNDED_QUANTIFIERS\|RESOLVER_REGISTRY\)"' || true); [ "$EXO" -eq 0 ] || { echo "THE ROUND DIFF REMOVES $EXO OF EXCLUSIONS OWN PREFIX LOCATORS — MAKING BOTH SIDES THE SAME EXPRESSION RESTORES CR-23'S TAUTOLOGY"; exit 1; }; DEC=$(grep -c 'RESOLVER_REGISTRY_SHADOW' "$SP" || true); [ "$DEC" -eq 0 ] || { echo "PASS 11'S PLANTED DECOY IS STILL IN THE COMMITTED FILE: $DEC"; exit 1; }; BOX=$(git diff "$RB"..HEAD -- .planning/REQUIREMENTS.md | grep -c . || true); [ "$BOX" -eq 0 ] || { echo "REQUIREMENTS.md CHANGED THIS ROUND: $BOX"; exit 1; }; CBX=$(grep -c 'CORE11_BOX_EXPECTED = "- \[ \] \*\*CORE-11\*\*"' "$SP" || true); [ "$CBX" -eq 1 ] || { echo "CORE11_BOX_EXPECTED IS NOT THE UNCHECKED FORM: $CBX"; exit 1; }; A=$(git diff --name-only "$RB"..HEAD -- packages/ scripts/ | grep -v '\.spec\.ts$' | grep -c . || true); [ "$A" -eq 0 ] || { echo "ROUND TOUCHED SHIPPED SOURCE: $A FILE(S)"; exit 1; }; N=$(git status --porcelain .planning/ | grep -c . || true); [ "$N" -eq 0 ] || { echo "PLANNING DIRTY: $N"; exit 1; }; P=$(grep -c '38 == 27 + 11' .planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md || true); [ "$P" -ge 1 ] || { echo "PROBE EQUALITY MISSING"; exit 1; }; git diff --exit-code -- "$SP" && echo TASK1-TREE-CLEAN</automated>
  </verify>

  <acceptance_criteria>
    - The precondition asserted and PASTED: `git rev-parse HEAD`, `882ff17` proved an ancestor, a clean
      status over `packages/ scripts/ .planning/`, and every baseline variable listed with its measured
      value. Each of the eight body/import lengths reconciled against round 10's 47 / 7 / 12 / 9 / 5 / 11
      / 1179 / 4, with every disagreement written up as a finding and its cause named — NOT adopted.
    - Pass 11's six arrival figures RE-MEASURED and pasted with their commands: gate suite 441, whole
      suite 31 files / 1381 tests, `tsc --build` exit 0, bundle at exactly one specifier `crypto`, 23
      modules at zero violations, `wc -l` 11502. Any difference attributed before the first edit.
    - The pre-edit reconciliation set pasted in full: all three exclusions' `from`/`to`/size,
      `SURFACE_LINES.length`, both full-line locators' resolved lines, both `EXCLUSIONS` prefix locators'
      resolved lines, and the widest-shadow pin's current maximum — reconciled against pass 11's
      `[[983,1564],[5985,5995],[4161,5793]]` and `9285`, with every disagreement recorded as a finding.
    - The hoisted binding shown by diff to live at the `describe` scope opening at `:9588`, to be a
      FULL-LINE equality and not a prefix matcher, and to be consumed by exactly three call sites — the
      uniqueness hit count, the pin's resolution, and the fixture's full-line answer — each shown by diff.
      The numeric checks from the verify block for `REAL_OPENER` at zero and `REGISTRY_OPEN` at three or
      more pasted with their output.
    - Both uniqueness assertions re-run AFTER the hoist and both shown still at exactly one, with the
      output pasted.
    - `LIST_OPEN`, its hit count and its resolution shown UNCHANGED by diff; `EXCLUSIONS`' own opening
      locators shown unremoved by the numeric check, with the in-file sentence explaining why they stay
      independent quoted verbatim.
    - STEP 1 WATCHED RED: the shipped resolution swapped to a prefix matcher, the gate suite observed RED,
      the failing case title and the FULL assertion message pasted, and pass 11's `Tests 441 passed (441)`
      for the same swap cited from `01-VERIFICATION.md`. `git log --oneline -1` pasted BEFORE the swap.
    - STEP 2 WATCHED: the registry-extending declaration planted 104 lines above the real opener on top of
      step 1; `SURFACE_LINES.length` printed FROM INSIDE THE SUITE with and without the plant and both
      pasted; the delta and the slid exclusion-three endpoint reconciled against pass 11's `9285 -> 9181`
      and `4161 -> 4057` with this session's own figures stated and any difference attributed.
    - Instrumentation removed, plant removed, resolution restored, `git diff --exit-code` shown clean, and
      the numeric check showing no shadow declaration survives in the committed file pasted.
    - If either step did NOT go red, the task stopped, the diagnosis is written up as a first-order
      finding, and no adjustment was made to force a red.
    - The universal about every line of the synthetic array shown REMOVED by diff rather than narrowed,
      with pass 11's confirmation that it is false of element five cited by line. The sentence at
      `:10433-10434` shown UNCHANGED and a one-line statement that it is now true for the first time.
    - The disclosure sentence quoted verbatim and shown to name, inside the claiming clause, both what the
      fixture turns red on and the call-site bypass it does not catch. A statement that no completeness
      claim about the number of call sites was written.
    - The widest-shadow pin re-run and its maximum pasted; shown unmoved, or any movement attributed line
      by line with the diff shown and recorded as a finding with an explicit statement that the pin was
      NOT moved to fit it.
    - The compiler-API check, the hand-rolled-containment net and the narrowing-token net all shown
      returning zero over the ROUND-BASE DIFF with their output pasted — AND a written per-sentence
      statement that NO CONTAINMENT WAS COMPUTED BY ANY MEANS, AST OR HAND-ROLLED. The identifier nets
      match SPELLINGS, not shapes; this written statement is what covers the residue and the executor
      states that it is doing so.
    - All eight body-range byte-identity pairs pasted with their non-vacuity line counts, each at 0 diff
      lines AND at an exact, separately re-derived length on BOTH sides. A range shorter than its recorded
      length is a finding ABOUT THE CHECK and is written up before the task is called done, because a
      collapsed range passes vacuously.
    - `git status --porcelain .planning/` asserted EMPTY numerically; `requirements mark-complete` absent
      from every command run; `CORE11_BOX_EXPECTED` byte-unchanged and still the unchecked form;
      `.planning/REQUIREMENTS.md` at zero diff lines against the round base.
    - Executed and pasted: `pnpm test` exit 0 with counts at or above arrival, `typecheck`, `lint`, `knip`
      each exit 0 AS SEPARATE STATEMENTS, `build:backend` then `check:bundle` at exactly one specifier
      `crypto`, and the real-tree walk at 23 modules / zero violations.
    - A one-line no-overclaim verdict recorded for EVERY sentence added or changed, reach claimed beside
      reach executed.
    - Task 1 committed SEPARATELY from Task 2, with `git log --oneline -1` pasted, because Task 2's
      removals-only gate reads its own commit and a mixed commit voids it.
  </acceptance_criteria>

  <done>
    The registry's full-line resolution exists exactly once, at the enclosing `describe` scope, and is
    read by the uniqueness count, by the pin and by the fixture alike; pass 11's step-1 swap has been
    watched turning the fixture RED and step 2's surface amputation counted from inside the suite; the
    false universal about the synthetic array is gone; and the bytes state, with the reach inside the
    claiming clause, the call-site bypass the fixture does not catch.
  </done>
</task>

<task type="auto">
  <name>Task 2: CR-24, CR-25, CR-27 and WR-61 closed by REMOVING BYTES in one commit that introduces no word it did not remove, with the removals-only gate itself watched RED against a single planted word</name>

  <files>packages/backend/src/outbound-prohibition.spec.ts</files>

  <reversibility rating="costly">
    Recoverable from git, but reversing it means RE-AUTHORING the four sentences — which is precisely the
    act pass 11 identifies as having generated 1, then 2, then 5 blockers across rounds 8, 9 and 10. The
    cost is not in restoring the bytes; it is that restoring them restarts the loop.
  </reversibility>

  <precondition>
    Task 1 is committed and `git log --oneline -1` shows its commit. `git diff --exit-code -- packages/`
    is clean. The baseline file exists and all four suite/module variables plus all eight body lengths
    read back. THIS TASK PRODUCES EXACTLY ONE COMMIT OVER EXACTLY ONE FILE, and its subject MUST begin
    `test(01-44): DELETION-ONLY —` so the gate can resolve it; if any other change is staged with it the
    gate is void and the task has failed regardless of what the greps say.
  </precondition>

  <read_first>
    - `.planning/phases/01-skeleton-persistence-compatibility/01-VERIFICATION.md` — the `missing:` entry
      beginning `CR-24, CR-25, CR-27 — CLOSE THESE THREE BY DELETION, NOT BY REPLACEMENT`, the entry
      beginning `WR-63:` for WR-61's two closes, and the body sections `### CR-24`, `### CR-25`,
      `### CR-27` (~`:248-307`) and `## Is exit 2 converging?` (~`:400-499`) IN FULL. The convergence
      section is this task's rationale and it is not optional reading.
    - `packages/backend/src/outbound-prohibition.spec.ts`, CR-24 — `:10770-10780`, where the sentence to
      remove opens at `:10777` with a dated present-tense locator and closes at `:10780`; and
      `:11216-11227`, where `:11225` through the first half of `:11226` carries the RECORD POINTER THAT
      MUST SURVIVE and the clause from `today,` to the end of `:11227` carries the live re-check that must
      go. RE-DERIVE both by their surrounding text.
    - `packages/backend/src/outbound-prohibition.spec.ts`, CR-25 — reach statement (2) at `:10173-10175`,
      and limit (5) at `:9107-9111`, whose closing clause states the same fact correctly and MUST SURVIVE.
    - `packages/backend/src/outbound-prohibition.spec.ts`, CR-27 — the retirement bracket at
      `:10625-10645`, whose numeric completeness clause runs from the sentence beginning `NOTHING WAS
      DELETED SILENTLY` at `:10634` to the end of `:10645`; the three contraposition messages at `:10985`,
      `:10999` and `:11136`; and the docblock at `:9396-9398` that disowns the word BY NAME and is the
      authority for removing it. NOTE: `:11136` SPELLS THE WORD IN CAPITALS and a case-sensitive grep
      misses it — every sweep in this task is case-INSENSITIVE.
    - `packages/backend/src/outbound-prohibition.spec.ts`, WR-61 — `:9095`, `:10196`, `:9270-9271` and
      `:10680-10681`. At `882ff17` the four stale figures are `8,846` twice and `11,417`/`11,416` twice.
      Each sweep's RESULT — the resolving set, the widest shadow's identity — survives; only the SCOPE
      TOTAL goes.
    - `.planning/phases/01-skeleton-persistence-compatibility/01-39-SUMMARY.md`, `:531` and its
      discrepancy row `D-1` at `:931`. Pass 11 read both and confirms they record the 57/58 pair and its
      cause. THIS TASK MUST NOT EDIT THEM; it must confirm they exist, because after this task they are
      the only thing the pair stands on.
  </read_first>

  <action>
    RE-DERIVE EVERY LINE RANGE IN `<read_first>` BY ITS SURROUNDING TEXT BEFORE THE FIRST EDIT — Task 1
    edited this file and every number above is a reconciliation anchor measured at `882ff17`, not an
    address. Record every disagreement as a finding.

    MEASURE THE CASE-INSENSITIVE POPULATION OF THE CONTRAPOSITION WORD BEFORE ANY EDIT and paste the line
    numbers. At `882ff17` it is TEN: `:9242 :9382 :9397 :9411 :9465 :10756 :10985 :10999 :11128 :11136`.
    A case-SENSITIVE grep returns eight and misses `:11136`; pass 11 named five of the ten. THIS
    DISCREPANCY IS THE FINDING THAT PROVES WHY THE SWEEP IS RE-DERIVED AND NOT CITED, and it belongs in
    the SUMMARY.

    THEN DELETE, AND DELETE ONLY. Four items, one commit, no replacement sentence anywhere.

    CR-24, BOTH SITES. At the first site remove the whole dated present-tense sentence that locates two
    table headers by line number and characterises the measurement — it begins at `:10777` and ends at
    `:10780` and comes out as whole lines. At the second site remove only the clause that re-checks those
    headers live, keeping the preceding sentence that records where the pair is written down; the record
    pointer to `01-39-SUMMARY.md` and its discrepancy row MUST survive, because after this edit it is the
    only support the 57/58 pair has. Do not write a replacement locator, do not write a corrected pair of
    line numbers, and do not write a sentence explaining why the old one went — pass 11 measured the
    headers non-identical and swept all 25 ruled tables for ZERO non-trivial duplicates, so there is no
    true version of this sentence to write.

    CR-25. From reach statement (2) remove the entire quantitative clause — both the wave-42 measurement
    of the second-widest shadow and the silent-growth ceiling — leaving only the qualitative heading that
    the case does not bound a non-maximal shadow. Limit (5) already states the quantitative fact
    correctly and it stays untouched, so nothing is lost. Do not renumber the reach list.

    CR-27, FOUR EDITS AND NO NEW SENTENCE. From each of the three contraposition messages remove the
    clause that asserts the anchor resolves a containing construct, leaving the factual half of the
    message — what was observed — intact. The docblock at `:9396-9398` already disowns the word by name
    and remains the authority; the seven other occurrences of the word are NOT touched, because they
    describe what the backward scan DOES when it falls through and are not claims about a resolved
    anchor. Then, from the retirement bracket, remove the numeric completeness clause in full — the
    counts, the enumeration of the five, and the universal that closes it. THE ENUMERATION OF WHAT
    SURVIVES GOES IN THE SUMMARY, DATED, NOT INTO THIS FILE. A sentence in the file is an assertable
    claim held to this phase's bar; a dated sentence in a SUMMARY is a record. Do NOT restate the count
    at a larger number: pass 11's own count was itself incomplete and a third count would be instance
    twelve.

    WR-61, FOUR SITES, THE DELETION-SHAPED CLOSE. Remove the four scope totals — the two surface-line
    figures and the `gateLines`/`wc -l` pair — and leave every sweep's RESULT exactly where it is. Do not
    re-derive them and write them back: a total that moves with every edit is a standing invitation to
    this defect, which is why it has now been stale in the commit that wrote it twice. The
    exhaustiveness these sentences need is already carried by the cases' own exhaustive-by-construction
    paragraphs, which are not touched.

    NOW WATCH THE GATE ITSELF FAIL, BEFORE TRUSTING IT. Stage the deletion commit. Then plant ONE word
    that appears nowhere in the removed lines onto one of the commit's added lines, amend, and run the
    added-word-token check: it MUST report a non-zero difference and name the planted word. Paste that
    output. Then remove the planted word, amend back, and re-run: it MUST report zero. A removals-only
    gate that has never seen an addition is the fourth consecutive round's defect and this round does not
    repeat it.

    ASSERT WHAT SURVIVED, NOT ONLY WHAT WENT. For each of the four items paste both a numeric grep at
    ZERO for the removed text and a numeric grep at ONE OR MORE for the thing that must remain: the
    record pointer for CR-24, limit (5)'s correct statement for CR-25, the docblock's disowning sentence
    for CR-27, and each sweep's result for WR-61. A deletion that took the survivor with it is a worse
    outcome than the defect.

    RE-RUN THE CASE-INSENSITIVE SWEEP AT THE CLOSE and paste the population again. It must have fallen by
    exactly three. If it fell by more or fewer, stop and write it up — do not adjust the file to make the
    number come out.

    RE-RUN THE WIDEST-SHADOW PIN. This task removes lines, some of them below `:1574` and some above, and
    a removal inside the `:417` anchor's raw span would move the maximum. Paste the maximum. If it moved,
    attribute the movement line by line with the diff shown and record it as a finding — DO NOT move the
    pin. Plan 01-45 owns the one authorised re-derivation of that pin.

    STATE THE SECURITY POSITION ONCE, IN THE SUMMARY, WITHOUT OVERCLAIMING. `01-SECURITY.md` carries six
    open blocking threats and all six are these findings: the false locator, the maximum-not-identity
    pin, the unpinned named shadow, the unwatched pin, the contraposition claims and the one bound
    published at two values. This task closes the disclosure half of four of them; plan 01-45 closes the
    mechanism half. Say which, and do not say `threats_open: 0` — that is pass 12's measurement to make.

    RE-READ EVERY EDIT AGAINST THE DELETION-ONLY PROHIBITION. If any edit required a word that was not
    already on a removed line, the edit is wrong and the cut boundary must move, not the discipline.

    RUN THE FULL GATE SET BY HAND AT THIS BOUNDARY.
  </action>

  <verify>
    <automated>set -o pipefail; cd /Users/six2dez/Tools/DefMiner && SP=packages/backend/src/outbound-prohibition.spec.ts; TD="${TMPDIR:-/tmp}"; B="$TD/defminer-round11-baseline"; [ -f "$B" ] || { echo "MISSING ROUND-11 BASELINE FILE"; exit 1; }; . "$B"; RB=$(git rev-parse 882ff17) || { echo "ROUND BASE UNRESOLVABLE"; exit 1; }; [ -n "$RB" ] || { echo "BASE REV EMPTY"; exit 1; }; DCN=$(git log --format=%H "$RB"..HEAD --grep='^test(01-44): DELETION-ONLY' | grep -c . || true); [ "$DCN" -eq 1 ] || { echo "EXPECTED EXACTLY ONE DELETION-ONLY COMMIT SINCE THE ROUND BASE, FOUND $DCN — THE GATE BELOW READS ONE COMMIT AND CANNOT SCOPE ITSELF TO SEVERAL"; exit 1; }; DC=$(git log --format=%H "$RB"..HEAD --grep='^test(01-44): DELETION-ONLY' | head -1); [ -n "$DC" ] || { echo "DELETION COMMIT UNRESOLVABLE"; exit 1; }; DCF=$(git show --name-only --format= "$DC" | grep -c . || true); [ "$DCF" -eq 1 ] || { echo "THE DELETION COMMIT TOUCHES $DCF FILES, EXPECTED 1"; exit 1; }; git show --format= --unified=0 "$DC" -- "$SP" > "$TD/r11.del.diff"; DEL=$(grep -c '^-[^-]' "$TD/r11.del.diff" || true); ADD=$(grep -c '^+[^+]' "$TD/r11.del.diff" || true); [ "$DEL" -ge 12 ] || { echo "THE DELETION COMMIT REMOVES ONLY $DEL LINES, EXPECTED AT LEAST 12 ACROSS FOUR ITEMS — EVERY ASSERTION BELOW WOULD PASS HAVING READ ALMOST NOTHING"; exit 1; }; [ "$ADD" -lt "$DEL" ] || { echo "THE DELETION COMMIT ADDS $ADD LINES AND REMOVES $DEL — A DELETION ROUND'S COMMIT MUST REMOVE MORE THAN IT ADDS"; exit 1; }; grep '^+[^+]' "$TD/r11.del.diff" | grep -oE '[A-Za-z0-9_]+' | sort -u > "$TD/r11.del.added.tok"; grep '^-[^-]' "$TD/r11.del.diff" | grep -oE '[A-Za-z0-9_]+' | sort -u > "$TD/r11.del.removed.tok"; NEWTOK=$(comm -23 "$TD/r11.del.added.tok" "$TD/r11.del.removed.tok" | grep -c . || true); [ "$NEWTOK" -eq 0 ] || { echo "THE DELETION COMMIT INTRODUCED $NEWTOK WORD(S) ABSENT FROM WHAT IT REMOVED — THIS IS A REPLACEMENT, NOT A DELETION:"; comm -23 "$TD/r11.del.added.tok" "$TD/r11.del.removed.tok"; exit 1; }; RMB=$(grep '^-[^-]' "$TD/r11.del.diff" | wc -c | tr -d ' '); ADB=$(grep '^+[^+]' "$TD/r11.del.diff" | wc -c | tr -d ' '); [ "$ADB" -lt "$RMB" ] || { echo "THE DELETION COMMIT ADDED $ADB BYTES AND REMOVED $RMB — NET BYTES MUST BE STRICTLY NEGATIVE"; exit 1; }; echo "DELETION-ONLY-OK removed=$DEL added=$ADD newtokens=$NEWTOK bytes=-$((RMB-ADB))"; C24A=$(grep -c 'Located by text TODAY' "$SP" || true); [ "$C24A" -eq 0 ] || { echo "CR-24 SITE ONE STILL PRESENT: $C24A"; exit 1; }; C24B=$(grep -c 'two identically-headed table headers sit at lines' "$SP" || true); [ "$C24B" -eq 0 ] || { echo "CR-24 SITE TWO STILL PRESENT: $C24B"; exit 1; }; C24S=$(grep -c '01-39-SUMMARY.md' "$SP" || true); [ "$C24S" -ge 1 ] || { echo "CR-24 TOOK THE RECORD POINTER WITH IT: 01-39-SUMMARY.md APPEARS $C24S TIMES — THE 57/58 PAIR NOW STANDS ON NOTHING"; exit 1; }; C25=$(grep -cE 'grow to 573' "$SP" || true); [ "$C25" -eq 0 ] || { echo "CR-25 CEILING STILL PRESENT: $C25"; exit 1; }; C25S=$(grep -c 'up to that maximum' "$SP" || true); [ "$C25S" -ge 1 ] || { echo "CR-25 TOOK LIMIT (5)'S CORRECT STATEMENT WITH IT: $C25S"; exit 1; }; C27A=$(grep -c 'has stopped resolving the enclosing construct' "$SP" || true); [ "$C27A" -eq 0 ] || { echo "CR-27 MESSAGE ONE STILL PRESENT: $C27A"; exit 1; }; C27B=$(grep -c 'so the anchor is not resolving the enclosing construct' "$SP" || true); [ "$C27B" -eq 0 ] || { echo "CR-27 MESSAGE TWO STILL PRESENT: $C27B"; exit 1; }; C27C=$(grep -c 'the anchor is not resolving the ENCLOSING construct' "$SP" || true); [ "$C27C" -eq 0 ] || { echo "CR-27 MESSAGE THREE STILL PRESENT: $C27C"; exit 1; }; C27D=$(grep -c 'claims that a well-formed anchor identifies anything' "$SP" || true); [ "$C27D" -eq 0 ] || { echo "THE BRACKET'S COMPLETENESS UNIVERSAL STILL PRESENT: $C27D"; exit 1; }; C27E=$(grep -c 'would claim a containment this scan does not compute' "$SP" || true); [ "$C27E" -ge 1 ] || { echo "CR-27 TOOK THE DOCBLOCK'S DISOWNING SENTENCE WITH IT: $C27E"; exit 1; }; ENC=$(grep -ic 'enclos' "$SP" || true); [ "$ENC" -eq 7 ] || { echo "CASE-INSENSITIVE POPULATION OF THE CONTRAPOSITION WORD IS $ENC, EXPECTED 7 (ARRIVAL 10, MINUS THE THREE PRESCRIBED). A DIFFERENT NUMBER IS A FINDING, NOT A TARGET"; exit 1; }; W61A=$(grep -c '8,846' "$SP" || true); [ "$W61A" -eq 0 ] || { echo "WR-61 STALE SURFACE TOTAL STILL PRESENT: $W61A"; exit 1; }; W61B=$(grep -c '11,417' "$SP" || true); [ "$W61B" -eq 0 ] || { echo "WR-61 STALE gateLines TOTAL STILL PRESENT: $W61B"; exit 1; }; W61C=$(grep -c '11,416' "$SP" || true); [ "$W61C" -eq 0 ] || { echo "WR-61 STALE wc -l TOTAL STILL PRESENT: $W61C"; exit 1; }; W61S=$(grep -c 'blanking line 1' "$SP" || true); [ "$W61S" -ge 2 ] || { echo "WR-61 TOOK A SWEEP RESULT WITH IT: THE WATCHED-DEPENDENCE RESULT APPEARS $W61S TIMES, EXPECTED AT LEAST 2"; exit 1; }; pnpm exec vitest run "$SP" > "$TD/r11.gate2.log" 2>&1; GRC=$?; cat "$TD/r11.gate2.log"; [ "$GRC" -eq 0 ] || { echo "GATE SUITE RED (rc=$GRC)"; exit 1; }; GT=$(grep -E '^[[:space:]]*Tests[[:space:]]' "$TD/r11.gate2.log" | tail -1 | sed -n 's/.*[^0-9]\([0-9][0-9]*\) passed.*/\1/p'); [ -n "$GT" ] || { echo "GATE PASSED-COUNT UNPARSEABLE"; exit 1; }; [ "$GT" -ge "$BASE_GATE_ARRIVAL" ] || { echo "GATE COUNT $GT BELOW ARRIVAL $BASE_GATE_ARRIVAL"; exit 1; }; pnpm test > "$TD/r11.full2.log" 2>&1; FRC=$?; cat "$TD/r11.full2.log"; [ "$FRC" -eq 0 ] || { echo "WHOLE SUITE RED (rc=$FRC)"; exit 1; }; FT=$(grep -E '^[[:space:]]*Tests[[:space:]]' "$TD/r11.full2.log" | tail -1 | sed -n 's/.*[^0-9]\([0-9][0-9]*\) passed.*/\1/p'); [ -n "$FT" ] || { echo "SUITE PASSED-COUNT UNPARSEABLE"; exit 1; }; FF=$(grep -E '^[[:space:]]*Test Files[[:space:]]' "$TD/r11.full2.log" | tail -1 | sed -n 's/.*[^0-9]\([0-9][0-9]*\) passed.*/\1/p'); [ "$FT" -ge "$BASE_TESTS_ARRIVAL" ] || { echo "SUITE COUNT $FT BELOW ARRIVAL"; exit 1; }; [ -n "$FF" ] && [ "$FF" -eq "$BASE_FILES" ] || { echo "FILE COUNT $FF != $BASE_FILES"; exit 1; }; pnpm exec tsc --build || { echo "tsc --build RED"; exit 1; }; pnpm typecheck || { echo "typecheck RED"; exit 1; }; pnpm lint || { echo "lint RED"; exit 1; }; pnpm knip || { echo "knip RED"; exit 1; }; pnpm build:backend || { echo "build:backend RED"; exit 1; }; pnpm check:bundle || { echo "check:bundle RED"; exit 1; }; M=$(find packages/*/src -name '*.ts' ! -name '*.spec.ts' | grep -c . || true); [ "$M" -eq "$BASE_MODULES" ] || { echo "MODULE COUNT $M != $BASE_MODULES"; exit 1; }; D=$(git diff "$RB"..HEAD -- "$SP"); AST=$(printf '%s\n' "$D" | grep -cE '^[+-].*(from "typescript"|[^A-Za-z]ts\.[A-Za-z])' || true); [ "$AST" -eq 0 ] || { echo "COMPILER-API LINES IN ROUND DIFF: $AST"; exit 1; }; NAR=$(printf '%s\n' "$D" | grep -cE '^[+-].*(DECLARATION =|FIXTURE_TITLE =|DOCBLOCK_OPEN =|isLineComment =|isRule =|CONSTRUCT_TOKEN_ELLIPSIS =|EXEMPTION_ANCHOR_SEP =|NO_PRECEDING_CONSTRUCT =|CLOSES_FROZEN_ARRAY =|lineNumber - 1|k < lineNumber)' || true); [ "$NAR" -eq 0 ] || { echo "NARROWING TOKENS IN ROUND DIFF: $NAR"; exit 1; }; A=$(git diff --name-only "$RB"..HEAD -- packages/ scripts/ | grep -v '\.spec\.ts$' | grep -c . || true); [ "$A" -eq 0 ] || { echo "ROUND TOUCHED SHIPPED SOURCE: $A"; exit 1; }; BOX=$(git diff "$RB"..HEAD -- .planning/REQUIREMENTS.md | grep -c . || true); [ "$BOX" -eq 0 ] || { echo "REQUIREMENTS.md CHANGED: $BOX"; exit 1; }; CBX=$(grep -c 'CORE11_BOX_EXPECTED = "- \[ \] \*\*CORE-11\*\*"' "$SP" || true); [ "$CBX" -eq 1 ] || { echo "CORE11_BOX_EXPECTED IS NOT THE UNCHECKED FORM: $CBX"; exit 1; }; N=$(git status --porcelain .planning/ | grep -c . || true); [ "$N" -eq 0 ] || { echo "PLANNING DIRTY: $N"; exit 1; }; P=$(grep -c '38 == 27 + 11' .planning/phases/01-skeleton-persistence-compatibility/01-PROBE.md || true); [ "$P" -ge 1 ] || { echo "PROBE EQUALITY MISSING"; exit 1; }; git diff --exit-code -- "$SP" && echo TASK2-TREE-CLEAN</automated>
  </verify>

  <acceptance_criteria>
    - The precondition asserted and PASTED, including Task 1's commit hash and a clean `packages/` diff.
    - The case-INSENSITIVE population of the contraposition word pasted BEFORE any edit with its line
      numbers, reconciled against this plan's ten at `882ff17`, AND the case-SENSITIVE count pasted
      beside it to show the eight-versus-ten discrepancy. A statement that pass 11 named five of the ten
      and that this is why the sweep was re-derived rather than cited — recorded as a finding.
    - EXACTLY ONE commit since the round base whose subject begins with the deletion prefix, touching
      exactly one file, with the numeric checks from the verify block pasted: removed lines at or above
      twelve, added lines strictly below removed, ADDED WORD TOKENS MINUS REMOVED WORD TOKENS AT ZERO,
      and net bytes strictly negative. The full `DELETION-ONLY-OK` line pasted.
    - THE REMOVALS-ONLY GATE WATCHED RED: one word absent from every removed line planted onto an added
      line, the check observed reporting a non-zero difference AND NAMING THAT WORD, the output pasted,
      the word removed, and the check observed at zero again. If the check did not go red, the task
      stopped and the diagnosis is a first-order finding.
    - CR-24: both numeric greps at ZERO pasted, and the record pointer's grep at ONE OR MORE pasted.
      `01-39-SUMMARY.md:531` and `:931` confirmed present ON DISK and shown UNEDITED by diff.
    - CR-25: the ceiling's grep at ZERO pasted, and limit (5)'s correct statement shown surviving by its
      grep at one or more, quoted verbatim.
    - CR-27: all three message greps at ZERO and the bracket's completeness universal at ZERO, pasted;
      the docblock's disowning sentence shown surviving; the closing case-insensitive population pasted
      and shown to be exactly three lower than arrival. THE DISPOSITION OF EVERY SURVIVOR RECORDED IN THE
      SUMMARY, with an explicit statement that no enumeration was written into the file and why.
    - WR-61: all four stale totals at ZERO by numeric grep, and each of the two sweeps' RESULTS shown
      surviving at its site, quoted verbatim. An explicit statement that no total was re-derived and
      written back, with the reason.
    - The four `<action>` cut boundaries each shown by diff, and for every added line in the commit a
      one-line statement that every word on it came off a removed line — the gate proves the set
      property, this states the executor checked the boundaries by reading.
    - The widest-shadow pin re-run and its maximum pasted; shown unmoved, or any movement attributed line
      by line with the diff and recorded as a finding with an explicit statement that the pin was NOT
      moved to fit it.
    - The compiler-API check and the narrowing-token net shown returning zero over the round-base diff
      with output pasted, and a written statement that this task computed no containment by any means.
    - `git status --porcelain .planning/` asserted EMPTY numerically; `requirements mark-complete` absent
      from every command; `CORE11_BOX_EXPECTED` byte-unchanged; `.planning/REQUIREMENTS.md` at zero diff
      lines against the round base.
    - Executed and pasted: gate suite at or above arrival, `pnpm test` exit 0 at or above 31 files / 1381
      tests, `typecheck` / `lint` / `knip` / `tsc --build` each exit 0 AS SEPARATE STATEMENTS,
      `check:bundle` at exactly one specifier `crypto`, 23 modules at zero violations.
    - The security position stated in the SUMMARY: which of `01-SECURITY.md`'s six open blocking threats
      have their DISCLOSURE half closed here, which await plan 01-45's mechanism half, and an explicit
      refusal to declare `threats_open: 0` because that is pass 12's measurement.
    - `01-PROBE.md`'s equality `38 == 27 + 11` shown present and unchanged; no probe row moved, and if one
      moved it moved in this same commit.
    - A one-line no-overclaim verdict recorded for the task as a whole, stating that a deletion removes a
      false claim and establishes no true one.
  </acceptance_criteria>

  <done>
    Four disclosure defects are absent from the gate file by numeric grep at zero, every survivor they
    were tangled with is present by numeric grep at one or more, the single commit that removed them adds
    no word it did not remove — proved by a set-difference check that was itself watched RED against one
    planted word — and the re-derived case-insensitive population has fallen by exactly three.
  </done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Produced by | What it carries |
| --- | --- | --- |
| `packages/backend/src/outbound-prohibition.spec.ts` | Task 1 | The registry's full-line resolution hoisted to the enclosing `describe` and read by the uniqueness count, the pin and the CR-23 fixture alike, so the fixture's stated reason for existing becomes executable; the false universal about the synthetic array removed; the call-site bypass disclosed with its reach inside the claiming clause |
| `packages/backend/src/outbound-prohibition.spec.ts` | Task 2 | CR-24's two present-tense locators, CR-25's silent-growth ceiling, CR-27's three contraposition clauses and its bracket's completeness universal, and WR-61's four stale scope totals — all ABSENT, removed in one commit proved to introduce no word it did not remove |
| `.planning/phases/01-skeleton-persistence-compatibility/01-44-SUMMARY.md` | Tasks 1-2 | The dated enumeration of the contraposition word's surviving occurrences — recorded where a record belongs rather than written into the file as a live claim — plus every measurement, every watched RED, and every reconciliation disagreement |

## Multi-source coverage audit

| Source | Item | Covered by |
| --- | --- | --- |
| GOAL | ROADMAP phase goal — installs, observes without stalling, durably remembers | Unchanged. No shipped source byte is touched by this plan; the must-NOT is re-measured at every task boundary |
| REQ | CORE-11 | Tasks 1-2. The box stays `[ ]`; `requirements mark-complete` stays unrun |
| VERIFICATION | CR-28 | Task 1 |
| VERIFICATION | CR-24, CR-25, CR-27, WR-61 | Task 2 |
| VERIFICATION | CR-26, WR-62, WR-63, WR-64, WR-65 | Plan 01-45 |
| VERIFICATION | IN-45, IN-46, IN-48, IN-49 | NOT COVERED BY DESIGN — pass 11 records them as reviewer-reported and not independently re-executed, and prohibits presenting them as closed. IN-47's element-five instance is closed by deletion in Task 1 because it sits inside the block already being opened |
| SECURITY | `T-01-289`, `T-01-286`, `T-01-263` | Task 2 (disclosure half) |
| SECURITY | `T-01-280`, `T-01-264`, `T-01-239` | Plan 01-45 (mechanism half) |
| PROBE | `38 == 27 + 11` | Asserted unchanged at both task boundaries |
| RESEARCH | No new item — this round adds no dependency, no API and no external surface | n/a |

No CONTEXT.md, SPEC.md, AI-SPEC.md or UI-SPEC.md exists for this phase and none ever has; their absence
is not a gap. `COVERAGE.md` exists and declares no external API surface for this round's scope — no row
is fabricated. No ORM files exist, so the schema push is skipped. The assumption detector fires with four
`pluralization` signals, all of them historical ROADMAP prose about waves 36, 40 and 42; none introduces
an unstated cardinality assumption inside this round's scope, and the executor states that verdict in the
SUMMARY rather than acting on it.

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| editor -> gate spec | The only boundary this plan crosses. Every change lands in a test-only file; no shipped module, no bundle input and no persisted row is touched |
| gate spec -> shipped artifact | The gate's scan target must stay byte-identical to the installable artifact. An edit that narrows what the gate scans is the one way this plan could weaken the outbound prohibition |

## STRIDE Threat Register — ASVS level 1, blocking on `high`

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-01-289 | Repudiation | the present-tense locator at both CR-24 sites | high | mitigate | Task 2 removes both by deletion; both asserted absent by numeric grep at zero and the record pointer asserted surviving |
| T-01-263 | Repudiation | the three contraposition messages and the bracket's completeness universal | high | mitigate | Task 2 removes all four by deletion; the case-insensitive population re-derived at both ends and required to fall by exactly three |
| T-01-286 | Repudiation | one bound published at two values | medium | mitigate | Task 2 deletes the second statement; limit (5)'s correct statement asserted surviving |
| T-01-291 | Tampering | the hoist silently narrowing what the gate scans | high | mitigate | `SURFACE_LINES.length` and all three exclusion spans measured before and after; both uniqueness assertions re-run and shown at one; the widest-shadow pin re-run and any movement attributed line by line rather than absorbed |
| T-01-292 | Repudiation | a deletion that removes the survivor along with the defect | high | mitigate | Every removal paired with a numeric grep at one or more for the thing that must remain — the record pointer, limit (5), the disowning docblock, each sweep's result |
| T-01-293 | Tampering | the removals-only gate passing with a replacement present | high | mitigate | The gate is watched RED against one planted word before it is trusted, then watched green again after the word is removed |
| T-01-294 | Repudiation | this round authoring instance twelve of the signature defect | high | mitigate | Exactly one sentence is written in this plan (Task 1's disclosure), it carries its reach inside the claiming clause, and a per-sentence no-overclaim verdict is recorded; the `open`-status DISCLOSED-NOT-CLOSED prohibition governs both tasks |
| T-01-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing. No package-manager command appears in any task, so the legitimacy gate has no input; `RESEARCH.md`'s audit table is unchanged and unconsulted |
</threat_model>

<verification>
Executed at every task boundary, never carried:

- `pnpm exec vitest run packages/backend/src/outbound-prohibition.spec.ts` — exit 0, passed count at or
  above 441. The PASSED figure is parsed, never the parenthesised total, and the return code is asserted
  before any parse.
- `pnpm test` — exit 0, 31 files, passed count at or above 1381.
- `pnpm exec tsc --build`, `pnpm typecheck`, `pnpm lint`, `pnpm knip`, `pnpm build:backend`,
  `pnpm check:bundle` — each a SEPARATE statement with its own failure branch, because a short-circuited
  chain leaves the later variables empty and voids every diff-scoped assertion below it.
- `pnpm check:bundle` — exactly one import specifier, `crypto`.
- 23 shipped non-spec modules across both SOURCE_ROOTS walked at zero violations.
- Round-base structural nets over the `882ff17..HEAD` diff: compiler-API at zero, hand-rolled containment
  at zero, recogniser/scan-bound/key-format tokens at zero.
- Eight body-range byte-identity pairs, each asserted at an exact re-derived length on BOTH sides and at
  zero diff lines.
- `git status --porcelain .planning/` at zero entries; `.planning/REQUIREMENTS.md` at zero diff lines;
  `CORE11_BOX_EXPECTED` byte-unchanged in the unchecked form; `requirements mark-complete` never run.
- `01-PROBE.md`'s `38 == 27 + 11` present and unchanged.
</verification>

<success_criteria>
- The full-line resolution of the registry's opening line exists exactly once and is read by three call
  sites; `REAL_OPENER` is absent by numeric grep at zero.
- Pass 11's step-1 swap has been observed turning the fixture RED, with the full message pasted, and its
  prior `Tests 441 passed (441)` cited.
- Step 2's surface amputation has been counted from inside the suite and reconciled against `9285 -> 9181`.
- Exactly one deletion commit exists since the round base; it touches one file, removes at least twelve
  lines, adds fewer than it removes, introduces zero word tokens absent from its removed lines, and is net
  negative in bytes.
- That removals-only check has been observed RED against one planted word and green again after removal.
- All eleven numeric greps for removed text return zero and all six greps for surviving text return one
  or more.
- The case-insensitive population of the contraposition word is exactly three lower than arrival.
- No shipped source byte changed; the must-NOT baseline is re-measured green at both boundaries.
- CORE-11's box is untouched at `[ ]` and `requirements mark-complete` was never run.
</success_criteria>

<output>
Create `.planning/phases/01-skeleton-persistence-compatibility/01-44-SUMMARY.md` when done.

It MUST carry, in addition to the standard sections: the dated enumeration of every surviving occurrence
of the contraposition word with its disposition, which is deliberately NOT written into the gate file;
every reconciliation disagreement against this plan's line numbers and against pass 11's measurements;
the full text of every watched RED; and the security position naming which of `01-SECURITY.md`'s six open
blocking threats have their disclosure half closed here and which await plan 01-45 — without declaring
`threats_open: 0`, which is pass 12's measurement to make.
</output>
