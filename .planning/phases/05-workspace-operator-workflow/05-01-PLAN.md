---
phase: 05-workspace-operator-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/package.json
  - packages/frontend/tsconfig.json
  - packages/frontend/vite.config.ts
  - packages/frontend/postcss.config.cjs
  - packages/frontend/tailwind.config.ts
  - packages/frontend/src/index.ts
  - packages/frontend/src/index.spec.ts
  - packages/frontend/src/App.vue
  - packages/frontend/src/App.spec.ts
  - packages/frontend/src/styles/index.css
  - packages/caido.config.ts
  - package.json
  - tsconfig.json
  - tsconfig.eslint.json
  - eslint.config.js
  - vitest.config.ts
  - knip.json
  - .browserslistrc
  - scripts/ci/prefixwrap.mjs
  - scripts/ci/prefixwrap.spec.ts
  - scripts/ci/frontend-externals.mjs
  - scripts/ci/frontend-externals.spec.ts
autonomous: false
requirements: [UI-01, UISEC-01]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "A sidebar item labelled DefMiner is registered and clicking it opens a single page at path /defminer that renders DefMiner's own markup inside a root element whose id is the prefixwrap root."
    - "The page renders three regions top to bottom — a 48px toolbar, a tab strip, and a split body — and the tab strip renders its fixed tabs immediately without waiting on any query."
    - "The tab strip renders every tab immediately with its fixed labels; a tab whose count is still resolving renders without a count rather than showing zero, because a zero reads as nothing-found-here and stops the operator opening it."
    - "A tab whose query fails still renders and still routes; the failure surfaces in the body region, and a tab is never removed on error."
    - "The tab strip wraps to a second row when it does not fit; it never scrolls horizontally and never collapses into an overflow menu."
    - "Every tab label is a DefMiner-authored entity-class name or one of the fixed Health / Settings labels; no target-controlled string reaches a tab."
    - "The built frontend CSS contains no rule whose selector is not scoped under the prefixwrap root, asserted against the build output rather than against the PostCSS config."
    - "The built frontend bundle contains no copy of Vue, no copy of the Caido frontend SDK and no CodeMirror or Lezer module — every one of them resolves as an external at runtime."
    - "Lint fails, as an error and not a warning, on a Vue template directive that injects raw HTML, and an inline disable comment cannot re-enable it in the frontend file block."
    - "Lint fails on dynamic code construction and on a javascript-scheme URL anywhere the repo lints."
  artifacts:
    - packages/frontend/package.json
    - packages/frontend/vite.config.ts
    - packages/frontend/src/index.ts
    - packages/frontend/src/App.vue
    - packages/frontend/src/styles/index.css
    - scripts/ci/prefixwrap.mjs
    - scripts/ci/frontend-externals.mjs
    - .browserslistrc
  key_links:
    - "packages/caido.config.ts frontend plugin entry -> packages/frontend/ build root — without the entry the workspace exists and never ships"
    - "packages/frontend/src/index.ts mount div id -> postcss.config.cjs prefixwrap selector — a mismatch silently unstyles the whole page"
    - "vite.config.ts external list -> scripts/ci/frontend-externals.mjs assertion set — the gate is the only thing that notices a duplicate Vue"
    - "vitest.config.ts plugins -> .vue single-file component imports in every later frontend spec"
  assumptions:
    - "EDGE UI-01 / adjacency — unresolved. Two navigation entries that resolve to the same path are assumed to collide rather than merge: sdk.navigation.addPage is called exactly once for /defminer and a second registration is not attempted."
    - "EDGE UI-01 / empty — unresolved. A page opened before any project is resolved is assumed to render the shell with an empty body region rather than failing to mount."
    - "EDGE UI-01 / ordering — unresolved. Tab order is assumed to be the declaration order of the fixed tab list and is not sorted at runtime."
    - "EDGE UI-01 / concurrency — unresolved. A second mount of the same page (operator navigates away and back) is assumed to reuse the mounted app rather than create a second Vue instance; onEnter is the refresh hook, not a remount hook."
    - "UI-SPEC ## UI Considerations `partial / settings-form` is `⚠ unresolved` and stays so: the threshold and budget field set is defined by Phases 2-4, so some-present-some-absent cannot be specified without inventing the schema. The settings shell itself is in scope and lands in plan 05-12."
  prohibitions:
    - "No hex colour literal appears in any DefMiner-authored frontend source or stylesheet."
    - "No font-family declaration is authored by DefMiner anywhere except the monospace utility class."
    - "No icon package is added to the dependency set, and no DefMiner-authored action is icon-only."
    - "No spacing value outside the seven-value scale appears in DefMiner-authored markup."
    - "No caret or tilde range is used for primevue, tailwindcss, @caido/primevue, @caido/tailwindcss, postcss-prefixwrap or vue-virtual-scroller."
    - "No `projects` or `workspace` key is added to vitest.config.ts."
    - "The frontend package declares no dependency on @defminer/backend and imports nothing from it."
---

<objective>
Create `packages/frontend/` from zero and prove the whole stack end to end: a Vue app mounted on a
plain element, registered as a Caido page and sidebar item, reading real rows from the already
shipped `getArtifacts` RPC and rendering them as text — then gate the two build properties that
silently break everything downstream (CSS containment and externals), and arm the lint rules that
rendering-safety R1 names.

Purpose: this is the tracer slice. Every later Phase 5 plan builds outward from it, and the
architecture it proves — `addPage` taking an `HTMLElement`, prefixwrap containment, externals,
PrimeVue's pass-through theme — is either right here or wrong everywhere.

Output: a buildable, testable frontend package; two build-output gates; a lint configuration that
enforces R1 rather than warning about it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-workspace-operator-workflow/05-CONTEXT.md
@.planning/phases/05-workspace-operator-workflow/05-UI-SPEC.md
@.planning/phases/05-workspace-operator-workflow/05-RESEARCH.md
@.planning/phases/05-workspace-operator-workflow/05-PATTERNS.md
@.planning/phases/05-workspace-operator-workflow/05-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 0: Package legitimacy — the two 250-download first-party Caido packages</name>
  <read_first>
    - .planning/phases/05-workspace-operator-workflow/05-RESEARCH.md § "Package Legitimacy Audit"
    - eslint.config.js lines 1-11 — the Phase 1 precedent, verbatim
    - pnpm-workspace.yaml — the DIST-06 overrides put there in Phase 1 so a Phase 5 frontend could not resolve a breaking version
  </read_first>
  <decision>
    Whether to install `@caido/primevue@0.3.3` (279 weekly downloads) and `@caido/tailwindcss@0.1.0`
    (258 weekly downloads) on the evidence already gathered, or to verify them on the registry first,
    or to reject them.
  </decision>
  <context>
    `05-RESEARCH.md § Package Legitimacy Audit` ran all seventeen frontend packages through
    `gsd-tools query package-legitimacy check --ecosystem npm` on 2026-08-28. No package is `[SLOP]`.
    Eleven are `[SUS]`. Nine of those eleven are flagged `too-new`, which the seam derives from the
    **latest publish date and not from package age** — `postcss` at 280,270,306 weekly downloads and
    `vue` at 15,444,459 carry the same flag, so that reason cannot discriminate.

    Two are flagged for genuinely low downloads and are the only ones this checkpoint is about:

    | Package | Weekly downloads | Reasons | Publisher |
    |---|---:|---|---|
    | `@caido/primevue@0.3.3` | 279 | `low-downloads`, `no-repository` | Caido, first-party |
    | `@caido/tailwindcss@0.1.0` | 258 | `low-downloads` (repo `github.com/caido/ui-kit`) | Caido, first-party |

    Both are from the same publisher as the already-installed and already-vetted
    `@caido/sdk-backend@0.57.1`, and `DIST-06` exists specifically to pin them —
    `pnpm-workspace.yaml` already carries `primevue: 4.1.0` and `tailwindcss: 3.4.13` overrides.

    The researcher's reading is that no blocking checkpoint is warranted on this evidence. The reason
    this checkpoint exists anyway: `@caido/eslint-config@0.10.0` at **153** weekly downloads went
    through a `gate="blocking-human"` checkpoint in Phase 1, and consistency with that precedent is
    the operator's call, not the planner's. A package-legitimacy checkpoint is never auto-approvable.
  </context>
  <options>
    <option id="option-a">
      <name>Approve both on the evidence already gathered, and install</name>
      <pros>Costs nothing; the evidence is first-party publisher, exact pins, and workspace-level overrides already in place from Phase 1.</pros>
      <cons>Diverges from the Phase 1 precedent, which put a lower-download first-party package through a blocking human check.</cons>
    </option>
    <option id="option-b">
      <name>Verify both on the registry first, then install</name>
      <pros>Matches the Phase 1 precedent exactly; confirms publisher, repository link and published file list with the operator's own eyes; costs two page loads.</pros>
      <cons>Two minutes of operator time on packages the research already assessed.</cons>
    </option>
    <option id="option-c">
      <name>Reject one or both</name>
      <pros>Removes two low-download dependencies from the shipped bundle.</pros>
      <cons>Stops the phase. `@caido/primevue` is the Caido pass-through theme and `@caido/tailwindcss` is what the zero-hex colour rule depends on; there is no substitute that keeps the design contract's colour section.</cons>
    </option>
  </options>
  <resume-signal>Reply with `option-a`, `option-b`, or `option-c`. Recommended: `option-b`.</resume-signal>
  <verify>
    <automated>node -e "const w=require('fs').readFileSync('pnpm-workspace.yaml','utf8');if(!/primevue:\s*4\.1\.0/.test(w)||!/tailwindcss:\s*3\.4\.13/.test(w)){process.exit(1)}" && echo "DIST-06 overrides present"</automated>
  </verify>
  <acceptance_criteria>
    - The operator's chosen option id is recorded verbatim in `05-01-SUMMARY.md` under a heading naming both packages and their download counts.
    - If `option-c` is chosen, the plan halts and the orchestrator is told the colour contract has no substitute.
    - The checkpoint is not auto-approved under any workflow setting.
  </acceptance_criteria>
  <done>An operator decision exists on the record for `@caido/primevue@0.3.3` and `@caido/tailwindcss@0.1.0`.</done>
</task>

<task type="tracer">
  <name>Task 1: End-to-end "the operator opens DefMiner and sees a real artifact row" — one path only</name>
  <precondition>The npm registry is reachable and `pnpm install` can add packages — run `pnpm -v` and confirm it reports an 11.x version before starting; `devEngines` rejects npm outright with `EBADDEVENGINES`.</precondition>
  <files>packages/frontend/package.json, packages/frontend/tsconfig.json, packages/frontend/vite.config.ts, packages/frontend/postcss.config.cjs, packages/frontend/tailwind.config.ts, packages/frontend/src/index.ts, packages/frontend/src/index.spec.ts, packages/frontend/src/App.vue, packages/frontend/src/App.spec.ts, packages/frontend/src/styles/index.css, packages/caido.config.ts, package.json, tsconfig.json, tsconfig.eslint.json</files>
  <read_first>
    - packages/caido.config.ts — the whole file, including the header explaining why it lives at `packages/` and not at `packages/backend/`
    - packages/backend/package.json — the manifest shape a workspace package uses here (`private`, `type: module`, `workspace:*`)
    - packages/engine/package.json — the explicit `exports` map convention; there is no barrel and no wildcard
    - packages/backend/src/index.ts lines 1-32 and 300-320 — the `init()` ordering-contract header and the four existing `sdk.api.register` calls, one of which is `getArtifacts`
    - packages/backend/src/store/artifacts.ts lines 117-170 — `ArtifactRow`, the exact column set `getArtifacts` returns
    - tsconfig.json and tsconfig.base.json — the project-reference layout a third package must join
    - .planning/phases/05-workspace-operator-workflow/05-RESEARCH.md § "Code Examples" #1 and § "Recommended Project Structure"
    - .planning/phases/05-workspace-operator-workflow/05-UI-SPEC.md § "Design System", § "Spacing Scale", § "Typography", § "Color", § "Data & Interaction Contract" (Layout)
  </read_first>
  <action>
    Wire ONE path from the shipped SQLite `artifacts` table to a rendered row on a Caido page. No
    other call site, no second endpoint, no second table.

    Create the workspace `packages/frontend` named `@defminer/frontend`, `private: true`,
    `type: "module"`, with `"@defminer/engine": "workspace:*"` and NO dependency on
    `@defminer/backend`. Install exactly, as exact pins with no caret and no tilde:
    `vue`, `primevue@4.1.0`, `@caido/primevue@0.3.3`, `@caido/tailwindcss@0.1.0`,
    `tailwindcss@3.4.13`, `tailwindcss-primeui@0.6.1`, `postcss-prefixwrap@1.57.2`,
    `vue-virtual-scroller@2.0.0-beta.8`, `pinia@3.0.4`, `@vueuse/core@14.2.0`; and as devDependencies
    `@vitejs/plugin-vue@6.0.1`, `vue-tsc@3.2.4`, `postcss`, `autoprefixer`, `@caido/sdk-frontend`.
    Add no icon package.

    `vite.config.ts`: library build, `@vitejs/plugin-vue`, and `rollupOptions.external` containing
    `vue`, `@caido/frontend-sdk`, and regexp matches for `@codemirror/` and `@lezer/` prefixes.
    `postcss.config.cjs`: `tailwindcss` (v3 PostCSS plugin, inline config),`autoprefixer`, and
    `postcss-prefixwrap` configured with the selector `#plugin--defminer` — this string must equal
    the mount element id in `src/index.ts` exactly.
    `tailwind.config.ts`: `corePlugins: { preflight: false }`,
    `darkMode: ["selector", '[data-mode="dark"]']`, plugins `tailwindcss-primeui` and
    `@caido/tailwindcss`, content globs over `src/**/*.{vue,ts}`.

    `src/index.ts` exports `init(sdk)` which: creates a `div`, sets its `id` to the prefixwrap
    selector value without the leading `#`, calls `createApp(App).use(createPinia()).use(PrimeVue,
    { unstyled: true, pt: Classic }).provide("sdk", sdk).mount(div)`, then
    `sdk.navigation.addPage("/defminer", { body: div })` and
    `sdk.sidebar.registerItem("DefMiner", "/defminer")`. `addPage` takes an `HTMLElement`, not a
    component (research P-03) — passing a component is the failure this task exists to rule out.
    `registerItem`'s `icon` option is omitted deliberately.

    `App.vue` renders the three regions of `05-UI-SPEC.md § Data & Interaction Contract`: a toolbar
    at the `2xl` scale value (48px), a tab strip, and a split body. The tab strip is driven by a
    frozen array of `{ id, label }` declared in `App.vue` with the entries `artifacts`
    (label `Artifacts`), `observations` (label `Observations`), `health` (label `Health`) and
    `settings` (label `Settings`); it renders every entry on first paint with no count, wraps to a
    second row via flex wrapping, and never renders a horizontal scroll container or an overflow
    menu. The active tab carries the accent token `primary-500`; every other tab is surface-toned.
    Only the `artifacts` tab has a body in this task: it calls `sdk.backend.getArtifacts()` on
    mount and renders each returned row as text — `sha256`, `byte_len`, `kind`, `last_seen_at` —
    with `sha256` in the `font-mono` utility class because it is target-derived. Rows render at the
    `xl` scale value (32px) with `white-space: pre` and `overflow: hidden`. The other three tab
    bodies render the DefMiner-authored empty-state heading from `05-UI-SPEC.md § Copywriting
    Contract`; they gain real bodies in plans 05-09, 05-10 and 05-12 and this task must not
    pre-empt those.

    Zero hex literals in `App.vue` and `styles/index.css`: colours come only from the six
    `@caido/tailwindcss` roles. No `font-family` declaration. Spacing uses only the seven scale
    values.

    `packages/caido.config.ts`: add a second entry to `plugins`,
    `{ kind: "frontend", id: "defminer-frontend", name: "DefMiner", root: "frontend" }`. `root` is
    `"frontend"`, never `"."` — the header's `@caido-community/dev@0.1.7` dist-deletion constraint
    applies to this entry too. The config schema is a `z.strictObject`; add no other key.

    Root `package.json`: rename the build script to `build` running `caido-dev build packages` and
    keep `build:backend` as an alias so `pretest` is unchanged. Root `tsconfig.json` and
    `tsconfig.eslint.json` gain the frontend project so `pnpm typecheck` and the typed lint rules
    cover it; the frontend's own typecheck runs through `vue-tsc`.

    `src/App.spec.ts` and `src/index.spec.ts` both carry the docblock
    `// @vitest-environment jsdom` on their first line — vitest 4 removed `environmentMatchGlobs`
    and this repo forbids a `projects` key (decision P2-D4). `App.spec.ts` mounts `App` with a stub
    sdk whose `backend.getArtifacts()` resolves two fixed rows and asserts both are rendered as
    text. `index.spec.ts` calls `init` with a stub sdk and asserts `addPage` received an object
    whose `body` is an `HTMLElement` carrying the prefixwrap id, and that `registerItem` was called
    with the path `/defminer`.
  </action>
  <verify>
    <automated>pnpm exec caido-dev build packages && pnpm vitest run packages/frontend/src/App.spec.ts packages/frontend/src/index.spec.ts --reporter=dot && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/dist/` (or the configured package output) contains a frontend bundle after `pnpm exec caido-dev build packages` exits 0.
    - `packages/frontend/src/index.spec.ts` asserts `addPage`'s `body` argument satisfies `instanceof HTMLElement` and that its `id` string-equals the selector in `postcss.config.cjs` with the leading `#` removed; the assertion reads the PostCSS config value rather than restating the literal.
    - `packages/frontend/src/App.spec.ts` asserts the rendered text of the artifacts tab body contains both stub rows' `sha256` values, and that four tab labels are present on first paint before any promise resolves.
    - `node -e "const p=require('./packages/frontend/package.json');const d={...p.dependencies,...p.devDependencies};for(const [k,v] of Object.entries(d)){if(/^[\^~]/.test(v)&&/^(primevue|tailwindcss|@caido\/primevue|@caido\/tailwindcss|postcss-prefixwrap|vue-virtual-scroller|pinia|@vueuse\/core)$/.test(k)){console.error(k,v);process.exit(1)}}"` exits 0.
    - `grep -rniE '#[0-9a-f]{3,8}\b' packages/frontend/src --include=*.vue --include=*.css | grep -v 'plugin--defminer'` prints nothing.
    - `node -e "const p=require('./packages/frontend/package.json');if(JSON.stringify(p).includes('@defminer/backend'))process.exit(1)"` exits 0.
    - `packages/caido.config.ts` `plugins` array has length 2 and the second entry's `root` is `"frontend"`.
  </acceptance_criteria>
  <done>The operator can open the DefMiner sidebar page and see real artifact rows from the shipped database, and that path is committed.</done>
  <reversibility rating="reversible">A frontend package can be deleted; nothing durable is written and no Caido-side state is created.</reversibility>
</task>

<task type="auto">
  <name>Task 2: The two build-output gates — CSS containment and externals</name>
  <files>scripts/ci/prefixwrap.mjs, scripts/ci/prefixwrap.spec.ts, scripts/ci/frontend-externals.mjs, scripts/ci/frontend-externals.spec.ts, package.json</files>
  <read_first>
    - scripts/ci/check-bundle-imports.mjs — the whole gate, including how it parses the built bundle with acorn and what it prints on failure
    - scripts/ci/check-bundle-imports.spec.ts lines 1-45 — the subprocess-invocation pattern and the header explaining why the spec asserts on exit code and stdout rather than on an internal
    - packages/frontend/vite.config.ts — as written in task 1, for the external list the gate must mirror
    - .planning/phases/05-workspace-operator-workflow/05-PATTERNS.md § "scripts/ci/prefixwrap.spec.ts and frontend-externals.spec.ts"
  </read_first>
  <action>
    Two standalone Node gates plus two specs, following `check-bundle-imports` exactly: the gate is
    a `.mjs` run as a subprocess against a **built artifact**, and the spec asserts on its exit code
    and printed output against deliberately-broken fixtures, never on an internal.

    `scripts/ci/prefixwrap.mjs` takes a CSS file path, parses it, and exits non-zero if any rule's
    selector list contains a selector that is not scoped under `#plugin--defminer`. It must also
    exit non-zero when the target file is absent or contains zero rules — a gate that cannot find
    its target, or that finds nothing to check, must never report success. Print the offending
    selectors.

    `scripts/ci/frontend-externals.mjs` takes the built frontend bundle path, parses it with the
    already-installed `acorn` the way `check-bundle-imports.mjs` does, collects every import
    specifier, and exits non-zero if `vue`, `@caido/frontend-sdk`, or any `@codemirror/` or
    `@lezer/` specifier is absent from the import set — because absent means bundled. It must also
    exit non-zero on a missing or empty bundle.

    Both specs drive their gate as a subprocess with `spawnSync(process.execPath, [GATE, arg])` and
    cover, for each gate: a passing fixture, a violating fixture, and the missing-file fixture. Each
    assertion checks both `status` and a substring of `stdout`/`stderr` so a gate that exits
    non-zero for the wrong reason is distinguishable from one that caught the real defect.

    Add `check:css` and `check:externals` scripts to root `package.json` pointing at the two gates
    with the real built paths.
  </action>
  <verify>
    <automated>pnpm vitest run scripts/ci/prefixwrap.spec.ts scripts/ci/frontend-externals.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run scripts/ci/prefixwrap.spec.ts` reports at least three test cases: pass, violation, missing target.
    - `pnpm vitest run scripts/ci/frontend-externals.spec.ts` reports at least three test cases: pass, a bundle with an inlined Vue (no `vue` import specifier), missing bundle.
    - Running `node scripts/ci/prefixwrap.mjs /nonexistent.css` exits non-zero and prints a message naming the missing path.
    - Running `node scripts/ci/frontend-externals.mjs` against the real built frontend bundle exits 0.
    - Both specs assert on `status` AND on printed output in every case; no case asserts on `status` alone.
  </acceptance_criteria>
  <done>CSS containment and externals are asserted on build output, with each gate's failing path executed against a fixture.</done>
</task>

<task type="auto">
  <name>Task 3: Arm the lint rules R1 names, and wire the test toolchain for single-file components</name>
  <files>eslint.config.js, .browserslistrc, vitest.config.ts, knip.json, package.json</files>
  <read_first>
    - eslint.config.js — the whole file, especially lines 12-23 where `vue: false` and `compat: false` are annotated "Re-enable in Phase 5" with their reasons
    - vitest.config.ts — the whole file, including the P2-D4 note forbidding a `projects` or `workspace` key
    - knip.json lines 45-85 — the `ignoreExportsUsedInFile: true` hole and its note "should be revisited when the frontend in Phase 5 gives most of these types a cross-module consumer"
    - .planning/phases/05-workspace-operator-workflow/05-RESEARCH.md § "Common Pitfalls" P-01, P-02 and P-10
    - .planning/phases/05-workspace-operator-workflow/05-UI-SPEC.md § "Rendering Safety Contract" R1
  </read_first>
  <action>
    Turn `defaultConfig({ vue: false, node: true, compat: false })` into
    `defaultConfig({ vue: true, node: true, compat: true })` and replace the two deferral comments
    with what actually happened, in the file's existing voice. `compat: true` requires a
    browserslist or `eslint-plugin-compat` falls back to `op_mini all` and reports `Promise` and
    `URL` unsupported — create `.browserslistrc` targeting the Chromium version Caido's renderer
    ships, stated as a single `chrome >= N` line with the N justified in a comment.

    Then add a config block scoped to `packages/frontend/**` that sets, as errors:
    `vue/no-v-html`, `no-eval`, `no-new-func`, `no-implied-eval`, `no-script-url`. The measurement
    behind this is that `eslint-plugin-vue@10.6.0`'s `flat/recommended`, which
    `@caido/eslint-config@0.10.0` applies, sets `vue/no-v-html` to `warn`, and that
    `eslint-plugin-no-unsanitized`'s recommended config covers `innerHTML` / `outerHTML` /
    `insertAdjacentHTML` / `document.write` but covers neither dynamic-code-construction form.
    Set `linterOptions: { noInlineConfig: true }` on that same block so a per-line disable comment
    cannot re-enable any of them. Record in the block's comment that lint is the weaker of two
    controls and that the static gate in plan 05-05 is the stronger one, for the reason
    `sql-discipline.spec.ts`'s header already states about behavioural tests.

    `vitest.config.ts`: add `plugins: [vue()]` importing from `@vitejs/plugin-vue`, at the
    `defineConfig` top level. Add NO `projects` and NO `workspace` key — five files resolve the
    Phase 0 results directory from a bare relative literal and a per-project root breaks all five.
    Leave `include` unchanged: `packages/*/src/**/*.spec.ts` already matches
    `packages/frontend/src/**`.

    Install `jsdom` and `@vue/test-utils` as root devDependencies via `pnpm add -Dw`.

    `knip.json`: add `packages/frontend` to the workspace entry map with its own `entry` and
    `project` globs. Then discharge the Phase 1 note by setting `ignoreExportsUsedInFile` to
    `false` and fixing whatever it then reports — the sixteen types it was hiding now have
    cross-module consumers, which is exactly the condition the note named. If any report remains
    that is a genuine single-file type, state each one by name in the summary rather than
    reinstating the flag wholesale.
  </action>
  <verify>
    <automated>pnpm lint && pnpm knip && pnpm vitest run packages/frontend/src/App.spec.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const {defaultConfig}=require('@caido/eslint-config');" ` is not required; instead: running `pnpm exec eslint --print-config packages/frontend/src/App.vue` prints `"vue/no-v-html": ["error"]` (or severity `2`), and prints severity `2` for each of `no-eval`, `no-new-func`, `no-implied-eval`, `no-script-url`.
    - `pnpm exec eslint --print-config packages/frontend/src/App.vue` shows `noInlineConfig` true.
    - `eslint.config.js` no longer contains the string `Re-enable it in Phase 5`.
    - `.browserslistrc` exists and contains exactly one target line plus comments.
    - `node -e "const k=require('./knip.json');if(k.ignoreExportsUsedInFile!==false)process.exit(1)"` exits 0, OR the summary names every export that forced an exception.
    - `pnpm knip` exits 0.
    - `grep -n 'projects\|workspace' vitest.config.ts` returns only comment lines, no config key.
  </acceptance_criteria>
  <done>Rendering-safety R1's lint half is an error that cannot be disabled inline, and `.vue` files compile under vitest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| target host -> `artifacts.sha256` / `kind` -> rendered table text | Values derived from bytes served by a possibly-hostile host cross into the DOM here for the first time in this project |
| DefMiner CSS -> Caido host document and every other installed plugin | An unwrapped rule escapes the plugin and restyles the operator's tool |
| npm registry -> `packages/frontend/node_modules` -> shipped bundle | Seventeen new packages enter the shipped artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | `App.vue` artifact row rendering | high | mitigate | Text interpolation only; `vue/no-v-html` at error with `noInlineConfig` (task 3); the static AST gate in plan 05-05 is the non-disableable half |
| T-05-02 | Tampering | built frontend CSS | medium | mitigate | `postcss-prefixwrap` at `#plugin--defminer`, asserted on the build output by `scripts/ci/prefixwrap.mjs` including its missing-file path (task 2) |
| T-05-03 | Elevation of Privilege | rollup externals | high | mitigate | A bundled duplicate Vue gives DefMiner a second reactivity runtime inside Caido's page; `scripts/ci/frontend-externals.mjs` fails the build output when an external is absent from the import set (task 2) |
| T-05-04 | Information Disclosure | `sha256` rendered in a table cell | low | accept | A content digest is not a secret and is already returned by the shipped `getArtifacts` RPC; the mandatory `font-mono` rule still applies because it is target-derived |
| T-05-SC | Tampering | pnpm installs (17 packages) | high | mitigate | `05-RESEARCH.md § Package Legitimacy Audit` (17 packages, npm, 2026-08-28, zero `[SLOP]`) plus the blocking operator checkpoint in task 0 for the two genuinely low-download packages; exact pins plus the `pnpm-workspace.yaml` DIST-06 overrides |
</threat_model>

<verification>
- `pnpm exec caido-dev build packages` exits 0 and produces both a backend and a frontend bundle.
- `pnpm test && pnpm lint && pnpm typecheck && pnpm knip && pnpm check:bundle` all exit 0.
- `node scripts/ci/prefixwrap.mjs <built css>` and `node scripts/ci/frontend-externals.mjs <built bundle>` both exit 0.
- The pre-existing Phase 0 exit gates in `tests/**` still run — `pnpm test` reports no drop in file count against `01-45-SUMMARY.md`'s recorded baseline.
</verification>

<success_criteria>
- A sidebar item opens a page rendering real rows from the shipped `artifacts` table.
- CSS containment and externals are proven against build output, each gate's failure path executed.
- `vue/no-v-html`, `no-eval`, `no-new-func`, `no-implied-eval` and `no-script-url` are errors in the frontend and cannot be disabled inline.
- The knip `ignoreExportsUsedInFile` hole opened in Phase 1 for this phase is closed or every exception is named.
</success_criteria>

## Artifacts this plan produces

**New files:** `packages/frontend/package.json`, `packages/frontend/tsconfig.json`,
`packages/frontend/vite.config.ts`, `packages/frontend/postcss.config.cjs`,
`packages/frontend/tailwind.config.ts`, `packages/frontend/src/index.ts`,
`packages/frontend/src/index.spec.ts`, `packages/frontend/src/App.vue`,
`packages/frontend/src/App.spec.ts`, `packages/frontend/src/styles/index.css`,
`.browserslistrc`, `scripts/ci/prefixwrap.mjs`, `scripts/ci/prefixwrap.spec.ts`,
`scripts/ci/frontend-externals.mjs`, `scripts/ci/frontend-externals.spec.ts`

**New symbols:** `init` (frontend entry, `packages/frontend/src/index.ts`), the `App` single-file
component, the frozen tab list constant in `App.vue`

**New package name:** `@defminer/frontend`

**New npm scripts:** `build`, `check:css`, `check:externals`

**New config entries:** `packages/caido.config.ts` `plugins[1]` = `{ kind: "frontend", id: "defminer-frontend", name: "DefMiner", root: "frontend" }`; `eslint.config.js` frontend block; `vitest.config.ts` `plugins`; `knip.json` `packages/frontend` workspace entry

**New dependencies:** `vue`, `primevue@4.1.0`, `@caido/primevue@0.3.3`, `@caido/tailwindcss@0.1.0`,
`tailwindcss@3.4.13`, `tailwindcss-primeui@0.6.1`, `postcss-prefixwrap@1.57.2`,
`vue-virtual-scroller@2.0.0-beta.8`, `pinia@3.0.4`, `@vueuse/core@14.2.0`,
`@vitejs/plugin-vue@6.0.1`, `vue-tsc@3.2.4`, `postcss`, `autoprefixer`, `@caido/sdk-frontend`,
`jsdom`, `@vue/test-utils`

## Multi-Source Coverage Audit (whole Phase 05 plan set)

One audit for the twelve-plan set, recorded in the lead plan. Scope is bounded by **D-05**, which
splits the phase rather than re-sequencing it. Deferred items are covered by an explicit, reasoned
deferral in plan 05-04's deferral register — they are COVERED as deferrals, not MISSING.

### GOAL — ROADMAP Phase 5 success criteria

| # | Criterion | Covered by |
|---|-----------|-----------|
| 1 | A sidebar page answers "every secret, endpoint, and host on this target" | 05-01, 05-09 (over the shipped tables; entity classes land in the deferred pass) |
| 2 | Tables stay responsive at 10,000 rows; event coalescing proven under load | 05-07, 05-08, 05-09 |
| 3 | Every entity links back to source request, artifact version and byte offsets, and shows which signals fired | 05-10 (frame, artifact version) + 05-04 deferral (byte offsets, signals — blocked on Phase 4 evidence and Phase 3 vocabulary) |
| 4 | Findings marked reviewed / false positive / accepted, surviving a corpus bump | 05-04 deferral (blocked on Phase 4 entity identity); write discipline designed in 05-04, audit substrate in 05-06 |
| 5 | A suppression rule stops a benign pattern reappearing without editing the corpus | 05-04 deferral (query-time mechanism decided; key blocked on Phase 4) |
| 6 | No extracted content rendered as markup; a hostile fixture renders inert and exports safely | 05-03, 05-05, 05-09, 05-11 |
| 7 | Native Findings only for high-signal, stable dedupe keys; entropy-only never projects | 05-04 deferral (blocked on the Phase 3/4 tier); D-01/D-02/D-03 carried forward verbatim |
| 8 | Degraded and partial analyses are visibly marked | 05-09, 05-10, 05-11 (embedded floor statement) |

### REQ — phase requirement ids

| ID | Covered by | Note |
|----|-----------|------|
| UI-01 | 05-01 | |
| UI-02 | 05-02, 05-07, 05-08, 05-09 | |
| UI-03 | 05-10 (frame) + 05-04 (deferral) | byte offsets blocked on Phase 4 plan 04-03 |
| UI-04 | 05-04 (deferral + published frame) | signal vocabulary blocked on Phase 3 plan 03-03 |
| UI-06 | 05-02, 05-11 | |
| UI-07 | 05-07, 05-08 | |
| UI-08 | 05-12 | |
| UI-09 | 05-09 | |
| OPS-01 | 05-04 (deferral) | blocked on Phase 4 entity identity |
| OPS-02 | 05-04 (deferral) | mechanism decided; key blocked |
| OPS-03 | 05-10 | fully built |
| OPS-04 | 05-04 (deferral) | blocked on the same identity |
| UISEC-01 | 05-01, 05-05, 05-09 | |
| UISEC-02 | 05-03, 05-11 | |
| UISEC-03 | 05-03, 05-05, 05-09 | |
| FIND-01 | 05-04 (deferral) | blocked on the Phase 3/4 tier |
| FIND-02 | 05-04 (deferral) | satisfied structurally once the tier exists |
| STORE-08 | 05-06 | `audit` only; the other two tables are Phase 4's |

### RESEARCH — features and constraints from 05-RESEARCH.md

| Item | Covered by |
|------|-----------|
| O-01 — fixed literal matrix, row-value cursor, bounded candidate window | 05-02 (gate + measurement), 05-07 (implementation) |
| O-01 — four measured blind spots in the SQL gate | 05-02 task 1 |
| O-01 — typed, versioned RPC contract; current spec helper, not the deprecated pair | 05-07 task 2 |
| O-02 — per-plan buildability and the upward contract | 05-04 |
| O-03 — audit shape, natural key, closed kind vocabulary | 05-06 |
| O-03 — suppression at query time; triage key as proposal | 05-04 deferral register |
| O-04 — the four Finding sanitisation rules and the five SDK facts | 05-04 deferral register |
| O-05 — projection warns rather than blocks | 05-04 deferral register (operator declined the block) |
| O-06 — the tier reduces to a conjunction; the checksum upward requirement on plan 03-01 | 05-04 task 2 |
| P-01 / P-02 — lint severity and the uncovered dynamic-code forms | 05-01 task 3, 05-05 task 1 |
| P-03 — the page takes an element, not a component | 05-01 task 1 |
| P-04 — the subscription handle must be stopped | 05-08 tasks 1, 3 |
| P-05 — each create individually caught | 05-04 deferral register |
| P-06 — a raw export cannot un-redact what was never stored | 05-11 tasks 1, 3 |
| P-07 — a frozen page is usually the backend | 05-09 error copy, 05-12 health |
| P-08 — duplicate registration rejected | 05-07 task 3, 05-10 task 1, 05-11 task 2, 05-12 task 1 |
| P-09 — adding a table is a two-place, operator-visible edit | 05-06 tasks 0, 2 |
| P-10 — the knip hole left open for this phase | 05-01 task 3 |
| Open question 1 — the suppressions-list bound | 05-04 task 2 (stated bound, as a proposal) |
| Open question 3 — export payload across the boundary | 05-02 task 3, 05-11 |
| Open question 4 — FLAG F1, the evidence-panel frame | 05-04 task 1 |
| Package legitimacy audit | 05-01 task 0 |
| Wave 0 list from 05-VALIDATION.md | 05-01, 05-02, 05-03, 05-05, 05-06 |

### CONTEXT — locked decisions

| ID | Covered by |
|----|-----------|
| D-01 | 05-04 (carried forward verbatim with its one-way rating; unspent, owed to the implementing pass) |
| D-02 | 05-04 |
| D-03 | 05-04 |
| D-04 | 05-02 (measurement), 05-11 (implementation) |
| D-05 | 05-04, and this audit's scope |
| D-06 | 05-06 task 3 |
| D-07 | 05-11 task 3 |

**Exclusions, not gaps:** CONTEXT.md `## Deferred Ideas` (hosted-file delivery, per-projection body
re-verification, cross-deploy diffing); UI-05, FIND-03/04 and the deployment requirements, which the
ROADMAP assigns to Phases 6 and 7.

**Result: no MISSING item.** Every GOAL, REQ, RESEARCH and CONTEXT item is either implemented in a
plan or covered by a reasoned deferral in 05-04's register naming its blocker and its unblocking
phase and plan.

<output>
Create `.planning/phases/05-workspace-operator-workflow/05-01-SUMMARY.md` when done.
</output>
